# Frozen expectations — WHERE EACH NUMBER CAME FROM

**STATUS: REGISTER (opened 14z-128). One row per file directly under
`tests/expected/`. Completeness is enforced both ways by
`tests/test_expectation_provenance.sh`: a file with no row fails, a row naming
a file that is gone fails.**

## Why this exists

The maintainer's rule for adjudicating a red gate (2026-09-02):

> *"to know if we should fix the gate or what it caught, we must use data we
> can trust, and that means measuring or relying on data that is known to be
> true for it was vetted by measurements."*

**A RED GATE IS A QUESTION, NOT AN ANSWER**, and the first thing the question
needs is which side rests on a measurement. Measured at the 14z-127 close: 30
of 45 frozen expectation files declare their provenance somewhere; 15 do not —
and several of those have it in a gate header or a STATE entry, which is not
where a triage is looking. **The file is what a triage opens.** This page is
that answer, kept beside the files.

The worked warning is 14z-127's own: `test_don_reactions.sh` was GREEN on
`native == 10`, a constant of PLAYTEST TESTIMONY presented as a measurement. It
happened to be correct. That is luck, not method.

## How to read a row

- **subject** — what the numbers describe. THE DISCRIMINATOR FOR RELEASE SCOPE
  is this column, not the romsets the gate touches (`tests/ci_emulator.tsv`).
- **rests on** — the class of evidence:
  - `in-emulator (reference)` — produced by running a REFERENCE and reading
    its own state: native `vsav2`, pristine `vsavj`, or `vhunt2`. The
    strongest thing here, because the number is what the reference DOES.
  - `in-emulator (ours)` — produced by running OUR build. This locks
    CURRENCY, not correctness: it says nothing has changed since the freeze,
    and nothing about whether the value is right.
    **THE PRECEDENCE OF REFERENCES (maintainer, 2026-09-04, 14z-132), which
    is why the two are not one class:** an explicit ruling that enforces a
    SPECIFIC disregard of the rules (the #112 black foot, the ±1 damage on
    Phobos's throws, the Donovan tick difference) > anything **vsavj** (the
    vanilla assets and the common engine) > **vs2** (whatever pertains to
    Donovan, Pyron and Phobos that the vsavj engine does not cover). **A
    build of OURS — solo or merged — is nowhere in that order.** A solo
    build agreeing with a frozen number demonstrates that the build was
    possible, not that the number is a reference; so if our build and the
    frozen file ever disagree, the arbiter is the hierarchy above, never the
    other build. An `in-emulator (ours)` row is therefore a candidate for
    re-anchoring, not a finished measurement.
  - `derived` — computed from ROM tables by a tool. Only as good as the
    reader; both 14z-125 defects were interpretation defects, and a
    regenerated page would have reproduced them.
  - `static` — read off the image or the tree without running anything.
  - `hash-lock` — a digest of generator output. Locks CURRENCY, never
    correctness ([VSP-12]).
  - `registry` — a ledger of decisions, not a measurement.
- **re-freeze** — the invocation that legitimately regenerates it. A file with
  no re-freeze path is hand-maintained, and changing it is a decision.

| file | owner gate | subject | rests on | re-freeze | since |
|---|---|---|---|---|---|
| `advancing_guard.txt` | `test_advancing_guard.sh` | the advancing guard (guard push) on NATIVE vs2 and on vsavj — the engine's own mechanic, both games | in-emulator (reference) | `FREEZE=1 tests/test_advancing_guard.sh` | 14z-123 |
| `charmap_pages.sha256` | `test_charmap_current.sh` | the six regenerated character pages under `../charpages/framedata` | hash-lock | regenerate with `tools/framedata_pages.sh`, then re-hash | 14z-126 |
| `community_crosscheck.txt` | `test_community_crosscheck.sh` | the VERDICT rows (EXACT / CONSTANT OFFSET / INCONSISTENT) of our vanilla frame data against the community workbook — never the workbook's values ([VSP-8]-adjacent, the 2026-08-31 privacy ruling) | derived, arbitrated in-emulator where it disagreed | `FREEZE=1 tests/test_community_crosscheck.sh` | 14z-125 |
| `df_accumulator.txt` | `audit_df_accumulator.sh` | `+0x161` as SASQUATCH's Dark Force armor — a vanilla character's engine behaviour | in-emulator (reference) | `FREEZE=1 tests/audit_df_accumulator.sh` | 14z-123 |
| `df_palette_seq_census.txt` | `audit_palette_seq_ids.sh` | which palette-seq ids each character requests in Dark Force, over the corpus | in-emulator (reference) | `CHARS="…" tests/audit_palette_seq_ids.sh` | 14z-118 |
| `df_startup_invuln.tsv` | `audit_df_startup_invuln.sh` | the DF startup invincibility window `+0x147`, per character, all 15 vanilla + the three tenants | in-emulator (reference + ours) | `FREEZE=1 tests/audit_df_startup_invuln.sh` | 14z-126 |
| `doc_anchor_census.tsv` | `test_doc_anchor_census.sh` | WHERE each skill anchor lives (file + section) | static | `python3 tools/doc_anchor_census.py --freeze` | 14z-126b |
| `escape_triage.txt` | `test_escape_triage.sh` | the H3.1 verdicts on uncovered word-form pc-relative escapes in the tenant builds | static | `FREEZE=1 tests/test_escape_triage.sh` | 14z-100 |
| `front_comparator.txt` | `audit_front_comparator.sh` | what `RAM:$FF8127` is and what its input byte `+0x10` is, on vanilla vsavj | in-emulator (reference) | `FREEZE=1 tests/audit_front_comparator.sh` | 14z-123 |
| `killshread_es.txt` | `test_killshread_es.sh` | the ES stance change's effect during the summon, on NATIVE vs2 — the maintainer's 14z-121 ruling, measured | in-emulator (reference) | `FREEZE=1 tests/test_killshread_es.sh` (re-frozen 14z-165 on P2 = Demitri) | 14z-121 **THE `stock` COLUMN IS NOT A MEASUREMENT OF THE ENGINE** (2026-09-22): the rig pokes `ff8509 := 09` before every event and the gate samples that same byte, so `stock` reads the poke back; [VSP-170]'s ES stock-drop discriminator is neutralised in this rig and the ES/normal distinction rests on the contact-wave counts instead. Re-frozen 14z-174 for that column ALONE (8 -> 9, the 14z-171 per-event top-up); move, frame, class, hp and white are unchanged. #171. |
| `ladder_tenant_vs_palette.txt` | `test_ladder_tenant_vs_palette.sh` | the arcade-ladder VS palette pool (`PRG:0x3A3CA0 + id*32`) for a TENANT opponent, read ON SCREEN | in-emulator (ours) | `FREEZE=1 tests/test_ladder_tenant_vs_palette.sh` | 14z-123 |
| `move_naming_donovan.txt` | `test_move_naming.sh` | which anim CHAIN each of Donovan's named moves enters, measured on NATIVE vs2 — the naming the whole character-data map is indexed by (P2 = Demitri since 14z-165) | in-emulator (reference) (since 14z-181 also the guard-cancel part — donovan_15 / pyron_7: Demitri's 5HP blocked, 623+button from hit+2, ten events, the kicks the negative rows, the no-block control a hit) | `FREEZE=1 TENANTS=donovan tests/test_move_naming.sh` | 14z-120 |
| `move_naming_huitzil.txt` | `test_move_naming.sh` | the same for Huitzil/Phobos | in-emulator (reference) | `FREEZE=1 TENANTS=huitzil tests/test_move_naming.sh` | 14z-120 |
| `move_naming_pyron.txt` | `test_move_naming.sh` | the same for Pyron | in-emulator (reference) (since 14z-181 also the guard-cancel part — donovan_15 / pyron_7: Demitri's 5HP blocked, 623+button from hit+2, ten events, the kicks the negative rows, the no-block control a hit) | `FREEZE=1 TENANTS=pyron tests/test_move_naming.sh` | 14z-120 |
| `forced_pick_fidelity.tsv` | `audit_forced_pick_fidelity.sh` | per tenant, the P1 fighter-block offsets a FORCED-PICK native leg leaves latched with another character's confirm (the rig's poked leg vs a real cursor pick on native `vsav2`), and the first in-play frame the latch changes anything else | in-emulator (reference) | `FREEZE=1 tests/audit_forced_pick_fidelity.sh` | 14z-160 |
| `move_parity_events.tsv` | `audit_move_parity.sh` | the per-EVENT verdict (IDENT / DIFF with its first frame and fields / VOID / NOT-IN-DF / DF-NOT-ENTERED) of every event of every tenant-move rig part, ours vs native `vsav2`, each event in its own window at a pinned speed level and RNG, both legs real cursor picks, the tenant's fields plus the meter fraction and P2's HP (GitHub #136; replaced the per-part `move_parity.tsv` at 14z-164; re-frozen 14z-165 on P2 = Demitri, his real route on both wheels — DECISIONS_HISTORY.md 14z-164b) | in-emulator (reference + ours) (MAME, 60 legs, 30 parts) | `ROMDIR=... ALL=1 FREEZE=1 tests/audit_move_parity.sh` then a verify run | 14z-164 |
| `palette_seq_ids_corpus.txt` | `audit_palette_seq_ids.sh` | which palette-seq ids LEGACY ever requests, over the replay corpus — the deadness evidence a tenant block rests on ([VSP-22], [VSP-151]) | in-emulator (reference) | `tests/audit_palette_seq_ids.sh` | 14z-79b |
| `projectile_census.txt` | `test_projectile_census.sh` | which `$FF9400`-pool types each TENANT's moves spawn, replayed on native vs2 | in-emulator (reference) | `FREEZE=1 tests/test_projectile_census.sh` (re-frozen 14z-160 on real cursor picks, GitHub #151; re-frozen 14z-165 on P2 = Demitri) | 14z-120 |
| `projectile_params.txt` | `test_projectile_params.sh` | each projectile type's inline parameter tables, decoded from the type HANDLER and confirmed on the live spawn | derived + in-emulator confirmation | `FREEZE=1 tests/test_projectile_params.sh` | 14z-121 |
| `reactions_donovan.txt` | `test_reactions.sh` | which chains Donovan runs AS THE VICTIM per reaction class, and how long, measured on NATIVE vs2 (the ONLY leg this gate runs; our build supplies the extract the chain DECODER reads, nothing more) | in-emulator (reference) | `FREEZE=1 TENANTS=donovan tests/test_reactions.sh` | 14z-120 |
| `reactions_huitzil.txt` | `test_reactions.sh` | the same for Huitzil/Phobos | in-emulator (reference) | `FREEZE=1 TENANTS=huitzil tests/test_reactions.sh` | 14z-120 |
| `reactions_pyron.txt` | `test_reactions.sh` | the same for Pyron | in-emulator (reference) | `FREEZE=1 TENANTS=pyron tests/test_reactions.sh` | 14z-120 |
| `victim_parity.tsv` | `audit_victim_parity.sh` | the tenant AS THE VICTIM, ours (merged-m19) vs native vs2, per contact of the three victim rigs (58 rows): the reaction class, freeze, translated chain path and frames to the stand — SAME on 41; of the 17 DIFFER rows the timing differences (len, frz) of the throw are charged to Victor's own differing throw-start chain a2:0x38 (25 vs 24 data frames; five of six hits a frame earlier on ours: 8 rows attacker, 2 attacker+tenant), and Pyron's heavy-hit chain (b:0x19 native, b:0x03 ours: two rows outside the throw, seven inside it) is charged to the tenant — open as #173; the tenant's chains asserted to decode to the same shapes from our build as from the vs2 extract; the `poked` rows record the native leg reproducing test_reactions' frozen lines | in-emulator (reference + ours) — MAME, field_trace, both legs REAL cursor picks from each game's decoded wheel, the speed level pinned at 8 (vs2's TURBO, the rigs' native tuning; our build's own is 6) and the RNG at 0000 on both, one P2 HP pin per event and none inside a compared window (the reader refuses one), P1 asserted Victor and never in a differing reaction chain, our P2 node pointers translated through placements.json | `FREEZE=1 tests/audit_victim_parity.sh` | 14z-181 |
| `hitbox_parity.tsv` | `audit_hitbox_parity.sh` | the hitboxes in play, ours (merged-m19) vs native vs2: on every frame where both legs are on the same node (translated) of donovan_1, pyron_4 and huitzil_1, the five resolved table pointers +0x80..+0x90, the hitbox base +0x60 and family table +0x64 (translated out of every placed region of the tenant) and the node's box-id word +0x94 — SAME on every field over 14,890 node-equal frames (donovan_1 4331/4331, pyron_4 3928/4691, huitzil_1 6631/6631) | in-emulator (reference + ours) — MAME, field_trace, both legs real cursor picks, the level pinned at 6 and the RNG at 0000 as audit_move_parity runs them, frames whose nodes differ counted and not compared (the parity gate's DIFF rows, attributed there) | `FREEZE=1 tests/audit_hitbox_parity.sh` | 14z-181 |
| `registry.tsv` | `tools/build_fingerprint.py` (+ `run_suite.sh`) | which program fingerprint maps to which expectation set — the auto-detecting runner's dispatch | registry | rows are added AT FREEZE TIME as a decision recorded in STATE, never by a tool | M2a |
| `vanilla_hit_damage.sha256` | `test_vanilla_frame_join.sh` | the regenerated per-move vanilla hit/damage table, which lives OUT of the tree | hash-lock | regenerate under `ROMDIR`, then re-hash | 14z-126 |
| `vanilla_meter_gain.sha256` | `test_meter_gain.sh` | the regenerated per-move vanilla METER table (the swing cost and the on-hit steps per connecting normal), which lives OUT of the tree | hash-lock | `FREEZE=1 ROMDIR=... tests/test_meter_gain.sh` | 14z-146 |
| `vanilla_normal_slots.tsv` | `test_vanilla_frame_join.sh` | which anim chain each VANILLA character's standing normals enter at two distances, measured on vsavj — the JOIN that overturned a fitted even/odd model | in-emulator (reference) | `FREEZE=1 tests/test_vanilla_frame_join.sh` | 14z-125 |
| `vanilla_aerial_slots.tsv` | `test_vanilla_aerial_join.sh` | which anim chain each VANILLA character's jumping normals enter from a NEUTRAL jump (U), a FORWARD jump (UR), a neutral jump then D+button, and a forward jump then D+button (14z-146, Anakaris's J.2K), measured on vsavj — the aerial JOIN: a2 0x12-0x17 is the neutral set and 0x18-0x1D the second set (forward-jump for most characters, Zabel's D+button set) | in-emulator (reference) | `FREEZE=1 tests/test_vanilla_aerial_join.sh` | 14z-145 |
| `expect/mister_prg_window.txt` | `test_mister_prg_window.sh` | the PRG-window probe's frozen PAIR: the profile-ON and profile-OFF legs' last per-frame report — program-ROM reads above/below `$400000`, the FIRST extension address the 68k executes and its frame — on the merged build | in-emulator (ours) — and it FOLLOWS THE ROMSET: the first executed extension address is the relocated OBJ walker's PLACEMENT, which the hole allocator moves whenever a freeze adds extension content before it (14z-134: frozen on merged-m10, stale from merged-m11, caught only by the M16 release run because the gate was `bitstream` cadence — now `romset`) | `ROMDIR=... tests/test_mister_prg_window.sh --freeze` (two Verilator legs, ~110 min); re-freeze at EVERY romset freeze | 14z-107 (11); re-frozen 14z-134 on merged-m16 |
| `expect/cps2w_rtl_delta.txt` | `test_mister_wide_gate.sh` | the frozen line-by-line delta of every override file in `cores/cps2w/hdl` against its reference-core sibling — the MiSTer edition of the emulator change budget | static (the fork pin) | no `--freeze`: regenerate from the gate's own `delta.txt` when a SLICE is ratified — a moved delta is a fork change, never a re-freeze | 14z-107 (6) |
| `expect/cps2w_game_yaml_delta.txt` | `test_jtcores_twin.sh` | the frozen diff between `cps2w`'s and `cps2`'s `game.yaml` file lists (files out + files in, [MSC-2]) | static (the fork pin) | no `--freeze`: regenerate when a slice changes the file list — a moved list is a fork change | 14z-107 (9) |

## What this page does NOT cover

The 43 DIRECTORIES under `tests/expected/` are the per-build expectation sets
(`donovan-m18/`, `merged1/`, `vsavj/`, …). Their provenance is
`tests/expected/registry.tsv` — one row per frozen build, fingerprint to
expectation set — plus the `freeze/<name>` git tag at the commit that froze it
([VSP-94]). A set is regenerated only by `run_suite.sh --freeze`, at a freeze,
as a decision recorded in STATE.

## The honest limit

A row here says HOW a file was produced, not that it is right. `in-emulator`
is the strongest class in this project and it is still only as good as the rig
that produced it — every entry in `[VSP-119]`..`[VSP-152]` is a way one of
these rigs has lied. What the row buys is the first question of a triage,
answered in seconds instead of an archaeology session.
| `checkdocs_rom_covered.tsv` | `test_checkdocs_rom.sh` | the atlas ROM-tier addresses each L3 check actually READS from the decrypted image — the shrink-only half of a NOTE-class coverage number. A check that stops reaching an address, or disappears, FAILS; growth is expected and re-freezes. Compared as a MULTISET, so a hand-added duplicate row fails too (the 14z-141 `audit_rule5.py` defect, same shape) | derived (the addresses are read from the image at run time by the checks themselves, never transcribed by hand; only the atlas's own named addresses become rows, so the file is coverage and not a byte dump) | `ROMDIR=... python3 tools/checkdocs_rom.py --freeze` | 14z-142 |
| `rule5_baked.tsv` | `test_rule5_census.sh` | the BAKED inventory of the rule-5 census — every GAMEPLAY and CODE value that exists only in a manifest row or a generator constant, frozen so it can only SHRINK. `fact` is deliberately NOT frozen (ordinary port work adds addresses and hex every session, so a shrink-only signal over it would fire on every commit and be answered by a reflexive re-freeze); it is reported as a NOTE-class number instead | static (the tree itself — the manifests and `tools/*.py`, no ROM and no emulator) | `python3 tools/audit_rule5.py --freeze tests/expected/rule5_baked.tsv`; it REFUSES to write a larger inventory without `--allow-growth "<reason>"`, and the reason is written into the file | 14z-141 |
| `must_fire_census.tsv` | `test_must_fire_census.sh` | the gates that DECLARE a must-fire control under the R10 grammar (`declares`, grows only), those whose control prints no `CONTROL FIRED:`/`DEAD:` line (`header-only`, shrinks only) and those still mentioning a control in a pre-grammar spelling (`retrofit-debt`, shrinks only) — the doctrine machine-read, `docs/project/must_fire_contract.md` | static | `FREEZE=1 tests/test_must_fire_census.sh` after reviewing the diff | 14z-145; the classes 14z-147 |
| `latch_readers.tsv` | `test_latch_readers.sh` | every instruction operand naming a fighter block's select-confirm latch offset (`+0x3BC`/`+0x3BD`/`+0x3C2`/`+0x3E0`/`+0x3E3`, in every 68000 addressing form) on the vsav2 and vsavj opcode views and the ported image — the whole population of readers a forced-pick leg's latch can reach (GitHub #151 step 3) | static (the decrypted views; the ported image is `build/m3b_merged26/verify_op.bin`; positive controls on the documented sites) | `FREEZE=1 tests/test_latch_readers.sh` | 14z-161 |
| `latch_reads.tsv` | `audit_latch_reads.sh` | per forced-pick leg shape, the in-play readers of the latch bytes with the VALUE each saw — Phobos real / over Donovan (01) / over Demitri (00), Donovan over Demitri and as a victim (none), Pyron (none), the WIDE build's Phobos (the shim's 00) | in-emulator (reference + ours) — MAME, native `vsav2` and the WIDE build, frames ≥ 2000 | `FREEZE=1 tests/audit_latch_reads.sh` | 14z-161 |
| `throw_registration.tsv` | `audit_throw_registration.sh` | per tenant throw contact (three naming-rig parts, one per tenant, Phobos's air throw included) the P1 and P2 meter steps on ours and on native, and the writer/reader sets of each engine's hit-registration pair — the mechanism behind #136's meter family, THE DEFECT FROZEN AS MEASURED (ours attacker +8 / victim the record, native the reverse); plus the LEGACY CONTROL `legacy_demitri` (Demitri throws Victor on pristine vsavj and on vsav2: +9 / +8 on both — the host rule is vs2's); a fix re-freezes it deliberately | in-emulator (reference + ours) — MAME, native `vsav2` and the WIDE build, non-debug read taps, frames ≥ 2300 | `FREEZE=1 tests/audit_throw_registration.sh` | 14z-166 |
| `facing_rule.tsv` | `audit_facing_rule.sh` | the victim FACING RULE 5 (GitHub #159): the compare chain of each game's facing-rule resolver (vsavj 2,3,4; vs2 2,3,4,5), every write to Demitri's `+0x5D` from 3850 to 3960 on the #136 rig donovan_3 (Killshread Summon (ES)) with its writer PC, Demitri's x at 3924 and 3946 on both legs, and the legacy control's facing-rule histogram per game (`tools/audit_facing_rules.py`) — THE DEFECT FROZEN AS MEASURED (ours writes 4 at `0x1886C` and holds at 835; native's rule-5 branch writes 1 / 0 and slides to 755); a fix re-freezes it deliberately | in-emulator (reference + ours) + static — MAME, native `vsav2` and the WIDE build, non-debug read taps and field_trace samples; the decrypted opcode images | `FREEZE=1 tests/audit_facing_rule.sh` | 14z-167 |
| `rig_opening.tsv` | `audit_rig_opening.sh` | the naming rigs' opening on huitzil_5 (huitzil_6/7 share it): the first frame `$FF812D` reads 1, and P1's x and seq at 2363, 2370, 2395, 2481, 2544, 2560, 2565, 2600 and 2795 on both legs — THE ENTRANCE DIFFERENCE AND THE CONVERGENCE FROZEN AS MEASURED. The two games draw different arrivals (native's car carries Phobos from 360, ours holds him at 552) and the legs CONVERGE at the rig's first position pin, which since #168 lands at 2565, after the 2545 round start; the rows from the pin on, the first event at 2795 included, are identical on both legs | in-emulator (reference + ours) — MAME, native `vsav2` and the WIDE build, `field_trace.lua` samples | `FREEZE=1 tests/audit_rig_opening.sh` | 14z-167b; re-frozen 14z-172 (#168: the pin moved after the round start, two sample frames added, the pre-fix rows superseded) |
| `df_modes.tsv` | `audit_df_modes.sh` | Dark Force POWER vs Dark Force CHANGE, one row per leg: vsav2 P+K for the 15 characters its wheel reaches, vsavj P+K for 15, ours P+K for Demitri and the three tenants, and the tenants' vs2 EX input on vsav2 and on ours — stock, frames in seq 0x16, the `+0x147` arming value and writer PC, the `+0x176` timer's init and decrement writers, the frames `+0x111` and `$FF802E` are held, and (since 14z-169) the frames vs2's Power flag `+0x1C3` is held — set only by vs2's P+K, never by Change or by the tenants' vs2 EX — THE EX ROUTE ON OURS FROZEN AS MEASURED (ruled disabled 2026-09-18) | in-emulator (reference + ours) — MAME, real cursor picks, field_trace + a non-debug read_tap over `+0x146..+0x177`, the level and RNG pinned; positive control on the `+0x1C3` read: every vsav2 P+K (Power) leg must hold it | `FREEZE=1 tests/audit_df_modes.sh` | 14z-168; re-frozen 14z-169 (the `pow` column) |
| `df_meter.tsv` | `audit_df_meter.sh` | every step of P1's gauge `$FF850A` through two whiffed attacks out of the mode and two in it: the shells (Bulleta, Demitri, Victor) on vsavj, Demitri and the tenants on ours (P+K), the tenants under their vs2 EX install on vsav2 — THE DEFECT FROZEN AS MEASURED (the shells gain nothing inside `+0x111`, our tenants +6; #157's Dark Force tail) | in-emulator (reference + ours) — MAME, field_trace, real cursor picks, P2 kept out of reach | `FREEZE=1 tests/audit_df_meter.sh` | 14z-168 |
| `df_field_readers.tsv` | `test_df_field_readers.sh` | every placed instruction of the merged build naming one of vs2's Dark Force Power fields (`+0x1C3/+0x1C4/+0x1C6/+0x1C7/+0x1C8`) by displacement, with its access and base register — 44 on merged-m18: 26 reads (all `+0x1C3`), 18 writes (re-frozen 14z-168 with the access column after rule-checker run 2026-09-18-46 Q4); one (the x028122 meter adder copy) measured to change play | static (`tools/audit_df_field_readers.py` on the build's decrypted opcode image over its own placed code spans; its `--selftest` of hand-assembled encodings is the false-row control; positive control: the measured reader must be listed as a read; missed readers controlled where the corpus runs by `audit_df_field_readers_live.sh`) | `FREEZE=1 tests/test_df_field_readers.sh` (at every freeze: placed addresses move) | 14z-168 |
| `guard_reentry.tsv` | `audit_guard_reentry.sh` | P1's seq, node counter and freeze at +8..+25 of three guard-cancel events on a Demitri mirror (the #136 rig huitzil part 5 at first event 2800), vsav2 and pristine vsavj, and the frames vsavj and ours differ (0); since the Q2 answer to rule-checker run 2026-09-18-46, two `pixels` rows (snapshots at hit+14..+26 of the 3640 event: vsavj vs ours 0 of 13 frames differ, vsav2 vs vsavj 13 of 13 — the comparison's liveness) — the ENGINE DIFFERENCE FROZEN AS MEASURED (vsavj re-enters the block animation while back is held; vsav2 ticks); and six `act` rows, the maintainer's first-possible-frame test (LP pressed every other frame after the advancing-guard window, back held to +21 / +50): hit+35 on all three games, ruled "it's identical" | in-emulator (reference + ours) — MAME, field_trace, snapshot_frames; TWO references, vsav2 and pristine vsavj | `FREEZE=1 tests/audit_guard_reentry.sh` | 14z-168 |
| `column_shock.tsv` | `audit_column_shock.sh` | (since 14z-168 also one `timeline` row per leg: the hit frames, each fighter's return to neutral and the gap — 26 native, 14 ours — the fix ruled 2026-09-18) every write to both fighters' `+0x5C` around Killshread Lightning [MP]'s column hits (#136 rig donovan_4, event 1) with its writer PC — THE DEFECT FROZEN AS MEASURED (native's 0x52 handler writes the victim 0x18 and exempts Donovan; ours' 14z-42 thunks write 0x0C and 4) | in-emulator (reference + ours) — MAME, non-debug read_tap (since 14z-181 also the `air` rows: the column on a JUMPING Demitri — class 7, the air stager's case, the same freeze and sub-state and Donovan's freeze on both legs; #163's airborne item) | `FREEZE=1 tests/audit_column_shock.sh` | 14z-168 |
| `trap_airborne.tsv` | `audit_trap_airborne.sh` | the Plasma Trap dome and a JUMPING Victor, native vs2 vs the merged build: the dome NEVER connects in the air — the jump clears it (apex 127, airborne 3493-3528 with P2 up at 3490) and the hit lands on the LANDING frame (f3529, y 43 -> 40; the ground is y = 40) on both legs, the shock then as on the ground — freeze from 0x18, sub-state 4, Phobos never frozen — the class 0x52 native / 0x38 merged (the ruled marker); the SWEEP=1 timings 3466-3486 read the same (SWEEP_JUMPS overrides the list; the 14z-181 census traced every press 3440-3499 — 41 landing-frame hits, 21 grounded after landing, 7 no-jump, 0 in the air), so no dome hit reaches the air stager with this rig (its air case is #175) (#163's airborne item; the first form read y > 0 as airborne and called the landing hit airborne — rule-checker run 2026-09-25-164) | in-emulator (reference + ours) — MAME, field_trace, the trap gate's forced-pick pokes, level 6 and RNG 0000 pinned on both | `FREEZE=1 tests/audit_trap_airborne.sh` | 14z-181 |
| `trap_air_hit.tsv` | `audit_trap_air_hit.sh` | the Plasma Trap dome and an AIRBORNE Felicia (j.HP node 4, whose hurtbox reaches below her feet), native vs2 vs the merged build, real cursor picks on both legs: per (press, leg) the first hit's frame, the height before and at it, the class and node at it, and the steps of every traced field 3500-3530 — press 3508 hit IN THE AIR (y 42, class 0x07) identically on both legs, press 3511 on the landing frame (0x52 / the ruled 0x38) | in-emulator (reference + ours) — MAME, tools/trap_air_probe.sh over field_trace, real picks, Felicia and Phobos x-pinned before the window, level 6 and RNG 0000 pinned | `FREEZE=1 tests/audit_trap_air_hit.sh` | 14z-182 |
| `move_parity_attribution.tsv` | `audit_move_parity_attribution.sh` | the root and class of every DIFF row of `move_parity_events.tsv`, found by ablation (the root event's inputs removed, both legs re-run) and named by a measured signature, plus rows that surface once an earlier root is removed | in-emulator (reference + ours) — MAME, the parity gate's rigs regenerated through `tools/name_moves.gen` (checked byte-identical to the committed rigs first), `tools/move_parity_attribution.py`; step 0 must reproduce the frozen parity table (since 14z-181 the class DMG-OPEN: Donovan and Pyron taking 13 from Demitri's 5HP for native's 12 with their rows already vs2's — #161's residual on every tenant) | `FREEZE=1 tests/audit_move_parity_attribution.sh` (with every re-freeze of `move_parity_events.tsv`) | 14z-168 |
| `entrance_draw.tsv` | `audit_entrance_draw.sh` | Phobos's round-start entrance variant per draw (the first `+0x0A` value other than 0/255 after the intro initialises, else 0 = the drive-in, checked against P1 x moving) and his x at 2363 over six RNG seeds, ours vs vsav2, and the set each leg draws ({0,2,6} on both) — re-frozen 14z-168 after rule-checker run 2026-09-18-46 Q3 (the first freeze counted the initialisation 0 as a draw) | in-emulator (reference + ours) — MAME, field_trace, one RNG poke at 2250, the level pinned | `FREEZE=1 tests/audit_entrance_draw.sh` | 14z-168 |
| `pass_overrun.tsv` | `audit_pass_overrun.sh` | the zero-pass frames (`$FF8081` not stepping) after the round start on the three Blizzard Sword parts, both legs, and the scheduler's idle-spin dispatches per event window (mean, frames with none) | in-emulator (reference + ours) — MAME, field_trace + a non-debug read_tap on the task table `$FF025C` | `FREEZE=1 tests/audit_pass_overrun.sh` | 14z-168 |
| `column_flash.tsv` | `audit_column_flash.sh` | the SHA-1 (12 hex, never the bytes: rule 7) of palette row 11 (`RAM:$90C160`, 32 bytes) at frames 2858/2860/2866 of Donovan's Killshread Lightning column (naming part donovan_4), native vs2 and our merged build — the orange flash on the deity, THE COSMETIC DEFECT FROZEN AS MEASURED (native's blue ramp throughout, ours a fire ramp from 2860), plus the build row | in-emulator (reference + ours) — MAME, palette-RAM dumps through `tools/run_replay_mame.sh` DUMPS | `FREEZE=1 tests/audit_column_flash.sh` | 14z-170 |
| `phobos_dmg_residual.tsv` | `audit_phobos_dmg_residual.sh` | every P1 (Phobos) HP loss on the #136 naming parts huitzil_5/huitzil_6 (Demitri's 5HP, f5966 and f7725), native vs2 and our merged build, at RNG pins 0000/1234/5a5a — THE RESIDUAL FROZEN AS MEASURED (native 11, ours 12 with his defense row already vs2's; merged-m18 read 13), ruled 2026-09-19 "Freeze, ticket it (Recommended)" and, once the legacy rows answered it, #161 ruled not-ours 2026-09-25, plus the ours build row | in-emulator (reference + ours) — MAME, field_trace (since 14z-181 also the `legacy` rows: Demitri's 5HP on VICTOR on pristine vsavj and pristine vsav2 at the three pins — vsavj 12 / vs2 11, Victor's rows byte-identical between the games, so the +1 is the two engines' — GitHub #161) | `FREEZE=1 tests/audit_phobos_dmg_residual.sh` | 14z-170 |
| `x2b7ef4_reach_m18.tsv` | `audit_x2b7ef4_reach_m18.sh` | per Donovan/Pyron #136 naming part on merged-m18: reads of the corrupted x2b7ef4 records (a -debug read watch on the corrupted bytes only, armed at frame 2000), the first read's frame, the frame-exact common prefix of the debug run's P1 anim-node trajectory with a non-debug run's, and the frame where a debug run skews — none read; 18 of 20 frame-exact, donovan_10/12 skew one frame at f2980/f3537 (frozen AS MEASURED), plus the build row | in-emulator (reference + ours) — MAME, trace_writes.lua (-debug) + field_trace.lua | `FREEZE=1 tests/audit_x2b7ef4_reach_m18.sh` | 14z-170 |
| `df_moves.tsv` | `audit_df_moves.sh` | (since 14z-181 also the four `huitzil_dfx2` rows of the CONTACT group — #109's folded leg: Phobos's beams with P2 grounded at a near pin and jumping into the band; the whole schedule re-indexed at one event per activation with every rig HP pin moved out of the compared windows, rule-checker runs `2026-09-25-148` to `-155`, the last resolved with the per-frame level/RNG pins ACCEPTED as the gates' equalised input and put to the maintainer — a defect in the RNG's ADVANCE is masked on both legs by construction) (since 14z-168 also the `form`/`sword` rows: no piece in Pyron's area drawn with a differing palette at f2690-2730, with the palettes seen differing listed as the comparison's liveness; Donovan's sword RNG-independent with the RNG read 201/198 times per leg (native/ours) in its window since the 14z-181 re-anchoring past the 5HP x pin (~630 under the 14z-168 window), its idle hold 10 frames later natively, its flight x identical over the whole window (14z-168 read a first pixel difference at +34 whose mechanism is NOT identified) and y 120 throughout — ruled identical) per in-DF event of the #136 schedules, the ordered hits (damage, P2 reaction class) and gauge steps inside a mode ENTERED on both legs: ours by P+K (Dark Force Change), native by the tenant's vs2 EX input (the personal Dark Force) — the moves match in hits, damage and hit frames but for Lightning Sword's class byte (appearance not compared: the capture is the maintainer's); the gauge rows frozen AS MEASURED (#157's Dark Force tail) | in-emulator (reference + ours) — MAME, field_trace, per-leg activation inputs generated through `tools/name_moves.gen`, every activation asserted entered (a stock spent on each leg, our flag up), every compared event MEASURED inside the mode (`+0x111` non-zero on both legs at the event and at every hit; added 14z-168 after run 2026-09-18-47 Q4, rows unchanged) and P1 measured to ACT on both legs (seq/sub leaves its pre-event state; added after run -48 Q3, rows unchanged; the no-hit events' state paths printed), positions pinned, level/RNG pinned | `FREEZE=1 tests/audit_df_moves.sh` | 14z-168 |
| `df_field_readers_live.tsv` | `audit_df_field_readers_live.sh` | the rows of the static Power-field census (`df_field_readers.tsv`) that the #136 corpus executes on ours (7 reads of `+0x1C3` on merged-m18, the three meter-adder copies among them), vsavj's own writes to the same bytes (the block clears), the fighter block each access touched, and the unsampled count (37) — every executed access from placed code cross-checked as a census row with the right access class; every run must show, in BOTH tapped ranges, a game write inside the read window and an END PROBE (a Lua poke of `+0x1C9` on the last frame) (re-frozen 14z-168 with the blocks column after rule-checker run 2026-09-18-47 Q4; the probe added after -48, rows unchanged) | in-emulator (ours) — MAME, `read_tap.lua` on both fighter blocks' `+0x1C2..+0x1C9`, the 30 naming parts with the parity gate's own inputs and pokes | `FREEZE=1 tests/audit_df_field_readers_live.sh` | 14z-168 |
| `defense_rows_census.tsv` | `test_defense_rows_census.sh` | the defense curve (vsavj `0x0B8940` / vs2 `0x0D2ABE`) and rally threshold (`0x0BCC80` / `0x0D6E1E`) of every character id: the 15 legacy characters identical between the games but Sasquatch's row, vsavj's variant ids COPIES of their base rows, and the tenants' rows on our build (their SHELLS' today; the 2026-09-18 ruling takes vs2's); since 14z-169 the READER rows — every instruction naming an address inside either table by an absolute long, or a pc-relative `lea`/`pea` landing in one, in vsavj's and vs2's opcode views and our whole image: exactly the two host reads on vsavj and ours | static (the decrypt cache's opcode and data views and the build's `verify_op.bin`/`verify_data.bin`; the maintainer's question of 2026-09-18; the `reader-planted` control, an absolute load of an address inside the curve and a pc-relative `lea` onto the threshold table planted in a copy, must add one row per arm) | `FREEZE=1 tests/test_defense_rows_census.sh` (at the defense-rows fix) | 14z-168; re-frozen 14z-169 (the reader rows, rule-checker run 2026-09-18-51 Q4) |
| `reaction_classes.tsv` | `test_reaction_classes.sh` | the route of a hit's class from the attack record (`+0x17`) through the three stager tables (ground / air / KO: what each handler writes into the victim's `+0x54`) to the reaction table, for classes 0x06/0x07/0x08/0x38/0x39/0x48/0x4E/0x52 on pristine vsavj, vs2 and the merged build; the property bytes; the shock handler's shape on each (vs2 = vsavj + `cmpi.b #$52,$54(a6); beq`; ours = vsavj with the two 14z-42 `jsr` thunks); the legacy record census by class (vsavj 1,085 reachable records, one Victor record 0x38; vs2 one Bishamon record 0x52); every constant write to `+0x54` (none writes 0x38); every constant compare against the record's `+0x17` (0x38 in the GUARD chain) and against `+0x54` (never 0x06/0x38; vs2 0x52 once); the three per-class tables `+0x54` indexes; since the re-freeze, every constant write to `+0x54` in our whole image (the placed `reaction_hook` and `index_window_018468` stager windows write 0x52 for a class-0x52 record, which no record on ours carries — `tools/audit_fsm_census.py` finds 0 node-state bytes 0x50-0x53 on merged-m18, signature-bounded, gate `test_fsm_census.sh`) and one `ours build` row naming the registry row the ours rows come from (merged-m18) | static (`tools/audit_reaction_classes.py` on the decrypted opcode and data views of both reference sets and the build's `verify_op.bin`; every table bound asserted, no row typed in; two planted copies — a stager entry re-pointed, a record re-classed — must move exactly their rows) | `FREEZE=1 tests/test_reaction_classes.sh` (at every freeze: the ours rows follow the build) | 14z-169 (re-frozen the same sitting after rule-checker run 2026-09-18-51 Q1) |
| `reaction_class_live.tsv` | `audit_reaction_class_live.sh` | every write (pc, value) and read (pc, values seen) of the victim's reaction class `+0x54` in both fighter blocks: pristine vsavj over every `tests/replays/*.rpl` (88 runs, 575k frames), ours and native vs2 over the 30 #136 naming parts with the parity gate's inputs and pins — no write puts 0x38 there on vsavj or ours; native writes 0x52 at `0x016FEC` (the column and the trap) where ours writes 6 at `0x0186D0` | in-emulator (reference + ours) — MAME, `read_tap.lua` on `+0x54/+0x55` of both blocks; every run must log the END PROBE (a Lua poke of `+0x55` on its last frame) in both windows; positive controls: ours' ground-stager 6 and native's 0x52 must appear; the `planted-38` run must report its poked 0x38 | `FREEZE=1 tests/audit_reaction_class_live.sh` (at every freeze: the ours rows carry placed pcs) | 14z-169 |
| `defense_row_reads.tsv` | `audit_defense_row_reads.sh` | every read of a fighter's `+0x382` by the damage chain's two defense reads (`PRG:0x018C10` the curve, `0x018C78` the rally threshold) and the attacker-side combo read (`0x018C40`), by the victim's identity and the index value read: the 12 naming victim parts, the 30 attacker parts and every suite replay on the merged build, every suite replay on pristine vsavj (219 runs) — every defense and threshold read indexes the VICTIM'S OWN id (Donovan 394 hits, Phobos 38, Pyron 28 on ours; 15 legacy characters on vsavj) | in-emulator (reference + ours) — MAME, `read_tap.lua` with `RPCS` on `+0x382` and `+0x60` of both blocks; identity = the hitbox base `+0x60` named by `roster_pairings/bases.tsv`, never `+0x382`; every run must log the END PROBE (`+0x383` poked on its last frame) in both windows; positive control: the victim parts must land hits on each tenant; the `planted-flavor` run (P2's `+0x382` poked to 0x01 before the first hit) must report a mismatch | `FREEZE=1 tests/audit_defense_row_reads.sh` (at every freeze: tenant bases move) | 14z-169 |
| `crouch_flag.tsv` | `audit_crouch_flag.sh` | P1's `+0x121` (with seq and sub) at every frame it changes over 2830-2880 of the donovan_1 naming rig, whose schedule holds Down 2840-2870, on native vsav2 and the merged build, and P2's `+0x121` held 0 — `+0x121` is 1 exactly while Down is held on both legs: the flag the guard decision tests before its two record-class lists | in-emulator (reference + ours) — MAME, `field_trace.lua`; P1's id asserted 0x13 on each leg; the `down-removed` control (the Down line deleted from the rig) must leave `+0x121` at 0 on both legs | `FREEZE=1 tests/audit_crouch_flag.sh` | 14z-169 |
| `ex_refused.tsv` | `audit_ex_refused.sh` | each tenant's vs2 EX input (Donovan 421+KK, Phobos 263+PP, Pyron 2623+PP) on native vsav2 and the merged build with the stock at 0 (refused) and 3 (entered): whether the Change field `+0x111` is ever set, the stock after, and P1's (seq/sub) path over 3255-3400 — the refused input never enters the mode and takes the SAME path on both games for all three tenants | in-emulator (reference + ours) — MAME, `field_trace.lua`, `tests/audit_df_modes.sh`'s rig with real cursor picks, the level and RNG pinned; the `stock-kept` control (the refused legs with stock 3) must read entered; captures `build/rc169/cap_ex/` delivered to the maintainer | `FREEZE=1 tests/audit_ex_refused.sh` (the EX-route fix re-freezes the ours rows) | 14z-169 |
| `poked_legs.tsv` | `test_poked_legs.sh` | every forced-pick leg in `tests/` and `tools/` paired with the replays its script names, resolved through the game's select wheel to the confirmed cell and classified SAME / CROSS-INERT / CROSS-FLAVOR / PARAM — the #151 step-3 census | static (the scripts, the replays, `tools/select_wheel.py` on vsav2's data view and the WIDE build's) | `FREEZE=1 tests/test_poked_legs.sh` | 14z-161 |
| `same_data_p2.tsv` | `test_same_data_p2.sh` | for every legacy id both games carry, whether its character data is the same on vsavj and vsav2 — the bank value rows, every anim node's resolved hitboxes and attack record (tables a/a2/b/c/proj), the defense-curve row — and which chains differ (GitHub #136 proposition 2; the P2 of the parity rigs) | static (`tools/audit_same_data_p2.py` on the two decrypted data views; positive control: the same view on both sides reads SAME-DATA) | `FREEZE=1 tests/test_same_data_p2.sh` | 14z-164 |
| `poked_legs_accepted.tsv` | `test_poked_legs.sh` | the CROSS-FLAVOR pairings allowed to stay, each with the measurement that lets it (a tap showing no read, or the note that the script never runs the pairing) | in-emulator (`audit_latch_reads.sh` legs) / static (the script's own leg list); hand-maintained | edit the row, then `tests/test_poked_legs.sh` | 14z-161 |
| `linux_host_provided.tsv` | `test_host_libs.sh` | the sonames a Linux release binary may ask the HOST for, read by `tools/check_host_libs.py` inside `test_release_binaries.sh` on a Linux host: every file's direct NEEDED soname is shipped in the folder or on this list. 23 rows are manylinux_2_39's published list; 13 are exceptions the maintainer ruled 2026-09-13, each with its reason (the glibc loader, the X11 companions, the Wayland clients, libdrm and libgbm, libasound and libpulse). NEVER derived from `tools/bundle_elf_libs.py`, which is the artifact's own policy ([VSP-166]) | registry (an adopted external policy plus rulings; the file header names the auditwheel version and the wheel's sha256) | regenerate from auditwheel's `manylinux-policy.json` plus the rulings; a new exception needs a maintainer ruling and its reason | 14z-152 |
| `module_refs_floor.txt` | `test_module_refs.sh` | the fewest cross-module Python references `tools/audit_module_refs.py` may count and still PASS — a floor under the census so an empty or shrunken measurement is a red (#171 shape 1b), never the references themselves | static census (the first floor was the tree at `97d84268`, measured by a `measurer` worker, `build/gatequal180/measurer_returns_14z180.txt` E/C2; the CURRENT floor is the file's own number and rises with each accepted import — the file, not this row, carries it) | `FREEZE=1 tests/test_module_refs.sh` — rises only, after review of what added the references | 14z-180 |
| `gate_descriptions.tsv` | `test_gate_descriptions.sh` | which gates carry the three description fields `# WHAT:` / `# HOW:` / `# EXPECTS:` in their leading comment block (`declares`, grows only) and which do not yet (`undeclared`, shrinks only) — GitHub #171 slice Q0, the maintainer's ruling of 2026-09-24 | static census (`tools/gate_descriptions.py` over tests/*.sh; the classes, never the descriptions' correctness, which is the maintainer's review) | `FREEZE=1 tests/test_gate_descriptions.sh` after each family batch is reviewed | 14z-180 |
| `gate_follows.tsv` | `test_gate_follows.sh` | which emulator-tier gates (every tests/ci_emulator.tsv row) carry a `# FOLLOWS:` header field naming the repo path prefixes their verdict depends on (`declares`, grows only) and which do not yet (`undeclared`, shrinks only) — GitHub #171 slice Q3, the maintainer's ruling of 2026-09-24 ("Header line") | static census (`tools/gate_follows.py` over the registry's scripts; the classes only — a declaration's WIDTH is proved by the same gate's reconciliation, never frozen) | `FREEZE=1 tests/test_gate_follows.sh` after the new declarations are reviewed | 14z-180 |
| `poke_readback.tsv` | `test_poke_readback.sh` | the classification of every finding of `tools/audit_poke_readback.py` — a gate that SAMPLES an address its own rig POKES — as UNCLASSIFIED (the maintainer's to rule), OBSERVES (grows only), READS-BACK (shrinks only), CONTROL-PLANT (a control leg's plant), CROSS-LEG (a poke joined to another leg's dump) or RETIRED — the 73 findings ruled 2026-09-25 (14z-181, `DECISIONS_HISTORY.md` "Ruled 2026-09-25 (14z-181) — #171 Q6"), a dropped READS-BACK column RETIRED with the ruling in its note — each with its LEG (main / control / mixed — a control leg's plant reads its poke back by design); GitHub #171 slice Q6, shape 4 of the scope | static census over the registry's scripts and the schedules the rig generators emit (the findings), plus the maintainer's rulings (the classes) — the one READS-BACK row is the killshread `stock` column ruled 2026-09-22 | `FREEZE=1 tests/test_poke_readback.sh` adds a new finding as UNCLASSIFIED; a class changes only by the maintainer's ruling, edited by hand with its note | 14z-180 |
| `state_open_lists_debt.txt` | `test_state_open_lists.sh` | the violations of the open-list rule (CLAUDE.md [VSP-17]) that existed when it was written — SHRINK-ONLY: a violation not listed fails, and a listed one that no longer occurs fails until it is removed | static (STATE.md, STATE_HISTORY.md and docs/NEXT_SESSION.md read without running anything) | `python3 tools/check_state_lists.py --freeze`, which REFUSES to add a key without `--allow-growth "<reason>"` and writes the reason into the file | 14z-154 |
| `tickets_debt.txt` | `test_tickets.sh` | the ticket rows not yet backfilled — the issues whose row may still carry `?` (CLAUDE.md [VSP-182]); SHRINK-ONLY | registry (a decision per row, not a measurement) | hand-edited: an issue leaves in the commit that answers its row's last `?`; adding one is a decision | 14z-154 |

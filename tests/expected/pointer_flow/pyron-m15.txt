# pointer-flow baseline for pyron-m15 (14z-111, the #99 root-cause fix): vs the 14z-105 set, WEAK data:long 776 -> 843 = the x100e3c (0xC8E) AI-script block's word-offset streams read as 4-byte hole longs (+67; the segmented oracle found 0 pointer fields). No STRONG change. Previous header follows.
# the two merged STRONG lines are the 14z-100-reviewed win_pal sparse-block biased bases,
# re-placed by the window's allocation shift; WEAK volume re-counted).
# STRONG lines are REVIEWED findings (see hardening_register.md);
# WEAK-COUNT lines freeze the noise volume — growth = investigate.
WEAK-COUNT	code:move_imm	1
WEAK-COUNT	data:long	843

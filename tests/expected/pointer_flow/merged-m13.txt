# pointer-flow baseline for merged-m13 (build/m3b_merged20), frozen 14z-117b — the random-select freeze (M11).
# vs the 14z-117 baseline (merged-m12): the two STRONG win_pal bases moved +0x30 (thunk + target) — the two
# random-select thunk bodies (hole b) are allocated per tenant iteration ahead of the ext placements, so the
# H/P ext allocations shift (+0xC0 Phobos / +0x30 Pyron, bases.tsv re-derived); WEAK counts UNCHANGED —
# neither new body carries a pointer-shaped operand (the jmp targets resolve as PATCHED).
# STRONG lines are REVIEWED findings (see hardening_register.md);
# WEAK-COUNT lines freeze the noise volume — growth = investigate.
STRONG	code:movea_imm	0x4d39a0+0x16	0x4c6f20	WIDE-HOLE
STRONG	code:movea_imm	0x4d39a0+0x24	0x4ccec0	WIDE-HOLE
WEAK-COUNT	data:long	1182
WEAK-COUNT	code:move_imm	3

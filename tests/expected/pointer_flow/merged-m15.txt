# pointer-flow baseline for merged-m15 (build/m3b_merged22), frozen 14z-130 — the M13 boot-title freeze.
# vs the 14z-119 baseline (merged-m14): IDENTICAL, and MEASURED so rather than assumed — the M13 build
# was run against the merged-m14 baseline and satisfied it with no growth and no missing line. The three
# boot-title [[aux_poke]] poke16 write TEXT (PRG:0x01C822/24/26), so they introduce no pointer-shaped
# long and allocate nothing; the gap_be27a -> capture_kf_ptr bank-map correction folded into this
# freeze is byte-neutral on every track (tests/test_capture_kf_ownership.sh).
# STRONG lines are REVIEWED findings (see hardening_register.md);
# WEAK-COUNT lines freeze the noise volume — growth = investigate.
STRONG	code:movea_imm	0x4d39a0+0x16	0x4c6f20	WIDE-HOLE
STRONG	code:movea_imm	0x4d39a0+0x24	0x4ccec0	WIDE-HOLE
WEAK-COUNT	data:long	1182
WEAK-COUNT	code:move_imm	3

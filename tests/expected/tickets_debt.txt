# tests/expected/tickets_debt.txt — the ticket rows NOT YET BACKFILLED: the issues whose
# row in docs/project/tickets.tsv may still carry `?` (CLAUDE.md [VSP-182]; the retrofit
# opened 2026-09-14, 14z-154). Read by tools/tickets.py (gate tests/test_tickets.sh).
# SHRINK-ONLY: a `?` in a row not listed here fails, and a listed row with no `?` left
# fails until it is removed. Adding a number here is a decision, never a reflex.
# At its opening every one of the 114 issues was listed; 73 audit rows already carried
# their `decided` link (the audit dispositions table).

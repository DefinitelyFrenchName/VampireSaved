POSITIVE fixture: the #136 rig and its claim as the 14z-159 session believed them
(commit fd3f164b; the freeze built on it was withdrawn — gotchas.md "A FORCED-PICK
NATIVE LEG MEASURES THE RIG"). Not handed to the checker.
EXPECT names Q3/Q4 (the shared poke path; no control for the native leg's
faithfulness). Calibration run 01 also fired Q1 and Q2; both are TRUE of the
packet: the claim names no untested premise while the gate's own header lists
what it does not cover and the compared fields omit the byte being changed (Q1),
and the conclusion is about how Phobos's jumps and floats play, drawn with no
capture before the rebuild — the gotcha's second rule (Q2). Extra firings that
are true findings are the checker working, not noise; EXPECT stays at the two
questions the incident was written from.
- 14z-178 (the PINNED READER, #172 S4 step 4): recalibrated as run 2026-09-24-114 on `.claude/agents/rule-checker.md` (rule-checker@efa6cdd64dd0: Opus 5.5, effort high, omitClaudeMd — no CLAUDE.md or memory in context, unlike every earlier run), plant CAUGHT, verdict VIOLATED Q1 Q2 Q3 Q4. Extra firings Q1 and Q2, as in run 01: TRUE of the packet (above).
- 14z-178, second round (the model pinned by VERSION id, claude-opus-5-5, never the alias — #158): run 2026-09-24-124 on rule-checker@163e4ce9cf7b, plant CAUGHT, verdict VIOLATED Q1 Q2 Q3 Q4; every reader verified from its own transcript (`rulecheck.py spawned`) and every verdict collected from it (`collect`). Extra firings as in the round above.

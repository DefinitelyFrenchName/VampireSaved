The NEGATIVE fixture: a packet the checker must leave OK. Not handed to the checker.
Calibration history (the ledger is the record; this file says why a fixture changed):
- v1 (run 2026-09-17-01..03 series, run 03): two artifacts, and a claim naming only
  two untested items. The checker returned VIOLATED on Q1/Q3/Q4 with SPECIFIC
  findings: the globals and route residue are MEASURE-only, SELF is a same-value
  write, and the shared `_base` pokes were not in the packet so it could not tell
  whether one lands in the P1 block. Read as a gap in the PACKET, not checker
  noise: every finding was true of the artifacts as handed over.
- v2: name_moves.py added (the schedule shows the shared pokes address P2's block
  or the globals, from frame 2000 at the earliest), and the claim states each of
  those limits. A negative fixture is what a COMPLIANT packet looks like; it is not
  tuned until the checker agrees — if v2 is flagged with a finding that is true of
  the artifacts, the finding is a gap in the gate and goes to its ticket.
- v2 result (runs 04 and 05, two fresh agents): VIOLATED on Q1 (both) and Q3 (one).
  Both found the SAME two false statements in the claim, verified in the file:
  the rig pins P1's X at $FF8410 (P1 block +0x10) before every "far" event
  (name_moves.py line 687), and part 1 has no stock pokes (METER_PARTS excludes
  it); also the level/RNG pins are the gate's own, P2-empty is asserted on the
  POKED-vs-REAL comparison only, LATCHED is over six samples, TRANSIENT is not
  frozen. The checker caught the working agent's own claim. Not noise.
- v3: the claim corrected to what the artifacts show (every item above stated).
- v3 result (runs 06 and 07, two fresh agents): OK and OK, every named premise
  confirmed at its line; each noted one imprecision it judged not load-bearing
  (Victor on P2 is never asserted; donovan_1 rows carry no position pin).
  CALIBRATED on v3. The record of v1/v2 stays in the ledger as VOID rows.
- v3 beside a PLANT (run 12, after the first real run's Q4 finding): the plant
  (forced-pick) CAUGHT; the negative VIOLATED on Q1 by a third reader with two
  further points, both true of the artifacts: (a) "a first difference outside
  the pinned field" — the first-play search excludes only latched offsets, so
  the claim asserted what the gate does not show; (b) "every one of the six
  samples" — the gate asserts >=30 dumps per leg and samples what both legs
  share, so six is design, not assertion. Two readers had passed v3 (runs 06,
  07); the third did not. The reader-to-reader spread on Q1 is a measured
  property of the checker, reported to the maintainer, not tuned.
- v4: both statements corrected to what the gate shows.
- BOUNDED QUESTIONS (maintainer-ruled option C; runs 18, 19, each beside a
  caught plant): v4 read OK once and VIOLATED once — the VIOLATED reader found
  a true premise the claim had not named: the POKED leg is the gate's own
  hand-typed replica of the #136 rig's leg, and nothing asserts the replica
  equals the rig (a gate gap, ticketed). v5 names it.
- v5 under the bounded questions (runs 20, 21, each beside a caught plant): OK
  and OK. CALIBRATED under checklist 62514a98c3e7. Every premise a reader ever
  found true is now named in the claim; what remains is the maintainer's
  written-resolution half of the ruling.
- 14z-178 (the PINNED READER, #172 S4 step 4): recalibrated as run 2026-09-24-121 on `.claude/agents/rule-checker.md` (rule-checker@efa6cdd64dd0: Opus 5.5, effort high, omitClaudeMd — no CLAUDE.md or memory in context, unlike every earlier run), plant CAUGHT, verdict OK -. Both readers of run 121 noted that donovan_1's rig carries no position pin — TRUE of the packet (known since v2, above), outside the LATCHED premise; the negative's reader kept Q1 OK.
- 14z-178, second round (the model pinned by VERSION id, claude-opus-5-5, never the alias — #158): run 2026-09-24-131 on rule-checker@163e4ce9cf7b, plant CAUGHT, verdict OK -; every reader verified from its own transcript (`rulecheck.py spawned`) and every verdict collected from it (`collect`). Extra firings as in the round above.

[2072 07:10] A  Resolved. Now the re-freeze, running in the background while I write the ruling, the gate header note and your ticket:
[2073 07:10] T  Bash: Re-freeze the three expectations :: cat > /tmp/refreeze.sh <<'EOF' #!/bin/sh # Re-freeze the three expectations, maintainer-ruled 2026-09-22 (option 1). # Each gate refuses FREEZE unless it otherwise passes with every leg and no control # mode, so a re-freeze cannot paper over a second cause. set -eu cd /Users/koneko/Developer/Vampire
[2073 07:10] D  DETACHED launch — `nohup` detaches a job the harness cannot track
[2074 07:10] R  ok: re-freeze started pid=43029 at 09:10:31 — freeze then verify, ~28 min
[2076 07:10] T  Bash: Open the gate-qualification ticket :: gh issue create --repo DefinitelyFrenchName/VampireSaved \ --title "Qualify every gate: prove it measures what it claims, and that it notices when its inputs move" \ --label enhancement --body-file - <<'EOF' **Maintainer's instruction, 2026-09-22**, on the M19 release tier coming back NOT GREEN with
[2077 07:11] R  ok: https://github.com/DefinitelyFrenchName/VampireSaved/issues/171 rc=0
[2079 07:11] T  Bash: Record the ruling :: cat > /tmp/ruling.py <<'OUTER_EOF' p = "DECISIONS_HISTORY.md"; s = open(p, encoding="utf-8").read() anchor = "## Ruled 2026-09-21 (14z-174) — #146 CLOSED, and the READMEs change on USER FEEDBACK from here" entry = """## Ruled 2026-09-22 (14z-174) — the three stale expectations are RE-FROZEN AS-IS, a
[2080 07:11] R  ok: ruling recorded 30:## Ruled 2026-09-22 (14z-174) — the three stale expectations are RE-FROZEN AS-IS, and every gate gets qualified
[2082 07:11] T  Bash: Check the re-freeze :: cat /tmp/refreeze.log; ps -p 43029 >/dev/null 2>&1 && echo "(re-freeze running $(date +%H:%M:%S))" || echo "(re-freeze done)"
[2083 07:11] R  ok: test_killshread_es ... FROZE audit_reaction_class_live ... (re-freeze running 09:11:31)
[2091 07:11] A  **Ruled, recorded, and executing. Nothing is waiting on you.**

## Done since your message

**The diffs are reviewed, paired row by row** — and all three are clean in the same way. **Every identity column is unchanged** in all three tables:

```
killshread_es          6 rows   move/frame/cls/hp/white UNCHANGED   stock 8→9
reaction_class_live   13 rows   leg/direction/pc/value  UNCHANGED   counts only
defense_row_reads      5 rows   leg/pc/kind/identity/value UNCHANGED  reads/runs only
```

No pc moved, no value moved, no victim identity moved. Only how many times things were seen — which is exactly what a rig schedule change produces.

**The rule-checker run is recorded and resolved**, with my own error written into the ledger rather than quietly dropped: Q1's two-cause narrative was broader than my evidence (ΔF=195 never touched `donovan_12` — its first event is `"far"`, and far parts do not move), and Q2's finding rested on a claim of mine that was simply wrong about damage.

**Your ruling is in `DECISIONS_HISTORY.md`** with your words verbatim.

**Ticket #171 is open** — *"Qualify every gate: prove it measures what it claims, and that it notices when its inputs move"*. It names the four distinct shapes this sweep found rather than the one that prompted it: a dead call (five gates, four days), stale expectations behind moved rigs (three gates), a control killed by its own timeout, and a column reading back a poke. And the common factor, which is the real target — the emulator tier only runs at a freeze or a release, so breakage accumulates and surfaces at the worst possible moment.

**The re-freeze is running**: `killshread_es` FROZE, the other two in progress, then all three re-run clean to verify. ~25 min.

## Then, straight through

`stock`'s true nature written into the gate header and `PROVENANCE.md` → all eight previously-red gates re-run → emulator tier without `--lane all` (~2 h, MiSTer carried on the recorded evidence) → publish and prune M18.
[2096 09:17] M  what's the status?
[2098 09:17] T  Bash: Check the re-freeze :: date; cat /tmp/refreeze.log; ps -p 43029 >/dev/null 2>&1 && echo "(running)" || echo "(done)"
[2099 09:17] R  ok: Tue Sep 22 11:17:49 CEST 2026 test_killshread_es ... FROZE audit_reaction_class_live ... FROZE audit_defense_row_reads ... FROZE == verify: a clean re-run must 
[2102 09:18] A  All three **froze and verified PASS** — and it finished around 09:40, so I let it sit again for 90 minutes. Going straight through the rest without pausing. Writing the `stock` note first:
[2103 09:18] T  Bash: Record the stock column's true nature :: cat > /tmp/stocknote.py <<'OUTER_EOF' p = "tests/test_killshread_es.sh"; s = open(p).read() anchor = "# Emulator tier (MAME, ~2 min). Usage: ROMDIR=... [MAME_BIN=...] [FREEZE=1] tests/test_killshread_es.sh" note = """# THE `stock` COLUMN RECORDS THE RIG, NOT THE ENGINE'S METER (found by the # rule-c
[2104 09:18] R  ok: noted in the gate header and in PROVENANCE syntax ok provenance gate PASS
[2106 09:18] T  Bash: Start the re-run with the mister lane carried :: export ROMDIR=/Users/koneko/Developer/Vampire_Saved/ROMS nohup env ROMDIR="$ROMDIR" tests/run_all_emulator.sh --scope all --strict --controls --jobs 4 \ > /tmp/rel_emu2_14z174.log 2>&1 & echo "$!" > /tmp/rel_emu2.pid; echo "emulator tier (NO --lane all: mister carried) started pid=$(cat /tmp/rel_emu
[2106 09:18] D  DETACHED launch — `nohup` detaches a job the harness cannot track
[2107 09:18] R  ok: emulator tier (NO --lane all: mister carried) started pid=62113 at 11:18:34 MAY CARRY: every subject of the mister lane is byte-unchanged since cf4dfd86, so its
[2115 09:18] T  Bash: Prove the tier is alive and stage the work :: wc -l /tmp/rel_emu2_14z174.log | awk '{print $1" lines"}'; tail -3 /tmp/rel_emu2_14z174.log | cut -c1-110 git add -u && git add tools/audit_lane_carry.py && git diff --cached --name-only | sed 's/^/ /'
[2116 09:18] R  ok: 22 lines MAME_BIN /Users/koneko/.cache/vampire-saved/mame/cps2 (runner default) == prereq lane (20 gates) == DECISIONS_HISTORY.md tests/ci_emulator.tsv tests/ex
[2118 09:19] T  Bash: Commit the repairs :: git add tests/rulecheck/runs/2026-09-22-91 2>/dev/null; git commit -F - <<'MSGEOF' 14z-174: the release tier's eight reds — five dead gates, three stale expectations, none a romset defect The M19 release emulator tier came back PASS 275 / SKIP 1 / FAIL 8 / TIMEOUT 1 in 6 h 24 min. Every red is diagn
[2119 09:19] R  ok: [main 2e296e78] 14z-174: the release tier's eight reds — five dead gates, three stale expectations, none a romset defect 17 files changed, 281 insertions(+), 41
[2121 09:19] A  All eight reds are fixed, re-frozen and committed. **The re-run is in flight.**

## Since your last check

**The re-freeze completed and verified** — and it had finished around 09:40, so I lost another 90 minutes to the same idling habit. That's the third time; I'm not going to keep narrating it, I'm just going to stop doing it.

```
test_killshread_es         FROZE → PASS
audit_reaction_class_live  FROZE → PASS
audit_defense_row_reads    FROZE → PASS
```

Each gate refuses `FREEZE=1` unless it otherwise passes with every leg and no control mode, so a re-freeze can't paper over a second cause.

**The `stock` column's true nature is now written where a reader will hit it** — in the gate's own header and in `PROVENANCE.md`: it reads the rig's poke back, `[VSP-170]`'s ES discriminator is neutralised in that rig, and the ES/normal distinction there rests on the contact-wave counts instead. Pointed at #171.

**Committed** as `2e296e78`, 17 files. The message records the rule-checker's three findings including the one that was about my own misreading, because the next person to read that ledger should see it.

**The emulator tier is re-running now** — `--scope all --strict --controls --jobs 4`, no `--lane all`, MiSTer carried on the evidence re-checked at launch (`MAY CARRY`: core 0, tooling 0, romset 0, nine gate scripts 0). Started 11:18, in the prereq lane, ~2 h against the 6 h 24 m of the full run.

## Remaining, in order

1. this tier green
2. the release-cadence **static** tier re-run — required, because I've changed `name_moves.py`, `vanilla_join_rig.py`, `ci_emulator.tsv`, three expectations and several docs since the last one was green (~46 min)
3. publish `freeze/merged-m19`, verify each asset byte-for-byte against the tree as it's served back, prune M18's seven

I'll come back when the tier lands — with the verdict read off the log's own GREEN line, not the exit status.
[2126 15:20] M  what's the status?
[2128 15:21] T  Bash: Check the re-run :: date; ps -p $(cat /tmp/rel_emu2.pid) >/dev/null 2>&1 && echo "TIER: RUNNING" || echo "TIER: FINISHED"; awk '/^ [a-z_]/ {print $2}' /tmp/rel_emu2_14z174.log | sort | uniq -c | command grep -E 'PASS|FAIL|SKIP|TIMEOUT' | tr '\n' ' '; echo; tail -5 /tmp/rel_emu2_14z174.log | cut -c1-115
[2129 15:21] R  ok: Tue Sep 22 17:21:23 CEST 2026 TIER: FINISHED 281 PASS 1 SKIP SKIP : audit_mask_window_ff42a2 results build/emu_sweep_20260922_111835/results.tsv --strict: SKIP 

TASKS (the jobs QP4 asks about)
  tracked tasks launched in the span with no completion notification by its end: none
  detached launches in the span (no notification can ever report them): 2
    [2073] Re-freeze the three expectations — `nohup` detaches a job the harness cannot track
    [2106] Start the re-run with the mister lane carried — `nohup` detaches a job the harness cannot track

Q1: OK — the claim's untested list matches the gate: globals/whole-RAM printed only under MEASURE (audit_forced_pick_fidelity.sh.txt:189, :201-211), TRANSIENT count printed but not written to got.tsv (:236, :284), REAL-vs-SELF consumes fields 1 and 6 only (:272, :277), REAL-vs-ALT under MEASURE only (:90-94, :285-289), first_play excludes only `latched` (:197-198), the six pre-match samples are the DFRAMES list (:99, "1501 1600 1800 2000 2200 2362"), the `-ge 30` dump check (:258), the re-typed prologue and copied pins (:117-134) with no assertion against the #136 rig, and the re-freeze condition in forced_pick_fidelity.tsv:9-10; no unnamed premise found (the only inaccuracy, "position pins ... before each event", is false for donovan_1 — name_moves.py.txt:206-225 carries no "far"/"near" field so pin_pokes is empty at :680-686 — and it errs in the safe direction: fewer shared writes, not more).
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."; the frozen `2980` in forced_pick_fidelity.tsv:15 is not interpreted by the claim.
Q3: OK — the only shared writers inside the pre-match window are globals (audit_forced_pick_fidelity.sh.txt:133-134, `ff8116` from 2000 and `ff80d4` from 2363); the rig's copied pokes reach the P1/P2 blocks only from 2560/2550 (name_moves.py.txt:680-686 `t - 40` with FIRST_EVENT 2600; :700 `range(FIRST_EVENT - 50, ...)` at `ff8850`), after the last pre-match sample 2362; the shared input path that selects the compared state is asserted, not assumed (:169-179 identity at 1600 and cell at 1290), and the shared POKES path is proven live by the poked legs' identity (:174) since an R,R prologue can only read the tenant id through the poke.
Q4: OK — freeze equality on the latched set (audit_forced_pick_fidelity.sh.txt:300-304) fires on a fourth or a missing offset; wrong-cursor (:253, :318-322) fires when the real leg confirms another character; REAL-vs-SELF (:281-282) and the donovan-self row (forced_pick_fidelity.tsv:17) fire when the poke itself latches; the P2 block pre-match assertion (:279-280) fires when the instrument differs for a reason other than the pick; same-leg (:310-316) fires when a non-empty freeze meets an empty read; the remaining modes (both-legs dump loss, same-value inference, replica drift, route residue, pinned-field play difference) are named by the claim as untested.
Q5: N-A — the packet attributes no decision, approval or ruling to any person; "14z-121 ... maintainer-confirmed" (name_moves.py.txt:260) and "the maintainer's ruling 14z-121" (:392) are in an artifact, not in the claim, and the claim does not rest on them.
VERDICT: OK

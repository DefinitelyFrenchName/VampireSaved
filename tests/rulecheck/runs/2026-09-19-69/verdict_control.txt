Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the artifacts show otherwise. audit_move_parity.sh.txt:105 samples 12 tenant fields only, and $FF87C2 is not one of them. Lines :25-29 and :119 start the comparison at the rig's first event and FIELD_FROM=2300, not at the start of the part. Lines :34-36 say "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters", and P2 is never compared. Only huitzil_1 was run, of ten Huitzil parts, all frozen DIVERGES (move_parity.tsv:20-29). The evidence is a RAM poke on the existing build, and the rebuilt flavor_default=0x01 ROM that the action ships was never run. pokes_for (:86-103) has no $FF87C2 poke, and the gate emits verdicts, not frame counts (:172-178), so no artifact shows "6851/6851". No artifact shows "every consumer ... enumerated" either. The level pinned at 6 against vsav2's TURBO default (:17-19) is also an unnamed premise.
Q2: VIOLATED — The conclusion is about how Phobos plays: the frozen verdict "huitzil_1 DIVERGES Jump [8]+21f" (move_parity.tsv:20), in a part made of jumps, air dashes and floats (name_moves.py.txt:448-459). The only support offered is a number ("6851/6851 frames identical"). No image or recording was put before the maintainer before the conclusion was drawn.
Q3: VIOLATED — Both legs share pokes that write fields the gate compares. The level byte ff8116 is poked every frame from frame 2000 on both legs (audit_move_parity.sh.txt:89,98,101), and :105 compares "ff8116:b:lvl". The character id ff8782 is forced on the native leg by name_moves.py.txt:672 and on ours by audit :92,:101, and :105 compares "ff8782:b:id". The x position ff8410 is pinned on both legs 40 frames before every "far" event (name_moves.py.txt:658; every part-1 event is "far", :448), and :105 compares "ff8410:w:x". The native reference 0x01 was also read only on a Phobos forced by poke over the default cursor's LP select (name_moves.py.txt:54, :58-60, :672), a rig ours shares.
Q4: VIOLATED — The gate has two controls, unpinned-level and no-translation (audit_move_parity.sh.txt:4-5). Neither would fire if the rebuilt flavor_default=0x01 ROM behaves differently from a runtime poke of RAM:$FF87C2, for example through a consumer that reads the latch during character init or the intro, which is not compared (:25-29). Neither covers consumers of the flavor byte outside part 1 or outside the 12 fields (:34-36, :105). The claim names neither gap. Also, with huitzil_1 as the control part (the first of SET, :182) and its frozen verdict DIVERGES (move_parity.tsv:20), the no-translation control reports DEAD (:195), and unpinned-level reports FIRED even though it tested nothing, because it fires whenever the frozen verdict is not IDENTICAL (:203-207).
Q5: N-A — The packet attributes no decision, approval or ruling to the maintainer or anyone else. "flavor_default becomes 0x01 ... frozen as M19" is the working agent's own action.
VERDICT: VIOLATED

Q1: VIOLATED — The claim says "Not tested: nothing further", but the artifacts show several untested premises. (1) The gate cannot apply the claimed poke. pokes_for (audit_move_parity.sh.txt:86-103) only builds the rig pokes, the pick at ff8782, the level at ff8116 and the RNG at ff80d4. It has nothing for ff87c2, and FIELDS (line 105) never samples ff87c2. So the "poked to 0x01 ... 6851/6851" run is not something this gate produced. (2) The frozen verdict for the tenant is "huitzil_1	DIVERGES	Jump [8]+21f" (move_parity.tsv:20), and parts 2-10 all read DIVERGES (lines 21-29). Only part 1 is claimed, yet all five tracks get rebuilt and frozen. (3) The comparison is not "over the whole part". It starts at the first event (audit_move_parity.sh.txt:25-29, "--first-event" at 135). (4) The gate says per-hit damage, meter gain, hitboxes, projectiles and P2's state are never compared (lines 34-36). (5) None of the named files shows the enumeration of consumers (including "pristine vsavj: zero"). (6) The rebuilt flavor_default=0x01 build was never run before the freeze.
Q2: VIOLATED — The claim draws a conclusion about how Phobos plays: the part-1 moves, including the "Jump [8]+21f" divergence (move_parity.tsv:20), now match native frame for frame. The only evidence is the number "6851/6851". No image or recording appears in any artifact, before or after the conclusion.
Q3: VIOLATED — Both legs load the same rig pokes: "_base ... json.load(open('$_j'))['pokes']" (audit_move_parity.sh.txt:88), used on the native leg (line 98) and on ours (line 101). Those pokes write compared state. Every Huitzil part-1 event is pinned "far" (name_moves.py.txt:448). The pin pokes "f\"{t - 40}:ff8410:{x1:04x}\"" (name_moves.py.txt:658) write P1 X, and X is a compared field: "ff8410:w:x" (audit_move_parity.sh.txt:105). The level pin "ff8116:06" goes on both legs (lines 89, 98, 101), and the level is also compared ("ff8116:b:lvl", line 105). These writes push both legs to the same values in the compared fields every event, so a fault there would make the legs agree.
Q4: VIOLATED — The gate has two controls: unpinned-level, which covers the play-mode speed level, and no-translation, which covers anim-pointer placement (audit_move_parity.sh.txt:4-5). Both run only on the first part in SET, which defaults to donovan_1 (lines 79, 182). No control covers the ways this claim could be wrong. Nothing withholds or perturbs the ff87c2 poke to show that it is what turns huitzil_1 from DIVERGES to IDENTICAL. Nothing shows the gate can see the latch at all: ff87c2 is not in FIELDS (line 105). Nothing checks that the rebuilt default at boot matches the runtime poke. Nothing covers Huitzil parts 2-10. The claim names none of these as a gap.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to the maintainer or any other person.
VERDICT: VIOLATED

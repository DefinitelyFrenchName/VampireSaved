Q1: VIOLATED — The claim does not name one premise it rests on: that the vs2 extract is vs2's own bytes. Both legs are labelled from that extract, and so are b:0x19's "4-node 12-frame loop" and the chain-shape identity behind "not the ported bytes". The gate checks that only OUR data view matches the romset ("cmp -s "$W/wide_da.bin" "$BUILD/verify_data.bin"", tests/audit_victim_parity.sh:130). It never compares `$EX/region_anim.bin` with the decrypted vsav2 (`v2_da.bin`). That view only feeds the census, the wheel and the P1 check (tests/audit_victim_parity.sh:124,133,243). The decoder and chains_identical read "os.path.join(extract_dir, "region_anim.bin")" (tools/victim_parity.py:359, tests/audit_victim_parity.sh:199-206). A second unnamed limit: the claim says the native leg reproduces the frozen lines "exactly", but the check compares only the first 8 labels ("b["path"].split(" ")[:8]", tools/victim_parity.py:319).
Q2: VIOLATED — The claim draws conclusions a player could feel: "FIVE of the six throw hits land one frame earlier on our leg", and Pyron's reaction is "a 4-node 12-frame loop" native against "24 frames held" ours. Both captures were made after those conclusions: "AFTER the RAM measurement that found it" and "AFTER the attribution was drawn from the traces" (build/agent181/captures_sent_14z181.txt:20-21). Both reads are "NOT YET GIVEN".
Q3: VIOLATED — Both legs get the same pokes file, which pins the level and the RNG on every frame: ""%d:ff8116:08" % f for f in range(2000, fr)" and ""%d:ff80d4:0000" % f" (tests/audit_victim_parity.sh:168). The same "$W/${TENANT}_$p.pokes" is passed to vsav2 and to vsavjw (tests/audit_victim_parity.sh:227-228). The level pin changes the compared lines: "reads 13 line(s) differently from the level-8 pin" (build/agent181/victim_parity_verify7.log:35). The pins are still pending, not ruled (STATE.md.lines-201-232:24).
Q4: VIOLATED — One failure mode is neither controlled nor named: the vs2 extract differing from vsav2.zip. Both legs would then be labelled, and our shapes judged, against bytes vs2 does not run. The only data-view control, chains-planted, changes OUR view only (tests/audit_victim_parity.sh:213-214). None of the nine MUST-FIRE controls perturbs the extract, and none perturbs the native-reproduction ("poked") check either (tests/audit_victim_parity.sh:36-44). The claim's NOT-tested list names sprite and link pointers and box tables, but never the extract's fidelity to the romset.
Q5: OK — The one decision credited to the maintainer, matched modes on 2026-09-15, is quoted in their own words: "Force the same speed level on both legs and assert ours == native at NORMAL (level 6) and TURBO (level 8)" (DECISIONS_HISTORY.md.lines-1218-1240:13-14). The pin acceptance is credited to runs 153-155 and stated as pending before the maintainer (STATE.md.lines-201-232:24). Both capture reads are recorded as "NOT YET GIVEN".
VERDICT: VIOLATED

THE PACKET

Decision kind: recommendation
Subject: 14z-181 CLOSE, checklist item 6 — the documentation packet: every finding of the sitting (a)-(ee) has a live home, and a reproducing test where one exists — the second packet, after run 180
Claim (the working agent's sentence): THE DOCUMENTATION PACKET of 14z-181 (checklist item 6): every finding of the sitting has a live home, and a reproducing test where one exists, as the findings table states — (a) the release-claim carriers outlived the release — home `release-inventory` memory + README; test: none, a discipline (measure `gh release` first). (b) the pyron_cosmo poke class was wrong in my packet — home the poke table row; test `test_poke_readback`. (c) an HP pin inside a compared window erased a hit on both legs — home `docs/project/gotchas.md` "A RIG'S SHARED PIN"; test `audit_df_moves` `pin-in-window`. (d) the expiry-wait pin and the second event's x pins — same gotcha; test `sword-poke-planted`. (e) the freeze ran before the checks — home the gate (freeze last); test the `REFUSED FREEZE` path. (f) run 10's sword rows were the old window; the header named ev19 — home the gate header; test: run 155's resolve. (g) a rig tuned at level 8 whiffs at 6 — home gotcha "A RIG IS TUNED AT ONE SPEED LEVEL"; test `audit_victim_parity` `poked` assertion. (h) the 8-label path cut hid the b:0x19 label — home `reaction_map.py` docstring; test `victim_parity.tsv` rows k=4-6. (i) Pyron enters b:0x03 where native enters b:0x19 — home `engine_internals.md` "The OURS leg", **#173**; test `audit_victim_parity`. (j) Victor's a2:0x38 differs by one data frame — home the same bullet + `victim_throw_spans.txt`; test the `attacker=` column. (k) the same-data TSV lists a/b/c only, a2 is a count — home `victim_parity.py` docstring; test the runtime census. (l) 76 Phobos c "chains" are walks into data — home `victim_parity.py` `valid()`; test `chains_identical_huitzil.txt`. (m) "0 once bound to the region" written before the measurement — home STATE row (9), corrected in place; test: none, a discipline. (n) a stale artifact staged before its regeneration — home NEXT_SESSION trap 6 (and the memory `rulecheck-workflow-traps`); test: run 156's resolve. (o) no control on the chain-shape and label checks — home the gate's two new controls; test their modes. (p) the no-translation control of the hitbox gate read DEAD on 0/0 frames — home `hitbox_parity.py` comment; test its mode. (q) Donovan's and Pyron's guard cancels fire on native as Phobos's do; PP gives the LP version — home the move lists; test `test_move_naming`. (r) Demitri's 5HP takes +1 on our build from EVERY tenant — home the attribution class DMG-OPEN; test `audit_move_parity_attribution`. (s) the DMG-OPEN check read the curve row alone — home the tool comment; test: the threshold byte in `ROW_NATIVE`. (t) **the +1 is vsavj's own engine (a legacy victim reads it)** — home `audit_phobos_dmg_residual.sh` header + STATE "Decisions pending"; test its `legacy` rows and `legacy-same`. (u) a gate edited while a measurer ran it — home NEXT_SESSION trap 7; test: none. (v) the 156 spawn wrapper refused at record — home NEXT_SESSION setup note; test: `rulecheck.py record`. (w) Donovan's vs2 EX activation enters a2:0x51/0x52 — home **#174**; test `never_probe.log` (scratch, not promoted: the ticket's rig comes with its inputs). (x) the throw charge absorbed a timing difference of any size — home `victim_parity.py` `explained()` and the gate header (Donovan's stand follows the release, Phobos's is fixed from the grab); test `timing-planted`. (y) the data view the chain check reads was assumed the romset's — home the gate's decrypt-and-cmp step; test: its FAIL branch (untested: no build with a stale view exists). (z) the vs2 extract both legs are labelled from was assumed vsav2's own — home the gate's per-tenant extract check; test `extract-planted`. (aa) the column on an airborne victim takes the air stager's class 7 identically on both legs — home `audit_column_shock.sh` 1c; test `air-marker`. (bb) RETRACTED and replaced by (cc): the trap dome does NOT connect with the jumping victim in the air — he clears it and is hit on the LANDING frame, ground path — home `audit_trap_airborne.sh`, the residue **#175**; test `air-class` + `grounded-hit`. (cc) THE GROUND IS y = 40: an airborne assertion is `y > 40`, a hit reading y = 40 is a hit on the ground or on the landing frame; my `y > 0` called a landed victim "the falling victim at height 40" and a six-timing sweep whose rows all read 40 "confirmed" it — the rule-checker (run 164) found it from a sweep row where the jump was pressed after the hit — home `docs/project/gotchas.md` "THE GROUND IS y = 40"; test `audit_trap_airborne` `grounded-hit`. (dd) an air-throw rig's outcome follows the victim's height at the grab frame in every swept row, and P2's two-frame jump lead is the rig knob that sets it at every phase; cause vs co-varying state not isolated, a same-frame tick race the hypothesis (#169) — home `docs/project/gotchas.md` "AN AIR-THROW RIG IS DECIDED BY THE VICTIM'S JUMP LEAD" + `name_moves.py` air_throw; test `test_move_naming` pyron (both lines a2:0x23). (ee) a near event whose previous event did not knock P2 left ends at the right wall (P1 pushes at ~2 px/frame from a pin the screen edge clamped to 664; a camera poke is overwritten in four frames); pyron_3's event 5, named "far (whiff)" by the rig, in fact connects — home `name_moves.py` "WHERE A NEAR EVENT REALLY STANDS" + `build/agent181/near_event_geometry_14z181.txt` (a per-event table from the traces); test: none yet (a rig fact; the "far (whiff)" name is a wording to correct at pyron_3's next re-freeze) — and each home named exists in the tree at HEAD carrying the finding; the retraction procedure's grep shows the live tree carries 'the falling victim at height 40' only as QUOTED RETRACTIONS (the gotcha, the trap gate's header, the STATE row), never as a claim, and the #169 retractions of runs 168-179 were grepped the same way. NOT tested: whether a home's wording survives the next rollover (STATE rows roll to STATE_HISTORY verbatim, the documents stay); that a named home is the RIGHT SECTION of its document is checked by my reading, not by a tool (tools/close_findings.py sees addresses, not findings: 0 checked, its clean result cited as what it is and never as completeness); FOUR findings name no reproducing test — (a) and (m) are disciplines, (u) the running-gate trap, (ee) the near-event geometry ('none yet') — (y)'s FAIL branch is untested for want of a stale build, and (f) and (n) cite past rule-checker resolves as their tests; (dd) and (ee) are RAM findings with no capture (the maintainer's read is theirs).
Artifacts (read every one, in full):
  - STATE.md.lines-72-100 (lines 72-100 of STATE.md)
  - build/agent181/close_findings_14z181.txt
  - docs/project/gotchas.md.lines-1-130 (lines 1-130 of docs/project/gotchas.md)
  - docs/game/engine_internals.md.lines-990-1020 (lines 990-1020 of docs/game/engine_internals.md)
  - tools/name_moves.py.lines-160-220 (lines 160-220 of tools/name_moves.py)
  - tools/name_moves.py.lines-735-795 (lines 735-795 of tools/name_moves.py)
  - docs/NEXT_SESSION.md
  - build/agent181/state_14z181_close.diff
  - tests/audit_trap_airborne.sh.lines-1-40 (lines 1-40 of tests/audit_trap_airborne.sh)
  - tests/audit_victim_parity.sh.lines-1-60 (lines 1-60 of tests/audit_victim_parity.sh)
  - docs/project/tickets.tsv.lines-74-90 (lines 74-90 of docs/project/tickets.tsv)
  - tests/rulecheck/runs/2026-09-25-180/verdict_real.txt
  - build/agent181/resolve180.txt

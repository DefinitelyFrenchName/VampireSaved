Q1: VIOLATED — The claim says the SELF leg pokes "a DIFFERENT id (a changed-value write)", so the inertness of the poke is "tested directly on the same write the poked leg performs". The gate does something else. build/rulecheck/2026-09-25-181/b/audit_forced_pick_fidelity.sh.txt:26-28 says "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". Line 135 builds the SELF poke from `$_id="$(row_id "$_row")"`, the tenant's own id, and line 138 gives that same pick poke to the self leg. So the poke that changes the value (0x10/0x11 over Donovan's 0x13) is never tested for inertness. The claim presents this as tested, which is a false stated basis. A lesser inaccuracy: the claim says position pins land "before each event", but DONOVAN part 1 events have no "far"/"near" field (name_moves.py.txt:206-224, 681), so the donovan-self row has no position pins.
Q2: N-A — The claim draws no conclusion about how the character plays: "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — The legs do share inputs. These are the rig's pokes (audit_forced_pick_fidelity.sh.txt:131-132), the level pin at $FF8116 from 2000 and the RNG pin at $FF80D4 from 2363 (lines 133-134), and the position and HP pins (name_moves.py.txt:687 at t-40 with FIRST_EVENT 2600, so from 2560; line 699 from 2550). But none writes the P1 block inside the pre-match window of 1501-2362 (audit_forced_pick_fidelity.sh.txt:180). The one pin that does land in the P1 block, P1's X at $FF8410 (name_moves.py.txt:687), starts at 2560, and the claim names its effect on the play frame.
Q4: VIOLATED — Nothing controls, and the claim never names, the risk that a poke which CHANGES the id at $FF8782 (inside the P1 block, +0x382) itself writes or triggers the three bytes. If it did, those bytes could not be charged to the confirm. Both controls that could catch this only write a value the id already holds. SELF is described at audit_forced_pick_fidelity.sh.txt:26-28 and built at line 135. donovan-self pokes 0x13 over Donovan's own 0x13 (lines 48-51, 87). The same-leg control (lines 308-316) and the wrong-cursor control (lines 317-322) cover a vanished difference and a wrong real pick, not this. The claim asserts the gap is covered ("tested directly on the same write the poked leg performs"), so it does not name it as open.
Q5: N-A — The packet credits no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

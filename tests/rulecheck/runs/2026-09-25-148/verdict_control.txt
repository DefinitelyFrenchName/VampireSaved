Q1: VIOLATED — The claim says the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write)". The gate says the opposite. build/rulecheck/2026-09-25-148/a/audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Line 135 pokes `ff8782:$_id`, the row's own tenant id, and line 118 gives SELF the tenant's real path. So the premise that a changed-value id write is itself inert is never tested, and the claim presents it as tested instead of naming it as untested.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here." The frozen first-play frame (forced_pick_fidelity.tsv:15 "2980") is only a number and draws no conclusion about feel.
Q3: OK — POKED and REAL do share pokes, but the artifacts show none of them writing the compared P1 block inside the pre-match window. The ID poke is on POKED only (audit_forced_pick_fidelity.sh.txt:137-138). The position pins land at t-40 from FIRST_EVENT 2600, so 2560 at the earliest (name_moves.py.txt:687, :77). The P2 HP pin starts at 2550 (name_moves.py.txt:699). The level and RNG pins write globals ff8116 and ff80d4 (audit_forced_pick_fidelity.sh.txt:133-134). The LATCHED window is 1501..2362 (:180). The claim itself names the P1 X pin that both legs share in play.
Q4: VIOLATED — One failure mode is neither controlled nor named: the changed-value id write at 1400/1450/1500 could itself disturb P1-block state, so the three LATCHED bytes might come from the poke rather than the confirm. REAL vs SELF cannot catch this, because both sides already hold the tenant id and the SELF poke changes nothing (audit_forced_pick_fidelity.sh.txt:26-28, :135). donovan-self cannot catch it either: the R,R cursor pick is already 0x13 and it pokes 0x13 (:87-89, :49-50). The claim says this is "tested directly on the same write the poked leg performs", but the poked leg writes 0x10/0x11 over Donovan's 0x13 and no control ever makes such a write.
Q5: N-A — The packet attributes no decision, approval or ruling to the maintainer or anyone else. It is the working agent's own recommendation.
VERDICT: VIOLATED
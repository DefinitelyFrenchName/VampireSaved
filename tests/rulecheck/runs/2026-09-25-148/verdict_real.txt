Q1: VIOLATED — The claim rests on "the beams reached P2 on both legs in every new event". The gate does not test which attack produced a hit: tests/audit_df_moves.sh:16-17 says "Not shown: that the named move came out AS that move", and :66 repeats it. The claim does not list this among its untested premises, and ev10 argues against it. build/agent181/df_moves_contact_run1.log:44 shows ev10 "native=[(5, 4)]@[13] m1=[18]". P1's 5LP press is at +10 (tests/audit_df_moves.sh:114, "(10, 13, \"1\")"), so the hit lands 3 frames after the press. At the same x=640, the grounded 5LP beam lands 6 frames after its press (log:41 "@[6, 7]", recipe "(0, 3, \"1\")"). Every other beam hit does 2 damage and gives native m1=[] (log:41-43, tests/expected/df_moves.tsv:32-33). A second untested premise is also not named: that the build was merged-m19. The log never prints the build, the default is "BUILD=\"${BUILD:-build/m3b_merged27}\"" (tests/audit_df_moves.sh:80), and build/agent181/clone_beam_contact_run1.log:1 reads "ours: m3b_merged27". No artifact links m3b_merged27 to merged-m19.
Q2: VIOLATED — The claim concludes that hits landed and were equal ("the beams reached P2 on both legs", "its hit (5, class 4) equal"). The only evidence for the four contact events is field-trace numbers (build/agent181/df_moves_contact_run1.log:41-44). None of the named artifacts contains an image or recording of these events, or shows one put before the maintainer. The gate's only captures are Pyron's palettes and Donovan's sword (tests/audit_df_moves.sh:310-335, pyron_dfx1/donovan_dfx1 only).
Q3: VIOLATED — Both legs share the P2 HP pin, which writes the compared field inside two of the four new windows. It is "[f\"{f}:ff8850:01200120\" for f in range(first - 50, end, HP_PIN_EVERY)]" (tools/name_moves.py:766, HP_PIN_EVERY=400 at :126). tests/audit_df_moves.sh:162 feeds that same poke list to both legs. ff8850 is the p2hp field ("ff8850:w:p2hp", :158), and :204 counts hits as p2hp decreases over [lo+1, hi). For huitzil_dfx2, first=2600, so pins land at 3350 (ev4 +35, window 3315-3535) and 4550 (ev10 +25, window 4525-4745). A hit that one leg takes on a pin frame is erased, and the two legs then read as agreeing.
Q4: VIOLATED — Nothing controls for this failure: a hit counted as "the beam reached P2" that came from some other contact (ev10: +3 after the press, 5 damage, native gauge 18; see Q1). All six controls perturb other things: mode-lost and idle-leg alter the first compared event (donovan_dfx1), hit-dropped drops our first hit on the first row (tests/audit_df_moves.sh:237, :281, :459), and the form controls touch only the pyron/donovan form rows. None identifies the attack behind a hit, and the claim does not name this gap.
Q5: VIOLATED — The no-gauge rule is properly sourced: the maintainer's own words are quoted, *"I see, it makes sense that you can't build meter during DF"* (tests/audit_df_moves.sh:58-59). The fold that the packet's subject calls "folded #109" is not. The new gate text attributes it to a person: "the maintainer folded into #136 on 2026-09-15 — \"P2 jumped into the beam band, comparing damage with native\"" (build/agent181/df_moves_gate_14z181.diff:20-21). The quoted words are the #109 description in build/agent181/issue_136.md:8. The only record of the fold is "(maintainer-ruled 2026-09-15, session 14z-157)" at issue_136.md:6. That comment comes from the owner account, which elsewhere refers to the maintainer in the third person (issue_136.md:54), so it is a record of a ruling, not the maintainer's own statement.
VERDICT: VIOLATED
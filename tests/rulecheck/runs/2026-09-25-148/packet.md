THE PACKET

Decision kind: expectation
Subject: 14z-181 #136, the folded #109 clone-beam CONTACT leg: re-freezing tests/expected/df_moves.tsv with the four new rows of huitzil_dfx2 — Phobos's 5LP/5HP inside Dark Force with P2 grounded at a near pin (640) and P2 jumping into the beam band, far and near — measured by tests/audit_df_moves.sh's new CONTACT group on merged-m19 (ours, Change by P+K) against native vsav2 (the vs2 EX install)
Claim (the working agent's sentence): The four new rows are frozen AS MEASURED by the same gate, the same VOID guards (both legs in the mode at every event and hit, both legs acting) and the same six controls as the existing 29 events, which came out byte-identical (the run's diff against the frozen file carries only the four new lines); the beams reached P2 on both legs in every new event (two hits of 2 grounded near for both 5LP and 5HP, one class-4 airborne hit far), and the one DIFFER row, P2 jumping near, differs in P1's gauge only (native 18, ours none) — the class the ruled no-gauge-in-Dark-Force rule already accounts for on 21 frozen rows — with its hit (5, class 4) equal; NOT tested: the hit FRAMES, which the gate prints but does not compare by its 14z-168 design (ev4's first hit lands at +6 natively and +7 on ours, the same one-frame phase dfx1's 5LP already shows at +8/+9), whether P2's jump puts him at the same height in the band on both legs (only the hit's class 4 shows he was airborne, on both), whether the near pin's P2 x poke (640) landed at the same x on both legs (no position is sampled by this gate), and the instrument that showed the hand rigs df/103-104 mistimed for a compared run — a scratch gate deleted before commit, whose log is the artifact.
Artifacts (read every one, in full):
  - tests/audit_df_moves.sh
  - build/agent181/df_moves_gate_14z181.diff
  - build/agent181/df_moves_contact_run1.log
  - tests/expected/df_moves.tsv
  - build/agent181/df_replays_14z181.diff
  - build/agent181/clone_beam_contact_run1.log
  - tools/name_moves.py
  - build/agent181/issue_136.md

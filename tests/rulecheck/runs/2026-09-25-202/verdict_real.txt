Q1: VIOLATED — The claim says the banner "names no last run and no packet count — the CLOSE row names the last, once", and the banner does defer: "the CLOSE row names the last" (STATE.md.lines-72-101:3). But the 14z-181 section has no CLOSE row. Its table runs from "opened with" to row (13) (STATE.md.lines-72-101:7-25), and "## Session 14z-180" starts at line 27. The checklist is also supposed to run "after the close entry" (STATE.md.lines-46-56:4). So the fix for (ii) depends on a row that is not in the tree, and the claim does not say that row is still to come. Second, "the whole fast documentation gate set is now run after every edit of this packet" has no log among the artifacts; the only support is the resolve's own "all green" (build/agent181/resolve201.txt:1). Third, (l) says "76 Phobos c 'chains' are walks into data", but the test it cites reads "139 chains native, 139 ours, 81 not chains on both (walks into data)" (build/agent181/victim_parity_plain_14z181.log:81).
Q2: VIOLATED — The packet restates conclusions a player could feel: (g) "a rig tuned at level 8 whiffs at 6", (ee) "pyron_3's event 5 ... in fact connects", and the +1 damage in (r)/(t). Every capture went out after its conclusion: "AFTER the RAM measurement that found it, not before" (build/agent181/captures_sent_14z181.txt:20, lines 21-24 alike) and "AFTER the measurement (the naming freeze of a91a555a)" (line 39). The claim itself says "still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t)".
Q3: N-A — The packet concludes where findings live and which tests exist; it draws no two-leg comparison of its own. The one pair it runs is built to disagree: "SELFTEST FAIL: (False, False, True, True) stem=True ticket=False prose=True fail=7" (build/agent181/test_close_tools_mode_build-home-planted.log:8) against "SELFTEST PASS" (build/agent181/test_close_tools_14z181.log:6).
Q4: OK — Each way the claim could fail is either controlled or named. An untracked or build/ home: `build-home-planted` fires (build/agent181/test_close_tools_14z181.log:14), and the live `--newest` row reads "FAIL 0" (line 8). A grep that does not reach the tree, or a gone wording that comes back: `unreachable-control` "(0 < 1)", `reach-threshold` "(2 < 3)" and `gone-reintroduced` fire (lines 10-12; the mode logs at line 4). A dropped case-fold: `case-fold-dropped` fires (line 13). A crash is not read as a fire: "REFUSED ... exit code: 3" (build/agent181/close_tools_crash_probe_14z181.log:5-6). The claim names the rest as its own reading: section homes, whether a test exercises its finding, carrier figures, pattern width, and whether the table and the 32 patterns are complete.
Q5: OK — Every attribution to the maintainer quotes their own words. #173: "yes, identical." (DECISIONS_HISTORY.md.lines-28-48:9). (j): "identical and validated otherwise" (line 10). #169's sheets: "looks identical. I'll double check the air throw on my end but everything looks good" (line 14). The first read: "correct" (line 6). The model switch: "I had to switch to Opus 5.5. Resume the work." (STATE.md.lines-72-101:3).
VERDICT: VIOLATED

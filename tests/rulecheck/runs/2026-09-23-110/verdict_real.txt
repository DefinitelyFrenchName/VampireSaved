Q1: VIOLATED — Three premises are untested and the claim does not name them. (1) P3(d) rests on "P3 sees the prompt: A8's input carries it" (build/agent172/s4_proposal.md:54), but no leg checks that. A8 passes on a denial count and on no worker running (tools/agent/probe_agents.sh:170-171). The hook logs `input` (probe_agents.sh:51-52), but nothing checks it for a `prompt` field. (2) A10's report arrival is not actually tested; see Q3/Q4. (3) The general-purpose half of A9 ran under a Haiku parent only (probe_agents.sh:183). Leg k tests only `w-nomodel` (probe_agents.sh:184). Yet P3(c) cites "A9, both for an undefined general-purpose call and for a definition with no model" (s4_proposal.md:53), and docs/project/agent_architecture_scope.md:161 says "(A9, both parents)".
Q2: N-A — No conclusion about behaviour a player could feel is drawn. The subject is agent tooling (build/agent172/s4_proposal.md:1).
Q3: VIOLATED — A10's "report delivered" check matches the worker's report against the orchestrator's transcript, and both sides share the spec token "A-low". The orchestrator's own prompt writes "echo A-low" into its transcript (tools/agent/probe_agents.sh:121). Every user record is collected into main_text (probe_agents.sh:203-204). The test is "any(report.strip()[:40] in x or \"A-low\" in x for x in main_text)" (probe_agents.sh:222). So the prompt record alone satisfies it, whether or not the report ever arrived.
Q4: VIOLATED — Two failure modes are neither controlled nor named by the claim. (a) "The worker's report never reaches the orchestrator's transcript" goes undetected, because A10's check passes on the orchestrator's own prompt (tools/agent/probe_agents.sh:121, 203-204, 222). P5 depends on that report ("its REPORT", build/agent172/s4_proposal.md:67). The claim names only "A10 does not assert which delivery form a foreground report takes". (b) "A general-purpose call defaults to Haiku whatever its caller runs on" is indistinguishable from "it inherits the caller's model", because the general-purpose leg has no Sonnet-parent leg beside it (probe_agents.sh:183-185).
Q5: OK — The caps and specs are quoted from the maintainer's #172 ask: "they should be at most opus-class or maybe even sonnet if context is small and specs clear and precise" and "max acceptable effort for fable is high, for Opus and Sonnet is xHigh" (docs/project/agent_architecture_scope.md:38-43). Item 4 is explicitly not claimed as ruled (build/agent172/s4_proposal.md:8-12). The pre_push install is quoted: "I'll install it" (DECISIONS_HISTORY.md:42).
VERDICT: VIOLATED

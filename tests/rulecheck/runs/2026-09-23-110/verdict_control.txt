Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the artifacts show gaps it does not name. audit_move_parity.sh.txt:25 says "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor". Line 105 compares only a fixed subset of fields (node, cnt, seq, sub, stock, x, y, p1hp, id, df, face, lvl), and RAM:$FF87C2 is not one of them. Lines 34-36 say "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters" and that P2 "is never compared". Three more premises go unnamed. Only huitzil_1 was run, while huitzil_2..10 are all frozen DIVERGES (move_parity.tsv:21-29). A runtime poke is assumed to equal a build-time flavor_default. The gate's pokes_for (lines 86-103) has no $FF87C2 poke, so the poked run is not the gate as written.
Q2: VIOLATED — The claim concludes about how Phobos plays: part 1 covers Jump, Air Dash and Float per name_moves.py.txt:448-459, and it is frozen as "huitzil_1	DIVERGES	Jump [8]+21f" at move_parity.tsv:20. The only evidence given is a number, "6851/6851 frames identical". No artifact shows an image or recording put before the maintainer.
Q3: VIOLATED — Both legs get the same rig pokes, and those pokes write state that is compared. name_moves.py.txt:658 pokes "ff8410" (P1 x) 40 frames before every "far" event, and every huitzil part-1 event is "far" (name_moves.py.txt:448). audit_move_parity.sh.txt:98/101 pass the same $_base and $_lvl pokes to both legs, including "ff8116:06". FIELDS at line 105 compares "ff8410:w:x" and "ff8116:b:lvl". Because the shared pin resets x before each event, the two legs would agree on x even where a flavor difference had moved it.
Q4: VIOLATED — The gate has two controls, "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:4-5). One covers the level pin and the other the pointer translation; neither covers the flavor byte. Two failure modes are neither controlled nor named by the claim. First, whether the build-time flavor_default reproduces the poke's effect: nothing checks that $FF87C2 holds 0x01 at the frames that matter, and the byte is not in FIELDS (line 105). Second, whether flavor-dependent state outside FIELDS still differs. Also, with huitzil_1 as the control part, line 195 reports "CONTROL DEAD ... is not frozen IDENTICAL", and line 178 would fail an IDENTICAL result against the frozen DIVERGES row. The gate as written cannot have passed the claimed run.
Q5: N-A — The claim attributes no decision, approval or ruling to anyone.
VERDICT: VIOLATED

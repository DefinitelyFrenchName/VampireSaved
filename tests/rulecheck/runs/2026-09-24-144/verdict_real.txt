Q1: VIOLATED — The claim says the only source of over-reporting is loops over rig parts. That is not the only untested premise. tools/audit_poke_readback.py:121-126 takes every literal `frame:addr:hex` token anywhere in a gate's non-comment text, and :153-156 joins those pokes to any sample with no check that both belong to the same leg or run. At least two of the 74 rows look like control-mode plants: tests/expected/poke_readback.tsv:12 (audit_defense_row_reads ff8b82, one poke at 2680) matches tests/expected/PROVENANCE.md:133 "the `planted-flavor` run (P2's `+0x382` poked to 0x01 before the first hit) must report a mismatch", and poke_readback.tsv:31 (audit_reaction_class_live ff8454, one poke at 3000) matches PROVENANCE.md:132 "the `planted-38` run must report its poked 0x38". That these two tokens sit in control legs is inferred from PROVENANCE, not read in either gate. Pokes read back on purpose count as "the rig's", so "the 74 is a floor on the true count" is not supported. The claim also does not name docs/project/gate_qualification_scope.md:332-333 "Q6 compares addresses, not dataflow". Its "first census ... found 72" has no artifact: build/gatequal180/q6_evidence_14z180.txt:1 records only the run "after the rig-poke width fix".
Q2: N-A — The claim draws no conclusion about how anything plays. STATE.md:210 says only "Nothing a player feels changes by the ruling — what changes is what a gate CLAIMS".
Q3: OK — The compared table is written from the same census (tests/test_poke_readback.sh:75 appends `new` rows from the tool's output), so the two sides share one tool. But the claim infers only that the table equals the derived set, and says "the 74 is a floor on the true count, not the count". It draws no correctness conclusion from their agreement.
Q4: VIOLATED — The controls cover two failures: known-case-blind, a removed sample (tests/test_poke_readback.sh:45-48, 91-94), and unclassified-finding, a planted literal poke-and-sample pair (:49-55, 95-97). Neither covers a poke from a control or planted leg, or from a different leg, being joined to a sample in the measuring leg (tools/audit_poke_readback.py:121-126, 153-156; PROVENANCE.md:132-133 against poke_readback.tsv:12, :31). The claim does not name this failure either. It names loops as the only over-report source.
Q5: OK — build/gatequal180/ruling_killshread_2026-09-22.txt:16-18 quotes the ruling verbatim: "Option 1 and ticket to qualify all gates in the future ...". Line 12 gives option (1) as "re-freeze as-is and record what the column now is". That is the maintainer's own statement behind the one READS-BACK row.
VERDICT: VIOLATED

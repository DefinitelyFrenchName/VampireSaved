Q1: VIOLATED — The claim rests on things no named artifact shows, and it does not name them as untested. (a) It says Pyron's "b:0x19 (a 4-node 12-frame loop)" against "b:0x03 (12 nodes, 24 frames held)" were drawn from "the chain decodes". Those shapes appear only as prose in build/agent181/issue_173.md:30-31 and tests/audit_victim_parity.sh:67-68. build/agent181/chains_identical_pyron.txt:3 gives table counts only, no per-chain nodes or durations. (b) The chain-shape identity reads "$BUILD/verify_data.bin" as our build (tests/audit_victim_parity.sh:193,201), but our leg runs "$BUILD/rompath" vsavjw (:211). No artifact shows the two are the same bytes, and the claim never names this premise. (c) The family (1) count assumes that a differing Victor chain running inside an event caused that row's DIFFER. This is unstated, and the artifacts contradict it (see Q4).
Q2: VIOLATED — The claim draws conclusions a player could feel: "FIVE of the six throw hits land one frame earlier on our leg", and Pyron's heavy-hit reaction runs a different chain ("12-frame loop" vs "24 frames held"). Both captures were made after those conclusions. build/agent181/captures_sent_14z181.txt:20 says "AFTER the RAM measurement that found it", and :21 says "AFTER the attribution was drawn from the traces". The maintainer's read is "NOT YET GIVEN".
Q3: VIOLATED — Both legs use one pokes file carrying the per-frame RNG pin: "%d:ff80d4:0000" % f for f in range(2363, fr) (tests/audit_victim_parity.sh:151), passed to native :210 and ours :211 alike. The RNG selects the compared state: "picks P1-first or P2-first on every pass from bit 0 of the engine RNG" (DECISIONS_HISTORY.md.lines-1218-1240:22-23). STATE.md.lines-201-230:23 says "a port defect in the RNG's ADVANCE would be reset away on both legs and read as agreement", and that item is still under "Decisions pending", not ruled.
Q4: VIOLATED — The gate's attribution is by co-occurrence only. attacker_for stamps any row whose event window overlaps a differing-chain span (tools/victim_parity.py:113-123). Nothing checks which side caused the difference. The artifacts show it absorbing a tenant-side difference. On tests/expected/victim_parity.tsv:32,33,35,36,37, Pyron's throw rows read "DIFFER(path)" with cls, frz and len equal: the only difference is b:0x19 vs b:0x03. They are still stamped "attacker=a2:0x38", and the claim counts them among family (1)'s "15 rows are Victor's throw". Only 10 rows carry a len or frz difference that could be Victor's. No control plants a tenant-side difference inside an attributed event. The claim's named gap covers only the opposite direction ("a differing chain that changed a LATER event's outcome without running inside it would not be caught").
Q5: OK — The one ruling credited to the maintainer is quoted in their own words: "Matched modes, both (Recommended)" ... "Force the same speed level on both legs" (DECISIONS_HISTORY.md.lines-1218-1240:12-14). The claim credits the pin acceptance to runs 153-155, not to the maintainer, says it is pending before them (STATE.md.lines-201-230:23), calls level 8 "a design choice of mine", and states both capture reads as not given.
VERDICT: VIOLATED

Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the gate lists what it leaves out: build/rulecheck/2026-09-25-158/b/audit_move_parity.sh.txt:34-36 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters ... P2 ... state is never compared". The window is not the whole part either: :25 "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor". Only the twelve FIELDS at :105 are compared, and $FF87C2 is not one of them. No poke of ff87c2 appears in pokes_for (:86-103), so this gate cannot have produced the poked run. Its frozen row reads build/rulecheck/2026-09-25-158/b/move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f", not 6851/6851. No artifact shows the "every consumer ... enumerated (pristine vsavj: zero)" census.
Q2: VIOLATED — The conclusion is that Phobos's part-1 moves play the same as native, a behaviour a player could feel. Part 1 includes the jumps, air dashes and floats (build/rulecheck/2026-09-25-158/b/name_moves.py.txt:448-460, e.g. "Float [j.8 hold]"). The only evidence given is the number "6851/6851 frames identical". No artifact shows an image or recording put before the maintainer.
Q3: VIOLATED — Both legs get the same pokes, and those pokes write fields that are compared. The level is poked every frame on both legs: build/rulecheck/2026-09-25-158/b/audit_move_parity.sh.txt:89 "f'{f}:ff8116:06' for f in range(2000,$_fr)", used at :98 (native) and :101 (ours), and it is a compared field at :105 "ff8116:b:lvl". Since vsav2 and vsavj differ exactly in play mode (:17-19), a flavor-driven speed difference would be pinned into agreement. The x position is re-poked before every far event on both legs (name_moves.py.txt:658 "f\"{t - 40}:ff8410:{x1:04x}\"") and compared as "ff8410:w:x" (:105). The tenant id is poked on both legs (name_moves.py.txt:672 "ff8782:{tid}" in _base, plus audit_move_parity.sh.txt:92) and compared as "ff8782:b:id". Both legs also replay the same .rpl (:110, :117) and pin the same RNG (:90).
Q4: VIOLATED — The gate has two controls. unpinned-level (:199-208) covers the play-mode difference; no-translation (:185-197) covers comparing the raw node pointer. Neither fires if the flavor byte's effect falls outside the compared fields or window: before the first event, in P2, damage, meter, hitboxes or projectiles. Nothing checks that the ff87c2 poke actually landed, because ff87c2 is not in FIELDS (:105). The claim names none of these gaps. The control part is also huitzil_1: with PARTS=huitzil_1 the control is the first part (:182), and its frozen verdict is DIVERGES (move_parity.tsv:20), so the gate's own branch applies: :195 "CONTROL DEAD: no-translation — the control part $CTL is not frozen IDENTICAL, so the control cannot discriminate".
Q5: N-A — The claim does not attribute any decision, approval or ruling to the maintainer or anyone else. "flavor_default becomes 0x01" is the working agent's own action.
VERDICT: VIOLATED

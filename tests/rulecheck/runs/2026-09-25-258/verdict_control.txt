Q1: VIOLATED — the claim says "Not tested: nothing further", but the artifacts show gaps it does not name. audit_move_parity.sh.txt:34-36 says "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters ... P2 is Victor, a legacy character, so his state is never compared". Part 1 is only movement, normals, 6x, air dash and float (name_moves.py.txt:448-460). The specials, meter and guard-cancel rigs are huitzil_2-10 (name_moves.py.txt:461-551), and the claim leaves them untested without saying so. The gate also has no FF87C2 poke anywhere in pokes_for (audit_move_parity.sh.txt:86-103). It emits per-part verdicts, not frame counts (lines 133-135, 172-179). Its frozen row is "huitzil_1	DIVERGES	Jump [8]+21f" (move_parity.tsv:20). So neither the "6851/6851" figure nor the poked run appears in the named artifacts.
Q2: VIOLATED — the claim concludes that Phobos's moves play the same as native ("6851/6851 frames identical to native", then a build and freeze). It rests only on a number. None of the three artifacts shows a capture (an image or a recording) put before the maintainer before that conclusion.
Q3: VIOLATED — both legs share the rig's pokes, and those pokes write compared fields. Every huitzil part-1 event is pinned "far" (name_moves.py.txt:448), and gen writes "{t - 40}:ff8410:{x1:04x}" on P1's X (name_moves.py.txt:658). Those pokes go into the json "pokes" that pokes_for applies to both legs as _base (audit_move_parity.sh.txt:88, 98, 101). ff8410 is the compared "ff8410:w:x" field (audit_move_parity.sh.txt:105). The shared ff8116:06 level pin (lines 89, 98, 101) and the ff8782 id poke (name_moves.py.txt:672) also write the compared "lvl" and "id" fields. A defect there would make the two legs agree.
Q4: VIOLATED — the gate has two controls, unpinned-level and no-translation (audit_move_parity.sh.txt:4-5, 185-208). Neither would fire on the ways this claim could be wrong. First, the FF87C2=0x01 poke might not take effect or hold on ours: ff87c2 is not in FIELDS (line 105), and no control withholds the poke and requires a divergence. Second, the latch might be read in states part 1 never enters (specials, ES and DF in huitzil_2-10). The claim names neither failure mode.
Q5: N-A — the packet attributes no decision, approval, ruling or closure to the maintainer or any other person.
VERDICT: VIOLATED

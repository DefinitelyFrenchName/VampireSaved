Q1: VIOLATED — The claim says "the tests of (b)'s meter half, (d), (g), (m), (n), (u) and (x) run the FIXED or current state … they fail on a regression". The artifacts show (m)'s only gate is pinned to the old build: build/agent183/packet_homes_14z183.txt:29 "PINNED TO THE M19 BUILD", and build/agent183/retraction_greps_14z183.txt:43 `build="${build:-build/m3b_merged27}" # pinned to merged-m19`. STATE.md:94 (m) reads "test: none reproduces the M20 divergence", yet the claim's "NOT tested at all" list leaves (m) out. (r) and (l) fall into none of the claim's three test categories. (r)'s test line (packet_homes_14z183.txt:41, tests/test_meter_gain.sh:87 "LE 0x0d hit_jump") runs the current Lei-Lei legs and cannot replay the 14z-182 record's Lilith error; this is the same kind of gap the claim does name for (y)'s mistake. The packet's own header (packet_homes_14z183.txt:1) says "Findings (i), (k), (q) name no test", but STATE.md:94 shows (h), (s), (v), (w) and (z) also read "test: none".
Q2: N-A — The claim concludes only where each finding (a)-(z) is documented and which tests exist ("every finding (a)-(z) … has a live home in a tracked file"). It draws no new conclusion about how a move plays, its timing or a hit.
Q3: OK — No two legs share a writer. build/agent183/packet_verify.py:9 re-opens each file:line from the tree on its own ("open(f, encoding=\"utf-8\").read().split(\"\\n\")[int(n) - 1]"), and no artifact shows anything that writes both the quote and the tree line.
Q4: VIOLATED — The claim's "every quote re-opened from the tree matches its file:line" rests on build/agent183/packet_verify.py, which has no plant. build/agent183/packet_quotes_verified.txt:55 shows only the clean "0 mismatch" result, and no run shows the script ever printing MISMATCH. classing_cover.py got a plant (build/agent183/classing_coverage.txt:24-26); this checker did not. The claim names only the empty-quote weakness, not the missing proof that the checker can fail. Separately, the regression check the claim credits to (m) comes from a gate pinned to merged-m19 (retraction_greps_14z183.txt:43). That gate looks at M19, not the M20 divergence the finding is about, so it cannot fire on an M20 regression, and the claim does not name this.
Q5: OK — The claim attributes no ruling itself. The rulings in the table are quoted in the maintainer's own words: packet_homes_14z183.txt:29 "(maintainer-ruled 2026-09-26, 14z-183: \"Pin it to M19", and packet_homes_14z183.txt:51 "answered *\"Land it (Recommended)\"*".
VERDICT: VIOLATED

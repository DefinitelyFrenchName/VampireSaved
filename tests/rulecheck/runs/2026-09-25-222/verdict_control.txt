Q1: VIOLATED — The claim says "Not tested: nothing further", but the gate lists its own gaps: audit_move_parity.sh.txt:34-36 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters ... P2 is Victor ... his state is never compared". A flavor latch could change any of these. The claim also rests on premises it never names as untested. (a) The "ours poked to 0x01" run: pokes_for (audit_move_parity.sh.txt:86-103) has no $FF87C2 poke, and FIELDS (line 105) never samples ff87c2. (b) A 6851/6851 frame count: the gate emits only a per-part verdict (move_parity.tsv:6, "Columns: part, verdict, first divergent event"). The only frozen Phobos row is move_parity.tsv:20 "huitzil_1 DIVERGES Jump [8]+21f", so an IDENTICAL run would fail line 178 "expected [$want] measured [$got]". (c) The consumer enumeration ("pristine vsavj: zero"): no artifact shows it. (d) Parts 2-10 of huitzil: not run. (e) The rebuilt five tracks: the claim compares a runtime poke, not a build with flavor_default 0x01.
Q2: VIOLATED — Part 1 is movement: jumps, air dash and float (name_moves.py.txt:448-460). The frozen divergence is "huitzil_1 DIVERGES Jump [8]+21f" (move_parity.tsv:20). So "reads 6851/6851 frames identical to native" is a conclusion about how Phobos's jump and float play. It rests on a number alone, and none of the artifacts shows a capture put before the maintainer.
Q3: VIOLATED — Both legs receive the rig's own pokes (audit_move_parity.sh.txt:88 `_base`, used on both legs at lines 98 and 101). Every part-1 event is "far"-pinned (name_moves.py.txt:448), so gen writes `f"{t - 40}:ff8410:{x1:04x}"` (name_moves.py.txt:658, 675) into those pokes. ff8410 is the compared x field ("ff8410:w:x", audit_move_parity.sh.txt:105), so the same poke resets P1's x on both legs before each event and hides any x drift across it. Likewise, the level pin "ff8116:06" goes to both legs (lines 89, 98, 101) and is itself compared ("ff8116:b:lvl", line 105). The tenant-id poke ff8782 (lines 92, 101; name_moves.py.txt:672) writes the compared field "ff8782:b:id".
Q4: VIOLATED — The gate has only two controls: "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:4-5, 61). Neither fires if the flavor byte changes damage, meter, hitboxes, projectiles or P2 state (lines 34-36), or if it changes Phobos in parts 2-10. Neither fires if a runtime poke of $FF87C2 behaves differently from a build with flavor_default 0x01, and the claim names none of these as untested. On a huitzil_1-only run the no-translation control would itself be dead: line 195 "CONTROL DEAD: no-translation — the control part $CTL is not frozen IDENTICAL", because move_parity.tsv:20 freezes it DIVERGES.
Q5: N-A — The packet attributes no decision, approval or ruling to any person.
VERDICT: VIOLATED

Q1: OK — the claim lists what it did not test: one character, move, spacing and press window; the level/RNG/x pins shared by both legs; the solo track; slot and node addresses; the engine_internals encoding and the zero-id rule; anim_nodes.py; cps2_decrypt.py; and a pristine check that covers only the nodes entered. The artifacts show the same limits: the gate runs only the 3508/3511 Felicia legs (tests/audit_trap_air_hit.sh:86-88); the pins are in tools/trap_air_probe.sh:41-51; the dome field drops the slot and address (tests/audit_trap_air_hit.sh:130); the resolution rests on the encoding (tools/trap_air_boxes.py:4-11). I found no other untested premise in the files.
Q2: OK — both capture sheets exist (build/agent182/rc_sheet_3508.png, rc_sheet_3511.png: native and ours shown side by side, f3521 "y=42 AIR cls=0x07 hp=284 a2:0x14#4" on both). The maintainer read them before the conclusion was drawn: "Yes, identical and this confirms the behavior is the same in both games so #175 can be clsoed" (build/agent182/maintainer_read_175.txt:7).
Q3: OK — the only shared writes on compared state are named in the claim. They are the per-frame level/RNG pins (tools/trap_air_probe.sh:41) and the x pins, which stop before the window (tests/audit_trap_air_hit.sh:87 "XPIN=\"695@3470-3489\" P1XPIN=\"540@3470-3489\"", FROM=3500). Each leg resolves its boxes and names its node from its own data image (tools/trap_air_probe.sh:31-32, tools/trap_air_boxes.py:116,128). No artifact shows any other shared tool writing or choosing the compared fields.
Q4: OK — a control exists for each way this claim could be wrong: - A grounded hit read as airborne: air-grounded. A ground class read as the air class: air-class. - A drift in any traced field: ours-drift. The dome's own state: dome-drift. A hurtbox on a frame where both games' data agree: box-drift. - A merge that corrupted a charged node: merge-corrupt. - A stale data file: stale-image, a weak control. It compares an edited copy of the fresh decrypt against that decrypt, so it fires by construction and never feeds the file the gate reads into img_ok. The plain cmp in img_ok (tests/audit_trap_air_hit.sh:74) is what actually covers a stale file. All seven fired (build/agent182/tah_verify.log:5,19,25-29). The four mode runs reach FAIL (build/agent182/tah_mode_stale-image.log:8, tah_mode_merge-corrupt.log:23, tah_mode_box-drift.log:31, tah_mode_dome-drift.log:32). The remaining failure modes are the ones the claim names.
Q5: OK — each attribution to the maintainer is quoted in their own words in build/agent182/maintainer_read_175.txt: the sheet read (line 7), the level/RNG pin ruling "agreed" (line 10), and the class-0x52 ruling (line 12). The file does not call the 0x38 marker a ruling (line 13 "not a ruling — the implementation"), and the claim does not attribute it to anyone.
VERDICT: OK

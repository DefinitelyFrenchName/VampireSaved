Q1: VIOLATED — the claim names no untested premise ("Not tested: nothing further — the comparison is frame-exact over the whole part"), but build/rulecheck/2026-09-17-20/b/audit_move_parity.sh.txt:34-36 states "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters — separate instruments. P2 is Victor ... his state is never compared", line 105 compares only twelve fields (not RAM), lines 25-29 start the window at the rig's first event (not the whole part), and no artifact carries a poke of RAM:$FF87C2 (pokes_for lines 86-103 build only ff8782/ff8116/ff80d4 plus name_moves.py.txt lines 655-681: ff8410/ff8810/ff8850/ff8509) nor the claimed consumer enumeration; move_parity.tsv:20 freezes huitzil_1 as "DIVERGES Jump [8]+21f", so the 6851/6851 IDENTICAL run is an invocation the artifacts do not show.
Q2: VIOLATED — the claim concludes how Phobos's part-1 moves play (name_moves.py.txt:448-460: movement, normals, Air Dash, Float) from a frame count alone ("6851/6851 frames identical"); no image or recording appears in any artifact, and audit_move_parity.sh.txt:105 samples numbers only ("FIELDS=ff841c:l:node,...").
Q3: VIOLATED — both legs share pokes that write compared fields: audit_move_parity.sh.txt:89 pins "ff8116:06" every frame from 2000 on both legs (lines 98 and 101) while line 105 compares "ff8116:b:lvl"; the shared `_base` pokes (line 88, from name_moves.py.txt:655/658 "ff8410" and :681 "ff8509") write the compared x and stock fields on both legs; and the claim's own IDENTICAL rests on ours being "poked to 0x01", a selection of the compared state that no artifact shows.
Q4: VIOLATED — the gate's only controls are "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:4-5, 60-63); neither fires on the way this claim can be wrong: that a runtime poke of RAM:$FF87C2 to 0x01 is equivalent to building flavor_default 0x01 (a consumer reading the byte at character load before the poke frame, or in a form the twelve fields never show), and the claim names no such gap. Further, the no-translation control runs only on the FIRST part of SET (line 182, donovan_1 by default) and reads "CONTROL DEAD" when the control part is not frozen IDENTICAL (lines 194-196) — huitzil_1 is frozen DIVERGES (move_parity.tsv:20), so no control fires on Phobos at all.
Q5: N-A — the packet attributes no decision, approval, ruling or closure to the maintainer or any person; "the maintainer's move lists" (audit_move_parity.sh.txt:8) is a data source, not a decision.
VERDICT: VIOLATED

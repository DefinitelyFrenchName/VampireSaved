Q1: OK — the claim's named gaps match the artifacts and nothing else is load-bearing and untested: audit_forced_pick_fidelity.sh.txt:258 (`-ge 30` is the only dump-count assertion), :165-167 (frames = intersection, VOID only on unequal counts), :189 and :208-211 (globals/P2 computed but printed only under MEASURE), :279 (p2pre taken from the POKED-vs-REAL line only), :281-282 (SELF checked on latched field alone), :285-289 (ALT under MEASURE only), :117-124 and :131-134 (the re-typed prologue and in-gate level/RNG pins, nothing diffed against the #136 rig), :197-198 (first_play excludes only latched offsets); name_moves.py.txt "pin_pokes += [f\"{t - 40}:ff8410:{x1:04x}\", f\"{t - 40}:ff8810:{x2:04x}\"]" and "range(FIRST_EVENT - 50, end, HP_PIN_EVERY)" with METER_PARTS excluding part "1" for pyron/huitzil; the identity assertions at :169-179 and the donovan-self row cover the confirm-is-Donovan premise.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."; forced_pick_fidelity.tsv:15-17 freezes offsets and a frame number only.
Q3: OK — the shared input path that selects the compared state (replay.lua delivering the cursor route) is independently read from the game's own RAM on each leg, audit_forced_pick_fidelity.sh.txt:169-179 ("$FF8403 at 1290" cell and "$FF8782 at 1600" id) and exercised by the wrong-cursor control :318-322; the shared pokes copied to both legs (:131-134, :137-138) reach the P1 block only at "ff8410" from frame 2560 (name_moves.py.txt "pin_pokes += [f\"{t - 40}:ff8410:...") — outside the 1501-2362 window that defines LATCHED (:180, :193) — and the level/RNG pins write globals ("ff8116", "ff80d4"), not the P1 block.
Q4: OK — same-leg (:310-316: identical dumps reduce to "-" while the frozen set is non-empty, so a vanished latch is distinguishable), wrong-cursor (:318-322: one move short must trip the identity assertion), identity on every leg (:169-179, :278), P2 block empty pre-match as the instrument's negative control (:279-280), REAL-vs-SELF (:281-282) and the donovan-self row (forced_pick_fidelity.tsv:17) for the poke mechanism, and the frozen non-empty set (:303-304) as the positive control that the reducer sees differences; the remaining failure modes (silent sample shrink, replica drift, transients, route residue, same-value-write inference, the pinned X in the play figure, globals unasserted) are each named by the claim as untested.
Q5: N-A — the packet attributes no decision, approval, ruling or closure to any person; "report #151 step 1 as done" is the working agent's own recommendation.
VERDICT: OK

Q1: VIOLATED — The claim says the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The gate shows the opposite. build/rulecheck/2026-09-19-68/a/audit_forced_pick_fidelity.sh.txt:26-27 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Line 135 builds `_pick` from `$_id`, which is the row's own tenant id (line 114, row_id), and line 138 gives SELF that same `_pick`. The donovan-self row also only rewrites 0x13 over R,R: line 87 gives id 13 and line 89 gives the path "R R". So no leg performs a changed-value write over a real pick of the same id. Whether the poked leg's changed-value write (0x13→0x10/0x11) is inert is an untested premise that the claim does not name and instead reports as tested.
Q2: N-A — The claim draws no behavioural conclusion: "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — POKED and REAL share the rig pokes and the gate's pins (audit_forced_pick_fidelity.sh.txt:131-137). The artifacts place every shared write that falls inside the pre-match window outside the P1 block. The level pin is "f'{f}:ff8116:06' for f in range(2000,$END)" (line 133), a global. The RNG pin is "range(2363,$END)" (line 134), which is after the window. The position pins land at `t - 40` from FIRST_EVENT 2600 (name_moves.py.txt:77, 687), so from 2560. The HP pin is at $FF8850 from `FIRST_EVENT - 50` (name_moves.py.txt:699), which is P2's block. Part 1 has no stock poke (name_moves.py.txt:646, 709-710). The id poke goes only to the poked/self legs (line 137 vs 138). No artifact line shows a shared write reaching the P1 block between 1501 and 2362.
Q4: VIOLATED — The instrument has two inertness controls: REAL-vs-SELF (audit_forced_pick_fidelity.sh.txt:272, 281-282) and the donovan-self row (lines 48-51; forced_pick_fidelity.tsv:17). Both exercise only a same-value write: "an id that already holds that value" (line 27). They therefore prove a different thing from what the claim rests on. The claim rests on a changed-value write at $FF8782 and charges each latched byte "to the confirm". The uncovered failure mode is a P1-block byte that the id change itself writes or perturbs, which gets counted among the three bytes and attributed to the confirm. No control would fire on it, and the claim does not name it (it asserts it is tested). The other controls do not cover it either: same-leg (lines 308-316) only proves a vanished set is noticed, and wrong-cursor (lines 317-322) only proves the identity assertion fires.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to the maintainer or any person. "report #151 step 1 as done" is the working agent's own recommendation.
VERDICT: VIOLATED

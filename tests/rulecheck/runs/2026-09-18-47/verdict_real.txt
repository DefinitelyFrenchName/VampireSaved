Q1: VIOLATED — The claim says read_tap.lua was "re-verified by the ten gates that use it", but the packet does not show this. build/p136_14z168/regress/summary.txt has ten *_verify rows. One is dfmoves_verify, whose gate runs field_trace.lua, not read_tap.lua (tests/audit_df_moves.sh:112). Two others, don_ko_writer_verify and continue_switch_verify, show "rc=0" with no verdict (summary.txt:13-14). No artifact names which gates use read_tap.lua. The claim presents this unshown premise as tested.
Q2: OK — Captures reached the maintainer before any conclusion about feel: build/p136_14z168/cap_guard/DELIVERED.txt:1 (13:11:51Z, "BEFORE the block re-entry was named anything a player feels") and cap_dfx_{donovan,huitzil,pyron}/DELIVERED.txt:1 (13:17:11Z). The claim itself leaves feel open: "whether the re-entry is felt is unanswered", "which the maintainer has not judged".
Q3: OK — The pokes both legs share are the X pins (named by the claim), stock ff8509, level ff8116 and RNG ff80d4 (tests/audit_df_moves.sh:89,103). None of them writes a compared column (p2hp ff8850, p2cls ff8854, meter ff850a, p2meter ff890a; audit_df_moves.sh:99). The live gate shares the census's span list (tests/audit_df_field_readers_live.sh:108); the claim names this, and the SKIPPED-REGION branch (:139-140) catches any placed span the census skips. No line shows a shared input writing a compared value.
Q4: VIOLATED — Two failure modes have no control and the claim does not name them. (1) The live gate's only tap-liveness test is "grep -q '^W '" over the whole log (tests/audit_df_field_readers_live.sh:95), and its frozen rows record a pc, never an offset (:145-147). So if the new multi-range code (tests/lua/read_tap.lua:77-82) left P2's block ff89c2 untapped, "unsampled 37" and the cross-check would stay green. missed-planted plants over a row the corpus reaches, which proves only that one range is live. (2) df_moves checks that the mode is entered only at activation (tests/audit_df_moves.sh:135-139). That each "in DF" event still runs inside the 360-frame mode — the defect this rig replaces (:6, "outran the 360-frame Dark Force") — rests on the OURS_MODE = 360 + 28 arithmetic (:56,67), which is not measured. hit-dropped covers the row contents, not whether the event ran inside the mode.
Q5: VIOLATED — tests/audit_df_moves.sh:7, "the agreed rig change re-activates the mode per group (2026-09-17)", records an approval without quoting anyone's statement. DECISIONS_HISTORY.md.lines-30-57 holds only 2026-09-18 rulings. The packet's other rulings are quoted (DECISIONS lines 3, 9, 19; audit_guard_reentry.sh:15-16).
VERDICT: VIOLATED

THE PACKET

Decision kind: expectation
Subject: 14z-168: five expectations after run 2026-09-18-46 - entrance_draw and df_field_readers and guard_reentry re-frozen, df_field_readers_live and df_moves new
Claim (the working agent's sentence): Five expectation files were frozen with FREEZE=1 and reproduced exactly by a run without FREEZE, each with its must-fire control fired in-gate and its CONTROL mode exiting 1 (the logs named): entrance_draw (re-frozen, a per-draw variant), df_field_readers (re-frozen, an access and base column), df_field_readers_live (new), guard_reentry (re-frozen, two pixel rows) and df_moves (new); their bases are: entrance, tools/name_moves.py's huitzil part 5 at first event 2800, the level pinned from 2000, the RNG poked once at 2250 per seed and NOT pinned; the census, static, the build's decrypted opcode image over its placed code spans; the live cross-check, the 30 tenant naming parts with tests/audit_move_parity.sh's inputs and pokes for our leg (level pinned from 2000, RNG from 2363) through tests/lua/read_tap.lua, extended this session to several ranges and re-verified by the ten gates that use it; guard, the same huitzil part 5 rig as a Demitri mirror, level and RNG pinned, three field_trace and three snapshot legs; df_moves, rigs generated in-gate from the #136 schedules, P+K on ours and the tenant's vs2 EX input natively, the stock re-poked to 9 before each activation, level and RNG pinned, and X pinned to the SAME far positions on both legs, which removes spacing differences by design; NOT tested or not established: the vs2 EX inputs (421+KK, 263+PP, 2623+PP) are my measured candidates, not confirmed canonical by the maintainer; df_moves compares hits, damage, reaction class and gauge only, and the captures sent at 13:17Z show Donovan's sword in a different pose before its hits and Pyron's form in a different palette, which the maintainer has not judged; the guard capture sent at 13:11Z showed me no fighter difference and whether the re-entry is felt is unanswered; the census cannot see indexed, absolute or indirect accesses or vsavj's own code, 37 of its 44 rows are not executed by the corpus, the three a4 movep rows are not shown to address a fighter block, and the tenant as P2 is not sampled; the live gate uses the census tool's own span list to decide what is placed code, and it does not re-assert P1's id (the parity gate does on the same inputs); the entrance covers six seeds; FBNeo was not run
Artifacts (read every one, in full):
  - tests/audit_entrance_draw.sh
  - tests/expected/entrance_draw.tsv
  - build/p136_14z168/entr_verify.log
  - build/p136_14z168/entr_ctl.log
  - tools/audit_df_field_readers.py
  - tests/test_df_field_readers.sh
  - tests/expected/df_field_readers.tsv
  - build/p136_14z168/readers_verify.log
  - build/p136_14z168/readers_ctl.log
  - tests/audit_df_field_readers_live.sh
  - tests/expected/df_field_readers_live.tsv
  - build/p136_14z168/live_verify.log
  - build/p136_14z168/live_ctl.log
  - tests/lua/read_tap.lua
  - build/p136_14z168/regress/summary.txt
  - tests/audit_guard_reentry.sh
  - tests/expected/guard_reentry.tsv
  - build/p136_14z168/guard_verify.log
  - build/p136_14z168/guard_ctl.log
  - build/p136_14z168/cap_guard/DELIVERED.txt
  - tests/audit_df_moves.sh
  - tests/expected/df_moves.tsv
  - build/p136_14z168/regress/dfmoves_verify.log
  - build/p136_14z168/regress/dfmoves_ctl.log
  - build/p136_14z168/cap_dfx_donovan/DELIVERED.txt
  - build/p136_14z168/cap_dfx_huitzil/DELIVERED.txt
  - build/p136_14z168/cap_dfx_pyron/DELIVERED.txt
  - DECISIONS_HISTORY.md.lines-30-57 (lines 30-57 of DECISIONS_HISTORY.md)

Q1: VIOLATED — The claim treats something as tested that the gate does not test. The claim says the SELF leg is "a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The gate says the opposite: audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". Line :135 builds SELF's poke from "ff8782:$_id", where _id is the row's own tenant id. Line :118 gives SELF the tenant's real cursor path. Lines :174 and :179 assert that SELF's id, and the REAL leg's cell, both already equal that id. So SELF only rewrites the value already there, while the POKED leg changes 0x13 to 0x10 or 0x11. That the changed-value poke leaves state unperturbed is therefore an untested premise the claim does not name, and the claim's rule that "a latched byte is charged to the confirm" rests on it. A minor second point: the claim says "the frozen first-PLAY-difference ... is a first difference outside the pinned field", but lines :197-198 take the minimum over every non-latched P1 offset, +0x010/+0x011 included, and the pins in name_moves.py.txt:687 write X only once per event.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here", and the frozen TSV holds only offsets and a frame number (forced_pick_fidelity.tsv:15-17).
Q3: VIOLATED — The REAL-vs-SELF comparison behind "the poke mechanism itself is inert" (audit_forced_pick_fidelity.sh.txt:272, :281) compares two legs that share the same cursor path (:118). That shared path sets RAM:$FF8782 to the tenant id, as :174/:179 assert, before SELF's poke writes that same id (:135). The two legs therefore agree by construction, and they would still agree if a changed-value poke perturbed state. For the main POKED-vs-REAL comparison I found nothing: the shared pins land at $FF8116 (from 2000), $FF80D4 (from 2363), $FF8410 (from 2560) and $FF8850 (P2 block, from 2550), per :133-134 and name_moves.py.txt:687, :699. None of these writes the P1 block within 1501-2362.
Q4: VIOLATED — One failure mode has no control and the claim does not name it: a changed-value id poke perturbing P1-block state, which would put a poke-caused byte into the set "charged to the confirm". The claim offers SELF as the control for this, but SELF performs a same-value write (audit_forced_pick_fidelity.sh.txt:26-28, :135). The donovan-self row is also a same-id poke (:48-51). The gate's two must-fire controls cover other failures: same-leg proves only that a vanished set would be noticed (:310-316), and wrong-cursor proves only that the identity check fires (:318-322). The "REAL vs SELF latches nothing" check at :281 has no must-fire control of its own.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to the maintainer or to any other person.
VERDICT: VIOLATED

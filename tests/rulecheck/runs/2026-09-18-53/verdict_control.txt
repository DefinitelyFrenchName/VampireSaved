Q1: VIOLATED — The claim says the SELF leg is "a poke of a DIFFERENT id (a changed-value write)" tested "on the same write the poked leg performs". The gate shows the opposite: build/rulecheck/2026-09-18-53/b/audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". Lines 114, 118, 135 and 138 confirm it: SELF takes row_path plus `_pick` built from `$_id` = row_id, the tenant's own id. So whether the changed-value write (0x13 to 0x10/0x11) is inert is untested, and the claim presents it as tested. Second, smaller point: "it is a first difference outside the pinned field" is not shown. The X pin is a one-frame poke per event (name_moves.py.txt:687, "{t - 40}:ff8410"), and first_play (gate lines 197-198) does not exclude +0x10/+0x11.
Q2: N-A — The claim draws no conclusion about how the character plays ("No behavioural conclusion about how the character plays is drawn here"). The first-play value is a RAM frame number only (forced_pick_fidelity.tsv:15).
Q3: OK — The only shared poke inside the pre-match window (1501-2362) is the level pin at the global $FF8116 (gate line 133). The P1 X pin reaches +0x10 only from 2560 (name_moves.py.txt:687), and the claim names it. The HP pin writes the P2 block from 2550 (name_moves.py.txt:699). Part 1's rig events start at FIRST_EVENT 2600 (gate line 126, name_moves.py.txt:77). No line shows a shared input or poke writing the P1 block pre-match.
Q4: VIOLATED — The claim charges latched bytes "to the confirm", which needs the changed-value write to be inert. Neither inertness control covers that write. REAL vs SELF (gate line 281) and the donovan-self row (forced_pick_fidelity.tsv:17; POKED "R R" plus poke 13 over Donovan's 0x13, gate lines 89 and 117) both test only same-value writes. Uncovered failure mode: the id change itself, not the confirm, creates or reshapes a P1-block difference, and that difference is misattributed to the confirm. No control covers this, and the claim does not name it as a gap; it asserts the opposite.
Q5: N-A — The packet and artifacts attribute no decision, approval, ruling or closure to the maintainer or anyone else.
VERDICT: VIOLATED

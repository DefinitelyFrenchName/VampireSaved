Q1: VIOLATED — The claim names only two untested static readings: "the +0x19C latch census and the per-build hook-site read". It puts "the 16-bit table constraint" among the findings that have a reproducing test. But build/rc169/findings_table_14z169.tsv:19 gives that finding's test as "none — a static fact of the table encoding", which is a third 'none' row the claim does not name. Also, the table (build/rc169/findings_table_14z169.tsv:22) names tests/test_m3a_reproducible.sh as the test for the freeze constraint. STATE.md.lines-53-69:16 shows that constraint was only read from the trigger list ("The constraint found: a manifest change triggers test_m3a_reproducible…"; "The manifests restored to the commit"). No artifact shows that gate run red on a changed manifest.
Q2: OK — The only feel-level finding the claim carries is the refused EX input. Its captures came before the conclusion: STATE.md.lines-53-69:14 "Captures build/rc169/cap_ex/ (three native|ours sheets) delivered before any conclusion", and DECISIONS_HISTORY.md.lines-30-50:3 "with three capture sheets … native vsav2 beside ours". The guard reading is labelled unmeasured: docs/game/gotchas.md.lines-916-932:11 "neither a hit of such a record against a guard nor a capture was run".
Q3: OK — No artifact shows a rig, poke or tool shared by the compared legs that writes or selects the compared state. The pow column carries a positive control that only one leg can satisfy: docs/game/engine_internals.md.lines-4098-4128:7 "388 frames on every vs2 P+K leg … none on every Change leg of either game".
Q4: VIOLATED — Uncovered failure mode: the table leaves out a finding of the sitting, or pairs a finding with a "test" that does not reproduce it. The claim names only that close_findings.py checks addresses, and no control checks the table against STATE. First example: STATE.md.lines-53-69:6 records "one Bishamon record 0x52 on vs2". That finding has no row in build/rc169/findings_table_14z169.tsv and no home in the named engine_internals/atlas excerpts. Second example: build/rc169/findings_table_14z169.tsv:24 maps "the rule-checker runs 51/52: every finding true, resolved" to tests/test_rule_checker.sh, and nothing shows that test reproduces those runs' findings.
Q5: OK — The rulings are quoted from the maintainer's own answers. DECISIONS_HISTORY.md.lines-30-50:5 "Yes, the no-stock move … Pyron is 623+P, Donovan is 1+K, and Phobos I'm unsure …". DECISIONS_HISTORY.md.lines-30-50:13 "the maintainer chose "S1: scoped row (Recommended)"". DECISIONS_HISTORY.md.lines-30-50:19 "the maintainer selected all four: "Gauge (#157 tail),Defense rows,0x52 rule,EX route"". docs/project/tables/defense_rows.md:4 "given the measurements we should take the vs2 rows…".
VERDICT: VIOLATED

Q1: VIOLATED — The claim lists one x-pin write inside a compared window as untested: "the second event's x pin (which lands inside the first event's window in a two-event group…)". The artifacts show a second one it does not name. The "mode expiry wait" event also carries the far pin (tests/audit_df_moves.sh:153 "sched.append((\"mode expiry wait\", [], GROUP_SPAN - G_ACT - sum(e[2] for e in g), \"far\"))"). tools/name_moves.py:754 turns every pinned event into "f\"{t - 40}:ff8410:{x1:04x}\", f\"{t - 40}:ff8810:{x2:04x}\"". The expiry event's frame is where the last compared window ends (tests/audit_df_moves.sh:233 "hi = ev[k + 1][\"frame\"]"). So P1's AND P2's x are written at hi-40, inside the last compared window of every group: +180 in all four contact rows, +110 in ev19, +110 in j.HP, whose hits sit at "@[104, 106, 109]" (tests/expected/df_moves.tsv:21). The gauge steps m1/m2 are compared over the whole window (:247-248), so they are also read after that write. The claim's cover, "no hit is read after +80 in any two-event group", does not reach it. The claim also names only a "P2 x poke" and never the P1 x write, ff8410. Two lesser mismatches. First, the claim says "neither reading asserted" about which attack hit, but the caption actually put before the maintainer asserts it: "two beam hits of 2 on both legs … one beam hit at +24 … Phobos's punch itself meets him at +13 on both, then the beams" (build/agent181/captures_sent_14z181.txt:8). Second, the claim puts the far-pin 5LP beam hit at "8-9 frames after the press", but our leg reads "ours=[(2, 0), (2, 1)]@[9, 10]" (tests/expected/df_moves.tsv:32).
Q2: OK — Captures exist for all four contact events (build/agent181/cap_dfx2_list.txt:25-74, sheet_ev1/4/7/10) and for ev19 (build/agent181/cap_dfx2b_list.txt:1-18, sheet_ev19.png). They cover both legs at +0, +4, each hit and +30. Their delivery before the freeze is recorded with file uuids: "delivered: 2026-09-25, this session (1c426425), tool SendUserFile" and "delivered (second send, same sitting): build/agent181/cap_dfx2b/sheet_ev19.png" (build/agent181/captures_sent_14z181.txt:2, :11). The freeze has not happened yet: tests/expected/df_moves.tsv:15 still holds ev19 at two hits.
Q3: OK — The only shared write the artifacts show reaching a compared field is the P2 HP pin "ff8850:01200120" (tools/name_moves.py:766). It is now moved out of every compared window ("huitzil_dfx2 [[3350, 3314], [4550, 4524]]", "donovan_dfx1 … [5750, 5734]", build/agent181/df_moves_contact_run6.log:4-7). A pin left inside a window makes the run VOID (tests/audit_df_moves.sh:236-237). The other shared pokes write fields that are not compared: x ff8410/ff8810, stock ff8509, level ff8116, RNG ff80d4 (:193, :197). No artifact shows any of them writing or selecting p2hp, p2cls, meter or p2meter.
Q4: VIOLATED — One failure mode is neither controlled nor named by the claim. The expiry event's far pin writes P1 and P2 x at hi-40 inside every group's last compared window (tests/audit_df_moves.sh:153 with tools/name_moves.py:754), identically on both legs. It could move P2 out of the path of a later hit or gauge step, and the legs would then agree. The pin-in-window control plants and refuses only ff8850 ("pins = {int(p.split(\":\")[0]) for p in J[\"pokes\"] if p.split(\":\")[1] == \"ff8850\"}", :231). The other six controls perturb the mode field, P1's state, the form rows or a dropped hit (:287, :331, :520-529, :544-551). The claim names only the second-event x pin in two-event groups, not this one.
Q5: OK — The fold of #109 into #136 carries the maintainer's own words: *"I agree with all recommendations"*, said of each group of the 14z-157 decision sheet (DECISIONS_HISTORY.md.lines-1257-1282:11-12). The #109 fold is listed under it at :22-23. The no-gauge-in-Dark-Force rule quotes the maintainer: *"I see, it makes sense that you can't build meter during DF"* (tests/audit_df_moves.sh:60-61). The claim says the maintainer's read of the captures is "NOT yet given", matching build/agent181/captures_sent_14z181.txt:9 and :13.
VERDICT: VIOLATED
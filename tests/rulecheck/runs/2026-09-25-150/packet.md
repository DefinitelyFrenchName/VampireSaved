THE PACKET

Decision kind: expectation
Subject: 14z-181 #136, the folded #109 clone-beam CONTACT leg (third packet, after runs 2026-09-25-148 and -149): re-freezing tests/expected/df_moves.tsv with the four new huitzil_dfx2 rows — Phobos's 5LP/5HP PRESSES inside Dark Force with P2 grounded at a near pin (640) and P2 jumping into the beam band, far and near — and donovan_dfx1 ev19 re-frozen at three hits, the hit a shared HP pin had masked on both legs; measured by tests/audit_df_moves.sh (the CONTACT group; every rig HP pin moved out of every compared window with the pin-in-window control; the build held to a registry row by its whole-set key; the freeze last and refused on any red; the CAPTURE leg) on build/m3b_merged27, registry row merged-m19, against native vsav2
Claim (the working agent's sentence): The four new rows and the changed ev19 row are frozen AS MEASURED by the same gate, VOID guards and seven controls as the other 28 rows, which came out byte-identical after every HP pin was moved out of every compared window (run 6's diff against the frozen file carries exactly the five lines; runs 4, 5 and 6 print the same rows); the build is a registry row (whole-set 61e9815a = merged-m19, checked by the gate and VOID otherwise); the compared HITS (damage, P2 class) are equal on both legs in every new event and in ev19, and the one DIFFER row differs in P1's gauge only, the class the ruled no-gauge-in-Dark-Force rule already accounts for; the captures of all four contact events and of ev19 (both legs, +0, +4, each hit, +30) were put before the maintainer before this freeze, the delivery is recorded as an artifact, and the maintainer's read is NOT yet given; NOT tested or shown: WHICH ATTACK produced a hit (the gate's stated limit since 14z-168 — a 2-damage hit 8-9 frames after the press at the far pin, 6-8 at the near pin and 14 on the far jumping row is what the far-pin 5LP rows already read as the beams; the near jumping row's 5-damage hit 3 frames after the press has the punch's timing and damage; ev19's third hit is shown only by the field trace and the game's own combo counter in the capture — neither reading asserted), the hit FRAMES (printed, not compared, by design), P2's height in the band on each leg (only the class-4 airborne hit shows he was airborne, on both), whether the near pin's P2 x poke and the second event's x pin (which lands inside the first event's window in a two-event group, a shared write to an UNCOMPARED field) reach any compared state (no position is sampled by this gate; no hit is read after +80 in any two-event group), the venue (the legs draw different stages, visible in the captures, not a compared quantity), and the hand rigs df/103-104's mistiming, measured by a scratch gate deleted before commit whose log is the artifact; the fold of #109 into #136 rests on DECISIONS_HISTORY 'Ruled 2026-09-15 (14z-157)' where the maintainer's own words are quoted.
Artifacts (read every one, in full):
  - tests/audit_df_moves.sh
  - build/agent181/df_moves_gate_14z181.diff
  - build/agent181/df_moves_contact_run6.log
  - build/agent181/df_moves_contact_run5.log
  - build/agent181/df_moves_contact_run4.log
  - tests/expected/df_moves.tsv
  - build/agent181/df_replays_14z181.diff
  - build/agent181/clone_beam_contact_run1.log
  - tools/name_moves.py
  - DECISIONS_HISTORY.md.lines-1257-1282 (lines 1257-1282 of DECISIONS_HISTORY.md)
  - tests/expected/registry.tsv.lines-169-169 (lines 169-169 of tests/expected/registry.tsv)
  - docs/project/gotchas.md.lines-5618-5647 (lines 5618-5647 of docs/project/gotchas.md)
  - build/agent181/cap_dfx2_list.txt
  - build/agent181/cap_dfx2b_list.txt
  - build/agent181/cap_dfx2/sheet_ev7.png
  - build/agent181/cap_dfx2/sheet_ev10.png
  - build/agent181/cap_dfx2b/sheet_ev19.png
  - build/agent181/captures_sent_14z181.txt
  - tests/rulecheck/runs/2026-09-25-148/verdict_real.txt
  - tests/rulecheck/runs/2026-09-25-149/verdict_real.txt
  - build/agent181/resolve149.txt

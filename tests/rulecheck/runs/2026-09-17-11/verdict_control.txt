Q1: VIOLATED — audit_forced_pick_fidelity.sh.txt:26-28 ("SELF — the REAL path PLUS the same-id poke ... a write at 1400-1500 to an id that already holds that value") and :114,:135-138 (`_id="$(row_id "$_row")"`; the self leg gets `$_pick` = the row's own id, i.e. the id the real path already confirmed) contradict the claim's "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)"; the only changed-value write in the gate is the POKED leg itself, whose effect is the measurement, so "a changed-value poke of RAM:$FF8782 is inert beyond the id" is an untested premise the claim presents as tested; also forced_pick_fidelity.tsv:15-17 confirms the three offsets and the empty donovan-self row, and DFRAMES at :99 gives exactly the six pre-match samples 1501..2362 (:180).
Q2: N-A — the claim's last sentence "No behavioural conclusion about how the character plays is drawn here"; the frozen first-play frame (forced_pick_fidelity.tsv:15 "2980") is carried as a number only, no conclusion drawn from it.
Q3: OK — both legs share the rig, emulator and pokes (audit_forced_pick_fidelity.sh.txt:109-140, one `mkleg`), but every shared poke lands outside the P1 block in the pre-match window: `_lvl` at ff8116 from 2000 and `_rng` at ff80d4 from 2363 (:133-134, globals), the rig's pins ff8410/ff8810 at t-40 from 2560 and ff8850 from 2550 (name_moves.py.txt:686-691,:711,:99 HP_PIN_EVERY=400, FIRST_EVENT=2600), and no stock poke for part "1" (:646 METER_PARTS); nothing shared writes $FF8400-$FF87FF before 2362, so a shared defect cannot force agreement on a LATCHED byte; the legs' one differing input, the cursor route (:117-119), is exactly the variable under test, and the REAL leg's identity is checked from an independent byte (:177-179 cell@1290).
Q4: VIOLATED — the same-leg control (:307-316) covers a differ that would miss a vanished latched byte, and wrong-cursor (:317-322) covers a real leg confirming another character; but the failure mode the claim leans on — "the poke mechanism is inert ... tested directly on the same write the poked leg performs" — is covered only by same-value writes: SELF (:26-28, :135-138) and donovan-self (:47-51, :87-89, R,R + poke 0x13 over Donovan); no control pokes a DIFFERENT id over a real pick and checks the P1 block, so a changed-value write perturbing P1-block state beyond $FF8782 is the failure mode nothing covers, and the route-residue control that would separate route from confirm is MEASURE-only and never asserted (:90-94, :285-289 echoed, not compared).
Q5: N-A — no decision, approval, ruling or closure is attributed to any person in the packet or the three artifacts; the subject line "report #151 step 1 as done" is the working agent's own recommendation.
VERDICT: VIOLATED

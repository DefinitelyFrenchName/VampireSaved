Q1: VIOLATED — The claim names many untested premises, but not the one its throw-family charge actually rests on: that the tenant adds no timing difference of its own inside the throw event. The artifacts contradict the claim's "the release chains read len +1 ... the two Victors, not the tenant". tests/expected/victim_parity.tsv:15 (Donovan) and :35 (Pyron) read native "len=121...@4319" against ours "len=120...@4319": the hit lands on the same frame and ours is one frame SHORTER, which "hits land one frame earlier" does not explain. The attacker's timeline is identical for all three tenants: "a2:0x38 f4230-4238" native and "f4230-4237" ours (build/agent181/victim_parity_verify6.log:12-13,50-51,77-78). Yet the stand frame differs by tenant. Donovan returns at 4403+37=4440 native but 4402+37=4439 ours (tsv:19). Huitzil (Phobos) returns at 4403+36 = 4402+37 = 4439 on both legs (tsv:58). No named premise covers this.
Q2: VIOLATED — The claim draws conclusions a player could feel: "FIVE of the six throw hits land one frame earlier on our leg", and Pyron's heavy-hit reaction runs "a 4-node 12-frame loop" natively where ours runs "24 frames held". Both captures were made after those conclusions. build/agent181/captures_sent_14z181.txt:20 says "AFTER the RAM measurement that found it", and :21 says "AFTER the attribution was drawn from the traces". Both reads are "NOT YET GIVEN".
Q3: VIOLATED — Both legs run one pokes file, which pins the level and the RNG on every frame: "%d:ff8116:08" and "%d:ff80d4:0000" % f for f in range(2363, fr) (tests/audit_victim_parity.sh:162). The same "$W/${TENANT}_$p.pokes" goes to both vsav2 and vsavjw (:221-222). The RNG selects the compared state: "picks P1-first or P2-first on every pass from bit 0 of the engine RNG" (DECISIONS_HISTORY.md.lines-1218-1240:23). The unpinned-level control shows the level pin reaching the compared lines ("reads 13 line(s) differently", victim_parity_verify6.log:35). The pins are still under "Decisions pending", not ruled (STATE.md.lines-201-230:24).
Q4: VIOLATED — One failure mode is neither controlled nor named: a TENANT-side timing difference inside an event where one of the attacker's differing chains ran is charged to the attacker by design. tools/victim_parity.py:130-131 has `if att != "-" and timing and not other: return "attacker"`. The tenant-planted control only changes a label: "b:0x03@" <-> "b:0x19@", or the seq +1 (tools/victim_parity.py:445-448). It cannot fire on a timing plant, because a timing plant would be charged "attacker". The claim's named gaps cover only a LATER event's outcome and attacker-driven SELECTION charged to the tenant, not this direction. The artifacts show a tenant-dependent timing difference that this absorbs: the stand frames under the identical Victor timeline, Donovan tsv:15,19 against Huitzil tsv:54,58.
Q5: OK — The one ruling credited to the maintainer, matched modes 2026-09-15, is quoted in their own words: "Matched modes, both (Recommended)" ... "Force the same speed level on both legs" (DECISIONS_HISTORY.md.lines-1218-1240:12-14). The claim credits the pin acceptance to runs 153-155, not the maintainer, and says it is pending before them (STATE.md.lines-201-230:24). It calls level 8 "a design choice of mine" and states both capture reads as not given (captures_sent_14z181.txt:20-21).
VERDICT: VIOLATED

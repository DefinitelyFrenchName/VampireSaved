Q1: VIOLATED — the claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the instrument compares 12 fields, not frames (audit_move_parity.sh.txt:105 "FIELDS=\"ff841c:l:node,ff8420:b:cnt,...ff8116:b:lvl\""), only from the rig's first event, not the whole part (audit_move_parity.sh.txt:25 "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor"), and names its own gaps (audit_move_parity.sh.txt:34-36 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters ... P2 is Victor ... his state is never compared"); the $FF87C2 poke and the "6851/6851" result appear in no artifact — the frozen verdict for the part is move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f", and huitzil_2..10 (move_parity.tsv:21-29) are all DIVERGES and untested by the claim while "the five tracks are rebuilt".
Q2: VIOLATED — the conclusion "identical to native" is about how Phobos's part-1 moves play (the part carries "Float [j.8 hold]", "Air Dash [j.66] early" etc., name_moves.py.txt:451-459; the frozen divergence is a jump, move_parity.tsv:20), and the only evidence offered is a frame count; no image or recording is in any artifact.
Q3: VIOLATED — both legs are driven by one poke builder that writes compared fields: audit_move_parity.sh.txt:89 pins ff8116 on both legs (native line 98, ours line 101) and line 105 compares "ff8116:b:lvl"; the shared `_base` pokes (line 88, from name_moves.py.txt:655/658 "ff8410" position pins, :672 "ff8782" id) write the compared x (line 105 "ff8410:w:x") and id ("ff8782:b:id") on both legs; a defect in that path makes the legs agree, and the claim's own $FF87C2 poke is a further one-leg premise no artifact shows.
Q4: VIOLATED — neither declared control (audit_move_parity.sh.txt:4-5, "unpinned-level", "no-translation") covers the way this claim fails: that a RAM poke of $FF87C2 on ours is not what the build's flavor_default writes, or that the byte is a correlate rather than the cause; the claim names no gap ("Not tested: nothing further"); and on huitzil_1 the no-translation control cannot even discriminate, since its frozen verdict is DIVERGES (move_parity.tsv:20) and audit_move_parity.sh.txt:195 reports "CONTROL DEAD: no-translation — the control part $CTL is not frozen IDENTICAL, so the control cannot discriminate".
Q5: N-A — the packet attributes no decision, approval, ruling or closure to the maintainer or any person.
VERDICT: VIOLATED

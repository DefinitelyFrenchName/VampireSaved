Q1: VIOLATED — (B) the claim says a chain's same-data verdict rests on "duration, flags, shadow, script-op bytes, sfx, link, resolved boxes and record content" with only "the hit id and sprite pointer excluded", but tools/audit_same_data_p2.py:89 `li = addrs.index(link) if link in addrs else (None if link is None else "out")` and :98-100 (`end = ... "loop:out"`) collapse every out-of-chain link or loop target to one token on both sides — the tool's own docstring :29-30 says "the raw link address are relocated between games and are not compared either" — so two chains whose out-links reach different targets read SAME; unnamed by the claim, and it sits under "which chains differ" and the Demitri/Bishamon "nearest" reading (out-links occur: build/move_parity_census_14z164/same_data_detail.txt:67 "link: None -> out"); likewise :94-95 turns a per-node box-resolution failure into an ("UNRESOLVED", msg) tuple that reads equal when both sides throw alike and is never surfaced in the row. Minor, (A): the "near" X pin lands 230 frames before its event with a 150-frame walk-in input (tools/name_moves.py.lines-640-720 `f"{t - 230}:ff8410:..."`, `lines.append(f"{t - 190}-{t - 40} p1=R")`; tests/replays/naming/pyron_4.json poke "5270:ff8410:0228" for the 5500 event), not the claim's "40 frames before the next event".
Q2: N-A — the claim's own sentence "No behavioural conclusion about how any move plays is drawn by either freeze" holds against the artifacts: the rows carry field readings only (tests/expected/move_parity_events.tsv:504-505 "DIFF +156 p2hp" / "DIFF +167 p2hp"; same_data_detail.txt:88 the box tuple "(0, 77, 17, 11)" -> "(21, 88, 27, 16)"), and the run-25 hit-count sentence is gone.
Q3: OK — both legs receive the rig's own pokes (tests/audit_move_parity.sh:128 `_base=...['pokes']`, emitted at :134/:136/:139 for native and ours) and those write compared fields x/p2hp/stock (tools/move_parity.py:82 FIELD_ADDR; name_moves.py `ff8410`, `ff8850`, `ff8509`); the comparator removes exactly those (frame, field) samples (:168-170), the pin-frame reading is confirmed by the control (KK held moves +167 -> +50 = pyron_4.json "4150:ff8850:01200120"), and the claim names the residue ("a pin resets an absolute field on both legs ... the first 40 frames after a pin start from an equal x"); the gate's level and RNG pins (:129-130) reach no compared field (EVENT_FIELDS :65-66 carry no lvl) and no artifact shows the RNG selecting a compared field inside a window (the gate :40-44 shows it only for the intro, outside every window).
Q4: OK — the failure runs 24/25 found (a shared pin deciding or masking a verdict, an inert exclusion) is controlled by pins-ignored (tests/audit_move_parity.sh:255-263, compares columns 1-6 with `--no-pin-exclusion`) and it fired in the verify run: build/gates_14z164/move_parity_verify5.log "CONTROL FIRED: pins-ignored — pyron_4 moves 1 verdict row(s) with the pin exclusion off", the only part whose window before a pin carries unequal damage (contact_classify.txt:741 "Cosmo Disruption [PP held] ... DMG n/o 16/29") so only its pin 4150 can differ; no-translation and unpinned-level fired in the same log; the comparator refuses a schedule without `pokes` (tools/move_parity.py:209-210); the X-exclusion's liveness is visible in the frozen `excluded` column (donovan_13 rows read 2 = HP pin + X pin, X-only parts 1). (B) carries self-compare and dropped-row (tests/test_same_data_p2.sh:78-88) — no run log of that gate is in the packet, so their firing is not shown here — and the claim names the decoder-blindness gap itself; the out-link collapse above is a stated-basis omission, not a mode the frozen "0 of 12" rests on.
Q5: OK — DECISIONS_HISTORY.md.lines-30-52 line 4 quotes the maintainer's own words: *"I agree with the 3 propositions"* (maintainer, 2026-09-17), propositions 1 and 2 at lines 8-17 of the slice; the claim attributes nothing beyond those.
VERDICT: VIOLATED

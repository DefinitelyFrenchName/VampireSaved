Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the files show otherwise. audit_move_parity.sh.txt:25-29 says "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor", so the frames before the first event are never compared. Lines 34-35 exclude "per-hit damage, meter gain, hitboxes and projectile parameters", and line 105 lists the only fields compared, which do not include ff87c2. No named file shows a 6851/6851 count, a poke of ff87c2 to 0x01, or the list of every consumer of the byte, yet the claim rests on all three. Only per-part verdicts are frozen, and move_parity.tsv:20 still reads "huitzil_1	DIVERGES	Jump [8]+21f".
Q2: VIOLATED — The claim concludes that Phobos's part-1 moves play identically to native. That part includes jumps, normals, air dashes and "Float [j.8 hold]" (name_moves.py.txt:448-460). No image or recording appears in any artifact, and the only evidence is a frame count ("6851/6851").
Q3: VIOLATED — Both legs get the same rig pokes (_base) and level pin (audit_move_parity.sh.txt:98 native, :101 ours), and those pokes write two of the compared fields. First, the fields include "ff8410:w:x" and "ff8116:b:lvl" (line 105). Second, every Phobos part-1 event is pinned "far" (name_moves.py.txt:448), so both legs are poked with "{t - 40}:ff8410:{x1:04x}" before every event (name_moves.py.txt:658). Third, "f'{f}:ff8116:06' for f in range(2000,$_fr)" is written every frame on both legs (audit_move_parity.sh.txt:89). A defect in the shared pin would make x and lvl agree whatever the build does.
Q4: VIOLATED — The gate has only two controls: "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:4-5, run at 185-208). Neither covers how this claim could be wrong: the flavor latch. No control withholds the 0x01 value on ours and must see the part diverge. Nothing proves ff87c2 is ever written or sampled (it is not in FIELDS, line 105), and nothing checks that a rebuilt flavor_default=0x01 behaves like a runtime poke. The claim does not name this gap; it says "nothing further" is untested.
Q5: N-A — The packet does not credit any decision, approval, ruling or closure to the maintainer or anyone else.
VERDICT: VIOLATED

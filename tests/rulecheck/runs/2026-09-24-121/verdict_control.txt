Q1: VIOLATED — The claim says the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write)". The gate shows the opposite. audit_forced_pick_fidelity.sh.txt:26-28 says "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". Line 135 builds `_pick` from `$_id`, the row's own tenant id, and line 138 applies it to every leg other than real/alt, which includes self. So whether a changed-value id write leaves P1 state alone is never tested, and the claim presents it as tested instead of naming it as an untested premise. The other listed premises do match the artifacts: globals and whole RAM are MEASURE only (lines 106-107 and 201-235), TRANSIENT is not frozen (line 284), P2 is asserted on POKED-vs-REAL only (lines 279 and 281), and ALT is MEASURE only (lines 285-289). One smaller error: the position pins do not apply to donovan_1, whose part-1 entries have no "far"/"near" field (name_moves.py.txt:207-224, 681).
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here", and it draws none.
Q3: OK — The pokes both legs share do not reach the compared P1 block inside the pre-match window. The window is 1501..2362 (audit_forced_pick_fidelity.sh.txt:180). The level pin at ff8116 and the RNG pin at ff80d4 are globals (lines 133-134). The X pins at ff8410/ff8810 land at t-40 >= 2560 (name_moves.py.txt:687), and the HP pin at ff8850 from 2550 is in the P2 block (name_moves.py.txt:699). The claim itself names the effect of the X pin on the first-PLAY-difference frame.
Q4: VIOLATED — Uncovered failure mode: the forced-pick write changes the id, from 0x13 to 0x10/0x11, and that changed-value write could itself change P1-block bytes that are then booked as confirm latches. Neither control writes a changed value. The SELF leg and the donovan-self row both poke the id the leg already holds (audit_forced_pick_fidelity.sh.txt:26-28, 49-51, 87, 135, 138). The must-fire controls cover only a vanished difference (same-leg, lines 308-316) and a wrong cursor path (wrong-cursor, lines 317-322). The claim does not name this gap; it says the opposite: "tested directly on the same write the poked leg performs".
Q5: N-A — No decision, approval or ruling is attributed to the maintainer or anyone else. The packet is only a recommendation.
VERDICT: VIOLATED

THE PACKET

Decision kind: recommendation
Subject: report #151 step 1 as done — what a forced-pick native leg gets wrong is measured
Claim (the working agent's sentence): Within the P1 fighter block, a forced-pick native leg differs from a real cursor pick of the same tenant in exactly three LATCHED bytes (+0x3BD, +0x3E0, +0x3C2) — LATCHED meaning differing at every pre-match sample the two legs share, six by design (1501, 1600, 1800, 2000, 2200, 2362), though the gate asserts only that each leg produced at least 30 dumps, so a dump missing on both legs would shrink the sample set silently — for Phobos and Pyron alike, and a same-id poke over Donovan's own confirm latches nothing. What this rests on that the gate does NOT test or freeze: the globals ($FF8000-$FF83FF) and the rest of work RAM are reported under MEASURE only and never asserted, so "exactly three" is a statement about the P1 block; the TRANSIENT count is reported, not frozen; P2's block (Victor on every leg) is asserted empty pre-match on the POKED-vs-REAL comparison only — the REAL-vs-SELF comparison reports the P1 block alone; two real picks of the same cell by different routes leave route residue (measured under MEASURE, never frozen), so a latched byte is charged to the confirm only inside the P1 block; the SELF leg and the donovan-self row are both same-value writes, so "the poke mechanism is inert" is inferred from them rather than from a changed-value write; the rig's own pokes copied into both legs (name_moves.py: the P1 and P2 position pins at $FF8410/$FF8810 before each event, from frame 2560 at the earliest, and the P2 HP pin at $FF8850 from 2550; no stock pokes, since part 1 is not a meter part) and the gate's own pins (the level at $FF8116 from 2000 and the RNG at $FF80D4 from 2363, both globals) all land outside the P1 block within the pre-match window, so they cannot mask a LATCHED byte on both legs — but the frozen first-PLAY-difference frame is measured with P1's X pinned identically on both legs from 2560, and the search excludes only the latched offsets, so the frozen frame may be a difference at the pinned field itself or anywhere else in the block (its offset is not frozen); whether any latched byte is READ in play is a separate tap, not this gate; and the POKED leg is the gate's OWN REPLICA of the #136 rig's leg — the prologue re-typed and the level and RNG pins copied from the rig's protocol — with nothing asserting that the replica still equals what the rig runs, so a change to the rig's prologue or poke protocol leaves this gate green until a human re-freezes it (the expectation's header asks for that), and "exactly three" is a statement about the replica. No behavioural conclusion about how the character plays is drawn here.
Artifacts (read every one, in full):
  - audit_forced_pick_fidelity.sh.txt (the gate; a shell script)
  - forced_pick_fidelity.tsv (its frozen expectation)
  - name_moves.py.txt (the rig generator whose schedule and pokes both legs copy)

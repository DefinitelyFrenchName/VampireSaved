Q1: OK — The claim's untested premises match what the files show. Only the LATCHED set and the first-play frame are frozen (audit_forced_pick_fidelity.sh.txt:284, 298-304). Globals and the rest of work RAM are MEASURE-only (lines 106-107, 201-235). The P2 pre-match check covers only POKED vs REAL (line 279; REAL vs SELF uses selfl only, lines 277, 281). Route residue (REAL vs ALT) is MEASURE-only (lines 285-289). The dump check is ">= 30" (line 258). The POKED prologue is re-typed as "R R" (line 117), with no check that it still matches the rig. One small inaccuracy: the claim says position pins come "before each event", but donovan_1's part-1 events have no 4th field, so that rig has no pins at all (name_moves.py.txt:207-224, 679-687). This affects the play window only, not the pre-match LATCHED premise.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — Both legs share one write that reaches the P1 block: the P1 X pin "{t - 40}:ff8410" (name_moves.py.txt:687), which lands at 2560 at the earliest. The LATCHED set is judged only on "pre = [f for f in frames if 1501 <= f <= 2362]" (audit_forced_pick_fidelity.sh.txt:180), so the pin cannot reach it. The level and RNG pins write globals ff8116/ff80d4 (lines 133-134), and the HP pin writes P2's block ff8850 from 2550 (name_moves.py.txt:699). None of the shared pokes writes the P1 block before the match. The pin's effect on the first-play frame is named in the claim.
Q4: OK — Wrong tenant on the REAL leg: the identity/cell assertions (audit_forced_pick_fidelity.sh.txt:172-179) and the wrong-cursor must-fire control (lines 318-322). A difference that vanishes: the same-leg must-fire control (lines 310-316) plus exact equality with the frozen set (line 303). The poke mechanism perturbing state: REAL vs SELF (lines 272, 281-282) and the donovan-self row frozen as "-" (forced_pick_fidelity.tsv:17). A noisy instrument: the P2-block negative control (lines 279-280). The failure modes left uncovered are route residue, replica drift, a shrunken sample set, and globals/work RAM, and the claim names each one.
Q5: N-A — The packet attributes no decision to anyone. It says only that re-freezing is left to "a human", which the expectation's header asks for (forced_pick_fidelity.tsv:9-10).
VERDICT: OK

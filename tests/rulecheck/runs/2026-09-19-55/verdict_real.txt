Q1: VIOLATED — The claim says "each new check was run in the failing direction (attribution.txt sections 4-5)", but build/rc170/freeze/rc_static/attribution.txt:42-45 runs only the ours `_n38` count in the failing direction. Three new checks have no failing-direction run, and the claim does not name them as untested: the reference-scoped 0x38 assertion (build/rc170/freeze/rc_static/gates.diff:122, `$1!="ours"`), the vs2 anchor-byte check (gates.diff:33) and the three-copies check (gates.diff:42). The claim also says every moved line has a cause "shown in attribution.txt or the diffs", but build/rc170/freeze/rc_static/expected.diff:200-201 moves the rule5 fact line from "7819 baked / 513 derived of 8332" to "7866 baked / 527 derived of 8393" and no artifact gives a cause. The STRONG findings' "+0x30" rests only on the new file's own comment (build/rc170/freeze/rc_static/pointer_flow_new_baselines.txt:4-5), because the old merged-m17 baseline is not in the packet. "unchanged from M18" for the 11 relocated_bad rows is not shown either: the M18 listing is capped (build/rc170/freeze/rc_static/charmaps.diff:850-913 lists only the 22 rows that went).
Q2: N-A — The claim draws conclusions only about static census and expectation lines ("Phobos's and Donovan's rows now vs2's", addresses, counts). It says nothing about how a move plays or feels.
Q3: OK — The before/after legs share each gate's census tool, but each tool has an in-gate must-fire control that fires (build/rc170/freeze/rc_static/verify.log:22-23, :34, :46-47, :60-61, :68, :75-76, :178-180). The pointer-flow matcher's tolerance, "a match = same (addr,target) or either +0x30" (attribution.txt:2), is a premise the claim names as untested. FREEZE=1 rewriting the compared state (gates.diff:66) is disclosed as "a second run after each freeze", not offered as independent confirmation.
Q4: VIOLATED — Failure mode: the reworked reaction_classes reference check is blind. gates.diff:122 now reads only non-"ours" imm54 rows, but no artifact shows that such a row exists: build/rc170/freeze/rc_static/expected.diff:156-159 goes straight from "vsav2 class-table" rows to "ours imm54". No control fires this check either: attribution.txt:42-45 and gates.diff:126-127 test only the ours exactly-one count. If the check is blind, the premise it guards, "vanilla never produces 0x38", has no guard, and the claim neither controls nor names this gap.
Q5: N-A — The claim credits no decision to a person. "Ruled" appears only in the passive, describing the fixes (gates.diff:50 "THE RULED GAUGE FIX"). "maintainer-ratified" appears only in unchanged gate output about slice G (verify.log:135), and this re-freeze does not rest on that ruling.
VERDICT: VIOLATED

Q1: VIOLATED — tools/audit_lane_carry.py:36-58 HARDCODES the mister lane's subjects and omits `tests/replays`, yet tests/ci_emulator.tsv:264 makes `tests/replays/36_pick_tenant_cell.rpl` a load-bearing operand of a mister gate ("THE ARGS ARE LOAD-BEARING"); ci_emulator.tsv itself, which carries those args and timeouts, is also not a subject, so "every subject it depends on ... byte-unchanged" is an untested premise the claim asserts instead of naming.
Q2: N-A — the claim draws no conclusion about how a move plays: "no claim is made that each new value is what the engine should produce", the conclusions being bytes ("byte-unmoved since the M19 freeze"), counts and expectation provenance.
Q3: VIOLATED — tests/test_killshread_es.sh:67 samples `ff8509:b:stock` while its own header (lines 27-29) records that `tools/name_moves.py:802` pokes `ff8509 := 09` before every event, so the rig writes the compared column; the attribution both legs run through is that same rig (tools/attribute_expectation.sh:119 "So the whole delta is those paths — the gate's SUBJECT did not move"), and a rig defect makes the frozen rows and the re-run agree — the `stock` 8 -> 9 row is the demonstrated case.
Q4: VIOLATED — failure mode neither controlled nor named: the carry's hardcoded subject list being incomplete or stale. The only demonstration is the mame lane's 25 moved paths (STATE.md:81), which exercises paths that ARE listed; nothing reconciles the list against the registry, and STATE.md:81's "reads each lane's subjects from the ruled `cadence` split in `ci_emulator.tsv`" is not what the code does — tools/audit_lane_carry.py:77-88 reads that file only for gate NAMES and never parses the cadence column.
Q5: OK — DECISIONS_HISTORY.md:46 **THE APPROVAL, verbatim:** *"yes"* for the `audit_mask_window_ff42a2` SKIP, and DECISIONS_HISTORY.md:72 **THE RULING, verbatim:** *"Option 1 and ticket to qualify all gates in the future..."* for the re-freeze.
VERDICT: VIOLATED

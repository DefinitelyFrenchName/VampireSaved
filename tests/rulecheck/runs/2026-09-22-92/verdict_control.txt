Q1: VIOLATED — claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but build/rulecheck/2026-09-22-92/a/audit_move_parity.sh.txt:34-36 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters — separate instruments. P2 is Victor ... his state is never compared", :105 compares only 12 mapped fields (none of them $FF87C2), and :25 "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor" — field-scoped, window-scoped, level- and RNG-pinned, not frame-exact over the part
Q2: VIOLATED — the part-1 rig the claim rests on names player-feel behaviour (build/rulecheck/2026-09-22-92/a/name_moves.py.txt:456 "Float [j.8 hold]", :457-459 the other floats, :455 air dashes) yet no artifact produces or shows any image or recording: the gate emits only TSV verdicts (audit_move_parity.sh.txt:172-179) and move_parity.tsv is numbers/labels only
Q3: VIOLATED — both legs are built by one poke function (audit_move_parity.sh.txt:86 "ONE function builds a leg's pokes") and the shared pokes WRITE compared fields: name_moves.py.txt:658 emits "{t-40}:ff8410:{x1:04x}" into the rig's own `pokes`, which audit_move_parity.sh.txt:88 loads as `_base` and :98/:101 apply to native AND ours, while :105 compares "ff8410:w:x"; same for the level pin :89 "{f}:ff8116:06" on both legs against compared field "ff8116:b:lvl", and the RNG pin :90 "{f}:ff80d4:0000" on both legs with no control withholding it from both — a defect in that poke channel makes the legs agree by construction
Q4: VIOLATED — the two declared controls are unpinned-level and no-translation (audit_move_parity.sh.txt:4-5, :185-208); neither fires on the way THIS claim fails: the flavor latch $FF87C2 appears nowhere in the instrument (not in FIELDS at :105, not in the rig's pokes at name_moves.py.txt:669-682), so nothing shows the gate can see a 0x00-vs-0x01 flavor difference or that a poked byte equals the built huitzil.toml value; and the frozen row move_parity.tsv:20 "huitzil_1 DIVERGES Jump [8]+21f" means an IDENTICAL part-1 result FAILS the gate as written (audit_move_parity.sh.txt:176-178) and can only go green via FREEZE=1 (:164-169), a gap the claim does not name
Q5: N-A — the claim attributes no decision to any person ("so flavor_default becomes 0x01 and the five tracks are rebuilt and frozen as M19"); the only maintainer mentions are inside the artifacts as provenance of the move lists (audit_move_parity.sh.txt:8 "the maintainer's move lists")
VERDICT: VIOLATED

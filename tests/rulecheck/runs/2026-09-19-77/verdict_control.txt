Q1: VIOLATED — The claim says the SELF leg pokes "a DIFFERENT id (a changed-value write)", so poke inertness "is tested directly on the same write the poked leg performs". The artifacts show the opposite. audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke … (a write at 1400-1500 to an id that already holds that value)". At :114 and :135-138 the SELF pick writes `$_id`, the row's own tenant id, over a real pick of that same tenant, and :43-45 asserts that id on the REAL leg. The POKED leg's write (0x13 to 0x10/0x11 over Donovan's R,R) is therefore untested. That leaves untested the premise that the three latched bytes come from the confirm and not from the id write, and the claim presents this as tested instead of naming it.
Q2: N-A — The claim draws no conclusion about how a character plays: "No behavioural conclusion about how the character plays is drawn here". The first-play frame is only a frozen number (forced_pick_fidelity.tsv:15-17).
Q3: OK — The shared pokes that could write the P1 block during 1501-2362 all land outside it. The position pins are at t-40 ≥ 2560 (name_moves.py.txt:687, FIRST_EVENT 2600 at :77). The HP pin is $FF8850 from 2550 (:699). There are no stock pokes for part 1 (:646). The level pin $FF8116 from 2000 and the RNG pin $FF80D4 from 2363 are globals (audit_forced_pick_fidelity.sh.txt:133-134), and the pre window is 1501-2362 (:180). The in-play P1 X pin is named by the claim.
Q4: VIOLATED — One failure mode is neither controlled nor named: the changed-value id write itself creates or erases differences in the P1 block, which then get charged to the confirm. Neither poke control can fire on it. SELF writes the id the real pick already holds (audit_forced_pick_fidelity.sh.txt:135, :281 "REAL vs SELF latches nothing (the poke mechanism itself is inert)"). donovan-self is also a same-value write ("poking 0x13 over Donovan's own confirm", :48-51; forced_pick_fidelity.tsv:11-12). The must-fire controls cover other failures: same-leg covers a vanished difference (:308-316) and wrong-cursor covers identity (:317-322). The claim asserts this gap is covered.
Q5: N-A — The packet attributes no decision, approval or ruling to any person.
VERDICT: VIOLATED

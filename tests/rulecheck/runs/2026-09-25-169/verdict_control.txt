Q1: VIOLATED — The claim treats one premise as tested when the gate does not test it. The claim says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The gate says the opposite at audit_forced_pick_fidelity.sh.txt:26-27: "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". The code agrees at lines 135 and 138: `_pick` writes `ff8782:$_id` with `_id="$(row_id "$_row")"`, the same tenant id the REAL leg already holds. So the premise that a changed-value id write leaves the P1 block alone is untested, and the claim does not name it as untested.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here", and it draws none.
Q3: OK — Both legs share the replay tail, the level/RNG pins and the rig pokes (audit_forced_pick_fidelity.sh.txt:126, 131-138). The level pin ($FF8116, from 2000) and RNG pin ($FF80D4, from 2363) are globals outside the P1 block. The rig's position pins ($FF8410/$FF8810) land at 2560 or later and the HP pin ($FF8850) from 2550 (name_moves.py.txt:687, 699), all after the pre-match window that ends at 2362. So no shared write reaches the compared pre-match P1 block, and the claim itself names the in-play X pin as a shared write behind the first-play frame.
Q4: VIOLATED — One failure mode is neither controlled nor named: the POKED leg's changed-value write at $FF8782, which is inside the P1 block at +0x382, could itself cause LATCHED differences that are then charged to the confirm. Neither control writes a changed value. The SELF control is a same-id write (audit_forced_pick_fidelity.sh.txt:26-27, 135). The donovan-self row is also a same-id poke, 13 over R,R (lines 48-50, 87-89). The same-leg and wrong-cursor controls (lines 4-5) test the diff reducer and the cursor identity, not the poke. The claim does not name this gap; it asserts the reverse ("tested directly on the same write the poked leg performs").
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

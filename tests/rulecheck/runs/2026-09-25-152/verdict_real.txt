Q1: VIOLATED — The claim says the form rows are "relabelled relative to the 5HP event", and it implies the old fixed window caused the old pixel difference ("where the old fixed window read a first pixel difference at +34"). That premise is untested and not named as untested, and the artifacts contradict it. The old window was at the same place relative to the 5HP. The old 5HP was at f3315: the old code read "range(3315, 3361)" (build/agent181/df_moves_gate_14z181.diff:296). The old idle holds were "native=3228 ours=3218" (tests/expected/df_moves.tsv:46), which is -87/-97 from f3315. The new rows read "native=5hp-87 ours=5hp-97" (build/agent181/df_moves_got_run9.tsv:44). So the same window measured different content. The flight x went from "first_px_diff=3349" (tests/expected/df_moves.tsv:47) to "first_px_diff=None" (df_moves_got_run9.tsv:45). The native RNG read count went from "native=636" (tests/expected/df_moves.tsv:49) to "native=630" (df_moves_got_run9.tsv:47), and the claim does not mention that change.
Q2: OK — The four contact events and 2HP were re-captured under the final rig on both legs: "captures: 64 PNGs under …/build/agent181/cap_dfx2d" (build/agent181/df_moves_contact_run9.log:23; build/agent181/cap_dfx2d_list.txt:1-69). They were delivered before any freeze: "delivered (third send, same sitting, FINAL rig — run 9" (build/agent181/captures_sent_14z181.txt:15). The frozen file still holds 2HP at two hits (tests/expected/df_moves.tsv:15). The maintainer's read is "NOT YET GIVEN" (captures_sent_14z181.txt:17), which matches the claim. One problem: that caption told the maintainer "no rig write inside any compared window" (captures_sent_14z181.txt:16), and the level and RNG pins contradict it.
Q3: VIOLATED — Both legs share a write of the RNG and the level on every frame of every compared window: "f'{f}:ff8116:06' for f in range(2000,$fr)" and "f'{f}:ff80d4:0000' for f in range(2363,$fr)" (tests/audit_df_moves.sh:202). The artifacts show both of those states choosing the compared hits. The RNG: "picks P1-first or P2-first on every pass from bit 0 of the engine RNG … a fifth hit landed" (DECISIONS_HISTORY.md.lines-1218-1256:22-27). The level: "the play mode sets the speed level RAM:$FF8116, which alone decides the extra logic passes" (DECISIONS_HISTORY.md.lines-1218-1256:6-7), and events "change which move they fire" (tools/name_moves.py:707-709). The two legs consume the RNG differently inside a window ("rng_reads_5hp_window native=630 / ours=621", build/agent181/df_moves_contact_run9.log:40-41), and the per-frame pin resets that difference on both legs. A port difference there would read as agreement, which is the "anything that can steer one" case in docs/project/gotchas.md.lines-5618-5656:33-35. The claim names this premise, but Q3 has no exemption for named gaps. The ruling it cites came with "a must-fire control that leaves the RNG unpinned and must diverge" (DECISIONS_HISTORY.md.lines-1218-1256:32-33), and this gate has no such control.
Q4: VIOLATED — Nothing controls a change in the form rows' content across the re-freeze, and the claim does not name that gap. The attribution tool reads only ev rows ("if r and r[0] == 'ev'", build/agent181/df_moves_attribute.py:8). The form checks test only the palettes, the HUD rows, seed independence and RNG reads > 0 (tests/audit_df_moves.sh:525-528). Nothing checks flight_x or the idle hold except the frozen compare, and the re-freeze replaces that. So the sword's flight-x difference disappearing ("first_px_diff=3349" -> "first_px_diff=None", tests/expected/df_moves.tsv:47 vs build/agent181/df_moves_got_run9.tsv:45) and the native RNG count moving from 636 to 630 would be frozen without any cause assigned. The claim's other gaps (which attack, the hit frames, P2's height and x, the venue, the level and RNG pins) are named, so they are OK here.
Q5: VIOLATED — The claim attributes to the maintainer "the maintainer's 2026-09-15 ruling that cross-game comparisons run at a matched speed level and a pinned RNG". The quoted words are about one gate only. The questions were "How should test_don_immortal_native assert from now on?" and "How should test_don_immortal_native handle the RNG?", and the answer was "Poke the level and $FF80D4-D5 = 0000 every frame 2400-2800 on both legs" (DECISIONS_HISTORY.md.lines-1218-1256:12-16, 29-33). No quoted statement extends this to the parity gates or to cross-game comparisons in general. The #109 fold ("I agree with all recommendations", DECISIONS_HISTORY.md.lines-1257-1282:11-12, 22-23) and the no-gauge-in-Dark-Force rule ("I see, it makes sense that you can't build meter during DF", tests/audit_df_moves.sh:60-61) are quoted correctly.
VERDICT: VIOLATED
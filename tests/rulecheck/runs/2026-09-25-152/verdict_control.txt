Q1: VIOLATED — The claim says "Not tested: nothing further", but the gate lists things it does not cover: audit_move_parity.sh.txt:34-36 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters ... P2 is Victor ... his state is never compared". It compares only the 12 FIELDS at audit_move_parity.sh.txt:105, and $FF87C2 is not among them. Only part 1 was run: move_parity.tsv:20-29 has huitzil_1 through huitzil_10, and all ten are frozen "DIVERGES". The claim that every consumer was enumerated ("pristine vsavj: zero") and the "6851/6851" count do not appear in any artifact. The gate outputs per-part verdicts, not frame counts.
Q2: VIOLATED — The conclusion is that Phobos's part-1 moves play identically to native. That covers jumps, air dashes, floats and normals (name_moves.py.txt:448-460), and the frozen divergence is "huitzil_1	DIVERGES	Jump [8]+21f" (move_parity.tsv:20). The only support is the number "6851/6851". No image or recording appears in any artifact.
Q3: VIOLATED — Both legs share pokes that write fields being compared. The speed-level pin "f'{f}:ff8116:06'" (audit_move_parity.sh.txt:89) goes to both legs (lines 98 and 101), and the same byte is compared as "ff8116:b:lvl" (line 105). The per-event X pin "f\"{t - 40}:ff8410:{x1:04x}\"" (name_moves.py.txt:658) comes from the shared _base pokes on both legs (audit_move_parity.sh.txt:88, 98, 101). Every huitzil part-1 event is "far"-pinned (name_moves.py.txt:448), and this writes the compared "ff8410:w:x". The P1 id is also written on both legs and compared: ff8782 comes from name_moves.py.txt:672 on native and from _pick at audit_move_parity.sh.txt:92 on ours, and is compared as "ff8782:b:id". Both legs also run the same replay, "REPLAY=\"$_r\"" (line 117).
Q4: VIOLATED — The gate has only two must-fire controls, "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:4-5, 60-62). Neither withholds the $FF87C2=0x01 poke from our leg and requires the result to diverge. Neither shows that a runtime RAM poke is equivalent to changing the huitzil.toml flavor_default that is actually being built. The gate contains no ff87c2 poke at all (pokes_for, lines 86-103), so the claimed agreement cannot be tied to the latch. The claim does not name these failure modes as gaps.
Q5: N-A — The packet attributes no decision, approval or ruling to the maintainer or any other person. The flavor_default change and the M19 freeze are stated as the working agent's own action.
VERDICT: VIOLATED
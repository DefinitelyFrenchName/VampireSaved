Q1: VIOLATED — The claim gets one of its stated premises backwards. It says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)", but audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke: ... (a write at 1400-1500 to an id that already holds that value)". Line 135 (`_pick` = `ff8782:$_id` with `_id="$(row_id "$_row")"`, the tenant's own id) and line 138 build it that way. So the premise that was actually left untested, whether the changed-value id write is itself inert, is never stated. The claim presents it as tested.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here", and it concludes nothing about how a move plays.
Q3: OK — The pokes both legs share do not land in the P1 block inside the pre-match window 1501-2362 (audit_forced_pick_fidelity.sh.txt:180). The level pin is global $FF8116 from 2000 (line 133). The RNG pin starts at 2363 (line 134). The position pins run from t-40 = 2560 (name_moves.py.txt:687) and the HP pin from 2550 (name_moves.py.txt:699). The one overlap the artifacts show is the P1 X pin in play, and the claim names it.
Q4: VIOLATED — Nothing controls for a latched byte being caused by the changed-value $FF8782 write rather than by Donovan's confirm, and the claim does not name this gap. REAL vs SELF (audit_forced_pick_fidelity.sh.txt:272, 281-282) and donovan-self (lines 48-51, forced_pick_fidelity.tsv:17) both write "an id that already holds that value". Both prove only that a same-value write is inert. They test a different thing from the claim's "tested directly on the same write the poked leg performs".
Q5: N-A — No decision, approval or closure is credited to the maintainer or anyone else. The packet is the working agent's own recommendation.
VERDICT: VIOLATED

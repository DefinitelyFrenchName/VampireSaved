Q1: VIOLATED — Most untested premises are named, but one is not. Per docs/project/tables/defense_rows.md.lines-1-40:39-40, "Two tables in the damage chain are indexed by the **victim's** character id": the 32-byte curve row and the threshold (lines 9-10, "its threshold as a WORD"). The DMG-OPEN check reads only the curve row (build/agent181/attribution_tool_14z181.diff:63-65, "_b = _bimg[0x0B8940 + _id * 32:0x0B8940 + _id * 32 + 32]"). Yet the claim says "with their defense rows already vs2's", and build/agent181/issue161_comment.md:3 concludes "the extra point enters somewhere common to the three tenants and downstream of the defense row". Nothing in the packet checks Donovan's or Pyron's threshold in the build, and the claim does not name that as untested. Also unshown: Pyron's curve row matching vh2 (the "0x30 in vsavj, vs2 and vh2" at defense_rows.md.lines-1-40:12-13 is the threshold neighbour byte, not the curve row). Also unshown: "the finding is posted on #161" (only a draft, build/agent181/issue161_comment.md).
Q2: VIOLATED — Conclusions a player could feel went into the move lists: "a kick in blockstun cancels nothing (block only)", "PP -> a2:0x2d (the LP version, not the ES)", "ours == native on every gc row" (build/agent181/moves_toml_gc_14z181.diff:10,23), plus the 12/13 damage. They were drawn from node ids and HP numbers only. None of the 25 artifacts is an image or a recording.
Q3: VIOLATED — Both legs get the same per-frame level and RNG writes. "printf '%s;%s;%s' "$_base" "$_lvl" "$_rng"" goes to native (tests/audit_move_parity.sh.lines-160-200:23) and to ours (line 26), where _lvl is "ff8116:06" every frame from 2000 (line 16). The pin sets the compared state: "CONTROL FIRED: unpinned-level — donovan_1 reads 0 IDENT events with the native level pin withheld (30 pinned)" (build/agent181/move_parity_gc_verify.log:44). So the "every gc row IDENT" / "ours == native" result rests on a shared write that would make the legs agree if the pinned level is wrong for either build. The claim discloses this, but its acceptance is still "before the maintainer".
Q4: OK — The gates' controls fire on their own failure modes: stock-starved, pins-ignored, no-translation and unpinned-level (move_parity_gc_verify.log:40-44), swapped-chain and own-name (move_naming_gc_verify.log:12-14), lag-planted (lag_budget_gc.log:7) and hot-block (x2b7ef4_reach_gc_verify.log:27). The DMG-OPEN branch has no discriminating control; the only attribution control is "no-ablation — donovan_11's row stays UNATTRIBUTED" (attribution_gc_verify.log:14). But the claim itself names that gap: "the DMG-OPEN attribution rests on the row comparison the tool makes ... the mechanism of the +1 is not measured".
Q5: OK — The one ruling underneath this is the M19 row port, and the maintainer's own words are quoted: "given the measurements we should take the vs2 rows" (docs/project/tables/defense_rows.md.lines-1-40:4-5). The claim's "accepted in writing at runs 153-156, before the maintainer" names no person and claims no maintainer ruling.
VERDICT: VIOLATED

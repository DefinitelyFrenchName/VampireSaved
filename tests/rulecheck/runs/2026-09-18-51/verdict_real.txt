Q1: VIOLATED — The claim says the build was "merged-m18's verify_op.bin". Every artifact names only build/m3b_merged26 (build/rc169/static_freeze.log:6, sha1 5888a8d2…; tests/expected/reaction_classes.tsv:3; tests/expected/reaction_class_live.tsv:2). No artifact links that directory to merged-m18, and the number is not the mark: tests/expected/roster_pairings/bases.tsv:6,10,13 pair m3b_merged18/20/19 with merged-m11/m13/m12. So which freeze the ours rows are frozen against is an unstated premise. Also unstated: "every constant write to +0x54" was never checked on ours. tools/audit_reaction_classes.py:248-249 runs imm_writers on vsavj and vsav2 only, and only below 0x100000 (line 183). Yet ours' placed code writes +0x54 live (tests/expected/reaction_class_live.tsv:167-172, pcs 41dbe4/41f588/4787f8), while the claim says the static census "sees constant writes".
Q2: VIOLATED — The claim itself names a player-feelable conclusion drawn with no capture: "that a record carrying class 0x38 would have to be blocked low … with no hit of a 0x38 record against a standing guard run and no capture". The artifacts state it as fact at tools/audit_reaction_classes.py:36-39 "(they must be blocked low)" and tests/audit_crouch_flag.sh:2 "the LOW list (standing guard: 0x02 0x03 0x38 0x39) and the OVERHEAD list".
Q3: OK — The legs share rigs and pokes, but the controls show the shared inputs do not write the compared state. With the Down removed, +0x121 stays 0 on both legs (build/rc169/crouch_verify.log:21). The planted +0x382 value is what every later read in all victim parts returns (build/rc169/defense_ctl_planted.log:86-94), so the committed pokes do not rewrite +0x382. A bases.tsv defect would show up as an "unknown" failure (tests/audit_defense_row_reads.sh:210-211), not as false agreement.
Q4: VIOLATED — defense_row_reads logs reads only at RPCS="018c10,018c78,018c40" (tests/audit_defense_row_reads.sh:64; tests/lua/read_tap.lua:69 "if rpcs == nil or rpcs[pc] then"). So a non-own defense or threshold read at any other pc, even on a RUN path, cannot be seen. The planted-flavor control pokes +0x382 and only proves the listed pcs are read. The static "no other reader" scan (tests/audit_defense_row_reads.sh:15-17) is not among the artifacts. The claim names only unrun paths and register-form writers and compares, not readers at unlisted pcs.
Q5: VIOLATED — Two lines attribute a ruling to the maintainer without quoting them: tests/expected/df_modes.tsv:5-6, written by this re-freeze (tests/audit_df_modes.sh:196-197), "the maintainer ruled it disabled (2026-09-18)", and tests/audit_df_modes.sh:20 "maintainer-ruled 2026-09-18: disable the EX route". The only quote, at tests/audit_df_modes.sh:7-12, is about Power vs Change and asks for a double check; it is not that ruling. "the ruled class-0x52 fix" (tests/test_reaction_classes.sh:2) has no quote either. Only the defense-rows ruling is quoted (tests/audit_defense_row_reads.sh:7-8).
VERDICT: VIOLATED

Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the artifacts show otherwise. (a) The comparison is not over the whole part and does not cover all state. It starts at the rig's first event ("THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor", audit_move_parity.sh.txt:25; --first-event at :130-135) and covers only the 12 FIELDS at audit_move_parity.sh.txt:105. It also states "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters ... P2 ... is never compared" (:34-36). (b) Only part 1 is claimed, yet all ten huitzil parts are frozen DIVERGES (move_parity.tsv:20-29), and parts 2-10 (ES, meter, Reflect Wall, Plasma Trap) are not claimed as re-run. (c) What was tested is a RAM poke on ours; the ROM flavor_default change and the five rebuilt tracks were never run. (d) No artifact contains the flavor-latch poke or a sample of it: FIELDS (:105) has no ff87c2, and pokes_for (:86-103) and gen (name_moves.py.txt:670-681) poke no ff87c2. The frozen row still reads "huitzil_1	DIVERGES	Jump [8]+21f" (move_parity.tsv:20). The gate prints verdicts, not frame counts, so no artifact shows where "6851/6851" came from. (e) The claim that "every consumer ... was enumerated in every addressing form" appears in no artifact.
Q2: VIOLATED — The claim concludes that Phobos's movement plays like native. The frozen divergence it is fixing is a jump ("Jump [8]+21f", move_parity.tsv:20), and part 1 includes the jumps, air dashes and floats ("Float [j.8 hold]", name_moves.py.txt:456). The only evidence offered is the number "6851/6851". No image or recording, and no maintainer review of one, appears in any artifact.
Q3: VIOLATED — Both legs receive the same schedule pokes: `_base` is read from the rig json (audit_move_parity.sh.txt:88) and passed to native (:98) and ours (:101). Those pokes write compared fields. The "far" position pin `{t - 40}:ff8410` (name_moves.py.txt:658) resets P1 x on both legs 40 frames before every part-1 event, since all of them are "far" (:448), and "ff8410:w:x" is compared (audit_move_parity.sh.txt:105). The level pin `ff8116:06` is written on both legs every frame from 2000 (:89) and "ff8116:b:lvl" is compared (:105). The pick poke `ff8782` is written on both legs (:92/:101 and name_moves.py.txt:672) and "ff8782:b:id" is compared. So part of the frame agreement is forced by shared pokes rather than measured. Also, native's Phobos is not picked through the select screen; like ours, he is forced by the same poke ("the other two are FORCED by the early-window poke", name_moves.py.txt:59).
Q4: VIOLATED — The gate's two controls are unpinned-level and no-translation (audit_move_parity.sh.txt:4-5). Neither fires on how this claim could be wrong, and the claim names none of these gaps: a RAM poke on ours not being the same as the rebuilt ROM default, the latch mattering in parts 2-10, or a missed consumer (no known-consumer plant exists for the enumeration). For PARTS=huitzil_1 the control part is frozen DIVERGES. That makes no-translation declare itself "CONTROL DEAD ... is not frozen IDENTICAL, so the control cannot discriminate" (:195), and unpinned-level fires whatever it measures, because it only goes dead when base is IDENTICAL (:203-207).
Q5: N-A — The claim credits no person with any decision, approval or ruling. "flavor_default becomes 0x01 ... frozen as M19" is stated as the working agent's own action.
VERDICT: VIOLATED

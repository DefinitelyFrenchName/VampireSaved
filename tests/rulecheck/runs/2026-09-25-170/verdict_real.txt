Q1: VIOLATED — The claim says "the five sweeps are one artifact and rerunnable as tools/air_throw_sweep.sh (walk length 30-150 ... the pair pinned at x 700-950 ... the press offset +4..+28 ...)". The tool does not support that. tools/air_throw_sweep.sh:9-13 has only four modes (lead, phase, press, xpin) and no walk mode; build/agent181/air_throw_sweeps_14z181.txt:3 says the walk field was "since removed". The xpin and press modes would not rerun sections 2-3 either. Those sections were measured with "P2 jumping with P1" (sweeps.txt:13, :29). The tool keeps "the OTHER inputs as the committed rig generates them" (air_throw_sweep.sh:15), and only lead mode rewrites the p2=U line (:63). So the tool now runs with the committed "(-2, 0, \"U\", \"p2\")" lead (tools/name_moves.py.lines-160-220:28). The tool's own xpin comment "P2 with P1, press +14" (:13) is false. The d0_a13/a15/a17 rows (sweeps.txt:51-53) can't be produced by any single mode. The same overclaim appears in name_moves.py.lines-160-220:23 "tools/air_throw_sweep.sh reruns every sweep", gotchas diff:42 and build/agent181/issue169_close.md:3. The claim does not name that the walk, x-pin and press eliminations, which the "three geometry theories ... eliminated" rests on, are one-off scratch results at lead 0 that cannot be reproduced.
Q2: VIOLATED — The conclusion is about a hit or whiff a player could feel: the ticket is "the air throw connects on only one strength" (docs/project/tickets.tsv.lines-80-80:1), and the close says "the MP throw connected at the wall" (build/agent181/issue169_close.md:3) and "fixed" (:1). It rests on RAM chain ids only, and the claim itself says "no capture was produced".
Q3: OK — Both legs share the rig's X-pin pokes, which write compared x (tests/audit_move_parity.sh.lines-1-60:24). That write is excluded, and the exclusion is proven live: "CONTROL FIRED: pins-ignored" (build/agent181/move_parity_169_verify.log:42). Node translation is controlled by "CONTROL FIRED: no-translation" (:43), and the level pin by "CONTROL FIRED: unpinned-level" (:44). The level/RNG pins are named in the claim as shared input. No artifact shows the shared P2 lead writing node, seq or meter on either leg.
Q4: OK — The naming compare is live on exactly this pair of rows. move_naming_169_run2.log:14-15 caught "-3 Galactic Throw [j.6HP] a2:0x14 / +3 Galactic Throw [j.6HP] a2:0x23", and run1.log:15 caught [j.6MP] reading a2:0x13. The own-name check fires when neither strength enters a2:0x23 (run1.log:21-22), and the verify log has "control: the swapped 5LP line fails the compare" (move_naming_169_verify.log:14). The phase gap is now measured at shifts 3-12 (build/agent181/air_throw_sweep_phase3_12.log:12-21, ends 7038..7047 showing each shift applied). The remaining gaps are named in the claim: ours-leg naming, j.4 throws, the mechanism, Huitzil's mechanism, and the equalised pins.
Q5: N-A — The packet attributes no decision to any person. The closing draft says "decided — none (a rig fix, no gameplay decision)" (build/agent181/issue169_close.md:9), and the equalised inputs are only described as "put to the maintainer under STATE 'Decisions pending'", not as approved.
VERDICT: VIOLATED

Q1: VIOLATED — tests/audit_df_moves.sh:91-94,107,117 build `pk` from OUR json, add the vsavj level (ff8116) and RNG (ff80d4) pokes, and pass the same POKES to the vsav2 native leg. Neither a test nor the claim establishes that those addresses mean the same thing on vsav2; the claim names only the shared X pins. The "eight gates" of build/p136_14z168/regress/read_tap_users.txt:2 rest on `grep -rln 'read_tap.lua"' tests/*.sh tools/*.sh`, which covers two globs and one literal that needs the closing quote. Their verdict logs are not in the packet, and the claim does not state that the user list may be incomplete. "the parity gate does on the same inputs" is asserted, but tests/audit_move_parity.sh is not among the artifacts.
Q2: OK — build/p136_14z168/cap_dfx_donovan/DELIVERED.txt:1 "delivered to the maintainer 2026-09-18T13:17:11Z". The claim draws no conclusion about how the moves look or feel: "how the moves look is on the captures sent at 13:17Z, which the maintainer has not judged".
Q3: VIOLATED — tests/audit_df_moves.sh:86 gives both legs the same move input and the same "far" pin (`sched.append((e[0], e[1], e[2], "far"))`), and :117 gives them the same POKES. If that shared input or pin produces no move or no contact on either build, the two legs agree as SAME. tests/expected/df_moves.tsv:18,21,24,28,29 (j.LP, j.LK, Hop Kick, Killshread [LK], Killshread Summon [LK]) are frozen SAME with no hit and no gauge step on either leg. Every other special pays m1=[12…] on both legs (:25-27, :34), so those five SAME rows can be the shared input's agreement rather than the game's.
Q4: VIOLATED — (a) df_moves never checks that a compared event's move came out on either leg. drop_hit (tests/audit_df_moves.sh:207) only perturbs a row with a hit ("ours=[("), and in_mode reads only +0x111. A both-empty SAME (df_moves.tsv:28-29) therefore passes, and the claim does not name this gap. (b) The live gate's per-range liveness accepts a write at any frame: tests/audit_df_field_readers_live.sh:101 has no frame test, and tests/lua/read_tap.lua:23-24 says "writes always log — the boot POST writes are the liveness control". Reads, however, are logged only in WINDOW 1400..end (:80), so a range that is tapped at boot and lost before or during the window still passes. The range-silent control (:105) deletes a range at every frame and does not model this. huitzil_5 reaches no census row at all (tests/expected/df_field_readers_live.tsv:5-11), and the claim does not name this gap either.
Q5: VIOLATED — tests/audit_df_moves.sh:12-15 "the maintainer caught it (2026-09-18, 14z-168): the tenants' vs2 reference is their personal Dark Force", and :31-32 "ours pays no hit gauge (vsav's rule, ruled correct 2026-09-18)". These attribute a baseline ruling and a gauge ruling without quoting the maintainer's words in any artifact. Only the rig-change agreement is quoted (DECISIONS_HISTORY.md.lines-92-104:3-6).
VERDICT: VIOLATED

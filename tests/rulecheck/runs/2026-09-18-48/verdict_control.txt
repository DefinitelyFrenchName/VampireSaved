Q1: VIOLATED — The claim says "Not tested: nothing further", but the gate's own header says "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters" (audit_move_parity.sh.txt:34-35). The comparison covers only 12 P1 fields (audit_move_parity.sh.txt:105), not the whole state, and its window starts at the first event (:25). Only huitzil_1 was run, while huitzil_2..huitzil_10 are all frozen DIVERGES (move_parity.tsv:21-29) and none was re-run with the poke. What was tested is a RAM poke, not the rebuilt flavor_default. No artifact shows the sweep that "enumerated every consumer in every addressing form". "6851/6851" cannot be derived from the packet: HUITZIL["1"] (name_moves.py.txt:448-460) ends at 8980 with frames=end+50=9030 (:683), so FIELD_FROM=2300..FIELD_TO (audit_move_parity.sh.txt:119) samples at most 6731 frames.
Q2: VIOLATED — The conclusion is that Phobos's jump, air dash, float and normals now play as they do natively: the frozen divergence is "Jump [8]+21f" (move_parity.tsv:20), and part 1 contains "Air Dash [j.66]" and "Float [j.8 hold]" (name_moves.py.txt:451-459). A VS2/VH2 flavor is a play-feel choice. The only evidence is the number "6851/6851"; no image or recording appears in any artifact.
Q3: VIOLATED — Both legs get Phobos from the same early-window id poke, not from a real pick. The native select prologue takes the R,R cursor (name_moves.py.txt:50-51, :58-59 "Donovan is the default cursor's R,R pick, the other two are FORCED by the early-window poke") and then writes ff8782:10 (:67, :671-672). Ours gets the same id (audit_move_parity.sh.txt:92, :101). That byte is itself a compared field ("ff8782:b:id", :105) and it selects whose state is compared. The native 0x01 latch reading that the fix copies was taken on this forced path, and ours was then poked to match. If the forced path leaves select-time state such as the latch at a value a genuine Phobos pick would not produce, the two legs agree while the build is wrong. The compared lvl (ff8116) and x (ff8410) fields are also written by shared pokes (audit_move_parity.sh.txt:89, name_moves.py.txt:658).
Q4: VIOLATED — The gate's only controls are unpinned-level and no-translation (audit_move_parity.sh.txt:4-5, :185-208). Neither touches $FF87C2, and pokes_for (:86-103) has no path for a latch poke, so the controlled run is not the run that produced the verdict. Two failure modes are neither controlled nor named: (a) a latch consumer outside part 1's movement and normals, i.e. specials, projectiles, damage and hitboxes (:34-35; huitzil_2..10 still DIVERGES at move_parity.tsv:21-29); (b) the built flavor_default differing from a runtime poke in timing or writer, since the rebuilt tracks are frozen with no re-comparison stated. Also, the in-gate controls run on the first part of SET (:182) and need a part frozen IDENTICAL (:189-196). huitzil_1 is frozen DIVERGES (move_parity.tsv:20), so a huitzil_1-only run would carry a dead control.
Q5: N-A — The packet attributes no decision, approval or ruling to the maintainer or anyone else. "flavor_default becomes 0x01" is the working agent's own step.
VERDICT: VIOLATED

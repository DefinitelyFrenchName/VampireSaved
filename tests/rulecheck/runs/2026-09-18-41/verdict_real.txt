Q1: VIOLATED — the claim calls the ids rows "the last pre-2300 write to each id byte", but tests/audit_throw_registration.sh:138 keeps only writes whose mask is exactly 0xff00, and :134 drops "any other mask" as "a write to the NEIGHBOUR (+0x383)". A word store is logged with mask 0000ffff (build/gates_14z166/throw_reg_verify4.log:39, the move.w at build/meter_probe_14z166/disasm.txt:243). So any later word or long write covering $FF8782/$FF8B82 is invisible to the ids rows. The premise that the id byte is only ever written as a byte is untested and unnamed. Also, "70 tenant rows unchanged in content from the first freeze" is shown by no artifact: the packet carries no prior freeze and no diff.
Q2: VIOLATED — the claim says the sheet "was produced AFTER the numbers were read", and build/meter_probe_14z166/captures/DELIVERED.txt:3 says "AFTER the legacy numbers were read (run 2026-09-18-35 Q2)". STATE.md.lines-44-57:12 shows the legacy conclusion was already in the gate and checked as run -34 before any legacy capture existed ("the legacy meter conclusion had no capture of its own"). The capture also cannot show the rule: STATE.md.lines-44-57:11 says the meter draws in 16-unit bands, so the sheet's own "Demitri 9, Victor 8" would render the same as a swapped 8/9. Those captions are tap numbers, not visible content.
Q3: OK — both legs share tests/replays/judge/02_throw.rpl, the speed-level and RNG pokes ($FF8116/$FF80D4, tests/audit_throw_registration.sh:161) and one reducer. No artifact shows any of them writing or selecting the meter words ($FF850A/$FF890A) or either pair. Each leg's contact frame comes from its own pair tap (:113). The ids are tapped per leg (:165-171), and the HUD names in legacy_throw_sheet.png show the characters independently.
Q4: VIOLATED — "every frozen step is a single write's change by measurement" rests on the `/Nw` marker never printing (tests/audit_throw_registration.sh:117-123). No control shows the marker can print. legs-swapped perturbs rows after reduction (:151), and dead-reader-planted appends an R line to the dead-pair tap (:152). Nothing plants a second W line on a contact frame of a meter tap, and no log in the packet carries a `/Nw`. The failure mode left open: a contact frame with two meter writes would still be read as one write. It is neither controlled nor named by the claim; the named gap covers only the legacy part.
Q5: OK — the one thing attributed to the maintainer is the request for the control, and it is quoted in their own words at tests/audit_throw_registration.sh:25-26 ("the values are quite widly different, we really need that control you're doing with a legacy character") and at STATE.md.lines-44-57:11. No ruling or approval is attributed to anyone.
VERDICT: VIOLATED

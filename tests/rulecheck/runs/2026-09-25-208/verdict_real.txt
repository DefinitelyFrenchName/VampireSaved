Q1: VIOLATED — The claim's NOT-tested list leaves out premises the artifacts show are unmeasured. It restates (t) as a fact: "**the +1 is vsavj's own engine (a legacy victim reads it)**". But STATE.md.lines-72-99:20 records two premises of that finding as not measured: "Premises NOT measured, named: Demitri's attacker-side 5HP data equal between the games (... his attack RECORD is not compared by that census), and the two legs' machine settings (each leg a fresh MAME sandbox)". Neither appears among the claim's untested premises. The claim's only shared-input caveat is "the parity gates' per-frame level and RNG pins" for the ours-against-native rows, and (t) is a vsavj-against-vs2 legacy comparison, so that caveat does not cover it. The claim also names only the level and RNG pins for the trap rows. The trap gate also rests on shared "forced-pick pokes, level and RNG pins" (tests/audit_trap_airborne.sh.lines-1-45:13). A poked pick is known to change state: "a poked pick carries the cursor cell's confirm-time latch" (tests/audit_victim_parity.sh.lines-1-60:57). That premise is not named either.
Q2: VIOLATED — The packet restates conclusions a player could feel: the level-6 whiff (g), pyron_3's event 5 connecting (ee), the dome hitting on the landing frame (bb)/(cc), both air-throw strengths connecting (dd), and the +1 damage (r)/(t). Every capture went out after its conclusion: "AFTER the RAM measurement that found it, not before" (build/agent181/captures_sent_14z181.txt:20, and lines 21-24 the same) and "AFTER the measurement (the naming freeze of a91a555a)" (captures_sent_14z181.txt:39). The claim itself says "still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t)".
Q3: VIOLATED — The ours-against-native findings the packet restates, (i), (j), (l) and the victim rows, share inputs the artifacts show writing or selecting the compared state. First, the level and RNG pins: "level pin on; RNG pin on" (build/agent181/victim_parity_plain_14z181.log:13). The level pin changes the compared lines: "reads 13 line(s) differently from the level-8 pin (frames stripped), so the level pin is load-bearing" (log:38). Second, the one classifier `valid()` that both legs pass through decides which chains are compared at all: "table c: 139 chains native, 139 ours, 81 not chains on both (walks into data), 0 differ in shape" (log:81). Before it, the same chains read "b:0x7f and 76 c chains 0x3d-0x8a differ in shape" (STATE.md.lines-72-99:18). These are accepted in writing, not ruled: "accepted in writing pending the ruling" (docs/NEXT_SESSION.md:24).
Q4: OK — Each way the claim could be wrong is either controlled or named by the claim. An untracked or build/ home is caught by `build-home-planted` (build/agent181/test_close_tools_14z181.log:14; its mode fails on "SELFTEST FAIL: (False, False", test_close_tools_mode_build-home-planted.log:8). A grep that does not reach the tree is caught by `unreachable-control` and `reach-threshold`. A retracted wording that comes back is caught by `gone-reintroduced`. A matcher without case-folding is caught by `case-fold-dropped` (test_close_tools_14z181.log:10-13). Each mode fails on the plant's own FAIL text (the four mode logs, line 4 or 6). A crash reads "REFUSED ... exit code: 3" (close_tools_crash_probe_14z181.log:5-6,10-11). The claim names the rest as its own reading, not a tool's: that the table is complete, that a named section carries its finding, that a cited test exercises it, that carrier figures agree, that patterns are wide enough, that a name resolving to one file is the right one, and the `valid()` drop.
Q5: OK — Each attribution to the maintainer quotes their own words. #173 closed: "yes, identical." (DECISIONS_HISTORY.md.lines-28-48:9). (j) validated: "identical and validated otherwise" (line 10). The first read: "correct" (line 6). The trap caveat (line 13). #169's sheets: "looks identical. I'll double check the air throw on my end but everything looks good" (line 14; build/agent181/captures_sent_14z181.txt:39). The model switch: "I had to switch to Opus 5.5. Resume the work." (STATE.md.lines-72-99:3).
VERDICT: VIOLATED

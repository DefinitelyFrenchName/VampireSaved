Q1: VIOLATED — The claim says the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The gate shows the opposite. audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". At :135 and :138 the self leg gets `_pick` with `_id="$(row_id "$_row")"`, which is the same tenant's id on top of that tenant's real path (:118). donovan-self is a same-id poke too (:87, :89). So the changed-value write that the POKED leg performs (0x13 to 0x10 or 0x11) is never isolated. The claim lists that premise as tested, when it should list it as untested.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here"; the gate freezes only offsets and a frame number (forced_pick_fidelity.tsv:15-17).
Q3: OK — The shared pokes that reach the P1 block are the X pins at $FF8410. They start at t-40, which is 2560 at the earliest (name_moves.py.txt:687, FIRST_EVENT=2600 at :77). That is outside the 1501-2362 LATCHED window (audit_forced_pick_fidelity.sh.txt:180). The level pin $FF8116 and the RNG pin $FF80D4 are globals (:133-134), and the HP pin $FF8850 is in P2's block from 2550 (name_moves.py.txt:699). None of them can write a P1-block byte pre-match on both legs. The X pin's effect on the first-play frame is disclosed in the claim.
Q4: VIOLATED — Failure mode: the LATCHED bytes, or their absence, come from the changed-value id write (the game reacting to the id changing under it at 1400-1500) and not from the confirm latch. The claim charges latched bytes to the confirm. Nothing controls for this. The SELF control only performs a same-value write ("a write at 1400-1500 to an id that already holds that value", audit_forced_pick_fidelity.sh.txt:27), and so does the donovan-self row (:48-51). The claim does not name this gap; it says the opposite, that the changed-value write is tested directly. The same-leg and wrong-cursor controls (:308-322) cover a vanished difference and a wrong identity, not this.
Q5: N-A — No decision, approval or ruling is attributed to the maintainer or to any person.
VERDICT: VIOLATED

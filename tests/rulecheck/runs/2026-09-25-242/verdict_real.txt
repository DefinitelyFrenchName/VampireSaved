Q1: VIOLATED — The claim says the instrument is "ruled out", but the check only covered "the tap, the three gates, the MAME build/patch inputs or the run wrappers" (build/rc183/refreeze2/instrument_commits.txt:1), and the corpus scan only covered tools/name_moves.py and tests/replays/naming/ (build/rc183/refreeze2/corpus_commits.txt:1). The gates also say they run on "tests/audit_move_parity.sh's own inputs and pokes (the merged wheel's path, the level pinned to 6 from 2000 and the RNG from 2363)" (build/rc183/refreeze2/gate_edits.diff:60) and on "tests/audit_move_parity.sh's inputs and pins" (gate_edits.diff:19). Neither scan checked those inputs, and the claim does not name them as untested. Yet it pins every df_field_readers_live and reaction_class_live move on the corpus commits. Its listed NOT-tested items (which corpus commit moves which row, the prg_window counters, #179, the P57 glyph assumption, the third store pair) do not cover this.
Q2: OK — The claim draws no conclusion about how the landing difference plays. It lists "#179's mechanism and how it plays (a capture is its next step...)" as NOT tested, and the ruling agrees: "the next step is a native-vs-ours capture for the maintainer before any mechanism work" (build/rc183/refreeze2/decisions_lines_30_58.txt:7). For #157, the maintainer played their own recordings before ruling (build/rc183/refreeze2/maintainer_playtest.txt:2).
Q3: OK — Both legs (ours/native, M19/M20, probe/M19) share rigs and pins, but no artifact shows a shared rig, poke or pin writing the compared x/face, meter-step or +0x382 state. The M19 leg really is the unfixed build: pyron_3 ours there reads "p1=+8 p2=+12" (build/rc183/refreeze2/m19_vs_m20_reds.txt:52-53), and the dry-run headers name m3b_merged27 against m3b_merged28 (build/rc183/refreeze2/df_field_readers_live.m19_vs_m20.diff:5-8).
Q4: VIOLATED — One failure mode is neither controlled nor named: a change to the parity rig's pins or pokes, which the gates use (gate_edits.diff:19, :60), could move the df_field_readers_live and reaction_class_live rows. The merged-m19 re-run would not catch this (the corpus is the same on both builds), and neither would instrument_commits.txt (its scope, line 1, leaves those inputs out). The other failure modes are controlled or named. The new class has six negative plants (build/rc183/refreeze2/class_groundtruth.txt:3-9). The throw gate's three controls fire, and each mode fails (build/rc183/refreeze2/throwreg_verify_m20.txt:11-47). Move parity passes on M19 against the meter-DIFF table, which shows the meter comparison still sees a difference (m19_vs_m20_reds.txt:4). The P57 glyph and per-replay tracing gaps are named in the claim.
Q5: OK — Each ruling is quoted in the maintainer's own words: "Freeze, ticket it (Recommended)" (decisions_lines_30_58.txt:5), "Pin it to M19 (Recommended)" (decisions_lines_30_58.txt:15), "Freeze M20 now" (decisions_lines_30_58.txt:23), and the playtest "Everything felt normal. However I didn't measure anything" (maintainer_playtest.txt:2).
VERDICT: VIOLATED
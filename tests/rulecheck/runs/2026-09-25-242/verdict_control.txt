Q1: VIOLATED — The claim says the SELF leg's poke is "a poke of a DIFFERENT id (a changed-value write)" and counts that as tested. The gate shows the opposite. audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Lines 135 and 138 poke the row's own id ("f'{f}:ff8782:$_id'"), which the real path already selected. So nothing tests whether a changed-value write perturbs state, and the claim presents that untested premise as tested.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here". forced_pick_fidelity.tsv:15-17 freezes only offsets and a frame number.
Q3: OK — Both legs share the rig pokes and pins. None of them reaches the P1 block inside the 1501-2362 window. The pins come from name_moves.py.txt:684/687 ("{t - 40}:ff8410") and start at 2560 or later. The HP pin comes from name_moves.py.txt:699 and writes P2's $FF8850 from 2550. The level pin (audit_forced_pick_fidelity.sh.txt:133) writes the global $FF8116. The RNG pin (audit_forced_pick_fidelity.sh.txt:134) starts at 2363. The claim itself names the play-window P1 X pin ($FF8410).
Q4: VIOLATED — Nothing controls for this failure mode: a write that changes $FF8782's value (what the POKED leg does, e.g. 0x13 to 0x10/0x11) itself perturbs P1-block bytes, and those bytes then get charged to the confirm. The SELF leg (audit_forced_pick_fidelity.sh.txt:135/138, "$_id") and the donovan-self row (audit_forced_pick_fidelity.sh.txt:48-50, "poking 0x13 over Donovan's own confirm") both write only the value already present. The claim does not name this gap: it says the poke mechanism is "tested directly on the same write the poked leg performs".
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED
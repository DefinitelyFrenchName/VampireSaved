Q1: VIOLATED — The claim says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)". The gate says the opposite: audit_forced_pick_fidelity.sh.txt:26-28 "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". The code matches the gate: :118 gives self the real row_path, and :135/:138 give it `_pick` with the same row's `$_id`, the id checked as the real leg's cell at :179. The donovan-self row is also a same-value write (:87-89: "R R" plus a poke of 13). So whether the poked leg's changed-value write (0x13 to 0x10/0x11) is inert was never tested, yet the claim presents it as tested instead of naming it as untested.
Q2: N-A — The claim is about RAM bytes and says "No behavioural conclusion about how the character plays is drawn here."
Q3: VIOLATED — REAL and SELF share the same real cursor path (audit_forced_pick_fidelity.sh.txt:118), whose confirm selects $FF8782 = the row id (asserted at :172-179). SELF's poke then writes that same `$_id` (:135, :138), a value $FF8782 already holds. If the poke mechanism perturbed state on a changed write, these two legs would still agree by construction. (POKED vs REAL is clean on this point: the shared P1-block X pins land at 2560 or later, per name_moves.py.txt:687, after the pre-match window at :180, and the claim names them.)
Q4: VIOLATED — Uncovered failure mode: a LATCHED byte could come from the poked leg's changed-value $FF8782 write rather than from the confirm, yet the claim charges latched bytes "to the confirm". The only poke controls cannot see this, because both write the value $FF8782 already holds: the REAL-vs-SELF check (audit_forced_pick_fidelity.sh.txt:281) and the donovan-self row (forced_pick_fidelity.tsv:17). The must-fire controls cover other failures: same-leg (:310-316) catches a blind comparison and wrong-cursor (:318-322) catches a wrong identity. The claim does not name this gap; it says the gap is tested.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

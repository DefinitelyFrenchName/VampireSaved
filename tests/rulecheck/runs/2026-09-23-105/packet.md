THE PACKET

Decision kind: recommendation
Subject: the 14z-176 close: the session group, its findings table, and the records of #172 slices S2 and S3
Claim (the working agent's sentence): The 14z-176 group and close row in STATE.md are accurate: the process sweep names attached and orphaned processes as its gate plants them and found, on its first live run, an 18-day orphan pipeline (evidence file); macOS hides the environment of its own binaries and of Chrome, so the sweep finds orphans by where they point; the browser gate's control leg could report a panel it never measured and now cannot; --refusals and the extract no longer read a quoted marker as an event; the procedure family is calibrated on Opus 5.5 with every plant caught and the negative OK beside a caught plant; the agentlib phantom-task fix and the push hook are proven but NOT installed, both awaiting the maintainer, whose words are quoted. NOT tested: that the tier-time browser-gate red was the incomplete-run mechanism (shown sufficient for its message, not proven for that instance); the orphan census (283/198/3), the 798/7 marker count and the transcript-drop count (12) come from inline probes not promoted; the push hook has never run on a real push; each positive fixture was calibrated by one reader; the sweep's task-id label is shown live only and no gate asserts it.
Artifacts (read every one, in full):
  - STATE.md.lines-71-86 (lines 71-86 of STATE.md)
  - DECISIONS_HISTORY.md.lines-30-52 (lines 30-52 of DECISIONS_HISTORY.md)
  - docs/project/agent_architecture_scope.md
  - docs/project/rule_checker.md.lines-113-298 (lines 113-298 of docs/project/rule_checker.md)
  - docs/platform/gotchas.md.lines-2756-2802 (lines 2756-2802 of docs/platform/gotchas.md)
  - tools/agent/sweep.py
  - tests/test_agent_sweep.sh
  - tools/agent/extract.py
  - tests/test_agent_extract.sh
  - tests/test_applier_page_browser.sh
  - build/agent172/sweep_first_live_14z176.txt
  - build/agent172/apply_launchfix.py
  - build/agent172/proposal_pre_push/prove.py
  - build/agent172/proposal_pre_push/pre_push.py
  - tests/rulecheck/ledger.tsv.lines-118-121 (lines 118-121 of tests/rulecheck/ledger.tsv)

Q1: VIOLATED — The claim leaves out three premises it rests on that were never tested. (a) The claim states "macOS hides the environment of its own binaries and of Chrome" as fact. No gate reproduces it: STATE.md.lines-71-86:15 lists it as "none — a platform fact". The gate cannot exercise it either, because every plant runs with the CLAUDE variables stripped: tests/test_agent_sweep.sh:73 "env = {k: v for k, v in os.environ.items() if not k.startswith("CLAUDE")}". (b) The claim says "--refusals ... no longer read a quoted marker". transcript_gaps.py is not among the artifacts, and the comparison behind it is unpromoted: STATE.md.lines-71-86:15 "the HEAD-vs-fixed `--refusals` comparison". (c) The claim says "both awaiting the maintainer, whose words are quoted". The only quoted words are about the agentlib fix (DECISIONS_HISTORY.md.lines-30-52:16-17). No artifact quotes the maintainer on the push hook: STATE.md.lines-71-86:13 says only "The hooks and settings are edit-locked, so it is the maintainer's to install".
Q2: N-A — No conclusion about behaviour a player could feel. The subject is agent tooling: sweep.py, extract.py, the browser gate and the push hook.
Q3: OK — No artifact shows a shared rig or input reaching the compared state so that both legs would agree. apply_launchfix.py:76-96 compares the live agentlib with its patched copy over the same archive and asserts removals only. The calibration negative (ledger.tsv.lines-118-121:4) ran beside a different, caught plant.
Q4: VIOLATED — The claim says the browser gate's control leg "could report a panel it never measured and now cannot". No control fires on the incomplete control-leg path at tests/test_applier_page_browser.sh:229-232. The gate's only must-fire is a content change: tests/test_applier_page_browser.sh:29 "MUST-FIRE: perturbed-copy: broken-page — ... one byte of its inlined manifest's declared set key changed". The probe that starved the budget was not promoted: STATE.md.lines-71-86:15 "its `broken-page` mode, and the starve probe, not promoted". The claim names as untested only whether the tier's red was this mechanism. It does not say that the fixed path itself has no control.
Q5: OK — Each decision attributed to the maintainer is quoted in their own words. DECISIONS_HISTORY.md.lines-30-52:16-20: "Happy to apply it but it'll be in a while. I have no access to the machine right now" and "Build it as described (Recommended)". STATE.md.lines-71-86:1 quotes "#172". The push hook is waiting on the edit lock, not on a decision attributed to the maintainer (STATE.md.lines-71-86:13).
VERDICT: VIOLATED

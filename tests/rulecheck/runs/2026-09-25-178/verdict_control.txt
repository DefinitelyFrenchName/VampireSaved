Q1: VIOLATED — The claim says it has tested something it has not. It says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The gate says the opposite at build/rulecheck/2026-09-25-178/b/audit_forced_pick_fidelity.sh.txt:26-27: "SELF — the REAL path PLUS the same-id poke … (a write at 1400-1500 to an id that already holds that value)". Line 135 (`f'{f}:ff8782:$_id'`, the row's own id) and line 138 (the `*)` branch, which the self leg takes) confirm it. So whether a changed-value id poke is inert is an untested premise, and the claim presents it as tested instead of naming it. Its other stated gaps match the artifacts (globals only under MEASURE, lines 208-210; got.tsv freezes only row, latched and first-play, line 284; P2 asserted only on POKED-vs-REAL, lines 272-280; the six pre-match samples, lines 99 and 180; rig pins from 2560 and 2550, name_moves.py.txt:687 and :699).
Q2: N-A — The claim states "No behavioural conclusion about how the character plays is drawn here", and none is drawn.
Q3: OK — The shared rig pokes do not land in the P1 block inside the 1501-2362 window. The only shared P1-block write is P1's X at $FF8410, which starts at t-40 = 2560 (name_moves.py.txt:687). Level and RNG are global pins (audit_forced_pick_fidelity.sh.txt:133-134). The id poke is on the poked leg only (line 137 vs 138). The claim itself names P1's X being pinned identically on both legs during play.
Q4: VIOLATED — Uncovered failure mode: the latched bytes +3bd/+3c2/+3e0 could be written by game code reacting to a changed id at 1400-1500, not by the confirm. No leg changes the id value except POKED. SELF and donovan-self both write a value the id already holds (audit_forced_pick_fidelity.sh.txt:26-27, 49-50, 135). So neither the "REAL vs SELF latches nothing" check (line 281) nor the donovan-self row (forced_pick_fidelity.tsv:17) can catch this. The claim does not name the gap; it says the gap is tested.
Q5: N-A — The packet attributes no decision, approval or closure to any person.
VERDICT: VIOLATED

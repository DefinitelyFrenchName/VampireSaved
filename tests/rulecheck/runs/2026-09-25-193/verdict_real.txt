Q1: VIOLATED — Four statements in the claim are contradicted by the artifacts, and the claim names none of them as untested. (1) It says the four must-fire controls were "each checked on the FAIL line's TEXT and not on the exit code alone". `build-home-planted` is not: tests/test_close_tools.sh:81-82 prints CONTROL FIRED on any non-zero exit of `homes_tracked.py --selftest --blind`, a traceback included. The mode path at tests/test_close_tools.sh:63-64 also reads only the exit code. (2) It calls the patterns "the 28 wordings retracted by the resolves of runs 163-192". The pattern file's own header says "runs 163-187" (tests/rulecheck/retractions/14z-181.tsv:1), and no row comes from runs 188-192. Run 192's retraction of the stale "35" (build/agent181/resolve192.txt:1) has no pattern. That figure still stands in #174's promoted repro, "the reproduction of GitHub #174 (the 35 never-entered a2 chains)" (tools/chain_census.py:9), while the re-run reads 15+11+10 = 36 (build/agent181/chain_census_rerun_14z181.txt:3,24,40). (3) "(the first four reads were "correct", the rest "identical")" is wrong. Only the first read was "correct"; reads 2-4 were "Move looks identical", "captures look identical" and "yes, identical." (DECISIONS_HISTORY.md:34-36). (4) Two STATE rows written this sitting outlived the reads: "the maintainer's read not yet given" (STATE.md:84) and "(both reads pending)" (STATE.md:93).
Q2: VIOLATED — The claim restates findings a player could feel: event 5 "in fact connects", the guard cancels, the level-6 whiff, the +1 damage. Every capture went out after its conclusion: "AFTER the measurement (the naming freeze of a91a555a)" (build/agent181/captures_sent_14z181.txt:39), and "AFTER the RAM measurement" for each earlier sheet (build/agent181/captures_sent_14z181.txt:20-24). The claim itself says "still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t)".
Q3: N-A — The packet's conclusion is that each finding has a tracked home and a test; it compares no legs. The ours-vs-native results it restates belong to their gates' own packets. The one pair it runs, the real homes tool against its `--blind` variant, is built to disagree (tests/test_close_tools.sh:81-82).
Q4: VIOLATED — Two failure modes are neither controlled nor named. (1) `build-home-planted` reads FIRED when the blind path crashes, not only when the plant goes uncaught. The code is "if python3 tools/homes_tracked.py --selftest --blind >"$W/c2.out" 2>&1; then vs_ctl_dead ... else vs_ctl_fired" (tests/test_close_tools.sh:81-82), with no check of the FAIL text. This is the traceback failure the claim says it fixed for `unreachable-control`, and the claim says all four controls are fixed. (2) The gate runs only `homes_tracked.py --selftest` (tests/test_close_tools.sh:63), never the tool on the live "(12) THE FINDINGS TABLE" row. build/agent181/homes_tracked_14z181.txt:1 carries no tree fingerprint. So a findings row edited after that output, as run 192's resolve did, is re-checked at no tier. The claim draws the snapshot-versus-live-check distinction only for the retraction grep.
Q5: OK — The maintainer's own words are quoted for every attribution. #173 closed on "yes, identical." (DECISIONS_HISTORY.md:36). (j) validated on "looks identical. The 1 frame difference and its origin must be documented but identical and validated otherwise." (DECISIONS_HISTORY.md:37). (dd)/(ee) read as "looks identical. I'll double check the air throw on my end but everything looks good" (DECISIONS_HISTORY.md:41; build/agent181/captures_sent_14z181.txt:39). #173's closure is stated as the agent's act on the read, not as a ruling. The wrong paraphrase "first four reads were correct" is recorded under Q1.
VERDICT: VIOLATED

Q1: VIOLATED — The claim says the SELF leg pokes "a DIFFERENT id (a changed-value write)", so the poke mechanism "is tested directly on the same write the poked leg performs". The gate does the opposite. audit_forced_pick_fidelity.sh.txt:26-27 says "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". Lines 118 and 135/138 build SELF from the tenant's own real path plus a poke of that same tenant's `_id`. The poked leg's changed-value write (Donovan 0x13 to 0x10/0x11) is therefore untested, and the claim calls it tested instead of naming it as a premise. The other untested premises it lists do check out against the gate: globals and WHOLE only under MEASURE (lines 106, 201-211); transient count not frozen (line 284); P2 asserted only on POKED vs REAL (lines 279-280); route residue only under MEASURE (lines 285-289); rig pokes from 2550/2560 at the earliest (name_moves.py.txt:687, 699).
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."
Q3: VIOLATED — The REAL-vs-SELF comparison is the basis for "the poke mechanism itself is inert" (audit_forced_pick_fidelity.sh.txt:281). Both legs share the real cursor path (line 118), and that path has already set $FF8782 (P1 block +0x382) to the tenant's id; the identity check at lines 43-46 asserts exactly that. SELF's poke then writes the same value (line 135), so both legs agree whether or not a changed-value id write disturbs the block. The same holds for donovan-self: R,R plus a poke of 13 over Donovan (lines 48-51).
Q4: VIOLATED — Nothing controls the failure mode where the changed-value id write at 1400/1450/1500 disturbs P1-block state by itself. That would make a difference the gate charges to the confirm (lines 32-34) come from the poke instead. The only poke controls, SELF and donovan-self, are both same-id writes (lines 26-28, 48-51). The must-fire controls cover a different failure: same-leg shows a vanished difference is noticed, and wrong-cursor covers a wrong real-path pick (lines 4-5, 308-322). The claim does not name this gap; it says it is tested.
Q5: N-A — the packet attributes no decision, approval, ruling or closure to anyone.
VERDICT: VIOLATED

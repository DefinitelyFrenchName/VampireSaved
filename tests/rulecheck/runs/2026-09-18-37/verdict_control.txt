Q1: VIOLATED — The claim says "Not tested: nothing further", but the artifacts show several untested premises. (1) audit_move_parity.sh.txt:34-35 says "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters". (2) The verdict covers only the 12 FIELDS at audit_move_parity.sh.txt:105, not the whole state. It starts at "THE RIG'S FIRST EVENT, not at the anchor" (lines 25-29), so "frame-exact over the whole part" overstates it. (3) Only huitzil_1 is cited, while huitzil_2..10 are frozen DIVERGES (move_parity.tsv:21-29) and no re-run of them is shown. (4) No ff87c2 poke appears in pokes_for (audit_move_parity.sh.txt:86-103) or in the rig's pokes (name_moves.py.txt:670-681), and ff87c2 is not in FIELDS (line 105). So the "6851/6851" run is not this gate, the gate outputs a verdict per part rather than a frame count (line 177), and the build's flavor_default (as opposed to a poke) was never compared. (5) The native "0x01" was read on a leg where Phobos is forced by poke, not selected (name_moves.py.txt:58-61, 671-672). (6) The claim enumerated the byte's consumers, not its writers.
Q2: VIOLATED — The conclusion is about how Phobos's part-1 moves play: jumps, air dashes and holds of "Float [j.8 hold]" (name_moves.py.txt:448-459). The divergence it fixes is "huitzil_1	DIVERGES	Jump [8]+21f" (move_parity.tsv:20). The only evidence is the number "6851/6851", and the packet names no image or recording put before the maintainer.
Q3: VIOLATED — Both legs share the forced-select poke ff8782:10 at f1400/1450/1500. It is in the native leg's base pokes (name_moves.py.txt:671-672) and on ours (audit_move_parity.sh.txt:92,101). It lands after the select confirm "1300-1302 p1=1" (name_moves.py.txt:54) on the cursor's pick ("Donovan is the default cursor's R,R pick, the other two are FORCED", name_moves.py.txt:58-60). The poke selects the compared character, and id is itself a compared field ("ff8782:b:id", audit_move_parity.sh.txt:105). So the native 0x01 reading and ours' match were both measured on a Phobos that neither leg really selected, and a defect in that forced select would be common to both legs and make them agree. The level pin ff8116 is also written on both legs every frame and compared (lines 89, 98, 101, 105).
Q4: VIOLATED — The gate's only controls are "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:4-5, 185-208). They run on the first part of SET, which defaults to donovan_1 (lines 79, 182). ff87c2 is neither sampled (line 105) nor poked (lines 86-103). One failure mode is neither controlled nor named by the claim: the rebuilt M19 build's flavor_default does not put 0x01 into RAM:$FF87C2 at the frames the poke did, because another writer overwrites the byte or the default is written at a different time. The claim enumerated readers, not writers, and no control compares the rebuilt build instead of the poked one.
Q5: N-A — No decision, approval, ruling or closure is attributed to the maintainer or any person. The claim is the working agent's own sentence.
VERDICT: VIOLATED

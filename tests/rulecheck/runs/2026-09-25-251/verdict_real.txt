Q1: VIOLATED — The claim says "close_findings_14z183.txt reports no GAP, REVIEW or PROMISE". The artifact says otherwise at build/agent183/close_findings_14z183.txt:3: "NO HOME  0x02231E       (0x2231E)  in: **(6b) #157 — THE LEGACY A/B AND THE DAMAGE FINDING**". STATE.md:95 repeats the wrong "GAPS none". The claim also misses untested premises. (e)'s finding is "connects at every press offset +17..+32", but its test tests/test_meter_gain.sh runs only PRESS_AT +24 (STATE.md:87). The sweep scripts were "not promoted" (STATE.md:95). (m)'s test is a gate "PINNED TO THE M19 BUILD" (build/agent183/packet_homes_14z183.txt:22), so it cannot reproduce the M20 divergence. (j)'s test "names such gates once a run of record exists" (STATE.md:94), so it does not reproduce the finding now. The claim also says (q) has no test, but the table names "tests/test_state_open_lists.sh (form only)" (STATE.md:94).
Q2: N-A — The claim's conclusion is about where each finding is documented and tested, not about how a move plays or looks. The findings about play were concluded in earlier rows (STATE.md:92, STATE.md:93) and are only restated here.
Q3: N-A — No two legs are compared. The instruments are file and grep checks: build/agent183/homes_tracked.txt:1 and build/agent183/retraction_greps_14z183.txt:1.
Q4: VIOLATED — One way the claim can be wrong is that a named test does not reproduce its finding. No control covers that, and the claim does not name it. homes_tracked checks only that files are tracked ("every file named as a home or test checked against git ls-files", build/agent183/homes_tracked.txt:1). The failure is real for (e), (j) and (m), as shown in Q1. The claim also says its test lines are quoted, but build/agent183/packet_homes_14z183.txt quotes no line of the named tests for (b) tests/audit_move_parity.sh, (d), (e), (l), (n), (p) or (q), despite its header "a (test) line names the reproducing gate's own line". The claim does name whether each home is sufficient ("judged here, not by a tool"), so that gap counts as accepted. The one control that did fire, close_findings' GAP for 0x02231E, is reported in the claim as clean.
Q5: OK — The claim itself names no decision-maker. The rulings the packet points to are quoted in the maintainer's own words: STATE.md:93 "Pin it to M19 (Recommended)" and "Freeze, ticket it (Recommended)", STATE.md:92 "Freeze M20 now", and build/agent183/packet_homes_14z183.txt:22 "maintainer-ruled 2026-09-26, 14z-183: \"Pin it to M19".
VERDICT: VIOLATED
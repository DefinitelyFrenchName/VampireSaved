Q1: VIOLATED — The claim treats an untested premise as tested. It says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The artifacts show a same-id write instead: audit_forced_pick_fidelity.sh.txt:26-27 "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". In the code, line 114 sets `_id="$(row_id "$_row")"`, line 135 builds `ff8782:$_id` from it, and line 138 gives that same `$_pick` to both the poked and the self legs. So whether a changed-value id write is inert is never tested, and the claim does not name it as an untested premise.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here". The first-play frame (forced_pick_fidelity.tsv:15, "2980") is frozen as a number and no feel is claimed from it.
Q3: OK — Nothing shared writes the P1 block within the pre-match window (audit_forced_pick_fidelity.sh.txt:180, 1501-2362). The level pin at "ff8116" and the RNG pin at "ff80d4" (lines 133-134) are globals. The position pins land at `t - 40` with t starting at 2600 (name_moves.py.txt:687, 74), so 2560 at the earliest. The HP pin starts at `FIRST_EVENT - 50` = 2550 (name_moves.py.txt:699). Part 1 has no stock poke (name_moves.py.txt:646). The one shared write that reaches compared state is the P1 X pin at $FF8410 (+0x010) during play, and the claim names it itself.
Q4: VIOLATED — One failure mode is neither controlled nor named: the id swap, as a changed-value write, could itself cause a LATCHED difference, and that difference would be wrongly charged to the confirm. Both controls that could catch this use a same-value write. The REAL-vs-SELF control uses a same-id poke (audit_forced_pick_fidelity.sh.txt:135 and 138). The donovan-self row pokes 0x13 over Donovan's own 0x13 (lines 87 and 89: `donovan-self) echo 13` and `"R R"`). Neither control would fire on this failure mode, and the claim says the opposite ("tested directly on the same write the poked leg performs"), so it is not a gap the claim names.
Q5: N-A — No decision, approval or closure is attributed to the maintainer or to anyone else; the packet is only the working agent's recommendation.
VERDICT: VIOLATED
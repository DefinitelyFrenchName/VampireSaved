Q1: VIOLATED — Two statements rest on premises the claim does not name as untested. (1) The move list says "ours == native on every gc row (audit_move_parity)" (build/agent181/moves_toml_gc_14z181.diff:10,23). But the events table shows a gc row as DIFF: "Ifrit Sword [gc v: 623 buffered pre-hit, LP at hit+2]	DIFF	+6	p1hp" (build/agent181/move_parity_events_gc_14z181.diff:16, and pyron line 33). The verdict records only the first difference, so whether the guard cancel itself matched in that event is not shown. (2) The legacy result "so the residual is the two ENGINES' damage pipelines" rests only on Victor's victim-side rows being equal: "Two tables in the damage chain are indexed by the **victim's** character id" (docs/project/tables/defense_rows.md.lines-1-40:39-40), and the replay's own reasoning "Victor's defense row is byte-identical ... so a different HP step here is the two ENGINES, not a row" (tests/replays/judge/04_demitri_5hp_victor.rpl:4-6). Nothing shows that Demitri's attacker-side 5HP data, or the machine settings, are equal between pristine vsavj and vsav2, and the claim does not name this. Also: the threshold values "0x28/0x30/0x30" are printed by no log (the tool at build/agent181/attribution_tool_14z181.diff:69 computes only a boolean). And the posted #161 text still asserts "Pyron's row 0x11 byte-identical in vsavj, vs2 and vh2" (build/agent181/issue161_posted.txt:6), where vh2 is not shown.
Q2: VIOLATED — Conclusions a player could feel went into the move lists before any capture existed: "a kick in blockstun cancels nothing (block only)", "PP -> a2:0x2d (the LP version, not the ES)", "ours == native on every gc row" (build/agent181/moves_toml_gc_14z181.diff:10,23). The only capture was sent afterwards: "sent 2026-09-25 (14z-181) ... AFTER the naming/parity measurement (rule-checker run 157 Q2); the maintainer's read: NOT YET GIVEN" (build/agent181/captures_sent_14z181.txt:22). It shows event 0 only, with no kick, PP or damage event.
Q3: VIOLATED — Both parity legs get the same per-frame writes. tests/audit_move_parity.sh.lines-160-200:16-17 defines "{f}:ff8116:06" and "{f}:ff80d4:0000", and lines 23 and 26 add them to native and to ours alike. That write sets the compared state: "CONTROL FIRED: unpinned-level — donovan_1 reads 0 IDENT events with the native level pin withheld (30 pinned)" (build/agent181/move_parity_gc_verify.log:44). So the "every guard-cancel row IDENT" agreement rests on one shared write. The legacy pair also takes the same pins on both games (build/agent181/residual_gate_14z181.diff:35). This is still unruled: "Decisions pending (human)" (STATE.md.lines-201-232:4,24).
Q4: OK — The controls fire on the gates' own failure modes: stock-starved, pins-ignored, no-translation, unpinned-level (build/agent181/move_parity_gc_verify.log:40-44); swapped-chain and own-name (build/agent181/move_naming_gc_verify.log:12-14,26-29); "CONTROL FIRED: legacy-same ... differs from the frozen rows (6 rows)" and residual-gone (build/agent181/residual_legacy_verify.log:34-35, with both modes at FAIL in the residual_mode_*.log files); lag-planted (build/agent181/lag_budget_gc.log:7); hot-block (build/agent181/x2b7ef4_reach_gc_verify.log:27). DMG-OPEN has no control of its own; the only attribution control is "no-ablation" (build/agent181/attribution_gc_verify2.log:14). The claim names that gap itself: "the DMG-OPEN attribution rests on the row comparison the tool makes ... the mechanism of the +1 ... is not measured".
Q5: OK — No ruling is attributed to the maintainer. #161 is put to them as "Decisions pending (human)" with the read "NOT YET GIVEN" (STATE.md.lines-201-232:4-6; build/agent181/captures_sent_14z181.txt:22). The rulings cited are quoted in the maintainer's own words: "given the measurements we should take the vs2 rows" (docs/project/tables/defense_rows.md.lines-1-40:4-5) and "Pin both, assert frames" (STATE.md.lines-201-232:24).
VERDICT: VIOLATED

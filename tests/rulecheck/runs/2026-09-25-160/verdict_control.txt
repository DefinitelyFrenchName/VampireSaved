Q1: VIOLATED — The claim says the SELF leg's poke writes "a DIFFERENT id (a changed-value write)", but it pokes the same id. audit_forced_pick_fidelity.sh.txt:135 builds `_pick` from `$_id`, the row's own tenant id, and :138 applies it to the self leg. The gate's header, :26-28, says "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". So "the poke mechanism is inert" is only tested for a write that changes nothing. That a changed-value poke leaves the P1 block alone is an untested premise, and the claim presents it as tested instead of naming it.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — The only shared rig write that reaches the P1 block is the P1 X pin: name_moves.py.txt:687 writes `{t - 40}:ff8410`, which is 2560 at the earliest for FIRST_EVENT 2600 (:77). The pre-match window is `1501 <= f <= 2362` (audit_forced_pick_fidelity.sh.txt:180), so the pin cannot mask a LATCHED byte. The claim names its effect on the first-PLAY-difference frame. The HP pin (ff8850, name_moves.py.txt:699) is in P2's block, and the level and RNG pins (ff8116 and ff80d4, audit_forced_pick_fidelity.sh.txt:133-134) are globals.
Q4: VIOLATED — Nothing controls the case where the changed-value id poke itself creates or hides P1-block bytes, which would make the three LATCHED bytes partly the poke's doing rather than the confirm's. Both controls for "the poke perturbs state" use a same-value write. SELF writes the same id (audit_forced_pick_fidelity.sh.txt:135,138). donovan-self pokes 13 over Donovan's own pick (:87,:89; forced_pick_fidelity.tsv:17). The claim does not name this gap; it says the opposite ("tested directly on the same write the poked leg performs").
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

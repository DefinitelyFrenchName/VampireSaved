Q1: VIOLATED — The claim presents some things as tested that the artifacts do not show. (1) It says "each control's mode makes the gate FAIL — logs in the artifacts", but the only mode log is build/agent181/test_close_tools_mode_build-home-planted.log. build/agent181/test_close_tools_14z181.log:8-10 are the in-gate section-3 fires. No mode run of unreachable-control, reach-threshold or gone-reintroduced is logged. (2) It says "the tool now REFUSES a malformed line by number", but tools/retraction_grep.py:58 prints `len(pats) + 1`. That is the pattern's position, not the file line: the header comment (tests/rulecheck/retractions/14z-181.tsv:1) and the blank line 32 are skipped. (3) There are not "32 wordings": 'height 40 on the way down' appears twice, once `retracted` (14z-181.tsv:3) and once `gone` (14z-181.tsv:33), so there are 31 distinct wordings. (4) The mode log for (g), build/agent181/victim_parity_mode5_unpinned-level.log, shows no timing-planted or extract-planted line, although tests/audit_victim_parity.sh:44-45 declare both as MUST-FIRE. Either that log comes from a gate without those controls or they did not run. In it, row k=4 (ours "@4269", native "@4267", len 188 vs 173) is charged "attributed=attacker". None of this is named as untested.
Q2: VIOLATED — Every capture went out after its conclusion was drawn: "AFTER the measurement (the naming freeze of a91a555a)" (build/agent181/captures_sent_14z181.txt:39), and "AFTER the RAM measurement" for the earlier sheets (build/agent181/captures_sent_14z181.txt:20-24). The claim itself says "still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t)".
Q3: N-A — The packet's conclusion is that each finding has a tracked home and a test; it compares no two legs. The ours-vs-native results it restates belong to their own gates. The one pair it runs, the real homes tool against its `--blind` variant, is built to disagree (tests/test_close_tools.sh:88-90).
Q4: VIOLATED — In MODE runs a control is still judged on the exit code alone, and the claim does not name this. tests/test_close_tools.sh:56-57 ("else bad \"$f: $(printf '%s\n' \"$out\" | grep '^FAIL' | cut -c1-160)\"") fails the gate on any non-zero exit of the grep: a REFUSED fused line or a traceback counts, with an empty FAIL text. tests/test_close_tools.sh:67 ("the self-test neither passed nor failed on its own line") also fails the gate. A mode passes when the gate FAILs, so in the unreachable-control, reach-threshold, gone-reintroduced and build-home-planted modes a crashed tool reads as the control firing. That is the traceback failure the claim says is fixed ("a traceback reads DEAD"), and it is fixed only in section 3 (lines 76-90).
Q5: OK — The maintainer's own words are quoted for every attribution. #173 closed on "yes, identical." (DECISIONS_HISTORY.md:36). (j) validated on "looks identical. The 1 frame difference and its origin must be documented but identical and validated otherwise." (DECISIONS_HISTORY.md:37). The #169 sheets: "looks identical. I'll double check the air throw on my end but everything looks good" (DECISIONS_HISTORY.md:41; build/agent181/captures_sent_14z181.txt:39). The first read: "correct" (DECISIONS_HISTORY.md:33).
VERDICT: VIOLATED

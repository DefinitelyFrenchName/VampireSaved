Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part". The artifacts contradict both halves. audit_move_parity.sh.txt:34 says "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters", and lines 35-36 add that P2's state "is never compared". A flavor latch could change any of these, and none is named as untested. The window is also not the whole part: lines 25-29 say "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor". Other premises go unnamed: no artifact shows the "every consumer enumerated" search, and the gate emits a per-part IDENTICAL/DIVERGES verdict, not a frame count, so nothing here yields "6851/6851".
Q2: VIOLATED — The conclusion is about how Phobos plays: that his moves become identical to native. The frozen divergence being cleared is a jump/float timing a player could feel (move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f"). No artifact shows an image or recording put before the maintainer; the only support is a number ("6851/6851 frames identical").
Q3: VIOLATED — Both legs take the same rig pokes, and some of them write fields the gate compares. name_moves.py.txt:658 pokes both fighters' X 40 frames before every "far" event (`f"{t - 40}:ff8410:{x1:04x}"`), and every Huitzil part-1 event is "far" (name_moves.py.txt:448). The gate compares that same address (audit_move_parity.sh.txt:105 "ff8410:w:x") and applies the same `_base` pokes on both legs (lines 98 and 101). It also pins the level on both legs (line 89 "ff8116:06") and compares it too ("ff8116:b:lvl", line 105). So position drift could be reset before each event and the legs would agree.
Q4: VIOLATED — The gate has only two controls: "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:4-5). Neither covers how this claim could be wrong. First, nothing shows the 0x01 poke reached or held at $FF87C2: the gate's pokes (lines 86-103) never write ff87c2, and FIELDS (line 105) never samples it. Second, the rebuilt huitzil.toml build was never run; only a poked build was measured. Third, the byte's effects fall outside the compared fields and window (damage, meter, hitboxes, projectiles, P2). None of these is named by the claim. Also, huitzil_1 is frozen DIVERGES (move_parity.tsv:20), so with it as the control part line 195 reports "CONTROL DEAD ... cannot discriminate", and an IDENTICAL result would FAIL against the frozen row (line 178).
Q5: N-A — The packet attributes no decision, approval, ruling or closure to the maintainer or anyone else; "flavor_default becomes 0x01" is the working agent's own proposal.
VERDICT: VIOLATED

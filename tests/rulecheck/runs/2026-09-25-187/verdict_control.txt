Q1: VIOLATED — The claim says it tested something the gate does not test. It says: "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The gate says otherwise at audit_forced_pick_fidelity.sh.txt:26-27: "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Line 135 builds the SELF poke with `_id="$(row_id "$_row")"`, which is the tenant's own id, and line 138 sends the SELF leg through the same `*)` branch as POKED. So the premise is stated as tested, but it was never tested.
Q2: N-A — The claim draws no conclusion about how the character plays ("No behavioural conclusion about how the character plays is drawn here"). Only the rows at forced_pick_fidelity.tsv:15-17 are frozen.
Q3: OK — The shared pokes do not reach the compared P1 block before the match. The level pin writes ff8116 and the RNG pin writes ff80d4, both globals (audit_forced_pick_fidelity.sh.txt:133-134). The HP pin writes ff8850 from 2550 (name_moves.py.txt:699). The P1 position pin writes ff8410 at t-40, first at 2560 (name_moves.py.txt:687), which is after the last pre-match sample (2362). The one shared thing that does reach the compared state in play is P1's pinned X. The claim names it ("measured with P1's X pinned identically on both legs").
Q4: VIOLATED — Nothing controls for the case where the changed-value write itself causes the difference. That is the POKED leg's write of a new id over a different, already-confirmed id at $FF8782. If that write moved P1-block bytes, they would be charged to the confirm. The claim does charge them there ("a latched byte is charged to the confirm only inside the P1 block"). Both inertness controls use same-value writes only. REAL vs SELF pokes the tenant's own id (audit_forced_pick_fidelity.sh.txt:26-27, 135, 138). donovan-self pokes 0x13 over Donovan's own confirm (lines 48-51). The claim does not name this as a gap; it says it is "tested directly".
Q5: N-A — No decision, approval or closure is attributed to any person.
VERDICT: VIOLATED

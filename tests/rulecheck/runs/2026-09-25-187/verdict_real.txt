Q1: VIOLATED — The claim never states that the test for (gg) exists only as an untracked build file. The test it gives for (gg) is "the close's greps artifact's three reach controls". That artifact is build/agent181/retraction_greps_14z181.txt. build/agent181/homes_tracked_14z181.txt:1 says "A token that resolves only under build/ (untracked by convention) or nowhere is a FAIL". docs/project/gotchas.md.lines-1-160:60-61 says the tool is "the inline script recorded in that close's artifact", but retraction_greps_14z181.txt holds only results and no script (homes_tracked_14z181.txt:1 says "inline script, recorded here" and holds none either). This is the same defect the claim corrected for (w) and (l), yet the claim counts (gg) as tested rather than among its "SIX findings name no reproducing test". It never names that this test is neither tracked nor re-runnable. A further unsupported point: the claim says every home "exists in the tree at HEAD", but the (ff)/(gg) gotcha section, table rows (aa)-(ii) and NEXT_SESSION traps 2/6/7 appear in the artifacts only as diffs (build/agent181/gotchas_close_14z181.diff:9-28, build/agent181/state_14z181_close.diff:21, build/agent181/close_carriers_14z181.diff:136-170).
Q2: VIOLATED — The claim restates hit and move-outcome conclusions: "pyron_3's event 5, named "far (whiff)" by the rig, in fact connects" and that both Galactic Throw strengths connect. It says itself "(q), (i), (dd) and (ee) are RAM findings with no capture". The five captures sent (docs/NEXT_SESSION.md:27-32) include none of event 5, the Galactic Throw pair or Pyron's Zodiac Fire guard cancel.
Q3: N-A — This packet concludes that each finding has a home and a test, and compares no two legs of its own. The ours-vs-native comparisons it restates belong to their own gates (build/agent181/victim_parity_mode5_unpinned-level.log, build/agent181/trap_air_mode4_air-hit.log).
Q4: VIOLATED — The only control for "home or test is tracked" checks backticked tokens only. build/agent181/homes_tracked_14z181.txt:1 says "every backticked token of STATE.md row (12) ... that names a file". So a home or test described in prose that is really an untracked build/ file passes unseen, which is exactly what (gg)'s "the close's greps artifact" does. The claim's stated gap covers something else, only "that a section, docstring or control mode named as a home carries the finding is my reading". It does not name prose-cited tests that resolve to build/ files. The control also cannot be re-run, because its "inline script, recorded here" is not in the file.
Q5: N-A — No decision, approval or closure is attributed to anyone. The claim says only "the maintainer's read is theirs", and (t) is homed in STATE "Decisions pending" (docs/NEXT_SESSION.md:22-27).
VERDICT: VIOLATED

Q1: VIOLATED — The claim says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)". The gate says the opposite. audit_forced_pick_fidelity.sh.txt:26-27 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". At lines 114, 135 and 138 the SELF poke is `ff8782:$_id`, where `_id` is that row's own tenant id. So an untested premise is presented as tested: that the changed-value write the POKED leg makes is inert.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here". The frozen file is byte offsets and one frame number (forced_pick_fidelity.tsv:15-17).
Q3: OK — Both legs share the rig's pokes (audit_forced_pick_fidelity.sh.txt:132-134, 137-138). None of them lands in the P1 block inside the pre-match window 1501-2362. The position pins land at t-40 from FIRST_EVENT 2600 (name_moves.py.txt:687). The HP pin at ff8850 starts at 2550 (name_moves.py.txt:699). Part 1 is not a meter part, so there is no ff8509 stock poke (name_moves.py.txt:646, 709-710). The level and RNG pins are globals (sh:133-134). The claim names the in-play P1 X pin (ff8410) that is shared.
Q4: VIOLATED — Nothing controls, and the claim does not name, the risk that the changed-value $FF8782 write at 1400/1450/1500 (only the POKED leg makes it) perturbs P1-block state. If it did, some bytes the claim charges to the confirm would come from the poke. Both negative controls write a value the id already holds. SELF is described at sh:26-28 and set up at sh:135. donovan-self pokes 0x13 over "R,R IS Donovan" at sh:48-50. The must-fire controls cover other failures only: same-leg catches a vanished difference (sh:308-316) and wrong-cursor catches identity (sh:317-322).
Q5: N-A — The packet credits no decision, approval or closure to any person.
VERDICT: VIOLATED

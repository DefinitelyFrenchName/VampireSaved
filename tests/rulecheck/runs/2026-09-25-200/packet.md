THE PACKET

Decision kind: recommendation
Subject: 14z-181 CLOSE, checklist item 6 — the documentation packet: every finding of the sitting (a)-(kk) has a live home in the tracked tree, and a reproducing test where one exists — after runs 180-199 (198 unrecorded)
Claim (the working agent's sentence): THE DOCUMENTATION PACKET of 14z-181 (checklist item 6): every finding of the sitting has a live home in the TRACKED tree, and a reproducing test where one exists, as the findings table states — (a) the release-claim carriers outlived the release — home `release-inventory` memory + README; test: none, a discipline (measure `gh release` first). (b) the pyron_cosmo poke class was wrong in my packet — home the poke table row; test `test_poke_readback`. (c) an HP pin inside a compared window erased a hit on both legs — home `docs/project/gotchas.md` "A RIG'S SHARED PIN"; test `audit_df_moves` `pin-in-window`. (d) the expiry-wait pin and the second event's x pins — same gotcha; test `sword-poke-planted`. (e) the freeze ran before the checks — home the gate (freeze last); test the `REFUSED FREEZE` path. (f) run 10's sword rows were the old window; the header named ev19 — home the gate header; test: run 155's resolve. (g) a rig tuned at level 8 whiffs at 6 — home gotcha "A RIG IS TUNED AT ONE SPEED LEVEL"; test `audit_victim_parity` `unpinned-level` (the level-6 leg reads every one of its 13 contact lines differently and fails the frozen lines — the LEVEL dependence; the whiff itself is the gotcha's trace-read fact, asserted by no gate; the mode RE-RUN on the current gate at the close, victim_parity_mode_unpinned-level_14z181.log: the gate FAILs as a mode must, the level-6 leg reading its 13 contact lines differently, and every one of the ten controls fires in the mode run too — run 198 found the mode5 log I had cited was a run of an OLDER gate build, and my 'section-3 controls skipped in a mode' sentence wrong for this gate; the PLAIN run victim_parity_plain_14z181.log PASSES with all TEN controls FIRED; row (4) now counts the ten, not the five it was written with (run 199); run 195 found both gates' EXPECTS lines counting their old controls — five for ten, two for three — corrected). (h) the 8-label path cut hid the b:0x19 label — home `reaction_map.py` docstring; test `victim_parity.tsv` rows k=4-6. (i) Pyron enters b:0x03 where native enters b:0x19 — home `engine_internals.md` "The OURS leg", **#173** (CLOSED on the maintainer's read of the capture, *"yes, identical."*); test `audit_victim_parity`. (j) Victor's a2:0x38 differs by one data frame — home the same bullet (the attribution VALIDATED by the maintainer's read: *"identical and validated otherwise"*); test `audit_victim_parity`'s `attacker=` column (rows k=4-10 of `victim_parity.tsv`) and its `tenant-planted` control. (k) the same-data TSV lists a/b/c only, a2 is a count — home `victim_parity.py` docstring; test the runtime census. (l) 76 Phobos c "chains" are walks into data — home `victim_parity.py` `valid()`; test `audit_victim_parity`'s chain-shape identity check and its `chains-planted` / `label-planted` controls. (m) "0 once bound to the region" written before the measurement — home STATE row (9), corrected in place; test: none, a discipline. (n) a stale artifact staged before its regeneration — home NEXT_SESSION trap 6 (and the memory `rulecheck-workflow-traps`); test: run 156's resolve. (o) no control on the chain-shape and label checks — home the gate's two new controls; test their modes. (p) the no-translation control of the hitbox gate read DEAD on 0/0 frames — home `hitbox_parity.py` comment; test its mode. (q) Donovan's and Pyron's guard cancels fire on native as Phobos's do; PP gives the LP version — home the move lists; test `test_move_naming`. (r) Demitri's 5HP takes +1 on our build from EVERY tenant — home the attribution class DMG-OPEN; test `audit_move_parity_attribution`. (s) the DMG-OPEN check read the curve row alone — home the tool comment; test: the threshold byte in `ROW_NATIVE`. (t) **the +1 is vsavj's own engine (a legacy victim reads it)** — home `audit_phobos_dmg_residual.sh` header + STATE "Decisions pending"; test its `legacy` rows and `legacy-same`. (u) a gate edited while a measurer ran it — home NEXT_SESSION trap 7; test: none. (v) the 156 spawn wrapper refused at record — home NEXT_SESSION setup note; test: `rulecheck.py record`. (w) Donovan's vs2 EX activation enters a2:0x51/0x52 — home `engine_internals.md` "The OURS leg" (its own bullet) + **#174** (its repro now `tools/chain_census.py`, promoted); test: none yet (the probe was scratch, not promoted: the ticket's rig comes with its inputs). (x) the throw charge absorbed a timing difference of any size — home `victim_parity.py` `explained()` and the gate header (Donovan's stand follows the release, Phobos's is fixed from the grab); test `timing-planted`. (y) the data view the chain check reads was assumed the romset's — home the gate's decrypt-and-cmp step; test: its FAIL branch (untested: no build with a stale view exists). (z) the vs2 extract both legs are labelled from was assumed vsav2's own — home the gate's per-tenant extract check; test `extract-planted`. (aa) the column on an airborne victim takes the air stager's class 7 identically on both legs — home `audit_column_shock.sh` 1c; test `air-marker`. (bb) RETRACTED and replaced by (cc): the trap dome does NOT connect with the jumping victim in the air — he clears it and is hit on the LANDING frame, ground path — home `audit_trap_airborne.sh`, the residue **#175**; test `air-hit` (a hit planted inside the airborne span must fail the landing-frame clause) + `grounded-hit` + `air-class`. (cc) THE GROUND IS y = 40: an airborne assertion is `y > 40`, a hit reading y = 40 is a hit on the ground or on the landing frame; my `y > 0` called a landed victim "the falling victim at height 40" and a six-timing sweep whose rows all read 40 "confirmed" it — the rule-checker (run 164) found it from a sweep row where the jump was pressed after the hit — home `docs/project/gotchas.md` "THE GROUND IS y = 40"; test `audit_trap_airborne` `grounded-hit`. (dd) an air-throw rig's outcome follows the victim's height at the grab frame in every swept row, and P2's two-frame jump lead is the rig knob that sets it at every phase; cause vs co-varying state not isolated, a same-frame tick race the hypothesis (#169; the sheets read by the maintainer: *"looks identical"*) — home `docs/project/gotchas.md` "AN AIR-THROW RIG IS DECIDED BY THE VICTIM'S JUMP LEAD" + `name_moves.py` air_throw; test `test_move_naming` pyron (both lines a2:0x23). (ee) a near event whose previous event did not knock P2 left ends at the right wall (P1 pushes at ~2 px/frame for most of the 150-frame walk, from 664 where the screen edge clamped the pin — the camera at 640 — and from 552/563 where it did not; a camera poke reads the unpoked value one frame later, overwritten at once); pyron_3's event 5, named "far (whiff)" by the rig, in fact connects — home `docs/project/gotchas.md` "AN AIR-THROW RIG IS DECIDED BY THE VICTIM'S JUMP LEAD" (the per-event table) + `name_moves.py` "WHERE A NEAR EVENT REALLY STANDS"; test: none yet (a rig fact; the "far (whiff)" name is a wording to correct at pyron_3's next re-freeze). (ff) "every timing" must be every timing — two packets claimed a traced range a third of which was untraced, and "every phase mod 13" from three shifts at one phase; the fix was to trace every frame and let the script that writes the artifact compute the counts and LIST its gaps — home `docs/project/gotchas.md` "\"EVERY\" MEANS EVERY" (rule half 1) + NEXT_SESSION trap 2; test: run 165/166/169's resolves. (gg) a retraction grep pattern narrower than its carrier (a trailing semicolon, a line wrap, a code-spanned token) reads 0 hits over a live carrier — home `docs/project/gotchas.md` "\"EVERY\" MEANS EVERY" (rule half 2) + `tools/retraction_grep.py`; test `test_close_tools` (`unreachable-control`: the three reach controls of `tests/rulecheck/retractions/14z-181.tsv`, one line-wrapped across a comment prefix; `case-fold-dropped`: the matcher's own self-test on planted texts, which a no-fold variant must fail). (hh) the mid-session tier went red on my own two changes (a FOLLOWS line that named fewer files than the gate's text, a generated page not regenerated after a header edit) — home row (5); test `test_gate_follows`, `test_gate_coverage_current`. (ii) a session's own STATE banner outlived its facts three times in one sitting (a count, a run range, an "every") — home the close checklist's item (6) as this sitting ran it (runs 180-188); test: none, a discipline — write the banner LAST (applied at runs 196-199, each finding the banner's own counts wrong again — packets, the OK-read sentence, run 179's N-A — until run 199 found the subject and the banner disagreeing on the packet count; the banner now names no last run and no packet count: it says the sitting's runs begin at 147 and the CLOSE row names the last, a figure written once, at the close). (kk) a side question asked with the btw command ran as a FORK of this session with its full context, and the fork did not stop at the answer: it re-ran the close's own steps — re-prepared rule-checker run 198 (the run dir deleted and rebuilt, the slots re-drawn, my saved verdict gone), spawned two readers whose reports went to the fork, and appended a classing note to the greps artifact — so the run I had spawned no longer matched its prompt files; found when `record` refused the spawn check — and the id 198 was LOST to it: re-prepared and read afresh by me, the spawn check still found my pre-fork spawn under the same run name and refused again, so the packet moved to id 199 (198's verdicts kept as an artifact, no ledger row) — home NEXT_SESSION trap 10 and the memory `btw-fork-acts`; test: none, a discipline (a side question is answered by a fork that must not act; after any fork, `git status` and the run dir's mtimes before the next `record`). (jj) the close's own two file-level checks lived in inline scripts recorded in no file, and the table they checked cited FIVE build-only files as homes or tests (a home of (j), the tests of (l), (w) and (gg), the greps method itself) — the rule-checker found the first at run 185 and the tool found the rest (runs 186-187) — home `tools/homes_tracked.py` + `tools/retraction_grep.py` (the pattern file `tests/rulecheck/retractions/14z-181.tsv`) and the gotcha's tool pointer; test `test_close_tools` (`build-home-planted`: the tool's BLIND variant must fail the self-test whose planted row cites build/ files, an unresolved name, an unresolved gate stem and a parenthesised scratch clause) — and every file the table names as a home or test is a TRACKED file, checked by a TOOL now (tools/homes_tracked.py; its output build/agent181/homes_tracked_14z181.txt is a SNAPSHOT of one row — its header carries the ROW FINGERPRINT, the sha1 of the row's text — and the CHECK is the gate's own `--newest` run on STATE's newest findings-table row at every tier, added at run 193; five reads of the row — every backticked file token against `git ls-files`, a gate cited by its stem (`test_x`, `audit_x`) resolved to tests/<stem>.sh, every ticket cited as #N resolved to its row in the tracked index docs/project/tickets.tsv whose answers must cite no build/ path and no untracked file — an ../ input is out of tree by rule and listed — with a planted index in the self-test (a clean row, a build/-citing row, an untracked-citing row) and the --blind variant blind to this branch too (this read's first cut FAILED on #175, whose repro was a build/agent181 sweep artifact — corrected to the gate's SWEEP mode — and run 189 found #174's repro was a build/ census output — its scratch script promoted to tools/chain_census.py and RE-RUN at the close against the out-of-tree pages (build/agent181/chain_census_rerun_14z181.txt: 36 never-entered a2 chains, 8 with attack records — Pyron's a2:0x03 joined the list once the HP throw rig stopped whiffing into it; #174's row and story updated); (w) itself now has a tracked home, its own bullet in engine_internals.md "The OURS leg"), every build/ path in prose or code, every document cited in PROSE by its bare name (README, NEXT_SESSION, STATE, HANDOFF, the histories) or as a bare name.ext outside backticks resolved to a tracked file (run 190: three such homes — README, NEXT_SESSION, STATE — had rested on my reading), every test clause up to the next finding letter, parentheses included, that cites an artifact/.txt/.log/scratch without a tracked script — 24 file tokens, 4 tickets and 3 prose-cited documents, 0 FAIL, 1 REVIEW, answered by hand under the output: (w)'s clause 'none yet (the probe was scratch, not promoted ...)' names no test; its first two cuts found five build/agent181 artifacts cited as a home of (j) and the tests of (l), (w) and (gg) and its own method, all corrected, and run 188 found the stems unresolved and the clause read stopping at a parenthesis, both fixed and both planted in the self-test, as are the ticket and prose-document reads), and each section-named home (a heading, a docstring, a control mode) exists in the WORKING TREE that the close commit records — HEAD does not carry the sitting's edits yet, the four diffs are those edits — carrying the finding (read by me, section by section); the retraction greps are a TOOL now (tools/retraction_grep.py over the tracked pattern file tests/rulecheck/retractions/14z-181.tsv; the output build/agent181/retraction_greps_14z181.txt is a SNAPSHOT of one tree — its header carries a TREE FINGERPRINT, the sha1 of every scanned file's normalised text with the rule-checker ledger left out — the ledger is scanned for hits but `record`, `resolve` and `prepare` append to it after every snapshot by construction, so a fingerprint that included it could never match a fresh run's (found at run 198's prepare: the only scanned file newer than the snapshot was the ledger), and it was RE-RUN after run 199's corrections — the banner, row (4)'s control count, (gg)'s test, the model switch — the last edits before this packet, its classing note saying so; the CHECK is the gate's own run on the live tree at every tier, never the file — run 192 found the previous output older than the opener it listed a hit in, run 196 one older than the EXPECTS edits): the 31 distinct wordings retracted by the resolves of runs 163-193 (the pattern file's header says so; run 193's four — the stale '35' in the census docstring, two STATE rows that outlived the reads, a wrong paraphrase of them — added as `gone`; run 194 found one wording listed twice, the `retracted` copy removed and the tool now REFUSING a repeated pattern), twenty of them marked `gone` (must read 0 hits anywhere; the tool exits 1 on any, so a re-introduced carrier is a red) and eleven `retracted` (may survive in quoted retractions and records, classed by hand), each the shortest distinctive fragment without trailing punctuation, matched over each file's WHOLE text with runs of whitespace, backticks and `#` comment prefixes collapsed and the text CASE-FOLDED (so a line-wrapped, code-spanned, comment-wrapped or UPPER-CASE heading carrier is found — run 196 found STATE row (8)'s heading carrying the stale 35 past a case-sensitive match; the heading now carries both counts), over docs (the GENERATED pages included — docs/GOTCHAS.md, the gate pages — since a stale generated page is a live carrier; run 189), HANDOFF.md, STATE.md, STATE_HISTORY.md, DECISIONS_HISTORY.md, tests, tools and build/manifest, excluding only the staged packets under tests/rulecheck/runs/, the tool and the pattern files, every hit listed with its matched text and classed by hand below the tool's output (quoted retractions, ledger run-subject records, older unrelated sentences, and for 'reruns every sweep' the carriers that kept the words and added the LEAD=0 condition — the corrected wording), none a live claim, with THREE reach controls found (the tool exits 1 when one reads fewer hits than it demands): a known live heading; a known live corrected wording that must be found in BOTH its plain and its code-spanned carrier (`reach:2`); a known carrier line-wrapped across a `#` comment prefix in the trap gate's header; both tools are locked by the new ci_portable gate tests/test_close_tools.sh (PASS — the log RE-WRITTEN at this packet, 15 lines ending in PASS: run 198 read an 8-line copy, cut mid-run — its FIVE must-fire controls FIRED — the fifth, `case-fold-dropped`, added at run 199: the grep's matcher is self-tested on PLANTED texts (an upper-case heading, a comment-wrapped and a code-spanned wording), and a `--nofold` known-bad variant must print SELFTEST FAIL naming the upper-case case; run 199 found no control would fire if the fold were dropped, and a LOWER-CASE reach pattern I tried first did not isolate it either — a probe with the fold removed still found six lower-case carriers of that wording in the tree, so the pattern was reverted and the check moved onto planted text — each checked on the FAIL line's TEXT and not on the exit code alone — the first cut of `unreachable-control` had fired on a traceback and run 193 found `build-home-planted` and the self-test's own read still on the exit code, both now on the SELFTEST line's text (a traceback reads DEAD), the pattern file lacking its final newline so the plant fused with the last line; the tool now REFUSES (exit 3, not a verdict) a malformed or repeated line by its FILE line number: `unreachable-control` — a planted unreachable reach pattern is named with (0 < 1); `reach-threshold` — the reach:2 control raised to reach:3 is named with (2 < 3), so a threshold that stopped counting cannot pass; `gone-reintroduced` — a live wording re-marked gone is named as back in the tree; `build-home-planted` — the homes tool's BLIND variant (`--blind`, every build/ read disabled) prints SELFTEST FAIL naming the two build/ reads as missed while the real tool prints SELFTEST PASS; each control's mode makes the gate FAIL on the plant's own FAIL text — all five mode logs in the artifacts — and a tool that exits non-zero WITHOUT its FAIL line, in a mode or in the plain run, makes the gate print REFUSED and exit 3 (dead to the runner), never a red: run 194 found the mode paths still reading the exit code, and a probe with a malformed pattern file in the unreachable-control mode reads REFUSED, exit 3 — its log close_tools_crash_probe_14z181.log is an artifact) and named at step (2) of the close checklist in STATE's header. NOT tested: that the table (a)-(kk) is EVERY finding of the sitting — it is checked by my reading against the opener's traps, the STATE rows and the resolves of runs 147-199, not by a tool (run 185 found four it lacked); whether a home's wording survives the next rollover (STATE rows roll to STATE_HISTORY verbatim, the documents stay); that a named home is the RIGHT SECTION of its document, that a cited test EXERCISES its finding (runs 183 found (g) citing the level-8 reproduction for a level-6 fact and (bb) omitting the control the gate itself names — the trap gate's plain run trap_air_verify4.log shows its three controls FIRED, air-class among them), that a gate NOTICES a declared control that never ran (that is the runner's census, `fired N / declared N`, at every tier — not the gate's own read), that a live carrier's FIGURES agree with the findings, that a fragment pattern is wide enough for every carrier, and that the 31 patterns are the sitting's COMPLETE retracted set, are all checked by my reading, not by a tool (tools/close_findings.py sees addresses, not findings: 0 checked, its clean result cited as what it is and never as completeness — runs 181-199 each found carriers, citations, a home's own stale sentence or missing rows my reading had missed); SEVEN findings name no reproducing test — (a), (m), (ii) and (kk) are disciplines, (u) the running-gate trap, (w) the #174 probe (scratch, not promoted), (ee) the near-event geometry ('none yet') — (y)'s FAIL branch is untested for want of a stale build, and (f), (n) and (ff) cite past rule-checker resolves as their tests; the captures: the maintainer READ the eight earlier sheets in the sitting — every one identical, verbatim in DECISIONS_HISTORY.md 'Ruled 2026-09-25 (14z-181) — the maintainer's reads' — which covers (i) (*"yes, identical."*, #173 CLOSED on it), (j) (*"identical and validated otherwise"*), (q)'s Donovan half and (aa)/(bb)/(cc) (the first read was *"correct"*, the other eight *"identical"* in the maintainer's words); then asked *"One thing where captures are missing is #169"* — the three #169 sheets (pyron_3 events 6, 7 and 5: both Galactic Throw strengths, and the 'far (whiff)' that connects) were taken under the shipped recipe and SENT, and READ: *"looks identical. I'll double check the air throw on my end but everything looks good"* — so (dd) and (ee) are captured and read, the air throw the maintainer's to double-check on their side; every capture of the sitting went out AFTER its RAM conclusion was drawn (the record says so per sheet) and the trap sheet went out under the retracted caption 'at height 40 on the way down' — corrected on #163 and in the opener, and stated to the maintainer in the close report; still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t) — RAM findings whose read is the maintainer's, standing since run 167; the tracked-homes tool checks FILE NAMES only — that a section, docstring or control mode named as a home carries the finding, and that a resolved gate EXERCISES it, is my reading; a prose-cited test that names an untracked file in words other than artifact/.txt/.log/scratch passes its test-clause read unseen, a test cited by a tool's stem without its extension (none in this table) is not resolved, and a home cited in prose by a name that is neither a project document nor a name.ext (a section title, 'the poke table row', 'the gate header', 'the move lists') is resolved by my reading alone; run 198 is UNRECORDED (finding (kk)): the fork's version of it was discarded (its readers reported to the fork), and my own re-prepared 198 — read by fresh readers, the plant CAUGHT, the real packet VIOLATED on Q1 for the stale counts corrected here — was REFUSED by `record`'s spawn check, which found my pre-fork spawn under the same run name beside the fresh one; its prompts and verdicts are kept as an artifact (build/agent181/run198_unrecorded/) and the ledger has no 198 row — the packet moved to id 199 (read, VIOLATED, resolved) and this one is id 200, both by `prepare --id` — the counter derives the next id from the ledger and would reuse 199 because 198 has no row; a home in a memory note ((a), (n), (kk)) is outside the tree by design and README / NEXT_SESSION carry those findings in-tree; a ticket home's GitHub story is outside the tree by design and its index row is what the tool reads.
Artifacts (read every one, in full):
  - STATE.md.lines-46-56 (lines 46-56 of STATE.md)
  - STATE.md.lines-72-101 (lines 72-101 of STATE.md)
  - STATE.md.lines-135-142 (lines 135-142 of STATE.md)
  - DECISIONS_HISTORY.md.lines-28-48 (lines 28-48 of DECISIONS_HISTORY.md)
  - build/agent181/captures_sent_14z181.txt
  - build/agent181/close_findings_14z181.txt
  - build/agent181/homes_tracked_14z181.txt
  - tools/homes_tracked.py
  - build/agent181/retraction_greps_14z181.txt
  - tools/retraction_grep.py
  - tests/rulecheck/retractions/14z-181.tsv
  - tests/test_close_tools.sh
  - build/agent181/test_close_tools_14z181.log
  - build/agent181/test_close_tools_mode_build-home-planted.log
  - build/agent181/test_close_tools_mode_unreachable-control.log
  - build/agent181/test_close_tools_mode_reach-threshold.log
  - build/agent181/test_close_tools_mode_gone-reintroduced.log
  - build/agent181/test_close_tools_mode_case-fold-dropped.log
  - build/agent181/close_tools_crash_probe_14z181.log
  - build/agent181/close_carriers_14z181.diff
  - build/agent181/name_moves_close_14z181.diff
  - build/agent181/trap_air_mode4_air-hit.log
  - build/agent181/trap_air_verify4.log
  - build/agent181/victim_parity_mode_unpinned-level_14z181.log
  - build/agent181/victim_parity_plain_14z181.log
  - docs/project/gotchas.md.lines-1-160 (lines 1-160 of docs/project/gotchas.md)
  - build/agent181/gotchas_close_14z181.diff
  - docs/game/engine_internals.md.lines-990-1030 (lines 990-1030 of docs/game/engine_internals.md)
  - tools/name_moves.py.lines-160-220 (lines 160-220 of tools/name_moves.py)
  - tools/name_moves.py.lines-735-795 (lines 735-795 of tools/name_moves.py)
  - docs/NEXT_SESSION.md
  - build/agent181/state_14z181_close.diff
  - tests/expected/victim_parity.tsv.lines-1-12 (lines 1-12 of tests/expected/victim_parity.tsv)
  - tests/audit_trap_airborne.sh.lines-1-45 (lines 1-45 of tests/audit_trap_airborne.sh)
  - tests/audit_victim_parity.sh.lines-1-60 (lines 1-60 of tests/audit_victim_parity.sh)
  - docs/project/tickets.tsv.lines-74-90 (lines 74-90 of docs/project/tickets.tsv)
  - tools/chain_census.py
  - build/agent181/chain_census_rerun_14z181.txt
  - tests/rulecheck/runs/2026-09-25-162/verdict_real.txt
  - tests/rulecheck/runs/2026-09-25-167/verdict_real.txt
  - tests/rulecheck/runs/2026-09-25-179/verdict_real.txt
  - tests/rulecheck/runs/2026-09-25-199/verdict_real.txt
  - build/agent181/run198_unrecorded/README.txt
  - build/agent181/run198_unrecorded/verdict_b.txt
  - build/agent181/resolve199.txt

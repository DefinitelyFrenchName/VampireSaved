Q1: OK — audit_forced_pick_fidelity.sh.txt:99 (DFRAMES gives exactly six pre-match samples 1501..2362), :258 ("-ge 30" is the only dump-count assertion), :166 (VOID only when counts differ, so a dump missing on BOTH legs shrinks `pre` silently), :189-199 and :201 (globals/P2/transient/first_play reported, not frozen), :281 (REAL vs SELF reads field 1 only), :90-94 (ALT route residue MEASURE-only), :131-135 (level pin $FF8116 from 2000, RNG $FF80D4 from 2363), name_moves.py.txt:670-674 and :686 (far pins at t-40 = 2560, HP pin at 2550), :646 (part 1 not a meter part); the named premises are the ones the artifacts show. One imprecision, not load-bearing: name_moves.py.txt:206-225 gives DONOVAN part "1" no "far" tag, so the donovan-self row carries NO position pins, while the claim says "the P1 and P2 position pins ... before each event" for the rig's pokes generally.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."; the gate freezes only offsets and a first-difference frame (forced_pick_fidelity.tsv:13).
Q3: VIOLATED — both legs are one function (audit_forced_pick_fidelity.sh.txt:109-112) and both carry the level pin `f'{f}:ff8116:06' for f in range(2000,$END)` (:133), which covers three of the six LATCHED samples (2000, 2200, 2362); the claim's "so they cannot mask a LATCHED byte on both legs" is an inference from the pin's address being a global, and no artifact shows that no P1-block byte is re-derived from $FF8116 in 2000-2362 — a block byte the confirm latched per character but which the game refreshes from the pinned level would agree on both legs from 2000 and be classed TRANSIENT (:194), shrinking "exactly three" with nothing to notice it.
Q4: VIOLATED — the controls cover three modes: same-leg (:310-316) fires when the comparator returns EMPTY against a frozen non-empty set; wrong-cursor (:318-322) fires when the real leg confirms another character; donovan-self (forced_pick_fidelity.tsv:17, :281-282) fires when the poke mechanism itself latches. Nothing covers the way "exactly three" is most likely wrong, an UNDERCOUNT or a misclassification: no control adds a fourth difference to prove it would be seen, and no assertion checks that the six pre-match frames (:99) are present on both legs — :258 accepts any 30 dumps, and :180 builds `pre` from whatever survived, so a sample missing on both legs changes the LATCHED/TRANSIENT verdict silently (the claim itself names this as untested).
Q5: N-A — the packet attributes no decision, approval, ruling or closure to any person; the only person-attribution in the artifacts is a dated code comment ("the maintainer's ruling 14z-121", name_moves.py.txt:392) unrelated to the claim.
VERDICT: VIOLATED

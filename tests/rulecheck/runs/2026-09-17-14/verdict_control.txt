Q1: VIOLATED — The claim states as TESTED a premise the artifacts show is untested: "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs" — but audit_forced_pick_fidelity.sh.txt:27-28 "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)" and :135 `_pick` is built from `$_id` = `row_id "$_row"` (the tenant's own id) on every leg, so SELF (huitzil: L,L,L confirms 0x10, then pokes 0x10) and donovan-self (R,R confirms 0x13, pokes 0x13; :48-50) are both same-value writes; no leg performs a changed-value poke over a matching confirm, so "a changed-value poke perturbs nothing beyond the id and its latched copies" is an untested premise the claim names as tested. (Minor: "measured with P1's X pinned identically ... so it is a first difference outside the pinned field" is not what the reducer computes — :197-198 excludes only `latched` offsets, not +0x010, and X moves freely between pins; and "two real picks ... leave route residue (measured under MEASURE)" is a result the artifacts do not carry, :90-94 only declare the ALT control.)
Q2: N-A — claim: "No behavioural conclusion about how the character plays is drawn here."; forced_pick_fidelity.tsv:15-16 freeze only a first-difference frame number (2980 / -), no conclusion about a move.
Q3: VIOLATED — The REAL-vs-SELF comparison (audit_forced_pick_fidelity.sh.txt:272, asserted at :281-282 as "the poke mechanism itself is inert") shares a premise that makes the legs AGREE whether or not the poke fires: the SELF poke writes the value the cursor already confirmed (:27-28, :135), and the only identity evidence on SELF, `id@1600` == tenant (:171-174), is satisfied by the cursor alone — a poke that never landed, or one that only perturbs on a value change, leaves REAL and SELF identical. donovan-self is the same method again (same-value write, :48-50), so the two "inert" controls are one method. (POKED-vs-REAL itself is OK: identity is asserted from the dumps on both legs plus cell@1290 on REAL, :169-179, and the wrong-cursor control :317-322 fires when the path premise fails.)
Q4: VIOLATED — The declared controls cover comparator blindness ("MUST-FIRE: perturbed-copy: same-leg", :4, :308-316) and a wrong cursor path ("MUST-FIRE: known-bad: wrong-cursor", :5, :317-322). Nothing covers the specific ways this claim could be wrong: (a) a CHANGED-value poke perturbing P1-block state — the negative controls at :48-50 and :26-28 are same-value writes, so a perturbation that only follows a value change would leave every control quiet and be counted among (or hidden inside) the frozen three; (b) the frozen three being route residue (R,R vs L,L,L) rather than confirm latch — the REAL-vs-ALT control exists only under MEASURE (:90-94, :249-251, :285-289, "MEASURE: nothing frozen, nothing compared" :291), so the gate as frozen (forced_pick_fidelity.tsv:15-16) cannot distinguish the two, while the claim charges the bytes to "the confirm".
Q5: N-A — the packet is a recommendation and attributes no decision, approval, ruling or closure to any person; the artifacts' only person-adjacent text is the history at audit_forced_pick_fidelity.sh.txt:12-16 ("#147 shipped a wrong fix"), which attributes nothing to the maintainer.
VERDICT: VIOLATED

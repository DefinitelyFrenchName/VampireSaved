Q1: VIOLATED — the claim says "Not tested: nothing further", but audit_move_parity.sh.txt:34-36 says "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters" and "P2 ... is never compared". At audit_move_parity.sh.txt:105, FIELDS samples 12 tenant fields and does not include RAM:$FF87C2, so this instrument measured neither the 0x01 and 0x00 latch readings nor whether the poke held. The window starts at the rig's first event, not over "the whole part" (audit_move_parity.sh.txt:25-29). The gate reports a per-part verdict, not a frame count (audit_move_parity.sh.txt:172). Only huitzil_1 is cited, out of ten Phobos parts that are all frozen DIVERGES (move_parity.tsv:20-29). pokes_for carries no ff87c2 poke (audit_move_parity.sh.txt:86-103). No artifact backs four premises: that a runtime poke is the same as the rebuilt flavor_default, the "every consumer ... enumerated" census, the other four tracks, and a native 0x01 read with Phobos forced by poke rather than picked (name_moves.py.txt:58-61, 672). The claim names none of them as untested.
Q2: VIOLATED — the conclusion is about how Phobos's movement and normals play: jumps, air dashes and floats (name_moves.py.txt:448-459), with the frozen divergence "huitzil_1	DIVERGES	Jump [8]+21f" (move_parity.tsv:20). Its only support is the count "6851/6851 frames identical". No artifact shows an image or recording, or one put before the maintainer.
Q3: VIOLATED — both legs share pokes that write fields being compared. The level pin "f'{f}:ff8116:06' for f in range(2000,$_fr)" goes into both legs (audit_move_parity.sh.txt:89, 98, 101), and "ff8116:b:lvl" is a compared field (audit_move_parity.sh.txt:105). The rig's own pokes, fed to both legs as _base (audit_move_parity.sh.txt:88), write P1's id with "ff8782:{tid}" (name_moves.py.txt:672). They also write P1's x with "{t - 40}:ff8410" before every far-pinned event (name_moves.py.txt:658), and every huitzil part-1 event is "far" (name_moves.py.txt:448). Both fields are compared ("ff8782:b:id", "ff8410:w:x", audit_move_parity.sh.txt:105).
Q4: VIOLATED — the gate's two controls, unpinned-level and no-translation (audit_move_parity.sh.txt:4-5), prove that the level pin and the pointer translation matter. Neither shows the instrument can see the flavor latch, which is not in FIELDS (audit_move_parity.sh.txt:105), and neither checks that a runtime poke stands in for the rebuilt default. Both controls run on the first part of the set (audit_move_parity.sh.txt:182), which is donovan_1 by default (audit_move_parity.sh.txt:79), so Donovan and not Phobos. If run on huitzil_1 alone, no-translation hits "CONTROL DEAD ... is not frozen IDENTICAL, so the control cannot discriminate" (audit_move_parity.sh.txt:195), and unpinned-level can only report FIRED, because it is judged DEAD only when the frozen verdict is IDENTICAL (audit_move_parity.sh.txt:203). An IDENTICAL huitzil_1 would also FAIL step 2 against the frozen DIVERGES row (move_parity.tsv:20; audit_move_parity.sh.txt:172-178). The claim names none of these gaps.
Q5: N-A — the claim credits no one with the flavor_default change or the M19 freeze. The maintainer rulings in name_moves.py.txt (:363 "the maintainer's ruling 14z-121", :434 "the maintainer's challenge (14z-120)") are about Donovan's and Pyron's rigs, not this action.
VERDICT: VIOLATED

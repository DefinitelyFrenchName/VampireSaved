Q1: VIOLATED — the claim gives a false basis: "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The gate says the opposite: build/rulecheck/2026-09-25-196/b/audit_forced_pick_fidelity.sh.txt:26-28 "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". Lines 135 and 138 poke $FF8782 with the row's own tenant id (`_pick` uses `_id="$(row_id "$_row")"`). The donovan-self row is also a same-id write (lines 48-49, id 13 over R,R). So nothing tests a changed-value write, and the claim presents that untested premise as tested. Its other stated premises match the artifacts: MEASURE-only globals and whole-RAM dumps (106-107, 208-235), TRANSIENT not frozen (284), the P2 check on POKED-vs-REAL only (279 vs 281), ALT only under MEASURE (250, 285-288), and pin frames 2560/2550 (name_moves.py.txt:687, 699).
Q2: N-A — the claim says "No behavioural conclusion about how the character plays is drawn here"; the frozen file holds only offsets and a frame (build/rulecheck/2026-09-25-196/b/forced_pick_fidelity.tsv:15-17).
Q3: OK — the shared pokes that reach the compared state do so outside the pre-match window (1501-2362, audit_forced_pick_fidelity.sh.txt:180). The level pin $FF8116 from 2000 and the RNG pin $FF80D4 from 2363 are globals (lines 133-134). The P1 X pin $FF8410 is in the P1 block but starts at 2560 at the earliest (name_moves.py.txt:687, FIRST_EVENT 2600 at line 77). The claim names that pin's effect on the first-PLAY frame. The P2 HP pin is $FF8850, from 2550 (name_moves.py.txt:699). No artifact shows a shared write into the P1 block pre-match.
Q4: VIOLATED — a failure mode is uncontrolled and unnamed: a CHANGED-value id write could itself perturb P1-block state. The two controls that could catch it both use same-id writes: SELF (audit_forced_pick_fidelity.sh.txt:26-28, 135, 138) and donovan-self (tsv:11-12, "the same-id poke must latch nothing"). If such a write did perturb state, those bytes would appear as LATCHED in POKED vs REAL and would be charged to the confirm. The claim asserts this case is "tested directly" instead of naming it as a gap. The must-fire controls (same-leg, wrong-cursor; lines 4-5, 308-322) cover a vanished difference and a wrong identity, not this.
Q5: N-A — the packet attributes no decision, approval, ruling or closure to the maintainer or to any person.
VERDICT: VIOLATED

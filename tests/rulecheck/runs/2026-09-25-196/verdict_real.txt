Q1: VIOLATED — The claim says the retraction-grep snapshot "was RE-RUN after the last edit of the sitting". The snapshot's own classing note says "re-run on the tree after run 194's corrections" (build/agent181/retraction_greps_14z181.txt:77). The sitting's edits did not stop at run 194: run 195 quoted the trap gate's EXPECTS still saying "the two controls failing" (tests/rulecheck/runs/2026-09-25-195/verdict_real.txt:4), and both gates' EXPECTS lines were corrected only after that ("both gates' EXPECTS lines corrected to their declared control counts (ten, three)", build/agent181/resolve195.txt:1; the diff is at build/agent181/close_carriers_14z181.diff:257-258 and 284-287). So no artifact shows that re-run, and the claim does not name it as unshown. Separately, the reading that (ii) says it applied (write the banner LAST) missed the live banner, which still reads "thirty-five rule-checker runs on my own packets (147-181)" (STATE.md.lines-72-101:3) although this packet is the seventeenth after runs up to 195. Row (13)'s heading still says "THE MAINTAINER'S EIGHT READS" although its body counts nine (STATE.md.lines-72-101:25).
Q2: VIOLATED — Player-facing findings are restated, and every sheet went out after its conclusion: "AFTER the RAM measurement" (build/agent181/captures_sent_14z181.txt:20-24) and "AFTER the measurement (the naming freeze of a91a555a)" (build/agent181/captures_sent_14z181.txt:39). The claim itself says "still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t)", yet it restates "a rig tuned at level 8 whiffs at 6" and "pyron_3's event 5 … in fact connects".
Q3: N-A — The packet's conclusion is about where findings live and which tests exist in the tracked tree. It runs no two-leg comparison of its own. The ours-vs-native results it restates belong to their gates. The one pair it does run, the homes tool against its `--blind` variant, is built to disagree ("SELFTEST FAIL: (False, False, True, True)", build/agent181/test_close_tools_mode_build-home-planted.log:6).
Q4: OK — Controls fire on the tool-checked failure modes. An untracked or build/ home is caught by `build-home-planted` (build/agent181/test_close_tools_14z181.log:11), with the live `--newest` row read at 2b (line 6). A grep that does not reach the tree is caught by `unreachable-control`, `reach-threshold` and `gone-reintroduced` (lines 8-10). A crash read as a plant is caught by the REFUSED path (build/agent181/close_tools_crash_probe_14z181.log:5-6). The claim names the rest as its reading, not a tool's: "that a live carrier's FIGURES agree with the findings, that a fragment pattern is wide enough for every carrier" and "that a named home is the RIGHT SECTION". That covers the stale banner and the case-sensitive miss of STATE row (8)'s "THE 35 NEVER-ENTERED a2 CHAINS" by the `gone` pattern 'the 35 never-entered a2 chains' (tests/rulecheck/retractions/14z-181.tsv:33; tools/retraction_grep.py:84 matches with no case folding).
Q5: OK — Each attribution has the maintainer's own words quoted: "yes, identical." for #173 (DECISIONS_HISTORY.md.lines-28-48:9); "identical and validated otherwise" for (j) (line 10); "looks identical. I'll double check the air throw on my end but everything looks good" for #169 (line 14; build/agent181/captures_sent_14z181.txt:39); and "correct" for the first read (DECISIONS_HISTORY.md.lines-28-48:6).
VERDICT: VIOLATED

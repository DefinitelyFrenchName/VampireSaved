Q1: VIOLATED — The claim says the banner discipline of (ii) was "applied at run 196" and "written for run 197 as the last packet". The banner it rewrote still gets its own count wrong: "180-197, seventeen packets on this close's own documentation" (STATE.md.lines-72-101:3). Runs 180 to 197 are eighteen packets, and the packet's own subject calls this one "the eighteenth packet, after runs 180-196". The same banner also says "every real verdict but one (run 158's, OK on Q1/Q4/Q5)". The artifacts contradict that: the gate names run 158's Q4 as the origin of a control, "tenant-planted … — rule-checker run 2026-09-25-158 Q4" (tests/audit_victim_parity.sh.lines-1-60:43). Row (4) says "the charge by field since the 158 finding" (STATE.md.lines-72-101:13). Row (11b) says the OK read was "the sixth" of runs 156-162 (STATE.md.lines-72-101:21). Separately, (g)'s test does not show "whiffs at 6". The unpinned-level mode reads the same number of contacts, with different lines: "reads 13 line(s) differently" and diff "1,13c1,13" (build/agent181/victim_parity_mode5_unpinned-level.log:36,103).
Q2: VIOLATED — The claim restates findings a player could feel, including "a rig tuned at level 8 whiffs at 6", "pyron_3's event 5 … in fact connects" and the +1 damage. Every capture went out after its conclusion: "AFTER the RAM measurement" (build/agent181/captures_sent_14z181.txt:20-24) and "AFTER the measurement (the naming freeze of a91a555a)" (build/agent181/captures_sent_14z181.txt:39). The claim itself says "still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t)".
Q3: N-A — The packet's conclusion is about where findings live in the tracked tree and which tests exist. It runs no two-leg comparison of its own. The one pair it does run, the homes tool against its --blind variant, is built to disagree: "SELFTEST FAIL: (False, False, True, True) stem=True ticket=False prose=True fail=6" (build/agent181/test_close_tools_mode_build-home-planted.log:6).
Q4: OK — Controls cover the failure modes the tools check. A build/ or untracked home is caught by build-home-planted (build/agent181/test_close_tools_14z181.log:11), and section 2b reads the live --newest row (line 6). A grep that does not reach the tree is caught by unreachable-control, reach-threshold and gone-reintroduced (lines 8-10). A crash mistaken for a plant ends in REFUSED, exit 3 (build/agent181/close_tools_crash_probe_14z181.log:5-6). The claim names the rest as its own reading, not a tool's: "that a live carrier's FIGURES agree with the findings" and "that a cited test EXERCISES its finding". That covers the banner miscounts and the (g) test mismatch found under Q1.
Q5: OK — Each attribution quotes the maintainer's own words. #173: "yes, identical." (DECISIONS_HISTORY.md.lines-28-48:9). (j): "identical and validated otherwise" (DECISIONS_HISTORY.md.lines-28-48:10). #169: "looks identical. I'll double check the air throw on my end but everything looks good" (DECISIONS_HISTORY.md.lines-28-48:14; build/agent181/captures_sent_14z181.txt:39). The first read: "correct" (DECISIONS_HISTORY.md.lines-28-48:6).
VERDICT: VIOLATED

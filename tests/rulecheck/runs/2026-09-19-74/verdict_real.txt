Q1: VIOLATED — build/rc170/freeze/rc_final/deletions.txt:3-5 reads only each directory's LAST mtime: "a directory's mtime moves when an entry is added OR removed; each is explained by an entry CREATED at that second (an addition), else flagged as a possible deletion". So if an untracked file was removed earlier from a directory that later gained an entry, the removal is reported as "an addition". That applies to tools 09:15:23Z, tests 13:20:38Z, tests/expected 13:16:39Z, docs and build (deletions.txt:7-8,24,45-49). The claim says "every directory changed since … is explained by an addition or one of five named removals". That rests on an unstated premise that no removal came before a later addition in the same directory, and the NOT-tested list does not name it.
Q2: OK — the captures were put before the maintainer before each behaviour sentence was drawn. The KO sheet was sent, then read "looks clean to me" (captures.txt:6-8). Four sheets were "sent first", then read "mechanically perfect" and "clean" (:15-17). The pinned trap was shown "with the captures before the maintainer", then read "Yes, as expected" (:24-26). For Pyron, "Capture put before the maintainer first", then ruled "Natural reading (Recommended)" (:33-37). recap/compare.tsv ties 78/78 of those snapshots to build/m3b_merged27.
Q3: OK — no artifact shows a shared element writing or selecting the compared state so that the legs are forced to agree. Under the same replay, pokes and comparison, merged-m18 and the fix still differ (recap/recap.log:7-13, AE up to 12999.3). The edits checker takes its EDIT set from tools/attribute_patch_delta.py, the same tool that produces attr2_*. That tool's split catches a planted change in a SAME, in-place, MOVED and target byte (tool_plants.txt:2-5). image_vs_patch and image_delta_classes read patch.json and the images directly, without that tool.
Q4: VIOLATED — one failure mode is neither controlled nor named. A file a gate read could have been removed after that gate started, from a directory that later received any new entry. deletions.txt carries no plant (final_inventory_plant.txt and mister_names_expected_plant.txt exist, but none for deletions) and sees only the last mtime. final_inventory.py:52 "if os.path.exists(inc): L += code(inc)" silently drops a gate-named file that no longer exists. So neither instrument would report such a removal.
Q5: OK — the maintainer's own words or selected options are quoted for every attributed ruling. "M19 supersedes it (Recommended)" (rulings.txt:10). "Natural reading (Recommended)" (rulings.txt:20). "Gauge (#157 tail),Defense rows,0x52 rule,EX route" (rulings.txt:30). "S1: scoped row (Recommended)" (rulings.txt:38). "I agree with all recommendations" (rulings.txt:42-43). "Freeze M19 now (Recommended)" (rulings.txt:61). "Freeze, ticket it (Recommended)" (rulings_emu.txt:10). "Re-author as described (Recommended)" (rulings_emu.txt:20). The capture reads are quoted in captures.txt:8,17,26, and the cadence ruling in ci_emulator.tsv.lines-30-56.txt:17-20.
VERDICT: VIOLATED

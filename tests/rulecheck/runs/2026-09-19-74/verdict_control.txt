Q1: VIOLATED — the claim says the SELF leg is "a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs", but the gate says the opposite: "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)" (audit_forced_pick_fidelity.sh.txt:26-27). In code, a non-poked leg takes the tenant's own path (line 118) and SELF writes that same tenant id with `_pick` (lines 135, 138). So the claim reports as tested something that is untested: whether the changed-value write (0x13 to 0x10/0x11) the poked leg performs is inert. The claim does not name this as untested.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — within the pre-match window (1501-2362), the only write both legs share is the level pin "{f}:ff8116:06" from 2000 (audit_forced_pick_fidelity.sh.txt:133), which is a global. The RNG pin starts at 2363 (line 134). The position pins land at t-40, 2560 at the earliest (name_moves.py.txt:687). The HP pin on $FF8850 starts at 2550 (name_moves.py.txt:699). None of these reaches the P1 block inside the window. The one shared write into the P1 block ($FF8410) affects only the first-play frame, and the claim names it.
Q4: VIOLATED — the only controls for poke inertness are the SELF legs (audit_forced_pick_fidelity.sh.txt:281, "REAL vs SELF latches nothing (the poke mechanism itself is inert)") and the donovan-self row (forced_pick_fidelity.tsv:17). Both poke an id that the real confirm already holds (lines 118, 135-138), so both only see a write that changes nothing. The claim rests on inertness of "the same write the poked leg performs", which is a changed-value write. No control exercises that write, and the claim does not name it as a gap.
Q5: N-A — the packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

Q1: VIOLATED — The claim has a long list of what it did not test, but two untested premises are missing from it. (1) Findings (i), (j), (aa) and (l) are restated as ours-against-native facts. Those comparisons rest on the per-frame level and RNG pins that both legs share. docs/NEXT_SESSION.md:22-24 lists those pins as "(a) the parity gates' per-frame level and RNG pins as their equalised INPUT (every rule-checker run of 14z-181 flagged it; accepted in writing pending the ruling)". The claim never names that premise, and its only mention of the level is (g)'s level dependence. (2) The claim says "the fast documentation gate set is run after every edit of this packet and logged". build/agent181/fast_doc_gates_14z181.log:1 records a single run ("run 2026-09-25 13:21 after the last edit before run 207"), so the artifacts do not show "every edit".
Q2: VIOLATED — The packet restates conclusions a player could feel: (g) "a rig tuned at level 8 whiffs at 6", (ee) "pyron_3's event 5 … in fact connects", the trap hit on the landing frame, and the +1 damage in (r)/(t). Every capture went out after its conclusion: "AFTER the RAM measurement that found it, not before" (build/agent181/captures_sent_14z181.txt:20, and lines 21-24 the same) and "AFTER the measurement (the naming freeze of a91a555a)" (captures_sent_14z181.txt:39). The claim itself says "still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t)".
Q3: VIOLATED — Finding (l) runs both legs through one classifier, `valid()`, and that classifier chooses which sequences get compared at all: "table c: 139 chains native, 139 ours, 81 not chains on both (walks into data), 0 differ in shape" (build/agent181/victim_parity_plain_14z181.log:81). Before it existed the same chains read "76 c chains 0x3d-0x8a differ in shape" (STATE.md.lines-72-99:18). If `valid()` wrongly called a real chain a walk into data, that chain would drop out on both legs and the check would read as agreement. The label check only covers chains a traced part enters. The legs also share the level and RNG pins ("level pin on; RNG pin on", victim_parity_plain_14z181.log:13), and the level pin is shown changing the compared lines: "reads 13 line(s) differently from the level-8 pin … the level pin is load-bearing" (log:38).
Q4: OK — Each way this claim could fail is either controlled or named. An untracked or build/ home is caught by `build-home-planted` ("CONTROL FIRED: build-home-planted", build/agent181/test_close_tools_14z181.log:14; its mode FAILs, test_close_tools_mode_build-home-planted.log:8). Missing names, gate stems, ticket rows, anchors and AMBIGUOUS names are caught by the real tool's self-test, which prints SELFTEST PASS (log:6). A grep that does not reach the tree trips `unreachable-control` and `reach-threshold`; a retracted wording that comes back trips `gone-reintroduced`; a matcher without case-folding trips `case-fold-dropped` (log:10-13). Each mode FAILs on the plant's own text, and a crash reads REFUSED, exit 3 (close_tools_crash_probe_14z181.log:5-6,10-11). The claim names the rest as its own reading: that the table is complete, that section homes carry their findings, that cited tests exercise them, that patterns are wide enough, that a single-file resolution is the right file, and the `valid()` drop.
Q5: OK — Each attribution to the maintainer quotes their own words: "yes, identical." (DECISIONS_HISTORY.md.lines-28-48:9), "identical and validated otherwise" (line 10), "correct" (line 6), the trap caveat (line 13), and "looks identical. I'll double check the air throw on my end but everything looks good" (line 14; build/agent181/captures_sent_14z181.txt:39). The model switch is quoted too: "I had to switch to Opus 5.5. Resume the work." (STATE.md.lines-72-99:3).
VERDICT: VIOLATED

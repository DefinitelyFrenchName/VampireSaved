Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the gate shows untested premises it does not name. It excludes the frames before the first event: audit_move_parity.sh.txt:25 "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor". It also excludes parts of the state: audit_move_parity.sh.txt:34 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters", and P2's state is never compared (line 35). The claim also rests on premises that no artifact tests. It ran only part 1 of Huitzil's ten parts (name_moves.py.txt:447-610). It tested a runtime poke of $FF87C2, not the rebuilt flavor_default: nothing re-measures the five rebuilt tracks. No named file shows the "every consumer ... enumerated (pristine vsavj: zero)" search.
Q2: VIOLATED — The claim concludes that Phobos's part-1 moves (movement, jumps, floats, normals) play identically to native. It offers only a count, "6851/6851 frames identical". None of the three artifacts is or cites an image or recording put before the maintainer.
Q3: VIOLATED — Both legs receive the same rig pokes: `_base` from the schedule json's 'pokes' goes into the native leg (audit_move_parity.sh.txt:96/98) and into ours (line 101). Those pokes write fields the comparison checks. The pin pokes write P1 X 40 frames before every "far" event: name_moves.py.txt:658 "f\"{t - 40}:ff8410:{x1:04x}\"", and every Huitzil part-1 event is "far" (line 448). The ID poke name_moves.py.txt:672 "f\"{f}:ff8782:{tid}\"" forces the character on both legs. The level pin audit_move_parity.sh.txt:89 "f'{f}:ff8116:06'" is applied to both legs. Line 105 compares all three: FIELDS contains "ff8410:w:x", "ff8782:b:id" and "ff8116:b:lvl". So on those frames and fields the legs agree by construction. The native "0x01" reading was also taken on a leg whose character was forced by that shared ff8782 poke, not selected natively.
Q4: VIOLATED — The gate has two controls, "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:4-5). Neither fires on this claim's own failure modes, and the claim names none of them: a poke that holds the latch versus the rebuilt default the engine writes at init, a consumer the enumeration missed, or the part-1 window standing in for parts 2-10. The claimed IDENTICAL result is also not a PASS of this instrument. The frozen expectation reads move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f", so an IDENTICAL result fails the frozen check (line 178 "expected [$want] measured [$got]"). If huitzil_1 is the control part, the gate reports "CONTROL DEAD ... is not frozen IDENTICAL" (line 195). The gate outputs a verdict per part, not a frame count like "6851/6851", and its pokes contain no $FF87C2 poke (lines 86-103).
Q5: N-A — The packet attributes no decision, approval, ruling or closure to the maintainer or to any person.
VERDICT: VIOLATED

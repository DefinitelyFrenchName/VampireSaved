Q1: VIOLATED — Claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but build/rulecheck/2026-09-17-01/a/audit_move_parity.sh.txt:34-36 states "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters ... P2 is Victor ... his state is never compared", :11-13 compares only ten tenant fields (not whole RAM; $FF87C2 is absent from FIELDS at :105), :25 "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor" (the intro is excluded), the gate carries no $FF87C2 poke anywhere in pokes_for (:86-103) so the "poked to 0x01" run is outside the artifact, and build/rulecheck/2026-09-17-01/a/move_parity.tsv:20-29 freezes huitzil_1 as "DIVERGES Jump [8]+21f" and parts 2-10 as DIVERGES — nine untested parts behind a freeze of "the five tracks".
Q2: VIOLATED — The conclusion is that Phobos's part-1 moves (incl. "Float [j.8 hold]", "Float [j.9 hold]", build/rulecheck/2026-09-17-01/a/name_moves.py.txt:456-459, and every normal and air dash) now play as native; the only evidence is the figure "6851/6851 frames" — no image or recording appears in any artifact, and the gate emits only a TSV verdict (audit_move_parity.sh.txt:133-135).
Q3: VIOLATED — audit_move_parity.sh.txt:10-11 "the same rig, the same inputs, on native vsav2 and on the merged WIDE build"; :88 `_base` (the schedule's HP-pin, stock and position pokes, name_moves.py.txt:670-681) is emitted for BOTH legs at :96 and :101, and both legs run the same .rpl and field_trace.lua (:109-121); name_moves.py.txt:17-18 "a move whose window shows no new chain is reported UNFIRED" — a rig that fails to fire a move fires it on neither leg and the legs AGREE.
Q4: VIOLATED — The two controls cover only the level pin and the placement translation (audit_move_parity.sh.txt:4-5) and run on the FIRST part of SET only (:182, donovan_1 in the default set at :79); if PARTS=huitzil_1 the no-translation control is declared dead because the frozen row is DIVERGES (:195, move_parity.tsv:20). Nothing fires on the failure mode of THIS claim: that a runtime poke of $FF87C2 on ours is not what a build-time flavor_default produces (char init reads at load, :22-24), and that the sampled fields (:105) never include the byte being changed.
Q5: N-A — the packet attributes no decision, approval, ruling or closure to the maintainer or any person; the only person-attributed statements in the artifacts (name_moves.py.txt:231 "maintainer-confirmed", :363 "the maintainer's ruling 14z-121") concern other, earlier matters.
VERDICT: VIOLATED

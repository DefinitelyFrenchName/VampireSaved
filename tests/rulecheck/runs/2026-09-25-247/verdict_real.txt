Q1: VIOLATED — The claim says test_mister_prg_window "was re-frozen from that lane's own log (mister_romset_results.tsv)", but build/rc183/freeze/mister_romset_results.tsv:3 is only "test_mister_prg_window	mister	release	FAIL	3754	exit 1: FAIL test_mister_prg_window". No named artifact shows the re-frozen pair, the lane measurement it was copied from, or a rule-checker `expectation` run for it. build/rc183/freeze/ledger_238_244.txt:3-9 covers only "the emulator expectations the battery's pass 1 found red", and the MiSTer lane is not in build/rc183/freeze/battery_p1_results.tsv. So the re-freeze existing, and matching that measurement, is an unshown premise the claim does not name.
Q2: N-A — The claim draws no conclusion about how anything plays. It records the maintainer's own playtest ("Everything felt normal. However I didn't measure anything", build/rc183/freeze/maintainer_playtest.txt:2), and it lists the zero-pay throws and #179 as untested or ticketed.
Q3: OK — Both legs' images are checked against a fresh decrypt of their own zip (build/rc183/freeze/images_vs_zip.txt:2-11). Every baseline matches its retired M19 pin (build/rc183/freeze/baseline_fingerprints.txt:2-6). No artifact shows a shared tool or input writing the compared words so that the two sides would agree.
Q4: VIOLATED — delta_attr.py's plant covers only the program opcode view ("if plant and view == \"verify_op.bin\"", build/rc183/freeze/delta_attr.py:24; build/rc183/freeze/delta_attr_plant.txt:7,9). The member-level claim (vm3j.04d + vsw.33m/37m, + vsw.41 on merged; that the gfx change is only the M20 mark) rests on the measurer's condensed return (build/rc183/freeze/deltas_measurer.txt:1-7, "p157_delta.py sha1 5ccbafc4", tool not shown). No control there would fire on an extra non-program member moving. The manifest re-pins take the M20 builds' own digests (build/rc183/freeze/pin_edits.diff:46,54,64,74,93), so such a move would be frozen in silently. The claim does not name this gap.
Q5: OK — The maintainer's own answers are quoted: "Freeze M20 now" (build/rc183/freeze/decisions_lines_30_68.txt:23), "Freeze, ticket it (Recommended)" (line 5), "Pin it to M19 (Recommended)" (line 15), and the playtest "Everything felt normal. However I didn't measure anything" (build/rc183/freeze/maintainer_playtest.txt:2).
VERDICT: VIOLATED
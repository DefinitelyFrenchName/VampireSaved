Q1: VIOLATED — The claim says every finding has "a live home in the TRACKED tree" and that "each home named exists in the tree at HEAD". Tracking is missing from its NOT-tested list, and the artifacts contradict it for (gg) and (ff). (gg)'s home is "the greps artifact's method", which is build/agent181/retraction_greps_14z181.txt:1, plus a memory. build/agent181 is untracked by the packet's own account: build/agent181/resolve185.txt:1 calls build/agent181/near_event_geometry_14z181.txt (build/agent181/gotchas_close_14z181.diff:11) "the untracked table", and (w) says "scratch, not promoted". No tracked file shown carries (gg): trap 6 at docs/NEXT_SESSION.md:75-77 is about prepare/record/resolve, not grep width. (ff)'s "sweep artifact's header rule" is also a build/agent181 file, `build/agent181/air_throw_sweeps_14z181.txt` (STATE.md.lines-72-100:21).
Q2: VIOLATED — Some findings are about how moves play, with no capture made before the conclusion. (ee): "pyron_3's event 5, named "far (whiff)" by the rig, in fact connects" (docs/project/gotchas.md.lines-1-160:91-92). (dd): both throw strengths connect. (q): Pyron's Zodiac Fire guard cancel fires. The claim itself says "(dd) and (ee) are RAM findings with no capture". None of these three is among the five captures sent (docs/NEXT_SESSION.md:27-32).
Q3: N-A — The packet concludes that each finding has a home and a test. It compares no legs of its own. The ours-vs-native comparisons it restates belong to their own gates and packets (build/agent181/trap_air_mode4_air-hit.log, build/agent181/victim_parity_mode5_unpinned-level.log).
Q4: VIOLATED — Two ways this claim could be wrong have no control and are not named. (1) A home that exists only in untracked build/ files. The only tool reads "session 14z-181: 0 addresses checked" (build/agent181/close_findings_14z181.txt:1), and run 185 found exactly this defect (tests/rulecheck/runs/2026-09-25-185/verdict_real.txt:1). (2) A retracted wording that wraps across a `#` comment line in tests/ or tools/. The matcher collapses whitespace and backticks but keeps `#` and punctuation, as the matched text shows: "d y > 0 as airborne and # called the landing-frame hit" (build/agent181/retraction_greps_14z181.txt:6). A pattern split by "# " therefore cannot match. Neither reach control is a line-wrapped carrier (build/agent181/retraction_greps_14z181.txt:50-64), yet the claim says "so a line-wrapped or code-spanned carrier is found".
Q5: N-A — No decision, approval or closure is credited to anyone. The claim says "the maintainer's read is theirs", and (t) is homed in STATE "Decisions pending" (docs/NEXT_SESSION.md:22-27).
VERDICT: VIOLATED

Q1: VIOLATED — Every box in the gate, live or static, is read from a data file on disk, not from the emulated ROM: "DATA="$MERGED/verify_data.bin"" (tools/trap_air_probe.sh:32), "vulns.append(world(box(data, t + 8 * ids[i]), X, Y, flip))" (tools/trap_air_boxes.py:116), and the pristine check reads the same file (tests/audit_trap_air_hit.sh:162,167). But the merged leg runs "$MERGED/rompath" (tools/trap_air_probe.sh:32), and the native leg runs the vsav2 ROMs while its boxes come from build/out/vsav2_data.bin. No named artifact shows that verify_data.bin or vsav2_data.bin is the data MAME actually runs. The claim's untested list names only "the engine_internals encoding and a zero vuln id being no box" and "tools/anim_nodes.py". It never names that each image is the one the leg runs, and both "our build's static hurtboxes equal PRISTINE vsavj's" and "charged to her own data" rest on it.
Q2: OK — build/agent182/rc_sheet_3508.png and rc_sheet_3511.png show both legs side by side at f3519-f3524 (the air hit at f3521 y=42 cls=0x07; the landing hit at f3522 y=40 cls 0x52/0x38). build/agent182/maintainer_read_175.txt:6-7 records the maintainer's reply to the two sheets: "Yes, identical and this confirms the behavior is the same in both games so #175 can be clsoed".
Q3: OK — Both legs share writes: the level and RNG pins through the window (tools/trap_air_probe.sh:41; not compared fields, and the claim names them as ruled, maintainer_read_175.txt:9-10 "agreed"), and the x pins on ff8810 and ff8410 over 3470-3489, before FROM=3500 (tests/audit_trap_air_hit.sh:69-70). The claim names both. The rest of the shared path (the replay, and the box resolution in trap_air_boxes.py) is named in the claim or shows no route to making differing compared state agree.
Q4: VIOLATED — The merge-corrupt control only plants into the dict parsed from verify_data.bin: "b = list(ours[first]); x, y, hw, hh = b[0]; b[0] = (x + 1, y, hw, hh)" (tests/audit_trap_air_hit.sh:176). That proves the check sees the file, not the build MAME runs. Failure mode with no control and not named by the claim: the running merged ROM's Felicia boxes differ from verify_data.bin (for example, the file is stale, or the merge changed the ROM after the file was written). Then both the live resolved boxes and the pristine check would still read pristine vsavj data and pass.
Q5: OK — The attributions are quoted in the maintainer's own words. The capture read: "Yes, identical and this confirms the behavior is the same in both games so #175 can be clsoed" (build/agent182/maintainer_read_175.txt:7). The level/RNG pin ruling: "agreed" (:10). The class-0x52 rule: "then I'm all for fixing. Once again, as long as we don't introduce noticeable lag and we don't break more things, it's a pure win/win" (:12).
VERDICT: VIOLATED

Q1: VIOLATED — The claim gets one of its stated premises wrong. It says the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The gate shows the opposite. audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Line 135 builds `_pick` from the row's own `$_id`, and line 138 gives that same poke to SELF. So the only write tested is a same-value write. The claim presents the untested premise, that a changed-value id write perturbs nothing, as tested.
Q2: N-A — The claim draws no conclusion about how the character plays: "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — POKED and REAL share pokes, but none reaches the P1 block in the pre-match window. The level pin writes $FF8116 from 2000 and the RNG pin writes $FF80D4 from 2363 (audit_forced_pick_fidelity.sh.txt:133-134), both globals. The HP pin writes $FF8850 from 2550 (name_moves.py.txt:699). The position pin writes $FF8410, which is inside the P1 block, but from t-40 = 2560 at the earliest (name_moves.py.txt:687), after the last pre-match sample at 2362. That pin's effect on the first-PLAY-difference frame is disclosed in the claim.
Q4: VIOLATED — The failure mode here is that the poked leg's changed-value write to $FF8782 itself causes the LATCHED bytes. They would then be wrongly charged to the confirm. Both controls that could catch this use only a same-value poke: REAL-vs-SELF (audit_forced_pick_fidelity.sh.txt:26-28, 135, 138, 281) and donovan-self (poke 13 over R,R, which is already 13, lines 48-51 and 87-89). Neither covers a changed-value write, and the claim does not name this as a gap. It says the opposite ("tested directly on the same write the poked leg performs"). The must-fire controls do not cover it either: same-leg (line 310) and wrong-cursor (line 318).
Q5: N-A — The packet attributes no decision, approval, ruling or closure to the maintainer or anyone else.
VERDICT: VIOLATED

Q1: OK — the claim's NOT-tested list matches the artifacts: only sites 1 and 2 appear as live-pair writers (build/agent183/p157_evidence/throw_registration_writers.txt lines 6,10,14: "0cfc14,0cfc18,0cfce2,0cfce6"), the gate still counts 0 contacts (same file lines 1-3), the two zero-pay throws are unexplained (zero_meter_throws.txt line 20), and the tracked M20 build is not in any artifact. No other untested premise the claim rests on is shown. The m19 legs "-136" and "-126" (recordings.txt lines 45,77) are the step minus 144, the bar rolling over into a stock, so "the reverse on merged-m19" holds.
Q2: OK — the maintainer played and named the probe recordings before the claim was made ("p157-probe-04 for Donovan / p157-probe-07 for Pyron / p157-probe-08 for Phobos", build/agent183/p157_evidence/maintainer_read.txt lines 3-5), then answered "Everything felt normal. However I didn't measure anything" (line 11). The claim says no human judged the feel beyond that.
Q3: OK — the stat poke was applied to both legs (ours and native), but the probe's verdicts do not depend on it: "probe stat8 table == probe unpoked table (verdict columns): 526 of 526" (build/agent183/p157_evidence/parity_moved.txt line 96). The native leg was pinned: "pinned native legs of the two runs identical: True" (xdiff_traces.txt line 2). No artifact shows a shared rig, tool or input writing the compared meter or P2 HP.
Q4: OK — controls are present. The 2P rig's shared stat could hide which fighter the damage scaler reads; the +0x3B3=8 separating poke covers that, since merged-m19 then differs from native in 49 events and the probe does not (parity_moved.txt lines 44-95). The A/B would pass falsely if both legs loaded one build; replays 110/111 differing covers that (legacy_ab_summary.txt lines 19-20). A stale native leg is covered by the pinned-leg check (xdiff_traces.txt line 2). The rest is named in the claim: the site-3 stores, the M20 build not yet built, the 1P-vs-CPU native leg, the clr.b rows, the gate's contact rule.
Q5: OK — each attribution to the maintainer quotes their own words: "Everything felt normal. However I didn't measure anything" (build/agent183/p157_evidence/maintainer_read.txt line 11), and the route they chose, "Freeze M20 now" (DECISIONS_HISTORY.md.lines-30-34 line 3; maintainer_read.txt line 12).
VERDICT: OK

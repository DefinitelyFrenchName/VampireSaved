Q1: VIOLATED — The claim says the design rests "only on platform facts measured this sitting by tools/agent/probe_agents.sh (A1-A8)". Two premises are neither measured by a probe leg nor named as untested. (1) An omitted `model` follows the orchestrator: build/agent172/s4_proposal.md:35-36 says "(A2/A4: an omitted model or effort follows the orchestrator", and P3(c) at s4_proposal.md:44-45 says "with no `model` — it would run on the orchestrator's model". But every probe definition writes a model (tools/agent/probe_agents.sh:60 "echo \"model: $2\""). A2 measures a call-side override (probe_agents.sh:120) and A4 measures an omitted effort only (probe_agents.sh:113-114). (2) "Where a worker's words land" (s4_proposal.md:19-20, the basis of P5) is sourced to "MEASURED (14z-176's transcript, read 14z-177)" (docs/project/agent_architecture_scope.md.lines-126-170:37), not to any A-leg. The claim's "each with a must-fire leg" also fails for A6, which has no "can" leg (probe_agents.sh:17-19 "a NEGATIVE finding").
Q2: N-A — The claim draws no conclusion about player-feel behaviour; it concerns agent definitions, hooks and gates.
Q3: OK — Each probe comparison uses legs that differ in the thing being compared (A4 low vs xhigh parent at probe_agents.sh:113-114; A5 vs A5c at :127-128; A8 vs A8c at :154-155). No artifact line shows a shared rig or premise writing the compared `effort`/`model` field.
Q4: VIOLATED — No leg and no control would fire if an omitted-`model` definition or a `general-purpose` call without `model` did NOT run on the orchestrator's model. P2's "never omitted" rule and P3(c)'s deny rest on that premise (s4_proposal.md:35-36, :44-45), and the claim does not name the gap. A6 also has no must-fire control showing that the scoped hook command (probe_agents.sh:47-52, :73-78) can fire and deny when wired. The claim names only "tested in one YAML form only", not a defective or unrunnable hook command.
Q5: VIOLATED — s4_proposal.md:3-6 says "as ruled (... DECISIONS_HISTORY.md 'Ruled 2026-09-23 (14z-175) — #172', item 4 of §6): ... model and effort capped (at most Opus-class, effort at most `xhigh`)". The quoted rulings (DECISIONS_HISTORY.md.lines-30-130:83-91) contain no statement by the maintainer on caps or worker specs. That file's item (4) is "S1 alone first (Recommended)", and scope §6 says item 4 was not itself ruled: "item 4 rides on item 3's checkpoints" (agent_architecture_scope.md.lines-276-340:37-38).
VERDICT: VIOLATED

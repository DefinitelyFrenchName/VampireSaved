Q1: VIOLATED — The claim says the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write)". The gate contradicts that. audit_forced_pick_fidelity.sh.txt:26-28 says "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". Lines 114, 135 and 138 build SELF's `_pick` from `row_id "$_row"`, which is the tenant's own id, the one the real pick already holds. So the claim lists as tested a premise that was never tested: whether a write that CHANGES $FF8782 perturbs the P1 block. It does not name that as untested either.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here". The frozen column is a RAM frame number (forced_pick_fidelity.tsv:15-17), not a conclusion about how anything feels to play.
Q3: OK — The only rig pokes that land inside the P1 block are the X pins "{t - 40}:ff8410" (name_moves.py.txt:687). With FIRST_EVENT 2600 (name_moves.py.txt:77) the earliest is at 2560, after the last pre-match sample, 2362 (audit_forced_pick_fidelity.sh.txt:180). The claim names their effect on the frozen first-play frame. The level pin ff8116, the RNG pin ff80d4 and the HP pin ff8850 all land outside the P1 block (audit_forced_pick_fidelity.sh.txt:133-134, name_moves.py.txt:699). No artifact shows a shared write reaching the P1 block before the match.
Q4: VIOLATED — The uncovered failure mode: the three LATCHED bytes could come from the id write CHANGING the value, rather than from the confirm. Neither control performs the changed-value write that the POKED leg makes. SELF pokes the same id (audit_forced_pick_fidelity.sh.txt:26-28, 138). donovan-self pokes 0x13 over Donovan's own R,R confirm (audit_forced_pick_fidelity.sh.txt:48-49), also a same-value write. The must-fire controls only cover an empty self-diff and a wrong-cursor identity (lines 4-5). The claim does not name this gap; it says this was "tested directly on the same write the poked leg performs".
Q5: N-A — The packet does not attribute a decision, approval, ruling or closure to anyone.
VERDICT: VIOLATED

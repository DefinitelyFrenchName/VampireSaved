Q1: VIOLATED — The claim says the SELF leg pokes "a DIFFERENT id (a changed-value write)", so the inertness of the poke is tested "on the same write the poked leg performs". The gate says otherwise: audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Lines 135 and 138 build SELF's poke from the row's own `$_id` (`f'{f}:ff8782:$_id'`), and the donovan-self row is also a same-id poke. So nothing tests whether a changed-value poke leaves state behind. The claim presents that as tested when it is an unstated untested premise.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here". The frozen first-play frame (forced_pick_fidelity.tsv:15 "2980") is only a number, not a conclusion about how the move feels.
Q3: OK — POKED and REAL share the rig, the level pin, the RNG pin, and the position and HP pins (audit_forced_pick_fidelity.sh.txt:131-139; name_moves.py.txt:687, 699). Within the pre-match window, only the level pin ($FF8116, a global, from 2000) lands, and it is outside the P1 block. The one pin inside the P1 block, $FF8410, starts at 2560 at the earliest, after the last pre-match sample at 2362, and the claim names its effect on the first-play frame. No artifact shows a shared element writing the compared pre-match P1 bytes.
Q4: VIOLATED — One way the claim could be wrong: the three LATCHED bytes come from writing a new id at 1400-1500 (the value changing under the running game), not from the confirm at ~1299. The gate has no control for this. Its only mechanism control, REAL vs SELF, is a same-id write (audit_forced_pick_fidelity.sh.txt:27 "a write at 1400-1500 to an id that already holds that value"; line 281 prints "the poke mechanism itself is inert" from that same-value write). The donovan-self row is also same-id (line 49 "poking 0x13 over Donovan's own confirm"). The claim does not name this gap as untested; it says the opposite ("tested directly on the same write the poked leg performs"). Its framing also attributes the latched bytes to the confirm ("a latched byte is charged to the confirm").
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

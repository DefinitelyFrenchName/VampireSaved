Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the artifacts show several untested premises. (1) audit_move_parity.sh.txt:34-36 says "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters — separate instruments. P2 is Victor ... his state is never compared". (2) The comparison is not over the whole part: audit_move_parity.sh.txt:25 says "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor", and line 135 passes --first-event. (3) Only part 1 was run, out of the 10 huitzil parts listed at move_parity.tsv:20-29, yet the claim rebuilds and freezes five tracks. (4) The latch byte ff87c2 appears in neither FIELDS (audit_move_parity.sh.txt:105) nor pokes_for (lines 86-103). So neither the "reads 0x01 on native" reading nor the poke on ours is part of this gate. (5) The consumer enumeration ("pristine vsavj: zero") appears in no artifact.
Q2: VIOLATED — The fix is judged by whether movement plays the same: the frozen huitzil_1 row is "DIVERGES	Jump [8]+21f" (move_parity.tsv:20), and part 1 is jumps, air dashes, floats and normals (name_moves.py.txt:448-459). The only evidence offered is a number, "6851/6851". None of the named artifacts contains or references an image or recording put before the maintainer.
Q3: VIOLATED — Both legs share the rig's poke list, and it writes fields that are then compared. name_moves.py.txt:658 `pin_pokes += [f"{t - 40}:ff8410:{x1:04x}", ...]` pokes P1 x before every "far" event; every huitzil part-1 event is "far" (line 448). ff8410 is the compared field "ff8410:w:x" (audit_move_parity.sh.txt:105), and both legs load it through `_base` (lines 88, 98, 101). The same happens for "ff8782:b:id", poked on both legs (name_moves.py.txt:672; audit_move_parity.sh.txt:92, 101), and for "ff8116:b:lvl", pinned to 06 on both legs (lines 89, 98, 101). A drift in x across events would be reset on both legs before each event and read as agreement.
Q4: VIOLATED — The gate has two controls: unpinned-level and no-translation (lines 4-5, 60-63). Neither covers the way this claim can fail, which is that the ff87c2 poke did not land or hold on our leg, or that the comparator cannot see the latch's effect. Nothing samples ff87c2, and no control runs our leg without the latch poke and requires it to diverge. Also, the gate as shown cannot pass huitzil_1. Its frozen row is DIVERGES (move_parity.tsv:20), so an IDENTICAL result fails at line 178 ("expected [$want] measured [$got]"). With huitzil_1 as the control part, the run also reports "CONTROL DEAD: no-translation — the control part $CTL is not frozen IDENTICAL" (line 195). The gate never prints a frame count like "6851/6851", so the claimed verdict did not come from this gate as written.
Q5: N-A — The packet attributes no decision, approval or ruling to the maintainer or anyone else. "his part-1 rig" refers to the character Phobos.
VERDICT: VIOLATED

Q1: VIOLATED — The claim says every finding has "a live home in a tracked file — or, for the two session events (i) and (j), a row of this group". For (j) the artifacts do not show that. build/agent183b/packet_homes.txt:19 cites STATE.md:79, which is row (2). Row (2) does not state the finding: it says only "several with no registry row", and never says that m5_stock19 and donovan6 fingerprint to the same registry row or that wide0 fingerprints to vanilla vsavj. The table itself (STATE.md:85) gives (j)'s home as "this group's row (2)'s census artifacts only", which means the untracked build/agent183b/ files. packet_verify.py:11 checks only that each line starts with its quoted text, so no tool saw this, and the claim's judgment that every home is enough is wrong for (j). Two smaller gaps: the claim reports homes_tracked as exit 0 but leaves out the REVIEW line it printed (build/agent183b/homes_tracked.txt:17, "REVIEW <- none, a census (the probe was a scratch run)."). It also states "no tracked wording having been retracted" as fact, but nothing in the artifacts tests whether the retraction list is complete, and the claim does not list that as untested.
Q2: N-A — The claim draws no conclusion about how anything plays. It concerns documentation homes, quotes and test classes only.
Q3: N-A — Nothing is compared across legs. packet_verify.py, class_letters.py and homes_tracked check the packet against the tree; they are not two methods compared against each other.
Q4: VIOLATED — Uncovered failure mode: a quoted test is not the test the table names. The claim says "each test the table names is quoted with its line in packet_homes.txt". For (b), the table names "test `tests/test_run_on_snapshot.sh` snapshots all of build/ and a sibling" (STATE.md:85), but build/agent183b/packet_homes.txt:4 quotes a different file: "(b) test: tests/run_on_snapshot.sh:112 [tracked] cow "$REPO/build/." "$C/build/"". That is the runner, not the test. packet_verify.py:10-11 only checks that each quote matches its own file:line. class_letters.py:10-15 reads only the header's letters. Neither checks that a quoted test's file is the one the row names. The claim's list of unchecked items (class assignment, completeness, whether quoted content is still true, whether each home is enough) does not cover this.
Q5: N-A — The claim attributes no decision, approval or ruling to anyone.
VERDICT: VIOLATED

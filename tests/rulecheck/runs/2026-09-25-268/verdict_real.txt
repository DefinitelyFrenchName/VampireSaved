Q1: VIOLATED — The claim says snapshots cost "under a second for each submodule, module dir and sibling". But plan_153.md:60 declares two siblings, "(../community, the bbh checkout via BBH_HOME)", and build/agent183b/snapshot_timing_submodules.txt:2-6 times only emu/fbneo, emu/jtcores, the two .git/modules dirs and "../community". The bbh checkout's snapshot cost was never measured, and neither the claim nor plan_153.md:77-90 ("Not tested") lists it. The same list also leaves out "ROMDIR is read in place and its checksums verified (docs/checksums.txt)" (plan_153.md:60-61), which no artifact exercises. The claim also calls the probe a "bare clone", while the artifact says "`git clone --shared --no-checkout`" (plan_153.md:12).
Q2: N-A — The claim is about how the test harness behaves (gate verdicts, copy timings), not about anything a player could feel.
Q3: OK — The clone runs read the clone's own copies, e.g. "run /private/tmp/.../scratchpad/clone153/build/emu_freeze_m20_p3" (build/agent183b/staleness_clone_freeze.log:2). The clone's emu/fbneo is copied from the tree, so clone_porcelain.txt:12 ("the same in the working tree's emu/fbneo") matches by construction. The claim instead rests on test_fbneo_tree_integrity passing (build/agent183b/runner_clone_supplied.log:125), not on that porcelain match. No artifact line shows a shared input that would make the tree and clone legs agree falsely.
Q4: OK — Immunity itself is named as untested ("Immunity is NOT claimed as shown: it is what S6 must prove once built"), and S6 has a positive in-place leg (plan_153.md:68-72). The fbneo check has its control passing in the clone: "test_fbneo_tree_integrity_control  PASS" (build/agent183b/runner_clone_supplied.log:126). A tracked-file change during the run is covered by "ok: no tracked file changed during the run" (build/agent183b/runner_clone_supplied.log:191). The claim names the rest: a plain (non-shared) clone, the real runner in the tree at freeze cadence, completeness beyond build/, the release cadence, a commit other than HEAD.
Q5: OK — The one ruling the plan relies on (S5's "the 2026-09-17 ruling") is quoted in the maintainer's own words: "my gut feeling is that the gain for session is marginal but the gain for freeze or release is massive" (build/agent183b/plan_153.md:8-9). The principle is also quoted verbatim at plan_153.md:4-6.
VERDICT: VIOLATED

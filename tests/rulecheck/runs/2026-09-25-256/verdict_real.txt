Q1: VIOLATED — The claim states things the artifacts do not show, and it leaves some untested premises unnamed. (1) It says "every retracted hit is classed by hand in the artifact". But build/agent183/retraction_greps_14z183.txt:56 shows a third 'Lilith j.LP/j.LK' hit, "tests/rulecheck/ledger.tsv: ...a 'lilith j.lp/j.lk' retracted pattern added...". The classing at line 73 covers only DECISIONS_HISTORY.md and docs/site/project/tickets.html, so this hit is not classed. (2) It says "30 backticked file tokens (29 distinct files)". build/agent183/homes_tracked.txt:3-32 lists 30 tokens but only 28 distinct files: tests/audit_throw_registration.sh (lines 12-13) and tests/test_rehit_ring.sh (lines 24-25) each appear twice. The count reaches 29 only with STATE_HISTORY.md, which is the prose-cited file at line 46, not a backticked token. (3) The not-tested list leaves out (x): its named test, build/agent183/packet_homes_14z183.txt:51 (tests/audit_throw_registration.sh:26, "MUST-FIRE: perturbed-copy: double-write"), plants a second meter write. It does not reproduce the dead-pair contact rule reading 0 contacts on the fix. (u) is also left out even though the claim applies the "current value, not the move" standard to (m) and (n): tests/test_rehit_ring.sh reproduces the fixed window, not the literal that the rig move broke.
Q2: N-A — The claim is about where each finding is documented and which test covers it. It draws no new conclusion about how a move plays, its timing or a hit.
Q3: N-A — No ours-vs-native, before/after or A/B comparison is drawn. packet_quotes_verified.txt re-reads the tree to check the packet's quotes; it does not compare two legs.
Q4: VIOLATED — The only control on the test quotes checks that each quote matches its file and line: build/agent183/packet_quotes_verified.txt:1 "the quoted text must be the start of that line, stripped". It does not check that a quoted test line reproduces its finding, and the claim does not name that gap. Examples: (x) cites the double-write control (tests/audit_throw_registration.sh:26), and (v) cites a FOLLOWS path list (tests/test_vanilla_frame_join.sh:17 "#   tests/expected/vanilla_normal_slots.tsv tests/lib/controls.sh ..."), neither of which reproduces the finding. Separately, nothing checks that the hand classing covers every retracted hit. The REACH line (retraction_greps_14z183.txt:63) proves only that the grep reaches files, and the unclassed ledger hit at line 56 went unnoticed.
Q5: N-A — The claim credits no decision, approval or ruling to the maintainer or anyone else. It only credits the checks to the working agent ("my full re-read of rows (1)-(14)").
VERDICT: VIOLATED

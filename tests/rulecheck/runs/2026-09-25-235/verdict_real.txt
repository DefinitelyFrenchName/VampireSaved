Q1: VIOLATED — The claim names two untested premises: the verify_data.bin cause, and that nothing about tenant behaviour is concluded. It leaves out a third that it relies on: that the character pages regenerated from the M19 builds hash to the frozen values. build/rc183/refreeze/framedata_pages.log:3,5,7 give only the M20 hashes, each "!= frozen" (979935f4…, 45a76279…, ee3b1456…). No artifact gives a hash for the regenerated M19 pages that the *.html.m19_vs_m20.diff files use as their left side.
Q2: N-A — the claim draws no conclusion a player could feel: "the tenant pages' rows did not change, so nothing about the tenants' behaviour is concluded here".
Q3: OK — reaction_classes is diffed against the frozen tsv (build/rc183/refreeze/reaction_classes.before.log:13-17). The charmap json and md pages are diffed against the committed files ("index 6b94e9d2..3a8737b5", build/rc183/refreeze/chars_pages.diff:2). No artifact shows a shared tool or input writing both legs so that a defect would make them agree.
Q4: VIOLATED — Uncontrolled and unnamed failure mode: the regenerated M19 page may not equal the frozen M19 page (for example, if the page generator changed after the freeze). In that case the new hash also moves content beyond the build token, and "moves nothing else" is false. The html diffs (build/rc183/refreeze/donovan.html.m19_vs_m20.diff:1-4, same for huitzil and pyron) compare against the regenerated M19 page, not against the frozen hash that FREEZE=1 replaces, and no artifact reconciles that page's hash with the frozen one. The gate's controls (build/rc183/refreeze/charmap.before.log:24-28, "empty-page-frozen", "changed-built-byte", "added-override") cover the row floor and byte attribution, not this.
Q5: N-A — no decision, approval or ruling is attributed to anyone.
VERDICT: VIOLATED
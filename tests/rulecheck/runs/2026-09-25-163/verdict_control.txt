Q1: VIOLATED — The claim says SELF is "a poke of a DIFFERENT id (a changed-value write)". The gate shows the opposite. audit_forced_pick_fidelity.sh.txt:26-27 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Line 135 builds the poke from `_id="$(row_id "$_row")"`, which is the tenant's own id, and line 138 applies it to the self leg. So nothing ever tests whether the value-changing write is inert (the write the POKED leg performs: 0x13 to 0x10/0x11). The claim lists that premise as tested when it is untested. The other untested premises it names do match the artifacts: globals and WHOLE are MEASURE-only (lines 106-107, 208-210). The TRANSIENT count is not frozen (line 284 writes only row, latched and fp). P2 is checked on POKED-vs-REAL only (line 272 uses only f1/f6 of the self comparison). ALT is MEASURE-only (lines 249-251, 285-289).
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here".
Q3: OK — The only per-leg pokes that write the P1 block ($FF8400-$FF87FF) inside the 1501-2362 window are none. The level pin (`ff8116`) and the RNG pin (`ff80d4`, from 2363) are globals (audit_forced_pick_fidelity.sh.txt:133-134). The rig's position pins at ff8410/ff8810 start at t-40 = 2560 (name_moves.py.txt:687, FIRST_EVENT 2600 at line 77), and the HP pin at ff8850 starts at 2550 (line 699). The id poke at ff8782 goes on one leg only (line 138). No artifact shows a shared write reaching the compared pre-match bytes.
Q4: VIOLATED — Uncontrolled failure mode: the POKED leg's value-changing id write at 1400/1450/1500 could itself perturb P1-block state, and the gate would then blame the confirm for it. Neither control changes the id's value, so neither covers this. The SELF leg writes the id the REAL leg already holds (audit_forced_pick_fidelity.sh.txt:26-27, 135, 138). donovan-self pokes 0x13 over Donovan's own 0x13 confirm ("poking 0x13 over Donovan's own confirm must latch NOTHING", line 49). The claim does not name this gap; it says the opposite: "'the poke mechanism is inert' is tested directly on the same write the poked leg performs".
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

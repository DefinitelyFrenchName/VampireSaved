Q1: VIOLATED — The claim says "The SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)", but the gate says the opposite. audit_forced_pick_fidelity.sh.txt:26-27 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Line 135 pokes `ff8782:$_id`, where `_id` is row_id(row), the tenant's own id, and REAL already holds that value. So the premise that a changed-value write to $FF8782 latches nothing is untested, and the claim presents it as tested instead of naming it. The donovan-self row is also a same-id write (lines 48-51, forced_pick_fidelity.tsv:17).
Q2: N-A — the claim says "No behavioural conclusion about how the character plays is drawn here", and it draws none.
Q3: OK — Both legs share pokes, but none of them writes to the P1 block inside the pre-match window (1501-2362). The level and RNG pins are globals (audit_forced_pick_fidelity.sh.txt:133-134). name_moves.py.txt:687 places the "far" position pins at t-40 (2560 or later), and :699 places the HP pin at ff8850 from 2550. :700 emits no id poke for a tenant with a "path", and :709-710 adds no stock poke outside METER_PARTS. The pin of P1's X during play is named in the claim.
Q4: VIOLATED — The failure mode here is that the three LATCHED bytes come from the poke itself changing the value, not from the confirm. The only control for "the poke mechanism perturbs state" is REAL vs SELF (audit_forced_pick_fidelity.sh.txt:272, 281-282). That control writes a value the id already holds (lines 26-27, 135), so it cannot fire on an effect that only a changed-value write produces. The claim does not name this gap. It says the opposite: "tested directly on the same write the poked leg performs".
Q5: N-A — the packet attributes no decision, approval, ruling or closure to the maintainer or to anyone else.
VERDICT: VIOLATED

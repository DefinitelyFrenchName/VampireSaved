Q1: VIOLATED — build/rc183/freeze/carry_and_suite.txt:1 "then freeze + verify on the M20 builds" and :12 "the merged set's 16 self-frozen tenant .sha1"; the PASS counts (:8-11, e.g. donovan-m24 PASS 69 = 53 .masked + 16 .sha1, :2) include the .sha1 legs that were frozen from the M20 builds in the same run. The only link to M19 shown is a count, "(predecessor .sha1 16)" (:2). Whether any .sha1 replay moved from M19 to M20, and why, was not tested, and the claim's NOT-tested list does not name it.
Q2: N-A — the claim concludes things about bytes, keys, members and gate verdicts, not about how a move feels. The maintainer's own play is on record anyway: build/rc183/freeze/maintainer_playtest.txt:2 "Everything felt normal. However I didn't measure anything".
Q3: VIOLATED — build/rc183/freeze/carry_and_suite.txt:1 "then freeze + verify on the M20 builds" with :12 "self-frozen tenant .sha1". The freeze step writes the .sha1 expectations from the same M20 build that verify then checks against them. So for those legs, "verify SUITE GREEN with no FAIL" (:8-11) agrees by construction: a defect in the M20 build's replay would be frozen and then pass. Only the carried .masked legs (:2-6) are an independent check.
Q4: VIOLATED — the plants in build/rc183/freeze/members_check_plant.txt:6, gfx_mark_check_plant.txt:3 and delta_attr_plant.txt:11 cover an extra moved member, a stray group-C byte and an unexplained program word. Nothing covers the case where a suite .sha1 replay's result moved from M19 to M20 and the re-freeze took it in silently (carry_and_suite.txt:2 compares .sha1 by count only). The claim does not name this failure mode either.
Q5: OK — build/rc183/freeze/decisions_lines_30_68.txt:5 "Freeze, ticket it (Recommended)", :15 "Pin it to M19 (Recommended)", :23 "The maintainer chose "Freeze M20 now""; build/rc183/freeze/maintainer_playtest.txt:2 "p157-probe-04 for Donovan / p157-probe-07 for Pyron / p157-probe-08 for Phobos".
VERDICT: VIOLATED
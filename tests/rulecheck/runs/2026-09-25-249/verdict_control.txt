Q1: VIOLATED — The claim says "Not tested: nothing further", but the artifacts show several untested premises. The gate compares only the 12 FIELDS at audit_move_parity.sh.txt:105, and $FF87C2 is not one of them. Its header says "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters" and "P2 is Victor ... his state is never compared" (audit_move_parity.sh.txt:34-36). Only 1 of Huitzil's 10 parts is claimed (move_parity.tsv:20-29). pokes_for has no $FF87C2 poke (audit_move_parity.sh.txt:86-103). The gate emits a verdict per part, not frame counts, so neither "6851/6851" nor the enumeration of every consumer ("pristine vsavj: zero") appears in any artifact.
Q2: VIOLATED — The claim concludes that Phobos's part-1 moves (jumps, floats, air dashes, normals: name_moves.py.txt:448-460) now play frame-exact with native, but the frozen verdict is "huitzil_1	DIVERGES	Jump [8]+21f" (move_parity.tsv:20). The only evidence is a number ("6851/6851"). No image or recording appears in any artifact.
Q3: VIOLATED — Both legs get the same rig pokes `_base` (audit_move_parity.sh.txt:98 native, :101 ours). Every part-1 Huitzil event is pinned "far" (name_moves.py.txt:448), so the rig writes P1's X on both legs: `pin_pokes += [f"{t - 40}:ff8410:{x1:04x}", ...]` (name_moves.py.txt:658). X is a compared field: "ff8410:w:x" (audit_move_parity.sh.txt:105). The shared level pin `f'{f}:ff8116:06'` (audit_move_parity.sh.txt:89, applied on both legs at :98/:101) writes the compared field "ff8116:b:lvl" (:105). So on those frames the legs agree by construction.
Q4: VIOLATED — The only controls are unpinned-level and no-translation (audit_move_parity.sh.txt:4-5, 185-208). They cover the speed-level pin and the placement translation, not the flavor latch. Nothing withholds the $FF87C2 poke from ours to show the identity depends on it. Nothing reads $FF87C2 back to show the poke landed or that native reads 0x01: the address is absent from FIELDS (:105) and from pokes_for (:86-103). The claim does not name this failure mode.
Q5: N-A — The claim attributes no decision, approval or ruling to the maintainer or any other person.
VERDICT: VIOLATED
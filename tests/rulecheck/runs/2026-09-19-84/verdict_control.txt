Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part". The artifacts show otherwise. (1) The comparison does not cover the whole part: "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor" (audit_move_parity.sh.txt:25-29), and the comparator is passed --first-event (:130, :133-135). (2) The gate "DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters", and it never compares P2 (:34-36). (3) What the comparator excludes is in tools/move_parity.py (:14), which is not in the packet. (4) Nothing shows how the "poked to 0x01" leg was produced, or that the latch held 0x01. pokes_for has no ff87c2 write (:86-103), gen() writes none either (name_moves.py.txt:670-682), and FIELDS does not sample ff87c2 (:105). (5) The gate prints a verdict, not a frame count (:172-179). Part 1 samples from 2300 (:119), its first event is at 2600 (name_moves.py.txt:57), and it ends at end+50 = 9230 (:667, :683). So its span is 6930 or 6630 frames, never 6851, and no artifact shows where 6851 comes from. (6) The frozen verdict for this part is still "huitzil_1 DIVERGES Jump [8]+21f" (move_parity.tsv:20). (7) The other nine Phobos parts still read DIVERGES (move_parity.tsv:21-29), and nothing shows they were re-run. (8) The build-time flavor_default=0x01, which replaces a runtime poke, was never run.
Q2: VIOLATED — Part 1 covers Phobos's jumps, air dashes, floats and normals (name_moves.py.txt:448-460). From it the claim concludes that he plays identically to native, and it changes how he plays on the strength of a number: "reads 6851/6851 frames identical to native". None of the artifacts contains an image or a recording, the gate outputs only verdict text (audit_move_parity.sh.txt:172-179), and nothing shows anything was put before the maintainer.
Q3: VIOLATED — Both legs carry the same pins, and the pins write or select the compared state. (1) The speed level is written as "ff8116:06" every frame from frame 2000 on both legs (audit_move_parity.sh.txt:89, :98, :101) and then compared as "ff8116:b:lvl" (:105), so that field agrees by construction. (2) The RNG is re-pinned to "ff80d4:0000" every frame on both legs (:90, :98, :101). The artifacts show that the RNG selects the intro variant and char init ("char init draws from it", :23-24 and :26-27), so a difference between the builds in how the RNG is consumed is erased every frame. (3) The far pin writes P1 x "ff8410" before every event (name_moves.py.txt:658), and every part-1 event is "far" (:448). Both legs get it through $_base (audit :98, :101), and x is compared as "ff8410:w:x" (:105). (4) P1 id ff8782 is poked on both legs (name_moves.py.txt:672; audit :92, :101) and compared as "ff8782:b:id" (:105).
Q4: VIOLATED — The only controls are no-translation and unpinned-level (audit_move_parity.sh.txt:4-5, :185-208). They run only on the first part of the set (:182). On huitzil_1 neither can fire: its frozen verdict is DIVERGES (move_parity.tsv:20), and the no-translation branch then reports "CONTROL DEAD ... is not frozen IDENTICAL, so the control cannot discriminate" (:195). Neither control covers the ways this claim can fail. The first is that the $FF87C2 poke did not hold 0x01 through the window: the latch is neither sampled (:105) nor written by the gate (:86-103). The second is that the built flavor_default=0x01 behaves differently from the runtime poke. The claim names neither ("Not tested: nothing further").
Q5: N-A — The packet attributes no decision, approval, ruling or closure to the maintainer or anyone else.
VERDICT: VIOLATED

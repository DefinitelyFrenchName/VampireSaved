Q1: VIOLATED — The NOT-tested list leaves out two premises the claim rests on. First, the model and effort for the whole sitting: build/agent172/pkt179/session_model_reads.txt line 2 reads only "model claude-fable-5-1: 83 assistant records", a snapshot taken partway through, yet the claim says "every assistant record on claude-fable-5-1 at effort high". Second, the hook-refusal record: build/agent172/pkt179/s5_gate_opener_reads.txt line 1 is "read 2026-09-24 13:28 at the opener's bookkeeping (before any worker)", and no close-time re-read appears among the claim's conditions. The claim also rests on "the gate's ruled rule is key resolution, not record coverage", which no named artifact shows. A smaller point: STATE.md.lines-71-80 line 8 gives "383 `## Session` headings ... over 232 distinct keys", but build/agent172/pkt179/ledger_coverage_14z179.txt line 51 gives "384 ; distinct keys: 233".
Q2: N-A — The claim draws no conclusion about how the game plays. It concerns agent tooling, ledger checks and closing a ticket.
Q3: N-A — The claim compares no two legs. It rests on single readings (session_model_reads.txt, s5_gate_opener_reads.txt, ledger_coverage_14z179.txt) and on one gate run (open_lists_gate_14z179.txt lines 2-19).
Q4: VIOLATED — The new control does cover the ledger-form fix. open_lists_gate_14z179.txt line 16 reads "CONTROL FIRED: unresolved-ledger-key-bold — ledger key 14z-998", which only fires because LEDGER_KEY_RE now matches `\*\*` (tools/check_state_lists.py.lines-60-72 line 11). But no control covers the close recommendation. The ruled gate says "Its close reads the hook refusals, the sweep and the procedure check, and verifies every worker's model" (DECISIONS_HISTORY.md.lines-30-51 lines 17-19). The claim's condition is only "the tier, the sweep, the procedure check over both extracts and transcript_gaps.py --cap". A hook refusal after the 13:28 opener read, or an orchestrator record off Fable/high after the 83-record read, would pass all four checks: --cap reads workers only ("subagents: 0 fallbacks: 0", s5_gate_opener_reads.txt line 5). The claim names neither gap. Separately, open_lists_gate_14z179.txt lines 25-27 are labelled "# CONTROL=unresolved-ledger-key (the old mode)" but print "CONTROL FIRED: unresolved-ledger-key-bold — ledger key 14z-998". So that single-mode run did not exercise the old mode. Only the full run, line 15, shows the old control still fires.
Q5: VIOLATED — The claim's "the gate's ruled rule is key resolution, not record coverage" has no quoted ruling behind it. The only sources are the gate's own output, "every ledger key resolves" (open_lists_gate_14z179.txt line 3), and a code comment (tools/check_state_lists.py.lines-60-72 lines 8-10). The other attributions are quoted: "Plain session (Recommended)" and "That gate (Recommended)" (DECISIONS_HISTORY.md.lines-30-51 lines 14-18), and "Let's carry them." (build/agent172/extract_55857809_tail.txt line 294). "The maintainer started it with an explicit model" (STATE.md.lines-71-80 line 5) is inferred from the settings reading "model = opus" and is not the maintainer's statement.
VERDICT: VIOLATED
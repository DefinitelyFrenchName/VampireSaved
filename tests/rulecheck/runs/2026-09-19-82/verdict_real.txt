Q1: VIOLATED — The claim says the harness was "re-pointed to M19 in the same four files the M18 re-point moved". Only the harness commit's own message says so (build/rc170/bbh_repoint.diff:10-11, "The same four files the M18 re-point (10a82d2) moved, moved again"). No artifact lists what 10a82d2 changed, and none greps the harness for the M18 build names (m3b_merged26/don_m22/hui56/pyron41/m5_stock17). The NOT-tested list covers only the grep "in the gate and tool trees" and "every other freeze's pins", so it never says the harness census is untested. Yet the rule it cites as its home says "Grep for the OLD FINGERPRINTS as well as the old build names, and re-point the harness's defaults" (docs/project/gotchas.md.lines-5344-5358:13-14).
Q2: N-A — The claim is about a fingerprint pin and the harness's default build names. It draws no conclusion about how a move plays, feels or is timed.
Q3: OK — The PASS and FAIL legs run the same build and differ only in the value they expect: "ok: c54f1fb8…" (build/rc170/phasec_after_refreeze.log:4) against "FAIL: fingerprint c54f1fb8…, expected e86e1d04…" (build/rc170/phasec_old_expect.log:4). The two sides of the attribution are separate builds: m5_stock17 "program e86e1d04" (build/rc170/freeze/rc_final/baselines.txt:6) and "STOCK build/m5_stock18 vsavj c54f1fb8" (build/rc170/bbh_fail_tier.log:36). The risk of SAME absorbing a change is checked pair by pair (build/rc170/freeze/rc_final/same_class_check.txt:5). No artifact shows a shared rig or premise that writes the compared state.
Q4: VIOLATED — The re-freeze stands on one verdict: "RESULT: every edit is a designed edit … UNEXPLAINED 0" (build/rc170/freeze/rc_final/edits_check_m5_stock18.txt:14-15). No plant shows that check reporting an undesigned edit as UNEXPLAINED. The only plants are for tools/attribute_patch_delta.py's classes (build/rc170/freeze/rc_final/tool_plants.txt:2-6, same_class_check.txt:7-10). The image-side check cannot catch it either, because it counts any changed byte "inside an M19 op" as explained (build/rc170/freeze/rc_final/image_delta_classes.txt:26), and its "0 NEITHER" has no plant of its own. So an undesigned byte inside an edited op's span, such as op[58]'s "160 bytes at +0x1f8e..+0xa3c9" (build/rc170/freeze/rc_final/attr2_m5_stock18.txt:12) or the three BODY thunks' "designed bytes", would pass every check. The claim does not name this failure mode.
Q5: OK — The claim gives no decision to any person ("The move is the freeze's own"). The freeze decision behind the moved twin is quoted in the maintainer's own words: "the maintainer's "Freeze M19 now (Recommended)"" (build/rc170/findings_table_14z170.tsv:32).
VERDICT: VIOLATED

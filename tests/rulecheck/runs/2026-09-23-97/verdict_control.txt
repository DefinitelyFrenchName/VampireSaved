Q1: VIOLATED — The claim says the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write)", so "the poke mechanism is inert" is tested "on the same write the poked leg performs". The gate shows the opposite. audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke … (a write at 1400-1500 to an id that already holds that value)". Lines 135-138 poke `_id = row_id "$_row"`, the tenant's own id, on every leg except real/alt. Lines 174/278 then require $FF8782 to equal that same id at 1600 on SELF. donovan-self is also a same-value write (0x13 over R,R = 0x13; lines 48-51, 87, 89). So the untested premise is that a CHANGED-value write to $FF8782 (0x13 to 0x10/0x11, which is the POKED leg's write) latches nothing on its own. The claim presents that premise as tested, when it is neither tested nor named. Secondary gap: the shared pokes come from the committed `tests/replays/naming/$_rig.json`/`.rpl` (line 115, 126, 132), not from name_moves.py. That those files match the generator is assumed, and neither file is in the packet.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here". The frozen first-play frame is only recorded (forced_pick_fidelity.tsv:15-16) and no conclusion is drawn from it.
Q3: OK — The POKED and REAL legs share `_base;_lvl;_rng` (audit_forced_pick_fidelity.sh.txt:131-138). $FF8116/$FF80D4 are globals, which are never asserted (line 189, reported only under MEASURE at 208-210). The rig's pins land at or after 2550/2560 (name_moves.py.txt:687 `{t - 40}:ff8410`, 699 `range(FIRST_EVENT - 50, …)`), outside the pre-match window `1501 <= f <= 2362` (line 180) that LATCHED is judged on. The only shared write that reaches compared state is the P1 X pin at $FF8410, which affects the first-play frame, and the claim names that one.
Q4: VIOLATED — The failure mode is that the LATCHED set, or part of it, is produced by the changed-value id poke itself (a game reaction to $FF8782 changing under a confirmed cell) rather than by the confirm. That failure is uncontrolled: SELF (lines 26-28, 135-138) and donovan-self (lines 48-51) both write a value the id already holds, so neither exercises the POKED leg's write. The same-leg control (lines 308-316) only shows that a vanished difference would be noticed, and wrong-cursor (lines 317-322) only checks identity. The claim does not name this gap; it asserts the gap is covered ("tested directly on the same write the poked leg performs").
Q5: N-A — The packet attributes no decision, approval, ruling or closure to anyone.
VERDICT: VIOLATED

Q1: VIOLATED — The claim gives "(14z-174: 97 detached, 0 tracked …)" and the doc says "14z-174 made **zero** tracked launches" (docs/project/agent_architecture_scope.md:69) as measured facts. But the tracked counter only counts `run_in_background` (tools/agent/transcript_gaps.py:70). Meanwhile a 14z-174 gap was ended by "<task-notification>\n<task-id>bwwv4higk" (build/agent172/census_14z174_detail.txt:11), and the Chrome's "Bash call hit the 120 s timeout and moved to the background" (build/agent172/issue_172.txt:50). So the premise that the counter sees every harness-tracked task is untested and not stated. The same goes for the documentation-only row "an event when a background Bash task finishes | **documented as ABSENT**" (scope.md:96), on which the C0.2 design rests: it is missing from the claim's list of things "documented … never exercised".
Q2: N-A — "No behaviour a player could feel is concluded"; the packet is about agent tooling. None of it is gameplay.
Q3: OK — No artifact shows a shared tool or premise that writes or selects the compared state so as to make two legs agree. The census contrasts sessions and does not conclude that they agree (build/agent172/census.txt:3-13), and the probes P1-P3b are single-leg measurements (build/agent172/probe_run.txt:2-5).
Q4: VIOLATED — The census's DETACHED classifier is `&\s*$|&\s*\n|&\s*echo` (tools/agent/transcript_gaps.py:41). It also matches the second `&` of an ordinary `cmd && echo …` or a `&&` line continuation, so the "97 detached" and every other detached count can be inflated by non-detaching commands. The selftest has only one positive case, "nohup x.sh > l &" (transcript_gaps.py:115), and no must-stay-quiet `&&` case. The claim names only the status classifier as under-checked, so this failure mode is neither controlled nor named. Also, P2 and P3a still pass on a bare `grep … "$T"` over the transcript (tools/agent/probe_hooks.sh:53,64), which is the embedded-system-prompt contamination the doc itself found (scope.md:91). No run without a hook shows those strings absent.
Q5: OK — The one maintainer attribution is the ask. It is quoted verbatim in build/agent172/issue_172.txt:5 and :11-14 under "The proposed shape (maintainer's words)", and it matches scope.md:14-31. No ruling is attributed to anyone.
VERDICT: VIOLATED

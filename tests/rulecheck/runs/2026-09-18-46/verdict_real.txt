Q1: VIOLATED — the claim says all eight gates were reproduced and every CONTROL mode exited 1, citing "(the logs)". build/p136_14z168/ has no log at all for tests/test_df_field_readers.sh. It has CONTROL-mode logs only for df_modes, df_meter and the attribution (df_modes_ctl.log, df_meter_ctl.log, attr_ctl.log), so no log shows vs2-pattern, native-shock, variant-lost, overrun-planted or planted exiting 1. The stated basis is also wrong for two gates. The claim says "the RNG pinned from 2363" and "#136 parity rigs", but tests/audit_df_meter.sh:56 has no ff80d4 pin and builds its own rig (lines 73-78), and tests/audit_df_modes.sh:80 uses tests/replays/df/97_df_mech.rpl. The claim does not name these gaps as untested.
Q2: VIOLATED — the claim lists column_shock among "the defect ... rows". That is a conclusion about hit timing a player can feel: "SHOCKS ITS VICTIM FOR 12 FRAMES ON OUR BUILD, 24 NATIVELY" and "the second hit lands 4 frames later on ours" (tests/audit_column_shock.sh:2, 15-16). It comes from a write tap alone, and line 17 says "Nothing on record rules on it". No artifact shows a capture put before the maintainer. The guard_reentry block-animation call (tests/audit_guard_reentry.sh:2) has the same gap.
Q3: VIOLATED — the gate compares the two legs only on their `set` rows (tests/audit_entrance_draw.sh:84). The shared reducer at tests/audit_entrance_draw.sh:74 puts every +0x0A value seen 2300..2544 into those sets. tests/expected/entrance_draw.tsv:6 reads "draw	native	1234	0,6	x2363=552", so 0 appears even when Phobos is held at 552 and did not drive in. Both sets therefore contain 0 whether or not either leg drew the car arrival, which is the variant in question. Only the unchecked x2363 column tells the cases apart.
Q4: VIOLATED — the field-reader census has no control for false rows or missed readers, though its header promises "none is forgotten and none appears unseen" (tests/test_df_field_readers.sh:10). The regex at tools/audit_df_field_readers.py:50 accepts any address register and any kind of access. The frozen "readers" therefore include writes such as "move.b #$1, $1c3(a6)" (tests/expected/df_field_readers.tsv:12), and three a4-based "movep.w d0, $1c8(a4)" rows (lines 45, 48, 50) that nothing shows to be fighter-block reads. The `planted` control fires only when a row the census already found is lost. The x028122 check fires only when that one known reader is missing. Neither fires on a false row or on a reader the method cannot see, and the claim names neither gap.
Q5: VIOLATED — the claim rests DEFENSE-ROW on "the ruled defense rows". The artifacts cite rulings without quoting the maintainer: "the ruled defense row (2026-08-14)" (tools/move_parity_attribution.py:36), "the maintainer-accepted 14z-85g(2) deviation" (:30), and "ruled 2026-09-02 #114" (tests/audit_guard_reentry.sh:15, tests/audit_pass_overrun.sh:14). DECISIONS_HISTORY.md:30-57 quotes only the 2026-09-18 rulings.
VERDICT: VIOLATED

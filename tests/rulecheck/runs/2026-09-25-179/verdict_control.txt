Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the artifacts show gaps it does not name. First, the gate lists what it does not check: build/rulecheck/2026-09-25-179/b/audit_move_parity.sh.txt:34-36 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters" and "P2 is Victor ... his state is never compared". Second, the comparison does not cover the whole part: :25 "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor" (applied at :130/:135 with --first-event). Third, $FF87C2 is not among the sampled FIELDS (:105), and pokes_for (:86-103) contains no ff87c2 poke, so "ours poked to 0x01" is not what this gate runs. Fourth, the gate's frozen row is build/rulecheck/2026-09-25-179/b/move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f", and no artifact holds 6851/6851. Fifth, no artifact shows the consumer enumeration ("pristine vsavj: zero"). Sixth, only part 1 is claimed, yet the freeze covers five tracks.
Q2: VIOLATED — The claim concludes Phobos's part 1 plays identically to native. That part covers jumps, air dashes and floats (build/rulecheck/2026-09-25-179/b/name_moves.py.txt:448-460), and the frozen divergence it would erase is a jump ("huitzil_1	DIVERGES	Jump [8]+21f", move_parity.tsv:20). The claim rests on a number alone ("6851/6851 frames identical"); no artifact shows an image or recording put before the maintainer.
Q3: VIOLATED — Both legs take the same rig pokes: _base comes from the schedule JSON pokes (audit_move_parity.sh.txt:88) and is included on the native leg (:98) and on ours (:101). Every Huitzil part-1 event is pinned "far" (name_moves.py.txt:448), so gen writes P1's X on both legs 40 frames before every event: name_moves.py.txt:658 "pin_pokes += [f\"{t - 40}:ff8410:{x1:04x}\", ...]". ff8410 is a compared field ("ff8410:w:x", audit_move_parity.sh.txt:105), so X drift between the legs is reset to agreement before each event. The compared "ff8116:b:lvl" (:105) is also forced to 06 every frame on both legs (:89). The compared "ff8782:b:id" is poked on both legs (:92, :101; name_moves.py.txt:672).
Q4: VIOLATED — The only controls are "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:4-5, :185-208). Neither can detect that the flavor byte was never poked or did not reach the compared state; no control runs ours with the ff87c2 poke withheld and requires it to diverge. No control covers the latch's effect on state outside FIELDS (hitboxes, damage, projectiles, P2; :34-36), or on the frames before the first event (:25). The shared X pin that can hide positional divergence (Q3) is not controlled either. The claim names none of these failure modes as untested.
Q5: N-A — the packet attributes no decision, approval, ruling or closure to the maintainer or any other person; the flavor_default change is the working agent's own proposal.
VERDICT: VIOLATED

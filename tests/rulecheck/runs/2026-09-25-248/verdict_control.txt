Q1: VIOLATED — The claim says the SELF leg is "a poke of a DIFFERENT id (a changed-value write)". The gate says the opposite at audit_forced_pick_fidelity.sh.txt:26-28: "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". Line 135 builds the poke from the row's own id (`_pick="...f'{f}:ff8782:$_id'..."`), and line 138 gives that poke to self. So SELF writes a value the byte already holds. Nothing tests whether the changed-value write the POKED leg makes (0x13 to 0x10/0x11) is inert, and the claim does not name this as untested; it presents it as "tested directly on the same write the poked leg performs".
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here", and it draws none.
Q3: OK — The only shared write that lands in the P1 block is the X pin `f"{t - 40}:ff8410:{x1:04x}"` (name_moves.py.txt:687), with the first event at 2600 (name_moves.py.txt:77). That is from 2560, after the 1501-2362 LATCHED window (audit_forced_pick_fidelity.sh.txt:180), and the claim names it for the PLAY frame. The level pin (ff8116, line 133) and the RNG pin (ff80d4, line 134) are globals. The HP pin (ff8850, name_moves.py.txt:699) is in P2's block from 2550. No shared write reaches the P1 block before the match.
Q4: VIOLATED — No control fires if the changed-value id poke itself changes P1-block state, so a byte it changed would be counted as a confirm latch. The REAL-vs-SELF check (audit_forced_pick_fidelity.sh.txt:281-282) and the donovan-self row (lines 48-51, 87-89: R,R plus a poke of 0x13 over Donovan, who is already 0x13) both use a same-value write. The same-leg control (lines 308-316) only shows an empty diff is noticed, and the wrong-cursor control (lines 317-322) only checks identity. The claim does not name this gap; it says the gap is tested.
Q5: N-A — The packet credits no decision, approval, ruling or closure to anyone; it is a recommendation ("report #151 step 1 as done").
VERDICT: VIOLATED
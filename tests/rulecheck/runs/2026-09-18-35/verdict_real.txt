Q1: VIOLATED — tests/audit_throw_registration.sh:116 (`st[f] = v - prev` is re-assigned per write, so a "contact" meter step is the LAST write's delta on that frame, not the frame's change: the premise that no other write lands on P1/P2 meter at 3062/3061 is untested on the legacy legs, where no adder breakpoint ran, and unnamed) and :133-137 (the ids row keeps neither PC nor mask, and under any mask but ff00 reads the LOW byte, +0x383 — so "the last write to each id byte before frame 2300, the select confirm's" is shown by no artifact; it is only corroborated by build/meter_probe_14z166/wheel_resolve.txt:2-3). The claim also states the earlier attempt's VOID firings from a log it says is overwritten — not shown.
Q2: VIOLATED — the claim concludes how a throw pays meter ("pays P1 +9 and P2 +8" / "the host engine pays a legacy throw as vs2 does"), written as fact in docs/game/engine_internals.md.lines-3040-3130:68-74 and STATE.md.lines-44-60:11; no capture of Demitri's throw on either game is among the artifacts — the only captures named are the tenant sheets that preceded this part (STATE.md.lines-44-60:9 "captures/meter_sheet_pyron3.png"); a number alone is not a capture, the same Q2 ruling STATE records for run -32 on this family.
Q3: OK — the legs share the replay, tests/lua/read_tap.lua, the reducer and the pokes, but the pokes name only $FF8116/$FF80D4 (tests/audit_throw_registration.sh:157), never the compared meter words $FF850A/$FF890A nor either pair; the picks are real and tapped per leg (:161-162, :166-167) and agree with the ROM-decoded wheels (wheel_resolve.txt:2-3; both wheels' table_b[5][0]=1 and table_b[1][0]=3).
Q4: OK — the wrong-character failure is controlled by the tapped ids row (tests/expected/throw_registration.tsv:82,85) against the independent wheel decode; the legacy part's lack of a perturbation control is named by the claim ("the legacy rows reading `as frozen` under both perturbations", confirmed at throw_reg_ctl_swapped3.log:67-68 and throw_reg_ctl_planted3.log:154-155). Noted, not a violation: the "VOID rule" checks only an END line (:107, :136) that tests/lua/read_tap.lua:165-166 writes at FRAMES regardless of hits — it catches an unfinished run, not a silent tap; the frozen contact and ids rows are what show these taps were live.
Q5: OK — the maintainer's own words are quoted in tests/audit_throw_registration.sh:25-26, build/meter_probe_14z166/legacy_throw.sh:2 and STATE.md.lines-44-60:11 (the carriers differ by one letter, "widly" vs "wildly" — one is not verbatim).
VERDICT: VIOLATED

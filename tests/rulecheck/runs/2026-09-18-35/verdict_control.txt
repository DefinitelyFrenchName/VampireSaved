Q1: VIOLATED — the claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but build/rulecheck/2026-09-18-35/b/audit_move_parity.sh.txt:34-36 states "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters — separate instruments. P2 is Victor ... his state is never compared", line 105 compares only twelve fields (not whole RAM), line 25 says "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor" (not the whole part), and the only Phobos part cited is part 1 while build/rulecheck/2026-09-18-35/b/move_parity.tsv:21-29 freeze huitzil_2..huitzil_10 as DIVERGES with no artifact showing them re-run under 0x01; no artifact contains the ff87c2 poke, the consumer enumeration, or the "five tracks" rebuild — none of these untested premises is named.
Q2: VIOLATED — the conclusion "identical to native" is about Phobos's jump/float/air-dash family (build/rulecheck/2026-09-18-35/b/name_moves.py.txt:448-459, "Jump [8]", "Float [j.8 hold]", "Air Dash [j.66] early"; move_parity.tsv:20 "huitzil_1 DIVERGES Jump [8]+21f"), a behaviour a player feels, and the only evidence in the packet is the number 6851/6851 from a field trace (audit_move_parity.sh.txt:105, 118 "FIELD_OUT"); no image or recording put before the maintainer appears in any artifact.
Q3: VIOLATED — both legs run the same generated rig and the same poke set: audit_move_parity.sh.txt:88 `_base="$(python3 -c "...['pokes']...")"` is applied to native (line 98) and ours (line 101), and those pokes WRITE compared fields — name_moves.py.txt:658 `pin_pokes += [f"{t - 40}:ff8410:{x1:04x}", ...]` writes x, which line 105 compares as "ff8410:w:x" (every huitzil part-1 event is "far", name_moves.py.txt:448); audit_move_parity.sh.txt:89 pins "ff8116:06" every frame on both legs and line 105 compares "ff8116:b:lvl"; line 92/name_moves.py.txt:672 write "ff8782" and line 105 compares "ff8782:b:id". A defect in the shared pin makes the legs agree on the state the gate reads.
Q4: VIOLATED — the gate's two controls (audit_move_parity.sh.txt:4-5, "unpinned-level", "no-translation") cover the speed-level pin and the placement translation, and both run only on the first part of SET (line 182 `CTL="$(echo $SET | awk '{print $1}')"`, default set "donovan_1 pyron_2 huitzil_1" at line 79 — donovan_1, not huitzil_1). No control covers the way this claim fails: the evidence is a RAM poke of $FF87C2 to 0x01 (line 86-103 `pokes_for` contains no ff87c2 poke, so it lives outside the instrument) while the decision is a manifest byte `flavor_default` in a rebuilt ROM — nothing shows the built byte reaches the latch the poke wrote, or that the rebuilt M19 rig reads IDENTICAL without the poke; the claim names this gap nowhere ("Not tested: nothing further").
Q5: N-A — the packet attributes no decision, approval, ruling or closure to the maintainer or any person; "flavor_default becomes 0x01 ... frozen as M19" is the working agent's own statement.
VERDICT: VIOLATED

Q1: VIOLATED — the claim's "the shipped file runs from file:// in a real browser engine" is not what any gate does: `tests/test_applier_page_browser.sh:56` regenerates the page into `"$W/apply_release.html"` and drives that copy (`:77`), and neither gate ever opens `release/<platform>/apply_release.html` — so "the page under test is the shipped bytes verbatim" (`tests/test_applier_page_browser.sh:24`, repeated at `docs/project/applier_app_scope.md:9`) rests on an unnamed premise (shipped file == fresh generation) that nothing checks; `tests/test_applier_page.sh:129-134` only compares two fresh generations to each other. Second unnamed premise: the whole fidelity leg runs `tools/applier/apply_node.mjs` (`tests/test_applier_page.sh:88,184`), a driver that is not an artifact and that the verbatim-inlining check excludes (`tests/test_applier_page.sh:163` checks only `vcdiff.mjs, zip.mjs, applier.mjs`), so "the page's applier" is measured through wiring never compared to `page_shell.html:301-336`.
Q2: N-A — no conclusion about in-game behaviour is drawn; the artifacts concern file production, refusals and page capability only, and the one human-facing gap is named by the claim itself ("no human has ever used the page (it has only been driven by a script)").
Q3: OK — the two fidelity legs are independent implementations, not one rig: `tools/apply_release.py:105` carries its own pure-Python VCDIFF decoder and `zipfile` writer while the JS leg uses the inlined modules (`tests/test_applier_page.sh:181-186`), and the `flipped-member` control (`tests/test_applier_page.sh:439-453`) shows the comparison registers a single post-verification byte change. The shared `$REL/manifest.json` gates each leg separately (`tools/apply_release.py:290`, `tools/applier/applier.mjs:180`), so a defect in it surfaces as a refusal in both rather than as agreement.
Q4: VIOLATED — "it contains no network primitive of any kind" is enforced by an enumeration of seven names plus a quoted `src=`/`href=` regex (`tools/gen_applier_page.py:71-82`, re-asserted at `tests/test_applier_page.sh:141-148`), while the only control fires on one member of that list (`page-with-fetch`, `tests/test_applier_page.sh:35,456-471`) — nothing fires for `RTCPeerConnection`, `WebTransport`, `new Worker`, `setAttribute("src", …)` or a URL assigned by property, and the page itself proves that blind spot by carrying `link.href = URL.createObjectURL(...)` (`tools/applier/page_shell.html:332`) which the checker cannot see. The same enumeration flaw covers "refuses every input the Python tool refuses": that leg is a source-text presence test (`tests/test_applier_page.sh:269` `missing = [f for f in frags if f not in jsrc]`) with only four inputs actually exercised (`:329-385`), so a refusal string present but unreachable passes. Neither failure mode is named in the claim's NOT TESTED list.
Q5: VIOLATED — no person's own statement about closing #144 (or about the remaining M19 blocker list) appears in any artifact; the only maintainer words present are the applier ask (`docs/project/applier_app_scope.md:27-31`) and the three §6 rulings on the page's form (`:156-172`, e.g. "preferably a local file in the asset"), and the document explicitly places #144 outside this work: "Building or shipping the emulators. That is #144/#145." (`docs/project/applier_app_scope.md:148`).
VERDICT: VIOLATED

THE PACKET

Decision kind: recommendation
Subject: the 14z-174 close packet: the applier page's fidelity and refusal claims, and the recommendation that M19 is now releasable
Claim (the working agent's sentence): CLAIMED: the release's new browser applier (release/<platform>/apply_release.html, generated by tools/gen_applier_page.py) produces the SAME romset as tools/apply_release.py, the tool of record, for both the standalone and the --no-qsound-bios variant; it refuses every input the Python tool refuses, naming the same thing and writing nothing; it contains no network primitive of any kind; and the shipped file runs from file:// in a real browser engine, driven through its own drop zone, radio and button. On that basis #146 and #144 are closed on the maintainer's rulings and the remaining M19 release blocker list is claimed to be empty but for #145.

NOT TESTED: the CONTAINER bytes of the zip the page writes are NOT identical to apply_release.py's and are not claimed to be (CompressionStream is not byte-comparable to zlib.compressobj); only Chromium is driven by a gate, Firefox was measured by hand on a capability probe only, and WebKit/Safari is UNMEASURED although it is the default for a double-clicked .html on macOS; no human has ever used the page (it has only been driven by a script); the page has never been run on Windows or Linux; the released artifact has not been re-run through the emulator tier since the release was re-packaged.
Artifacts (read every one, in full):
  - tools/gen_applier_page.py
  - tools/applier/applier.mjs
  - tools/applier/zip.mjs
  - tools/applier/page_shell.html
  - tools/apply_release.py
  - tests/test_applier_page.sh
  - tests/test_applier_page_browser.sh
  - docs/project/applier_app_scope.md

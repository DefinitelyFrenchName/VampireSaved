Q1: VIOLATED — The claim lists what it did not test, but it gets one premise wrong: it says the SELF leg pokes "a DIFFERENT id (a changed-value write)". The gate pokes the tenant's own id on the tenant's own real path: build/rulecheck/2026-09-25-218/b/audit_forced_pick_fidelity.sh.txt:26-28 "SELF — the REAL path PLUS the same-id poke … (a write at 1400-1500 to an id that already holds that value)"; :118 (self uses row_path) and :135 "f'{f}:ff8782:$_id'" with _id = row_id of the same row. So "the poke mechanism is inert" is not tested on the write the poked leg makes (0x13 to 0x10/0x11), and the claim does not list it as untested. A second premise is also unnamed: "exactly three" rests on a frozen file, and a FREEZE run exits 0 even when the identity, P2 or SELF checks fail, before the must-fire controls run (:292-296 "cp \"$W/new.tsv\" \"$EXPECT\" … exit 0"). None of these files shows the re-run that verifies it.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — Both legs share the position pins, but those write the P1 block ($FF8410) only from frame 2560 at the earliest (build/rulecheck/2026-09-25-218/b/name_moves.py.txt:687 "f\"{t - 40}:ff8410:{x1:04x}\"", FIRST_EVENT 2600, and part 1 is all "far"). The P2 HP pin writes $FF8850 from 2550 (:699). The level and RNG pins write globals (audit_forced_pick_fidelity.sh.txt:133-134). No file shows a shared poke or tool writing the P1 block in the pre-match window 1501-2362 (:180). The claim itself names the X pin's effect on the first-play frame.
Q4: VIOLATED — This failure mode is neither controlled nor named: the changed-value id write (Donovan's 0x13 overwritten with 0x10/0x11 at 1400/1450/1500) could itself perturb state, so a LATCHED byte would be the poke's doing, not the confirm's. Both controls that should catch it use a same-value write. REAL-vs-SELF: audit_forced_pick_fidelity.sh.txt:26-28 and :135. The donovan-self row: :48-51 "poking 0x13 over Donovan's own confirm", row_id donovan-self = 13 at :87. same-leg and wrong-cursor (:308-322) cover other failure modes. The claim asserts this failure mode is "tested directly on the same write the poked leg performs". The artifacts show it is not.
Q5: N-A — No decision, approval or ruling is attributed to the maintainer or to any person; the packet is a recommendation to report step 1 as done.
VERDICT: VIOLATED

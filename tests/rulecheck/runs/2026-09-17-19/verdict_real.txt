Q1: VIOLATED — the claim names its untested premises inside the gate but not the one the gate rests on: the POKED leg is the gate's own hand-written replica of the #136 leg, not the leg itself — the prologue is re-typed at audit_forced_pick_fidelity.sh.txt:122-124 and the level/RNG pins hard-coded at :133-134 under the comment "pokes_for's protocol" (:129-130, header :21-22 "(pokes_for, copied)"), and no artifact asserts the replica equals what #136 runs; "exactly three" is therefore a statement about the replica, which the claim does not say. Secondary: "two real picks of the same cell by different routes leave route residue" is asserted as measured, but the artifacts show only the MEASURE-mode mechanism (:90-94, :285-289), not a result.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."; the frozen play frame (forced_pick_fidelity.tsv:15 "2980") is a first-difference frame number with no move, timing or feel attached.
Q3: OK — the shared writes copied into both legs (audit_forced_pick_fidelity.sh.txt:131-139; name_moves.py.txt pins `{t - 40}:ff8410`/`ff8810` at 2560, HP pin `ff8850` from 2550, gate `ff8116` from 2000 and `ff80d4` from 2363) touch no P1-block byte inside the pre-match window 1501-2362 (:180, :189), so they cannot make the LATCHED set agree; the one shared write that does reach the compared block in play ($FF8410 from 2560) is named by the claim as the caveat on the frozen first-play frame.
Q4: VIOLATED — controls present: same-leg (:310-316, reducer blind), wrong-cursor (:318-322, identity assertion live), SELF and donovan-self (:281-282, forced_pick_fidelity.tsv:17, poke inertness), P2 block empty (:279-280), exact comparison to the frozen set (:298-305); the failure mode neither controlled nor named is the replica drifting from the #136 leg it stands for — a change to pokes_for or the rig's prologue leaves this gate green (the TSV header :9-10 asks for a re-freeze only "after a change to the naming rigs' prologue or to the poke protocol", a human obligation, not a control).
Q5: N-A — the packet attributes no decision, approval, ruling or closure to any person; the gate's header cites ticket history (:7-18) but the claim rests on none of it.
VERDICT: VIOLATED

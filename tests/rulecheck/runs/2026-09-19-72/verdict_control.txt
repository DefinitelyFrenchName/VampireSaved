Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the gate states its own gaps. audit_move_parity.sh.txt:34-36 says "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters ... P2 is Victor ... his state is never compared". The window is not the whole part: it "STARTS AT THE RIG'S FIRST EVENT" (:25-29, :119, :130-135). The comparison covers 12 tenant fields, not all state (:105). Only huitzil_1 was run, and it holds movement and normals only (name_moves.py.txt:448-460). Parts huitzil_2 to huitzil_10 are all frozen DIVERGES (move_parity.tsv:21-29) and were not re-run. The gate never reads or pokes $FF87C2 (:105, :86-103). No artifact shows the 0x01/0x00 readings, the poke run, the consumer enumeration ("pristine vsavj: zero"), or the rebuilt flavor_default build. The gate prints a verdict per part, not frame counts (:172-179). The 6851 figure fits no window I can derive: the part-1 schedule gives FRAMES=9230 (name_moves.py.txt:666-683), which is 6630 frames from the first event at 2600 and 6930 from FIELD_FROM=2300.
Q2: VIOLATED — The change alters how Phobos plays. His frozen part-1 divergence is a jump: move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f". The claim concludes he now matches native from the number "6851/6851 frames identical" alone. No image or recording shown to the maintainer appears in the artifacts.
Q3: VIOLATED — Both legs share the rig's base pokes (audit_move_parity.sh.txt:88, used by both branches at :98 and :101). Those pokes write compared fields. The tenant id ff8782 is forced on both legs (name_moves.py.txt:671-672; :92 on ours) and compared as "ff8782:b:id" (:105). The speed level ff8116 is written every frame from 2000 on both legs (:89) and compared as "ff8116:b:lvl" (:105). The x position ff8410 is re-pinned before every event (name_moves.py.txt:658, :675) and compared as "ff8410:w:x" (:105). So these fields agree by construction. Also, the native leg never selects Phobos at the cursor. It picks "Donovan ... the default cursor's R,R pick", and Phobos is then "FORCED by the early-window poke" (name_moves.py.txt:58-60; gate :30-32). The native 0x01 that the fix copies was therefore read under the same forced select both legs share.
Q4: VIOLATED — No control fires on this claim's failure modes. The instrument neither samples nor pokes $FF87C2 (:105, :86-103), so nothing proves ours held 0x01 during the run. Both controls read the frozen verdict of the first part in the set (:182, :188). With huitzil_1 frozen DIVERGES (move_parity.tsv:20), the no-translation control reports "CONTROL DEAD" (:195). The unpinned-level control prints "CONTROL FIRED" whenever the frozen verdict is not IDENTICAL, so it cannot fail here (:203-207). The claim names none of these failure modes, and no control covers them: damage, meter, hitboxes and projectiles (:34-35); huitzil_2 to huitzil_10; Phobos on the P2 side (:35-36); and whether the built flavor_default=0x01 behaves like a runtime poke.
Q5: N-A — The packet attributes no decision, approval or ruling to the maintainer or to any person. The claim is the working agent's own sentence.
VERDICT: VIOLATED

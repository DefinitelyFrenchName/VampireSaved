Q1: VIOLATED — The claim says "every file the table names as a home or test is a TRACKED file, checked by a TOOL now", but the table names files in plain prose that the tool never reads: (a) "README", (n)/(u)/(v)/(ff) "NEXT_SESSION trap …" / "setup note", and (m) "STATE row (9)". The tool resolves backticked tokens only (tools/homes_tracked.py:52) and reads prose only for build/ paths (tools/homes_tracked.py:91). Its output lists 24 tokens, and none of them is README, NEXT_SESSION or STATE (build/agent181/homes_tracked_14z181.txt:2-26). The untested list names this gap only for tests ("a prose-cited test that names an untracked file … passes its third read unseen"), not for homes. So whether these homes are tracked rests on the agent's reading, and the claim does not say so.
Q2: VIOLATED — The claim restates conclusions about hits a player can feel: "(ee) … pyron_3's event 5, named "far (whiff)" by the rig, in fact connects", and (dd) both Galactic Throw strengths throw ("test `test_move_naming` pyron (both lines a2:0x23)"). The five captures sent to the maintainer (docs/NEXT_SESSION.md:27-30) include neither. The claim concedes "(dd) and (ee) are RAM findings with no capture, and so is (q)'s Pyron half (Zodiac Fire)". Naming the gap does not produce a capture.
Q3: N-A — The packet compares no two legs of its own; its conclusion is about documentation. The ours-vs-native comparisons it restates belong to their gates (build/agent181/victim_parity_mode5_unpinned-level.log:36, build/agent181/trap_air_mode4_air-hit.log:6-10). The one pair it runs, the real tool against `--blind`, is built to disagree, and the gate reads DEAD if both pass (tests/test_close_tools.sh:58-59).
Q4: VIOLATED — Two failure modes are neither controlled nor named. (1) A home cited in prose by a bare file name (no backticks, no build/ prefix) that is untracked or missing passes read (1), which takes backticks only, and read (2), which takes build/ only (tools/homes_tracked.py:52,91). Read (3) scans test clauses only, and the self-test plants no such home (tools/homes_tracked.py:115). (2) Reach control 2 is described as "a known live corrected wording that must be found in BOTH its plain and its code-spanned carrier", but tools/retraction_grep.py:74 fails only when a control has 0 hits. If backtick collapsing stopped working, the plain carrier in tools/name_moves.py would still give 1 hit, so test_close_tools would stay green on a code-spanned miss. The claim nonetheless says "both tools are locked by the new ci_portable gate".
Q5: N-A — The claim attributes no decision, approval, ruling or closure to anyone. It says "the maintainer's read is theirs" and "awaiting the maintainer's read", and (t)'s recommendation sits under STATE "Decisions pending" (docs/NEXT_SESSION.md:22-27).
VERDICT: VIOLATED

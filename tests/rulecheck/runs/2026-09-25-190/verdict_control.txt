Q1: VIOLATED — The claim says the SELF leg pokes "a DIFFERENT id (a changed-value write)", so the inertness of the poke mechanism is "tested directly on the same write the poked leg performs". The gate shows the opposite. build/rulecheck/2026-09-25-190/b/audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". Lines 135 and 138 poke `$_id` (the row's own tenant id) on the self leg, after a real path that already confirmed that tenant. So the premise "a changed-value write to $FF8782 perturbs nothing" is untested, and the claim presents it as tested instead of naming it. A smaller point: the claim says the frozen first-PLAY frame "is a first difference outside the pinned field", but lines 197-198 only exclude LATCHED offsets, so P1 X (+0x010) can still set that frame.
Q2: N-A — The claim states "No behavioural conclusion about how the character plays is drawn here". The frozen first-play frame is only a RAM-difference frame (forced_pick_fidelity.tsv:15-16).
Q3: OK — The shared pokes that reach the P1 block are the P1 X pins at $FF8410. They are written only at t-40 from FIRST_EVENT 2600 (name_moves.py.txt:687), which is after the 1501-2362 LATCHED window (audit_forced_pick_fidelity.sh.txt:180). The claim names this pin as shared on both legs for the first-PLAY frame. The level and RNG pins are globals (lines 133-134), and the HP pin at $FF8850 starts at 2550 in P2's block (name_moves.py.txt:699). None of these writes the compared P1 state before the match.
Q4: VIOLATED — Failure mode: part of the POKED-vs-REAL LATCHED set could come from the poke's changed-value write (0x13 changed to the tenant id) rather than from Donovan's confirm. The claim does not name this gap; it says the gap is covered. The only control offered is REAL vs SELF (audit_forced_pick_fidelity.sh.txt:272, 281-282), and it writes an id that already holds that value (lines 26-28, 135). The donovan-self row is also a same-id poke of 0x13 over R,R (line 87-89, forced_pick_fidelity.tsv:12). Both controls show that a no-op write is inert, which is a different thing from the changed-value write the poked leg makes. The same-leg and wrong-cursor must-fire controls (lines 310-322) cover a vanished difference and a wrong identity, not this.
Q5: N-A — The packet attributes no decision, approval or closure to any person. It is the working agent's own recommendation.
VERDICT: VIOLATED

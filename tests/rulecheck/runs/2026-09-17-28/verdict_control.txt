Q1: VIOLATED — The claim says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)"; the artifacts show the opposite: audit_forced_pick_fidelity.sh.txt:135 builds `_pick` from `$_id` = `row_id` (the id the real path already confirmed) and line 138 applies it to the self leg, matching the header at lines 26-28, "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". So the inertness of a CHANGED-value id poke — the write the poked leg actually performs — is an untested premise the claim presents as tested; the other named gaps (globals/whole RAM MEASURE-only, TRANSIENT reported not frozen, P2 asserted on POKED-vs-REAL only at line 279, ALT route residue MEASURE-only at lines 90-94/285-289, pins at lines 131-134 and name_moves.py.txt:687/699, no stock pokes per name_moves.py.txt:646) are confirmed by the artifacts.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — The only shared poke the artifacts show reaching the compared P1 block is the position pin at $FF8410 (name_moves.py.txt:687, copied into both legs by audit_forced_pick_fidelity.sh.txt:131-132), which lands at frame 2560 onward, outside the 1501-2362 window that defines LATCHED (audit_forced_pick_fidelity.sh.txt:180); the claim names its effect on the first-play-difference figure. The level and RNG pins (lines 133-134) write globals, the HP pin (name_moves.py.txt:699) writes the P2 block.
Q4: VIOLATED — No control fires on the failure mode "a changed-value id poke at 1400-1500 itself latches P1-block bytes, which would then be counted among the 'exactly three'": donovan-self pokes 0x13 over an R,R confirm that IS 0x13 (lines 47-51, 87, 89), and REAL-vs-SELF pokes the row's own id over its real pick (lines 135, 138, 272, 281-282) — both same-value writes. The claim does not name this as a gap; it names it as "tested directly on the same write the poked leg performs", which the artifacts contradict. The same-leg (lines 310-316) and wrong-cursor (lines 253, 318-322) controls cover a vanished difference and a dead identity assertion, not this.
Q5: N-A — No decision, approval, ruling or closure is attributed to any person in the packet.
VERDICT: VIOLATED

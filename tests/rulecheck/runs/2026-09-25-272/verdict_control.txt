Q1: VIOLATED — The claim treats the SELF leg's poke as tested because it is "a poke of a DIFFERENT id (a changed-value write)". The gate shows a same-id poke instead: audit_forced_pick_fidelity.sh.txt:26-28 "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". Lines 114 and 135 build `_pick` from `_id="$(row_id "$_row")"`, the same row's tenant id, and line 138 applies it to the self leg. So the premise that a value-changing write to $FF8782 leaves the rest of the P1 block alone is untested, and the claim does not name it. The claim's other untested premises do match the artifacts: globals and whole-RAM output are MEASURE-only (lines 106-107, 208-211); TRANSIENT is not frozen (lines 284, 298-305); P2 is asserted only on POKED-vs-REAL (lines 279-281); ALT runs only under MEASURE (lines 249-251, 285-289); the pin frames are 2550/2560 (name_moves.py.txt:687, 699).
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here", and it draws none.
Q3: OK — The two legs share the rig's pokes (audit_forced_pick_fidelity.sh.txt:131-134, 137-138), but none of them writes the P1 block in the pre-match window (1501-2362). $FF8116 and $FF80D4 are globals. $FF8850 is in the P2 block, from 2550 (name_moves.py.txt:699). $FF8410/$FF8810 start at 2560 at the earliest (name_moves.py.txt:687). The only poke that reaches $FF8782 is on the POKED leg alone (audit_forced_pick_fidelity.sh.txt:137-138).
Q4: VIOLATED — One failure mode has no control and is not named as a gap: the POKED leg's value-changing id write (0x13 -> 0x10/0x11 at 1400/1450/1500) could itself cause one or more of +0x3BD/+0x3E0/+0x3C2 to differ, so those bytes might not come from the confirm. The gate's poke-mechanism controls cannot see this, because both write a value the byte already holds. The SELF leg is "the same-id poke" (audit_forced_pick_fidelity.sh.txt:26-28, 135, 138). The donovan-self row pokes 13 over Donovan's own 0x13 (lines 48-51, 87; forced_pick_fidelity.tsv:17). The must-fire controls are same-leg (lines 308-316) and wrong-cursor (lines 317-322); they cover a vanished difference and a wrong identity, not this. The claim says the changed-value write "is tested directly", so it does not name the gap.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

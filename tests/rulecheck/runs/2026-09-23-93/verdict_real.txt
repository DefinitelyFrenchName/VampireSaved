Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the gate's own header lists what it leaves out: audit_move_parity.sh.txt:34-36 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters ... P2 ... is never compared". Its window starts at the first event, not at the start of the part (line 25: "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT"). None of the three artifacts shows these claimed premises: the RAM:$FF87C2 reads (0x01 native, 0x00 ours), a poke of ff87c2, the 6851/6851 count (the gate emits only IDENTICAL/DIVERGES verdicts, lines 172-178), the consumer enumeration ("pristine vsavj: zero"), or any test of the rebuilt flavor_default=0x01 ROM as opposed to a runtime poke. Only part 1 was compared, and move_parity.tsv:20-29 freezes huitzil_1 through huitzil_10 all as DIVERGES.
Q2: VIOLATED — The conclusion is about how Phobos plays. Part 1 is his movement and normals (name_moves.py.txt:448-460: Jump, Air Dash, "Float [j.8 hold]"), and the frozen divergence is "huitzil_1	DIVERGES	Jump [8]+21f" (move_parity.tsv:20). The claim rests only on a number ("6851/6851 frames identical"). No image or recording put before the maintainer is named.
Q3: VIOLATED — Both legs receive the same base pokes (audit_move_parity.sh.txt:88 `_base` from the shared json, used on both legs at lines 98 and 101), and those pokes write compared fields. First, every huitzil part-1 event is pinned "far" (name_moves.py.txt:448), so name_moves.py.txt:658 pokes `{t - 40}:ff8410` on both legs, and ff8410 is a compared field ("ff8410:w:x", audit_move_parity.sh.txt:105). Position is reset on both legs before every event. Second, `_lvl` pokes ff8116 on both legs every frame from 2000 (lines 89, 98, 101), and "ff8116:b:lvl" is compared (line 105), so that field agrees by construction. Both legs also run the same .rpl rig (line 110).
Q4: VIOLATED — The gate has two controls. "unpinned-level" covers the play-mode mismatch and "no-translation" covers the pointer placement (lines 4-5). Neither covers the ways this claim could be wrong, and the claim names none of them as untested: (a) the ff87c2 poke not taking effect, or being overwritten, during the window. ff87c2 is not in FIELDS (line 105) and no instrument samples it. (b) A runtime poke is not the same as the built flavor_default=0x01 (the latch may be read at init, before the poke). (c) The flavor's effect on parts 2-10 and on damage, hitboxes and projectiles (lines 34-35). Also, the controls run on the first part of SET (line 182). For huitzil_1, frozen as DIVERGES, the no-translation control reports "CONTROL DEAD ... is not frozen IDENTICAL" (line 195), so this gate cannot produce a controlled IDENTICAL verdict for that part.
Q5: N-A — The packet attributes no decision, approval or ruling to the maintainer or to anyone else.
VERDICT: VIOLATED

Q1: VIOLATED — Finding (dd) says the outcome "follows the victim's height at the grab frame in every swept row". That is not true of every sweep. tools/name_moves.py.lines-160-220:28-29 limits it to "every row that records it (the lead, phase and walk-under-fix sweeps; the walk, x-pin and press sweeps at lead 0 sample no y)". docs/project/gotchas.md.lines-1-160:87 and STATE.md.lines-72-99:23 limit it the same way. So three sweeps' rows never recorded the height, and the claim does not list this among its untested points. It is the same kind of "every" overclaim that finding (ff) is about.
Q2: VIOLATED — The packet repeats conclusions a player could feel: (g) the level-6 whiff, (ee) "event 5 ... in fact connects", the landing-frame trap hit, and the +1 damage in (r)/(t). Every capture went out after its conclusion: "AFTER the RAM measurement that found it, not before" (build/agent181/captures_sent_14z181.txt:20, and lines 21-24 alike), and "AFTER the measurement (the naming freeze of a91a555a)" (line 39). The claim itself says "still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t)".
Q3: VIOLATED — In finding (l), our build and the vs2 extract both go through one classifier, `valid()`. That classifier decides which sequences get compared on both sides: "table c: 139 chains native, 139 ours, 81 not chains on both (walks into data), 0 differ in shape" (build/agent181/victim_parity_plain_14z181.log:81). Before `valid()` existed, "76 c chains 0x3d-0x8a differ in shape on our build" (STATE.md.lines-72-99:18). If `valid()` wrongly called a real chain a walk into data, it would drop that chain on both sides and the result would read as agreement. The legs also share the level pin ("level pin on; RNG pin on", log:13), and the unpinned-level control shows that pin changes the compared lines: "reads 13 line(s) differently from the level-8 pin ... the level pin is load-bearing" (log:38).
Q4: VIOLATED — Failure mode: tools/homes_tracked.py can match a home to the wrong file. It matches a name against every tracked file ending in that name, exact path first, then the shallowest (tools/homes_tracked.py:76, 161). So a home whose own file is untracked or missing still reads TRACKED if a file with the same name is tracked anywhere else, and the tree has such copies: "named README.md as release/merged-m17/mame/README.md" (build/agent181/homes_tracked_14z181.txt:42). No control fires on this. The self-test plants only full paths, names with no match at all, and README through its fixed name list (tools/homes_tracked.py:185). The live `--newest` gate checks only the exit status (tests/test_close_tools.sh:98). The claim calls the resolver "fixed" but does not say this case is untested or uncovered.
Q5: OK — Each attribution to the maintainer quotes their own words: "yes, identical." (DECISIONS_HISTORY.md.lines-28-48:9), "identical and validated otherwise" (line 10), "correct" (line 6), the trap caveat (line 13), and #169's "looks identical. I'll double check the air throw on my end but everything looks good" (line 14). The model switch is quoted too: "I had to switch to Opus 5.5. Resume the work." (STATE.md.lines-72-99:3).
VERDICT: VIOLATED

Q1: VIOLATED — The claim says the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write)", but the gate says the opposite. audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke: ... (a write at 1400-1500 to an id that already holds that value)". Lines 135 and 138 confirm it: SELF pokes `ff8782:$_id`, and `_id` is the row's own tenant id. So the claim counts an untested premise as tested: nothing shows that a changed-value write is inert. Its other listed gaps do match the artifacts: the globals and WHOLE are MEASURE-only (lines 106-107, 208-211); P2 is asserted on POKED-vs-REAL only (lines 279-282); ALT is MEASURE-only (lines 249-250, 285-289); the rig pins start at 2550 and 2560 (name_moves.py.txt:687, 699).
Q2: N-A — The claim draws no behavioural conclusion: "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — Both legs share the rig's pokes and the gate's pins. No artifact shows any of them writing the P1 block in the pre-match window (1501-2362). The level pin is the global $FF8116 (line 133). The RNG pin starts at 2363 (line 134). The P1 and P2 position pins start at t-40 = 2560 or later (name_moves.py.txt:687). The HP pin starts at 2550 and goes to $FF8850 in the P2 block (name_moves.py.txt:699). The P1 X pin in play is named by the claim.
Q4: VIOLATED — One failure mode is neither controlled nor named by the claim: the id changing from Donovan to the tenant at 1400-1500 could itself cause some of the "LATCHED" bytes, instead of the confirm latching them. The only mechanism controls are REAL vs SELF (line 272) and the donovan-self row (audit_forced_pick_fidelity.sh.txt:48-51, forced_pick_fidelity.tsv:17). Both use a same-id write that changes no value, so neither can fire on this. The claim says this is "tested directly on the same write the poked leg performs", and it is not. The must-fire controls do not cover it either: same-leg (lines 310-316) only shows that an empty diff fails the freeze, and wrong-cursor (lines 318-322) only checks identity.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

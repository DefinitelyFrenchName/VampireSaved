Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the gate names what it leaves out. audit_move_parity.sh.txt:34-36 says "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters ... P2 ... his state is never compared". Lines 25-29 show the window "STARTS AT THE RIG'S FIRST EVENT, not at the anchor", so it is not the whole part. Only huitzil_1 is claimed, yet move_parity.tsv:20-29 lists ten huitzil parts, and the "five tracks" are rebuilt on it. The "every consumer ... (pristine vsavj: zero)" enumeration appears in no artifact.
Q2: VIOLATED — The claim concludes how Phobos's part-1 moves play: node, x, y and the jump that move_parity.tsv:20 freezes as "huitzil_1	DIVERGES	Jump [8]+21f". It rests only on the number "6851/6851". No artifact shows an image or recording, or that one was put before the maintainer.
Q3: VIOLATED — Both legs get the same rig pokes, and those pokes write fields the gate compares. audit_move_parity.sh.txt:98 (native) and :101 (ours) both include "$_base" (the rig JSON's pokes, line 88) and "$_lvl". Line 105 compares "ff8410:w:x" and "ff8116:b:lvl". name_moves.py.txt:658 writes ff8410 (P1 x) 40 f before every "far" event, and huitzil part 1 is all "far" (name_moves.py.txt:448). Line 89 pins ff8116 to 06 on both legs. So x and the level are forced equal on both legs, and a defect in those pokes would make the legs agree.
Q4: VIOLATED — Nothing checks that the poke of the flavor latch actually held on our leg. $FF87C2 is not among the sampled fields (audit_move_parity.sh.txt:105 FIELDS has no ff87c2), and pokes_for (lines 86-103) has no flavor poke, so the gate as shown cannot run the claimed poke. The gate's own controls do not work for this part. huitzil_1 is frozen DIVERGES (move_parity.tsv:20), so the no-translation control reports "CONTROL DEAD ... not frozen IDENTICAL" (line 195). The unpinned-level control prints FIRED whenever the base verdict is not IDENTICAL (lines 203-207), so it cannot tell anything apart here. Step 2 would also FAIL an IDENTICAL result against the frozen DIVERGES row (line 178). The gate outputs verdict strings, not frame counts, so nothing in it produces "6851/6851".
Q5: N-A — The claim attributes no decision, approval or ruling to any person.
VERDICT: VIOLATED

Q1: VIOLATED — The claim says the greps snapshot was "RE-RUN after run 203's corrections ... its classing note saying so", with "33 distinct wordings ... twenty-two of them marked `gone`". The hand-written classing note in that same snapshot still carries the pre-203 counts: "The 21 `gone` wordings read 0 hits case-folded ... 32 distinct patterns." (build/agent181/retraction_greps_14z181.txt:84). The tool output above it reads "GONE: every gone wording absent (22)" (line 83) and "36 patterns" (line 1). So the hand classing, which the claim rests on, was not redone against the current output. Second, `gone` wordings "must read 0 hits anywhere", but the tool scans only `ROOTS = ["docs", "HANDOFF.md", "STATE.md", "STATE_HISTORY.md", "DECISIONS_HISTORY.md", "tests", "tools", "build/manifest"]` (tools/retraction_grep.py:41). README.md and the other root files are not scanned, and the NOT-tested list never names that. Third, the cited capture record still says the #169 sheets' "read is pending" (build/agent181/captures_sent_14z181.txt:37), two lines above the read itself (line 39).
Q2: VIOLATED — The packet restates conclusions a player could feel: (g) "a rig tuned at level 8 whiffs at 6", (ee) "event 5 ... in fact connects", the landing-frame trap hit of (bb)/(cc), and the +1 damage of (r)/(t). Every capture that exists went out after its conclusion: "AFTER the RAM measurement that found it, not before" (build/agent181/captures_sent_14z181.txt:20, and lines 21-24 alike) and "AFTER the measurement (the naming freeze of a91a555a)" (line 39). The claim itself says "still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t)".
Q3: VIOLATED — Finding (l) compares Phobos's c chains on our build against the vs2 extract, and both legs go through one classifier, `valid()`. That classifier picks which sequences get compared: "table c: 139 chains native, 139 ours, 81 not chains on both (walks into data), 0 differ in shape" (build/agent181/victim_parity_plain_14z181.log:81). Before `valid()`, 76 of those same sequences were "76 c chains 0x3d-0x8a differ in shape on our build" (STATE.md.lines-72-99:18). So the agreement ("0 differ") comes from the shared classifier dropping them on both legs. If `valid()` over-classed a real chain as a walk into data, the two legs would agree for that reason alone. The cited controls do not plant such a chain: `chains-planted` edits a valid Donovan chain, "b:0x00 first node 0xdad44 dur 2 -> 3" (line 10), and `label-planted` only flags an entered label (line 27).
Q4: VIOLATED — The claim can be wrong if a `gone` or retracted wording survives in a root document outside the grep's ROOTS. Nothing controls this and the claim does not name it. README.md is a home in the table itself ("home `release-inventory` memory + README", STATE.md.lines-72-99:24), and the sitting's first retraction was "grep-driven correction across STATE, HANDOFF, README, the memory" (STATE.md.lines-72-99:8). Yet README.md is not among the ROOTS (tools/retraction_grep.py:41). All three reach controls sit inside the scanned roots (tests/rulecheck/retractions/14z-181.tsv:28-30), so none can fire on an unscanned file. The claim's named gaps cover pattern width and pattern-set completeness, not which files are scanned, and it asserts "must read 0 hits anywhere".
Q5: OK — Every attribution to the maintainer is quoted in their own words. #173: "yes, identical." (DECISIONS_HISTORY.md.lines-28-48:9). (j): "identical and validated otherwise" (line 10). The trap caveat is at line 13. #169's sheets and the air-throw double-check: "looks identical. I'll double check the air throw on my end but everything looks good" (line 14; build/agent181/captures_sent_14z181.txt:39). The model switch: "I had to switch to Opus 5.5. Resume the work." (STATE.md.lines-72-99:3).
VERDICT: VIOLATED

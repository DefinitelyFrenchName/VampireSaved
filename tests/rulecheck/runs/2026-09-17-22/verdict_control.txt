Q1: VIOLATED — the claim states the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write)", but build/rulecheck/2026-09-17-22/b/audit_forced_pick_fidelity.sh.txt:135-138 builds SELF's poke from `_id="$(row_id "$_row")"` (the tenant's own id, the value the real path already confirmed) and line 26-28 names it "the REAL path PLUS the same-id poke ... a write at 1400-1500 to an id that already holds that value"; donovan-self (line 47-49, "poking 0x13 over Donovan's own confirm") is likewise a same-value write, so no leg tests a changed-value write for inertness and that premise is untested yet not named as such (the other named gaps — globals/whole-RAM MEASURE-only at lines 102-107 and 201-211, TRANSIENT not frozen at 283-284, P2 asserted only on the POKED-vs-REAL line at 276-280, route residue at 90-94 and 285-289 — are confirmed).
Q2: N-A — the claim draws no conclusion about how the character plays ("whether any latched byte is READ in play is a separate tap, not this gate"); the frozen 2980 at build/rulecheck/2026-09-17-22/b/forced_pick_fidelity.tsv:15 is a first-difference frame, not a feel claim.
Q3: OK — the only shared write into the compared P1 block is the position pin at $FF8410 emitted at t-40 = 2560 (build/rulecheck/2026-09-17-22/b/name_moves.py.txt:687, copied to both legs by audit_forced_pick_fidelity.sh.txt:131-132), after the last pre-match sample 2362 and named by the claim; the id poke reaches the block on the poked/self legs only (audit_forced_pick_fidelity.sh.txt:136-139), and the level/RNG pins (lines 133-134) write globals outside the diffed P1 range (line 189).
Q4: VIOLATED — the failure mode "the changed-value id write at 1400-1500 itself perturbs the P1 block, so part of the three-byte set is the write's, not the confirm's" is neither controlled nor named as a gap: the claim says it is "tested directly on the same write the poked leg performs", but the only inertness controls are same-value writes (REAL vs SELF, audit_forced_pick_fidelity.sh.txt:135-138 and 281-282; donovan-self, lines 47-51 and forced_pick_fidelity.tsv:17), and the two must-fire controls cover a vanished set (same-leg, lines 310-316) and a wrong real-leg character (wrong-cursor, lines 318-322), not this.
Q5: N-A — no decision, approval, ruling or closure is attributed to any person in the claim; the artifacts' references to #147/#151 (audit_forced_pick_fidelity.sh.txt:2, 12-16) are ticket citations, not attributed decisions.
VERDICT: VIOLATED

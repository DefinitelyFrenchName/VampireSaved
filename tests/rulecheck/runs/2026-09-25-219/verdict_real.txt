Q1: VIOLATED — The claim says "every traced field equal over 3500-3530", but the probe also traces vflip, the hurtbox tables vt0/vt1/vt2, and every $FF9400 pool slot's type/flip/x/y/node/rec (tools/trap_air_probe.sh:61-64). The gate compares only `FIELDS = ("vx", "vy", "vhp", "vcls", "vfrz", "vseq", "vfam", "p1x", "p1hp", "p1frz")` plus the victim node (tests/audit_trap_air_hit.sh:76, :92). Whether the dome's own state and the victim's facing and hurtbox tables are equal across the two legs is never tested, and the claim does not list that as an untested premise.
Q2: VIOLATED — The 3508 air hit has a capture (build/agent182/rc_sheet_3508.png) and the maintainer's reply "Yes, identical and this confirms the behavior is the same in both games" (build/agent182/maintainer_read_175.txt:7). The claim's second conclusion, the landing-frame hit at 3511 (y 42 -> 40, 0x52 / 0x38), has no capture among the artifacts. A 3511 sheet is mentioned only in the agent-written header "j.HP pressed at 3508 and at 3511" (build/agent182/maintainer_read_175.txt:6).
Q3: OK — Both legs share the replay and the level, RNG and x pins. The level and RNG pins write ff8116/ff80d4, which are not compared fields. The x pins write ff8810 and ff8410 only over 3470-3489 (tools/trap_air_probe.sh:44-51, tests/audit_trap_air_hit.sh:59), before FROM=3500, and the claim names them. The legs are shown to be different builds by the class byte at 3511: "cls_at_hit=82" native and "cls_at_hit=56" merged (tests/expected/trap_air_hit.tsv:7-8). Each leg resolves its fighters and nodes through its own data image (tools/trap_air_probe.sh:31-32).
Q4: VIOLATED — There are three controls, and each only perturbs the reduced rows. air-grounded rewrites y_at_hit, air-class rewrites cls_at_hit, and ours-drift moves one vy step (tests/audit_trap_air_hit.sh:121-123). So ours-drift proves the comparison can see the victim's y, not the other traced fields. No control would fire if the legs differed in the dome's own slot state (x/y/node/rec) or in the victim's flip or hurtbox tables, because those fields are never compared (tests/audit_trap_air_hit.sh:76). The claim does not name this gap. It names only that "which dome record (5 or 6) is live is read from the node, not asserted".
Q5: VIOLATED — The packet says "the level/RNG pins ruled the equalised input 2026-09-25", and the gate calls 0x38 "the ruled marker" (tests/audit_trap_air_hit.sh:8, :13, :116). No artifact quotes anyone making either ruling. build/agent182/maintainer_read_175.txt quotes only "agreed.", "Yes, do the Felicia rig…" and "Yes, identical … #175 can be clsoed", which supports the capture-sheet read and the closure but not these rulings.
VERDICT: VIOLATED

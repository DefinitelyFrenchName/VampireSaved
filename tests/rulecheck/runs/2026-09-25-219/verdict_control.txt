Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the artifacts show untested premises it does not name. (a) The gate compares only from the first event, not the whole part: audit_move_parity.sh.txt:25 "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor". (b) It compares only some fields, listed at audit_move_parity.sh.txt:105. Per lines 34-36 it does not cover "per-hit damage, meter gain, hitboxes and projectile parameters", and P2's state is never compared. (c) Only part 1 was run, but Huitzil/Phobos has frozen DIVERGES verdicts in parts 2-10 (move_parity.tsv:21-29), and the claim draws no conclusion about them. (d) The gate has no way to apply a $FF87C2 poke to one leg (pokes_for, audit_move_parity.sh.txt:86-103), so no artifact shows how the 6851/6851 run was made. (e) Nothing in the artifacts shows the consumer enumeration ("pristine vsavj: zero").
Q2: VIOLATED — The claim concludes Phobos's part-1 moves (jumps, floats, air dashes, normals: name_moves.py.txt:448-460) play the same as native. That rests only on the number "6851/6851 frames identical"; no image or recording appears in any artifact. The frozen divergence being overturned is a player-visible jump: move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f".
Q3: VIOLATED — Both legs get the same rig JSON pokes (_base), the same level pin and the same RNG pin: audit_move_parity.sh.txt:98 (native) and :101 (ours). Those pokes write fields that are then compared (line 105). (a) Every part-1 event is pinned "far" (name_moves.py.txt:448), so x is written on both legs 40 frames before each event: name_moves.py.txt:658 "{t - 40}:ff8410:{x1:04x}", compared as "ff8410:w:x". This re-syncs position before every event, which hides position drift that builds up between events. (b) The level byte ff8116 is poked to 06 on both legs (line 89) and compared as "ff8116:b:lvl". (c) The id byte ff8782 is poked to 10 on both legs, native via name_moves.py.txt:672 and ours via audit_move_parity.sh.txt:92, and compared as "ff8782:b:id".
Q4: VIOLATED — The gate has two controls: unpinned-level (covers a native leg left at the wrong play mode) and no-translation (covers a raw anim pointer compared without its placement translation). Neither covers how this claim could be wrong. (a) The latch is not sampled: "ff87c2" is absent from FIELDS (audit_move_parity.sh.txt:105), so nothing shows the poke took effect or that native reads 0x01. (b) No control runs ours without the flavor poke and requires it to diverge, or requires the built flavor_default=0x01 to match the run-time poke. (c) On huitzil_1 the no-translation control is dead by construction because the part is frozen DIVERGES: audit_move_parity.sh.txt:195 "CONTROL DEAD: no-translation — the control part $CTL is not frozen IDENTICAL, so the control cannot discriminate". The claim names none of these.
Q5: N-A — The claim credits no decision, approval or ruling to the maintainer or any other person; it states only the working agent's own build step ("flavor_default becomes 0x01").
VERDICT: VIOLATED

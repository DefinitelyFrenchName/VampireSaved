Q1: VIOLATED — The claim presents an untested premise as tested. It says the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The gate does not do this. build/rulecheck/2026-09-24-139/a/audit_forced_pick_fidelity.sh.txt:26-28 says "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Lines 114 and 135 build the SELF poke from the row's own id ("_pick ... f'{f}:ff8782:$_id'"), and line 138 applies that poke to the self leg. So SELF writes the same value that is already there. Whether a changed-value write to $FF8782 is inert, which is the write the POKED leg actually performs, is untested and not stated. The other stated premises match the artifacts: the globals are MEASURE-only (lines 208-210); TRANSIENT is not frozen (line 284); P2 is asserted only on POKED vs REAL (lines 272, 279); the pins land at 2550/2560 or later (name_moves.py.txt:687, 699).
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — The shared rig pokes cannot reach the compared P1-block state inside the pre-match window (1501-2362). The P1 position pin "{t - 40}:ff8410" is first emitted at 2560 (build/rulecheck/2026-09-24-139/a/name_moves.py.txt:687), the HP pin "ff8850" at 2550 (name_moves.py.txt:699), the level pin "ff8116" is a global, and the RNG pin starts at 2363 (audit_forced_pick_fidelity.sh.txt:133-134). The shared X pin that reaches the P1 block in play (+0x10) is disclosed by the claim. That pin is a one-frame write at t-40, not a hold, so the claim's "a first difference outside the pinned field" is looser than what the artifacts show. It is not a shared premise that could make the LATCHED set agree.
Q4: VIOLATED — Nothing controls for this failure mode: the three LATCHED bytes could be caused by the changed-value id write (0x13 -> 0x10/0x11 at $FF8782) rather than by the confirm latch, even though the claim charges them to the confirm. Both controls that exercise the poke use a same-value write. REAL-vs-SELF uses the row's own id (build/rulecheck/2026-09-24-139/a/audit_forced_pick_fidelity.sh.txt:135, 138), and donovan-self pokes 0x13 over Donovan's own 0x13 (lines 87, 89, and lines 48-51, "poking 0x13 over Donovan's own confirm must latch NOTHING"). The must-fire controls (same-leg, wrong-cursor; lines 308-322) cover neither. The claim does not name this as a gap; it asserts it is tested ("tested directly on the same write the poked leg performs").
Q5: N-A — No decision, approval or ruling is attributed to the maintainer or any person; the packet is the working agent's own recommendation.
VERDICT: VIOLATED
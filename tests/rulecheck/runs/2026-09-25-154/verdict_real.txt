Q1: OK — The claim lists what it did not test: "NOT tested or shown: WHICH ATTACK produced a hit … the hit FRAMES, P2's height … the level and RNG pins' own effect … the mechanism of the old sword +34 difference". What it presents as tested holds up. The HEAD reproduction passes: "ok    every row as frozen" / "PASS: audit_df_moves" (build/agent181/df_moves_HEAD_run.log:39,41). The row counts check out: "{'IDENTICAL': 19, 'CHANGED-CONTENT': 1, 'CHANGED-FRAMES-ONLY': 9, 'NEW': 4}" (build/agent181/df_moves_attribution_run11.txt:55). The old pin at +15 follows from the arithmetic: pins at 2550+400k (tools/name_moves.py:766) and 2HP at 5735 under the two-per-group rig (build/agent181/df_moves_HEAD.sh:107), recorded in docs/project/gotchas.md.lines-5618-5656:10-15. One gap: run 9's rows are not among the artifacts, so "runs 9, 10 and 11 print identical ev rows" is shown only for runs 10 and 11. The run-9 capture frames do match run 11's hit offsets, e.g. "donovan_dfx1_ev25_native_f7565.png" = +15 (build/agent181/cap_dfx2d_list.txt:5).
Q2: OK — The four contact events and 2HP were re-captured on both legs and delivered before the freeze: "delivered (third send, same sitting, FINAL rig — run 9 …)" and "the maintainer's read: NOT YET GIVEN" (build/agent181/captures_sent_14z181.txt:15,17). The sheets show both legs, e.g. "2 HIT" at +18 and "3 HIT" at +30 on native and ours (build/agent181/cap_dfx2d/sheet_donovan_dfx1_ev25.png). The claim reports the captions' overstatements and does not treat the maintainer's silence as a read.
Q3: VIOLATED — Both legs share a write to the compared state on every frame: "f'{f}:ff8116:06' for f in range(2000,$fr)" and "f'{f}:ff80d4:0000' for f in range(2363,$fr)" (tests/audit_df_moves.sh:207). The artifacts show that this write selects the compared hits. The RNG "picks P1-first or P2-first on every pass from bit 0 of the engine RNG … a fifth hit landed" (DECISIONS_HISTORY.md.lines-1218-1256:22-27). The gate's own control shows the pins change hits, class or gauge: "reads 2 row(s) differently in hits, class or gauge (frames stripped)" (build/agent181/df_moves_contact_run11.log:12). The legs also consume the RNG differently inside the sword window ("native=201" / "ours=198", build/agent181/df_moves_contact_run11.log:40-41). So a defect in the RNG's advance is reset away on both legs and would read as agreement. The claim names and accepts this as design, but naming it does not stop the two legs being one method on this premise.
Q4: VIOLATED — The claim says the SCHEDULE writes nothing inside the sword windows because "the gate VOIDs otherwise; run 11 VOID-free". That VOID (tests/audit_df_moves.sh:455-460) has no must-fire control. The eight MUST-FIRE lines (tests/audit_df_moves.sh:24-31) include none that plants a poke inside [T5-114, T5-41) or [T5, T5+46). The ev-window refusal does have one ("pin-in-window", tests/audit_df_moves.sh:287-288), but that control reads the ev windows only, not the sword windows. So a dead or mis-ranged sword-window check reads exactly like a clean run. The claim neither controls this failure nor names it. The claim's other failure modes are covered: instrument drift by the HEAD run, a probe knob that never reaches the legs by the positive ablation ("rng_reads_5hp_window native=174", build/agent181/df_moves_ablation_pos.log:41), and the pins by pins-unpinned with frames stripped (tests/audit_df_moves.sh:292-293).
Q5: OK — The #109 fold is the maintainer's own words: "*"I agree with all recommendations"*, said of each group of the 14z-157 decision sheet" … "#109: the clone-beam positive-contact damage leg is FOLDED INTO #136" (DECISIONS_HISTORY.md.lines-1257-1282:11-12,22). The 14z-158 method is quoted and limited to its own gate: "Asked "How should test_don_immortal_native handle the RNG?", the maintainer chose *"Pin both, assert frames (Recommended)"*" (DECISIONS_HISTORY.md.lines-1218-1256:29-30). The no-gauge rule is quoted: "*"I see, it makes sense that you can't build meter during DF"*" (tests/audit_df_moves.sh:62-63). The maintainer's read of the captures is recorded as not given.
VERDICT: VIOLATED
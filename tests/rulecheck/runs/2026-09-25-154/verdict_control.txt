Q1: VIOLATED — The claim says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)", but the gate says otherwise. audit_forced_pick_fidelity.sh.txt:26-27 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". At audit_forced_pick_fidelity.sh.txt:135 `_pick` writes `$_id` = row_id(row), which is the tenant's own id, on the self leg. So "the poke mechanism is inert … on the same write the poked leg performs" is presented as tested when the artifacts show it was not. The other untested premises the claim lists do match the gate (lines 201-211, 249-251, 284, 279).
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here". The first-play frame 2980 in forced_pick_fidelity.tsv:15 is frozen, but no conclusion is drawn from it about how the character feels to play.
Q3: VIOLATED — REAL and SELF, the pair used to prove the poke "inert", both hold the tenant's id before the poke. Identity is asserted at audit_forced_pick_fidelity.sh.txt:175-179: the REAL leg's cell must equal ID at 1290. The SELF poke at line 135 then writes that same `$_id`. So any perturbation that depends on the id value changing cannot make the two legs differ, and they agree by construction. The rig pins that reach the P1 block (the X at $FF8410 from 2560, name_moves.py.txt:687) are named in the claim.
Q4: VIOLATED — One failure mode is neither controlled nor named: the changed-value id write (0x13 to 0x10/0x11 at $FF8782, audit_forced_pick_fidelity.sh.txt:135/138) might itself cause a P1-block byte to differ, and that byte would then be charged to the confirm latch. The SELF control (lines 26-28, 281-282) and the donovan-self row (lines 48-51, "poking 0x13 over Donovan's own confirm") both perform same-value writes only. The must-fire controls also miss it: same-leg (lines 308-316) and wrong-cursor (lines 317-322) prove only that an empty diff and a wrong identity are noticed. The claim does not name this gap; it states the opposite ("tested directly on the same write the poked leg performs").
Q5: N-A — The packet attributes no decision, approval or ruling to the maintainer or any person; "report #151 step 1 as done" is the working agent's own recommendation.
VERDICT: VIOLATED
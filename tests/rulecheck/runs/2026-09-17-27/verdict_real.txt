Q1: VIOLATED — two statements the artifacts do not show: (i) "Victor's b:0x00-0x05 hitstun head hurtbox retuned on vs2" — the only node-level artifact, build/move_parity_census_14z164/same_data_detail.txt, stops at line 11-14 (`Traceback ... IndexError: string index out of range`) inside Demitri's c:0x2e, before Bishamon's and Victor's sections; no box tuple for Victor exists in the packet, and tests/expected/same_data_p2.tsv:4 records only that b:0x00-0x05 differ; the sole carriers are assertions (tests/test_same_data_p2.sh:12-13, tools/audit_same_data_p2.py:8-10); (ii) "invalid chains excluded and counted" / "NOT compared node by node but counted per row as an invalid chain" — a seq INVALID on ONE side and live on the other is still counted in `differ` and in the "nearest" reading: tools/audit_same_data_p2.py:144 `out[seq] = ("INVALID", ch["end"], 0)`, then :253-255 `A, B = ...; if A == B: continue; nd += 1` and :262 `"differ": sorted(k for k in ka & kb if sh["vsavj"][k] != sh["vsav2"][k])`; Demitri's c:0x2e is exactly that case — same_data_detail.txt:9 "c:0x2e: nodes 4/7 end hold/out_of_region" (the 7 is len("INVALID")) — and it sits in tests/expected/same_data_p2.tsv:3 c_differ "0x2e,0x2f", which the claim cites as Demitri's differing reaction chains "unchanged" by the exclusion, without stating that 0x2e differs by being an invalid entry on vs2, not by content.
Q2: N-A — the claim's own sentence "No behavioural conclusion about how any move plays is drawn by either freeze" holds: the rows carry field readings only (tests/expected/move_parity_events.tsv:504-505 "DIFF +156 p2hp" / "DIFF +167 p2hp"; contact_classify.txt:274 "DMG n/o 16/29 hits n/o 2/4" as numbers), and "retuned" names a box-tuple difference, not play.
Q3: OK — both legs receive the rig's own pokes (tests/audit_move_parity.sh:128 `_base=...['pokes']`, emitted at :134/:136/:139) and those write compared fields x/p2hp/stock (tests/replays/naming/pyron_4.json "4150:ff8850:01200120", "4060:ff8410:0228", "2540:ff8509:09"); the comparator removes exactly those (frame, field) samples (tools/move_parity.py:82 FIELD_ADDR, :168-170 `use = [f for f in live if f not in pins]`), the control shows the removal is what decides the one row it reaches (build/gates_14z164/move_parity_verify5.log:36), and the claim names the residue ("a pin resets an absolute field on both legs ... the frames after a pin start from an equal x"); the level and RNG pins (:129-130) reach no field in EVENT_FIELDS (tools/move_parity.py:65-66) and no artifact shows the RNG selecting a compared field inside a window; (B) shares decoders on both images but its verdict is DIFFERS, which a shared premise cannot force.
Q4: OK — the way runs 24/25 found this freeze wrong (a shared pin deciding a verdict, an inert exclusion) is controlled by pins-ignored and fired: build/gates_14z164/move_parity_verify5.log:36 "CONTROL FIRED: pins-ignored — pyron_4 moves 1 verdict row(s) with the pin exclusion off", the mode run FAILs at pyron_4's rows (build/gates_14z164/move_parity_ctl5_pins-ignored.log:18-30, EXIT 1), and the comparator refuses a schedule without `pokes` (tools/move_parity.py:209-210); no-translation and unpinned-level fired (verify5.log:37-38) and their modes FAIL (ctl4 logs:46-47, EXIT 1); (B)'s self-compare and dropped-row fired (build/gates_14z164/same_data_verify.log:7-8) and their modes FAIL (same_data_ctl_self.log:26-30, same_data_ctl_dropped.log:5-19); the remaining gaps — decoder blindness, the walker's INVALID verdict trusted as an instrument, attribution, meter mechanism, the 90% threshold, the one-run freeze — are named by the claim.
Q5: OK — DECISIONS_HISTORY.md.lines-30-52 line 4 quotes the maintainer's own words: *"I agree with the 3 propositions"* (maintainer, 2026-09-17), propositions 1 and 2 at lines 8-17; the claim attributes nothing beyond those.
VERDICT: VIOLATED

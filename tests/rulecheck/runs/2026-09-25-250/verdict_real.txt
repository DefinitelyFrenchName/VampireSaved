Q1: OK — the claim names what it leaves untested ("NOT tested: that the tags, the --stale re-run and the tier re-run come out green … the re-frozen prg_window pair was not re-run … its move is measured, not explained; that Donovan's solo track moves 110/111 for the same cause … #179 … the third store pair … the two stock-spend throws … the whole-RAM A/B's reach to M20 assumes the probe's mark glyphs change no execution"), and each of its other statements has an artifact behind it: build/rc183/freeze/sets_and_fingerprints.txt:2-7, stage4_rebuild.txt:2-3, baseline_fingerprints.txt:2-6, images_vs_zip.txt:2-11, members_check.txt:6, gfx_mark_check.txt:1-8, delta_attr.txt:11, m3a_repro.log:18, carry_and_suite.txt:8-15, sha1_moves.txt:2-7, battery_p2_results.tsv:2-10, tier_reds.txt:5-9; the untested items it lists match the open lines at prg_window_refreeze.txt:16 and staleness_fail.log:4
Q2: OK — the #157 behaviour change being frozen was played by the maintainer before the ruling, from their own recordings: build/rc183/freeze/maintainer_playtest.txt:2 "p157-probe-04 for Donovan / p157-probe-07 for Pyron / p157-probe-08 for Phobos" … "Everything felt normal. However I didn't measure anything"; the 4-vs-6 damage figure (sha1_moves.txt:9) is used only to explain why two expectations moved, not to say how anything plays
Q3: OK — the M19 and M20 images come from fresh decrypts of each build's own zip (build/rc183/freeze/images_vs_zip.txt:2-11), and the member comparison reads raw zip bytes by SHA-1 (members_check.py:20). delta_attr takes its addresses from the new build's placements.json (delta_attr.py:16), but in the opcode view it also requires the OLD build to hold b48c/b48e at those addresses (delta_attr.py:28), so a misplaced address would not agree. No line shows a shared tool that writes or selects the compared state
Q4: OK — each failure mode has a control that fires: an extra moved member (members_check_plant.txt:6 "FAIL on 5 pair(s)"); a gfx byte outside the mark glyphs (gfx_mark_check_plant.txt:3 "FAIL [(1, 1048577)]"); a program word no #157 row explains (delta_attr_plant.txt:7,9 "UNEXPLAINED 1 ['0x1234']", RESULT "2 finding(s)"); a row whose word did not move is reported as MISSING (delta_attr.py:31). The gaps not covered (prg_window not re-run, the solo track's 110/111 cause, the A/B's reach to M20, the post-commit tier) are the ones the claim itself lists as untested
Q5: OK — the maintainer's own statements are quoted: build/rc183/freeze/decisions_lines_30_68.txt:5 "Freeze, ticket it (Recommended)", :15 "Pin it to M19 (Recommended)", :23 "The maintainer chose "Freeze M20 now"", and maintainer_playtest.txt:2 "Everything felt normal. However I didn't measure anything"
VERDICT: OK
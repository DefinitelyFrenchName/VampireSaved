Q1: VIOLATED — The claim says "runs 10, 11 and 12 print identical rows (their GOT_OUT files are artifacts)", but run 10's file does not match. Its sword rows read "rng_reads_5hp_window	native=630" / "ours=621" (build/agent181/df_moves_got_run10.tsv:47-48), while runs 11 and 12 read "native=201" / "ours=198" (build/agent181/df_moves_got_run11.tsv:47-48, build/agent181/df_moves_got_run12.tsv:47-48). Only the 33 ev rows match in all three runs. The RNG-read row the claim relies on is reproduced by two runs, not three, and run 10 evidently read a different window, so it was not run under the final rig. A second point: the freeze header the gate will write still says "donovan_dfx1 ev19 at three hits" (tests/audit_df_moves.sh:635). Under this rig ev19 is 2LP, and the three-hit 2HP row is ev25 (build/agent181/df_moves_attribution_run12.txt:3). Everything else stated checks out, and the untested premises are named: HEAD reproduces "ok    every row as frozen" / "PASS: audit_df_moves" (build/agent181/df_moves_HEAD_run.log:39,41); the counts are "{'IDENTICAL': 19, 'CHANGED-CONTENT': 1, 'CHANGED-FRAMES-ONLY': 9, 'NEW': 4}" (build/agent181/df_moves_attribution_run12.txt:55); and the ablations read 174/171 and 201/198 (build/agent181/df_moves_ablation_pos.log:41-42, build/agent181/df_moves_ablation_neg.log:41-42).
Q2: OK — The four contact events and 2HP were re-captured under the final rig on both legs and delivered before the freeze, with the maintainer's read recorded as not given: "delivered (third send, same sitting, FINAL rig — run 9 …)" and "the maintainer's read: NOT YET GIVEN" (build/agent181/captures_sent_14z181.txt:15,17). The two corrective notes are at lines 14 and 18. The sheets show native above ours at each hit frame, e.g. "2 HIT" at +18 and "3 HIT" at +30 on both legs (build/agent181/cap_dfx2d/sheet_donovan_dfx1_ev25.png).
Q3: VIOLATED — Both legs are written on every frame with "f'{f}:ff8116:06' for f in range(2000,$fr)" and "f'{f}:ff80d4:0000' for f in range(2363,$fr)" (tests/audit_df_moves.sh:208). The artifacts show that this write selects the compared hits: "picks P1-first or P2-first on every pass from bit 0 of the engine RNG … a fifth hit landed" (DECISIONS_HISTORY.md.lines-1218-1256:22-27). The gate's own control confirms it: "reads 2 row(s) differently in hits, class or gauge (frames stripped), so the pins are load-bearing" (build/agent181/df_moves_contact_run12.log:12). So a defect in the RNG's advance is reset away on both legs and would read as agreement. The premise is named and is only pending as a ruling ("Options: (a) RULE the per-frame pins …", STATE.md.lines-206-206:1). Naming it does not make the two legs independent.
Q4: OK — Each way this claim could fail that the claim does not itself name has a control that fired in run 12: instrument drift: the HEAD reproduction (build/agent181/df_moves_HEAD_run.log:39-41); a shared write inside an ev window: pin-in-window (build/agent181/df_moves_contact_run12.log:11); a shared write inside the sword windows: sword-poke-planted, "a poke planted at T5+10 inside the sword window is refused" (build/agent181/df_moves_contact_run12.log:26); pins that are not load-bearing: pins-unpinned (line 12); the mode not entered, or an idle leg: mode-lost and idle-leg (lines 14, 23); a dead RNG read or a blind palette: rng-dead and palette-blind (lines 46-47); a dropped hit: hit-dropped (line 50); a probe knob that never reaches the legs: the positive ablation "native=174" (build/agent181/df_moves_ablation_pos.log:41); attribution collapsing rows: the row-count assert (build/agent181/df_moves_attribute.py:26); an unregistered build: the registry VOID (tests/audit_df_moves.sh:106-108). Which attack produced a hit, the hit frames, P2's position and height, and the pins' separate effects are named by the claim as untested.
Q5: OK — The #109 fold quotes the maintainer: "*"I agree with all recommendations"*, said of each group of the 14z-157 decision sheet" … "#109: the clone-beam positive-contact damage leg is FOLDED INTO #136" (DECISIONS_HISTORY.md.lines-1257-1282:11-12,22). The no-gauge rule is quoted: "*"I see, it makes sense that you can't build meter during DF"*" (tests/audit_df_moves.sh:63-64). The acceptances of runs 153 and 154 are the session's own (build/agent181/resolve153.txt:1, build/agent181/resolve154.txt:1), not credited to the maintainer. The maintainer's read of the captures is recorded as "NOT YET GIVEN".
VERDICT: VIOLATED

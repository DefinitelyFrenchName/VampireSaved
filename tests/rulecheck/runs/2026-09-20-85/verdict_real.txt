Q1: VIOLATED — STATE.md:65 quotes the tier's own outcome inside the close row ("the tier RE-RUN IN FULL … **PASS 151, SKIP 0, FAIL 0, MISSING 0, `fired 170 / declared 170`, GREEN**"), so that row and `docs/NEXT_SESSION.md:1` ("rewritten at the 14z-171 CLOSE") post-date the run whose doc gates the claim leans on (build/rig171/close_tier2.log:59,62,77 — test_checkdocs, test_docshape, test_state_open_lists PASS; line 159 "no tracked file changed during the run"); the two preceding closes record the missing step ("this row written after the tier (the doc gates re-run over it; the tier is not)", STATE.md:105; same at STATE.md:87) and this one does not, yet the claim's NOT-TESTED clause names only the emulator tier.
Q2: OK — STATE.md:78: the flash conclusion follows sheets put before the maintainer, who is quoted on them ("cosmetic but strange ... why it happens would answer whether it's a minor side effect or something that could have impacts elsewhere"); the 14z-171 answer (engine_internals.md:1356-1381, [VSP-186]) is the mechanism behind that capture.
Q3: OK — the one shared thing writing a compared field is the per-event stock poke, emitted by one function for both legs (tools/name_moves.py:750, `pokes += [f"{e['frame'] - 60}:ff8509:09" …]`; the field is compared at tests/audit_move_parity.sh:186 `ff8509:b:stock`), and build/rig171/stock_delivery.log:10-13 shows it landing rather than masking (pyron_5 old "9 9 8 7 6 5", new all 9), so a defect in it would separate, not agree; section 2b reads each leg's own trace (tests/audit_move_parity.sh:379-402).
Q4: OK — the stock-starved must-fire control (tests/audit_move_parity.sh:6, in-gate 405-431, mode 381) covers the way 2b could be wrong — a reader that reports rc=0 because it sees nothing — by zeroing a REAL trace at a REAL event frame and failing if no starved event is read ("CONTROL DEAD … fail=1", line 427); the uncovered mode (a gate outside the eleven re-run that reads the naming rigs) is named by the claim itself.
Q5: VIOLATED — STATE.md:63 "**Maintainer-ruled: land the stock fix, ticket the rest as #168, keep it on the next priorities**" and docs/NEXT_SESSION.md:25 "#162 … is ANSWERED and PARKED by agreement" (repeated at :46, and NEXT_SESSION.md:31 "THE RIG FIX THE MAINTAINER PUT ON THE NEXT PRIORITIES") attribute rulings with no statement of the maintainer's quoted anywhere in the artifacts; the sitting's only quote, STATE.md:62 ("is it measured or is it wishful thinking … it has to be demonstrably the case not just a neat idea"), is the landing CONDITION, not the ticketing, priority or parking decisions.
VERDICT: VIOLATED

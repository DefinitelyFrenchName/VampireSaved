Q1: VIOLATED — the claim says "every build input last modified before that rebuild", but build/rc170/freeze/rc_final/build_inputs_vs_m3a.txt:1 does not show that. It compares against "test_m3a_reproducible's log time", which is when the log was written at the end of the run, and no artifact records when the rebuild started. It also checks only a hand-picked list ("every manifest and the generator/builder tools"), and nothing shows that list is complete (for example, the modules the builders import are not listed). This premise is untested, and the claim does not name it among its untested items.
Q2: N-A — the claim draws no conclusion about how anything plays. The two differences a player could notice are frozen as measured and left open: Phobos's +1 (build/rc170/freeze/rc_final/rulings_emu.txt:10 "Freeze, ticket it (Recommended)", #161) and the column flash (#162). The one ruling about how a move reads cites a capture shown to the maintainer first (build/rc170/freeze/rc_final/rulings.txt:16 "Capture put before the maintainer first").
Q3: OK — the comparisons that agree take their references from independent sources: M18 against M19 members by CRC/SHA-1, images against their own patches, image deltas against the base, the x2b7ef4 bytes and the defense rows against vs2's own data image (build/rc170/freeze/rc_final/edits_check_tracks.py:41). edits_check_tracks.py:31-33 does take its op list from tools/attribute_patch_delta.py, but the claim does not present it as independent proof of the SAME/RELOCATED/MOVED split. The split is planted separately (build/rc170/freeze/rc_final/tool_plants.txt:2-6). The legs of the new gates disagree rather than agree (build/rc170/freeze/rc_final/phobos_residual_gate.verify.log:6-7).
Q4: VIOLATED — the claim says "one inventory as of this check covers EVERY gate that ran", but nothing controls for a gate the inventory never read. build/rc170/freeze/rc_final/final_inventory.py:46-47 (`p = f"tests/{g}.sh"` / `if not os.path.exists(p): continue`) silently drops any gate whose script is not at that path. The "# gates: 164" at build/rc170/freeze/rc_final/final_inventory.txt:2 is len(start) (final_inventory.py:43), not a count of scripts actually opened, so a skipped gate looks the same as a gate with no mentions. This failure mode is neither planted nor named: the named gap is a gate whose code was read but does not name the file.
Q5: OK — every ruling attributed to the maintainer is quoted in their own words: build/rc170/freeze/rc_final/rulings.txt:10, 20, 30, 38, 42-43 ("I agree with all recommendations"), 61 ("Freeze M19 now (Recommended)"); build/rc170/freeze/rc_final/rulings_emu.txt:10, 20; and the cadence ruling at build/rc170/freeze/rc_final/ci_emulator.tsv.lines-30-56.txt:17-21.
VERDICT: VIOLATED

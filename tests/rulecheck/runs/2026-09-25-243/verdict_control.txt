Q1: VIOLATED — The claim says "Not tested: nothing further", but the gate lists what it does not test: audit_move_parity.sh.txt:34-36 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters" and "P2 is Victor ... his state is never compared". The window starts at the rig's first event, not the part's start (audit_move_parity.sh.txt:25-29 and :130 `--first-event`), so "frame-exact over the whole part" is not what the gate measures. The claim also leaves unnamed that part 1 is only 1 of Huitzil's 10 parts (move_parity.tsv:20-29), and that a runtime poke of $FF87C2 stands in for a build-time flavor_default. No artifact shows either the consumer enumeration ("pristine vsavj: zero") or any figure of 6851 frames.
Q2: VIOLATED — The claim concludes that Phobos plays identically to native across his part-1 moves (the frozen row is move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f", a jump/float divergence a player can feel). It rests only on a number, "6851/6851", and no artifact shows an image or recording put before the maintainer.
Q3: VIOLATED — Both legs get the same rig pokes (audit_move_parity.sh.txt:88 `_base` from the json, applied to native at :98 and to ours at :101), and those pokes write compared fields. name_moves.py.txt:658 writes "ff8410" (P1 x) 40 frames before every "far" event, and every huitzil part-1 event is "far" (name_moves.py.txt:448), while ff8410 is a compared field ("ff8410:w:x", audit_move_parity.sh.txt:105). The level byte is poked to 06 on every frame from 2000 on both legs (:89 "ff8116:06") and is also compared ("ff8116:b:lvl", :105). The id byte ff8782 is poked on both legs (:92, name_moves.py.txt:672) and compared (:105). The flavor byte ff87c2 is not in FIELDS, and no line of pokes_for (:86-103) pokes it.
Q4: VIOLATED — The gate has only two controls, "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:61, :185-208). Neither would fire if the $FF87C2=0x01 poke never took effect, or if a runtime poke does not equal the build default. Neither would catch the per-event x re-pin (name_moves.py.txt:658) hiding a movement divergence. A PARTS=huitzil_1 run could not even pass: its frozen row is DIVERGES (move_parity.tsv:20), so :195-196 reports "CONTROL DEAD: ... not frozen IDENTICAL" and :178 fails the verdict. So the "6851/6851" result was not produced by a passing run of this gate, and the claim names none of these failure modes.
Q5: N-A — The claim attributes no decision, approval or ruling to any person ("flavor_default becomes 0x01" is the working agent's own action).
VERDICT: VIOLATED
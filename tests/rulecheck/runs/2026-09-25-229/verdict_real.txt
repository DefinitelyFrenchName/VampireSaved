Q1: VIOLATED — The claim says "the rows that move are only LP rows, SA's HP ..., the six VOID meter legs (now 1 hit, BOTH) ..." and that "the meter table was re-frozen from the +24 rig". The only artifact that shows the moved meter rows is build/agent183/p134_evidence/outoftree_diff.txt, and its line 1 says only "vs new (the fixed rig)". It never says which Lei-Lei j.LK offset produced the "new" side (+18, per runs 226/227, or +24). build/agent183/p134g/meter_freeze24.log:16-18 prints no row body, only "FROZE" and the hash self-match. So nothing shows that the frozen +24 table (diff.patch:98) differs from the old table only in the rows listed at outoftree_diff.txt:3-38 (e.g. the row "LE	hit_jump	LK	J.LK ... BOTH" at +24). That premise is neither stated nor shown. The premises the claim does name (the HP-drop count, the unchanged sets, SA's knife edge, the offsets outside +10..+34, video on only two legs) are all confirmed by the artifacts.
Q2: OK — Captures were made and read by the maintainer: build/agent183/p134_evidence/p134_captures_strip.png (old and new SA 5LP; j.LK at +14 and +18) and p134_captures_dense.png. The maintainer's read is maintainer_read.txt:8 "yes and this is as expected.", :15 "yes", :17 "yes, but to be precise she is too high to hit ..." and :19 "yes". The claim itself names the +24 leg and the other fourteen moved legs as uncaptured.
Q3: OK — The before/after legs share tools/vanilla_join_rig.py and the HP-drop reader, but they disagree (separations.txt:3 "hits 0" against :41 "hits [1, ..."), so a shared defect cannot be what makes them look the same. The one agreement, SA_near_2600 against the frozen rows (separations.txt:43), is the deliberate single-variable control. The dense sheet the maintainer confirmed carries only frame numbers and no trace-derived HP labels, so that read is independent of the trace.
Q4: OK — Each failure mode is covered by a control or named by the claim: "the rig change, not the timing, moved the rows" by the f2600 leg reproducing the old chains (separations.txt:43 "['a2:0x01', 'a2:0x02', 'a2:0x04', ..."); "the press sits at a band edge" by lk_sweep.txt:2-26 (every offset +10..+34); "the rehit window selects wrongly" by both control modes FAILing (rehit_ring_ctl_clear-on-attack-node.log:35, rehit_ring_ctl_missing-contact.log:37); the gates by their must-fire controls (join_freeze.log:15, meter_freeze24.log:20, crosscheck_freeze.log:12). The HP-drop premise is named by the claim as untested.
Q5: OK — Every attribution to the maintainer is backed by their own quoted words: maintainer_read.txt:17 "yes, but to be precise she is too high to hit and the jump does not result in a cross-up (i.e. she doesn't change sides). Adding the precisison because your description was ambiguous"; and the ruling at DECISIONS_HISTORY.md.lines-30-36:3 "Fix, and chase j.LK too".
VERDICT: VIOLATED

Q1: VIOLATED — The claim says the SELF leg pokes "a DIFFERENT id (a changed-value write)", so the poke mechanism is tested on the same write the poked leg makes. The gate shows the opposite. audit_forced_pick_fidelity.sh.txt:26-27 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Lines 118 and 135 build SELF from row_path plus `ff8782:$_id`, the row's own tenant id. So a changed-value write (0x13 to 0x10/0x11) is never tested, and the claim does not list that as an untested premise. A second unstated premise: the gate copies its pokes from tests/replays/naming/$_rig.json (line 115, line 132), not from name_moves.py. No artifact shows that JSON matches the generator the claim cites.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here." It concludes nothing about how a move plays.
Q3: OK — The shared pokes do not reach the compared P1 block in the pre-match window 1501-2362 (audit_forced_pick_fidelity.sh.txt:180). The level pin ff8116 and RNG pin ff80d4 are globals (lines 133-134). The HP pin ff8850 is in P2's block from 2550 (name_moves.py.txt:699). The position pins ff8410/ff8810 start at t-40 = 2560 at the earliest (name_moves.py.txt:687). The claim names the P1-X pin's effect on the first-play frame. The REAL leg's identity is not poked (line 137).
Q4: VIOLATED — Uncovered failure mode: the changed-value id write (Donovan's 0x13 overwritten by 0x10/0x11 at 1400/1450/1500) could itself perturb P1-block state, which would break the charge of the LATCHED bytes to the confirm. Both controls that could catch this use a same-value write: SELF (audit_forced_pick_fidelity.sh.txt:26-28, 135) and donovan-self (row_id 13 over path "R R", lines 87 and 89). So they see a different write than the poked huitzil/pyron legs make. The claim calls this failure mode tested ("tested directly on the same write the poked leg performs"); it does not name it as a gap.
Q5: N-A — The packet credits no decision, approval, ruling or closure to the maintainer or any other person.
VERDICT: VIOLATED

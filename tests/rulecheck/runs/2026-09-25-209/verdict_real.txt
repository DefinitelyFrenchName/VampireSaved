Q1: VIOLATED — The claim limits the pins premise to "the ours-against-native findings ((i), (j), (l), (aa) and the victim and trap rows)", which leaves (r) out. (r)'s "+1 on our build" is an ours-against-native parity result: "takes 12 from Donovan and from Pyron natively and 13 on our build" (STATE.md.lines-72-99:19). It comes from `audit_move_parity`, which runs at "real picks, level 6, RNG 0000" (STATE.md.lines-72-99:16). Row (10) names no pin premise, so the claim's catch-all ("every premise a finding's own STATE row or gate header names") does not cover it either. The claim also calls the victim gate a "FORCED-PICK leg" resting on "the shared pick poke", but the gate header says "REAL cursor picks ... no id poke on either leg" (tests/audit_victim_parity.sh.lines-1-60:8-9) and the log says "dropped 6 id pokes" (build/agent181/victim_parity_plain_14z181.log:13), so that stated basis is wrong.
Q2: VIOLATED — The packet restates conclusions a player could feel: the level-6 whiff (g), event 5 connecting (ee), the dome hitting on the landing frame (bb)/(cc), both air-throw strengths connecting (dd), and the +1 damage (r)/(t). Every capture went out after its conclusion: "AFTER the RAM measurement that found it, not before" (build/agent181/captures_sent_14z181.txt:20, and lines 21-24 alike) and "AFTER the measurement (the naming freeze of a91a555a)" (captures_sent_14z181.txt:39). The claim itself says "still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t)".
Q3: VIOLATED — The packet restates ours-against-native agreements ((i), (j), (l), (aa), "identically on both legs") that rest on a pin both legs share and that writes the compared state. The log shows "level pin on; RNG pin on" (build/agent181/victim_parity_plain_14z181.log:13) and "reads 13 line(s) differently from the level-8 pin (frames stripped), so the level pin is load-bearing" (build/agent181/victim_parity_plain_14z181.log:38). Both legs also go through one classifier: "81 not chains on both (walks into data)" (victim_parity_plain_14z181.log:81). The pins premise is still not ruled: "accepted in writing pending the ruling" (docs/NEXT_SESSION.md:24).
Q4: OK — Each way this packet could be wrong is either controlled or named by the claim. The controls: an untracked or build/ home is caught by `build-home-planted`; a grep that does not reach the tree by `unreachable-control` and `reach-threshold`; a retracted wording that comes back by `gone-reintroduced`; a matcher without case-folding by `case-fold-dropped` (all five "CONTROL FIRED", build/agent181/test_close_tools_14z181.log:10-14). Each mode fails on its plant's own FAIL text (the five test_close_tools_mode_*.log files, line 4, 6 or 8). A crash reads "REFUSED ... exit code: 3" (build/agent181/close_tools_crash_probe_14z181.log:5-6,10-11). Named by the claim as its own reading, not a tool's: that the table is complete, that a named section carries its finding, that a cited test exercises it, that carrier figures agree, that patterns are wide enough, and the `valid()` drop.
Q5: OK — Every attribution to the maintainer quotes their own words. #173 closed: "yes, identical." (DECISIONS_HISTORY.md.lines-28-48:9). (j) validated: "identical and validated otherwise" (line 10). The trap caveat (line 13). #169's sheets: "looks identical. I'll double check the air throw on my end but everything looks good" (line 14). The model switch: "I had to switch to Opus 5.5. Resume the work." (STATE.md.lines-72-99:3). The pins acceptance is credited to the working agent in writing, not to the maintainer.
VERDICT: VIOLATED

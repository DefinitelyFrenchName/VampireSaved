Q1: VIOLATED — build/rc170/freeze/rc_freeze/registry.diff:23: the merged-m19 note ("Ops 831->835", "Donovan 40, Pyron 12", "a uniform +0x30 shift of 27 placed regions", citing build/rc170/delta_ops_step1.txt, _step2.txt and delta_edits_check.txt) has no op-by-op attribution in the packet. The only delta files are delta_don_m23, delta_hui57, delta_pyron42, delta_m5_stock18 and stage4_delta, yet the claim says "each row's delta note matches" them and names no gap. The rows' "Members:" lists (registry.diff:20-24) and the stage-4 baseline "against HEAD's rebuild reproducing 108f7523" (registry.diff:25) also appear in no artifact; stage4_delta.txt:1-5 shows ops only.
Q2: N-A — the claim's sentence concludes only about fingerprints, set contents and the verdicts of the suite and the battery (masked RAM-checksum classes), not about how a move plays. The behavioural statement "the column, the trap and the EX paths equal native vs2 pinned" (registry.diff:23) is in the row, not in the claim, and the Q1 gap covers it.
Q3: OK — the legacy legs compare against frozen expectations that do not come from the build under test (donovan-m23.verify.log:4 "PASS masked-window (divergent frames 787, runs 1, window 889..1675"; stock.log:2 "diverges from vsavj/masked-v2 at exactly 4278"). The only same-rig agreement is freeze-then-verify of the self-frozen .sha1 on one build (donovan-m23.freeze.log:12 "frozen aab10131…" / verify.log:12 "PASS"), and the claim does not use it as proof of correctness: it names "moved for the fixes and nothing else" as untested. No artifact shows a shared element writing the compared state.
Q4: VIOLATED — build/rc170/freeze/rc_freeze/delta_don_m23.txt:9 "SAME 322  MOVED/RELOCATED 9  EDIT 9": the nine relocated ops are not itemized, and registry.diff:20 ("attributed op by op") does not mention them. No content-identity control for the MOVED/RELOCATED class is in the packet: delta_edits_check.txt is cited for merged but not supplied. So a byte change the tool classes as a relocation would pass unseen, and the merged row has no delta instrument at all. The claim names neither failure mode.
Q5: VIOLATED — the claim's "deleted after the freeze per the #111 ruling", registry.diff:20 "THE FOUR RULED #136 FIXES" and registry.diff:24 "the ratified #96 practice" all rest on rulings whose makers' own words appear in no artifact. sha1_moves.txt:13 says only "after the #111 deletion". The one quote, registry.diff:22 "Natural reading", names no speaker and is about Pyron's input reading, not these rulings.
VERDICT: VIOLATED

Q1: VIOLATED — The claim lists as tested a premise the artifacts show was not tested. It says the SELF leg is "a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The gate says "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)" (audit_forced_pick_fidelity.sh.txt:26-27). That leg's `_pick` is built from the row's own `$_id` (line 135) and applied to the self leg (line 138). So whether a changed-value write to $FF8782 is inert is untested, and the claim never names it as untested. A second untested premise also goes unstated: the pokes are copied from the tracked `tests/replays/naming/$_rig.json` (lines 115, 131-132), not from name_moves.py, and nothing checks that file against the generator.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."
Q3: VIOLATED — The REAL-vs-SELF comparison that backs "the poke mechanism is inert" is two legs sharing one real cursor path, and that path already selects the compared id (identity asserted on both at 1600, lines 172-174). The SELF poke therefore rewrites a value that is already there ("an id that already holds that value", line 27; lines 135/138). The two legs agree because of the shared path, not because the poke was shown to be inert. The main POKED-vs-REAL comparison is clean: no artifact shows a shared poke or input reaching the P1 block between 1501 and 2362.
Q4: VIOLATED — Three controls exist, each for a different failure: same-leg for a difference that vanishes (line 310, and it compares a leg with itself), wrong-cursor for identity (line 318), and donovan-self, a same-id poke of 0x13 over Donovan's own 0x13 (line 49). None of them can fire if the changed-value id write (0x13 to 0x10/0x11 at 1400/1450/1500) perturbs state by itself. The claim does not name that failure as a gap; it asserts SELF tests it, which lines 27, 135 and 138 contradict.
Q5: N-A — no decision, approval or ruling is attributed to any person in the packet.
VERDICT: VIOLATED

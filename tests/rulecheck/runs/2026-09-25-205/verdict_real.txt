Q1: VIOLATED — The claim says that when a tool exits non-zero WITHOUT its FAIL line, "in a mode or in the plain run", the gate prints REFUSED and exits 3, "never a red". The plain run's controls section does not do that. Its crash branch is `vs_ctl_dead ... ; fail=1` (tests/test_close_tools.sh:105-107, 110-112, 114-116, 117-119, 120-122), so the gate prints "FAIL" and exits 1 (tests/test_close_tools.sh:124-125). The only probe ran in a mode, "then CONTROL=unreachable-control tests/test_close_tools.sh" (build/agent181/close_tools_crash_probe_14z181.log:1). So the plain-run half is untested, the code contradicts it, and the claim does not name it. Second, the claim says "run 204 retracted a scope, not a wording". But build/agent181/resolve204.txt:2 reads "captures_sent's 'read is pending' line corrected". That is a stale-status wording of the same kind run 193 added as `gone` rows ("read not yet given", "both reads pending", tests/rulecheck/retractions/14z-181.tsv:35-36), and no pattern was added for it.
Q2: VIOLATED — The packet restates conclusions a player could feel: (g) "a rig tuned at level 8 whiffs at 6", (ee) "event 5 ... in fact connects", the landing-frame trap hit of (bb)/(cc), and the +1 damage of (r)/(t). Every capture went out after its conclusion: "AFTER the RAM measurement that found it, not before" (build/agent181/captures_sent_14z181.txt:20, lines 21-24 alike), and "AFTER the measurement (the naming freeze of a91a555a)" (line 39). The claim itself says "still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t)".
Q3: VIOLATED — Finding (l) compares Phobos's c chains, our build against the vs2 extract, and both legs go through one classifier, `valid()`. That classifier removes sequences from the comparison on both legs: "table c: 139 chains native, 139 ours, 81 not chains on both (walks into data), 0 differ in shape" (build/agent181/victim_parity_plain_14z181.log:81). Before it, "76 c chains 0x3d-0x8a differ in shape on our build" (STATE.md.lines-72-99:18). If `valid()` wrongly classed a real chain as a walk into data, that chain would be dropped on both legs and read as agreement. `label-planted` covers only an ENTERED label: "a line's b:0x03 replaced by the walk-into-data a:0x32 is flagged" (log:27). `chains-planted` edits a valid Donovan chain: "b:0x00 first node 0xdad44 dur 2 -> 3" (log:10). So the "0 differ in shape" on BOTH builds is one method, not two.
Q4: OK — Each failure mode the claim leaves open to a tool has a control that fires. An untracked or build/ home: `build-home-planted` fires (build/agent181/test_close_tools_14z181.log:14), its mode FAILs on "SELFTEST FAIL: (False, False, True, True)" (test_close_tools_mode_build-home-planted.log:8), and the live `--newest` row reads "FAIL 0" (test_close_tools_14z181.log:8). A grep that does not reach the tree, a raised reach threshold, a gone wording coming back and a dropped case-fold: `unreachable-control`, `reach-threshold`, `gone-reintroduced` and `case-fold-dropped` fire (log:10-13), and each mode FAILs on the plant's own text (mode logs, line 4 or 6). A crash in a mode: it reads "REFUSED ... exit code: 3" (close_tools_crash_probe_14z181.log:5-6). The victim gate fires all ten controls (victim_parity_plain_14z181.log:5-108) and the trap gate its three (trap_air_verify4.log:8-11). The claim names the rest as its own reading: table completeness, the right section, whether a test exercises its finding, carrier figures, the `valid()` residual, pattern width and the completeness of the retracted set.
Q5: OK — Every attribution to the maintainer quotes their own words. #173: "yes, identical." (DECISIONS_HISTORY.md.lines-28-48:9). (j): "The 1 frame difference and its origin must be documented but identical and validated otherwise." (line 10). The first read: "correct" (line 6). The trap caveat is at line 13. #169's sheets: "looks identical. I'll double check the air throw on my end but everything looks good" (line 14). The model switch: "I had to switch to Opus 5.5. Resume the work." (STATE.md.lines-72-99:3).
VERDICT: VIOLATED

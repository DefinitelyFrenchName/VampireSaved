Q1: VIOLATED — the claim says "every directory changed since is explained by an addition or one of five named removals", but build/rc170/freeze/rc_final/deletions.txt:3 scans only "every directory (outside build/, plus build/manifest and build/merged1)". The twelve untracked build directories, their baselines and build/out are covered only by outside_git_changes.txt:1-2, which reads the mtimes of files still present and so cannot see a removal. Separately, build/merged1's prg/ and rompath/ entries were re-created at 13:42:12-13Z (deletions.txt:52-53), after every pass-2 start. Those are ROM images git does not list, so they appear in neither final_inventory.py:20-21 (git-listed files only) nor outside_git_changes.txt:3-17. The NOT-tested list names neither gap.
Q2: VIOLATED — the frozen merged-m19 row concludes "the column, the trap and the EX paths equal native vs2 pinned" (build/rc170/freeze/rc_final/registry.diff:23). The claim also states "on merged-m18 the move runs six frames later", which rests only on uploader frame numbers (flash_gate.m18.log:11 "2807,2813,2864" against flash_gate.verify.log:11 "2807,2813,2858"). No artifact shows a capture of the column or the trap put before the maintainer; the only captures cited are the EX and Pyron sheets (rulings_emu.txt:38, rulings.txt:16). Meanwhile flash_gate.verify.log:7-8 shows our row 11 differing from native at 2860 and 2866.
Q3: OK — no artifact shows a shared writer reaching the compared state. M18 and M19 members are compared by two independent hashes (members_all.txt:7 "CRC and SHA-1 disagree on: none"). The DEFENSE and x2b7ef4 bytes are checked against vs2 "read from vs2's data image, not from our manifests" (edits_check_tracks.py:12-14). The rebuild is checked against the registered fingerprints (test_m3a_reproducible_now.log:4-18).
Q4: VIOLATED — failure mode: a carried authored .masked, .skip or mask was changed or loosened and the comparison missed it. The only evidence is "authored files IDENTICAL" from a "diff -r excluding .sha1/logs/README" (build/rc170/freeze/rc_final/sets_and_fingerprints.txt:1-7). Unlike the other checkers in the packet, this comparison has no plant, and the verify logs would PASS a loosened .masked just the same. The claim neither controls nor names this.
Q5: OK — the rulings are quoted verbatim: "Freeze, ticket it (Recommended)" (rulings_emu.txt:10), "I agree with all recommendations" (rulings.txt:42-43), "it should be applied ALWAYS on release but not for a freeze, UNLESS explicitly in scope" (ci_emulator.tsv.lines-30-56.txt:17-19), "Natural reading (Recommended)" (rulings.txt:20), "Freeze M19 now (Recommended)" (rulings.txt:61).
VERDICT: VIOLATED

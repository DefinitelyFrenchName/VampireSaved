Q1: VIOLATED — The claim says the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write)". The gate says the opposite: audit_forced_pick_fidelity.sh.txt:26-27 "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". The code agrees: self takes row_path at :118, and :135/:138 poke the row's own `_id`. So "the poke mechanism is inert on the same write the poked leg performs" is presented as tested, but nothing tests it, and the claim does not name it as a gap. The other stated premises do match the artifacts: globals and whole-RAM are MEASURE-only (:201-235), TRANSIENT is not frozen (:283-284), the P2 check is on POKED-vs-REAL only (:279), route residue is MEASURE-only (:285-289), and the pins are at :133-134 and name_moves.py.txt:684-699.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here". Only RAM offsets are frozen (forced_pick_fidelity.tsv:15-17).
Q3: VIOLATED — REAL and SELF both run the real cursor confirm, and it writes RAM:$FF8782, the id at select/commit, to the tenant id (audit_forced_pick_fidelity.sh.txt:43-46). That byte is inside the compared P1 block (:29, :189 `diff_region(0x400, 0x800)`). SELF then pokes that same value (:26-27 "an id that already holds that value"; :135). So REAL and SELF agree by construction, whether or not a changed-value poke disturbs state. The same holds for donovan-self, which pokes 0x13 over Donovan's own 0x13 confirm (:48-51). The claim treats this agreement as independent proof that the poke is inert.
Q4: VIOLATED — One failure mode is neither controlled nor named. The poked leg changes the id (0x13 to 0x10 or 0x11) between 1400 and 1500. If that change itself makes the game write other state, some of the three LATCHED bytes that the claim charges to the confirm belong to the poke. Both inertness controls write an unchanged value: SELF (:26-27, :135) and donovan-self (:48-51). No leg performs a changed-value poke over a real pick. The claim does not name this gap; it asserts the opposite ("a changed-value write ... tested directly on the same write the poked leg performs"). The same-leg control (:308-316) and the wrong-cursor control (:317-322) cover different failures: an emptied diff and a wrong identity.
Q5: N-A — No decision, approval or ruling is attributed to the maintainer or anyone else. The packet is a recommendation to "report #151 step 1 as done".
VERDICT: VIOLATED

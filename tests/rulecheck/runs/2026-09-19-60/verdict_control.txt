Q1: VIOLATED — The claim names no untested premise ("Not tested: nothing further — the comparison is frame-exact over the whole part"), but the artifacts show several. audit_move_parity.sh.txt:34-36 says "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters" and P2 "is never compared". Line 25 says "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT", so the comparison does not cover the whole part. Line 105 compares 12 tenant fields, not whole state. Part 1 is movement and normals only (name_moves.py.txt:448-460), with no specials, ES moves or Dark Force (parts 2-10). The passing leg was a runtime poke of $FF87C2, not the rebuilt flavor_default=0x01 build that gets frozen. The "every addressing form" consumer enumeration appears in no artifact. The frozen row "huitzil_1	DIVERGES	Jump [8]+21f" (move_parity.tsv:20) means an IDENTICAL result would FAIL this gate (audit_move_parity.sh.txt:174-178), and the gate reports a verdict, not a "6851/6851" tally.
Q2: VIOLATED — The conclusion is that Phobos now plays like native: the known divergence is "Jump [8]+21f" (move_parity.tsv:20), and part 1 is jumps, air dashes, floats and normals (name_moves.py.txt:448-460). The only evidence offered is the number "6851/6851". No image or recording appears anywhere in the packet.
Q3: VIOLATED — Both legs receive the same rig pokes, and some of them write compared fields. name_moves.py.txt:658 `f"{t - 40}:ff8410:{x1:04x}"` pins P1 X 40 frames before every "far" event, and every Huitzil part-1 event is "far" (:448). X is a compared field (audit_move_parity.sh.txt:105 "ff8410:w:x"), so the pin resyncs position on both legs before each event. The level pin `f'{f}:ff8116:06'` (:89) is applied to both legs (:98, :101) and is itself compared (:105 "ff8116:b:lvl"). The RNG pin ff80d4 (:90) is on both legs from frame 2363.
Q4: VIOLATED — The gate's only controls are unpinned-level and no-translation (audit_move_parity.sh.txt:4-5, 185-208), and both run on the first part of SET (:182). On huitzil_1, whose frozen verdict is DIVERGES, no-translation reports itself DEAD (:195). unpinned-level prints "CONTROL FIRED" whenever the frozen base is not IDENTICAL (:203-207), so it proves nothing there. Nothing covers this claim's own failure modes, and the claim names none of them: (a) whether the $FF87C2 poke landed and held on our leg — it is not in FIELDS (:105), and pokes_for has no ff87c2 poke at all (:86-103), so the 6851/6851 run happened outside this instrument; (b) whether a runtime poke is equivalent to a build-time flavor_default; (c) whether flavor consumers act outside the 12 fields or outside part 1.
Q5: N-A — The claim attributes the flavor_default change and the M19 freeze to no one, and invokes no maintainer ruling or approval.
VERDICT: VIOLATED

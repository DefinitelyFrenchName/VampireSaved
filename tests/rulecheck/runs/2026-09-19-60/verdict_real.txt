Q1: VIOLATED — The claim says "Each moved line's cause is in attribution_emu.txt", and the artifacts show otherwise. First, for move_parity_attribution the file says only "re-derived on the new events table" (build/rc170/freeze/rc_emu/attribution_emu.txt:39), yet its DEFENSE-ROW evidence moves from "Phobos takes 11 native / 13 ours" to "11 native / 12 ours" (expected.diff:416-418, :440-441), and nothing explains it. That value is still not native after a fix that "gave Phobos and Donovan vs2's own rows" (gates.diff:245-246). Second, for reaction_class_live the file says "read rows +0x30" (attribution_emu.txt:31), but the diff adds 0x38 to eleven readers' values and adds three new reader PCs, 3ffbc4/3ffc04/3ffc14 (expected.diff:618-628, :653-655). Third, the move-parity "second run" shows "exit 0" with no PASS line and lists only 27 parts against "60 legs ran" (verify.log:62-92). Fourth, the headline's "144 PASS" and the legacy "SUITE GREEN ... PASS 53" appear in no log in the packet; attribution_emu.txt:33 only asserts them. The claim names none of these as untested.
Q2: OK — The phase-1b capture was put before the maintainer as part of the question ("captures `build/rc170/freeze/contsw/phase1b_post_ko.png`", ruling_contsw.txt:3). replay62_post_ko.png shows the earlier KO and the skipped quote screen that the win-pal rework rests on. The fix effects are presented as re-freezes of earlier rulings, not new conclusions about how a move feels.
Q3: OK — The compared legs share replays and a read-only tap ("tests/lua/read_tap.lua, non-debug", expected.diff:40). No artifact line shows a shared rig, poke or tap writing +0x5C, the palette rows 0x15-0x19 or the continue landing. The reworked live gates also split the two builds rather than making them agree: they fail on merged-m18 (groundtruth.txt:27-41).
Q4: VIOLATED — No control covers a ruled fix moving a row to a value that is neither the old defect nor native, which then gets re-frozen as "by design". Example: "DEFENSE-ROW ... Phobos takes 11 native / 12 ours" (expected.diff:418). The attribution gate's only control is "no-ablation" (verify.log:97), which proves every DIFF row is attributed, not that a fix reached native. The claim does not name this gap. Separately, the win-pal check accepts any 3 consecutive pool-matching samples in K+300..K+1100, and nothing shows the matched window (from f5113 / f5273) is the quote screen: replay62_post_ko.png shows the original replay, not the reworked one. The plant is recorded only as "exit 1 ... best run 0" (groundtruth.txt:18-24) and is never described as a wrong-pool plant.
Q5: VIOLATED — The packet calls several changes ruled but quotes no one for them: "a table the ruled fixes move by design", "the EX route refused (the ruled fix)" (attribution_emu.txt:23) and "Pyron spends one on 623+PP, the ruled natural reading" (attribution_emu.txt:25). No maintainer statement is quoted for the EX-route, column/class-0x52 or defense-row rulings. The only person-sourced records are the continue-switch selection (ruling_contsw.txt:5-7) and an unattributed quote, "it makes sense that you can't build meter during DF" (attribution_emu.txt:27).
VERDICT: VIOLATED

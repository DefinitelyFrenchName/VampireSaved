Q1: VIOLATED — The claim says "every file the table names as a home or test is a TRACKED file, checked by a TOOL now (... 14 tokens, 0 FAIL, 0 REVIEW)". But the tool puts the tests the table cites by gate stem under OTHER and never checks them against `git ls-files`. build/agent181/homes_tracked_14z181.txt:19 lists "OTHER (sections, modes, columns, gate stems, memories): ... audit_df_moves | audit_move_parity_attribution | ... test_close_tools | test_gate_coverage_current | test_gate_follows | test_move_naming | test_poke_readback". The code that does this is tools/homes_tracked.py:47 and :56: a token without a `/` or an extension goes to `other.append(t)`. The claim's list of untested premises names only "that a section, docstring or control mode named as a home carries the finding is my reading" and the prose-words gap. It never says that the tests (b), (c), (q), (r), (hh) and (jj) name are not resolved to tracked files by any tool.
Q2: VIOLATED — The claim restates conclusions about hits: "(ee) ... pyron_3's event 5, named "far (whiff)" by the rig, in fact connects" and "(dd) ... test `test_move_naming` pyron (both lines a2:0x23)" (both throw strengths connect). It also says "(q), (i), (dd) and (ee) are RAM findings with no capture". The five captures put before the maintainer (docs/NEXT_SESSION.md:27-32) include neither event 5 nor the Galactic Throw pair. Naming the missing capture does not produce one.
Q3: N-A — This packet compares no two legs of its own. It concludes that each finding has a tracked home and test. The ours-vs-native comparisons it restates belong to their own gates (build/agent181/victim_parity_mode5_unpinned-level.log, build/agent181/trap_air_mode4_air-hit.log).
Q4: VIOLATED — The third read of the homes tool cannot see any test clause that contains a parenthesis, and the claim does not name this. tools/homes_tracked.py:67 captures the clause with `([^()]*?)`, so a clause can never run past a "(". (w)'s clause is "test: none yet (the probe was scratch, not promoted: ...)" (STATE.md.lines-72-100:24). It says "scratch", yet the tool reports "0 for REVIEW" (build/agent181/homes_tracked_14z181.txt:18). The only gap the claim names is prose "in words other than artifact/.txt/.log/scratch", and here the word is "scratch". The self-test plant also has no parentheses (tools/homes_tracked.py:81), so the self-test cannot catch this. Separately, the `build-home-planted` mode that the claim cites as failing "as declared" does not test the tool's catch at all. tests/test_close_tools.sh:50-59 marks the gate bad whenever a clean row reads clean, and a tool that caught nothing would do the same (build/agent181/test_close_tools_mode_build-home-planted.log:6).
Q5: N-A — The claim attributes no decision, approval or closure to anyone. It says only "the maintainer's read is theirs", and (t)'s decision is homed in STATE "Decisions pending" (docs/NEXT_SESSION.md:22-27).
VERDICT: VIOLATED

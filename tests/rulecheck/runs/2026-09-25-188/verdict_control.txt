Q1: VIOLATED — The claim says "Not tested: nothing further", but the gate lists what it leaves out: audit_move_parity.sh.txt:34-36 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters ... P2 is Victor, a legacy character, so his state is never compared". The flavor byte is not among the compared fields either (audit_move_parity.sh.txt:105 FIELDS has no ff87c2). Only huitzil part 1 is claimed, yet flavor_default is to change for all five tracks. No artifact shows the "every consumer enumerated (pristine vsavj: zero)" premise, the "6851/6851" count, or any ff87c2 poke.
Q2: VIOLATED — The claim concludes that the part-1 moves (jumps, air dashes, "Float [j.8 hold]" etc., name_moves.py.txt:448-460) play identically to native. It rests on a frame count only. No artifact shows an image or recording put before the maintainer.
Q3: VIOLATED — Both legs get the same rig pokes (_base, audit_move_parity.sh.txt:88, used for both legs at :98 and :101), and those pokes write compared state. (1) Before every "far" event (all of huitzil part 1), both legs have P1 x poked to the same value: name_moves.py.txt:658 "pin_pokes += [f\"{t - 40}:ff8410:{x1:04x}\", ...]", added at :675. That is the compared field "ff8410:w:x" (audit_move_parity.sh.txt:105). (2) Both legs have the compared "ff8116:b:lvl" pinned every frame from 2000 (:89, :98, :101). (3) Both legs have the compared "ff8782:b:id" poked to the tenant id (name_moves.py.txt:671-672). A defect in any of these would make the legs agree.
Q4: VIOLATED — The gate has two controls: unpinned-level and no-translation (audit_move_parity.sh.txt:4-5). Neither covers this claim's failure modes: that the 0x01 poke never reached the latch, or that a poke differs from a rebuilt build with flavor_default=0x01. No control withholds the flavor poke and requires ours to diverge. For huitzil_1 both controls are dead anyway, because move_parity.tsv:20 freezes it "DIVERGES Jump [8]+21f". The no-translation control then reports "CONTROL DEAD ... is not frozen IDENTICAL, so the control cannot discriminate" (:195). The unpinned-level control prints "CONTROL FIRED" whenever base is not IDENTICAL (:203-207), whatever the result. Also, an IDENTICAL huitzil_1 would fail against the frozen row (:174-178), and the gate prints a verdict and first divergent event, not an "N/N frames" count.
Q5: N-A — The packet credits no decision, approval, ruling or closure to the maintainer or any other person. "flavor_default becomes 0x01" is the working agent's own step.
VERDICT: VIOLATED

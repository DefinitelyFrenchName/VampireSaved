Q1: VIOLATED — three unnamed premises. (a) The pre-change events table is in no artifact, so "176 of 506 rows, 531 -> 707 samples ... every verdict, offset, field set, live-frame count and coupling byte-identical" rests on a diff nothing shows — only the post state exists (tests/expected/move_parity_events.tsv: 506 data rows, excluded column sums to 707). (b) The attribution re-freeze is unverified: every run of that gate FAILS (build/rig171/attr_stock.log:31 "FAIL: audit_move_parity_attribution"; build/rig171/v2/summary.txt:3 "audit_move_parity_attribution rc=1 46s FAIL") and tests/expected/move_parity_attribution.tsv:26 still carries the pre-change "stock 6 native vs 7 ours". (c) donovan_12 is one of the 14 moved meter parts (tools/name_moves.py:659) but test_move_naming SKIPS it (tools/name_moves.py:657 "NOT naming parts: test_move_naming.sh skips them"; build/rig171/naming_stockonly.log:1 lists donovan parts "1 2 3 4 5 6 7 8 13 14"), so the named separating control covers 13 of the 14 parts whose poke lists move.
Q2: N-A — no conclusion about behaviour in a build is drawn; tools/name_moves.py:739-740 writes only a .rpl and a schedule json, and the claim's results are expectation-table values (tests/expected/move_parity_events.tsv) and chain identity (build/rig171/naming_stockonly.log:7 "202 event lines identical to tests/expected/move_naming_donovan.txt").
Q3: VIOLATED — the compared legs ("ours (merged-m18) vs native vsav2", tests/expected/move_parity_events.tsv:5-6) run one generated rig carrying one stock poke, tools/name_moves.py:736 `pokes += [f"{e['frame'] - 60}:ff8509:09" for e in sched["events"]]`; that poke writes a COMPARED field ("the rig itself writes that field on that frame — its X pins, P2 HP pin, stock poke", tests/expected/move_parity_events.tsv:17-19) and build/rig171/attr_stock.log:18-20 shows it moving the compared state on BOTH legs together (huitzil_4:11 frozen "stock 5 native vs 6 ours" -> measured "stock 7 native vs 8 ours"). A poke that failed to land would leave both legs on an empty meter, both degrading to the normal version, i.e. AGREEING as IDENT — and its own samples are excluded from the comparison by construction.
Q4: VIOLATED — the specific failure mode is "the per-event poke does not actually deliver a stocked meter at the event", which produces exactly the reported ZERO chain differences, so the claim's separating control cannot separate "nothing ran dry" from "the poke does nothing"; and the instrument freezes only chain ids — tools/name_moves.py:879 emits `<part>\t<event>\t<chains>` with no stock field (stock is printed by `analyse` at :831 but never asserted) — so it does not see the meter the claim rests on. Controls present cover other things: build/rig171/mp_verify_stock.log:37-39 (pins-ignored on X pins, no-translation, unpinned-level), build/rig171/naming_stockonly.log:12-14 (swapped seqs, swapped chain), build/rig171/attr_stock.log:30 (no-ablation). The claim names other gaps but not this one; the only delivery evidence in the packet is the 4 DF-STOCK rows of build/rig171/attr_stock.log:14-28.
Q5: N-A — the claim attributes no decision, approval, ruling or closure to any person; the maintainer lines in the artifacts (tools/name_moves.py:85-87 "maintainer-ruled 2026-09-17, DECISIONS_HISTORY.md"; tests/expected/move_parity_events.tsv:20-21 "maintainer-agreed 2026-09-17") belong to the prior P2=Demitri and #136 freezes, not to this action.
VERDICT: VIOLATED

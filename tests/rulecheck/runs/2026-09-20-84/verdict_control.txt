Q1: VIOLATED — the claim says "Not tested: nothing further", but issue_151_timeline.txt:2 states "At this date the sessions and the maintainer shared ONE GitHub account (DefinitelyFrenchName)", so "the maintainer's account as its actor" is itself an untested premise the claim does not name; issue_151_timeline.txt:5 carries `commit=afff6938…`, the session's own 14z-161 close commit (git_log.txt:3), a second untested premise.
Q2: N-A — the packet concludes only about issue/tree bookkeeping (state_row_draft.txt:1); no behaviour a player could feel is claimed.
Q3: VIOLATED — the claim contrasts this close with the earlier one ("deliberate this time"), but both legs are read through the same actor field of the one shared account (issue_151_timeline.txt:2) and both carry a `commit=` of a session-close commit pushed seconds earlier (issue_151_timeline.txt:3 `commit=58b3c0d9…` = git_log.txt:2 at 00:22:52+02:00; issue_151_timeline.txt:5 `commit=afff6938…` = git_log.txt:3 at 02:17:05+02:00, i.e. 00:17:05Z, three seconds before the 00:17:08Z close) — a closing-keyword auto-close on push writes both events identically, so the two legs AGREE as "maintainer closed it" by construction.
Q4: OK — the `commit=` field is the control, and it discriminates: it is `-` on the manual reopen (issue_151_timeline.txt:4) and populated on both closes (issue_151_timeline.txt:3, :5), so it covers the failure mode "the close was written by a pushed commit's closing keyword, not by a person" — and here it fires against the claim.
Q5: VIOLATED — the packet attributes the closure to the maintainer ("CLOSED BY THE MAINTAINER", state_row_draft.txt:1) with no statement of theirs quoted in any artifact; the attribution rests only on an actor field of an account the artifacts say is shared (issue_151_timeline.txt:2) and on a timestamp.
VERDICT: VIOLATED

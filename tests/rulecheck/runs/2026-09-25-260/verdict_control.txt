Q1: VIOLATED — The claim says the SELF leg is "a poke of a DIFFERENT id (a changed-value write)" and lists this among the things that are tested. The gate does the opposite. audit_forced_pick_fidelity.sh.txt:135 builds the SELF poke from the row's own id ("f'{f}:ff8782:$_id'", with _id = row_id of the same tenant, line 114), and lines 26-27 say "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". So the premise that the changed-value poke write is inert is untested, and the claim presents it as tested instead of naming it.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here", and it draws none.
Q3: VIOLATED — REAL and SELF share the tenant id, and SELF's poke writes that same value into a byte inside the compared block. The poke writes $FF8782, which is offset 0x782 and so inside the P1 range diff_region(0x400, 0x800) (audit_forced_pick_fidelity.sh.txt:189). It writes the id the REAL leg already holds; the gate asserts that at line 172 ("v = byte(d, 1600, 0xFF8782)"). The poke in audit_forced_pick_fidelity.sh.txt:135 uses the same _id as the REAL leg. The two legs therefore agree by construction, and the check at line 281 ("REAL vs SELF latches nothing (the poke mechanism itself is inert)") is one method, not an independent comparison.
Q4: VIOLATED — One failure mode is neither controlled nor named by the claim: the POKED leg's changed-value write (0x13 to 0x10/0x11 at 1400/1450/1500) could itself cause some of the "LATCHED" bytes, instead of Donovan's confirm causing them. No control can separate these. SELF writes only the same value (audit_forced_pick_fidelity.sh.txt:26-28, 135). donovan-self is also a same-id poke ("poking 0x13 over Donovan's own confirm", lines 48-50). The declared controls are same-leg and wrong-cursor (lines 4-5). They cover a vanished difference and a wrong cell, not a changed-value poke effect. The claim says this failure mode is tested "directly on the same write the poked leg performs", which is false, so it does not name it as a gap.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

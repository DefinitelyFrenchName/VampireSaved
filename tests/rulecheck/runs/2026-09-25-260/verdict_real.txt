Q1: VIOLATED — The claim says "two PROVENANCE rows corrected", but the artifacts show only one. build/agent183/packet_homes_14z183.txt:58 quotes tests/expected/PROVENANCE.md:133, and build/agent183/retraction_greps_14z183.txt:84 classes that one row ("the row now reads 'the naming parts (30 at that freeze; 32 runs at the M20 re-freeze'"). No artifact shows the second row or its correction, and the claim does not name this as unchecked.
Q2: N-A — The claim is about where each finding is documented and what each test does. It draws no conclusion about how a move plays or feels.
Q3: N-A — No two legs are compared. packet_verify.py (build/agent183/packet_verify.py:10-11) checks the packet's quotes against the tree, and class_letters.py and classing_cover.py check that the packet's own text is complete.
Q4: VIOLATED — One way this could be wrong is neither controlled nor named: a count in the close record going stale after a later packet run. STATE.md:95, CLOSE row item (2), still reads "eight retracted wordings at first, eleven after runs -254 (Lilith) and -255 (two #134 wordings) ... and four reach controls". build/agent183/retraction_greps_14z183.txt:1 shows "17 patterns", 13 of them retracted; the two (aa) patterns 'the 506 rows equal to' and 'the 30 naming parts' at lines 63 and 65 are missing from that record. This is the stale-506 class again, in a row the claim itself cites ("both stated in STATE.md's CLOSE row"). The claim's named gap covers only quoted lines ("whether a quoted line's CONTENT is still true"), and this row is not quoted in the packet. The named gap on the retraction list's completeness is also real, though it is OK under the rule: STATE.md:84, row (6), still says "x 179 / 35 frames from +116, y 39 from +118 in the raw traces" and "the meter difference at +89". docs/project/gotchas.md:5879-5881 says the +116 came from the unpinned trace ("against the pinned leg ... x first differed at +156"). Row (6) has no retraction pattern and no in-place mark.
Q5: OK — The claim credits no decision to anyone. The one ruling in the packet is quoted in the maintainer's own words: build/agent183/packet_homes_14z183.txt:29, "maintainer-ruled 2026-09-26, 14z-183: "Pin it to M19".
VERDICT: VIOLATED

Q1: VIOLATED — The claim presents the rig facts as measured, not as untested: "the screen-edge clamp of a pin after a throw (real, ...)" and "the HP event stands at 960/1000, where the 150-frame walk-in leaves a near event the previous one did not displace". No named artifact measures the clamp. The only statement of it is prose: "real — a 552 pin after a throw reads 664" (build/agent181/issue169_close.md:3). The same holds for "A camera poke ($FF8290) at the pin frame does NOT hold" (tools/name_moves.py.lines-735-780:22-23). The generator's model is "so the wall is certain ... 'near' is 'at the wall' unless the previous event left P2 displaced" (tools/name_moves.py.lines-735-780:21-24). The walk sweep contradicts that model: the MP event stops at 858/898, not the wall, at walks 120, 135 and 150 (build/agent181/air_throw_sweeps_14z181.txt:9-11). At 2.65 px/frame from 552, a 150-frame walk would reach about 950, so something stops the pair at 898, and no artifact names it. No artifact shows the MP event's previous event (the "Planet Burning [MP] far (whiff)", tests/expected/move_naming_pyron.txt:63) displacing P2. The claim does not name either premise as untested.
Q2: VIOLATED — The conclusions are about a hit a player could feel. The ticket reads "the air throw connects on only one strength" (docs/project/tickets.tsv.lines-80-80:1), and the carriers say "the HP throw whiffed just the same pinned OFF the wall at 858/898, and with the lead it connects ON it" (build/agent181/gotchas_169_14z181.diff:21-22) and "the MP throw connected and the HP one never did" (tools/name_moves.py.lines-735-780:25-26). All of it rests on RAM chain ids, and the claim itself says "no capture was produced".
Q3: OK — The shared X-pin pokes write the compared x, but that write is excluded and the exclusion is proven live: "CONTROL FIRED: pins-ignored" (build/agent181/move_parity_169_verify.log:42). Node translation is covered by "CONTROL FIRED: no-translation" (:43) and the shared level pin by "CONTROL FIRED: unpinned-level" (:44). The level/RNG pins and pokes are named in the claim as the shared equalised input. No artifact shows the P2 jump lead writing node, seq or meter on one leg only.
Q4: OK — The naming compare is live on exactly these rows: "-3 Galactic Throw [j.6HP] a2:0x14 / +3 ... a2:0x23" (build/agent181/move_naming_169_run2.log:14-15). The own-name check fires when neither strength enters a2:0x23 (build/agent181/move_naming_169_run1.log:21-22), and "control: the swapped 5LP line fails the compare" holds (build/agent181/move_naming_169_verify.log:14). The phase shifts are shown applied: the rigs end 7038-7047 against 7035 (build/agent181/air_throw_sweep_phase3_12.log:2-11; build/agent181/air_throw_sweep_repro_walk.log:2). The instrument reproduces the lead-0 rows (build/agent181/air_throw_sweep_repro_walk.log:4-5, build/agent181/air_throw_sweep_repro_xpin.log:3, build/agent181/air_throw_sweep_repro_press.log:3). The claim names the remaining gaps: the mechanism, the ours leg read only through parity fields, j.4, damage/ES, Huitzil's mechanism, and native-only sweeps.
Q5: N-A — The packet attributes no decision to any person. The close says "decided — none (a rig fact, no gameplay decision)" (build/agent181/issue169_close.md:9), the close is "PROPOSED", and the pins are only "put to the maintainer under STATE "Decisions pending"", not presented as approved.
VERDICT: VIOLATED

Q1: VIOLATED — The claim describes the SELF leg as poking "a DIFFERENT id (a changed-value write)" and says inertness is "tested directly on the same write the poked leg performs". The gate does not do that. audit_forced_pick_fidelity.sh.txt:26-28 defines SELF as "the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Lines 114, 135 and 138 build the SELF poke from `_id="$(row_id "$_row")"`, which is the tenant's own id. So the premise that the POKED leg's changed-value write (0x13 to 0x10/0x11) is inert is untested, and the claim says the opposite instead of naming it. The other stated premises check out against the artifacts: globals and whole-RAM are MEASURE-only (lines 102-107, 201-235, 291); TRANSIENT is not frozen (line 284); P2 is asserted only on POKED-vs-REAL (lines 272, 279); there are six pre-match frames (line 99); and the pins land where the claim says (name_moves.py.txt:687 and 699; gate lines 133-134).
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here". The only play-side item is the frozen frame number 2980 (forced_pick_fidelity.tsv:15).
Q3: OK — The shared pokes do not reach the compared P1 block before the match. The level pin (ff8116) and RNG pin (ff80d4) are globals (audit_forced_pick_fidelity.sh.txt:133-134). The position pins ff8410/ff8810 start at `t - 40` = 2560 and the HP pin ff8850 at 2550 (name_moves.py.txt:687 and 699), after the 1501-2362 window (gate line 180). The claim names the P1-X pin's overlap with the first-PLAY-difference measurement.
Q4: VIOLATED — Neither control covers this failure mode: the POKED leg's changed-value id write at 1400/1450/1500 itself disturbs P1-block state, and the gate charges that to the confirm as LATCHED. The REAL-vs-SELF check (audit_forced_pick_fidelity.sh.txt:272 and 281) and the donovan-self row (line 87, id 13 over R,R) both exercise only a same-value write (lines 26-28 and 135). The same-leg and wrong-cursor controls (lines 308-322) test for a vanished difference and for identity, not this. The claim does not name the gap; it asserts the gap is covered.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

Q1: VIOLATED — the claim does not name one untested premise: that each difference charged "to her own data" is vsavj's own Felicia data and not something the merge introduced. The charge compares the vs2 image only against the merged build's own image, `SB = {"native": static_boxes(sys.argv[2], "vsav2"), "merged": static_boxes(sys.argv[3], "vsavj")}` with argv[3] = "$MERGED/verify_data.bin" (tests/audit_trap_air_hit.sh:85,109). The gate states the equivalence rather than testing it: "(on ours vsavj's, by the superset invariant)" (tests/audit_trap_air_hit.sh:78; also :23). None of the named files reads vsavj's original data. All the premises the claim does name are confirmed by the files (level and RNG pins inside the window, tools/trap_air_probe.sh:41; x pins 3470-3489 with FROM=3500, tests/audit_trap_air_hit.sh:66-67).
Q2: OK — both capture sheets exist (build/agent182/rc_sheet_3508.png shows f3521 "y=42 AIR cls=0x07 hp=284 a2:0x14#4" on both games; build/agent182/rc_sheet_3511.png shows f3522 "cls=0x52" / "cls=0x38"). The maintainer's read of them is quoted at build/agent182/maintainer_read_175.txt:7, "Yes, identical and this confirms the behavior is the same in both games so #175 can be clsoed".
Q3: OK — the only pokes that reach compared state are the level pin ff8116 and the RNG pin ff80d4 (tools/trap_air_probe.sh:41), which were ruled the equalised input and are named in the claim. The x pins end at 3489, before FROM=3500 (tests/audit_trap_air_hit.sh:66-67), and are also named. PICK=real drops the id pokes (tools/trap_air_probe.sh:40). Box resolution and the per-node charge are shared tools, and the claim names both.
Q4: VIOLATED — no control fires if a hurtbox difference on a charged frame comes from the merge corrupting Felicia's b:0x0f data rather than from vsavj's own data. `charged()` (tests/audit_trap_air_hit.sh:110-111) would charge that difference and freeze it per leg. The five controls (air-grounded, air-class, ours-drift, box-drift, dome-drift; build/agent182/tah_verify.log:17-21) cover only the airborne height, the air class, same-data fields, same-data hurtboxes and the dome's state. The claim does not name this failure mode.
Q5: OK — each attribution is quoted in build/agent182/maintainer_read_175.txt: the sheet read at :7 ("Yes, identical ..."), the level/RNG pin ruling at :10 ("agreed"), and the class-0x52 ruling at :12 ("then I'm all for fixing ..."). Line 13 of that file correctly labels the 0x38 marker "not a ruling — the implementation".
VERDICT: VIOLATED

Q1: VIOLATED — The claim relies on test_mister_sdram_census PASS and test_mister_gfxc_fetch PASS (build/rc170/freeze/rc_final/mister_m19_results.tsv:2,4). The runner flagged that same run's tree: build/rc170/freeze/rc_final/mister_m19_summary.txt:2-15 "DIRTIED by the run — a sweep that rewrites tracked artifacts invalidates its own evidence" (build/merged1/patch/patch.json, tests/expected/PROVENANCE.md, STATE_HISTORY.md, tests/expect/mister_prg_window.txt …). No artifact explains that list: tree_dirtied_attribution.txt:1 covers "battery pass 2" only. Neither gate is in any inventory: p2_tree_inventory.txt:3 covers "the pass-2 gates" and mister_inventory.txt:1 covers test_mister_prg_window only. So "those two PASSes still hold on the final tree" is untested, and the NOT-tested list does not name it. Also unnamed: the column-flash expectation moved from M18 to M19, with its writer frame going 2864 -> 2858 and one more row differing from native (flash_gate.m18.log:19-31). Unlike prg_window, the claim gives no "recorded as measured, not explained" note for it.
Q2: OK — The claim itself draws no new conclusion about how anything plays. The one move reading its rows carry (Pyron's 2623+PP, registry.diff:22) was ruled after "Capture put before the maintainer first (build/rc170/cap_pyr/Pyron_2623PP_after_fix_sheet.png …)" (rulings.txt:16).
Q3: OK — edits_check_tracks.py:29-33 takes its ops from tools/attribute_patch_delta.py, so it shares that tool's selection of which ops count as edits. That split is planted in tool_plants.txt:2-6 ("same … EDIT, as required", "inplace … EDIT", "moved … EDIT", "target … listed"). image_outside_ops.py reads both images without the tool. The claim itself names the self-frozen .sha1 and prg_window agreements as self-agreement.
Q4: VIOLATED — Failure mode with no control and not named: an M18 baseline image that does not hold its own patch's bytes inside a SAME op's span would hide an M18->M19 program change. That byte lies inside an op, so image_outside_ops passes. The M19 image matches its patch, so image_vs_patch passes. The attribution calls the op SAME because the patch hex is identical. image_vs_patch ran only on the five M19 dirs (image_vs_patch.txt:1-13), and its plant only on m3b_merged27 (image_vs_patch_plant.txt:1-4). Yet image_vs_patch.py:4-6 claims "a changed image byte cannot hide inside a SAME op's span", which also needs the baseline images checked.
Q5: OK — Each ruling is quoted in the maintainer's own selected answer or words: rulings.txt:10 "M19 supersedes it (Recommended)", :20 "Natural reading (Recommended)", :38 "S1: scoped row (Recommended)", :42-43 "I agree with all recommendations" (#111, #96), :61 "Freeze M19 now (Recommended)"; rulings_emu.txt:10 "Freeze, ticket it (Recommended)"; ci_emulator.tsv.lines-30-56.txt:16-20 "it should be applied ALWAYS on release but not for a freeze, UNLESS explicitly in scope".
VERDICT: VIOLATED

Q1: VIOLATED — The claim says SELF is "a poke of a DIFFERENT id (a changed-value write)", so "the poke mechanism is inert" is "tested directly on the same write the poked leg performs". The gate says the opposite: "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)" (build/rulecheck/2026-09-19-65/a/audit_forced_pick_fidelity.sh.txt:26-28). SELF's poke is `_pick` built from `$_id` = row_id of the row (lines 114, 135, 138), which is the same id the REAL leg must already hold (lines 172-179, 278). So whether a changed-value write at 1400-1500 perturbs state is untested, and the claim does not name it as untested. A lesser point: "so it is a first difference outside the pinned field" is not shown either, because the X pin is a single-frame poke at t-40 (name_moves.py.txt:687: `f"{t - 40}:ff8410:{x1:04x}"`), not a hold.
Q2: N-A — The claim draws no conclusion about how the character plays: "No behavioural conclusion about how the character plays is drawn here."
Q3: VIOLATED — POKED vs REAL passes: inside 1501-2362 the only shared write is the global level pin (audit_forced_pick_fidelity.sh.txt:133), and the P1 X pin at $FF8410 starts at 2560 (name_moves.py.txt:687) and is named in the claim. REAL vs SELF, the comparison offered as proof that "the poke mechanism is inert", fails. SELF writes $FF8782 (inside the compared P1 block, diff_region(0x400,0x800) at line 189) with the same `$_id` that the shared real cursor path already confirmed (lines 114, 135, 138, 174, "an id that already holds that value" at line 28). The two legs therefore agree by construction, whatever a changed-value write does.
Q4: VIOLATED — Uncovered failure mode: the changed-value poke (0x10/0x11 written over Donovan's 0x13 at 1400/1450/1500, lines 135 and 138) itself perturbs P1-block state. A byte is then "charged to the confirm" when the poke caused it. Neither control does a changed-value write. SELF is same-id (lines 26-28), and donovan-self pokes 0x13 over R,R, which "IS Donovan" (lines 48-51, 87-89). Both see a no-op write, which is not what the claim rests on. The claim does not name this gap; it says the opposite. The existing controls cover other failures: same-leg (lines 308-316) catches a vanished difference, wrong-cursor (lines 317-322) catches identity, and route residue is named as MEASURE-only.
Q5: N-A — No decision, approval, ruling or closure is attributed to any person; the packet is the working agent's own recommendation.
VERDICT: VIOLATED

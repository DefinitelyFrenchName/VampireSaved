Q1: VIOLATED — the claim says the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write) ... tested directly on the same write the poked leg performs", but build/rulecheck/2026-09-20-88/b/audit_forced_pick_fidelity.sh.txt:26-28 says "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)" (:135 pokes the row's own `$_id` onto the real path set at :118), so the premise that the poke's value-CHANGING write is inert is untested and unnamed.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."; the frozen columns are P1-block offsets and a first-difference frame only (build/rulecheck/2026-09-20-88/b/forced_pick_fidelity.tsv:13).
Q3: VIOLATED — both legs copy the rig's position pin (build/rulecheck/2026-09-20-88/b/name_moves.py.txt:687 "pin_pokes += [f\"{t - 40}:ff8410:{x1:04x}\", ...]") via `_base` (audit_forced_pick_fidelity.sh.txt:132), and $FF8410 is P1-block +0x010 inside the diffed region "p1 = diff_region(0x400, 0x800)" (:189), so the frozen in-play column — pyron "-", i.e. NO in-play P1-block difference (forced_pick_fidelity.tsv:16) — is an agreement over a field a shared poke writes on both legs; the claim's escape ("a first difference outside the pinned field") is not computed anywhere: first_play at :197-198 takes the min over ALL non-latched P1 offsets, +0x010 included.
Q4: VIOLATED — uncontrolled failure mode: the three bytes are an artifact of the poke's value-CHANGING write (0x13 → 0x10/0x11) rather than of the confirm. Both "poke mechanism" controls write a value that is already there — donovan-self "poking 0x13 over Donovan's own confirm" (:48-51) and REAL-vs-SELF "the poke mechanism itself is inert" (:281) with `_pick` = the row's own id on the real leg's own pick — so they see a no-change write, not the write the measured legs perform; the claim names this as covered, not as a gap.
Q5: N-A — the packet attributes no decision, approval, ruling or closure to any person; the only maintainer references are in the rig's schedule comments (name_moves.py.txt:392 "the maintainer's ruling 14z-121", :463).
VERDICT: VIOLATED

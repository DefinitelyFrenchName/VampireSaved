Q1: VIOLATED — `tests/test_release_binaries.sh:368` is the gate's ONLY `-verifyroms`, run `-rompath "$REPO/$MERGED/rompath;$ROMDIR"` (parent + BIOS zips present), and it is the run `build/release_binaries_14z173.log:4` reports, so "MAME -verifyroms reports zero NOT FOUND from that one zip" is an unnamed untested premise; likewise the FBNeo half — `tests/test_release_binaries.sh:335` stages every `$ROMDIR` zip AND the build rompath into the FBNeo play dir for a 20 s boot (:343), the only replay legs are MAME (`:443-444` `run_replay_mame.sh`), so "work RAM bit-identical on ... FBNeo and a bit-identical FBNeo framebuffer" rests on no artifact while the claim instead lists "any MAME framebuffer comparison" as NOT TESTED.
Q2: N-A — the conclusions drawn are bit-identity of machine streams, not how anything plays: "work RAM bit-identical ... and a bit-identical ... framebuffer" (`tests/test_release_binaries.sh:483-488` compares the RAM log and the framebuffer stream only); no move, timing, float, hold or hit conclusion is drawn.
Q3: VIOLATED — the §1 legs share the packager's single derivation: `tools/package_release.py:210` calls `standalone_completion(present)` once and writes from it both the `pristine_from` rows the applier copies (`:216`) and the `standalone_completion` list (`:219`, `:259`) plus `applied_set_key` over the already-completed contents (`:217`, `:258`); the checker then reads that same declaration as its ground truth (`tests/test_release_roundtrip.sh:155-157`, `:169` `ref = zipfile.ZipFile(os.path.join(romdir, declared[e]))`) and `:173` compares the applier's additions to the applier's own declaration (`sorted(extra) != sorted(declared)`), so a defect in that one derivation — an omitted parent-resident member, or a wrong source zip — writes both sides and they AGREE; the rot-catcher the packet names for exactly this (`docs/project/release_format.md:93-95` "§2b fails on the emulator's own `-verifyroms`") does not exist: §2b runs no `-verifyroms` and the gate's one invocation has `$ROMDIR` in its rompath (`tests/test_release_binaries.sh:368`).
Q4: VIOLATED — the control present is `standalone-member-content` (`tests/test_release_binaries.sh:19`, `:475` swaps vm3.16m's bytes into vm3.14m), which fired on the MAME framebuffer (`build/release_binaries_14z173.log:6`) and covers a GFX completion member carrying wrong content. Neither controlled nor named: (a) the shipped set being incomplete/wrong for FBNeo — no FBNeo leg ever runs the standalone set (`:335` stages all dumps beside it, `:343` 20 s boot) and FBNeo asserts no framebuffer anywhere, while FBNeo is precisely the loader whose CRC-then-0xFF-fill runs 12,120 frames "with work RAM BIT-IDENTICAL" on missing members (`docs/platform/gotchas.md`, `build/standalone_14z173.diff:129-137`); (b) the audio path — 3 of the 7 completion members are `vm3.11m`/`vm3.12m` (QSound samples) and `dl-1425.bin` (QSound BIOS), invisible to both compared streams (RAM log + framebuffer, `:483-488`), and the one content control perturbs a gfx member, i.e. it proves the instrument sees tiles, not samples. The claim's NOT TESTED list names neither FBNeo nor sound.
Q5: OK — the maintainer's own words are quoted verbatim for the route ruling: `build/standalone_14z173.diff:68` `+> *"C"*`, and for the order/scope ruling at `:38-41` ("Agreed with one caveat: let's focus on MacOS first...").
VERDICT: VIOLATED

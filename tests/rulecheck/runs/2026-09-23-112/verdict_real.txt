Q1: VIOLATED — Some premises in the records came from neither the instruments the claim names nor its NOT-tested list. (1) docs/project/agent_architecture_scope.md:165 says "The BACKGROUND hand-back form rests on 14z-176's transcript alone, not on a controlled leg". (2) docs/project/agent_architecture_scope.md:162 cites an archive figure, "Opus 5 under every archived Fable parent", and no named tool produced it. (3) STATE.md:83 reports a "LIVE" install check ("a `fable` call and a no-model `general-purpose` call REFUSED") that was observed by hand, not by a gate. (4) The records state their basis as "Claude Code 2.1.280" (docs/platform/gotchas.md:2804), but the attached log reads "2.1.281 (Claude Code)" (build/agent172/probe_agents_14z177.txt:1).
Q2: N-A — The records draw no conclusion about how anything plays; they cover agent definitions, hooks and transcripts only.
Q3: OK — No shared thing is shown making the compared legs agree. Each "cannot" leg is paired with a "can" leg read by the same parser, and that parser tells the two outcomes apart (tools/agent/probe_agents.sh:149, "low under low, xhigh under xhigh"; tools/agent/probe_agents.sh:288, Explore against general-purpose under the same Fable parent).
Q4: VIOLATED — docs/platform/gotchas.md:2846 states as fact that "a background worker's final report reaches the orchestrator's transcript as an `<\agent-message from="<id>">[Subagent hand-back]` record … the completion `<\task-notification>` explicitly NOT repeating it". A10, the only leg for this, exercised the foreground form only (build/agent172/probe_agents_14z177.txt:11, "(1:11111:tool-result)"). No control would fire if the background form were stated wrongly, and the claim does not name this gap.
Q5: VIOLATED — DECISIONS_HISTORY.md:38-40 says a headless session "very likely loads CLAUDE.md, which the 2026-09-23 ruling forbids the checker", and docs/NEXT_SESSION.md:31-32 says "the ruling says the checker never reads it". No maintainer words saying that are quoted anywhere in the packet. The only verbatim checker ask is "only knows the rules and ways of working, no project-specific context" (docs/project/agent_architecture_scope.md:34). In the same way, DECISIONS_HISTORY.md:45-46 turns the label *"Subagents (Recommended)"* into "and first measures whether a reader sees CLAUDE.md", with no quote showing the maintainer ruled that step.
VERDICT: VIOLATED

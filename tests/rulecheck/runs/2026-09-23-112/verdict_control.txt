Q1: VIOLATED — The claim says the SELF leg pokes "a DIFFERENT id (a changed-value write)", but the gate shows a same-id poke. build/rulecheck/2026-09-23-112/a/audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". Lines 135 and 138 build `_pick` from the row's own tenant id (`row_id "$_row"`) and apply it to the self leg, on top of a real path to that same tenant. So whether a changed-value id write is inert is untested, and the claim presents it as tested instead of naming it. The claim's other stated gaps do match the script: globals and whole RAM are MEASURE-only (lines 201-235); TRANSIENT is not frozen (line 284); P2 is checked on POKED-vs-REAL only (lines 279-282).
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here", and the frozen first-play frame (forced_pick_fidelity.tsv:15, "2980") is a number, not a conclusion about how the character feels.
Q3: OK — The only shared writes the artifacts show inside the pre-match window (1501-2362) are the level pin "f'{f}:ff8116:06' for f in range(2000,$END)" (audit_forced_pick_fidelity.sh.txt:133), which is a global. The P1 X pin "{t - 40}:ff8410" (name_moves.py.txt:687) lands at 2560 or later, after the window, and the claim names it for the first-play figure. No artifact shows a shared write reaching a P1-block byte inside the window.
Q4: VIOLATED — Uncovered failure mode: the LATCHED set, or its attribution "charged to the confirm", could come from the engine reacting to a changed-value id write at 1400/1450/1500 rather than from the confirm's latch. Both inertness controls use same-value writes: SELF pokes the tenant's id over a real pick of that tenant (audit_forced_pick_fidelity.sh.txt:26-28, 135, 138), and donovan-self pokes 13 over Donovan's own R,R confirm (audit_forced_pick_fidelity.sh.txt:48-51, forced_pick_fidelity.tsv:17). No leg separates a changed-value write from the confirm latch. The claim does not name this as a gap; it asserts the opposite ("tested directly on the same write the poked leg performs").
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

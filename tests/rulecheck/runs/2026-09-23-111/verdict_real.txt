Q1: VIOLATED — The claim rests on three things it does not list as untested. (1) The "83 of them calls that ran within the cap" figure: no artifact produces it. INSTALL.md:13 says "read from their own transcripts", but prove.py:103-104 counts only (verdict, type, model parameter), and prove_14z177.txt:26-35 has no column for the model a call actually ran on. The same holds for "56 of them on Fable 5.1" in INSTALL.md:12. (2) The hook allows any defined worker called with no model, whatever its definition says (pre_agent.py:83-92). A9 (probe_agents.sh:200-205) shows a definition with no model line runs on the caller's model. So the cap on defined workers depends on "tests/test_agent_defs.sh" (pre_agent.py:11), which is not in the packet and is not named as a premise. (3) INSTALL.md:23-24 says to replace ".claude/settings.json with settings_snippet.json (the only change: a new PreToolUse entry...)", but the current settings.json is not in the packet, so "the only change" is unshown.
Q2: N-A — The claim concerns an agent-call hook, not behaviour a player could feel.
Q3: OK — The only paired legs are the must-deny and must-allow cases (prove.py:31-54) and A11's Explore/general-purpose pair (probe_agents.sh:273-283). No shared rig is shown that would make them agree: a defect in run_hook or workers() would make one leg of each pair fail, not pass both ("ok = bool(ex) and all(cap.match(m) ...) and 'fable' in gp").
Q4: VIOLATED — Install step 2 overwrites the whole settings file ("replace `.claude/settings.json` with `settings_snippet.json`", INSTALL.md:23). The failure mode is that any existing settings entry missing from settings_snippet.json is silently dropped. Nothing controls for that (no diff against the current settings.json anywhere in the packet), and the claim does not name it. The 83-within-cap figure likewise has no instrument and no control in prove.py.
Q5: VIOLATED — pre_agent.py:7 says "workers are at most Opus-class — the maintainer's #172 ask", and pre_agent.py:33 says "(the 2026-09-23 ruling: a block binds the task at fault, never the session)". Neither statement is quoted from the maintainer in any artifact. The verbatim rulings in DECISIONS_HISTORY.md.lines-30-62:18-30 cover only forks, the pinned checker, QP3/QP5 and the order, and say nothing of an Opus-class cap or of a block binding the task.
VERDICT: VIOLATED

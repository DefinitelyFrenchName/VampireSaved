Q1: VIOLATED — the claim says the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs"; the artifacts show a SAME-value write: audit_forced_pick_fidelity.sh.txt:26-28 "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)", and mkleg pokes `row_id "$_row"` (the tenant's own id, line 114/135) over `row_path` (the tenant's real route, line 118) for the self leg (line 138). A changed-value id write over a real pick is therefore an untested premise the claim presents as tested, so the named untested premises are not the only ones.
Q2: N-A — claim: "No behavioural conclusion about how the character plays is drawn here"; the frozen 2980 (forced_pick_fidelity.tsv:15) is a RAM first-difference frame, no play conclusion is stated.
Q3: OK — the pokes both legs share land outside the P1 block within the pre-match window 1501-2362: $FF8116 from 2000 and $FF80D4 from 2363 (audit_forced_pick_fidelity.sh.txt:133-134, globals), $FF8850 from 2550 (name_moves.py.txt:699, P2 block), $FF8410/$FF8810 from 2560 at the earliest (name_moves.py.txt:684,687); identity is asserted per leg, not shared (audit_forced_pick_fidelity.sh.txt:170-179: $FF8782 at 1600 on both legs, $FF8403 at 1290 on REAL), so a shared input-path or poke-path defect that made both legs run the same character would fail identity rather than make them agree.
Q4: VIOLATED — controls carried: same-leg (lines 310-316, an empty set against the frozen non-empty set), wrong-cursor (lines 318-322, one move short fails identity), P2 block empty pre-match (lines 279-280), REAL vs SELF (lines 281-282). Neither controlled nor named: the changed-value id write at 1400-1500 (0x13 -> 0x10/0x11, the write only the POKED leg performs, lines 117/135/138) itself producing a LATCHED byte that the gate then charges to the confirm — the two poke-mechanism controls are same-value writes (SELF: line 135 over line 118; donovan-self: line 89 "R R" + line 87 id 13, line 49 "R,R IS Donovan on vsav2"), and the claim names the opposite rather than the gap.
Q5: N-A — no decision, approval, ruling or closure is attributed to the maintainer or any person in the packet.
VERDICT: VIOLATED

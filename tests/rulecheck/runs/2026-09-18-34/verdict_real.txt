Q1: VIOLATED — the claim presents the forced-pick premise as cleared for Demitri by measurement ("the confirm-latch defect of a forced pick is measured to matter only for Phobos's and Donovan's cells, tests/audit_forced_pick_fidelity.sh, not for Demitri"), but tests/audit_forced_pick_fidelity.sh.lines-1-60 line 53 measures only `ROWS="huitzil pyron donovan-self"` and line 54 says "native vsav2 only (no build)": no Demitri row exists and nothing was measured on pristine vsavj, while tests/replays/judge/02_throw.rpl lines 10-13 confirm P1 with no cursor move before the 1400 id poke (tests/audit_throw_registration.sh line 138) on both games — an untested premise stated as tested; the other named gaps (one throw/one character/one rig, the collision-PC exclusion, run-to-run on one host) are consistent with the artifacts.
Q2: N-A — the conclusion drawn is a mechanism attribution (which engine site writes the pair and which fighter the stager pays, build/meter_probe_14z166/legacy_throw/vsavj_pair.txt lines 3295-3296 and vsavj_m1.txt line 743 / vsavj_m2.txt line 743), not a claim about how the throw plays or feels; the tenant rows' player-facing meaning was frozen in the earlier runs the packet says are "not re-argued here".
Q3: OK — the vsavj and native legs share tests/replays/judge/02_throw.rpl, the id/speed/RNG pokes (tests/audit_throw_registration.sh line 138), tests/lua/read_tap.lua and the reducer (lines 92-127), but no artifact shows any of these writing or selecting the compared state: the meter steps are written by each engine's own adder (vsavj_m1.txt line 743 "W 3062 PC 029ab0", vsav2_m1.txt line 741 "W 3061 PC 028de2") and the pair by each engine's own throw site (vsavj_pair.txt line 3295 "PC 029694", vsav2_pair.txt line 3287 "PC 0289c6"); the pokes touch $FF8782/$FF8B82/$FF8116/$FF80D4 only, none of the tapped addresses.
Q4: OK — neither must-fire control exercises the legacy part (tests/audit_throw_registration.sh line 174 swaps only `[ "$leg" = ours ]`; line 217 plants only `$first.ours.dead.txt`; build/gates_14z166/throw_reg_ctl_swapped2.log lines 67-68 and throw_reg_ctl_planted2.log lines 154-155 read "legacy_demitri vsavj: as frozen" / "native: as frozen" under both perturbations), and the claim itself names this ("they perturb ours' rows and ours' dead-pair tap, which the legacy part has neither of"); a dead legacy tap is caught in-gate by the VOID rule (line 104) and the empty-tap FAIL (line 165), and both legacy taps carry END lines (vsavj_pair.txt line 5215, vsav2_pair.txt line 5216).
Q5: VIOLATED — the packet attributes the request for this control to the maintainer ("the LEGACY CONTROL the maintainer asked for") and the artifacts repeat it as tests/audit_throw_registration.sh line 25 "THE LEGACY CONTROL (maintainer-asked, 2026-09-18)", build/meter_probe_14z166/legacy_throw.sh line 2 "THE SEPARATING CONTROL (maintainer, 2026-09-18)" and docs/game/engine_internals.md.lines-3040-3130 line 68 "THE LEGACY CONTROL (the maintainer's, 2026-09-18)", but no artifact quotes the maintainer's own words; every instance is the working agent's paraphrase.
VERDICT: VIOLATED

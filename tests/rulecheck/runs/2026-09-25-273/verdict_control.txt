Q1: VIOLATED — The claim says "Not tested: nothing further", but the gate lists what it leaves out: build/rulecheck/2026-09-25-273/b/audit_move_parity.sh.txt:34-36 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters ... P2 is Victor ... his state is never compared". The comparison also covers only the 12 FIELDS at :105, and only part 1 of Huitzil's 10 parts (name_moves.py.txt:447-610), yet the decision rebuilds five tracks. None of the named artifacts shows the claimed consumer enumeration ("every consumer of the byte ... pristine vsavj: zero"). No artifact shows a $FF87C2 poke either: pokes_for (audit_move_parity.sh.txt:86-103) has none, and gen (name_moves.py.txt:670-681) has none. No artifact records "6851/6851"; the gate emits a per-part verdict and first divergent event, not a frame count (:172-179), and the frozen row is build/rulecheck/2026-09-25-273/b/move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f".
Q2: VIOLATED — The conclusion covers feelable behaviour. Part 1 is jumps, normals, Air Dash and Float (name_moves.py.txt:448-460), and the frozen divergence is "Jump [8]+21f" (move_parity.tsv:20). The only evidence is a count, "6851/6851 frames identical", and no image or recording in the artifacts was put before the maintainer.
Q3: VIOLATED — Both legs load the same rig pokes (_base): audit_move_parity.sh.txt:96-101 "printf '%s;%s;%s' "$_base" "$_lvl" "$_rng"" on the native leg and "$_pick" "$_base" "$_lvl" "$_rng" on ours. Those pokes write compared fields. For every "far" event in Huitzil part 1 (name_moves.py.txt:448), name_moves.py.txt:658 writes P1 x on both legs: "pin_pokes += [f"{t - 40}:ff8410:{x1:04x}", ...]". That same address is compared: audit_move_parity.sh.txt:105 "ff8410:w:x". Also, :89 pins ff8116 to 06 on both legs, and :105 compares it as "ff8116:b:lvl". The tenant id ff8782 is poked on both legs (name_moves.py.txt:672; audit_move_parity.sh.txt:92,101) and compared as "ff8782:b:id". A defect in these shared pokes would make x, level and id agree whatever the flavor byte does.
Q4: VIOLATED — Neither declared control addresses the flavor latch (audit_move_parity.sh.txt:4-5, "unpinned-level" and "no-translation"). The controls run on the first part of the set (:182). For huitzil_1 the frozen verdict is DIVERGES (move_parity.tsv:20), so the no-translation control ends "CONTROL DEAD ... not frozen IDENTICAL, so the control cannot discriminate" (:195). The unpinned-level control prints "CONTROL FIRED" whenever the frozen verdict is not IDENTICAL (:203-207), so it proves nothing here. Two failure modes are neither controlled nor named by the claim. First, poking $FF87C2 on a running leg may not match building flavor_default=0x01 (for example, a consumer that reads the byte before the poke frame). Second, the latch's effect may lie outside the 12 compared fields or outside part 1. No control has the poke withheld on ours, and none checks the rebuilt ROM without the poke.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person. "his part-1 rig" refers to the character Phobos.
VERDICT: VIOLATED

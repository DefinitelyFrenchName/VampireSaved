Q1: VIOLATED — The claim says "Not tested: nothing further", but the artifacts show untested premises. audit_move_parity.sh.txt:34 says "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters" and :35-36 says P2's state "is never compared". Only part 1 of Huitzil's ten parts is claimed (name_moves.py.txt:447-551). The "every consumer ... enumerated" premise appears in no artifact. The gate also has no path that pokes $FF87C2: pokes_for at audit_move_parity.sh.txt:86-103 builds only the level, RNG, pick and base pokes. It emits a part verdict ("IDENTICAL"/"DIVERGES"), not a frame count, so "6851/6851" is not an output this gate produces. And move_parity.tsv:20 is frozen "huitzil_1	DIVERGES	Jump [8]+21f".
Q2: VIOLATED — The claim concludes that Phobos's part-1 behaviour (movement, jumps, Float, Air Dash, normals, per name_moves.py.txt:448-460) plays "frame-identical to native". The divergence it resolves is a jump ("Jump [8]+21f", move_parity.tsv:20). No image or recording appears anywhere in the packet; only the number "6851/6851" is given.
Q3: VIOLATED — Both legs apply the same schedule pokes (audit_move_parity.sh.txt:88 `_base` from the rig json, used on the native leg at :98 and on ours at :101). Those pokes write compared fields. name_moves.py.txt:658 writes P1 X "ff8410" 40 frames before every "far" event, and all of Huitzil part 1 is "far" (:448); ff8410 is compared as "ff8410:w:x" (audit_move_parity.sh.txt:105). The level pin "ff8116:06" is written on both legs every frame (:89, :98, :101) and compared as "ff8116:b:lvl" (:105). The tenant id is also forced by the pokes: ours gets "ff8782" (:92/:101) and native gets it from the rig's own pokes (name_moves.py.txt:672), and it is compared as "ff8782:b:id".
Q4: VIOLATED — The gate's only controls are "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:4-5, :185-208). Neither would fire if the $FF87C2=0x01 poke failed to reach ours or was never read. ff87c2 is not in FIELDS (:105), and the gate has no way to inject it (:86-103). Neither control covers latch consumers that part 1 does not exercise. The claim does not name either failure mode. With huitzil_1 as the control part, its frozen verdict is DIVERGES, so the no-translation control reports "CONTROL DEAD ... is not frozen IDENTICAL, so the control cannot discriminate" (:195) and the gate fails.
Q5: N-A — The packet attributes no decision, approval or ruling to the maintainer or any person; "flavor_default becomes 0x01" is the working agent's own action.
VERDICT: VIOLATED

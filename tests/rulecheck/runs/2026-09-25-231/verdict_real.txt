Q1: OK — The claim names its untested premises: "Every connect and whiff in this packet is a drop of the victim's HP in the trace (the premise the counts rest on)" and the NOT-tested list (the unchanged sets, SA's HP knife edge, offsets outside +10..+34, SA 5LP's strip not independent of the trace, j.LK +24 not captured, the fourteen other moved legs resting on the HP-drop count alone). The artifacts show nothing else untested: build/agent183/p134_evidence/separations.txt:28-48 measures every near set at f2800 (43-60 px) plus the f2600 control; lk_sweep.txt:1-26 covers all 25 offsets; outoftree_diff.txt:4-5 shows the current bodies MATCH the frozen hashes.
Q2: OK — The captures were put before the maintainer, and the maintainer's reads are quoted in build/agent183/p134_evidence/maintainer_read.txt. For the strip (line 8): "yes and this is as expected". For the dense sheet, which has no trace values (lines 15-19): "yes, but to be precise she is too high to hit and the jump does not result in a cross-up". p134_captures_dense.png carries only frame labels. The +24 press, which was not captured, is stated as trace-only.
Q3: OK — The old and new tables share the rig and the HP-drop reader (diff.patch:311, the HP pin at event-20, which is unchanged). But the moved rows disagree, so the shared rig does not make them agree. The dense capture is a video read independent of the trace (maintainer_read.txt:11-13), and capture_identity.txt:6-7 only ties the strip's +14 frame to the dense run's (one sha1). No artifact shows a shared premise producing agreement in a compared state.
Q4: OK — The f2600 control leg reproduces SA's frozen near rows: separations.txt:43 "SA_near_2600 seps [134, 60, 59, 59, 59, 60] hits [0, 1, 1, 1, 1, 1] chains ['a2:0x01', ...]". That control covers the mis-timed first event. The rehit ring's schedule-read window fires its JE control: build/agent183/p134g/rehit_ring_ctl_clear-on-attack-node.log:29-30 "a clear fell on a non-last attack-node sample (1)" / "FAIL JE". The chain slot a2:0x15 on the +24 row (outoftree_diff.txt:24) guards against a landing move being counted as the j.LK. The gates' own must-fire controls fired too (join_verify.log:13, meter_verify24.log:19, crosscheck_freeze.log:12). Every remaining failure mode (the HP-drop premise, the knife edge, the edges of the offset band, the unchanged sets) is named in the claim.
Q5: OK — The attributed reads are quoted verbatim in build/agent183/p134_evidence/maintainer_read.txt:3,8,15,17,19, and the #134 ruling is quoted in DECISIONS_HISTORY.md.lines-30-36:3: "Fix, and chase j.LK too" — "As recommended, plus a per-move timing or pin so Lei-Lei's j.LK connects and her cell gets measured."
VERDICT: OK

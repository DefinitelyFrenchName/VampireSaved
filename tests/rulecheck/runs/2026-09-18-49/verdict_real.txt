Q1: VIOLATED — The claim presents two things as measured that no named artifact shows. First, "(y reads 120 on both legs)": the reducer keeps only the node and x of the sword (tests/audit_df_moves.sh:316 "obj[int(m.group(1))] = (int(m.group(4), 16), int(m.group(2)))"), and no row or log records y. Second, the HUD and the banner "use palettes that differ between the games": only a comment carries this (tests/audit_df_moves.sh:321-322 "the HUD palettes do differ"); no form row or log prints any palette seen differing (tests/expected/df_moves.tsv:36-39). The claim also leaves unnamed the premise that each P-dump line is the OBJ palettes indexed by the attribute's pal number (tests/audit_df_moves.sh:313, 325 "p * 32: p * 32 + 32").
Q2: OK — Each conclusion a player could feel had a capture put before the maintainer first: build/p136_14z168/col_full/DELIVERED.txt:1 ("BEFORE the reply quoted"), build/p136_14z168/cap_guard/DELIVERED.txt:1 ("13:11:51Z ... BEFORE the block re-entry was named anything a player feels"), build/p136_14z168/cap_dfx_pyron/DELIVERED.txt:1 and build/p136_14z168/don_sword/DELIVERED.txt:1. The census is static table content, not a behaviour conclusion.
Q3: OK — The legs share the pokes the gate names (tests/audit_df_moves.sh:41 "the move inputs, the far X pins, the stock re-poke, the level ($FF8116) and RNG ($FF80D4) pins"; the stock re-poke at line 110 "ff8509:09"). No artifact shows any of them writing a compared field: P1's seq at ff8406, +0x5C, the palette RAM or the rows of the data view.
Q4: VIOLATED — form-moved rewrites the reducer's output (tests/audit_df_moves.sh:355 "$5=\"differing_pieces=17\"") after the palette comparison (line 325). It therefore cannot fire on a blind palette read: a wrong region or layout would also give "differing_pieces=0" on every frame, exactly as frozen (tests/expected/df_moves.tsv:36-39). No row checks that a palette known to differ is seen differing, as guard_reentry does (tests/audit_guard_reentry.sh:164 "it is blind"). The sword has the same gap: no check that seed 1234 changes any state driven by the RNG (tests/audit_df_moves.sh:332-336), so a pin that never reaches the RNG would still read "seed_independent=yes", and no must-fire control covers the sword rows. The claim names neither failure mode.
Q5: OK — The maintainer's own words are quoted: DECISIONS_HISTORY.md.lines-30-63:11 ("we should take the vs2 rows"), :24 ("then I'm all for fixing"), :30-33 ("agreed, all the tests converge : it's identical", "so that too is confirmed identical", "My conclusion then is that it is identical"). The gates repeat them (tests/audit_column_shock.sh:27-28, tests/audit_guard_reentry.sh:37-38 and 167-168, tests/audit_df_moves.sh:15-18 and 37-38, tests/test_defense_rows_census.sh:7-9).
VERDICT: VIOLATED

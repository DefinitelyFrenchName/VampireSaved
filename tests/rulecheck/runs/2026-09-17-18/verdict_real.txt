Q1: OK — the claim's named gaps match what the gate leaves unasserted: build/rulecheck/2026-09-17-18/a/audit_forced_pick_fidelity.sh.txt:258 (only `-ge 30` dumps; :166 VOIDs on unequal counts, so a dump missing on both legs shrinks `pre` at :180 silently), :236/:284 (only latched offsets and first-play frame frozen; transient count, globals :189/:208-210 and the REAL-vs-SELF P2 field :272/:277 never asserted), :90-94/:285-289 (REAL-vs-ALT under MEASURE only), :197-198 (first_play excludes only latched offsets), name_moves.py.txt:686-688/:698/:647 (far pins at t-40 from 2600 = 2560, HP pin from 2550, part 1 in no METER_PARTS); no further untested premise is visible in the artifacts.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here"; the frozen columns are offsets and a frame only (build/rulecheck/2026-09-17-18/a/forced_pick_fidelity.tsv:13-17).
Q3: OK — the only shared write reaching the compared P1 block is the position pin at $FF8410 from frame 2560 (name_moves.py.txt:688 with FIRST_EVENT=2600 at :77), after the last pre-match sample 2362 (audit_forced_pick_fidelity.sh.txt:99, :180); the id poke into the block is emitted for poked/self legs only (:136-139) and the gate's own pins are globals (:133-134).
Q4: OK — wrong-cursor (audit_forced_pick_fidelity.sh.txt:253, :318-322) covers the REAL leg confirming another character; identity on every leg (:169-179, :278); donovan-self and REAL-vs-SELF (:281-282, forced_pick_fidelity.tsv:17) cover the poke itself creating the difference; P2 block empty pre-match (:279-280) covers instrument noise; same-leg (:310-316) covers a vanished set passing; the remaining failure modes (shared-missing dumps, route residue, transient, same-value inertness, first-play offset) are named by the claim.
Q5: N-A — the packet attributes no decision, approval, ruling or closure to any person; "report #151 step 1 as done" is the working agent's own recommendation.
VERDICT: OK

Q1: VIOLATED — (B)'s stated basis omits two exclusions the tool makes: `auto` rows are "reported, never counted as a data difference on its own" (tools/audit_same_data_p2.py:17-19, verdict at :167 `same = not (values_diff or anim_diff) and defense_same`) yet differ for 10 of 12 characters (build/move_parity_census_14z164/same_data_p2.txt:4-15 "auto-rows-differing:2/1/3/0/0/5/5/2/2/1/5/5"), and every other `data_ptr` row is skipped outright (:119-120 `if kind == "data_ptr": continue`) — the claim names only the 21 code_ptr rows, the hit id and the sprite pointer; (A)'s list says the shared pokes yield only "agreement on a pinned frame by construction", but the artifacts show a shared pin DECIDING a frozen DIFF (tests/expected/move_parity_events.tsv:501 "pyron_4 6 Cosmo Disruption [KK held] DIFF +50 p2hp", first frame 4100+50=4150 = tests/replays/naming/pyron_4.json:101 "4150:ff8850:01200120", with p2_state_classify.txt:233 reading "p2hp@+0" raw). Minor: tests/audit_move_parity.sh:19 still says "each of the 461 events" and tests/expected/PROVENANCE.md line 77 "MAME, 54 legs" while build/gates_14z164/move_parity_freeze2.log:1-5 shows "30 parts x 2 ... 60 legs ran ... 506 events".
Q2: N-A — the claim's last sentence "No behavioural conclusion about how any move plays is drawn by either freeze"; "retuned head hurtbox" is a box-tuple reading, build/move_parity_census_14z164/same_data_detail.txt:35 "('vuln', ((0, 77, 17, 11), ...) -> ('vuln', ((21, 88, 27, 16), ...)".
Q3: VIOLATED — both legs receive the rig's own pokes (tests/audit_move_parity.sh:127 `_base=... json.load(...)['pokes']`, emitted for native at :135 and ours at :138) and those pokes write compared fields: tools/name_moves.py.lines-640-720 line 48 `ff8410` (x), line 60 `ff8850` (p2hp, every 400 frames), line 71 `ff8509` (stock), all in the compared set at tests/audit_move_parity.sh:156 `FIELDS="...ff8509:b:stock,ff8410:w:x,...ff8850:w:p2hp"` and tools/move_parity.py:59-60; the shared HP pin is what sets the verdict of tests/expected/move_parity_events.tsv:501 (first differing frame 4150 = the pin frame, pyron_4.json:101).
Q4: VIOLATED — uncontrolled and unnamed failure mode: a cumulative field is compared as its change from the window's first live frame (tools/move_parity.py:141 `n[f][key] - n[f_first][key] != o[f][key] - o[f_first][key]`), so a carried p2hp offset is invisible until the shared periodic pin resets both legs to 0x0120 mid-window, at which frame the deltas differ and a DIFF is frozen against THAT event — tests/expected/move_parity_events.tsv:501 "pyron_4 6 Cosmo Disruption [KK held] DIFF +50 p2hp" lands exactly on tests/replays/naming/pyron_4.json:101 "4150:ff8850:01200120" while build/move_parity_census_14z164/p2_state_classify.txt:233 shows Victor's HP already differed at "+0" raw; the claim names only "agreement on a pinned frame is by construction", the opposite direction, and neither of the two controls (tests/audit_move_parity.sh:249-270, translation and level) touches the pin schedule.
Q5: OK — DECISIONS_HISTORY.md.lines-30-52 line 4 quotes the maintainer's own words: *"I agree with the 3 propositions"* (maintainer, 2026-09-17), for propositions 1 and 2 at lines 8-17.
VERDICT: VIOLATED

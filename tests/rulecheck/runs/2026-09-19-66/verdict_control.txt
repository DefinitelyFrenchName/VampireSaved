Q1: VIOLATED — the claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the artifacts show several untested premises. audit_move_parity.sh.txt:25 says "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor". :34 says "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters". :105 compares only 12 fields, and ff87c2 is not one of them, so this gate never reads the latch that the claim says is 0x01 on native and 0x00 on ours. :86-103 has no way to poke ff87c2, so the "poked to 0x01" run is not this gate. The comparator's exclusions live in tools/move_parity.py (:13-14), which is not in the packet. Only part 1 of Phobos's ten parts was run (move_parity.tsv:20-29 freezes huitzil_1..10 all DIVERGES). The figures "6851/6851" and "every consumer enumerated" appear in no artifact. Two premises go unnamed: that a runtime RAM poke equals building flavor_default=0x01, and that the five rebuilt tracks are correct.
Q2: VIOLATED — the conclusion is that Phobos's part 1 now plays the same as native: jumps, "Float [j.8 hold]", "Air Dash [j.66] early" and the normals (name_moves.py.txt:448-459). The frozen verdict for that part is "huitzil_1	DIVERGES	Jump [8]+21f" (move_parity.tsv:20). The packet gives only a frame count. No image or recording was put before the maintainer.
Q3: VIOLATED — both legs apply the same rig's pokes (audit_move_parity.sh.txt:88 `_base` from the shared json, used at :98 and :101), and those pokes write fields that are compared. name_moves.py.txt:658 "pin_pokes += [f\"{t - 40}:ff8410:{x1:04x}\"" re-pins x before every event, and every huitzil part-1 event is "far" (:448). ff8410 is the compared x (audit:105 "ff8410:w:x"). ff8782, the compared id, is poked on both legs (name_moves.py.txt:672; audit:92, :101). ff8116, the compared lvl, is poked on both legs (audit:89, :98, :101). So the shared pokes force agreement in those fields and in the state that follows each re-pin; that agreement is not measured.
Q4: VIOLATED — the gate has only two controls, unpinned-level and no-translation (audit_move_parity.sh.txt:4-5). Both run on the FIRST part of SET (:182; the default is donovan_1, :79). The no-translation control reports DEAD on any part not frozen IDENTICAL (:195), and huitzil_1 is frozen DIVERGES (move_parity.tsv:20). No control fires, and the claim names none of these failure modes: the ff87c2 RAM poke may not match a build with flavor_default 0x01, since no leg runs a rebuilt build (pokes_for :86-103). The flavor change may perturb Phobos parts 2-10, which were not run. It may also change fields the gate does not measure (damage, hitboxes, projectiles, :34).
Q5: N-A — the packet does not attribute any decision to the maintainer or to anyone else. "flavor_default becomes 0x01" is the working agent's own sentence.
VERDICT: VIOLATED

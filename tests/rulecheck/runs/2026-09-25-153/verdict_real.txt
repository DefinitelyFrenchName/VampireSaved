Q1: VIOLATED — The claim says "the rig's SCHEDULE writes nothing inside any compared window". The sword form rows are a compared window too: they are frozen, and the gate reads them over "range($T5-115,$T5+46)" (tests/audit_df_moves.sh:455), with the idle hold at T5-114..T5-15 and the flight x at T5..T5+45 (tests/audit_df_moves.sh:532, 539). Every event's own x pins are written at t-40, on both fighters and on both legs: "pin_pokes += [f\"{t - 40}:ff8410:{x1:04x}\", f\"{t - 40}:ff8810:{x2:04x}\"]" (tools/name_moves.py:754). So the 5HP's pins at T5-40 land inside that window, and the sprite runs carry them ("base=...json...['pokes']", tests/audit_df_moves.sh:454). The reader's refusal covers only the ev windows (tests/audit_df_moves.sh:255-261). The claim neither names nor tests this shared write inside the sword window, whose content changed at this re-freeze ("first_px_diff=3349" -> "first_px_diff=None", build/agent181/df_moves_attribution_run10.txt:64-65).
Q2: OK — The four contact events and 2HP were re-captured on both legs (build/agent181/cap_dfx2d_list.txt:1-69; the sheets show native on top and ours below, e.g. build/agent181/cap_dfx2d/sheet_donovan_dfx1_ev25.png, which shows "2 HIT" at +18 and "3 HIT" at +30 on both legs). They were delivered before the freeze: "delivered (third send, same sitting, FINAL rig" (build/agent181/captures_sent_14z181.txt:15). "the maintainer's read: NOT YET GIVEN" (build/agent181/captures_sent_14z181.txt:17) matches the claim.
Q3: VIOLATED — Both legs share a write on every frame of every compared window: "f'{f}:ff8116:06' for f in range(2000,$fr)" and "f'{f}:ff80d4:0000' for f in range(2363,$fr)" (tests/audit_df_moves.sh:207). The RNG selects the compared hits: "picks P1-first or P2-first on every pass from bit 0 of the engine RNG ... a fifth hit landed" (DECISIONS_HISTORY.md.lines-1218-1256:22-27). The legs consume the RNG differently in a window ("native=630" / "ours=621", build/agent181/df_moves_contact_run10.log:40-41), and the per-frame reset erases that difference on both legs, so a port defect there would read as agreement. The new control only confirms that this shared write reaches the rows ("reads 3 row(s) differently, so the pins are load-bearing", build/agent181/df_moves_contact_run10.log:12). It does not show that the agreement between ours and native is not produced by the write.
Q4: VIOLATED — The claim "two ablations restoring the old HP pin ... change nothing, so the HP pin is not the cause" has no control showing that the probe pin reached the runs. FORM_EXTRA_POKES is appended silently ("${FORM_EXTRA_POKES:+;$FORM_EXTRA_POKES}", tests/audit_df_moves.sh:457, 474), and neither ablation log records it. Both logs match run 10 row for row: build/agent181/df_moves_ablation_run.log:37-41 and build/agent181/df_moves_ablation2_run.log:37-41 equal build/agent181/df_moves_contact_run10.log:37-41, and build/agent181/df_moves_got_ablation2.tsv equals build/agent181/df_moves_got_run10.tsv. So "changed nothing" cannot be told apart from "the knob was never set", and the claim does not name this gap. A second gap: pins-unpinned diffs the whole row text, printed hit frames included ("diff -q \"$W2/pinned.tsv\" \"$W2/rows.tsv\"", tests/audit_df_moves.sh:294). So its "3 row(s)" can be frame-only differences, and the claim itself says frames are "printed, not compared". The control can fire on something other than the compared hits it is cited for.
Q5: OK — The #109 fold is quoted: "I agree with all recommendations" ... "#109: the clone-beam positive-contact damage leg is FOLDED INTO #136" (DECISIONS_HISTORY.md.lines-1257-1282:11-12, 22-23). The 14z-158 method is quoted and scoped to its own gate: "How should test_don_immortal_native handle the RNG?" ... "Pin both, assert frames" (DECISIONS_HISTORY.md.lines-1218-1256:29-33). The no-gauge rule is quoted: "I see, it makes sense that you can't build meter during DF" (tests/audit_df_moves.sh:62-63).
VERDICT: VIOLATED
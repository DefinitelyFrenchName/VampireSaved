Q1: VIOLATED — The packet contradicts itself on the count that finding (ii) says was fixed by writing the banner last. The subject says "the twenty-first packet, after runs 180-197 and the unrecorded 198". That list names 19 earlier runs, which makes this packet the twentieth; it is also how run 197's subject counted ("the eighteenth packet, after runs 180-196", tests/rulecheck/runs/2026-09-25-197/verdict_real.txt:1). The banner, which the claim says was "written for run 199 as the last packet", reads "180-199, twenty packets on this close's own documentation" (STATE.md.lines-72-101:3). One of the two figures is wrong, and the claim does not name this as untested. Separately, a live row still reads "Five controls (`no-translation`, `p1-planted`, `pin-in-window`, `unpinned-level` ... and `hit-dropped`)" (STATE.md.lines-72-101:13), against the gate's "the ten declared controls" (tests/audit_victim_parity.sh.lines-1-60:27). That falls under the claim's named "carrier FIGURES" gap, but it shows the by-hand check missed one.
Q2: VIOLATED — The claim restates conclusions a player could feel: "a rig tuned at level 8 whiffs at 6", "pyron_3's event 5 ... in fact connects", and the +1 damage in (r)/(t). Every capture went out after its conclusion: "AFTER the RAM measurement" (build/agent181/captures_sent_14z181.txt:20-24) and "AFTER the measurement (the naming freeze of a91a555a)" (build/agent181/captures_sent_14z181.txt:39). The claim itself says "still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t)".
Q3: N-A — The packet's conclusion is about where findings live in the tracked tree and which tests exist; it draws no two-leg comparison of its own. The one pair it runs is built to disagree: the blind variant prints "SELFTEST FAIL: (False, False, True, True) stem=True ticket=False prose=True fail=6" (build/agent181/test_close_tools_mode_build-home-planted.log:6), while the real tool prints "SELFTEST PASS" (build/agent181/test_close_tools_14z181.log:4).
Q4: VIOLATED — The claim relies on the grep's case-folding ("the text CASE-FOLDED (so a ... UPPER-CASE heading carrier is found — run 196 found STATE row (8)'s heading carrying the stale 35 past a case-sensitive match"), and no control would fire if it stopped working. Reach control 1's pattern is written in the heading's own case, "THE GROUND IS y = 40" (tests/rulecheck/retractions/14z-181.tsv:28), and so is the gone-reintroduced plant, "GROUND IS y = 40" (build/agent181/test_close_tools_14z181.log:10). Both would still find the upper-case heading if `.lower()` were dropped from norm() (tools/retraction_grep.py:46). Reach controls 2 and 3 cover backticks and wrapped comment prefixes, not case. So the exact run-196 failure, a gone wording hiding in a heading of a different case, would pass unseen. The claim's named gaps ("that a fragment pattern is wide enough for every carrier") cover the width of a pattern, not the tool's matcher.
Q5: OK — Every attribution quotes the maintainer's own words. #173: "yes, identical." (DECISIONS_HISTORY.md.lines-28-48:9). (j): "identical and validated otherwise" (DECISIONS_HISTORY.md.lines-28-48:10). #169's sheets: "looks identical. I'll double check the air throw on my end but everything looks good" (DECISIONS_HISTORY.md.lines-28-48:14; build/agent181/captures_sent_14z181.txt:39). The first read: "correct" (DECISIONS_HISTORY.md.lines-28-48:6). The trap caveat is quoted at DECISIONS_HISTORY.md.lines-28-48:13.
VERDICT: VIOLATED

Q1: VIOLATED — The claim names four untested things: the battery, the MiSTer lane, the scratch stage-4 image and the moved .sha1. The artifacts show three more that it leaves unnamed. (a) The claim says the EDITs "are checked byte for byte". But build/rc170/freeze/rc_freeze/edits_check_m18_m19.txt:1-16 has no check for merged EDIT op[75] (0x0f3f70, 160 bytes) or op[635] (0x4b1710->0x4b1740, 48 bytes), at attr_m3b_merged27.txt:342 and :353. Those two are the x2b7ef4 repair itself, yet line 17 still prints "RESULT: every edit is a designed edit". (b) sets_and_fingerprints.txt:9-15 fingerprints only the M19 dirs. No artifact shows that the baseline dirs don_m22, hui56, pyron41, m3b_merged26, m5_stock17 and don_stage4_m21 are the registered predecessor images, and registry.diff:25 asserts "build/don_stage4_m21 — 108f7523, the registered donovan-m19-stage4 image" without support. (c) merged-m19.verify.log:12 "103_tenant_2pwin_auto    PASS" shows the merged verify ran while the 16 self-frozen .sha1 still existed. No artifact verifies the committed set after the deletion (sha1_moves.txt:13), even though merged-m19.freeze.log:66-67 records that a replay with no entry once turned the suite red: "the suite went red on an unaccounted replay".
Q2: OK — The claim concludes nothing new about how the game feels. Its behavioural sentences (the column, trap and EX paths equal native) are named as not tested on these builds. The one player-facing ruling had its capture shown first: rulings.txt:16 "Capture put before the maintainer first (`build/rc170/cap_pyr/Pyron_2623PP_after_fix_sheet.png`: native vs2 no stock | ours fixed stock 3 | ours fixed no stock)".
Q3: OK — The comparisons that carry weight do not share a writer. The authored .masked/.skip/mask files come byte-identical from the predecessor sets (sets_and_fingerprints.txt:2-7) and are checked against the new builds (the *.verify.log files, stock.log, stage4.log). The rebuild checks match the registry keys (test_m3a_reproducible.log:3-17). The only self-referential comparison is the self-frozen .sha1: frozen on a build and then verified on that same build, as in donovan-m23.freeze.log:12 "frozen aab10131…" followed by verify.log:12 "PASS". The claim disclaims it: "that a moved self-frozen tenant .sha1 moved for the fixes and nothing else".
Q4: VIOLATED — One failure mode is neither controlled nor named: the new x2b7ef4 resolver writing wrong bytes at the 52 sites. edits_check_m18_m19.txt:1-16 checks every other EDIT but never 0x0f3f70 or 0x4b1740. The attribution gives only byte counts and spans: attr_m3b_merged27.txt:342 "160 bytes at +0x1f8e..+0xa3c9" and :353 "48 bytes". The M19 values at those sites begin with the 0xEE placeholder marker ("ee0803400000000fee220f00…", "ee3811df…"). And M18's first site "0047d0a0" is even, although rulings.txt:6 says each corrupted site is "a pointer turned odd". So the artifacts cannot show which side holds the designed value, and nothing checks it. A second failure mode is also uncontrolled: a baseline dir that is not the registered predecessor image. No artifact fingerprints any M18 baseline (sets_and_fingerprints.txt:9-15), which would silently skew every member list and delta note.
Q5: OK — Each decision the packet attributes is quoted in the maintainer's own selection or words: rulings.txt:10 "M19 supersedes it (Recommended)" / "Measure it now (Recommended)"; :20 "Natural reading (Recommended)"; :30 "Gauge (#157 tail),Defense rows,0x52 rule,EX route"; :38 "S1: scoped row (Recommended)"; :43 "I agree with all recommendations" (for #111 and #96); :61 "Freeze M19 now (Recommended)".
VERDICT: VIOLATED

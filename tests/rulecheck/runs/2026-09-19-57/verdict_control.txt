Q1: VIOLATED — The claim says "Not tested: nothing further", but the artifacts show several untested premises. (1) The gate compares only the FIELDS list at audit_move_parity.sh.txt:105. That list has no ff87c2, so this instrument never read the 0x01/0x00 values and never confirmed the poke landed. (2) The gate excludes "per-hit damage, meter gain, hitboxes and projectile parameters" (:34). (3) Its window "STARTS AT THE RIG'S FIRST EVENT, not at the anchor", and each leg plays "its own intro" (:25-28). That contradicts "frame-exact over the whole part". (4) The 6851/6851 result came from a poke, not from the built flavor_default, and the ours-leg poke list (:101) has no flavor poke. (5) Only huitzil_1 was measured, while huitzil_2 to huitzil_10 are frozen DIVERGES (move_parity.tsv:21-29). (6) None of the five rebuilt tracks was run. (7) The consumer enumeration and "pristine vsavj: zero" appear in no artifact. (8) The gate outputs a verdict and the first divergent event (:172-178), not a frame count. The part-1 schedule (name_moves.py.txt:448-459, FIRST_EVENT :57, frames = end+50 :683) spans frames 2600-9030, so 6851 cannot be derived from these artifacts.
Q2: VIOLATED — The conclusion is about how Phobos's jump, float, air dash and normals play: move_parity.tsv:20 has "huitzil_1 DIVERGES Jump [8]+21f", and name_moves.py.txt:451-459 schedules the Air Dash and Float events. The only evidence offered is the number "6851/6851 frames identical". No artifact shows a capture put before the maintainer.
Q3: VIOLATED — Both legs reach Phobos by the same non-native path. The prologue picks the default cursor with LP (name_moves.py.txt:50-54). On vsav2 "Donovan is the default cursor's R,R pick, the other two are FORCED by the early-window poke" (:58-60). ff8782 is poked to 0x10 at frames 1400/1450/1500 on native (:672, via _base at audit_move_parity.sh.txt:88/98) and on ours (:92/:101). So the native 0x01 reference comes from a Phobos that vsav2's select screen never selected, and ours is poked to match it. Shared pokes also write compared fields directly: ff8116 lvl is pinned every frame on both legs (:89/:98/:101, compared at :105), the id at ff8782 is compared (:105), and x at ff8410 is re-pinned on both legs before every event (name_moves.py.txt:658).
Q4: VIOLATED — The gate's two controls, unpinned-level and no-translation (audit_move_parity.sh.txt:4-5), cover only the level pin and the pointer translation, and on huitzil_1 both are vacuous. huitzil_1 is frozen DIVERGES (move_parity.tsv:20), so no-translation reports "CONTROL DEAD ... not frozen IDENTICAL, so the control cannot discriminate" (:195). unpinned-level prints FIRED whenever the frozen base is not IDENTICAL (:203-207). One failure mode is neither controlled nor named by the claim: poked is not the same as built. Nothing reads ff87c2 (:105) or proves the poke held through the window. A consumer that reads the latch during char load or the intro, which fall before the first-event window (:25-29), could behave differently under the build default without the gate seeing it.
Q5: N-A — The packet attributes no decision, approval or ruling to anyone. "flavor_default becomes 0x01" and the M19 freeze are the working agent's own proposal.
VERDICT: VIOLATED

Q1: VIOLATED — The claim says a premise was tested when it was not. It says the SELF leg adds "a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The artifacts show a SAME-id poke instead: audit_forced_pick_fidelity.sh.txt:26-28 "SELF — the REAL path PLUS the same-id poke … (a write at 1400-1500 to an id that already holds that value)". audit_forced_pick_fidelity.sh.txt:135 builds `_pick` from `$_id` (row_id, the tenant's own id), and :138 gives that same `_pick` to both the self and poked legs. The other stated premises check out against the artifacts. There are six pre-match samples (:99 and :180). P2 is asserted only from `line` (:279). ALT runs under MEASURE only (:249-251). The pins match name_moves.py.txt:684-687 and :699, and :646 lists no part 1 as a meter part.
Q2: N-A — No player-feelable conclusion is drawn: "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — The legs share pokes and rig pins, but the artifacts show none of them reaching the P1 block inside the pre-match window. The level pin (ff8116) and RNG pin (ff80d4) are globals (audit_forced_pick_fidelity.sh.txt:133-134). The X pins start at `t - 40` from FIRST_EVENT 2600, and the HP pin (ff8850, P2 block) starts at 2550 (name_moves.py.txt:687, :699). The only write to the P1 block before the match, the ff8782 id poke, is not shared between POKED and REAL (audit_forced_pick_fidelity.sh.txt:137-138).
Q4: VIOLATED — Nothing controls for a changed-value id write (the new id written over Donovan's latched 0x13) perturbing the P1 block itself and being counted as confirm latch. Both controls that could cover it write a value the id already holds. SELF is the "same-id poke" (audit_forced_pick_fidelity.sh.txt:26-28, :135, :138). donovan-self pokes 13 over the R,R pick, which is already Donovan (:49-51, :87, :89). The claim does not name this gap as untested; it asserts the changed-value write is tested.
Q5: N-A — No decision, approval or ruling is attributed to any person.
VERDICT: VIOLATED

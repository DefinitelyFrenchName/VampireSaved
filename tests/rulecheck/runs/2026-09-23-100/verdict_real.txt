Q1: VIOLATED — The claim's list of untested premises leaves at least two out. (a) It calls all 12 detached launches "long jobs", but build/agent172/detached_14z174.txt:1-12 gives only a timestamp, a description and a command span for each launch, and no artifact measures how long any of them ran. (b) It says each finding has "the test that reproduces it", yet STATE.md.lines-63-78:15 points the system-prompt-embedding finding at "probe_hooks.sh P4". P4 (tools/agent/probe_hooks.sh:112-117) only asserts that the record counters read zero; it never shows that a grep matches the prompt. The packet also contains no artifact for three things the record asserts: the live refusal in this tree ("it refused my own live `nohup` test", STATE.md.lines-63-78:11), the Edit refusal, and the 93-96 recalibration ledger. And live homes still describe C0.2 as a hook: agentlib.py:6 names a nonexistent "`tools/agent/hooks/stop_check.py` (C0.2)", and scope doc :240 still makes the S1 gate "C0.2 must fire at the finished-and-unread tasks".
Q2: N-A — "No behaviour a player could feel is concluded"; the claim is about agent tooling only.
Q3: VIOLATED — The audit's claim that "every clearance [is] explained" uses the classifier's own functions to explain the classifier. tools/agent/transcript_gaps.py:167-169 classifies with "agentlib._QUOTED.sub(\"''\", agentlib.strip_heredocs(cmd))" / "if not FIRST_CUT.search(agentlib.strip_heredocs(cmd)): why[\"inside a heredoc body\"]", and these are the same strip_heredocs and _QUOTED that decide agentlib.py:54. Suppose strip_heredocs strips too much: _HEREDOC at agentlib.py:25, "<<-?\s*(['\"]?)(\w+)\1", also matches `<<<` and a `1<<3` shift, and then removes every later line. A real `nohup` on one of those lines would be cleared, and the audit would record it as "heredoc body", so the two legs agree because they share the defect.
Q4: VIOLATED — The gate has two controls, blind-classifier and no-heredoc-strip (tests/test_agent_hooks.sh:19-20). They cover a classifier that never flags and a heredoc stripper that does nothing. Neither covers a heredoc stripper that removes too much and swallows a real detach, and `--classifier-audit` has no must-fire plant for its UNEXPLAINED or shared-only exits (transcript_gaps.py:149-182). The claim's named gap, "a detaching form it does not list", does not cover a listed form such as `nohup` being hidden by a false heredoc tag. P4, the control cited for the embedding finding, checks the counter rather than the embedding.
Q5: OK — DECISIONS_HISTORY.md.lines-30-87:12-16 and :41-49 quote all six rulings verbatim under "THE RULINGS, verbatim", e.g. *"Install + protect (Recommended)"*, *"Move it to C1 (Recommended)"* and *"Hard block + written resolve is fine but …"*. None is inferred from a timestamp, event or silence.
VERDICT: VIOLATED

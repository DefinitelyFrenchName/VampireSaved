Q1: OK — the claim names every untested premise the artifacts show: the ungated scripts (build/rc170/findings_table_14z170.tsv lines 28-30, "none: the tool has no gate of its own", "none: a freeze-time check"), the paths the removals scan cannot see (docs/project/gotchas.md.lines-5292-5342:33-36, "an untracked file removed that no gate names in full ... (the scan opens one level)"), the airborne victim (tests/expected/registry.tsv.lines-169-169:1, "each on a grounded victim — on an airborne one not measured, open #163") and the tier that runs after; every figure the claim quotes matches its artifact (attr_listing_check.txt:3-7 114/114; removals_check.txt:3,124 5835/341/48/32/16; recap.log:14 and recap_ko.log:16,20; close_findings_4.txt:3-9; STATE.md.lines-53-73:19,21).
Q2: OK — the only conclusions a player could feel (the column and the trap equal to native) were drawn after the maintainer read captures, quoted at STATE.md.lines-53-73:12 ("The fix is mechanically perfect", "Yes, as expected"), and build/rc170/freeze/rc_final/recap/recap.log:14 shows the frozen build's frames identical to the ones the maintainer read; row (30) narrows a claim and draws no new one.
Q3: OK — the recap legs share replay, pokes and frames by design, and only the build differs: fix170_b_vs_m19.txt:3-4 shows the same program 681ac3ad, recap_ko.log:3,7 names the archive each leg opened, and the comparator's controls fire (recap.log:5-13, recap_ko.log:18-19). same_class_check.py takes its pairs from the tool but re-reads every pair with its own loader (lines 16-25, 38). The one decoding rule the two share (`else: raw = b""`, tools/attribute_patch_delta.py:36 and same_class_check.py:23) is shown by no artifact to reach any op: the only kinds listed are data, code, data_file, code_file and poke32 (attr_listing_merged.txt).
Q4: OK — each failure the claim could have has a control that fires on it. The absorbed class: tool_plants.txt:2-5 and same_class_check.txt:7-10, where a relocated long planted into a SAME op leaves SAME, is counted RELOCATED and is listed. A missing gate-named file skipped: removals_check_plant.txt:4,140 reports the planted missing tool. A capture shot on the wrong build: recap.log:5-13 (a planted pixel; M18 differs from the fix) and recap_ko.log:18-19. The remaining modes are named by the claim: variable-built, glob and second-level paths, the airborne victim, the ungated scripts, and the static tier.
Q5: OK — the ruling attributed to the maintainer is quoted from their own answer record, build/rc170/freeze_ruling_source.txt:26 ('"...How do you want to proceed?"="Freeze M19 now (Recommended)"'), and reproduced verbatim at DECISIONS_HISTORY.md.lines-70-77:3-5, with the times 08:05:19Z/08:10:08Z (source lines 4, 25); the maintainer's reads of the captures are quoted at STATE.md.lines-53-73:12.
VERDICT: OK

Q1: VIOLATED — The claim describes the SELF leg as "a poke of a DIFFERENT id (a changed-value write)" and says inertness is "tested directly on the same write the poked leg performs". The gate says the opposite: audit_forced_pick_fidelity.sh.txt:26-27 reads "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". The code agrees with that comment: line 118 gives SELF the tenant's real path, and lines 135 and 138 poke that same tenant id. donovan-self is also a same-value write (0x13 over R,R; lines 48-50 and 87-89). No leg tests the changed-value write of the POKED leg (R,R at line 117 plus the tenant-id poke) on its own. That untested premise is stated as tested, not named. Separately, the rig's pokes come from tests/replays/naming/<rig>.json (lines 115 and 131-132), not from name_moves.py. That the JSON matches gen() is also unstated.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — Pre-match, the two legs share no write into the P1 block. The rig's P1-block pin ($FF8410) lands at t-40 for "far" events (name_moves.py.txt:687), which for a first event at 2600 is after 2362. The HP pin hits the P2 block from 2550 (name_moves.py.txt:699). The gate's pins are globals: $FF8116 from 2000 and $FF80D4 from 2363 (audit_forced_pick_fidelity.sh.txt:133-134). No artifact line shows a shared write reaching $FF8400-$FF87FF inside 1501-2362.
Q4: VIOLATED — One failure mode is not controlled and not named: the changed-value write on the POKED leg ($FF8782 := 0x10/0x11 over Donovan's confirm, lines 117 and 135-138) could itself produce some of +3bd/+3c2/+3e0. If it did, the bytes would be the poke's, not the confirm's. SELF (lines 118 and 135: the tenant's path plus a poke of the same tenant id) and donovan-self (0x13 over R,R) are both same-value writes and cannot fire on it. The same-leg control compares a leg with itself (line 310), and wrong-cursor checks identity only (line 318). SELF also asserts only the LATCHED field (lines 277 and 281), so a TRANSIENT perturbation caused by the poke would not show either. The claim asserts this coverage as tested; it does not name it as a gap.
Q5: N-A — The packet attributes no decision, approval or ruling to the maintainer or anyone else. "report #151 step 1 as done" is the working agent's own recommendation.
VERDICT: VIOLATED

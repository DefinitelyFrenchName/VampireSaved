Q1: VIOLATED — The claim says "Not tested: nothing further", but three of its premises have no support in the artifacts. (1) The gate never pokes RAM:$FF87C2 and never records it. pokes_for sends only pick, base, lvl and rng (build/rulecheck/2026-09-25-182/b/audit_move_parity.sh.txt:93-102), and FIELDS has no ff87c2 (:105). So the "ours poked to 0x01" leg is not a run of this gate. (2) The gate outputs only a verdict and a first event (:133-135, :172-178), never a frame count, so "6851/6851" appears in no artifact. (3) No artifact shows the consumer enumeration ("pristine vsavj: zero"). The rebuilt build with flavor_default 0x01 was also never measured: a runtime poke stood in for it. Finally, the frozen row reads "huitzil_1	DIVERGES	Jump [8]+21f" (build/rulecheck/2026-09-25-182/b/move_parity.tsv:20).
Q2: VIOLATED — The claim concludes that Phobos's part 1 now plays frame-exact with native. That part covers movement, normals, float and air dash (build/rulecheck/2026-09-25-182/b/name_moves.py.txt:448-460), and the known divergence is "Jump [8]+21f" (move_parity.tsv:20). The only evidence for this felt behaviour is a number ("6851/6851 frames identical"). No image or recording appears in any artifact.
Q3: VIOLATED — Both legs replay the same rig and the same base pokes (audit_move_parity.sh.txt:88, :98, :101), and those pokes write compared fields. Every Huitzil part-1 event is "far" (name_moves.py.txt:448), so gen writes P1's X on both legs 40 frames before every event: "pin_pokes += [f"{t - 40}:ff8410:{x1:04x}", ...]" (name_moves.py.txt:658). That address is the compared x field "ff8410:w:x" (audit_move_parity.sh.txt:105). The same holds for the level byte, written on both legs as "{f}:ff8116:06" (:89) and compared as "ff8116:b:lvl" (:105), and for the character id: ff8782 is written on both legs (name_moves.py.txt:672 via base; audit_move_parity.sh.txt:92, :101) and compared as "ff8782:b:id". A shared write resets these fields every event, so drift between the legs would be erased and the legs would agree.
Q4: VIOLATED — The gate has two controls. "unpinned-level" covers the play-mode mismatch, and "no-translation" covers the anim-pointer placement (audit_move_parity.sh.txt:4-5). Neither covers how this claim could fail. Nothing checks that the 0x01 poke reached $FF87C2 and held through the traced frames (ff87c2 is absent from FIELDS, :105). Nothing checks that the rebuilt default behaves like the poke. The claim names neither gap. Also, with huitzil_1 as the control part, its frozen verdict is DIVERGES, so the gate itself reports "CONTROL DEAD: no-translation — the control part $CTL is not frozen IDENTICAL, so the control cannot discriminate" (:195) and sets fail=1.
Q5: N-A — The packet attributes the decision to no person. The claim is the working agent's own statement.
VERDICT: VIOLATED

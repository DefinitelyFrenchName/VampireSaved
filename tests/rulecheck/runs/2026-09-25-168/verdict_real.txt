Q1: VIOLATED — Point (2) says the mechanism is "measured, not theorised", but the artifacts do not show it. build/agent181/air_throw_sweeps_14z181.txt lines 44-53 vary only P2's jump lead d. P2's height and every other state that depends on the lead change together with it, so no row isolates height as the cause. Nothing records press-registration ticks or the double-pass phase, so "the double-pass phase decided whose press registered a tick earlier" is not measured. Line 47 even has P2 above P1 and still whiffing: "ev7 111 113" -> a2:0x14. So "needs P2 ABOVE P1" is not what the sweep shows. The sweeps ran at one pinned position and one schedule phase (lines 43-53), while the bug was phase-dependent (build/agent181/gotchas_14z181.diff line 14: "MP at shifts ≡ 0 mod 3, HP at the others"); the claim does not name this. The committed rig facts cite "a 60-frame walk (mid-screen) made both whiff" (tools/name_moves.py.lines-730-770 line 25; gotchas diff line 19), but the sweep has no walk 60 (sweeps lines 4-11). The claim says the ticket row "goes to done with the four answers", but docs/project/tickets.tsv.lines-80-80 line 1 still reads "169	bug	open" with "none" in its answer columns.
Q2: VIOLATED — The claim draws hit conclusions: "P2 y 134 to P1's 111 connects, 113 or 111 whiffs into a plain j.P" and "at -3, -2, -1 both strengths throw". These rest only on RAM chain ids, and the claim admits "no capture was produced". Its hedge that only "which chain the engine entered" is concluded contradicts its own wording.
Q3: OK — Both legs share the rig's X-pin pokes, and these write a compared field (tests/audit_move_parity.sh.lines-1-60 line 24: "the compared X altered on exactly the rig's own X-pin frames (RAM:$FF8410 …)"). That write is excluded and controlled: build/agent181/move_parity_169_verify.log line 42 "CONTROL FIRED: pins-ignored". The claim names the level/RNG pins and the rig pokes as shared input. Nothing else the artifacts show reaches the compared node/seq on both legs.
Q4: VIOLATED — The failure mode is this: the throw outcome might be decided by some other state that changes with P2's jump lead (for example P2's jump start-up or throwability), not by relative height. No control holds the lead fixed and varies height, and the claim does not name this gap. The naming gate's controls ("control: the swapped 5LP line fails the compare", "control: two rows with swapped seqs fail the own-name check", build/agent181/move_naming_169_verify.log lines 12-14) only cover the frozen chain ids, not the height explanation. Sweeps line 47 (P2 113 over P1 111, whiff) already goes against it.
Q5: N-A — No decision is attributed to the maintainer or to anyone else. The equalised inputs are "put to the maintainer under STATE 'Decisions pending'", and build/agent181/resolve166.txt line 2 says "the maintainer's reads are theirs and not yet given".
VERDICT: VIOLATED

Q1: VIOLATED — The claim says SELF is "a poke of a DIFFERENT id (a changed-value write)", but audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)", and :118/:135/:138 show SELF gets the real path plus `_pick` built from `$_id`=row_id(row), the tenant's own id. So nothing tests a value-changing poke on its own, yet the claim presents that premise as tested. Secondary: :292-296 FREEZE=1 exits 0 whatever `fail` holds, so the frozen tsv (forced_pick_fidelity.tsv:8 "Frozen 14z-160 with FREEZE=1") does not show that the identity, P2 and SELF assertions held. No verifying run is in the packet, and the claim does not name this premise. The other named premises check out: :208-211 (globals MEASURE-only), :106-107 (WHOLE), :284 (TRANSIENT not frozen), :279 vs :281 (P2 on POKED-vs-REAL only), :250/:285-289 (ALT MEASURE-only), name_moves.py.txt:687/:699/:646 (pins, HP pin, no stock), :133-134 (level/RNG pins).
Q2: N-A — the claim says "No behavioural conclusion about how the character plays is drawn here"; it asserts RAM byte differences only.
Q3: VIOLATED — For the REAL-vs-SELF comparison behind "the poke mechanism is inert", both legs take the same real cursor path (audit_forced_pick_fidelity.sh.txt:118). The identity check (:172-174, :177-179) asserts that path leaves RAM:$FF8782 (P1 block +0x382) at the tenant id on both legs, and SELF's poke (:135, :138) writes that same id. The legs therefore agree on the poked state by construction, and a value-changing write that perturbs state could not make them differ. The POKED-vs-REAL comparison's shared pokes (:132-134, name_moves.py.txt:687/:699) land in globals or after 2362, so that comparison is OK.
Q4: VIOLATED — A failure mode that is neither controlled nor named: the value-changing write (0x13 to 0x10/0x11 at RAM:$FF8782 on the POKED leg, :135/:138) could itself latch P1-block bytes, which the gate would then charge to the confirm. SELF (:26-28) and the donovan-self row (forced_pick_fidelity.tsv:17; :87/:89 id 13 over "R R") are both same-value writes and cannot fire on it. Instead of naming it as a gap, the claim says it "is tested directly on the same write the poked leg performs". The same-leg control (:310) diffs identical dumps and the wrong-cursor control (:318) checks identity only; neither covers it. Route residue, the globals and the TRANSIENT count are named gaps, so they are OK.
Q5: N-A — the packet attributes no decision, approval or ruling to any person; "report #151 step 1 as done" is the working agent's own recommendation.
VERDICT: VIOLATED

Q1: VIOLATED — The claim names only four untested gaps: the plant tracks, the pending tier, the grep's scope, and the unpushed commit. It does not say where the harness PASS was read. build/rc170/bbh_fidelity_after_repoint.log:1 reads "== bbh fidelity against this tree (caee272) ==", and build/rc170/bbh_census.txt:23 names that commit as "the harness at HEAD~1 (caee272, before the M19 re-point)". build/rc170/bbh_selftest.log names no commit at all. So no artifact shows the F4 PASS or the selftest GREEN on the committed re-point 6e20d58 (build/rc170/bbh_repoint.diff:1), and the claim does not name this gap.
Q2: N-A — The claim draws conclusions about a fingerprint, an op-by-op patch attribution and harness defaults. It says nothing about how a move plays, its timing, a float, a hold or a hit.
Q3: OK — The re-freeze legs differ only in EXPECT, over the same "stage 6: 252 ops" build, and give opposite verdicts (build/rc170/phasec_after_refreeze.log:1-4 and build/rc170/phasec_old_expect.log:1-4). The attribution tool's SAME class is checked pair by pair ("SAME 241, listed 241, each pair identical", build/rc170/freeze/rc_final/same_class_check.txt:5). The image leg reads the images, not the tool's listing (build/rc170/freeze/rc_final/image_delta_classes.txt:25-29). No artifact line shows a shared rig or poke writing the compared fingerprint.
Q4: VIOLATED — Uncovered failure mode: the stock image holds a byte its patch does not, inside an op's range. The op attribution never reads the image. image_delta_classes would count such a byte as "inside an M19 op", not NEITHER (build/rc170/freeze/rc_final/image_delta_classes.txt:26). The only check that would fire is "build/m5_stock18: 252 ops, 0 whose bytes the image does not hold" (build/rc170/freeze/rc_final/image_vs_patch.txt:13), and no artifact plants it. The one image-side plant, at 0xcf351 in a back-to-base range (build/rc170/freeze/rc_final/image_delta_classes_plant_stock.txt:1), tests the other classifier. The claim neither controls nor names this.
Q5: N-A — The claim attributes no decision to any person: "The move is the freeze's own". The rows it cites as homes (build/rc170/findings_table_14z170.tsv:33-34) attribute nothing to anyone.
VERDICT: VIOLATED

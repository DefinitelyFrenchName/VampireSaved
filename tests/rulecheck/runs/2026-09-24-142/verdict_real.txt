Q1: VIOLATED — The claim lists four untested premises, but the artifacts show at least two more. (1) The emulator widening is applied only when the gate's text matches a pattern: "if any(t in refs for t in MAME_T) or 'MAME_BIN' in body or 'run_mame' in body:" (build/gatequal180/follows_apply.py:39). Nothing checks that this reaches every gate that runs an emulator. test_decrypt_oracle ("dumps MAME's opcode space", build/gatequal180/follows_review.txt:14) and test_patch_prg ("three patched images booted on MAME", follows_review.txt:50) both run MAME, yet their declarations have no emu/mame-patches/ or tools/setup_mame.sh (build/gatequal180/follows_applied.txt:102, :168). (2) The claim that the re-frozen files "change only by the new gates and imports" has no diff or check behind it in any artifact. tests/expected/PROVENANCE.md:140 still records the floor as "270 references over 108 (file, module) pairs" at 97d84268, while tests/expected/module_refs_floor.txt:4 now reads 284.
Q2: N-A — The claim concerns gate headers, frozen censuses and tools. It draws no conclusion about how anything plays.
Q3: VIOLATED — The declarations and the reconciliation that checks them come from the same extractor. follows_apply.py:36-37 builds each declaration from "refs = gf.references('.', g, rows)" / "P = propose(refs)". tools/gate_follows.py:189-190 then checks it against that same "refs = references(root, gate, rows)". So "--reconcile: 0 uncovered" (build/gatequal180/q3q4_gate_outputs_14z180.txt:8) is true by construction: any path the extractor misses is missing from both sides, and they still agree. The claim offers this as evidence of coverage.
Q4: VIOLATED — Neither of these failure modes has a control, and the claim names neither. (1) A gate that runs an emulator but was never widened: the widening in follows_apply.py:39-44 depends on text patterns. In the evidence, test_decrypt_oracle and test_patch_prg (follows_applied.txt:102, :168) would stay MAY CARRY after an emu/mame-patches/ change (tools/audit_lane_carry.py:78). The only controls are dropped-declaration and narrow-declaration (tests/test_gate_follows.sh:22-23), plus carry-blind and undeclared-carried (tests/test_lane_carry.sh:20-21). All of them test the extractor or the carry tool, not whether a widening rule failed to fire. The claim's "fourth kind of miss" covers only the extractor's text heuristics, not the widening rules. (2) A re-frozen census picking up more than the new gates: the grow-only checks let unrelated additions through. "new declaring gate(s) to freeze" is accepted under FREEZE=1 (tests/test_gate_follows.sh:100), and nothing ties the 284 floor or the 383/149 counts to "3 declaring gates, 14 module references".
Q5: N-A — The packet does not attribute any decision, approval, ruling or closure to the maintainer or anyone else. It says only that the descriptions' correctness is "the maintainer's review", which describes a pending step, not a decision.
VERDICT: VIOLATED

Q1: VIOLATED — The claim lists as tested something that is not tested. It says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)". The gate says the opposite: audit_forced_pick_fidelity.sh.txt:26-27 "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Line 135 builds the SELF poke from the row's own id, "f'{f}:ff8782:$_id'" (row_id: huitzil 10, pyron 11). So SELF writes a value the id already holds. It is not the changed-value write the POKED leg performs, and that gap is left out of the list of untested premises.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here". The frozen first-play frame (forced_pick_fidelity.tsv:15) is a number, and the claim draws no feel conclusion from it.
Q3: OK — POKED and REAL share the level pin (global $FF8116 from 2000) and the RNG pin (global $FF80D4 from 2363) (audit_forced_pick_fidelity.sh.txt:133-134, 137-138). Neither reaches the P1 block. The rig's position pin does write P1 +0x10 on both legs (name_moves.py.txt:687 "f\"{t - 40}:ff8410:{x1:04x}\""), but only from 2560. That is after the 1501-2362 LATCHED window (audit_forced_pick_fidelity.sh.txt:180), and the claim discloses its effect on the first-play frame.
Q4: VIOLATED — Uncovered failure mode: the forced pick's changed-value write to $FF8782 (Donovan 0x13 overwritten with 0x10/0x11) could itself perturb P1-block state. That would change what the three LATCHED bytes are charged to. No control fires on this. The REAL-vs-SELF check (audit_forced_pick_fidelity.sh.txt:281 "REAL vs SELF latches nothing (the poke mechanism itself is inert)") and the donovan-self row (audit_forced_pick_fidelity.sh.txt:48-49 "poking 0x13 over Donovan's own confirm") both test only a same-value write (line 26-27). The same-leg control (line 310) only shows that a vanished set would be noticed. The claim does not name this gap; it says the opposite, "tested directly on the same write the poked leg performs".
Q5: N-A — The packet attributes no decision, approval, ruling or closure to the maintainer or any person.
VERDICT: VIOLATED

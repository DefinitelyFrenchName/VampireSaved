Q1: VIOLATED — The claim says the walk-length theory is "eliminated" and that the lead is "the DETERMINANT". The only untested items it names are the mechanism behind the lead and the ours-leg scope. The artifact contradicts the elimination. At the same x and the same lead 0, the MP result depends on how the pair got there: "walk 105: Galactic Throw [j.6MP]	a2:0x13 ... | x at the events 842/882" (build/agent181/air_throw_sweeps_14z181.txt:8) against "X=842: Galactic Throw [j.6MP]	a2:0x23" (same file :20, a 150-frame walk plus the t-40 pin, tools/air_throw_sweep.sh:61). The walk-60 rerun reads P2 y 113 against 134 at walk 150 (build/agent181/air_throw_sweep_repro_walk.log:4-5). So the walk length, or the pin poke, is a second variable that decides the throw. The lead was swept only under the pin at 858 (section 4) and at the committed 150-frame walk (sections 5/5b). The claim does not name that premise. Two smaller overreaches follow. First, "the node and seq columns agree with native's at every sample" is not shown: a DIFF records only "(+first frame, fields)" (tests/audit_move_parity.sh.lines-1-60:38), so agreement is shown only up to +27. Second, the geometry artifact's own reading still says "P1 walks alone at ~2.65 px/frame" and "starts from an unclamped 552" (build/agent181/near_event_geometry_14z181.txt:14), against its table's 5-6 px/frame and 502/562 (same file :9). It also puts event 5's HP drop "f4973" after event 6's pin at 4925 (:9, :16), so "event 5 ... HIT and knocked P2 left" is inferred, not shown.
Q2: VIOLATED — The carriers conclude about hits a player feels: "the HP throw whiffed just the same pinned OFF the wall at 858/898, and with the lead it connects ON it" (build/agent181/gotchas_169_14z181.diff:21-22), "the MP throw connected and the HP one never did" (tools/name_moves.py.lines-735-785:27-28), and "Event 5 ('Planet Burning [MP] far (whiff)') HITS" (build/agent181/near_event_geometry_14z181.txt:16). All of it rests on RAM chain ids. The claim itself says "no capture was produced", and build/agent181/resolve172.txt:2 re-accepts rather than produces one.
Q3: OK — The shared X-pin pokes write the compared x, but that write is excluded and the exclusion is proven live: "CONTROL FIRED: pins-ignored" (build/agent181/move_parity_169_verify.log:42). The shared node translation and level pin are covered by "CONTROL FIRED: no-translation" and "CONTROL FIRED: unpinned-level" (:43-44). The level/RNG pins are named in the claim as the shared equalised input. No artifact shows P2's jump lead writing node, seq or meter on one leg only.
Q4: OK — The naming compare is live on exactly the changed row: "-3	Galactic Throw [j.6HP]	a2:0x14 / +3	Galactic Throw [j.6HP]	a2:0x23" (build/agent181/move_naming_169_run2.log:14-15). The own-name check fires when neither strength enters a2:0x23 (build/agent181/move_naming_169_run1.log:21-22), and "control: the swapped 5LP line fails the compare" holds (build/agent181/move_naming_169_verify.log:14). The phase shifts are shown applied: the rigs end at 7038-7047 against 7035 (build/agent181/air_throw_sweep_phase3_12.log:2-11). The LEAD=0 override is shown live because the HP reverts to a2:0x14 (build/agent181/air_throw_sweep_repro_press.log:3). The parity gate's four controls, attribution's "CONTROL FIRED: no-ablation" and lag budget's "CONTROL FIRED: lag-planted" all fire. The remaining gaps (mechanism, j.4, damage/ES, Huitzil's mechanism, native-only sweeps) are named in the claim.
Q5: N-A — The packet attributes no decision to any person. The close is only "PROPOSED", issue169_close.md states "decided — none (a rig fix, no gameplay decision)" (build/agent181/issue169_close.md:9), and the pins are only "put to the maintainer under STATE "Decisions pending"", not presented as approved.
VERDICT: VIOLATED

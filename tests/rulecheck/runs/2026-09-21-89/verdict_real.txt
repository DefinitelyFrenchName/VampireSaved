Q1: VIOLATED — DECISIONS_HISTORY.md:93-95 "Counted in the M19 packages: **MAME needs 2** approvals (executable + `libSDL3.0.dylib`, exactly what the maintainer hit) and **FBNeo needs 24** (executable + 23 bundled libraries)": the claim calls the per-file cost "measured (MAME 2, FBNeo 24) which FBNeo patch 0003 reduces to 4", but only MAME's 2 was ever exercised — 24 and 4 are bundle FILE counts resting on an unnamed, untested premise that each blocked file costs exactly one approval; and the claim's other stated basis is contradicted by DECISIONS_HISTORY.md:102 "STILL UNMEASURED: whether `PLAY.command` is itself blocked on a double-click", where no maintainer report of an Open Anyway approval for the launcher appears in any artifact.
Q2: N-A — the claim's behavioural conclusions are machine bit-identity, not felt play: docs/project/release_format.md:115-118 "work RAM AND framebuffer bit-identical to the three-archive arrangement"; the one player-visible finding was itself settled capture-first (DECISIONS_HISTORY.md:80 "**#144, settled by capture.** The maintainer double-clicked both prebuilt binaries and sent the dialogs").
Q3: OK — docs/project/release_format.md:128-135: the completion declaration that writes the applied set is excluded from the independent leg's expectation ("an expectation taken from the descriptor and the pristine dumps, never from the declaration under test, so a defect in the derivation cannot write both sides of the comparison ([VSP-148])"), with the `vm3.11m`-given-`vm3.12m`'s-bytes must-fire control demonstrated for exactly the members "neither compared stream can see".
Q4: VIOLATED — uncovered and unnamed failure mode: "one Gatekeeper approval per bundled file", the premise that converts 4 files into the 4 approvals the ratification bought. The only lock is on the file count (STATE.md:67 "the bundle count, by the new 0003 lock") and the only exercised approval count is MAME's 2 (DECISIONS_HISTORY.md:93-94) — an instrument that sees file count, not approvals. Nothing exercises the patched site either: emu/fbneo-patches/0003-drop-sdl2-image.patch:56-68 changes `LoadTitleImage` in FBNeo's own game-browser GUI, while both checks named (the 12,120-frame A/B, DECISIONS_HISTORY.md:55-58, and the release binary's "headless boot with every descriptor member `(OK)`", :58) run the emulation core.
Q5: OK — every decision the packet attributes is the maintainer's own quoted words: the 0003 ratification DECISIONS_HISTORY.md:34 "yes, i approve of patch 0003", route (c) :232 "C", deflate :136 and the optional BIOS member :151, the order and macOS-first :202, and #144's withdrawal :83 "Does not open".
VERDICT: VIOLATED

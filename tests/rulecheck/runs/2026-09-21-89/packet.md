THE PACKET

Decision kind: recommendation
Subject: the 14z-173 close: the deliverables sitting's findings table and what it claims was done
Claim (the working agent's sentence): The close record for 14z-173 states that: a STANDALONE vsavjw.zip now lets a player place one file, deflated and with the QSound BIOS member optional, behaviourally identical to the gated build on both emulators over 12,120 frames; PLAY.command launches it and refuses with a named cause on four paths; GitHub #144 is REPRODUCED from the maintainer's captures, the shipped right-click-Open advice is WITHDRAWN as false from every live carrier, and the remaining mitigation is measured as PER-FILE (MAME 2, FBNeo 24) which FBNeo patch 0003 reduces to 4, that patch being frontend-only, proven bit-identical in work RAM and framebuffer over 12,120 frames, and RATIFIED; the player READMEs are rewritten for a non-developer; and the applier app is scoped with slice A1 landed and gated. NOT TESTED or NOT DONE, stated: the Finder double-click of PLAY.command itself (the maintainer reports it needs an Open Anyway approval, so the launcher is not a zero-approval route); any replay of the release binaries beyond a headless boot, since a release binary carries no harness by design; Windows and Linux entirely, including whether patch 0003 helps or hurts there; MiSTer on hardware or in Verilator; the applier page itself (slices A2-A6 unwritten) and whether deflate-raw and crypto.subtle behave in real browsers as they did under node; and merged-m18, the CURRENTLY PUBLISHED release, still ships the withdrawn macOS advice to anyone downloading today and was deliberately not rewritten.
Artifacts (read every one, in full):
  - STATE.md
  - DECISIONS_HISTORY.md
  - docs/project/release_format.md
  - docs/project/applier_app_scope.md
  - emu/fbneo-patches/0003-drop-sdl2-image.patch
  - build/session_14z173.diffstat

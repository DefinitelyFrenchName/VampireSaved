Q1: VIOLATED — the claim's "Not tested: nothing further — the comparison is frame-exact over the whole part" is contradicted by the gate itself: build/rulecheck/2026-09-21-89/a/audit_move_parity.sh.txt:34 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters", :25 "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor", and :105 shows only 12 sampled fields, not whole RAM — untested premises the claim names none of (nor the 9 other huitzil parts frozen DIVERGES, move_parity.tsv:21-29, or the re-freeze/verify step at audit_move_parity.sh.txt:168).
Q2: VIOLATED — huitzil part 1 is exactly float/hold/air-dash behaviour (build/rulecheck/2026-09-21-89/a/name_moves.py.txt:456-459 "Float [j.8 hold]", "Float [j.9 hold]", :451-455 "Air Dash [j.66]"), and the conclusion that it now plays as native rests on a number alone ("6851/6851 frames identical"); no image or recording appears anywhere in the three artifacts.
Q3: VIOLATED — both legs are given the SAME schedule pokes (build/rulecheck/2026-09-21-89/a/audit_move_parity.sh.txt:88 `_base=...json.load(open('$_j'))['pokes']`, used on native at :98 and on ours at :101), and for huitzil_1 every event is "far"-pinned (name_moves.py.txt:448) so name_moves.py.txt:658 `pin_pokes += [f"{t - 40}:ff8410:{x1:04x}", ...]` writes P1's X on both legs 40 f before every event — and ff8410 is a COMPARED field (audit_move_parity.sh.txt:105 "ff8410:w:x"); likewise the shared id poke name_moves.py.txt:672 `ff8782:{tid}` and stock :681 `ff8509:09` write compared fields (:105 "ff8782:b:id", "ff8509:b:stock"), so a defect in position, id or stock is written identically into both legs' compared state and makes them AGREE.
Q4: VIOLATED — the gate declares only two controls (build/rulecheck/2026-09-21-89/a/audit_move_parity.sh.txt:61 "unpinned-level|no-translation"); neither fires on THIS claim's failure mode, that the agreement does not come from the flavor latch (nothing withholds or re-pokes $FF87C2, and FIELDS at :105 never samples it — the instrument sees a different set of bytes than the claim rests on), nor on the shipped `flavor_default` producing 0x01 at runtime rather than a poke; worse, on the very part claimed the frozen verdict is DIVERGES (move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f"), which makes no-translation self-declare dead (:195) and drives unpinned-level into its non-discriminating else branch (:203-208 prints "CONTROL FIRED" whenever base is not IDENTICAL). The claim names none of these gaps.
Q5: N-A — the packet credits no person with a decision: "so flavor_default becomes 0x01 and the five tracks are rebuilt and frozen as M19" names no approver, and the artifacts' only maintainer references are provenance/history, not approval of this action (audit_move_parity.sh.txt:8 "the maintainer's move lists", name_moves.py.txt:363 "the maintainer's ruling 14z-121").
VERDICT: VIOLATED

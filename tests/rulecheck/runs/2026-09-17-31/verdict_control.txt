Q1: VIOLATED — The claim says the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write)" tested "on the same write the poked leg performs"; the gate builds SELF as the real path plus the SAME id: audit_forced_pick_fidelity.sh.txt:27-28 "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)", :118 (non-poked legs take row_path), :135 (_pick uses the row's own $_id), :138 (self gets _pick). The changed-value write the poked leg performs (Donovan 0x13 -> 0x10/0x11 at 1400-1500) is therefore never isolated on any leg, and the claim names it as tested rather than as an untested premise.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — The only shared writes reaching the compared P1 block are the position pins (name_moves.py.txt:687 "{t - 40}:ff8410", copied into both legs by audit_forced_pick_fidelity.sh.txt:132), which land at >=2560, after the LATCHED window (audit_forced_pick_fidelity.sh.txt:180 "1501 <= f <= 2362"); the level and RNG pins (:133-134) are globals; the claim names the in-play X pin as a shared premise of the first-play-difference frame.
Q4: VIOLATED — Failure mode: the changed-value poke to RAM:$FF8782 at 1400-1500 itself perturbing or masking P1-block bytes (so a LATCHED byte is the poke's, not the confirm's). Neither control covers it: SELF is a same-value write (audit_forced_pick_fidelity.sh.txt:27-28, :135) and donovan-self is "poking 0x13 over Donovan's own confirm" (:48-49), also same-value. The claim does not name this gap; it states it is covered ("tested directly on the same write the poked leg performs"), and a control that sees a different write does not count as covering it. The same-leg (:310-316) and wrong-cursor (:318-322) controls cover reducer blindness and cursor identity, not this.
Q5: N-A — No decision, approval or ruling is attributed to any person in the claim; the gate's "#147 shipped a wrong fix" (audit_forced_pick_fidelity.sh.txt:12) and the TSV's "Frozen 14z-160 with FREEZE=1" (forced_pick_fidelity.tsv:8-9) are events, not attributed decisions.
VERDICT: VIOLATED

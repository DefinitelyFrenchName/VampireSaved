Q1: VIOLATED — The claim names three untested items but misses one more. Nothing shows the installed hook allowing a push of this repository against the real `tests/rulecheck/ledger.tsv` and run-dir `meta.tsv`. The hook reads those files by position, using a hard-coded column tuple, and it skips the header line (tools/agent/hooks/pre_push.py:90-98, :93, :103). The one live allow went to a non-existent directory, where `git rev-parse` fails and `same_repo` returns False before the ledger is read (pre_push.py:69-70, :122-123). The one live deny happens whether the ledger is read correctly or not. STATE.md.lines-85-89:5 confirms this: "this push is the hook's first real one — results below". The claim also cites "failed that case 3 of 3 before installation", but no artifact shows that run: the pre-install proposal and its prove.py output are not in the packet. A stale carrier also remains: pre_push.py:3 still says "to be wired in `.claude/settings.json`" after the docstring fix recorded in STATE.md.lines-85-89:2.
Q2: N-A — No conclusion is drawn about behaviour a player could feel. The claim is about agent tooling: a hook, a gate and a transcript library.
Q3: VIOLATED — The hook and its 18 expected verdicts rely on the same picture of the ledger. The case file writes the ledger row the hook will read, in exactly the hook's assumed column order (tests/lib/pre_push_cases.py:55, "{rid}\t2026-09-23\t14z-176\tprocedure\ts\tproc-x\t{caught}\t{verdict}\t...") and a `head` line in the hook's assumed form (pre_push_cases.py:54). The hook then uses those same fields to choose allow or deny (pre_push.py:95-104). The hook ignores the header (pre_push.py:93), so if the real ledger's layout differed, the hook and its cases would still agree 18/18 while the live hook misread every real run.
Q4: VIOLATED — Control `open-push-hook` (tests/test_agent_hooks.sh:22, :70-78, :167-171) covers only an always-allow hook on the 7 must-deny cases. No control covers the hook misreading the real ledger or meta.tsv, which would deny every checked push of this repository or wrongly count a run. Every must-allow case runs on a ledger the case file writes itself (pre_push_cases.py:41, :54-55). The claim does not name this gap: its three stated limits are the byte-identity check, the non-existent-directory allow, and the scope limits.
Q5: OK — Each decision is quoted in the maintainer's own words: STATE.md.lines-85-89:1 "The agentlib.py fix … -> done"; DECISIONS_HISTORY.md.lines-30-48:13 "well, since we both think the push hook is a good idea, I'll install it", then "it should be done"; STATE.md.lines-85-89:2 "done" (the docstring); STATE.md.lines-85-89:5 "push and close".
VERDICT: VIOLATED

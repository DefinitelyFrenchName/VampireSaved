Q1: VIOLATED — The claim names four untested premises, but it rests on at least two more that it does not name. First, it assumes every reader in the ledger before 2026-09-24-114 was a general-purpose worker. build/agent172/pkt178/context_census_14z178_afternoon.txt lines 13-20 count workers by type and never link one to a ledger run; the Explore/Plan/fork rows on lines 1-12 show "context -". Second, it assumes "all twenty calibration readers (rulecheck.py spawned reads their own transcripts)" passed that check. No `spawned` output for runs 2026-09-24-114..133 is among the artifacts. Only the working agent's own doc says so: docs/project/rule_checker.md.lines-277-306 line 25, "Every reader of both rounds was verified from its OWN transcript".
Q2: N-A — The claim draws no conclusion about how a move plays, its timing, a float, a hold or a hit. It concerns reader context, calibration and transcript retention.
Q3: OK — The two legs, general-purpose against the pinned rule-checker, came out DIFFERENT (build/agent172/pkt178/memory_probe_evidence.txt line 5 "['Project:CLAUDE.md', 'AutoMem:MEMORY.md']" against line 18 "instructions attachment files: NONE"). A defect they share would have made them agree, so none is shown. The one shared tool that could not tell the two definitions apart is that `spawned` compares only model, effort and context (tools/rulecheck.py.lines-555-720 line 62), and both rounds ran claude-opus-5-5. The claim itself names that as untested: "whether this session's harness reloaded the edited definition".
Q4: VIOLATED — Failure mode neither controlled nor named: a real run is recorded while its readers fell back to another model or carried an instructions attachment, because `spawned` was never run or its failure was ignored. The claim's reason (1) for closing #158 says "every run's readers are checked from their own transcripts". The artifacts show only a subcommand that exits 1 (tools/rulecheck.py.lines-555-720 lines 165-166 "if bad or missing: sys.exit(1)") and a documented order, "The run: `prepare` -> spawn -> `collect` -> `spawned` -> `record`" (docs/project/rule_checker.md.lines-74-90 lines 12-13). No artifact shows `record` refusing a run without a passing `spawned`. This risk is not hypothetical: the pinned version id fell back in the probe (build/agent172/probe_agents_14z178d.log line 14 "ran=claude-opus-4-8 FALLBACK claude-opus-5-5->claude-opus-4-8").
Q5: VIOLATED — #158, which the claim recommends closing, and agent_defs.py both attribute a rule to the maintainer's 2026-09-17 ruling: build/agent172/pkt178/issue_158.txt line 3 "the 2026-09-17 ruling (14z-163) says 'a change of model is a recalibration'"; tools/agent/agent_defs.py lines 53-54. The maintainer's verbatim words are DECISIONS_HISTORY.md.lines-879-900 lines 9-11: "I am fine with default model. We can always recalibrate later if we need, right?" That sentence does not contain the rule. The phrase comes from the working agent's own "What it means" gloss (line 19). The 14z-177 rulings the claim cites are quoted verbatim ("Pin it now (Recommended)", "Subagents (Recommended)").
VERDICT: VIOLATED

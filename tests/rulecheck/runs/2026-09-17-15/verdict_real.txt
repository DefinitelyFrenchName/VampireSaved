Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but build/rulecheck/2026-09-17-15/b/audit_move_parity.sh.txt:34-36 lists what the gate does NOT cover ("per-hit damage, meter gain, hitboxes and projectile parameters — separate instruments. P2 is Victor ... his state is never compared"), line 25 says "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor" (the intro is excluded, so not "the whole part"), and line 105 shows only twelve fields are compared, RAM:$FF87C2 not among them, so "frame-exact" is field-exact; further, no artifact shows the "every consumer ... enumerated" pass or the 0x01 poke to $FF87C2 (pokes_for, lines 86-103, and name_moves.py.txt:669-682 emit only ff8782/ff8850/ff8410/ff8810/ff8509/ff8116/ff80d4), and build/rulecheck/2026-09-17-15/b/move_parity.tsv:20 freezes huitzil_1 as "DIVERGES	Jump [8]+21f", not IDENTICAL.
Q2: VIOLATED — The claim concludes how Phobos's part-1 moves (movement, normals, air dash, float; name_moves.py.txt:448-460) play under the 0x01 flavor and moves a shipped variant-selecting byte on that basis, yet the only evidence is a number ("6851/6851 frames identical"); the instrument emits a TSV verdict line (audit_move_parity.sh.txt:133-135) and no artifact shows an image or recording put before the maintainer.
Q3: VIOLATED — Both legs run the same generated rig and pokes: audit_move_parity.sh.txt:88 takes `_base` from the shared schedule JSON for both legs and lines 89/98/101 pin ff8116 on both; name_moves.py.txt:658 writes `ff8410` (P1 x) 40 frames before every "far" event on both legs, and line 105 compares exactly those bytes ("ff8410:w:x", "ff8116:b:lvl", "ff8509:b:stock"), so a shared pin writes compared state on both legs and a defect in it makes them agree.
Q4: VIOLATED — The uncontrolled failure mode is the flavor byte being irrelevant to the verdict (IDENTICAL with 0x01 poked would also read IDENTICAL with 0x00 if part 1 never consumes $FF87C2); the gate's only controls are "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:4-5, 181-208), neither withholds or perturbs $FF87C2, and both are DEAD for huitzil_1 by construction because line 189-196 requires the control part's frozen verdict to be IDENTICAL while move_parity.tsv:20 freezes it DIVERGES; the claim names no gap ("nothing further").
Q5: N-A — The packet attributes no decision, approval, ruling or closure to the maintainer or any person; the build decision is stated as the working agent's own.
VERDICT: VIOLATED

Q1: VIOLATED — tools/name_moves.py:421 pins donovan part 12's first event "far", and :714/:737-740 shift a part only when `_short = ROUND_START - (FIRST_EVENT - _lead)` is positive (far: 2545-2560 = -15), which the file states outright at :697 ("a 'far' first event pins at FIRST_EVENT-40 = 2560, already past it, so those 35 parts do not move") and :693 names the seven parts #168 did move — donovan_12 is not one; so the killshread_es rig was NOT shifted, the claim's "DF=195 schedule shift" cannot reach it, and the one 14z-171/172 change that does (the per-event stock poke, :673 + :802) carries an inertness argument at :799-801 resting on test_move_naming.sh, which skips part 12 by construction (:671) — an untested premise the claim neither names nor tests, while DECISIONS_HISTORY.md:297 records the poke's ratified cost as "176 stock samples excluded from the parity comparison", not moved damage or reaction classes.
Q2: VIOLATED — the claim concludes hp -4→-3, white -6→-5 and cls 0x01→0x16 are a rig-state artifact ("the shifted rig meets the victim in a different state") and states "No capture was taken of any of it", yet test_killshread_es.sh:2-5 makes the gate's subject a maintainer ruling about how the move plays ("the returning Killshread attacks going away AND coming back"), and a knockdown class and per-contact damage are what a player feels; the project's own practice puts the capture first (DECISIONS_HISTORY.md:364, 374).
Q3: VIOLATED — both compared legs (the frozen M19 rows and the restored-rig re-run) execute the same rig poke list, and the artifacts show it WRITES the compared state: tools/name_moves.py:766 pokes `{f}:ff8850:01200120` and :802 pokes `{frame-60}:ff8509:09`, while test_killshread_es.sh:50 samples exactly `ff8850:w:p2hp,ff8852:w:p2white,...,ff8509:b:stock` and :69-71 build every frozen row's `hp=`, `white=` and `stock=` from those bytes — so the `stock` column is the rig's own poke read back, and a defect in the pin phase yields identical wrong deltas on both legs; same shape for defense_row_reads, whose tapped/compared window `RTAP="ff8782,2;ff8b82,2"` (audit_defense_row_reads.sh:68) is poked directly by tools/name_moves.py:768,770.
Q4: OK — audit_reaction_class_live.sh:225 and audit_defense_row_reads.sh:230 refuse FREEZE unless `fail=0` with all legs and no control mode, so planted-38 / planted-flavor / range-silent and the substantive checks must have fired correctly before any re-freeze is written; test_killshread_es.sh:45 (the rig equals a regeneration — the check that caught the claim's own confounded first attempt) plus the exact diffs at :75, audit_reaction_class_live.sh:240 and audit_defense_row_reads.sh:246 would fire on a second, non-rig cause behind the moved rows; the residual — that each new value is what the engine should produce — the claim names as untested.
Q5: N-A — the packet attributes no decision, approval, ruling or closure to the maintainer or any person (it says only that the rows "may be RE-FROZEN" and that "the mister lane is being carried forward on tools/audit_lane_carry.py's evidence"); the maintainer quotes in the artifacts (e.g. DECISIONS_HISTORY.md:301, audit_defense_row_reads.sh:7) are the gates' subjects, not attributions the claim makes.
VERDICT: VIOLATED

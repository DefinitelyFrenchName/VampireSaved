Q1: VIOLATED — the claim says the SELF leg is "a poke of a DIFFERENT id (a changed-value write)", but audit_forced_pick_fidelity.sh.txt:27 defines it as "a write at 1400-1500 to an id that already holds that value" and mkleg builds it that way (lines 118 + 135: the self leg takes row_path, which confirms row_id, then pokes that same row_id); the poked leg's write changes 0x13 → 0x10/0x11, so the unnamed untested premise is that a value-CHANGING id write at 1400-1500 is inert.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here." and "whether any latched byte is READ in play is a separate tap, not this gate."
Q3: VIOLATED — both legs copy the rig's own pokes (audit_forced_pick_fidelity.sh.txt:131-132, `_base`), and one of them writes INTO the compared region: name_moves.py.txt:687 `pin_pokes += [f"{t - 40}:ff8410:{x1:04x}", …]` pins P1's X at P1-block offset +0x010 identically on both legs, while classify's first_play (audit_forced_pick_fidelity.sh.txt:197-198) ranges over every non-latched P1 offset including that one — and the pin is a SINGLE-frame poke per event (contrast the every-frame level pin at line 133), so the claim's "it is a first difference outside the pinned field" is not what the gate does; the frozen play column (huitzil 2980, pyron `-`) rests on that shared equalising write.
Q4: VIOLATED — the failure mode "the three latched bytes are written by the value-changing id poke itself, not by Donovan's confirm" is neither controlled nor named: the gate's control for it is REAL vs SELF (lines 281-282, "the poke mechanism itself is inert"), but SELF performs a same-value write (line 27) and the donovan-self row is likewise a same-value poke over its own confirm (row_path "R R" = id 0x13, line 89/87), so no leg anywhere performs a value-changing poke on a correctly-confirmed fighter; the claim asserts the opposite ("tested directly on the same write the poked leg performs").
Q5: N-A — no decision, approval, ruling or closure is attributed to any person; the packet proposes one ("report #151 step 1 as done — what a forced-pick native leg gets wrong is measured").
VERDICT: VIOLATED

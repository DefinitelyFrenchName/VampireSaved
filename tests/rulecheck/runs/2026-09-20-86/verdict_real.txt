Q1: VIOLATED — the claim's untested list omits (and the artifacts refute) that its two "corrections" are new ours-vs-native divergences: build/rig172/move_parity_events.head.tsv:493,518 read `pyron_3 0 Corona Whip [6MP] IDENT` and `pyron_5 0 Planet Burning [MP] step back (control) IDENT`, while build/rig172/move_parity_events.fix195.tsv:493,518 read both as `DIFF meter`; likewise 5 of the "15 x-only rows gone" are still DIFF under another field (head:348 `x` → fix195:348 `meter`; head:394 `x` → fix195:394 `p1hp`), so 102→94 is 10 rows converged, 5 relabelled and 2 newly divergent, not 15 removed.
Q2: VIOLATED — the claim concludes what two inputs fire ("both corrections of events that had been recording a WALK as their named move", re-frozen at build/rig172/move_naming_fix195.log:29-30 and :38-39) and states its own basis as "supported by their agreement with sibling rows in the same expectation file and by the stock discriminator, not by a capture"; the only capture named anywhere in the packet is tests/audit_rig_opening.sh:17's 14z-167 entrance sheet, which is of huitzil_5's opening, not of these two events.
Q3: VIOLATED — the compared field IS the poked address: tools/name_moves.py:737 emits `{t-230}:ff8410:` / `ff8810:` from PIN (tools/name_moves.py:669) into the schedule's one poke list, tests/audit_rig_opening.sh:132-135 hands that same `$pk` to both the native and the ours leg, and line 89 samples `FIELDS="ff8410:w:x,..."` — so a defect in the pin writes both legs' x identically; sample 2565 is the pin frame itself and is inside the convergence assertion (tests/audit_rig_opening.sh:189, rows tests/expected/rig_opening.tsv:15 and :25, both `x=552`, the poked value).
Q4: OK — the shift sweep is the control for this claim's failure mode (a shift that lands an input-window-edge event on a different double-pass phase fires a different move): four alternative shifts re-run through the whole-corpus naming gate, which fires on them (build/rig172/naming_ctl192.log:21-24 and naming_ctl194.log:69-71 move rows 195 leaves still, build/rig172/naming_ctl273.log:37-38 fires the own-name check), plus the must-fire controls at build/rig172/tick_phase.log:10-11 (flat-pass, quantum-off) and tests/audit_rig_opening.sh:222 (pin-before-round); the residual — why divisibility by 39 rather than 3 — is named untested by the claim.
Q5: N-A — no decision, approval, ruling or closure about this shift, this quantum or the six re-freezes is attributed to any person; the packet's only maintainer attributions are prior rulings cited to an archive (tools/name_moves.py:85-86 "maintainer-ruled 2026-09-17, DECISIONS_HISTORY.md ...") and an identification against a named capture (tests/audit_rig_opening.sh:16).
VERDICT: VIOLATED

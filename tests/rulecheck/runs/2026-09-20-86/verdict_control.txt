Q1: VIOLATED — claim: "Not tested: nothing further — the comparison is frame-exact over the whole part", but build/rulecheck/2026-09-20-86/a/audit_move_parity.sh.txt:25 ("THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor") and :34-36 ("WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters... P2 is Victor... his state is never compared") show the part is neither whole nor frame-exact, plus :14 defers further exclusions to tools/move_parity.py, which is not in the packet.
Q2: VIOLATED — the conclusion covers player-feel behaviour (build/rulecheck/2026-09-20-86/a/name_moves.py.txt:456 `("Float [j.8 hold]", ...)` in HUITZIL part 1; move_parity.tsv:20 `huitzil_1 DIVERGES Jump [8]+21f`), yet the only evidence offered is a count ("6851/6851 frames identical") and no artifact shows any image or recording put before the maintainer.
Q3: VIOLATED — both legs run the same rig json's `_base` pokes (audit_move_parity.sh.txt:88, emitted to native at :98 and to ours at :101), and those base pokes include the character-select write `ff8782` (name_moves.py.txt:672, huitzil id "10" at :67) while the level pin writes `ff8116:06` on both legs every frame (:89); both addresses are themselves compared fields (:105 `ff8782:b:id`, `ff8116:b:lvl`), so a defect in the forced-tenant or speed path is written to the same value on both legs and the compared state AGREES.
Q4: VIOLATED — the gate declares only two controls (audit_move_parity.sh.txt:4-5, unpinned-level and no-translation) and REFUSES any other (:62), and FIELDS (:105) samples no `ff87c2` at all; nothing fires on the failure mode that the IDENTICAL verdict is produced by something other than the flavor byte (no leg re-run with the $FF87C2 poke withheld), nor on the flavor_default build change perturbing legacy/vanilla content in the M19 freeze — neither mode is named as untested by the claim.
Q5: N-A — the packet attributes no decision to a person: "so flavor_default becomes 0x01 and the five tracks are rebuilt and frozen as M19" names no approver.
VERDICT: VIOLATED

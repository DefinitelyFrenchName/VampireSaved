Q1: VIOLATED — The claim treats its no-capture list as complete: "(dd) and (ee) are RAM findings with no capture, and so is (q)'s Pyron half". It leaves out (g), whose home says "at level 6 every length moves and Pyron's j.HP whiffs" (docs/game/engine_internals.md:1016), and the damage figures of (r) and (t) (12 vs 13; 12 on vsavj, 11 on vs2). None of these is among the five captures sent (docs/NEXT_SESSION.md:27-30). The claim also presents "#174 (its repro now `tools/chain_census.py`, promoted)" as a reproduction without saying the promoted script was never re-run. No artifact shows a run of it, and it reads an input "kept OUT of the tree under ../charpages/framedata/" (tools/chain_census.py:3).
Q2: VIOLATED — The claim restates conclusions about hits a player can feel: "pyron_3's event 5, named "far (whiff)" by the rig, in fact connects", and for (dd) both Galactic Throw strengths throw ("both lines a2:0x23"). It also concedes "(dd) and (ee) are RAM findings with no capture, and so is (q)'s Pyron half (Zodiac Fire)". None of these is among the five captures sent (docs/NEXT_SESSION.md:27-30).
Q3: N-A — The packet's own conclusion is about documentation and compares no two legs. The ours-vs-native comparisons it restates belong to their own gates. The one pair it runs, the real tool against its `--blind` variant, is built to disagree, and the gate reads DEAD if both pass (tests/test_close_tools.sh:60-61).
Q4: VIOLATED — The ticket read's build/ branch has no control. That branch is the check that caught #175 and #174 (tools/homes_tracked.py:89 "elif re.search(r\"\bbuild/(?!manifest)\", tickets[n])"). The self-test plants only a ticket with no index row (#0) plus the first real ticket, which is clean. Its `ticket_ok` checks only "ticket-not-in-index" and a TICKET line, and it expects fail == 6 with no "ticket-row-cites-build/" plant (tools/homes_tracked.py:136-145). `--blind` does not touch this branch either (tools/homes_tracked.py:51,64,97). So if the branch went dead, a ticket row citing build/ would pass and test_close_tools would stay green. The claim nonetheless says "as are the ticket and prose-document reads" planted, and does not name this gap. The read also does not check non-build/ files a ticket row cites, such as tools/framedata_pages.sh on #174's row (docs/project/tickets.tsv:75), or out-of-tree inputs. This is not named either.
Q5: N-A — The claim attributes no decision, approval, ruling or closure to anyone. It says "the maintainer's read is theirs" and "awaiting the maintainer's read", and (t)'s recommendation sits in STATE "Decisions pending" (docs/NEXT_SESSION.md:22-27).
VERDICT: VIOLATED

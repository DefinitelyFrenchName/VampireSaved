Q1: VIOLATED — The claim says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)". The gate pokes the SAME id: audit_forced_pick_fidelity.sh.txt:135 writes `_pick` with `_id="$(row_id "$_row")"`, the tenant's own id; lines 136-138 apply `_pick` to the self leg; and lines 26-27 say "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". So "the poke mechanism is inert on a changed-value write" is presented as tested, but it is an untested premise the claim does not state. A second unstated premise: the gate copies pokes from the frozen rig JSON ("tests/replays/naming/$_rig.json", line 115, read at 131-132), not from name_moves.py. The claim describes the pokes from the generator and never states that the JSON matches it.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — The shared pokes do not reach the P1 block inside the pre-match window. The position pins land at t-40 (name_moves.py.txt:687), which is 2560 at the earliest (FIRST_EVENT 2600). The HP pin at $FF8850 starts at 2550 (name_moves.py.txt:699). The level and RNG pins are globals (audit_forced_pick_fidelity.sh.txt:133-134). The window is 1501-2362 (line 180). The claim names the pinned P1 X ($FF8410) in play itself.
Q4: VIOLATED — The poke-mechanism controls are the donovan-self row (forced_pick_fidelity.tsv:17) and the REAL-vs-SELF check (audit_forced_pick_fidelity.sh.txt:281-282). Both are same-id writes: "an id that already holds that value" (line 27). Neither would fire if the latch came from a changed-value write to $FF8782 at 1400-1500 (the game reacting to the id change) rather than from the confirm. That is exactly how "charged to the confirm" could be wrong. The claim does not name this gap as untested; it claims the gap is covered.
Q5: N-A — The packet attributes no decision, approval or ruling to anyone.
VERDICT: VIOLATED
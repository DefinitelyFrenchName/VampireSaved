Q1: VIOLATED — Some §1 figures trace to no worker figure or re-derivation in build/gatequal180/measurer_returns_14z180.txt, and the claim does not list them as untested: docs/project/gate_qualification_scope.md:37 "nine session closes, every one GREEN", :39 "the gate alone takes 234 s under `--jobs 4`, the control 210 s standalone", :65 "the other 185 take the runner's 5,400 s default" (the orchestrator's note at measurer_returns_14z180.txt:42 re-derives only the 14/185 column counts, not 5,400). The returns artifact records only FIG lines keyed to C-numbers, not "a numbered worker command recorded verbatim", and it abridges entries itself (lines 16, 32, 45). §2 says "the 136 files it covers" (gate_qualification_scope.md:99), but the figure is B/C16 "provenance_noncomment_lines = 136" (measurer_returns_14z180.txt:35), and tests/expected/PROVENANCE.md.lines-55-70 lines 1-6 show non-comment lines that are prose and table header/separator, not files. The claim's line "every one resolves today" also rests on an untested premise it does not name: that the extractor finds the reference the way vanilla_join_rig.py actually wrote it (see Q4).
Q2: N-A — no conclusion is drawn about behaviour a player could feel; the scope document gives census counts and proposals only.
Q3: OK — the only comparison is worker C5 "13" against the orchestrator's re-run giving "14" (measurer_returns_14z180.txt:42), and they disagree. The agreeing leg (C6's 14-name list, line 43) reads the registry that is the measured object. No artifact shows a shared rig, poke or tool writing or selecting the compared state.
Q4: VIOLATED — The control that §1 and slice Q1 lean on does not exercise the reference-extraction pass. tools/audit_module_refs.py:132-133 "if a.plant: refs.append((\"tools/vanilla_join_rig.py\", \"name_moves\", \"PROLOGUE\", 0))" adds the reference AFTER references() has parsed the files. It proves the resolve step only, not that the AST walk detects a reference written like the historical one. Yet gate_qualification_scope.md:71-72 concludes "a static resolve check would have caught the PROLOGUE case on the session it happened". The census also has no floor on the reference count: tools/audit_module_refs.py:154 prints "PASS: every one of {len(refs)} cross-module references resolves" even when that count is 0, the same empty-measurement shape as 1b. Neither failure mode is controlled or named in the claim.
Q5: OK — The one maintainer decision attributed is the ask, and it is the maintainer's own statement quoted verbatim: build/gatequal180/ruling_14z174.txt:20-22 "Option 1 and ticket to qualify all gates in the future because either the gates are moving silently or they never were validated and in both cases that's a lot of both uncertainty and wasted time". It matches docs/project/gate_qualification_scope.md:20-22 word for word.
VERDICT: VIOLATED

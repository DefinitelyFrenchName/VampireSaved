Q1: VIOLATED — The ticket index row for #173, which the claim cites as a home of (i), still carries the old sheet count and points to a heading that no longer exists. Its decided answer reads "DECISIONS_HISTORY.md § Ruled 2026-09-25 (14z-181) — the maintainer's reads of the eight capture sheets" (docs/project/tickets.tsv.lines-74-90:3). The real heading is "the maintainer's reads of the capture sheets: every one identical; ..." (DECISIONS_HISTORY.md.lines-28-48:3). The claim says run 200's count was "corrected in STATE, the opener, the standing ruling and DECISIONS_HISTORY's heading", which leaves this tracked carrier out. The claim also says the ticket read requires each row's answers to "cite no build/ path and no untracked file". But tools/homes_tracked.py:102 only matches paths that start with "../", "build/", "docs/", "tests/" or "tools/". So the read never sees a root-level document such as DECISIONS_HISTORY.md or its § anchor, and the claim does not name that gap for ticket rows. Separately, "(the record says so per sheet)" is not borne out: build/agent181/captures_sent_14z181.txt:4-18 (ten sheets) say nothing about AFTER, and line 8's caption says "before any freeze".
Q2: VIOLATED — The packet restates conclusions a player could feel: (g) "a rig tuned at level 8 whiffs at 6", (ee) "pyron_3's event 5 ... in fact connects", and the +1 damage in (r)/(t). Every capture went out after its conclusion: "AFTER the RAM measurement that found it, not before" (build/agent181/captures_sent_14z181.txt:20, and lines 21-24 the same way) and "AFTER the measurement (the naming freeze of a91a555a)" (line 39). Some have no capture at all, as the claim itself says: "still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t)".
Q3: N-A — The packet concludes where findings live and which tests exist; it draws no two-leg comparison of its own. The one pair it runs is built to disagree: the blind variant prints "SELFTEST FAIL: (False, False, True, True) stem=True ticket=False prose=True fail=6" (build/agent181/test_close_tools_mode_build-home-planted.log:8), while the real tool prints "SELFTEST PASS" (build/agent181/test_close_tools_14z181.log:6).
Q4: OK — Each way the claim could fail is either controlled or named. An untracked or build/ home: `build-home-planted` fires (build/agent181/test_close_tools_14z181.log:14), and the live `--newest` row reads "FAIL 0" (line 8). A grep that does not reach the tree: `unreachable-control`, `reach-threshold` and `gone-reintroduced` fire (lines 10-12). A dropped case-fold: `case-fold-dropped` fires (line 13). Each mode fails on the plant's own text (tests/test_close_tools.sh:62-69,82,92; the four mode logs at lines 4, 6 and 8). A crash is not read as a fire: "REFUSED ... exit code: 3" (build/agent181/close_tools_crash_probe_14z181.log:5-6). The claim names the rest as its own reading: the tool "checks FILE NAMES only" (sections and anchors are left to reading), whether a live carrier's figures agree, whether the table and the 31 patterns are complete, and whether a cited test exercises its finding.
Q5: OK — Every attribution quotes the maintainer's own words. #173: "yes, identical." (DECISIONS_HISTORY.md.lines-28-48:9). (j): "identical and validated otherwise" (line 10). #169: "looks identical. I'll double check the air throw on my end but everything looks good" (line 14). The first read: "correct" (line 6). The trap caveat (line 13). The model switch: "I had to switch to Opus 5.5. Resume the work." (STATE.md.lines-72-101:3).
VERDICT: VIOLATED

Q1: VIOLATED — The claim says "Not tested: nothing further", but the gate lists its own gaps: audit_move_parity.sh.txt:34 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters". It also compares only the FIELDS at :105 (the flavor byte ff87c2 is not among them), and starts the window "AT THE RIG'S FIRST EVENT, not at the anchor" (:25), so the claim's "over the whole part" is not true. Part 1 is movement and normals only (name_moves.py.txt:448-460), so none of Phobos's specials or meter moves (parts 2-10) are covered. pokes_for (:86-103) has no way to poke $FF87C2, the gate prints a verdict per part rather than a frame count, and the only frozen row, move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f", does not show "6851/6851". Nothing in the artifacts shows that "every consumer was enumerated"; none of these premises is named.
Q2: VIOLATED — The claim concludes that Phobos's part-1 moves, including air dashes and floats a player feels (name_moves.py.txt:451-459), play "identical to native", and it rests on a number alone ("6851/6851 frames"). None of the named artifacts is or refers to an image or recording shown to the maintainer.
Q3: VIOLATED — Both legs take the same poke list from the rig's json (audit_move_parity.sh.txt:88 `_base=...['pokes']`, used on both legs at :98 and :101). That list includes name_moves.py.txt:658 `pin_pokes += [f"{t - 40}:ff8410:{x1:04x}", ...]`, and every event in huitzil part 1 is "far" (:448). ff8410 is a compared field (audit_move_parity.sh.txt:105 "ff8410:w:x"). The level pin `f'{f}:ff8116:06' for f in range(2000,$_fr)` (:89) writes "ff8116:b:lvl" every frame on both legs, and that is also a compared field. So the shared rig writes the compared x and level state and forces the legs to agree.
Q4: VIOLATED — The gate's only controls are unpinned-level and no-translation (audit_move_parity.sh.txt:4-5). Neither changes $FF87C2, and neither shows that a runtime poke of the latch equals a build with flavor_default=0x01, which matters if something reads the byte before the poke lands. The controls run on the first part in SET (:182). Because huitzil_1 is frozen DIVERGES (move_parity.tsv:20), running the no-translation control on it gives "CONTROL DEAD: no-translation — the control part $CTL is not frozen IDENTICAL, so the control cannot discriminate" (:195). The claim does not name this failure mode (the poke not matching the build, or the byte acting on fields and parts the gate does not compare).
Q5: N-A — The packet credits no decision, approval or ruling to the maintainer or anyone else; the working agent states "flavor_default becomes 0x01" as its own action.
VERDICT: VIOLATED

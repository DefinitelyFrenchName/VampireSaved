THE PACKET

Decision kind: expectation
Subject: 14z-169: after run 2026-09-18-51 - reaction_classes and defense_rows_census re-frozen with the constant writers on ours, the build identity and the table-reader rows; df_modes re-frozen with the EX-route ruling quoted
Claim (the working agent's sentence): Three expectation files were re-frozen with FREEZE=1 after rule-checker run 2026-09-18-51 and reproduced exactly by a run without FREEZE, every declared must-fire control fired in-gate and every CONTROL mode exited 1 (the logs named): reaction_classes (now also every constant move.b #imm,$54(An) in our whole 6 MB verify_op.bin, none writing 0x38 and two placed stager windows writing 0x52, and one ours build row: the build directory's whole-set fingerprint resolved through tests/expected/registry.tsv to merged-m18), defense_rows_census (now also reader rows: every absolute long equal to a defense table's base and every pc-relative lea/pea landing in one, over vsavj's and vs2's opcode views below 0x100000 and our whole image — exactly the two host reads on vsavj and ours, with a new reader-planted control) and df_modes (rows unchanged; its header now quotes the EX-route ruling); the maintainer's words for the two rulings the gate headers name are quoted from DECISIONS_HISTORY.md; the statement that no record on merged-m18 carries class 0x50-0x53 rests on tools/audit_fsm_census.py, whose node signature bounds it; NOT tested or not established: a table base computed at run time, a (d8,pc,Xn) form or a pointer in data (named in both defense gates' headers as uncovered); the constant-write census sees only move.b #imm,$54(An), not register writes or other immediate forms (clr.b is a separate form it does not list); whether the two placed 0x52 writers are reachable by any path the corpora do not run beyond the fsm census's signature; the reference views' scan stops at 0x100000; FBNeo was not run; no conclusion about how a move feels is drawn (the guard lists are stated as code facts, their play reading labelled unmeasured).
Artifacts (read every one, in full):
  - tools/audit_reaction_classes.py
  - tests/test_reaction_classes.sh
  - tests/expected/reaction_classes.tsv
  - build/rc169/static_freeze2.log
  - build/rc169/static_verify2.log
  - build/rc169/static_ctl2_stager-38-copy.log
  - build/rc169/static_ctl2_record-06-to-38.log
  - tests/test_defense_rows_census.sh
  - tests/expected/defense_rows_census.tsv
  - build/rc169/defcensus_freeze.log
  - build/rc169/defcensus_verify.log
  - build/rc169/defcensus_ctl_tenant-row-moved.log
  - build/rc169/defcensus_ctl_reader-planted.log
  - tests/audit_df_modes.sh
  - tests/expected/df_modes.tsv
  - build/rc169/dfmodes_freeze2.log
  - build/rc169/dfmodes_verify2.log
  - build/rc169/dfmodes_ctl2.log
  - tests/audit_defense_row_reads.sh
  - tools/audit_fsm_census.py
  - build/rc169/fsm_census.log
  - tests/expected/registry.tsv.lines-165-165 (lines 165-165 of tests/expected/registry.tsv)
  - DECISIONS_HISTORY.md.lines-46-79 (lines 46-79 of DECISIONS_HISTORY.md)

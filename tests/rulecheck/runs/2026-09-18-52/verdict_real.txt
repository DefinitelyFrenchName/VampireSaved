Q1: VIOLATED — the NOT-tested list leaves out premises that the artifacts show: (1) tools/audit_fsm_census.py:142-147,155 scans only the VS2-tagged rows of the build's patch/atlas_fragment.md, so "no record on merged-m18 carries class 0x50-0x53" is limited by provenance region as well as by the node signature the claim names; (2) the reader census's absolute arm matches only a table's exact base (tests/test_defense_rows_census.sh:81-84), and its pc-relative arm matches only lea/pea (line 87), so "exactly the two host reads" also rests on no absolute long naming an address inside a table and no (d16,pc) operand of any other instruction; neither is among the named gaps (run-time base, (d8,pc,Xn), pointer in data); (3) no artifact shows the "two placed stager windows": imm_writers is a raw byte match at every even offset of the whole image, code and data alike (tools/audit_reaction_classes.py:186-189), and tests/expected/reaction_classes.tsv:192 gives only "0fff50/a1,4716dc/a1"; (4) the frozen build fingerprint "1d8bedc5" (tests/expected/reaction_classes.tsv:211) is not merged-m18's whole-set key 00f9cf13…: tests/expected/registry.tsv.lines-165-165 names it as the program key and says "the program key 1d8bedc5 is shared with build/merged1"
Q2: N-A — the claim draws no conclusion about how a move feels ("no conclusion about how a move feels is drawn"), and the one play reading in the artifacts is labelled unmeasured: tools/audit_reaction_classes.py:40-41 "How that plays (a low) is a reading of the code: no 0x38 hit against a guard was run and nothing was captured."
Q3: OK — the only legs compared are the FREEZE=1 run and the run without FREEZE, the same static tool on the same inputs (build/rc169/static_freeze2.log:3-7 and static_verify2.log:3-7 print the same five sha1s; defcensus_freeze.log:2-4 matches defcensus_verify.log:2-4), and the claim concludes only that the rows reproduced; the vsavj and ours reader legs share the table bases (tests/test_defense_rows_census.sh:77 TAB["ours"] = TAB["vsavj"]), but if our build had moved a table, the ours host-read rows at tests/expected/defense_rows_census.tsv:45-46 would disappear, so no artifact shows a shared premise forcing agreement
Q4: VIOLATED — (1) reader-planted, the only reader control, plants only "227c000b8940" (movea.l #$000B8940,a1, tests/test_defense_rows_census.sh:66), which tests only the absolute-long arm (lines 81-84); the pc-relative lea/pea arm (lines 85-90), which "every pc-relative lea/pea landing in one … exactly the two host reads" also rests on, has no plant, so a lea/pea arm that matches nothing would pass unchanged, and the claim does not name that gap; (2) the fsm census's "total out-of-range node-state bytes: 0" (build/rc169/fsm_census.log:3) has no control and no count of regions or node runs walked, so an empty VS2-region list from patch/atlas_fragment.md (tools/audit_fsm_census.py:142-147) would give the same 0; the claim names the signature bound, not a census that walked nothing
Q5: OK — the class-0x52 ruling at tests/test_reaction_classes.sh:2 is verbatim DECISIONS_HISTORY.md.lines-46-79:10 "then I'm all for fixing. Once again, as long as we don't introduce noticeable lag and we don't break more things, it's a pure win/win"; the EX-route question and its "agreed" (tests/audit_df_modes.sh:20-22, tests/expected/df_modes.tsv:5-7) are quoted at DECISIONS_HISTORY.md.lines-46-79:31; the defense-rows ruling that tests/test_defense_rows_census.sh:2 names is quoted in the maintainer's own words at tests/audit_defense_row_reads.sh:7-8 "given the measurements we should take the vs2 rows"
VERDICT: VIOLATED

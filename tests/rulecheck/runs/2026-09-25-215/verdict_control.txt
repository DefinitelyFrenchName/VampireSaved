Q1: VIOLATED — The claim says the SELF leg adds "a poke of a DIFFERENT id (a changed-value write)", but the gate does something else. audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Lines 135 and 138 poke the row's own `$_id` on the SELF leg. So the premise that the changed-value poke the POKED leg performs (Donovan 0x13 to the tenant id) does not itself latch P1-block state is not tested, and the claim presents it as tested instead of naming it.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — POKED and REAL share the rig's pokes (audit_forced_pick_fidelity.sh.txt:137-138). None of them reaches the P1 block inside the 1501-2362 window that defines LATCHED (line 180). The position pins land at t-40, so 2560 or later (name_moves.py.txt:687). The HP pin at $FF8850 starts at 2550 (name_moves.py.txt:699) and is in P2's block. The level pin $FF8116 and the RNG pin $FF80D4 are globals (audit_forced_pick_fidelity.sh.txt:133-134). The one shared write into the compared block is the in-play P1 X pin at $FF8410, and the claim names it.
Q4: VIOLATED — The failure mode that is neither controlled nor named: a changed-value write to $FF8782 at 1400/1450/1500 could itself make the game rewrite +0x3BD/+0x3C2/+0x3E0. Neither control writes a changed value. REAL vs SELF pokes the same id (audit_forced_pick_fidelity.sh.txt:26-28, 135, 272). donovan-self pokes 0x13 over Donovan's own confirm, which is also a same-value write (lines 48-51, 87). Both show only that a no-op write is inert, which is a different thing from the write the POKED leg makes. The same-leg and wrong-cursor controls (lines 4-5, 308-322) cover a vanished difference and a wrong identity, not this.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to the maintainer or any other person.
VERDICT: VIOLATED

Q1: VIOLATED — The claim treats build/m3b_merged22 as merged-m15's build dir ("its build dir build/m3b_merged22 remain the way back") and does not list that link as untested. The artifact does not support it. build/agent182/t132_measurer_return.txt:7 shows only that the directory exists: "FIG m3b_merged22_exists = yes (README.txt, gen.log, prg, rompath, patch, verify_*.bin, ...) <- C6". No line in the file ties that directory to freeze/merged-m15 or to 1101112c. The claim's named untested point ("whether build/m3b_merged22 still rebuilds byte-identical from its tag") assumes that link rather than naming it.
Q2: N-A — The claim draws no conclusion about how the game plays. It is about packaging only: "no gameplay is affected by any option".
Q3: N-A — Nothing is compared against anything. The file lists inventory reads C1–C9 and some document quotes (build/agent182/t132_measurer_return.txt:2-19).
Q4: VIOLATED — No control would catch the failure mode where build/m3b_merged22 is not the merged-m15 build, which would make the "way back" wrong. C6 (line 7) checks only that the directory exists and lists what is in it. No command checks it against the tag or commit (line 10: "freeze/merged-m15 -> 1101112c"), for example through its README or gen.log contents. The claim does not name this gap.
Q5: N-A — The packet makes a recommendation to the maintainer and attributes no decision to any person. The earlier recording is credited to a document, and the quote is given word for word: docs/NEXT_SESSION_HISTORY.md:4049 "superseded before release, recorded, not owed." (build/agent182/t132_measurer_return.txt:16).
VERDICT: VIOLATED

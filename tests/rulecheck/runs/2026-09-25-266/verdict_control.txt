Q1: VIOLATED — The claim treats one premise as tested when it is not. The claim says the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write)". The gate says the opposite: audit_forced_pick_fidelity.sh.txt:26-27 "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Line 135 writes `$_id`, which is row_id, the tenant the REAL path already confirmed (asserted at lines 174-179). So the premise that a changed-value id write leaves the P1 block alone is untested and not named in the claim.
Q2: N-A — The claim draws no behavioural conclusion: "No behavioural conclusion about how the character plays is drawn here." The frozen first-play frame (forced_pick_fidelity.tsv:15, "2980") is only a number.
Q3: VIOLATED — REAL and SELF share the cursor path, and the SELF poke writes back the id that shared path already put in the compared P1 block. audit_forced_pick_fidelity.sh.txt:135 writes "ff8782:$_id" with _id = row_id, and $FF8782 is P1 block +0x382. If the poke disturbs state only when the value changes, REAL and SELF would still agree, so "REAL vs SELF latches nothing" (line 281) is built to pass. POKED vs REAL share no poke that lands in the P1 block before the match: the position pins start at t-40 (2560 at the earliest) per name_moves.py.txt:687, and the level and RNG pins are globals (gate lines 133-134).
Q4: VIOLATED — No control covers this failure: a changed-value write to $FF8782 (the one the POKED leg makes, 0x13 -> 0x10/0x11) could itself write +0x3BD/+0x3C2/+0x3E0. If so, the three LATCHED bytes would come from the poke, not the confirm. Both would-be controls use a same-value write: SELF (gate lines 26-28 and 135), and donovan-self (gate lines 48-51, "poking 0x13 over Donovan's own confirm"; frozen "-" at forced_pick_fidelity.tsv:17). The claim does not name this gap; it says the gap is "tested directly on the same write the poked leg performs". The must-fire controls "same-leg" (line 310) and "wrong-cursor" (line 318) cover a vanished difference and the wrong identity, not this.
Q5: N-A — The packet credits no decision, approval, ruling or closure to the maintainer or any other person; it is the working agent's own recommendation.
VERDICT: VIOLATED

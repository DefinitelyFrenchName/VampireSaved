Q1: VIOLATED — build/agent183b/run50_clone.txt:1 and :52 show "porcelain(tracked)=1" in the clone Control B ran in, so one tracked path there differed from 12571807. No artifact names that path or shows that "only out-of-git inputs" were supplied. S1's "verified whole ... an empty tracked porcelain" (plan_153.md:36-37) was therefore never achieved in any run, and that premise is not in the "Not tested" list (plan_153.md:53-62). Also, the "9.2 s for 62,279 files" figure appears only as prose at plan_153.md:27, with no timing log among the artifacts.
Q2: N-A — the claim draws no conclusion about how a move plays, its timing or a hit; it is about test-tier isolation and snapshot cost.
Q3: VIOLATED — Control A and Control B both take their verdict from the same script: build/agent183b/run50.sh:11-12 "vs_classify "$st" "$OUT/$g.out" 58 "tests/$g.sh"" then prints "$VS_VERDICT". That script writes the compared state (PASS/SKIP/FAIL) in both legs, so if it classifies more leniently than tests/run_all_static.sh, both legs agree on PASS. The runner's own verdict is shown to depend on more than the exit code: build/agent183b/probe153_static.log:248 fails test_freeze_artifacts_current on "(controls RED: one-value-change(not fired) stale-dir(not fi)" while the gate itself printed "PASS: every romset-following tracked artifact is current" at :252. Also, B's inputs are copy-on-write copies of A's own files (plan_153.md:18-19).
Q4: VIOLATED — no control ties run50.sh's verdict to the runner's verdict for the same gate and the same inputs. The probe used tests/run_all_static.sh (plan_153.md:13), while A and B used run50.sh ("invoked as the runner does", plan_153.md:16, which no artifact shows). So the probe-FAIL against B-PASS change could come from the harness, not the inputs. Control A cannot catch this because it uses the same run50.sh. The claim does not name this failure mode: its "full tier in a snapshot" item is about "the other 128" gates (plan_153.md:54), not whether the harness gives the same verdicts.
Q5: OK — the claim attributes no decision to anyone. The one ruling the plan cites for session cadence (plan_153.md:51) is backed by the maintainer's own words quoted at plan_153.md:8-9: "my gut feeling is that the gain for session is marginal but the gain for freeze or release is massive".
VERDICT: VIOLATED

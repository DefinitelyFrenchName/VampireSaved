Q1: VIOLATED — The claim says the SELF leg is "a poke of a DIFFERENT id (a changed-value write)", but the gate gives SELF the tenant's own id. Line 135 of build/rulecheck/2026-09-25-166/a/audit_forced_pick_fidelity.sh.txt is "f'{f}:ff8782:$_id' for f in (1400,1450,1500)", where _id is row_id of the same row (line 114). The self and poked legs share the "*)" branch at line 138, and the header at lines 26-27 says SELF is "a write at 1400-1500 to an id that already holds that value". So whether a changed-value poke leaves the P1 block untouched is an untested premise the claim does not name; it presents it as tested.
Q2: N-A — The claim draws no conclusion about how anything plays: "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — No shared write reaches the compared P1 block before the match. The id poke is not shared: it is stripped from the REAL leg's pokes (build/rulecheck/2026-09-25-166/a/audit_forced_pick_fidelity.sh.txt:132, 137). The level and RNG pins write globals ff8116 and ff80d4 (lines 133-134). The position pins land at "{t - 40}:ff8410" with t starting at 2600, i.e. from 2560 (build/rulecheck/2026-09-25-166/a/name_moves.py.txt:687, 77). The HP pin writes P2's block ff8850 from 2550 (name_moves.py.txt:699). The pre-match window is "1501 <= f <= 2362" (audit_forced_pick_fidelity.sh.txt:180). The claim itself names the ff8410 pin sitting under the first play difference.
Q4: VIOLATED — One failure mode is neither controlled nor named: a poke that CHANGES the id's value could itself write the three bytes, and they would then be charged to the confirm. The control the claim relies on, "REAL vs SELF latches nothing (the poke mechanism itself is inert)" (build/rulecheck/2026-09-25-166/a/audit_forced_pick_fidelity.sh.txt:281), and the donovan-self row ("donovan-self	-	-", build/rulecheck/2026-09-25-166/a/forced_pick_fidelity.tsv:17) both write a value the id already holds (audit_forced_pick_fidelity.sh.txt:26-27, 135; row_path donovan-self "R R" = the poked prologue, lines 89 and 117). No leg runs a real pick followed by a different-id poke. The same-leg and wrong-cursor controls (lines 310-322) cover a vanished difference and a wrong identity, not this.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to anyone.
VERDICT: VIOLATED

Q1: VIOLATED — The claim asserts "a reproducing test where one exists" for every finding but never names as untested that each cited test actually exercises its finding. The artifacts show two that do not. (1) Finding (g), "a rig tuned at level 8 whiffs at 6", cites the `poked` assertion, which runs only at level 8 (tests/audit_victim_parity.sh lines 59-60: "at level 8 the real-pick, per-event-pinned, RNG-pinned native leg reproduces test_reactions' frozen ... lines"). The level-6 case is exercised by the `unpinned-level` control (line 40), which the table does not cite. (2) Finding (bb), "the trap dome does NOT connect ... in the air", cites `air-class` + `grounded-hit`. But the gate names a different control, `air-hit`, as "the clause the conclusion rests on, exercised on its own" (tests/audit_trap_airborne.sh line 31). The table does not cite it, and no artifact shows it was ever run: STATE.md line 91 lists only "Controls `air-marker`, `air-class` and `grounded-hit` ... every mode at FAIL". Also, the "22 wordings retracted" include one the artifact itself calls "now a TRUE statement" (build/agent181/retraction_greps_14z181.txt line 13). And the "planted" control is a heading that already existed, not one planted (line 45).
Q2: VIOLATED — Finding (ee) states a hit: "pyron_3's event 5, named "far (whiff)" by the rig, in fact connects" (tools/name_moves.py lines 775-777: "HP 288 -> 273 at f4973"). Finding (dd) states that both throw strengths connect. The claim itself says "(dd) and (ee) are RAM findings with no capture". The five captures sent do not include either (docs/NEXT_SESSION.md lines 27-32). build/agent181/resolve182.txt line 1 only "ACCEPTED" this gap, and it claims "no visual conclusion" for a hit.
Q3: N-A — The packet concludes that each finding has a home and a test. It concludes no ours-vs-native or before/after equality of its own. The leg comparisons it restates, (aa), (bb)/(cc) and (i), belong to their own gates.
Q4: VIOLATED — Nothing controls for, and the claim does not name, the case where a retraction pattern is too narrow to match the carriers where its wording survives. The only control is an exact, single-line, pre-existing heading ("THE GROUND IS y = 40", build/agent181/retraction_greps_14z181.txt line 45). It proves that the grep reaches docs. It does not prove that each pattern matches its wording. Example: the grep lists "== 'reruns every sweep.'  (run 170): 0 hit(s)" (line 22), with a trailing period, yet the words survive at docs/project/gotchas.md line 102 ("`tools/air_throw_sweep.sh` reruns every sweep") and at tools/name_moves.py line 185 ("reruns every sweep;"). The claim's named gap covers only whether the 22 patterns are the COMPLETE set, not whether each pattern finds all of its own carriers. Nothing checks either that a cited test exercises its finding (see Q1: (g) cites `poked`, and (bb) omits `air-hit`). A figure mismatch is not charged here because the claim names that gap ("a live carrier's FIGURES agree with the findings"): finding (ee) says the wall events push "from a pin the screen edge clamped to 664", but its own home's table shows events 0 and 3 unclamped at 552/728/448 and 563/728/534 and still ending at 960/1000 (docs/project/gotchas.md lines 78 and 81).
Q5: N-A — The packet credits no person with a decision, approval or closure. It says "the maintainer's read is theirs" (still pending), and (t) points to STATE "Decisions pending".
VERDICT: VIOLATED

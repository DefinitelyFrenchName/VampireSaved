Q1: VIOLATED — The claim's named untested premises all check out against the artifacts (pre = six frames, audit_forced_pick_fidelity.sh.txt:99,180; P1-block-only classification :189-196; TRANSIENT/globals/P2 reported not frozen :236-237,279-284; ALT route control MEASURE-only :249-251,285-289; SELF and donovan-self both same-value writes :117-119,135-138; pins outside the P1 block pre-match: name_moves.py.txt:688-693 (position pins at t-40 = 2560), :708 (HP pin from 2550), :716-717 (no stock poke for part 1), gate :133-134 (level from 2000, RNG from 2363)), but the artifacts show one it does not name: the frozen rows in forced_pick_fidelity.tsv:15-17 come from a FREEZE run, and the FREEZE branch (audit_forced_pick_fidelity.sh.txt:292-296) copies got.tsv and exits 0 WITHOUT testing `$fail` from section 2 (identity MISMATCH, P2 dirty, REAL-vs-SELF non-empty all only set fail=1, :278-282) and BEFORE "== 3. must-fire controls" (:307), so "for Phobos and Pyron alike" and "latches nothing" rest on identity and controls that only a non-FREEZE run asserts — and no artifact is such a run. Two smaller statements the artifacts do not show: "the gate asserts only that each leg produced at least 30 dumps" — it also VOIDs on `len(da) != len(db)` (:166), conservative but not "only"; and "two real picks ... leave route residue" is asserted as a measured fact while the ALT leg exists only under MEASURE and no artifact carries its result.
Q2: N-A — the claim ends "No behavioural conclusion about how the character plays is drawn here" and freezes only a RAM first-difference frame whose offset is not frozen (audit_forced_pick_fidelity.sh.txt:197-198,284; forced_pick_fidelity.tsv:13).
Q3: OK — the legs share the rig schedule, MAME, replay.lua's dump path and the copied pokes; the claim names the shared pokes and the artifacts bound them: one `mkleg` writes every leg (:109-140), `_base`/`_lvl`/`_rng` are identical on all legs (:131-139), none writes $FF8400-$FF87FF before 2560 (name_moves.py.txt:688-693,708; gate :133-134), identity is read from the same dumps on every leg (:169-179) and the frozen non-empty set (tsv:15-16) shows the shared dump path does discriminate the legs; the confirm frame is held at 1300 on both routes (:122-124; name_moves.py.txt:59-76).
Q4: OK — wrong-cursor control (:253,:318-322) fires when a real leg's path confirms another character (the identity failure mode); the identity assertions on POKED/REAL/SELF at 1600 and REAL's cell at 1290 (:169-179,278) fire if the poke did not take; the P2-block-empty assertion (:279-280) fires on instrument noise/false positives; the donovan-self row (:47-51, tsv:17) fires if a same-value poke perturbs state; the same-leg must-fire (:310-316) fires if the comparator went blind against a non-empty freeze. Nothing covers a symmetric dump loss shrinking `pre` (:166,:180,:258) or route residue inside the P1 block (ALT is MEASURE-only, :90-94,:249-251) — both named by the claim as unfrozen; and under FREEZE none of the section-3 controls executes (:292-296 exits before :307).
Q5: N-A — the packet attributes no decision, approval, ruling or closure to any person; the only maintainer words in the artifacts (name_moves.py.txt:260 "maintainer-confirmed", :392 "the maintainer's ruling 14z-121", :463) are about Killshread/Planet Burning schedules, not this claim.
VERDICT: VIOLATED

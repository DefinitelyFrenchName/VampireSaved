Q1: VIOLATED — audit_forced_pick_fidelity.sh.txt:26-28 "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)" and :114/:135-139 (`_id="$(row_id "$_row")"` is the row's own id on every leg, so `real`+`self` poke the value the real path already confirmed); the claim says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)" — no leg performs a changed-value write over a real confirm (donovan-self is the same, :49 "poking 0x13 over Donovan's own confirm"), so a changed-value poke's inertness is an untested premise the claim presents as tested, and the named untested premises are not the only ones.
Q2: N-A — the claim's last sentence "No behavioural conclusion about how the character plays is drawn here."; the gate's first-play frame (forced_pick_fidelity.tsv:15 `2980`) is reported as a byte-difference frame, not as a felt behaviour.
Q3: OK — both legs share the rig schedule and pokes, and the claim names them; the artifacts confirm they fall outside the P1 block × pre-match window: name_moves.py.txt gen `pin_pokes += [f"{t - 40}:ff8410:..."]` with `FIRST_EVENT = 2600` (:77) and every part-1 event "far" (:425, :477), the HP pin `range(FIRST_EVENT - 50, end, HP_PIN_EVERY)` (2550, $FF8850), part 1 absent from METER_PARTS (:646), and the gate's own pins audit_forced_pick_fidelity.sh.txt:133-134 (`ff8116` from 2000, `ff80d4` from 2363, both globals); identity is asserted per leg (:169-179) so a shared wrong cursor decode cannot make the legs agree.
Q4: VIOLATED — the controls present are same-leg (:308-316: an empty set against a non-empty freeze is noticed), wrong-cursor (:317-322: a one-move-short real leg fails identity), the P2 block empty (:279) and donovan-self (forced_pick_fidelity.tsv:17); the control the claim names for "the poke mechanism is inert ... tested directly on the same write the poked leg performs" does not exercise that write: the poked leg's write CHANGES $FF8782 (0x13 → tenant), while SELF's and donovan-self's writes leave the value already present (audit_forced_pick_fidelity.sh.txt:26-28, :49, :135-139); nothing covers a changed-value write at 1400-1500 perturbing P1-block bytes beyond the id, so the charge of all three bytes to the confirm rather than the poke rests on a control that sees a different write.
Q5: N-A — the claim attributes no decision, approval, ruling or closure to any person; "report #151 step 1 as done" is the recommendation itself, and the artifacts' "Frozen 14z-160" (forced_pick_fidelity.tsv:8) names a session, not a person's statement.
VERDICT: VIOLATED

Q1: VIOLATED — the claim names "the near/far MP slot table" as basis but not that the table does not cover Pyron and that its id→distance mapping is character-dependent: tests/expected/vanilla_normal_slots.tsv:1-2 freezes it "MEASURED on vsavj (14z-125)" for "each vanilla character" (15 tabs — AN AU BI BU DE FE GA JE LE LI MO QB SA VI ZA — no tenant row), and :57 reads "ZA far MP a2:0x02", the very id build/rig172/issue168_correction.md:12 reads as "the NEAR one" (5 of the 15 characters take a2:0x02 at BOTH distances), so "a2:0x03 is the FAR standing MP slot" applied to Pyron — and with it "the chain id itself says the two fighters met the event AT RANGE" (build/rig172/issue168_correction.md:14) — is an untested premise, unnamed.
Q2: OK — build/rig172/issue168_correction.md:16-19 records the capture ("Captured, before and after, on native vsav2 ... six frames per leg at matched offsets from the event; build/rig172/cap/, and put before the maintainer", BEFORE "Demitri stands beside him untouched", AFTER "Demitri is thrown"), and build/rig172/issue168_close.md:58 shows it preceded the conclusion of record: "Q2 demanded a capture, and producing it falsified the wording this issue's body carried".
Q3: VIOLATED — audit_tick_phase §3's "separating control" for ROUND_START compares two legs that share the rig's own input path: tests/audit_tick_phase.sh:62-63 generates ONE replay and ONE base poke list, :70 hands both legs `REPLAY="$W/r.rpl" POKES="$BASE$2"`, and :78-79 differ only by the level-6 pin, while the compared state is the frame $FF812D first reads 1 (:170-184) — the replay is what walks the game through the select screen into the round (tests/audit_rig_opening.sh:76-80 rewrites its 1100-1280 select lines), so a defect in it moves the round start on BOTH legs and the gate still reports :33-34's "the round intro's property rather than the rig's" (:184 "2545 would then be the rig's number"); only the speed pin is withheld, not the rig.
Q4: OK — the shift sweep is the control for the way this claim could be wrong (a landed shift that re-rolls an input-window-edge event's pass phase and fires a different move unnoticed): build/rig172/shift_sweep.txt:9-16 re-runs the whole-corpus naming gate at seven other shifts and fires on them — ":11 194 ... Galactic Throw x2, Circuit Scrapper x3, huitzil_6 gc", one frame from 195 — plus the must-fire pair at tests/audit_tick_phase.sh:204-211 (flat-pass, quantum-off) and pin-before-round at tests/audit_rig_opening.sh:245-249; the residual (divisibility by 39 vs 3, and 273) is named untested by the claim.
Q5: OK — the closure's attribution "The complement half the maintainer asked for is reached" (build/rig172/issue168_close.md:60) is backed by the maintainer's own words quoted in the artifacts, tests/rulecheck/ledger.tsv:102 — "Do 1 but the complement ticket to reach \"2\" should be on the list of next priorities," and "I'd rather pick up #168 first" — and this sitting's direction is quoted at build/rig172/state_14z172.md:5 ("#168 first", "carry on, the findings are valuable, make things right"), with the release ruling quoted at docs/NEXT_SESSION.md:15-16.
VERDICT: VIOLATED

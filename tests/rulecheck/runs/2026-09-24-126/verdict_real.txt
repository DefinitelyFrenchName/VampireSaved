Q1: VIOLATED — The claim says the poke-mechanism premise was tested, but the artifacts show it was not. The claim says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)". In audit_forced_pick_fidelity.sh.txt:135, `_pick` pokes "ff8782:$_id", and `_id` is the row's own tenant id (lines 87 and 114). The self leg takes the `*)` branch at line 138, which is the same one the poked leg uses. Lines 26-27 of the same file call it "the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". So inertness under a changed-value write is an untested premise that the claim does not name. A smaller point: the claim says the rig's position pins are copied into both legs "from frame 2560 at the earliest", but the donovan-self row's rig is donovan_1 (line 88). Its part-1 events have no "far"/"near" field (name_moves.py.txt:207-224), so gen emits no pins for it (name_moves.py.txt:681).
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here", and the frozen TSV holds only offsets and a frame number (forced_pick_fidelity.tsv:15-17).
Q3: OK — The pokes both legs share land outside the P1 block before the match. The position pins are at t-40, which is 2560 at the earliest (name_moves.py.txt:687). The HP pin is at $FF8850 from 2550 (name_moves.py.txt:699). The level pin at $FF8116 and the RNG pin at $FF80D4 are both globals (audit_forced_pick_fidelity.sh.txt:133-134). No artifact line shows a shared poke or input writing $FF8400-$FF87FF between frames 1501 and 2362.
Q4: VIOLATED — The failure mode here is that a changed-value id write perturbs the P1 block. That write is what the poked leg does: 0x13 becomes 0x10 or 0x11. Neither control writes a changed value. SELF pokes the same id over a real pick of that id (audit_forced_pick_fidelity.sh.txt:26-28, 135, 138). donovan-self pokes 0x13 over Donovan's own 0x13 (lines 48-51, 87). Both show that a no-op write is inert, which is not what the poked leg does. The claim names this as tested, not as a gap, so it is neither controlled nor named.
Q5: N-A — The packet does not attribute any decision, approval, ruling or closure to the maintainer or to anyone else.
VERDICT: VIOLATED

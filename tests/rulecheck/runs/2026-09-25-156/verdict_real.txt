Q1: VIOLATED — The claim rests on reaction_map.py having "gained with_frame and max_labels flags whose default output is unchanged", but the named diff does not show that. It adds only `with_frame` ("+def contacts(sched_path, trace_path, chains_dir, with_frame=False):", build/agent181/reaction_map_14z181.diff:10) and leaves the path cut at 8 labels ("{' '.join(path[:8])}", :22). There is no max_labels anywhere in it. Yet tools/victim_parity.py:338 calls "reaction_map.contacts(argv[1], argv[2], argv[3], with_frame=True, max_labels=64)", and tests/expected/victim_parity.tsv:12,32 carry paths longer than 8 labels. So the version of reaction_map that produced the frozen rows, including its unchanged default and the full-path compare, appears in no named artifact. (Minor: build/agent181/issue_173.md:41 says "The other 13 DIFFER rows"; the table has 15.)
Q2: VIOLATED — The claim draws conclusions a player could feel: throw hits land "one frame earlier", and Pyron runs a different hit reaction. Both capture records lack the delivery record the file says it keeps. build/agent181/captures_sent_14z181.txt:1 says the file is "the record of the SendUserFile delivery", and every earlier send carries a file_uuid (:4-7, :11, :15). Lines :20-21 carry none: they only say "sent 2026-09-25". The Pyron sheet was also "sent ... with the gate finding" (:20), at the same time as the conclusion, not before it. And build/agent181/rulecheck156_verdict_real_unrecorded.txt:2 shows the throw conclusion ("the two Victors, not the tenant") was first drawn while "no throw capture exists".
Q3: VIOLATED — Both legs use one pokes file (tests/audit_victim_parity.sh:199-201). That file writes the RNG every frame, "%d:ff80d4:0000" % f for f in range(2363, fr) (:149). The RNG selects the compared hits: "picks P1-first or P2-first on every pass from bit 0 of the engine RNG" (DECISIONS_HISTORY.md.lines-1218-1240:22-23). STATE.md.lines-201-215:6 says "a port defect in the RNG's ADVANCE would be reset away on both legs and read as agreement", and that ruling is still under "Decisions pending". The claim names this premise, but Q3 has no exception for a named premise.
Q4: VIOLATED — Two conclusions rest on the chains-identical check: "Pyron's b table decodes to the same shapes on both builds — so the engine's selection, not the ported bytes", and that a label means one thing on both legs. No control shows that check can fail. The five MUST-FIRE controls (tests/audit_victim_parity.sh:31-35: no-translation, p1-planted, pin-in-window, unpinned-level, hit-dropped) perturb traces, pokes, level and lines. None perturbs our build's data view or the chain shapes. So a chains-identical that could never report a difference would still pass (:191-192, always "0 differ in shape" in victim_parity_verify4.log:6,42,69). The label check ("every chain label ... is a compared chain", :245-251) also has no control. The claim names the check's scope limits (no pointers, no box tables) but not this failure mode.
Q5: OK — The one ruling credited to the maintainer is quoted in their own words: "Matched modes, both (Recommended)" ... "Force the same speed level on both legs and assert ours == native at NORMAL (level 6) and TURBO (level 8)" (DECISIONS_HISTORY.md.lines-1218-1240:12-15). The claim credits the acceptance of the pins to the session, not the maintainer ("The session accepted the finding in writing", STATE.md.lines-201-215:6). It calls level 8 "a design choice of mine", and it states the maintainer's read of both captures as "NOT yet given".
VERDICT: VIOLATED

THE PACKET

Decision kind: expectation
Subject: 14z-181 #136: the tenant AS THE VICTIM measured ours-vs-native — tests/audit_victim_parity.sh + tools/victim_parity.py, tests/expected/victim_parity.tsv frozen (58 contact rows, 41 SAME, 17 DIFFER in two attributed families; GitHub #173 for Pyron's b:0x19/b:0x03) — the second packet, after the unrecorded run 156's reader found four true defects (all acted on)
Claim (the working agent's sentence): The tenant AS THE VICTIM is measured ours-vs-native for the first time and frozen (58 contact rows: donovan parts 1-3, pyron 1-2, huitzil 1-2) by a new gate that runs the phase-3 victim rigs on BOTH legs as REAL cursor picks decoded from each game's own select wheel (no id poke), with the speed level pinned at 8 on both (vs2's TURBO, the level the rigs and test_reactions' frozen lines were measured at; our build's own is NORMAL 6 — matched modes as the maintainer ruled 2026-09-15) and the RNG at 0000 on both, one P2 HP pin ten frames before each event and the reader refusing any schedule poke inside a contact window, our P2 node pointers translated through placements.json and both legs read by ONE decoder (tools/reaction_map.py, which gained with_frame and max_labels flags whose default output is unchanged) over the tenant's vs2 extract — whose a/a2/b/c chains the gate asserts decode to the SAME SHAPES (node count, dur/flags/sfx/hbA per node) from our build's placed region as from the extract, index entries that walk into data (out-of-region ends, or a node whose sprite pointer leaves the anim region) counted on both sides and never compared, and every chain label the legs' lines carry asserted to be a compared chain; the rig premise is MEASURED not assumed: at level 8 the real-pick, per-event-pinned, RNG-pinned native leg reproduces tests/expected/reactions_<tenant>.txt exactly (13/13 on donovan_victim 1 and pyron_victim 1, the level pinned or absent alike) and the gate asserts that reproduction on every part with the pre-contact stance's entry index masked, while at level 6 every line's length moved and Pyron's j.HP whiffed; the reaction PATH is compared in full (64 labels) after a first form cut it at 8 and read two of Pyron's throw rows SAME with the release chain hidden behind the throw's eight c: labels; the verify run PASSES with every row as frozen and all five declared controls firing (no-translation, p1-planted, pin-in-window, unpinned-level on OUR leg since vs2's own level equals the pin, hit-dropped), each of the five modes reaches FAIL; 41 rows are SAME and the 17 DIFFER rows fall in two families, attributed by the gate itself: (1) 15 rows are Victor's throw on all three tenants, where the attacker's own data differs between the games — the gate runs tools/audit_same_data_p2.py on the two data views for id 0x03 (18 a2, 7 b, 19 c, 3 proj chains differ), reports every span of a differing ATTACK chain he runs, refuses a leg where he enters a differing REACTION chain, and stamps each row with the differing chains that ran inside its event (attacker=); his throw start a2:0x38 is 25 data frames on vs2 and 24 on vsavj (first node dur 2 vs 1), FIVE of the six throw hits land one frame earlier on our leg (the third, f4319, on the same frame; the victim's HP steps 4266/4292/4319/4345/4371/4397 for native's 4267/4293/4319/4346/4372/4398) and the release chains read len +1 or the last hit's freeze 11 for 10 — the two Victors, not the tenant, the throw captured on both legs at every hit frame and sent to the maintainer; (2) 2 rows outside the throw and the label on 7 of Pyron's throw rows are PYRON ONLY: on Victor's 5HP (class 0x04), j.HP (0x37) and the throw release native Pyron enters b:0x19 (a 4-node 12-frame loop) where ours enters the generic b:0x03 (12 nodes, 24 frames held) with class, freeze and length equal — Donovan and Phobos enter b:0x03 on both legs, and Pyron's b table decodes to the same shapes on both builds — so the engine's selection, not the ported bytes, filed as GitHub #173 with the capture (native vs ours at the 5HP contact, every third frame) put before the maintainer, whose read is NOT yet given for either capture. NOT tested: WHERE vs2 selects b:0x19 for Pyron (a per-character override, a class remap or his own code) and whether b:0x19 and b:0x03 carry different vuln boxes during the hitstun (a gameplay difference) or only different frames; the per-frame level and RNG pins are a premise both legs share BY DESIGN (the parity gates' equalised input, accepted in writing at runs 153-155 and put to the maintainer under STATE "Decisions pending") and a defect in the RNG's advance is masked by construction; the choice of level 8 here against the parity gates' 6 is a design choice of mine (both legs matched either way), not a ruling; the P2 x position at the contacts is not sampled on either leg (the victim rigs pin no x); the poked assertion masks the first label's @entry index (the pre-contact stance) by my reading of what that index means, and Pyron's part-2 lines read that index one node later than the frozen file on three lines under the real pick; the chain-shape identity compares dur/flags/sfx/hbA and node counts, never the sprite or link pointers themselves (relocated by the port) nor the box tables the hbA indexes (the hitbox gate's subject); the throw family's attribution rests on the same-data census's differ list and on a2:0x38 being the only differing chain inside the throw event — a differing chain that changed a LATER event's outcome without running inside it would not be caught; hit reactions from any attacker but Victor, the tenant's own guard cancels (measured separately this sitting) and P2's HP are not this gate's; and the maintainer has read neither capture.
Artifacts (read every one, in full):
  - tests/audit_victim_parity.sh
  - tools/victim_parity.py
  - build/agent181/reaction_map_14z181.diff
  - tests/expected/victim_parity.tsv
  - build/agent181/victim_parity_freeze3.log
  - build/agent181/victim_parity_verify4.log
  - build/agent181/victim_parity_mode3_no-translation.log
  - build/agent181/victim_parity_mode3_p1-planted.log
  - build/agent181/victim_parity_mode3_pin-in-window.log
  - build/agent181/victim_parity_mode3_unpinned-level.log
  - build/agent181/victim_parity_mode3_hit-dropped.log
  - build/agent181/victim_parity_run1.log
  - build/agent181/victim_level_experiment.txt
  - build/agent181/victim_throw_spans.txt
  - build/agent181/victor_same_14z181.json
  - build/agent181/chains_identical_donovan.txt
  - build/agent181/chains_identical_huitzil.txt
  - build/agent181/chains_identical_pyron.txt
  - build/agent181/vp_keep/p1_1_native.txt
  - build/agent181/vp_keep/p1_1_ours.txt
  - build/agent181/cap_victim_pyron_5hp.png
  - build/agent181/cap_victim_throw_donovan.png
  - build/agent181/captures_sent_14z181.txt
  - build/agent181/issue_173.md
  - build/agent181/rulecheck156_verdict_real_unrecorded.txt
  - tests/expected/reactions_pyron.txt
  - tests/expected/reactions_donovan.txt
  - DECISIONS_HISTORY.md.lines-1218-1240 (lines 1218-1240 of DECISIONS_HISTORY.md)
  - STATE.md.lines-201-215 (lines 201-215 of STATE.md)
  - docs/game/engine_internals.md.lines-995-1016 (lines 995-1016 of docs/game/engine_internals.md)

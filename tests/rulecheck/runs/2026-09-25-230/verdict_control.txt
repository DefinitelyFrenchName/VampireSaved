Q1: VIOLATED — The claim says the SELF leg is "the real cursor path plus a poke of a DIFFERENT id (a changed-value write)". The gate pokes the SAME id. See build/rulecheck/2026-09-25-230/a/audit_forced_pick_fidelity.sh.txt:26-27 "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)", and lines 135/138, which use `ff8782:$_id` = row_id, the tenant's own id, on the self leg. So the premise that the poked leg's changed-value write (0x13 over to 0x10/0x11) is inert is untested, but the claim presents it as tested.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — The shared rig pokes (audit_forced_pick_fidelity.sh.txt:132-137) start at 2550 or later for $FF8410/$FF8810/$FF8850 (name_moves.py.txt:687, 699, 425, 477). The level and RNG pins are globals (lines 133-134). None of them reaches the P1 block in the pre-match window 1501-2362 (line 180) that sets LATCHED. The one shared write into P1 during play, the X pin at $FF8410, is named in the claim.
Q4: VIOLATED — Nothing controls for the changed-value id write itself perturbing P1-block bytes, and the claim does not name this gap. REAL-vs-SELF (lines 272, 281) and the donovan-self row (line 87 id 13 over the R,R confirm; forced_pick_fidelity.tsv:17) both use a same-value write. Neither would fire if the 0x13-to-tenant write latched bytes that the gate then charges to the confirm. The claim says instead that inertness "is tested directly on the same write the poked leg performs".
Q5: N-A — The packet attributes no decision, approval or ruling to any person.
VERDICT: VIOLATED

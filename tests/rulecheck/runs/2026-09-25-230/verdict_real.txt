Q1: OK — The claim lists four things it did not test, and the artifacts show nothing else untested: the far, crouch, jump, jump_fwd and jump_down sets were not re-measured, and build/agent183/p134g/test_vanilla_aerial_join.log:8 and audit_df_startup_invuln.log:24 only PASS against unchanged frozen tables; SA's HP knife edge is recorded, not stabilised (separations.txt:43-44, "SA_near_2600 ... [134, 60, 59, ...] ... 'a2:0x04'" vs "SA_near_2800 [59, 59, 60, ...] ... 'a2:0x05'"); the sweep covers only +10..+34 (lk_sweep.txt:1, "offsets measured: 10..34, every one (25 legs; missing: none)"); the video covers two legs (maintainer_read.txt:4-6, SA 5LP and j.LK +14/+18, not +24).
Q2: OK — Captures were made and shown to the maintainer before the conclusion: build/agent183/p134_evidence/p134_captures_strip.png and p134_captures_dense.png. The maintainer's read is quoted in maintainer_read.txt:8 ("yes and this is as expected.") and maintainer_read.txt:17 ("she is too high to hit and the jump does not result in a cross-up"). The legs without a capture, including +24, are named in the claim.
Q3: VIOLATED — The claim says the video confirms the hits "independently of the trace", but the strip the maintainer judged prints the trace's verdict under every frame (maintainer_read.txt:4-6, "Victor's HP from each leg's trace"). The strip's captions read "f2803 (press+3) Victor HP 285 <- HIT", in red. SA 5LP appears only on this strip, since the unlabelled dense sheet shows only Lei-Lei. The strip got one overall "yes and this is as expected." (maintainer_read.txt:8), not the statement-by-statement read the claim describes. So SA 5LP's video check and trace count share the trace, and a wrong trace verdict would push the two to agree.
Q4: OK — Three controls cover the specific failures. The moved window in test_rehit_ring.sh finds a candidate: when a clear is inserted into JE from the new window, the gate fails (build/agent183/p134g/rehit_ring_ctl_clear-on-attack-node.log:12, "cleared_on_attack_not_last=1", and :30 "FAIL JE"). The change of first event alone explains the old rows: the f2600 control leg reproduces the frozen SA near rows (separations.txt:43). The frozen hashes come from the +24 rig: outoftree_diff.txt:4-7 matches both hashes and quotes "PRESS_AT = {(0x0D, "hit_jump", "LK"): 24}", and meter_verify24.log:17 matches on an independent re-run. Relying on the victim's HP drop is named in the claim as the premise.
Q5: OK — The ruling to fix the rig and chase the j.LK is quoted verbatim in DECISIONS_HISTORY.md.lines-30-36:3 ("Fix, and chase j.LK too"). The maintainer's read of the captures is quoted verbatim in maintainer_read.txt:3, 8, 15, 17 and 19. One misattribution: the "correction" of the description was the working agent's own, made after rule-checker run 228 (maintainer_read.txt:9-13). The maintainer's own words at :17 only add a precision.
VERDICT: VIOLATED

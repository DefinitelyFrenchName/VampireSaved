Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the artifacts show several premises that were not tested. audit_move_parity.sh.txt:34-36 says the gate does not cover "per-hit damage, meter gain, hitboxes and projectile parameters", and that P2's "state is never compared". Only one of Huitzil's ten parts is claimed, and move_parity.tsv:21-29 freezes huitzil_2..huitzil_10 as DIVERGES. The gate outputs an IDENTICAL/DIVERGES verdict, not a frame count (audit_move_parity.sh.txt:172-178), so the "6851/6851" figure does not appear in any artifact. The frozen row for this part is "huitzil_1	DIVERGES	Jump [8]+21f" (move_parity.tsv:20). No artifact shows the ff87c2 poke or the claimed enumeration of the byte's consumers.
Q2: VIOLATED — The claim concludes that Phobos's part-1 movement and normals now play identically to native. The part-1 schedule is jumps, dashes, air dashes and floats (name_moves.py.txt:448-460), and the frozen divergence was "Jump [8]+21f" (move_parity.tsv:20). That is timing and motion a player can feel. The conclusion rests only on a number ("6851/6851 frames"), and no artifact shows a capture being produced or put before the maintainer.
Q3: VIOLATED — Both legs get the same rig pokes, and those pokes write fields that are then compared. name_moves.py.txt:658 adds "{t - 40}:ff8410:{x1:04x}" (the P1 X pin) before every "far" event, and every Huitzil part-1 event is "far" (name_moves.py.txt:448). That poke list is `_base`, which goes into both the native and ours legs (audit_move_parity.sh.txt:98, 101). ff8410 is a compared field ("ff8410:w:x", audit_move_parity.sh.txt:105). So both legs' X is reset to the same value before each event, which hides any position drift that builds up across events. The level pin ff8116:06 is also applied to both legs (lines 89, 98, 101) and compared as "ff8116:b:lvl" (line 105). The character id ff8782 is poked on both legs (name_moves.py.txt:672; audit_move_parity.sh.txt:92, 101) and compared as "ff8782:b:id" (line 105).
Q4: VIOLATED — The gate has only two controls, "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:4-5, 61). Neither covers the ways this claim could be wrong, and the claim does not name those ways as untested. First, ff87c2 is not among the fields the gate samples (audit_move_parity.sh.txt:105), so nothing checks that ours actually holds 0x01. That matters both for the runtime poke and after flavor_default is built in. Second, a runtime RAM poke may not be equivalent to the build-time flavor_default change, and no control checks that. Third, the per-event X pin could mask divergence, and no control checks that either. The existing controls also cannot run on this part. The no-translation control needs the control part frozen IDENTICAL, but huitzil_1 is frozen DIVERGES (move_parity.tsv:20), so the gate reports "CONTROL DEAD: no-translation — the control part $CTL is not frozen IDENTICAL" (audit_move_parity.sh.txt:195).
Q5: N-A — The packet does not attribute any decision, approval, ruling or closure to the maintainer or any other person.
VERDICT: VIOLATED
Q1: OK — The claim names two untested premises: "why verify_data.bin moved is attributed only to the six program words per track (deltas_measurer.txt), not traced byte by byte" and that nothing is concluded about the tenants' behaviour. The artifacts show no other untested premise. build/rc183/refreeze/deltas_measurer.txt:2-4 gives only word-level deltas ("6 words ... members vm3j.04d, vsw.33m, vsw.37m"). build/rc183/refreeze/chars_pages.diff:9-33 changes only manifest, verify_data.bin, set and one sha1_ours line. build/rc183/refreeze/reaction_classes.before.log:14-17 differs only at "204c204".
Q2: N-A — The claim draws no conclusion a player could feel ("nothing about the tenants' behaviour is concluded here"). It is a static expectation re-freeze.
Q3: OK — The legs are M19-regenerated against frozen, M19 against M20, and census against frozen. No artifact line shows a shared rig or tool writing or selecting the compared state. The M19 regeneration reproduces the independently frozen hash: build/rc183/refreeze/m19_regen_vs_frozen.txt:2-4 "EQUAL". Each side of the page diffs is named with its sha256 (build/rc183/refreeze/donovan.html.m19_vs_m20.diff:1-2), and those values match build/rc183/refreeze/framedata_pages.log:3,5,7.
Q4: OK — There is a control for each way this claim could be wrong. (1) The census could be blind to row changes: build/rc183/refreeze/reaction_classes.before.log:10-11 "CONTROL FIRED: stager-38-copy — the planted copy moved exactly its two rows". (2) The page could fail to regenerate deterministically, so an M19-vs-M20 diff would not stand in for frozen-vs-M20: m19_regen_vs_frozen.txt:2-4 "EQUAL". (3) The freeze could write more than the build identity: build/rc183/refreeze/dryrun_freeze.diff:7-33 shows only the three html hashes, the header "m3b_merged28" and the build row. (4) The dry run could be left unrestored: dryrun_freeze.diff:34 "git diff empty on both files (restored to HEAD)". (5) A changed built byte could go unseen: build/rc183/refreeze/charmap.before.log:25 "one changed built byte -> unattributed 558 -> 559". The remaining gap, verify_data.bin's move not traced byte by byte, is named by the claim itself. One small point: dryrun_freeze.diff:1 promises "sha1 below, before = after restore", but no sha1 appears. Only the git-diff-empty line backs the restore.
Q5: N-A — The packet credits no decision, approval, ruling or closure to the maintainer or anyone else.
VERDICT: OK
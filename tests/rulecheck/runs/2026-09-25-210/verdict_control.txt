Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the artifacts show several untested premises. (1) audit_move_parity.sh.txt:34 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters", and :35 says P2's state "is never compared". (2) The window is not the whole part: :25 "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor". (3) Only part 1 was run, while move_parity.tsv:21-29 lists nine more huitzil parts (2-10), all DIVERGES and untested under the new byte. The M19 freeze also rebuilds five tracks, and none of those was measured. (4) The claim says ours was "poked" to 0x01, but the change is a build default (flavor_default). pokes_for (audit_move_parity.sh.txt:86-103) holds no ff87c2 poke, so the poke's timing against the byte's first read is not shown. (5) Nothing in the artifacts shows the claimed consumer enumeration ("pristine vsavj: zero"). (6) The gate outputs a verdict and an event, not a frame count (:133-135, :172-179). From the schedule, name_moves.py.txt:448-460 (41 events, 6260 frames of gaps), part 1 runs from frame 2600 to "frames" = 9110, so the comparison spans 6510 frames, not 6851.
Q2: VIOLATED — The claim concludes that Phobos's part 1 now plays like native. Part 1 is jumps, air dashes and floats (name_moves.py.txt:448-460), and its frozen divergence is at a jump, "huitzil_1	DIVERGES	Jump [8]+21f" (move_parity.tsv:20). That is a conclusion a player could feel, and it rests on the number "6851/6851" alone. No image or recording is named or produced, and none was put before the maintainer.
Q3: VIOLATED — Both legs share the rig's poke list, and it writes compared fields. pokes_for passes "$_base" (the rig's json pokes, audit_move_parity.sh.txt:88) to the native leg at :98 and to ours at :101. For every "far" event, _base includes "pin_pokes += [f\"{t - 40}:ff8410:{x1:04x}\", ...]" (name_moves.py.txt:658), and every part-1 event is "far" (name_moves.py.txt:448). ff8410 is the compared x field, "ff8410:w:x" (audit_move_parity.sh.txt:105). Speed level ff8116 is also written on both legs every frame from 2000 (:89, :98, :101) and compared as "ff8116:b:lvl" (:105). A defect in these shared writes would reset or force both legs to the same values, so the two legs would agree.
Q4: VIOLATED — The gate has two controls, unpinned-level and no-translation (audit_move_parity.sh.txt:4-5). Neither fires on how this claim could be wrong: the poked byte versus the build-default byte, a consumer that reads $FF87C2 before the poke, or a consumer whose effect falls outside the compared fields. ff87c2 is not even in FIELDS (:105). For PARTS="huitzil_1", the no-translation control is dead by construction because the frozen row is DIVERGES: "CONTROL DEAD: no-translation — the control part $CTL is not frozen IDENTICAL, so the control cannot discriminate" (:195, with base read at :188). An IDENTICAL result against the frozen "huitzil_1	DIVERGES	Jump [8]+21f" would also FAIL step 2 (:178), so an IDENTICAL verdict could not have come from a passing run of this gate.
Q5: N-A — The packet attributes no decision, approval or ruling to the maintainer or any other person. "flavor_default becomes 0x01" is the working agent's own action.
VERDICT: VIOLATED

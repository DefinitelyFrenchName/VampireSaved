Q1: VIOLATED — The claim says the grep re-run after runs 204-206 captured "the last edits before this packet". The artifacts show later edits. The snapshot's note dates it to "re-run on the tree after run 206's 23rd `gone` row" (build/agent181/retraction_greps_14z181.txt:89). STATE's CLOSE row then records tree edits made for runs 207-209: "run 207 found "every edit" unshown" and "209 ... both corrected; the terminal read PENDING (runs 210 onward)" (STATE.md.lines-72-99:26). The gate's live run never judges a `retracted` hit: tools/retraction_grep.py:130 fails only on `gone`, `if kind == "gone" and hits:`. So "every hit ... classed by hand ... none a live claim" was checked on a tree older than this packet's. The claim does not name this gap; it says the opposite.
Q2: VIOLATED — The packet restates conclusions a player could feel: the dome hitting on the landing frame (bb)/(cc), both throw strengths connecting (dd), event 5 connecting (ee), the level-6 whiff (g), and the +1 damage (r)/(t). Every capture was sent after its conclusion: "AFTER the RAM measurement that found it, not before" (build/agent181/captures_sent_14z181.txt:20, and lines 21-24 alike) and "AFTER the measurement (the naming freeze of a91a555a)" (build/agent181/captures_sent_14z181.txt:39). The claim itself says "still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t)".
Q3: VIOLATED — The ours-against-native agreements the packet restates ((i), (j), (l), the victim rows) rest on pins both legs share, and the artifacts show those pins change the compared lines. The log shows "level pin on; RNG pin on" (build/agent181/victim_parity_plain_14z181.log:13) and "reads 13 line(s) differently from the level-8 pin (frames stripped), so the level pin is load-bearing" (build/agent181/victim_parity_plain_14z181.log:38). The trap rows run with "the trap gate's forced-pick pokes, level and RNG pins" on both legs (tests/audit_trap_airborne.sh.lines-1-45:13). The pins are still unruled: "accepted in writing pending the ruling" (docs/NEXT_SESSION.md:23-24).
Q4: OK — Each way the two tools could fail has a control that fires on the plant's own text: `build-home-planted` (an untracked or build/ home), `unreachable-control` and `reach-threshold` (a grep that does not reach the tree), `gone-reintroduced` (a `gone` wording that comes back), and `case-fold-dropped` (a matcher without case-folding). All five show "CONTROL FIRED" (build/agent181/test_close_tools_14z181.log:10-14), and each mode log FAILs on its plant. A crash reads "REFUSED ... exit code: 3" (build/agent181/close_tools_crash_probe_14z181.log:5-6,10-11). The live newest row reads FAIL 0 (build/agent181/test_close_tools_14z181.log:8). Everything else is named in the claim as its own reading rather than a tool's check: the table's completeness, section homes, whether a test exercises its finding, the hand classing of `retracted` hits, pattern width and the `valid()` drop.
Q5: OK — Every attribution to the maintainer quotes their own words. #173: "yes, identical." (DECISIONS_HISTORY.md.lines-28-48:9). (j): "identical and validated otherwise" (DECISIONS_HISTORY.md.lines-28-48:10). The Ifrit Sword guard cancel: "looks identical" (DECISIONS_HISTORY.md.lines-28-48:11). The trap caveat (DECISIONS_HISTORY.md.lines-28-48:13). #169: "looks identical. I'll double check the air throw on my end but everything looks good" (DECISIONS_HISTORY.md.lines-28-48:14). The model switch: "I had to switch to Opus 5.5. Resume the work." (STATE.md.lines-72-99:3). The acceptance of the pins is credited to the working agent in writing, not to the maintainer.
VERDICT: VIOLATED

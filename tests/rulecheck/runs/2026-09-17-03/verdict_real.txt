Q1: VIOLATED — The claim names only two untested items (reads in play; post-anchor behaviour), but the artifacts show more the claim rests on and does not name: audit_forced_pick_fidelity.sh.txt:39-41 "P2's block and the globals are reported, not frozen" and :189/:208 compute `gl` (globals $FF8000-$FF83FF) yet never assert it outside MEASURE; :102-105 "a block copy (vs2 PRG:0x000D36) reads the latched bytes, so their values can propagate outside the block. Diagnostics only; nothing frozen from it" — so "exactly three latched bytes" is exactly-three-inside-one-1KB-window, with the rest of work RAM untested; :90-94 and :285-291 the REAL-vs-ALT route-residue control ("a byte that differs between two real picks of the SAME cell is left by the cursor's route ... and cannot be charged to the poke") runs only under MEASURE=1 ("nothing frozen, nothing compared"), so "the confirm is the whole effect" rests on route residue being nil, untested in the gate; :26-28 SELF is "the same-id poke" (a write of a value already present), so "the poke mechanism is inert" rests on a same-value write standing in for the changed-value write (0x13→0x10/0x11) the poked leg actually performs — not tested, not named.
Q2: N-A — The claim states "no behavioural conclusion about how the character plays is drawn here"; the gate freezes only offsets and a first-play-difference frame (forced_pick_fidelity.tsv:15-17), which is a number, and the claim does not read it as behaviour.
Q3: VIOLATED — POKED and REAL share everything but the cursor route and the id poke: the same rig's remaining pokes `_base` (audit_forced_pick_fidelity.sh.txt:131-132, "id pokes stripped", every other poke up to END applied to BOTH legs, contents not shown in the artifacts), the same level pin ":133 ff8116:06 from 2000", the same RNG pin ":134 ff80d4:0000 from 2363", the same replay events awk'd from the rig's .rpl (:126), the same MAME/replay.lua (:146). A shared `_base` poke that lands in the P1 block ($FF8400-$FF87FF) would overwrite a real latched difference on both legs and make them AGREE at that byte, shrinking "exactly three" without any assertion noticing; nothing in the gate inspects `_base` for P1-block addresses.
Q4: VIOLATED — The two must-fire controls cover other failures: same-leg (:310-316) fires whenever the frozen set is non-empty and A-vs-A is empty, proving the comparator sees SOME difference, not that it sees ALL of them; wrong-cursor (:318-322) covers the real leg confirming the wrong character (identity), not the completeness of the latched set. The negative controls (donovan-self :48-51; P2 empty :279-280; REAL-vs-SELF :281-282) cover "the poke perturbs state" only for a same-value write. Nothing covers the specific ways this claim could be wrong: (a) a fourth latched byte outside the P1 block (globals never asserted, :39-41; whole-RAM inventory MEASURE-only, :102-107) or masked by a shared `_base` poke (:131-132); (b) the three bytes being route residue rather than the confirm's latch — the control that would fire (REAL vs ALT, :285-289) is not run in gate mode (:291 "MEASURE: nothing frozen, nothing compared").
Q5: N-A — The packet is a recommendation and attributes no decision, approval, ruling or closure to any person; the script's references (:2 "GitHub #151, 14z-160", :12-16 "#147 shipped a wrong fix") are provenance, not attributed rulings.
VERDICT: VIOLATED

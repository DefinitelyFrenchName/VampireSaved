THE PACKET

Decision kind: recommendation
Subject: report #151 step 1 as done — the forced-pick native leg's defect is measured
Claim (the working agent's sentence): A forced-pick native leg differs from a real cursor pick of the same tenant in exactly three latched bytes of the P1 fighter block (+0x3BD, +0x3E0, +0x3C2), for Phobos and Pyron alike, and a same-id poke over Donovan's own confirm latches nothing, so the poke mechanism is inert and the confirm is the whole effect; P2's block (Victor on every leg) is the negative control and is empty pre-match. Not tested: whether any of the three bytes is READ in play (that is a separate tap, not this gate), and the legs' behaviour after the match anchor beyond the PLAY flag; no behavioural conclusion about how the character plays is drawn here.
Artifacts (read every one, in full):
  - audit_forced_pick_fidelity.sh.txt (the gate; a shell script)
  - forced_pick_fidelity.tsv (its frozen expectation)

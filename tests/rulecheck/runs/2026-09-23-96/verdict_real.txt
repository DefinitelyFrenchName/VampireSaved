Q1: OK — Every untested premise the claim names checks out against the artifacts, and I found no other one. Only the latched set and the first-play frame are frozen: "printf '%s\t%s\t%s\n' "$row" "$latched" "$fp" >> "$W/got.tsv"" (audit_forced_pick_fidelity.sh.txt:284). Globals, transients and REAL-vs-ALT are reported only under MEASURE (lines 201-211, 285-291). The dump check is only "-ge 30" (line 258), and the sample set is "sorted(set(da) & set(db))" (line 165). The POKED leg's prologue is re-typed and its level and RNG pins are generated inside the gate (lines 117-135). The six pre-match samples match DFRAMES (line 99) and "1501 <= f <= 2362" (line 180).
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here", and it leaves reads during play to "a separate tap, not this gate".
Q3: OK — The only shared write that reaches the compared P1 block is the position pin "f"{t - 40}:ff8410:{x1:04x}"" (name_moves.py.txt:687). With FIRST_EVENT = 2600 (line 77), it lands at 2560 at the earliest, after the pre-match window "1501 <= f <= 2362" (audit_forced_pick_fidelity.sh.txt:180), and the claim names its effect on the first-play frame. The gate's own pins "ff8116" and "ff80d4" (lines 133-134) and the HP pin "ff8850" from 2550 (name_moves.py.txt:699) all land outside the P1 block.
Q4: OK — The same-leg control catches a difference that vanished: "CONTROL FIRED: same-leg — $CTLROW poked-vs-poked latches nothing where the frozen set is [$fz]" (lines 310-316). The wrong-cursor control catches a real leg that picks the wrong character (lines 318-322), backed by identity checks at 1600 and 1290 (lines 170-179). The P2 block (lines 279-280), donovan-self "donovan-self	-	-" (forced_pick_fidelity.tsv:17) and REAL-vs-SELF (lines 281-282) catch differences the instrument or the poke introduces. The failure modes left over are all named in the claim: dumps missing on both legs, the transient count, route residue, changed-value poke writes, and drift between the replica and the rig.
Q5: N-A — The packet credits no decision, approval or ruling to any person.
VERDICT: OK

Q1: VIOLATED — The claim lists two untested items: whether each home states its finding correctly, and the fact that the table is the agent's own enumeration. It also rests on an unnamed judgment: that the REVIEW address `$FF8081` is "not yet a finding with a home (named as such in the group)". No artifact shows that naming. STATE.md:59 calls it only "the cnt-family observation the first version counted as homed". The tool's own docstring says the opposite, at tools/close_findings.py:26-27: "the case where a new finding about an old address has no home of its own (`$FF8081`, 14z-167)". Three other results are asserted, not named as untested, and shown by no artifact: step 2's tool output, step 3's run-41 pointers on 14z-166's row (8) and banner, and step 4's grep patterns.
Q2: N-A — The claim is about where documents sit. It draws no conclusion about behaviour a player could feel.
Q3: N-A — No two legs are compared. The table and the tool are not presented as agreeing methods, and the tool's clean result is not cited as completeness.
Q4: VIOLATED — Two ways the claim could be wrong are neither controlled nor named. (a) The `$FF8081` REVIEW answer could be wrong. Its only carriers are "#136's comment and NEXT_SESSION", and neither counts as a live home under the tool's definition at tools/close_findings.py:32-37: "The live documents are docs/game, docs/platform and docs/project ... HANDOFF.md, the skills, and the leading comment block of every tests/*.sh ... An address named only in STATE, the archives, a ticket or a build/ artifact is reported". The REVIEW control fired, and the claim overrides it by reclassifying the address with nothing to back that. (b) Step 4's promise grep states no pattern and carries no planted promise, so "finds one commitment" is the result of an instrument with no control. The per-finding "its test" column that STATE.md:99 requires is also missing. The claim's "this claims presence" names that gap, so it is not counted here.
Q5: OK — The rulings the claim relies on are quoted in the maintainer's own words at DECISIONS_HISTORY.md:32: "then let's implement them and keep item 2" and "Item 6 is good, I really like it, let's keep it." The same quotes appear at STATE.md:59 and STATE.md:99.
VERDICT: VIOLATED

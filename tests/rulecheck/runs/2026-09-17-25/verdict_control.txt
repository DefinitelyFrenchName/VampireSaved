Q1: VIOLATED — the claim states "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs", but audit_forced_pick_fidelity.sh.txt:26-28 defines "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)", and lines 118 and 135-138 build it exactly so (self takes row_path, then `_pick` writes row_id, the tenant's own id, which the REAL leg already holds at 1600 per lines 171-174); the changed-value write the poked leg performs (0x13 -> tenant id) is an untested premise the claim names as tested, so the listed untested premises are not the only ones.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here"; forced_pick_fidelity.tsv:13 freezes only "first in-play difference frame" as a number, with no conclusion drawn from it.
Q3: OK — the id poke is on the poked leg only (audit_forced_pick_fidelity.sh.txt:136-139: "real|alt) printf '%s;%s;%s' \"$_base\" \"$_lvl\" \"$_rng\""); the shared pokes that reach the compared P1 block are the position pins at $FF8410 (name_moves.py.txt:691 and 694, at t-40 / t-230 with FIRST_EVENT = 2600, line 77), which land after the pre-match window of line 180 ("pre = [f for f in frames if 1501 <= f <= 2362]"); the shared level and RNG pins (lines 133-134) write globals outside the block, and the claim names the play-window pin.
Q4: VIOLATED — the control the claim rests on for "the poke mechanism is inert" is REAL vs SELF (audit_forced_pick_fidelity.sh.txt:281), and it exercises a same-value write ("a write at 1400-1500 to an id that already holds that value", line 28), a different thing from the changed-value write the poked leg performs; the failure mode "the changed-value poke itself produces or alters P1-block bytes that are then charged to the confirm" is neither controlled (same-leg at lines 310-316 covers a vanished difference; wrong-cursor at lines 318-322 covers a wrong cell; donovan-self at forced_pick_fidelity.tsv:17 is also a same-value write, 0x13 over R,R's 0x13) nor named as untested by the claim, which asserts the opposite.
Q5: N-A — no decision, approval, ruling or closure is attributed to any person in the packet.
VERDICT: VIOLATED

Q1: VIOLATED — two premises the artifacts show and the claim does not name: (1) "the projectile chains are resolved through the projectile hitbox tables" is conditional in the tool — tools/audit_same_data_p2.py:157-158 `except Exception as e: phb[g] = None` then :164 `(phb[g] or hb[g]) if name == "anim_index_proj" else hb[g]` falls back SILENTLY to the FIGHTER's tables with no report, so a proj column may rest on the wrong tables (Jedah's row carries no proj entry in build/move_parity_census_14z164/same_data_p2.txt:16 and reads proj 0 in the tsv); (2) "which chains differ per table" and the Demitri/Bishamon "nearest" ranking rest on shared sequences only — tests/test_same_data_p2.sh run_audit writes `def seqs(t): return ",".join(d[t]["differ"])` / `def cnt(t): return str(len(d[t]["differ"]))`, while :181-182 of the tool keeps `only_vsavj`/`only_vsav2` in the JSON alone; same_data_p2.txt:11 reads Bishamon "c(seqs 56/178, 0 differ: {})" and "b(seqs 122/192", :7 Zabel "b(seqs 178/285", yet tests/expected/same_data_p2.tsv:9 freezes Bishamon's c_differ as "-" — 122 one-sided c chains and 70 one-sided b chains are neither frozen nor stated as excluded from "nearest".
Q2: VIOLATED — the claim draws a hit-count conclusion, "that row now reads +167 and its [PP held] twin +156, the genuine 2-versus-4-hit difference", while no artifact shows a hit count or a capture: tests/expected/move_parity_events.tsv:501-502 carry only "DIFF +156 p2hp" / "DIFF +167 p2hp" and build/move_parity_census_14z164/p2_state_classify.txt:232-233 only "p2cls@+141 p2x@+152 p2hp@+156" / "p2hp@+0 p2cls@+141 p2x@+152"; the claim's own sentences "a DIFF row's ATTRIBUTION (which side, which mechanism) is not part of the freeze" and "No behavioural conclusion about how any move plays is drawn" contradict it.
Q3: VIOLATED — both legs receive the rig's own pokes (tests/audit_move_parity.sh:127 `_base="$(python3 -c "...json.load(open('$_j'))['pokes']")"`, emitted to native at :135 and ours at :138) and those pokes write a compared, NON-cumulative field: tools/name_moves.py.lines-640-720 line 48 `f"{t - 40}:ff8410:{x1:04x}"` writes P1 x, which is in FIELDS at :156 `ff8410:w:x` and absent from tools/move_parity.py:73 `CUMULATIVE`, so :159 compares it as an absolute — e.g. tests/replays/naming/pyron_4.json poke "4060:ff8410:0228" lands inside window [3700,4100) of event 5; agreement on x there is the poke's, on both legs.
Q4: VIOLATED — the specific way this corrected freeze could be wrong is the pin exclusion being inert or wrong (the very defect run 24 found), and no control fires on it: tools/move_parity.py:189 `sched.get("pokes", ())` silently yields NO exclusion if the schedule carries no `pokes`, and :156 excludes only `f not in pins` by the json frame; the gate's two controls perturb translation and level only (tests/audit_move_parity.sh:249-270), run on the first part alone (:246 `CTL="$(echo $SET | awk '{print $1}')"`, donovan_1, whose schedule has no x or stock pin), and the freeze path exits before them (:228-234); the claim names the exclusion as "the only handling of them" but not that it is uncontrolled. Likewise (B)'s `phb[g] or hb[g]` fallback (tools/audit_same_data_p2.py:164) is neither named nor controlled — the self-compare control reads SAME-DATA whether or not the fallback fired.
Q5: OK — DECISIONS_HISTORY.md.lines-30-52 line 4 quotes the maintainer's own statement: *"I agree with the 3 propositions"* (maintainer, 2026-09-17), with propositions 1 and 2 at lines 8-17.
VERDICT: VIOLATED

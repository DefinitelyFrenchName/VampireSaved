Q1: OK — every premise the claim lists as untested is untested in the gate, and I found no other one. Globals and the rest of work RAM are printed only under MEASURE (audit_forced_pick_fidelity.sh.txt:201-235). Only the latched set and the first-play frame are frozen (:284, :298-305), so the TRANSIENT count is not. The P2 check uses the POKED-vs-REAL line only; from REAL-vs-SELF just the latched set and identity are read (:277, :279). The ALT route legs run only under MEASURE (:249-251). The dump check is ">= 30" (:258). The pins are :133-134, the copied rig pokes name_moves.py.txt:687 and :699, and the replica prologue is typed into the gate (:117). One variant belongs to the named shrink gap: a VOID fires only when the two legs have different dump counts (:166), so two legs each missing a different dump also shrink the set silently.
Q2: N-A — the claim says "No behavioural conclusion about how the character plays is drawn here"; it reports RAM offsets and a frame number only.
Q3: OK — the only shared write that reaches the P1 block is the P1 X pin, "{t - 40}:ff8410" (name_moves.py.txt:687), which starts at 2560 (FIRST_EVENT 2600, :77). The LATCHED window ends at 2362 (audit_forced_pick_fidelity.sh.txt:180), so the pin cannot reach it. The level and RNG pins write the globals $FF8116 and $FF80D4 (:133-134), outside the compared block. The pin's effect on the frozen play frame, and REAL-vs-SELF being a same-value write, are both named in the claim.
Q4: OK — a leg being the wrong character is caught by the identity checks at 1600 and 1290 (:169-179) and the wrong-cursor must-fire (:317-322). Legs out of step, or instrument noise, would show in the P2 block, which is asserted empty pre-match (:199, :279). A poke that perturbs state is caught by "REAL vs SELF latches nothing" (:281) and by donovan-self frozen as "-" (forced_pick_fidelity.tsv:17). Drift in the set is caught by the exact comparison against the frozen expectation (:298-305). The remaining gaps are all named in the claim: silent sample shrink, same-value-only writes, the replica drifting from the rig, route residue, and the pinned field in the play frame.
Q5: N-A — the claim attributes no decision, approval, ruling or closure to the maintainer or anyone else.
VERDICT: OK

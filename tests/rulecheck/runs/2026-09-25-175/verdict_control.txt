Q1: VIOLATED — The claim says the SELF leg is "a poke of a DIFFERENT id (a changed-value write)". The gate says otherwise. audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Line 135 builds the poke from the row's own tenant id: "_pick=... f'{f}:ff8782:$_id'". Line 138 adds that same poke to every leg that is not real or alt, which includes self. So no changed-value write is ever tested for inertness. That is an untested premise, and the claim does not name it: it states the opposite, that the premise is "tested directly on the same write the poked leg performs". The other untested premises the claim lists do match the artifacts: globals and whole-RAM under MEASURE only (lines 106-107, 208-210); the TRANSIENT count not frozen (line 284); P2 asserted on POKED-vs-REAL only (lines 272, 279); ALT route residue under MEASURE only (lines 250, 285-289); the rig's pins after 2550/2560 (name_moves.py.txt:687, 699).
Q2: N-A — No behavioural conclusion is drawn: "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — The two legs do share things: the base pokes, the level pin and the RNG pin (audit_forced_pick_fidelity.sh.txt:131-138). Their writes land where the claim says. $FF8116 and $FF80D4 are globals, outside the P1 block (line 189: p1 = 0x400-0x800). The P1/P2 X pins start at t-40 = 2560 and the P2 HP pin at 2550 (name_moves.py.txt:687, 699). Both are after the pre-match window "1501 <= f <= 2362" (line 180). No artifact shows a shared write reaching the P1 block before the match. The claim itself names the X pin covering the first-PLAY frame.
Q4: VIOLATED — Uncontrolled failure mode: the changed-value id write at $FF8782 (P1 block +0x382) could itself cause the three LATCHED bytes, which would make charging them "to the confirm" wrong. Both controls meant to prove the poke mechanism is inert use a same-value write. REAL vs SELF pokes the tenant's own id over a real pick of that tenant (lines 26-28, 135, 138). donovan-self pokes 0x13 over Donovan's own R,R pick (lines 48-51, 87-89). Neither sees the changed-value write the POKED leg performs. The claim does not name this gap; it asserts the gap is covered. The must-fire controls (same-leg, lines 308-316, and wrong-cursor, lines 317-322) cover a vanished difference and a wrong identity, not this.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

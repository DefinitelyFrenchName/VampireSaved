Q1: VIOLATED — the claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but audit_move_parity.sh.txt:105 compares twelve fields only ("FIELDS=ff841c:l:node,...ff8116:b:lvl"), :34-36 declare "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters", :25 "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor"; huitzil part 1 is movement/normals/6+button/air dash/float only (name_moves.py.txt:448-460) while parts 2-10 are frozen DIVERGES (move_parity.tsv:21-29); no artifact contains a poke or a field for $FF87C2, and the "6851/6851" figure appears in no artifact — the frozen verdict for the part is "huitzil_1	DIVERGES	Jump [8]+21f" (move_parity.tsv:20).
Q2: VIOLATED — the conclusion "identical to native" over Phobos's jumps, floats, air dashes and normals (name_moves.py.txt:448-460) and the shipping of flavor 0x01 as M19 are player-feelable; the only evidence offered is the frame count, and no artifact shows an image or recording put before the maintainer.
Q3: VIOLATED — both legs receive the same rig pokes from the schedule JSON (audit_move_parity.sh.txt:88 "_base=... ['pokes']", :98 native and :101 ours both append "$_base;$_lvl;$_rng"); those pokes write compared fields: the per-event position pin "f\"{t - 40}:ff8410:{x1:04x}\"" (name_moves.py.txt:658, every huitzil_1 event is "far", :448) writes x, which is compared as "ff8410:w:x" (:105), and the level pin ":ff8116:06" (:89) writes the compared "ff8116:b:lvl" (:105) on both legs.
Q4: VIOLATED — the instrument's two controls cover translation (:187-197) and the level pin (:199-208); neither fires on the way this claim could be wrong: that the verdict does not rest on $FF87C2 / flavor_default (the claim's poke is applied on ours only, the byte is not in FIELDS at :105, and no control withholds it); worse, the controls run on the FIRST part of SET (:182, default "donovan_1 pyron_2 huitzil_1" at :79) and are declared "CONTROL DEAD" for a part not frozen IDENTICAL (:195), which huitzil_1 is not (move_parity.tsv:20), so on this part they cannot discriminate at all; the claim names no untested failure mode.
Q5: N-A — the packet attributes no decision, approval, ruling or closure to the maintainer or any person.
VERDICT: VIOLATED

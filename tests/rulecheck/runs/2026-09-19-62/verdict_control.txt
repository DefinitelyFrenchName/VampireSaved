Q1: VIOLATED — The claim lists as tested that "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)". The gate says otherwise at audit_forced_pick_fidelity.sh.txt:26-27: "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". Line 135 builds the SELF poke from `$_id`, which line 114 takes from the row's own tenant id (row_id, line 87), and lines 172-174 assert that the REAL leg already holds that id. So one premise is untested: that the POKED leg's changed-value write ($FF8782 0x13 -> 0x10/0x11) does not itself create P1-block differences. Charging the LATCHED bytes to the confirm rests on it, and the claim does not name it (the donovan-self row is also a same-id poke, forced_pick_fidelity.tsv:11-12). Two more premises are unstated: the rig's pokes are read from tests/replays/naming/<rig>.json (lines 115, 131-132), not produced by name_moves.py, and the POKED leg is a hand copy of "pokes_for's protocol" (lines 21-22, 129-130).
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here."
Q3: VIOLATED — "the poke mechanism is inert" is drawn from the REAL-vs-SELF comparison. Both legs hold the same id at $FF8782: REAL from its confirm (asserted at audit_forced_pick_fidelity.sh.txt:172-174) and SELF from a poke of that same `$_id` (line 135). $FF8782 lies inside the compared region (line 189 "p1 = diff_region(0x400, 0x800)"). The poke therefore writes a value both legs already share, so they agree by construction, and a poke defect that fires only on a changed value cannot make them differ. The POKED-vs-REAL comparison is otherwise clean: the shared pins land in the globals (lines 133-134, from 2000 and 2363), and the rig pins and HP pin start at 2550/2560 (name_moves.py.txt:684-687, 699), after the pre-match window.
Q4: VIOLATED — Failure mode: the POKED leg's changed-value write at 1400/1450/1500 (audit_forced_pick_fidelity.sh.txt:135-138) itself perturbs the P1 block, so a LATCHED byte comes from the poke, not the confirm. No control covers it. SELF and donovan-self both poke a value already held (lines 26-28, 48-51). same-leg compares one leg's dumps with themselves (line 310). wrong-cursor covers identity only (line 318). The claim also says this failure mode is covered ("tested directly on the same write the poked leg performs") instead of naming it as a gap.
Q5: N-A — The claim attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

Q1: VIOLATED — The claim says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)", so the poke mechanism's inertness "is tested directly on the same write the poked leg performs". The gate does not do that. At audit_forced_pick_fidelity.sh.txt:26-27 it reads "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". At lines 118, 135 and 138 the self leg gets row_path plus `ff8782:$_id`, where `_id=row_id` of the same row: the tenant that the real path confirms, asserted at lines 175-179. donovan-self is also a same-value write: "R R" at line 89 plus the poke of 13. So the premise that a CHANGED-value id write (0x13→0x10/0x11, the POKED leg's write) perturbs nothing is untested, and the claim presents it as tested instead of naming it.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here." The claim's conclusions are about RAM bytes only.
Q3: OK — On POKED vs REAL, no shared write reaches the P1 block ($FF8400-$FF87FF) in the 1501-2362 window. The level pin is `ff8116` from 2000, a global (audit_forced_pick_fidelity.sh.txt:133). The RNG pin is `ff80d4` from 2363 (line 134). The rig's pins are `ff8410/ff8810` at t-40 with t≥2600 (name_moves.py.txt:687, 77). The HP pin is `ff8850` from 2550 (name_moves.py.txt:699). No rig input comes before FIRST_EVENT 2600 (gate line 126 keeps only lines ≥2000). The play-time X pin does reach P1 +0x010, and the claim names it. The SELF vacuity is covered under Q1/Q4.
Q4: VIOLATED — One failure mode is neither controlled nor named: the changed-value id poke itself perturbing P1-block state, so a LATCHED byte (or its absence) would be the poke's doing rather than the confirm's. The claim needs this ruled out to charge "a latched byte ... to the confirm". The SELF check (lines 281-282) and the donovan-self row (TSV line 17; gate lines 48-51 "poking 0x13 over Donovan's own confirm") both write "an id that already holds that value" (line 27). Each proves only a same-value write inert, which is a different thing from what the claim rests on. The gate's declared controls are same-leg (lines 308-316: a vanished difference) and wrong-cursor (lines 317-322: identity), and neither covers this.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to the maintainer or any person.
VERDICT: VIOLATED

Q1: VIOLATED — the claim presents as tested a premise the artifacts show is untested: "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)" — but audit_forced_pick_fidelity.sh.txt:26-27 says "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)", and the code agrees: mkleg sets `_path="$(row_path "$_row")"` for every non-poked leg (line 118) and `_pick` writes `$_id` = row_id of the same row (lines 114, 135, 138), so for huitzil SELF is L,L,L (cell 0x10) plus a poke of 0x10; donovan-self (R,R + 0x13 over 0x13, lines 87-89) is likewise same-value. No leg tests a changed-value id write's inertness; the listed untested premises are therefore not the only ones.
Q2: N-A — the claim draws no behavioural conclusion ("No behavioural conclusion about how the character plays is drawn here"; "whether any latched byte is READ in play is a separate tap, not this gate").
Q3: OK — both legs are built by one function (audit_forced_pick_fidelity.sh.txt:109-140) and share the rig's pokes; the only shared write that reaches the compared P1 block is the position pin at $FF8410 (+0x010), which name_moves.py.txt:694 places at `t - 40` with t = FIRST_EVENT 2600 (line 77), i.e. frame 2560 at the earliest, outside the 1501-2362 LATCHED window (lines 99, 180); the level pin ff8116 and RNG pin ff80d4 (lines 133-134) are globals below 0x400; the claim names all of these.
Q4: VIOLATED — the failure mode "the three bytes are written by the game's reaction to the id CHANGING at 1400-1500 rather than latched by the confirm" is neither controlled nor named as untested: the claim says inertness "is tested directly on the same write the poked leg performs", but the only inertness controls (REAL vs SELF, line 281-282; donovan-self, lines 48-51) perform a same-value write (lines 26-27, 118, 135), while the poked leg writes 0x10/0x11 over Donovan's 0x13 — the control sees a different thing than the claim rests on.
Q5: N-A — no decision, approval, ruling or closure is attributed to any person in the packet.
VERDICT: VIOLATED

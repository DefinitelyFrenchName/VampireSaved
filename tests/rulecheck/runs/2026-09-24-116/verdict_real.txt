Q1: VIOLATED — The claim says the SELF leg pokes "a DIFFERENT id (a changed-value write)". The gate shows the opposite. audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Line 135 builds `_pick` from the row's own `_id`, and line 138 applies it to every leg that is not real or alt, which includes self. So a changed-value write is never tested. Only the POKED leg makes one (the R,R prologue at line 117, then the poke to 10 or 11). The claim presents this as a tested premise when it is an untested one, and does not name it. The other stated gaps check out against lines 106-107, 201-211, 272-284 and 291.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here". The gate compares bytes only.
Q3: OK — Every shared poke lands outside the P1 block within the pre-match window 1501-2362. The level pin `ff8116` is written from 2000 and the RNG pin `ff80d4` from 2363 (audit_forced_pick_fidelity.sh.txt:133-134). The position pins go in at t-40, from 2560 (name_moves.py.txt:687). The P2 HP pin `ff8850` starts at 2550 (name_moves.py.txt:699). The one shared write into the compared P1 block is the X pin at `ff8410`, from 2560, and it touches only the play-difference frame. The claim names that itself: "measured with P1's X pinned identically on both legs".
Q4: VIOLATED — No control fires if the poked leg's changed-value write to $FF8782 (13 to 10 or 11, after the confirm) is what disturbs P1-block state. Neither REAL-vs-SELF nor the donovan-self row covers it. Both write a value the id already holds: audit_forced_pick_fidelity.sh.txt:135 uses `f'{f}:ff8782:$_id'` with the row's own id, and 48-51 describe the donovan-self row as "poking 0x13 over Donovan's own confirm". The must-fire controls cover something else: same-leg catches a difference that has vanished (line 310), and wrong-cursor catches the real leg's identity (line 318). The claim does not name this gap; it states the opposite.
Q5: N-A — The packet attributes no decision, approval or ruling to any person.
VERDICT: VIOLATED

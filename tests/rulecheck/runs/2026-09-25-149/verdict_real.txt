Q1: VIOLATED — The claim treats the build's identity as tested: "whole-set fingerprint 681ac3ad = merged-m19 in the registry". The registry says otherwise. Its merged-m19 row is keyed on "61e9815a3956…" as a "WHOLE-SET key ONLY (program 681ac3ad, which build/merged1 — the blanks-only legacy instrument … — carries too" (tests/expected/registry.tsv:169). So the 681ac3ad printed at build/agent181/df_moves_contact_run5.log:2 is merged-m19's program key, which merged1 also carries. It does not match merged-m19's whole-set key. The claim does not name this premise as untested. The claim also misstates a timing premise. It says "the far-pin 5LP rows' 2-damage hits 6-7 frames after the press". But the far-pin 5LP row hits at "@[8, 9]" native and "@[9, 10]" ours (tests/expected/df_moves.tsv:32), and the far jumping row hits 14 frames after its +10 press ("@[24]", run5.log:55). Only the NEAR rows hit at 6-7 (run5.log:53-54). Aside: docs/project/gotchas.md:5628-5630 says ev19's hit returned "with the pin moved off frame 4150". The window arithmetic puts 4150 in ev9's window (the ev8 window is 3920-4040 at run3.log:10, so ev9's is 4040-4160). The pin that fell on ev19's +15 hit is "[5750, 5734]" (run5.log:4).
Q2: VIOLATED — Re-freezing donovan_dfx1 ev19 from two hits to three is a conclusion about a hit. It rests on field-trace numbers alone (run5.log:49-51). The CAPTURE leg covers only "huitzil_dfx*) ;; *) continue" (tests/audit_df_moves.sh:357), and build/agent181/cap_dfx2_list.txt has no donovan file. The four new events do have captures: "captures: 70 PNGs" (run5.log:22), and sheet_ev7.png and sheet_ev10.png show both legs at +0, +4, the hit frames and +30. But no artifact records that they were put before the maintainer. That rests on the claim's word alone.
Q3: OK — The only shared rig write to a compared field was the P2 HP pin "ff8850:01200120" (tools/name_moves.py:766). It is now moved out of every compared window: "huitzil_dfx2 [[3350, 3314], [4550, 4524]]" and "donovan_dfx1 … [5750, 5734]" (run5.log:4-8). The compare refuses any pin left inside a window (tests/audit_df_moves.sh:229-230). The other shared pokes write fields that are not compared: stock ff8509, x ff8410/ff8810, level ff8116, RNG ff80d4 (tests/audit_df_moves.sh:152-156, 186, 190). In a two-event group, the second event's x pin lands in the first event's window at hi-40. No artifact shows a hit after +80 in any such row, so that write is not shown reaching a compared state.
Q4: VIOLATED — Two failure modes are neither controlled nor named. (1) The build could be the wrong one. The fingerprint line prints "ok" unconditionally and compares against nothing (tests/audit_df_moves.sh:96), and the value it prints is a program key that build/merged1 also carries (tests/expected/registry.tsv:169). (2) The freeze run does not enforce "the same gate, VOID guards and now seven controls". A failed in-mode check, a failed acted check, a failed form check or a DEAD control only sets "fail=1" and carries on (tests/audit_df_moves.sh:256, 292, 297, 345, 350, 523, 531). The FREEZE block at :549-556 then writes the expectation and exits 0 without testing $fail, and the hit-dropped control runs only after that block (:562-568). So a freeze run with an event outside the mode or a dead control would still freeze. The pin-in-window control (run5.log:10) covers only the HP-pin masking.
Q5: OK — The fold of #109 into #136 carries the maintainer's own words: *"I agree with all recommendations"*, said of each group of the 14z-157 decision sheet (DECISIONS_HISTORY.md:1267-1268), with the #109 fold listed under it at DECISIONS_HISTORY.md:1278-1279. The no-gauge rule quotes the maintainer: *"I see, it makes sense that you can't build meter during DF"* (tests/audit_df_moves.sh:60-61).
VERDICT: VIOLATED
Q1: VIOLATED — The claim's "Not tested: nothing further — the comparison is frame-exact over the whole part" does not match the artifacts. The gate lists what it does not cover at audit_move_parity.sh.txt:34-36: "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters" and "P2 ... his state is never compared". The comparison is not over the whole part either: "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor" (line 25). The flavor byte ff87c2 is not in FIELDS (line 105), and pokes_for (lines 86-103) has no ff87c2 poke, so this gate as written cannot have run the "poked to 0x01" leg. Part 1 is movement plus normals only (name_moves.py.txt:448-460), so parts 2-10 (specials, ES, meter, guard cancel) are untested premises of a five-track freeze. The gate prints IDENTICAL/DIVERGES per part (lines 172-179), not a frame count, and "6851/6851" appears nowhere in the artifacts.
Q2: VIOLATED — The claim concludes that player-feelable behaviour (jumps, air dashes, floats in part 1, name_moves.py.txt:448-460) plays identically to native. It rests only on a number, "6851/6851 frames identical". No image or recording is named or produced in any artifact. The frozen row move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f" shows the compared behaviour is a jump.
Q3: VIOLATED — Both legs get the same _base pokes (audit_move_parity.sh.txt:98 and :101). _base includes the per-event position pin "{t - 40}:ff8410:{x1:04x}" (name_moves.py.txt:658), and every huitzil part-1 event is pinned "far" (name_moves.py.txt:448). ff8410 is the compared field "ff8410:w:x" (audit_move_parity.sh.txt:105), so a shared poke rewrites P1's x on both legs before every event, and a drift in x would be erased and read as agreement. In the same way, "ff8116:b:lvl" is compared while "{f}:ff8116:06" is poked on both legs from frame 2000 (line 89). "ff8782:b:id" is compared while ff8782 is poked on both legs (line 92 ours; name_moves.py.txt:672 native via _base).
Q4: VIOLATED — The gate has two controls: unpinned-level (lines 199-208) and no-translation (lines 185-197). Neither covers this claim's failure modes. (a) No control removes the ff87c2 poke and requires divergence, and none checks that a build-time flavor_default=0x01 gives the same state as a runtime poke. That build-time vs runtime-poke path is exactly what is being frozen, and the claim does not name it. (b) Both controls run on the first part of SET (line 182). For huitzil_1, frozen as DIVERGES (move_parity.tsv:20), both controls fail to discriminate: no-translation takes the branch at line 195 "CONTROL DEAD ... is not frozen IDENTICAL, so the control cannot discriminate". (c) Consumers of the byte outside part 1's movement and normals are uncontrolled and not named: specials, meter, projectiles, P2.
Q5: N-A — The packet does not attribute any decision, approval or ruling to the maintainer or to anyone. "his part-1 rig" refers to the character (Phobos).
VERDICT: VIOLATED
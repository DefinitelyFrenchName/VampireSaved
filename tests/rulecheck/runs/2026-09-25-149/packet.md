THE PACKET

Decision kind: expectation
Subject: 14z-181 #136, the folded #109 clone-beam CONTACT leg (second packet, after run 2026-09-25-148): re-freezing tests/expected/df_moves.tsv with the four new huitzil_dfx2 rows — Phobos's 5LP/5HP PRESSES inside Dark Force with P2 grounded at a near pin (640) and P2 jumping into the beam band, far and near — AND with donovan_dfx1 ev19 re-frozen at three hits, the hit a shared HP pin had masked on both legs; measured by tests/audit_df_moves.sh (its CONTACT group, its HP pins moved out of every compared window, its new pin-in-window control, its CAPTURE leg) on build/m3b_merged27 (whole-set fingerprint 681ac3ad = merged-m19 in the registry) against native vsav2
Claim (the working agent's sentence): The four new rows and the changed ev19 row are frozen AS MEASURED by the same gate, VOID guards and now seven controls as the other 28 rows, which came out byte-identical after every HP pin was moved out of every compared window (run 5's diff carries exactly the five lines; run 5 and run 4 print the same rows); the compared HITS (damage, P2 class) are equal on both legs in every new event and the one DIFFER row differs in P1's gauge only, the class the ruled no-gauge-in-Dark-Force rule already accounts for; the captures of all four events at +0, +4, each hit and +30 on both legs were put before the maintainer before this freeze and their read is NOT yet given; NOT tested or shown: WHICH ATTACK produced a hit (the gate's stated limit since 14z-168 — the far-pin 5LP rows' 2-damage hits 6-7 frames after the press are what earlier rows read as the beams, the near jumping row's 5-damage hit 3 frames after the press has the punch's timing and damage, neither asserted), the hit FRAMES (printed, not compared, by design), P2's height in the band on each leg (only the class-4 airborne hit shows he was airborne, on both), whether the near pin's P2 x poke landed at the same x on both legs (no position is sampled by this gate), the venue (the legs draw different stages, visible in the captures, not a compared quantity), and the hand rigs df/103-104's mistiming, measured by a scratch gate deleted before commit whose log is the artifact; the fold of #109 into #136 rests on DECISIONS_HISTORY 'Ruled 2026-09-15 (14z-157)' where the maintainer's own words are quoted.
Artifacts (read every one, in full):
  - tests/audit_df_moves.sh
  - build/agent181/df_moves_gate_14z181.diff
  - build/agent181/df_moves_contact_run5.log
  - build/agent181/df_moves_contact_run4.log
  - build/agent181/df_moves_contact_run3.log
  - tests/expected/df_moves.tsv
  - build/agent181/df_replays_14z181.diff
  - build/agent181/clone_beam_contact_run1.log
  - tools/name_moves.py
  - DECISIONS_HISTORY.md.lines-1257-1282 (lines 1257-1282 of DECISIONS_HISTORY.md)
  - tests/expected/registry.tsv.lines-169-169 (lines 169-169 of tests/expected/registry.tsv)
  - docs/project/gotchas.md.lines-5618-5645 (lines 5618-5645 of docs/project/gotchas.md)
  - build/agent181/cap_dfx2_list.txt
  - build/agent181/cap_dfx2/sheet_ev7.png
  - build/agent181/cap_dfx2/sheet_ev10.png
  - tests/rulecheck/runs/2026-09-25-148/verdict_real.txt

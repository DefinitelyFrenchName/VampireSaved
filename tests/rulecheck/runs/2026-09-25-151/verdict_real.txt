Q1: VIOLATED — The claim says "Every ev row of run 9 is attributed by event NAME" and gives the split 17 identical + 4 new + 1 content change + 8 frame-only, which is 30 rows, not the 33 in run 9 (build/agent181/df_moves_contact_run9.log:46 "33 in-DF events"). The attribution is keyed by name, so the four rows named "5LP in DF" collapse into one line (build/agent181/df_moves_attribution_run9.txt:20 "IDENTICAL	5LP in DF	pyron_dfx1 ev1 -> pyron_dfx1 ev1"). Three rows are never attributed: donovan_dfx1 ev1, huitzil_dfx1 ev1 and donovan_dfx3 ev7. One of them is a ninth frame-only change: frozen "native=[(4, 1)]@[17]" (tests/expected/df_moves.tsv:31) against measured "native=[(4, 1)]@[16]" (build/agent181/df_moves_got_run9.tsv:25). The claim also says "the rig under test writes nothing inside any compared window … the reader refusing every poke address". But tests/audit_df_moves.sh:202 writes the level and the RNG on every frame of every compared window on both legs ("f'{f}:ff8116:06' for f in range(2000,$fr)" and "f'{f}:ff80d4:0000' for f in range(2363,$fr)"). The reader never sees those writes: it builds its set only from the rig's JSON pokes (tests/audit_df_moves.sh:236 "pins = {int(p.split(":")[0]) for p in J["pokes"]}"). The claim's list of untested premises names neither gap.
Q2: OK — The four contact events and donovan_dfx1 ev25 (2HP in DF) were captured on both legs under the final rig: "captures: 64 PNGs under …/build/agent181/cap_dfx2d" (build/agent181/df_moves_contact_run9.log:23), and build/agent181/cap_dfx2d_list.txt:1-69. The record of the third delivery gives file uuids and says the maintainer's read is not yet given (build/agent181/captures_sent_14z181.txt:15-17). No re-freeze has been written yet: tests/expected/df_moves.tsv:15 still holds 2HP at two hits. build/agent181/cap_dfx2d/sheet_donovan_dfx1_ev25.png shows "2 HIT" at +18 and "3 HIT" at +30 on both legs.
Q3: OK — The shared write shown reaching a compared field is the P2 HP pin "ff8850:01200120" (tools/name_moves.py:766). Every one is now moved out of the compared windows ("donovan_dfx1 [[3350, 3314], [4550, 4524], [5750, 5734], [6950, 6944]]", build/agent181/df_moves_contact_run9.log:4). A pin left inside a window is refused (tests/audit_df_moves.sh:241-242). The x pins land at t-40, before each window (tools/name_moves.py:754), and the expiry wait carries no pin (tests/audit_df_moves.sh:158). The level and RNG pins are shared by design (tests/audit_df_moves.sh:64-67), and no artifact shows them writing or selecting p2hp, p2cls, meter or p2meter.
Q4: VIOLATED — Two failure modes are neither controlled nor named by the claim. First, the attribution by NAME has no row-count check and no check for duplicate names. A content change in any duplicate-named row would be silently dropped. Here three rows were dropped this way (build/agent181/df_moves_attribution_run9.txt has one "5LP in DF" line for the four rows at build/agent181/df_moves_got_run9.tsv:1,25,26,32), and the claim's counts are wrong as a result. Second, the pin-in-window control only proves the reader refuses a frame added to the JSON poke set ("if plant: pins.add(lo + 5)", tests/audit_df_moves.sh:240). It does not see the level and RNG writes on every frame of every compared window (tests/audit_df_moves.sh:202). So it cannot back the claim "the rig under test writes nothing inside any compared window". That gap applies directly here: the game reads the RNG in the window ("rng_reads_5hp_window native=630 / ours=621", build/agent181/df_moves_contact_run9.log:40-41).
Q5: OK — The fold of #109 into #136 quotes the maintainer: *"I agree with all recommendations"*, said of each group of the 14z-157 sheet, with "#109: the clone-beam positive-contact damage leg is FOLDED INTO #136" (DECISIONS_HISTORY.md.lines-1257-1282:11-12, :22-23). The no-gauge-in-Dark-Force rule is quoted too: *"I see, it makes sense that you can't build meter during DF"* (tests/audit_df_moves.sh:60-61). The maintainer's read of the captures is recorded as "NOT YET GIVEN" (build/agent181/captures_sent_14z181.txt:17), which matches the claim.
VERDICT: VIOLATED
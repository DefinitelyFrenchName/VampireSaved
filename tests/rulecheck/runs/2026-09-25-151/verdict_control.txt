Q1: VIOLATED — The claim says the SELF leg uses "a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The artifacts show the opposite. audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". audit_forced_pick_fidelity.sh.txt:87,135,138 give the SELF leg `_pick` = "ff8782:$_id", the row's own tenant id, which the real cursor pick already put there. So the effect of a changed-value id write is an untested premise that the claim presents as tested.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here", and it concludes only about RAM bytes.
Q3: OK — The rig pokes both legs share do not reach the P1 block inside the compared pre-match window. The P1/P2 X pins are "{t - 40}:ff8410" with t starting at FIRST_EVENT 2600 (name_moves.py.txt:77,687), so the earliest is 2560. Part 1 of huitzil and pyron has no "near" events (name_moves.py.txt:425,477), and donovan part 1 has no pin field (name_moves.py.txt:206-225). The HP pin starts at 2550 in the P2 block (name_moves.py.txt:699). The level pin (ff8116) and RNG pin (ff80d4) are globals (audit_forced_pick_fidelity.sh.txt:133-134). The pre-match window is 1501-2362 (audit_forced_pick_fidelity.sh.txt:180). The claim itself names the play-window X pin.
Q4: VIOLATED — No control fires if a CHANGED-value write to $FF8782 itself perturbs P1-block state, which would put some or all of the three LATCHED bytes on the poke rather than the confirm. The only poke-mechanism controls are REAL vs SELF, a same-id write (audit_forced_pick_fidelity.sh.txt:26-28,135,281-282), and donovan-self, 0x13 poked over a Donovan confirm (audit_forced_pick_fidelity.sh.txt:48-51, forced_pick_fidelity.tsv:17). Both write a value the byte already holds, so neither performs the swap the POKED leg does. The claim does not name this gap; it says the opposite, that inertness "is tested directly on the same write the poked leg performs". The same-leg and wrong-cursor controls (audit_forced_pick_fidelity.sh.txt:308-322) cover a vanished difference and a wrong identity, not this failure.
Q5: N-A — The packet credits no decision, approval or ruling to the maintainer or anyone else; "report #151 step 1 as done" is the working agent's own recommendation.
VERDICT: VIOLATED
Q1: VIOLATED — The claim's enumeration of shared pokes is contradicted by the artifacts: it says the rig's shared pokes "address the P2 block or the globals and never the P1 block", but name_moves.py.txt:687 emits `pin_pokes += [f"{t - 40}:ff8410:{x1:04x}", ...]` — $FF8410 is P1 block +0x010 — for every event carrying a "far"/"near" field, and every PYRON part "1" event (name_moves.py.txt:425 `[(n, r, g, "far") for n, r, g in MOVEMENT + NORMALS]`) and HUITZIL part "1" event (:477) carries "far"; the gate copies these into both legs unfiltered (audit_forced_pick_fidelity.sh.txt:131-132 strips only `:ff8782:`). The claim also names "the stock pokes at FIRST_EVENT-60", which do not exist on the rows used: name_moves.py.txt:709-710 emits them only for METER_PARTS, and part "1" is in none (:646). Further, "P2's block is asserted empty pre-match on every leg" is asserted only from the POKED-vs-REAL classify (audit_forced_pick_fidelity.sh.txt:276,279); the SELF leg's P2 field is never read (:277 takes only f1 and f6 of `$self`). The pin pokes begin at 2560, after the pre-match window 1501-2362 (:180), so the LATCHED set itself is not masked by them — but the stated basis is wrong, and the frozen play column (forced_pick_fidelity.tsv:15 "2980") is measured with P1's X pinned identically on both legs.
Q2: N-A — The claim draws no behavioural conclusion ("No behavioural conclusion about how the character plays is drawn here"); the tsv's third column is a RAM first-difference frame, not a stated behaviour.
Q3: VIOLATED — Both legs share the rig's P1-block position pin: name_moves.py.txt:687 writes $FF8410 (P1 X, block +0x010) at t-40 before every "far" event from frame 2560 on, on POKED and REAL alike (audit_forced_pick_fidelity.sh.txt:137-138 include `$_base` on every leg), so the legs are forced to AGREE on that P1-block field in play whatever the latch did to position; the claim denies any shared P1-block poke exists. The SELF-vs-REAL "poke mechanism is inert" inference rests on a same-value write (audit_forced_pick_fidelity.sh.txt:26-28), which the claim does name.
Q4: OK — same-leg (audit_forced_pick_fidelity.sh.txt:310-316) fires when the comparator reports an empty set against the frozen non-empty set, covering a comparator that stops seeing differences; wrong-cursor (:318-322) fires the identity/cell assertion on a one-move-short REAL path, covering a REAL leg that confirmed another character; the frozen row comparison (:300-304) fails on any fourth latched offset or a changed first-play frame; donovan-self (forced_pick_fidelity.tsv:17, gate :47-51) covers the poke perturbing state on a same-value write. Not covered, and named by the claim: a changed-value poke perturbation; not covered and NOT named: a shared P1-block write in play ($FF8410, see Q1/Q3).
Q5: N-A — No decision, approval, ruling or closure is attributed to any person in the packet or the three artifacts; "#147 shipped a wrong fix" (audit_forced_pick_fidelity.sh.txt:12-16) and "GitHub #151, 14z-160" are citations, not attributed decisions.
VERDICT: VIOLATED

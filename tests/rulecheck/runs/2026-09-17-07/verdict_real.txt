Q1: OK — the claim's untested list matches the gate: globals and P2 reported, only the P1 LATCHED set and first-play frame frozen (audit_forced_pick_fidelity.sh.txt:37-41, 236-237, 284, 298-305); TRANSIENT/play-only counts printed not frozen (283); REAL-vs-ALT MEASURE-only (90-94, 285-289); SELF and donovan-self are same-value writes (26-28, 47-51, 135-138); the pins are as stated — level $FF8116 from 2000 and RNG $FF80D4 from 2363 (133-134), HP pin $FF8850 from FIRST_EVENT-50=2550 (name_moves.py.txt:699), position pins at t-40 → 2560 for "far" events (687), part 1 absent from METER_PARTS so no stock poke (646). Only imprecision: DONOVAN["1"] rows carry no far/near field (206-225) so donovan_1 has no position pins at all — "before each event" overstates, harmless to the frozen set.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."; the 2980 first-play column (forced_pick_fidelity.tsv:15) is frozen but not interpreted.
Q3: OK — the legs share schedule, pins and instrument as controlled variables; each leg's identity is asserted independently (audit_forced_pick_fidelity.sh.txt:169-179, 278), and the finding is a frozen NON-empty difference (forced_pick_fidelity.tsv:15-16), so a shared defect that made the legs agree fails the gate at 298-305; the one shared premise that could hide a defect — a same-value poke on SELF/donovan-self is indistinguishable from an unapplied poke at the 1600 identity check — is named in the claim ("inferred from them rather than from a changed-value write"), and the changed-value POKED legs prove the poke path live (174).
Q4: OK — donovan-self (same route R,R, same-value poke, must read "-": audit_forced_pick_fidelity.sh.txt:47-51, forced_pick_fidelity.tsv:17) covers "the poke write itself produces the latched set"; wrong-cursor (253, 317-322) covers "the REAL leg confirmed another character"; P2 block empty pre-match (279-280) covers "leg-to-leg noise reads as a latch". Nothing covers a confirm-written byte equal at one of the six samples (classed TRANSIENT, 193-194) or route-LENGTH residue (ALT routes are equal-length to REAL, 94, while POKED is the two-move R,R, 117) — both scoped out by the claim's caveats, not by a control.
Q5: N-A — the claim attributes no decision, approval or ruling to any person; the artifacts' "maintainer" mentions (name_moves.py.txt:260, 392, 463) are not cited by the packet.
VERDICT: OK

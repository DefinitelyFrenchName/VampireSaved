Q1: VIOLATED — The claim names no untested premise ("Not tested: nothing further — the comparison is frame-exact over the whole part"), but the artifacts show several. (a) The comparison does not cover the whole part. Its window starts at the first event and leaves out the intro (audit_move_parity.sh.txt:25-29; :130-135 "--first-event"). (b) Only 12 tenant fields are compared (audit_move_parity.sh.txt:105). The gate says "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters", and P2 is never compared (:34-36). (c) Part 1 contains only movement, normals, air dash and float, all at "far" (name_moves.py.txt:448-460). huitzil_2 to huitzil_10 cover specials, throws, meter and guard cancel; they are frozen DIVERGES and were not run with the flavor (move_parity.tsv:21-29). (d) What was tested was a RAM poke, not the huitzil.toml default that will be built. Neither the gate's pokes (pokes_for :86-103) nor its FIELDS (:105) nor the generator's pokes (name_moves.py.txt:670-682) poke or read $FF87C2, so the 6851/6851 run is not the gate shown. 6851 is also not part 1's length under the generator: the gaps sum to 6380, so frames = 9030, which gives 6431 frames from the first event at 2600 and 6731 from FIELD_FROM=2300. (e) The consumer enumeration ("pristine vsavj: zero") appears in no artifact.
Q2: VIOLATED — The conclusion is that Phobos's jumps, air dashes and floats now play as they do on native (name_moves.py.txt:451-459; the unpoked verdict is "huitzil_1	DIVERGES	Jump [8]+21f", move_parity.tsv:20). It rests only on a count, "6851/6851 frames identical". The gate writes a text field trace (audit_move_parity.sh.txt:118 "FIELD_OUT=\"$W/tr_${_t}_${_p}_$_leg.txt\""), and no artifact shows an image or recording being put before the maintainer.
Q3: VIOLATED — Both legs get the same pins every frame: "ff80d4:0000" from 2363 and "ff8116:06" from 2000 (audit_move_parity.sh.txt:89-90; applied to native at :98 and to ours at :101). The gate itself says these select the compared state: "char init draws from it" (:24), "The intro variant is an RNG draw at char load" (:26), and without the level pin "an unpinned comparison measures the play mode" (:18-19). lvl is itself a compared field (:105). The shared json pokes (:88) also write two other compared fields on both legs: P1 x, through the far pin "ff8410" 40 f before every event (name_moves.py.txt:658; every part-1 event is "far", :448-460), and id "ff8782" (:671-672). So if our Phobos used the RNG or handled the speed level differently from native, the pins would overwrite that identically on both legs every frame, and the legs would agree.
Q4: VIOLATED — The gate's two must-fire controls run only on the first part of SET (audit_move_parity.sh.txt:182), and both depend on its frozen verdict (:188). For huitzil_1 the frozen verdict is DIVERGES (move_parity.tsv:20), so no-translation reports "CONTROL DEAD" (:194-196) and unpinned-level prints "CONTROL FIRED" whatever it measures (:203-208). No control fires on a false IDENTICAL for Phobos. The claim also neither controls nor names three failure modes: (1) whether the runtime poke behaves like the built flavor_default: every leg runs BUILD=build/m3b_merged26 (:48), and no leg ran a build with flavor_default=0x01; (2) Phobos on the P2 side: $FF87C2 sits in P1's block beside id ff8782, P2's id is ff8b82 (name_moves.py.txt:672-674), and move_parity.tsv has no huitzil_victim row; (3) the flavor's effect on huitzil_2 to huitzil_10 and on damage, meter, hitboxes and projectiles (:34-35).
Q5: N-A — The claim attributes no decision, approval or ruling to anyone. "flavor_default becomes 0x01 and the five tracks are rebuilt and frozen as M19" is the working agent's own action.
VERDICT: VIOLATED

Q1: VIOLATED — The claim says all five runs were "read back through the shipped reader as OK", but the evidence file has reader output for only two of them: build/gatequal180/q2_evidence_14z180.txt:31 "## the reader over the two MAME logs", with lines 32-33 covering only test_meter_gain and test_vanilla_frame_join. No reader result is shown for charmap_current, release_os_metadata or the contract gate. Three other premises go unstated. (a) No named artifact shows "the three runner ground truths unchanged and green". (b) The binary-record-rows basis was read from the release copies (q2_evidence_14z180.txt:28 "release/merged-m19/fbneo/emulator/bin/macos-arm64/BINARY.txt 4 rows"). The gate reads different files: tests/test_release_binaries.sh:97,126 reads `ROOT="${RELEASE_EMULATORS:-release/emulators}"` and `FB="$ROOT/fbneo/$OSARCH"`. (c) The dogfood count is a hard-coded constant, not a count: tests/test_measures_contract.sh:106 "vs_measured synthetic-cases 14".
Q2: N-A — The claim is about how the verdict classifier and the measurement floors behave. It draws no conclusion about anything a player could feel.
Q3: VIOLATED — Each floor and the value it is checked against come from the same run and the same count expression, so the "read back as OK" agrees by construction. tests/test_meter_gain.sh:23 "49 measured 2026-09-24" is checked against build/gatequal180/meter_gain_q2.log:16 "MEASURED: meter-gain-rows = 49". The "ok … (N rows)" lines that sit beside each MEASURED line in the evidence file are not a second witness. They use the identical expression on the identical file: tests/test_meter_gain.sh:156 and :157 both compute `grep -c . "$W/table.tsv"`, and tests/test_vanilla_frame_join.sh:151/152 and :200/201 both compute `grep -c . "$W/g.txt"` and `grep -c . "$W/hg.txt"`. A defect in what the expression counts would appear in both, and they would agree.
Q4: VIOLATED — The only controls are in tests/test_measures_contract.sh:100-103 (missing-measurement, below-floor). They push hand-written logs from synthetic stub gates through the reader. They prove the reader, not that any of the five real gates' MEASURED line follows its hashed table. No control empties or shrinks a real gate's table to show its printed count falls below the floor, which is the M19 failure this slice exists to catch. The claim does not name this gap. The dogfood shows the risk directly: tests/test_measures_contract.sh:106 prints the constant "vs_measured synthetic-cases 14" whatever runs, and the file has 17 synthetic checks (3 in §1, 6 in §2, 6 in §3, 2 in §4). The binary floor of 2 also adds nothing. tests/test_release_binaries.sh:250 already fails a record with no rows ("FAIL: $d/BINARY.txt has no sha256 rows"), so 2 is below anything a passing run can print, even though docs/project/gate_header_contract.md:128 says "A floor is the count at the freeze it was written against". The claim names the floor of 2 as unmeasured, but not that it can never fire.
Q5: N-A — The claim and subject attribute no decision, approval or ruling to the maintainer or anyone else. (The artifacts' own "ruled 2026-09-24 "Declared/fired grammar"" in tests/lib/measures.sh:3 is not cited by the claim.)
VERDICT: VIOLATED

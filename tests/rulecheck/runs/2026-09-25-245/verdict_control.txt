Q1: VIOLATED — The claim says the SELF leg is "a poke of a DIFFERENT id (a changed-value write)", which it counts as a tested premise. The gate says the opposite. build/rulecheck/2026-09-25-245/a/audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Lines 114 and 135 build the SELF poke from `_id="$(row_id "$_row")"`, which is the same tenant id the REAL leg already holds. So the claim presents an untested premise as tested: that a changed-value id write leaves the P1 block unchanged. Its list of untested premises is therefore incomplete.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here". The frozen first-play frame (forced_pick_fidelity.tsv:15, "2980") is not interpreted as behaviour.
Q3: OK — The legs share the base, level and RNG pokes. None of them reaches the P1 block inside the pre-match window (1501-2362). Position pins start at t-40 = 2560 (name_moves.py.txt:687, FIRST_EVENT 2600) and the HP pin writes ff8850 from 2550 (name_moves.py.txt:699). The level pin writes ff8116 from 2000 and the RNG pin writes ff80d4 from 2363, both globals (audit_forced_pick_fidelity.sh.txt:133-134). The one shared write that does land in the P1 block, ff8410 in play, is named by the claim.
Q4: VIOLATED — Nothing controls the failure mode "the changed-value id write at 1400/1450/1500 itself produces the LATCHED bytes, so they are wrongly charged to the confirm". The claim does not name this failure mode; it says the opposite, "tested directly on the same write the poked leg performs". The control it relies on, REAL vs SELF (audit_forced_pick_fidelity.sh.txt:272, 281-282), pokes the id the leg already holds (lines 26-28, 135). The donovan-self row is also a same-value write: 0x13 over R,R, which is Donovan (lines 48-51, 87). Both show only that a no-op write is inert. That is a different thing from the changed-value write in the POKED leg.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to the maintainer or to anyone else.
VERDICT: VIOLATED
Q1: VIOLATED — the claim says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)". The gate shows the opposite. audit_forced_pick_fidelity.sh.txt:26 reads "SELF — the REAL path PLUS the same-id poke … (a write at 1400-1500 to an id that already holds that value)". At line 135 the SELF poke uses `$_id` = row_id of the same row, and line 138 sends every non-real leg through that same `_pick`. So the claim's premise that a changed-value poke is "tested directly" is false, and the artifacts show no stated basis for it.
Q2: N-A — the claim says "No behavioural conclusion about how the character plays is drawn here", and the frozen TSV holds only offsets and a frame number.
Q3: OK — the shared writes reach no compared state inside the pre-match window. The level pin is `ff8116` from 2000 and the RNG pin is `ff80d4` from 2363 (lines 133-134), both globals, which diff_region(0x400,0x800) (line 189) does not compare. The position pins at `ff8410`/`ff8810` land at t-40, first at 2560 (name_moves.py.txt:687), and the HP pin lands at FIRST_EVENT-50 = 2550 (name_moves.py.txt:699), all after the last pre sample, 2362 (line 180). The claim names the one exposure (the first-play frame is measured with P1's X pinned).
Q4: VIOLATED — failure mode: the id poke itself perturbs state when it CHANGES the value, which is what the POKED leg does, writing 10 or 11 over Donovan's 13. No control covers it and the claim does not name it. Both negative controls write a value the byte already holds: SELF uses the same id (line 135), and donovan-self is "poking 0x13 over Donovan's own confirm" (line 49). The claim asserts the opposite ("tested directly on the same write the poked leg performs"). The must-fire controls do not cover it either: same-leg (line 310) and wrong-cursor (line 318) test the reducer and the identity check, not the poke mechanism.
Q5: N-A — no decision, approval or ruling is attributed to anyone.
VERDICT: VIOLATED

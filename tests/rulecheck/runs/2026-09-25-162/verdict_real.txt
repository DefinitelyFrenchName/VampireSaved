Q1: OK — The claim names what it rests on without testing: the shared level and RNG pins, the level-8 choice, P2 x left unsampled, the masked @entry index, the 8-label and no-control limits of the poked check, Pyron's part-2 index shift (a:0x13@8 for frozen @7, tests/expected/victim_parity.tsv:42), the shape check skipping pointers and box tables, and the throw charge's reliance on the census, its delta rule and the same-size-shift blind spot. What the artifacts show fits that list. The throw's third hit lands on the same frame on both legs (4319 in "p2hp steps", build/agent181/victim_throw_spans.txt:7,13) even though a2:0x36 is the same data on both, and that falls inside the named "cannot tell the side of a same-sized shift" gap.
Q2: VIOLATED — The claim draws timing conclusions a player could feel: "FIVE of the six throw hits land one frame earlier on our leg" and the release "len +1 / freeze 11 for 10". Both captures came after those conclusions: "AFTER the RAM measurement that found it" and "AFTER the attribution was drawn from the traces" (build/agent181/captures_sent_14z181.txt:20-21). Neither has been read: "the maintainer's read: NOT YET GIVEN".
Q3: VIOLATED — Both legs get the same pokes file, with the level and RNG written on every frame: "\"%d:ff8116:08\" % f for f in range(2000, fr)" and "\"%d:ff80d4:0000\" % f" (tests/audit_victim_parity.sh:170). The same "$W/${TENANT}_$p.pokes" goes to vsav2 and to vsavjw (tests/audit_victim_parity.sh:258-259). The level pin moves the compared lines: "reads 13 line(s) differently from the level-8 pin" (build/agent181/victim_parity_verify8.log:38). The RNG's bit 0 "picks P1-first or P2-first on every pass" (DECISIONS_HISTORY.md.lines-1218-1240:22-23), so "a port defect in the RNG's ADVANCE would be reset away on both legs and read as agreement". That premise is still under "Decisions pending", not ruled (STATE.md.lines-201-232:24).
Q4: OK — Controls cover the ways this claim could fail: extract-planted (the extract is vsav2's own bytes), chains-planted (ported shapes), label-planted (walk-into-data labels), tenant-planted and timing-planted (charging by co-occurrence or wrong size), p1-planted (attacker entered a differing reaction chain), pin-in-window (a rig write inside a window), no-translation, unpinned-level and hit-dropped. The verify run shows all ten firing (build/agent181/victim_parity_verify8.log:5,10,21,24,27,36,38-40,108), and each of the ten mode logs ends "FAIL: audit_victim_parity (control mode)". The remaining gaps are named in the claim: the poked check has no control, a whole table misclassed passes vacuously, a same-size shift can't be assigned a side, and an attacker-driven selection is charged to the tenant.
Q5: OK — The one maintainer decision cited, matched modes on 2026-09-15, is quoted in their own words: "Force the same speed level on both legs and assert ours == native at NORMAL (level 6) and TURBO (level 8)" (DECISIONS_HISTORY.md.lines-1218-1240:13-14). The pin acceptance is credited to runs 153-155 and stated as pending (STATE.md.lines-201-232:24). The capture reads are recorded as "NOT YET GIVEN".
VERDICT: VIOLATED

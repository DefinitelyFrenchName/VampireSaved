Q1: VIOLATED — The claim says "Not tested: nothing further", but the artifacts show at least three untested premises. (1) The gate's own scope excludes things: audit_move_parity.sh.txt:34 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters", and :35-36 "P2 ... his state is never compared". (2) Only part 1 was run, and HUITZIL has parts "1" to "10" (name_moves.py.txt:447-552, 610). (3) A runtime poke of ours was used in place of the rebuilt flavor_default, and that poke is in no artifact: pokes_for (audit_move_parity.sh.txt:86-103) never writes ff87c2, and FIELDS (:105) never reads it. So neither "reads 0x01 ... and 0x00 on ours" nor "6851/6851" appears anywhere; the gate emits a verdict, not frame counts (:133-135, :177). The frozen row also contradicts the claim: move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f". The enumeration of every consumer "(pristine vsavj: zero)" is shown by no artifact either.
Q2: VIOLATED — The conclusion is that Phobos's part-1 moves play identically to native, and name_moves.py.txt:448-459 shows part 1 includes Jump, Air Dash and "Float [j.8 hold]". It rests on the number "6851/6851" alone. No image or recording, and no maintainer viewing of one, appears in any artifact.
Q3: VIOLATED — Both legs get the same base pokes from the rig JSON (audit_move_parity.sh.txt:98 native "%s;%s;%s" "$_base" "$_lvl" "$_rng" and :101 ours "$_pick" "$_base" "$_lvl" "$_rng"), and some of these write compared fields. Every huitzil part-1 event is "far" (name_moves.py.txt:448), so both legs get P1 X re-poked 40 frames before each event: name_moves.py.txt:658 "f\"{t - 40}:ff8410:{x1:04x}\"". That same address is a compared field: audit_move_parity.sh.txt:105 "ff8410:w:x". The level ff8116 is also poked every frame on both legs (:89) and compared as "ff8116:b:lvl" (:105). The P1 id ff8782:10 is poked on both legs (name_moves.py.txt:672, audit_move_parity.sh.txt:92) and compared as "ff8782:b:id" (:105). A defect these pins hide (for example X drift) would be reset on both legs, making them agree.
Q4: VIOLATED — The gate has only two controls, "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:4-5, :60-62). Neither fires if the flavor byte does not reach the compared state, if the runtime poke does not represent the rebuilt flavor_default, or if the fields cannot see flavor-dependent behaviour. What is missing is a perturbed leg with ours at ff87c2=0x00 that must diverge. The claim does not name this gap. The controls also run on "the control part $CTL", the first part of SET (:182). For huitzil_1, frozen DIVERGES (move_parity.tsv:20), the gate prints "CONTROL DEAD: no-translation — the control part $CTL is not frozen IDENTICAL, so the control cannot discriminate" (:195). So on this part the instrument's own control is dead.
Q5: N-A — The claim attributes no decision, approval or ruling to any person; "flavor_default becomes 0x01" is the working agent's own proposal.
VERDICT: VIOLATED
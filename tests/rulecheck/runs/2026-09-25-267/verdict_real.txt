Q1: VIOLATED — The claim rests on premises that were never tested and are missing from the "Not tested" list (build/agent183b/plan_153.md:63-73). (1) The design's central premise was never exercised: no run changed the working tree while a clone or snapshot run was going, yet S2 asserts "so nothing the working tree does during the run reaches it" (build/agent183b/plan_153.md:48). The clone is also `git clone --shared` (build/agent183b/plan_153.md:12), so it reads the tree's object store. (2) Plan line 32 says "So the runner agrees between clone and tree on all 178", but the real runner never ran in the tree. The tree side is run50.sh (build/agent183b/run50_tree.txt:1), which the claim itself calls "not the runner", plus one gate (build/agent183b/staleness_tree_freeze.out). (3) The clone's staleness FAIL is said to "fail identically", but its log never names the three stale gates; it shows only "== 3. controls ... FAIL" (build/agent183b/runner_clone_supplied.log:32-37). (4) The snapshot timing covers build/ only (build/agent183b/snapshot_timing.txt:1). The submodule checkouts and siblings that S2 also snapshots were not timed.
Q2: N-A — The claim is about test-tier infrastructure. It draws no conclusion about how anything plays, times or hits.
Q3: VIOLATED — S1's evidence compares the clone's emu/fbneo porcelain with the working tree's: "the same eight patched files as the working tree — clone_porcelain.txt" (build/agent183b/plan_153.md:46; build/agent183b/clone_porcelain.txt:2-20). But the clone's emu/fbneo checkout was itself a copy-on-write copy taken from the working tree (build/agent183b/plan_153.md:19 "emu/fbneo and emu/jtcores checkouts with their .git/modules dirs"). Before that copy, the probe clone had no checkout at all (build/agent183b/probe153_static.log:172 "FAIL: no emu/fbneo submodule"). The shared copy writes the compared state, so a wrongly patched tree would agree with itself. No check against the patch files in emu/*-patches/ appears in any artifact.
Q4: VIOLATED — No control would fire if the working tree reaches a clone or snapshot run while it is in progress, for example through the shared object store of `git clone --shared` (build/agent183b/plan_153.md:12) or a gate resolving a path in the tree. Controls A-C only show that supplying out-of-git inputs turns 50 gates from non-PASS to PASS (build/agent183b/run50_clone.txt, build/agent183b/runner_clone_supplied.log:209). No run changed the tree during a clone run. This failure mode is not named in the "Not tested" list (build/agent183b/plan_153.md:63-73).
Q5: OK — The only decisions attributed to the maintainer are quoted verbatim in the maintainer's own words: build/agent183b/plan_153.md:4-6 "you don't have to reuse the same mechanism used elsewhere..." and plan_153.md:8-9 "my gut feeling is that the gain for session is marginal but the gain for freeze or release is massive...".
VERDICT: VIOLATED

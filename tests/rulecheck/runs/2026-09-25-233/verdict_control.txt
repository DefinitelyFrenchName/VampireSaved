Q1: VIOLATED — the claim gives the SELF leg as the basis for "the poke mechanism is inert": "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)". The gate says the opposite. audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Line 135 builds the self leg's poke from the row's own id ("f'{f}:ff8782:$_id'"), and line 138 applies it to the self leg. So the untested premise is misdescribed: the poke that changes the value is never tested for inertness. The other stated items match the artifacts (lines 99/180 give six pre-match samples; lines 208-211 and 285-289 are MEASURE-only; line 279 checks P2 only on POKED-vs-REAL; line 284 freezes only the latched set and the first-play frame; name_moves.py.txt:687, 699 and 646 match the pin, HP-pin and stock statements).
Q2: N-A — the claim says "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — both legs share the rig pokes plus the level and RNG pins (audit_forced_pick_fidelity.sh.txt:131-138). None of them reaches the P1 block inside the 1501-2362 window: "{f}:ff8116:06" for f in range(2000,…) and "{f}:ff80d4:0000" for f in range(2363,…) are globals. The only P1-block write is the X pin "{t - 40}:ff8410", from frame 2560 at the earliest; part 1 is all "far" (name_moves.py.txt:687, 425, 477). The HP pin ":ff8850:" starts at 2550 (name_moves.py.txt:699). No artifact line shows a shared input writing the compared pre-match state.
Q4: VIOLATED — failure mode: the poked leg's write changes the value at $FF8782 (0x13 to the tenant's id), and that write itself could disturb the P1 block, so bytes charged to the confirm could come from the poke. The claim does not name this as a gap; it calls it "tested directly on the same write the poked leg performs". The control offered, REAL vs SELF (lines 272 and 281), writes the value "an id that already holds" (line 28). The donovan-self row is also a same-id write ("the same-id poke must latch nothing", forced_pick_fidelity.tsv:11-12). Both controls test a different write from the one the claim rests on, so neither covers this failure.
Q5: N-A — the packet attributes no decision, approval, ruling or closure to anyone.
VERDICT: VIOLATED

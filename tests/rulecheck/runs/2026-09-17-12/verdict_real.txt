Q1: VIOLATED — The named premises check out (globals/whole-RAM MEASURE-only: audit_forced_pick_fidelity.sh.txt:201-235; TRANSIENT echoed not frozen: :283-284; P2 asserted on POKED-vs-REAL only: :279, REAL-vs-SELF read as fields 1 and 6 only: :277,281; ALT route-residue MEASURE-only: :90-94,285-289; gate pins in globals: :133-134; rig pins at t-40=2560 and HP pin at 2550: name_moves.py.txt:687,699; no stock poke for part 1: :646,710). Two things the claim states that the artifacts do not show: (a) "so it is a first difference outside the pinned field" — :197-198 excludes only `latched` offsets from first_play, so the pinned +0x010/+0x011 is inside the search, and forced_pick_fidelity.tsv:15 freezes the frame 2980 without its offset; nothing shows the first play difference lies outside P1's X. (b) "every one of the six pre-match samples" — :258 asserts only >=30 dumps per leg and :180 builds `pre` from whatever dumps both legs share, so a pre-match dump missing on BOTH legs shrinks the sample count silently and loosens LATCHED; the six-sample premise is untested and unnamed.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — POKED and REAL share MAME, replay.lua, the rig's events from 2000 (:126) and its pokes (:132) and the gate's pins (:133-134); the shared writes inside the 1501-2362 window are globals only ($FF8116, $FF80D4), and the in-block pins land at 2560 (name_moves.py.txt:687) and 2550 (:699), so no shared write can equalise a P1-block byte at the pre-match samples; the claim names these.
Q4: OK — donovan-self (:47-51,87-89; forced_pick_fidelity.tsv:17) fires if the poke mechanism itself adds a latched byte, inflating "exactly three"; wrong-cursor (:317-322) fires if the real leg confirmed another character; same-leg (:310-316) fires if the frozen difference vanished; the P2 pre-match assertion (:279-280) is the in-run negative. Not covered: an under-count from a shared dump defect — there is no injected-difference positive control, only the non-empty frozen set evidences that the dump reads the block.
Q5: N-A — the packet attributes no decision, approval or ruling to anyone; "report #151 step 1 as done" is the recommendation itself.
VERDICT: VIOLATED

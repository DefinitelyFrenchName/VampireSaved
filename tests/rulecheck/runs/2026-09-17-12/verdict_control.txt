Q1: VIOLATED — the claim names no untested premise ("Not tested: nothing further — the comparison is frame-exact over the whole part"), but the artifacts show several: audit_move_parity.sh.txt:25 "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor"; :34-36 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters ... P2 is Victor ... his state is never compared"; :105 compares only twelve fields (FIELDS=...), not frames; and pokes_for (:86-103) carries no ff87c2 poke at all, so the "ours poked to 0x01" leg is not produced by the named gate, while move_parity.tsv:20 freezes "huitzil_1	DIVERGES	Jump [8]+21f", not IDENTICAL, and the gate emits no "6851/6851" count anywhere.
Q2: VIOLATED — the VS2/VH2 flavor latch selects how Phobos plays, and the conclusion "identical to native" is a number from a field trace; no image or recording appears in any artifact, and name_moves.py.txt:11 itself warns "A scripted input is NOT the move it names ([VSE-47])".
Q3: VIOLATED — both legs share the rig, its inputs and its pokes: audit_move_parity.sh.txt:10-11 "the same rig, the same inputs, on native vsav2 and on the merged WIDE build"; :88 applies the json's pokes (name_moves.py.txt:670-682: HP pin, id pin, position pins, stock pokes) to BOTH legs; a recipe that fires no move, or a pin that masks the effect, reads UNFIRED/identical on both sides and the legs agree (name_moves.py.txt:17-18 "a move whose window shows no new chain is reported UNFIRED").
Q4: VIOLATED — the gate's only controls are unpinned-level and no-translation (audit_move_parity.sh.txt:4-5), which prove the level pin and the pointer translation are load-bearing, not that the ff87c2 poke took or that the flavor byte's consumers fall inside the compared window/fields; the controls run on the FIRST part of SET (:182, donovan_1 by default), and the no-translation control declares itself unable to discriminate on a part frozen DIVERGES (:195), which huitzil_1 is (move_parity.tsv:20). Nothing covers: ours at flavor 0x00 vs 0x01 must diverge, and the poke must precede every consumer's first read.
Q5: N-A — the packet attributes no decision, approval, ruling or closure to the maintainer or any person; "the M19 freeze" is the working agent's own proposed action.
VERDICT: VIOLATED

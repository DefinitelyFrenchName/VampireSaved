THE PACKET

Decision kind: recommendation
Subject: report #151 step 1 as done — what a forced-pick native leg gets wrong is measured
Claim (the working agent's sentence): Within the P1 fighter block, a forced-pick native leg differs from a real cursor pick of the same tenant in exactly three LATCHED bytes (+0x3BD, +0x3E0, +0x3C2) — LATCHED meaning differing at every one of the six pre-match samples from the last poke (1501) to the frame before the match anchor (2362) — for Phobos and Pyron alike, and a same-id poke over Donovan's own confirm latches nothing. What this rests on that the gate does NOT test or freeze: the globals ($FF8000-$FF83FF) and the rest of work RAM are reported under MEASURE only and never asserted, so "exactly three" is a statement about the P1 block; the TRANSIENT count is reported, not frozen; P2's block (Victor on every leg) is asserted empty pre-match on the POKED-vs-REAL comparison only — the REAL-vs-SELF comparison reports the P1 block alone; two real picks of the same cell by different routes leave route residue (measured under MEASURE, never frozen), so a latched byte is charged to the confirm only inside the P1 block; the SELF leg and the donovan-self row are both same-value writes, so "the poke mechanism is inert" is inferred from them rather than from a changed-value write; the rig's own pokes copied into both legs (name_moves.py: the P1 and P2 position pins at $FF8410/$FF8810 before each event, from frame 2560 at the earliest, and the P2 HP pin at $FF8850 from 2550; no stock pokes, since part 1 is not a meter part) and the gate's own pins (the level at $FF8116 from 2000 and the RNG at $FF80D4 from 2363, both globals) all land outside the P1 block within the pre-match window, so they cannot mask a LATCHED byte on both legs — but the frozen first-PLAY-difference frame is measured with P1's X pinned identically on both legs, so it is a first difference outside the pinned field; whether any latched byte is READ in play is a separate tap, not this gate. No behavioural conclusion about how the character plays is drawn here.
Artifacts (read every one, in full):
  - audit_forced_pick_fidelity.sh.txt (the gate; a shell script)
  - forced_pick_fidelity.tsv (its frozen expectation)
  - name_moves.py.txt (the rig generator whose schedule and pokes both legs copy)

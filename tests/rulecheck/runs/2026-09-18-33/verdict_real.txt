Q1: VIOLATED — the claim states as measured what the artifacts do not show: (a) "all 28 placed sites naming vs2's displacements ... are writes" — build/meter_probe_14z166/census.txt lines 125-153 list 28 sites of which 4 (lines 125-128) are region=vsavj-engine and 2 of those are READ; the placed sites are 24, all writes — the figure and the set are misdescribed in the claim and copied into docs/game/engine_internals.md line 31 and docs/project/gotchas.md line 19; (b) "reached through a dispatch table rather than a branch (no bsr/jsr/bra targets it in the engine range)" — no artifact contains a branch-target search (census.py parts 1-4 search adder jsr sites and displacement bytes only; disasm.txt line 302 only lists the code); (c) the "ONLY vsavj instructions reading those addresses" rests on an unnamed premise: census.py line 44 sweeps only the d16(a5) byte patterns b48c/b48e/b4c3, so a read through a pointer (lea of -$4b76/-$4b78(a5) then (a0)+) or an absolute $FF348C access is outside the census, and the claim does not say so; (d) the rig's input fidelity is an unnamed premise — build/meter_probe_14z166/pyron_3/readout.txt line 3 "alarms native: ['INPUT-VIOLATION 5880 :IN0 live 7f7f expected 7f67', 'INPUT-VIOLATIONS 3'] ours: []" and donovan_5/readout.txt line 3 (violations at 5211 on both legs), while tests/lua/read_tap.lua carries no input check at all, so the gate's rows cannot see it.
Q2: VIOLATED — the conclusion is about a player-feelable thing (which fighter's meter bar fills on a throw) and the claim itself states the capture came after: "was produced AFTER the conclusion had been drawn from the RAM taps"; the only capture artifacts are build/meter_probe_14z166/captures/meter_sheet_pyron3.png and captures/ours/index.txt lines 1-5 / captures/native/index.txt lines 1-5 (four snapshots at 3030/3070/3930/3980), and no artifact shows the maintainer received them.
Q3: OK — the legs share the rig, pokes and cursor routes (tests/audit_throw_registration.sh lines 60-75: pokes to ff8850, ff8116, ff80d4, ff8410/ff8810, and ff8509 in donovan_5.json), but none of the shared writes reaches the compared state (the meter words $FF850A/$FF890A at lines 133-138, the live pair $FF343A-D, the dead pair $FF348C-F), the contact sets are defined per leg by different writer PCs (line 101), and the compared rows DISAGREE between legs (tests/expected/throw_registration.tsv lines 10-16 vs 18-24), which a shared defect would not produce.
Q4: OK — legs-swapped (tests/audit_throw_registration.sh lines 122, 154, 193-196; build/gates_14z166/throw_reg_ctl_swapped.log rc=1 with the p1/p2 diffs on all three ours legs) covers the failure "the frozen rows do not actually record the attacker/victim direction, so the reverse would pass"; dead-reader-planted (lines 123, 149, 197-200; throw_reg_ctl_planted.log adds 0189a0 to every dead-pair-readers row, rc=1) covers "a vsavj reader of $FF348C-F firing in play would go unnoticed"; the tap's own visibility of reads at that address is evidenced by the boot-test R lines the frozen dead-pair-readers row requires (throw_reg_verify.log "R 5 PC 000d32 off ff348c"), and a log without END is VOID (line 96). The remaining gaps (contact frames rig-bound, wrap, placed-site-defined contacts, Pyron's air throw, the ring trigger, the scaler reads, one-host determinism) are named by the claim.
Q5: N-A — no decision, approval, ruling or closure is attributed to the maintainer or anyone; the claim says only that the capture "was put before the maintainer", and the documents leave the fix "the maintainer's to schedule" (docs/game/engine_internals.md line 68; build/manifest/huitzil.toml line 20 "Untouched pending the maintainer's ruling").
VERDICT: VIOLATED

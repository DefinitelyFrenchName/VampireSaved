Q1: VIOLATED — The claim says the classes rest on "six reader workers" and that "the returns are the artifacts". The artifacts show five reader specs: A, B/C/D and E (build/agent181/reader_returns/groupA_14z181.txt:1, groupBCD_deciding_lines_14z181.txt:1, groupE_14z181.txt:1). They are also extracts made by the orchestrator, not the returns: "the DECIDING lines only, cut by the orchestrator from the verbatim hand-backs; the full returns ... are in this session's transcript" (groupBCD_deciding_lines_14z181.txt:1-2). Some deciding premises appear in no reader quote at all. One is the test_hitbox_encoding WATCH row's "a Lua poke has no A3, so the tap does not see the pins" (build/agent181/poke_rulings_packet_14z181.tsv:46, where the reader gives only "WATCH="ff8850,6" l.107 (-debug write tap)", groupBCD_deciding_lines_14z181.txt:20). Another is the "HANDOFF [VSP-123]" basis for test_pyron_cosmo ff8782 (poke_rulings_packet_14z181.tsv:65). The claim names none of these premises as untested.
Q2: N-A — No conclusion about how the game plays is drawn: "Nothing a player feels changes" (DECISIONS_HISTORY.md.lines-30-58:28).
Q3: OK — The only comparisons are the eight re-runs against their gates' own frozen expectations (e.g. build/agent181/rerun_advancing_guard.log:19 "44 lines identical to tests/expected/advancing_guard.txt") and the census against the table. No artifact shows a shared rig, poke or tool writing the columns that decide whether the drop changed a verdict.
Q4: OK — Two failure modes are controlled. A RETIRED row that still derived would appear as a "finding with no row" and FAIL (tests/test_poke_readback.sh:89-97). The unclassified-finding control proves that path fires (build/agent181/pokegate_14z181.log:17), and the log shows 53 derived = 53 live (pokegate_14z181.log:2-4). A dropped column that was actually read would change or break the eight gates' frozen compares, and all eight PASS on them (e.g. rerun_vanilla_frame_join.log:7,11; rerun_df_startup_invuln.log:24). The per-leg question is one the claim names as untested.
Q5: OK — The maintainer's own words are quoted: "**Ruling (verbatim):** *"The 73 poke read-back rulings are in front of you -> I agree with the proposal"*" (DECISIONS_HISTORY.md.lines-30-58:26), against the packet at build/agent181/poke_rulings_packet_14z181.tsv.
VERDICT: VIOLATED
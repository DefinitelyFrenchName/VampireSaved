Q1: VIOLATED — The claim says every pattern is "the shortest distinctive fragment without trailing punctuation" and that no grep hit is "a live claim". Neither is tested, and neither is named as untested. Run 183's retracted wording was grepped with a trailing semicolon, "== 'from a pin the screen edge clamped to 664;'  (run 183): 0 hit(s)" (build/agent181/retraction_greps_14z181.txt:47). The same wording is still live in (ee)'s own home, docs/project/gotchas.md:50-51: "P1 pushing at ~2 px/frame from a pin the screen edge / clamped to 664 — leaves every near event whose previous event did not knock P2 left". That is the figure resolve183 says it corrected: "the wall is reached from 664 where the pin was clamped AND from 552/563 where it was not". The line break also hides it from a line-based grep. REACH CONTROL 2 is described as finding "the generator comment / the gotcha", but it prints "1 hit(s)", only tools/name_moves.py:185. The gotcha writes "(`LEAD=0` for the walk" with backticks (docs/project/gotchas.md:103), so the grep misses it and the control still passes.
Q2: VIOLATED — (ee) concludes a hit: "pyron_3's event 5, named "far (whiff)" by the rig, in fact connects" (docs/project/gotchas.md:91-92, "Event 5 CONNECTS as a grab ... HP 288 -> 273 at f4973"). (dd) concludes that both throw strengths connect. The claim says "(dd) and (ee) are RAM findings with no capture". docs/NEXT_SESSION.md:27-32 lists five captures and neither of these is among them; resolve183.txt:1 only "ACCEPTED" the gap. (q)'s Pyron Zodiac Fire guard cancel also has no capture: only "Donovan's Ifrit Sword guard cancel" is listed.
Q3: N-A — The packet concludes that each finding has a home and a test, and makes no ours-vs-native or before/after comparison of its own. The leg comparisons it restates, (aa), (bb)/(cc) and (i), belong to their own gates (build/agent181/trap_air_mode4_air-hit.log, build/agent181/victim_parity_mode5_unpinned-level.log).
Q4: OK — The failure shown under Q1, a pattern too narrow for its carrier (trailing punctuation, a line-wrapped carrier, a backticked token), falls under the gap the claim itself names: "that a fragment pattern is wide enough for every carrier ... checked by my reading, not by a tool". A stale figure in a home falls under "that a live carrier's FIGURES agree with the findings". The cited controls fire on what they cover: "CONTROL FIRED: air-hit" and "FAIL: audit_trap_airborne (control mode)" (build/agent181/trap_air_mode4_air-hit.log:10,19); "CONTROL FIRED: unpinned-level ... so the level pin is load-bearing" (build/agent181/victim_parity_mode5_unpinned-level.log:36).
Q5: N-A — The claim credits no person with a decision, approval or closure. It says "the maintainer's read is theirs", and (t) points to STATE "Decisions pending" as pending (docs/NEXT_SESSION.md:22-27).
VERDICT: VIOLATED

Q1: VIOLATED — The claim treats one premise as tested when the gate does not test it. The claim says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The gate says the opposite: build/rulecheck/2026-09-25-184/a/audit_forced_pick_fidelity.sh.txt:26-27 "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". Line 135 builds the self poke from `$_id` = row_id of the same row (`f'{f}:ff8782:$_id'`), and line 138 applies it to self. So whether a changed-value write to $FF8782 (0x13 to 0x10/0x11, which only the POKED leg performs) is inert is untested, and the claim does not list it as untested. The other stated gaps match the script: globals and WHOLE are MEASURE-only (lines 106-107, 208-211); only row, latched and first-play are frozen (line 284); the P2 check is skipped on SELF (line 272 uses only cut -f1/-f6); ALT is MEASURE-only (lines 249-251, 285-289).
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here", and the packet draws none.
Q3: OK — The legs share the rig's pokes, the level pin and the RNG pin (audit_forced_pick_fidelity.sh.txt:131-138). None of them reaches the P1 block inside the compared window 1501-2362 (line 180). The position pins land at t-40 ≥ 2560 (name_moves.py.txt:687, FIRST_EVENT 2600 at line 77). The HP pin is at $FF8850 in P2's block from 2550 (line 699). $FF8116 and $FF80D4 are globals (lines 133-134). The shared P1 X pin in play, which touches the frozen first-play frame, is named by the claim.
Q4: VIOLATED — One failure mode has no control and the claim does not name it: the changed-value id poke itself writing into the P1 block. Both inertness controls perform same-value writes. SELF writes the tenant's own id (audit_forced_pick_fidelity.sh.txt:135, 26-27). donovan-self pokes 0x13 over Donovan's 0x13 (lines 48-51; forced_pick_fidelity.tsv:17 "donovan-self	-	-"). The must-fire controls cover other failures: same-leg covers a vanished difference and wrong-cursor covers identity (lines 307-322). So nothing would fire if the 0x13 to 0x10/0x11 write perturbed state. The claim says this is "tested directly on the same write the poked leg performs" instead of naming it as a gap.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

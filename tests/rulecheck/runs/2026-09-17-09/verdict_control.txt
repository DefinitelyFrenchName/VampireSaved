Q1: VIOLATED — the claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but build/rulecheck/2026-09-17-09/a/audit_move_parity.sh.txt:105 compares a twelve-field subset (FIELDS="ff841c:l:node,ff8420:b:cnt,...ff8116:b:lvl"), not whole RAM; :34 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters"; :25 "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor"; and no artifact carries a poke of $FF87C2 at all (pokes_for at :86-103 emits only ff8782/ff8116/ff80d4 plus the json pokes, and name_moves.py.txt:655-681 emits only ff8410/ff8810/ff8850/ff8782/ff8b82/ff8509), so the "poked to 0x01" run and the "every consumer enumerated (pristine vsavj: zero)" premise rest on instruments outside the artifacts, while the frozen row is move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f".
Q2: VIOLATED — the conclusion is about how Phobos's moves play (part 1 is movement, normals, air dashes and floats: name_moves.py.txt:456 "Float [j.8 hold]", :451 "Air Dash [j.66] early") under a variant-selection byte, yet the only evidence offered is a number ("reads 6851/6851 frames identical to native"); neither the claim nor any artifact shows an image or recording put before the maintainer before the conclusion.
Q3: VIOLATED — both legs share the rig, the inputs, the pokes and the instrument: audit_move_parity.sh.txt:10-11 "the same rig, the same inputs, on native vsav2 and on the merged WIDE build"; :88 builds "_base" from the same schedule json for both legs; :89-90 pin ff8116 and ff80d4:0000 on both legs; :105 one FIELDS list and :121 one field_trace.lua for both; and name_moves.py.txt:11 "A scripted input is NOT the move it names" — an event that fires on neither leg (or a flavor-dependent draw masked by the shared RNG pin) reads IDENTICAL on both.
Q4: VIOLATED — the gate's two controls cover the node translation (audit_move_parity.sh.txt:5, :187-197) and the speed-level pin (:4, :199-208), which is a different thing than the flavor byte the claim rests on; both run only on the FIRST part of SET (:182 "CTL=$(echo $SET | awk '{print $1}')", default :79 "donovan_1 pyron_2 huitzil_1") and require a frozen IDENTICAL base (:195 "the control part $CTL is not frozen IDENTICAL, so the control cannot discriminate"), which huitzil_1 is not (move_parity.tsv:20); nothing covers the failure mode "the $FF87C2 poke did not land or the rig exercises no flavor-dependent move, so both legs agree regardless".
Q5: N-A — the packet attributes no decision, approval, ruling or closure to the maintainer or any person; the freeze is stated as the working agent's own step ("flavor_default becomes 0x01 and the five tracks are rebuilt and frozen as M19"), and the maintainer mentions in name_moves.py.txt:231 and :363 concern other rulings (14z-121), not this one.
VERDICT: VIOLATED

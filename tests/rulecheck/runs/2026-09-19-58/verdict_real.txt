Q1: VIOLATED — The claim says the non-x2b7ef4 EDITs "are checked byte for byte to be the designed edits on the registered builds", but that check covers only the merged build. build/rc170/freeze/rc_freeze/edits_check_m18_m19.txt:8 says "the trap's two records (Phobos hitbox_proj, merged)". Its lines 10-15 find the thunks only at merged's 0x3ffbb0/0x3ffbf0/0x3ffc30 (attr_m3b_merged27.txt:5-6,344-347). Nothing checks the EDITs of don_m23 (thunks at 0x41a040/0x41a080/0x41a0c0, attr_don_m23.txt:5-6,25-28), hui57, pyron42 or stage4. Nothing checks the stock twin either, and its edits differ from merged's: its attacker thunk tests slot "0c2c000f", not merged's 0x13 (attr_m5_stock18.txt:4), and it has a 3-byte retarget "0x0185ca: 3 bytes at +0x3..+0x5: 0cf350 -> 3fff30" (attr_m5_stock18.txt:17), where merged changes one byte. The claim's NOT-tested list does not name this gap.
Q2: OK — The one feel conclusion the rows carry is Pyron's "2623+PP reads 623+PP, ES with a stock" (registry.diff:22). A capture was put before the maintainer before that was ruled: "Capture put before the maintainer first (`build/rc170/cap_pyr/Pyron_2623PP_after_fix_sheet.png` ...)" (rulings.txt:16). The claim names the other behaviour sentences (column, trap and EX paths equal native; no new zero-pass frame) as untested on these builds.
Q3: OK — The x2b7ef4 bytes are checked against "vs2's own source bytes", an independent reference (x2b7ef4_sites_check.txt:4). The M18-vs-M19 attribution reads two builds from their registered baselines (baselines.txt:2-7), and no artifact shows the tool writing or selecting either one's state. The only shared writer is the same build writing both the self-frozen .sha1 and its verify (e.g. donovan-m23.freeze.log:12 against donovan-m23.verify.log:12). The claim names that as untested ("that a moved self-frozen tenant .sha1 moved for the fixes and nothing else").
Q4: VIOLATED — Two failure modes are neither controlled nor named. (1) A wrong byte in one of the two standalone Donovan x2b7ef4 repairs. There are four x2b7ef4 EDITs: attr_don_m23.txt:23, attr_m5_stock18.txt:12, attr_m3b_merged27.txt:342 and :353. The "four copies" checked are merged Donovan, Pyron and Phobos (x2b7ef4_sites_check.txt:1-3; Phobos's copy is not an EDIT at all) plus ONE standalone 0x0f3f70 copy (x2b7ef4_sites_check_solo.txt:1). So the 160-byte repair in either don_m23 or the stock twin has no check, and the planted-byte control (x2b7ef4_sites_check_plant.txt:1-3) covers only a copy that was checked. (2) edits_check_m18_m19.txt, which carries the "every edit is a designed edit" verdict (line 17), has no planted-wrong-edit control, so nothing shows it would fire on a non-designed byte that its "relocation does not explain" logic absorbed.
Q5: OK — Each attributed ruling quotes the maintainer's own selection: "the maintainer selected *"M19 supersedes it (Recommended)"*" (rulings.txt:10); "the maintainer selected *"Natural reading (Recommended)"*" (:20); "*"S1: scoped row (Recommended)"*" (:38); #111/#96 "*"I agree with all recommendations"*" (:43); and the freeze itself, "Freeze M19 now (Recommended)" (:61).
VERDICT: VIOLATED

Q1: VIOLATED — The claim's list of untested premises leaves some out. Finding (b) names #157's damage half ("the damage scaler read the victim's `+0x3B3`") but gives only `tests/audit_throw_registration.sh` and `tests/audit_move_parity.sh` as its tests. STATE.md:85 says of the parity corpus "invisible in 2P versus (both 0x10, which is why the parity corpus read equal damage)". The only separating experiment was a scratch copy of the gate with `EXTRA_POKES` (`build/agent183/stat8_*`), and STATE.md:95 lists "the probe-era census/delta/A-B/stat scripts" as "not promoted". The same gap applies to (c) "six store words per tenant copy and nothing else": its test `tests/test_m3a_reproducible.sh` checks one whole-image sha1, while the word-level attribution script `delta_attr.py` was "not promoted" (STATE.md:95). It also applies to (p) "moved only for Donovan's 110/111": that was a one-off comparison against M19 at the freeze (STATE.md:93 "compared to M19's, 49 of 51 identical"), and `tests/run_suite.sh:51` checks only the current expectations. None of these three is in the claim's NOT-tested list.
Q2: N-A — The claim says where each finding is documented and which tests are quoted. It draws no new conclusion about how a move plays or feels.
Q3: N-A — No two legs are compared. The re-runs of `close_findings`, `homes_tracked` and `retraction_grep` are single-instrument checks (build/agent183/close_findings_14z183.txt:1-8, build/agent183/homes_tracked.txt:1).
Q4: VIOLATED — The failure mode here is a named test that does not reproduce the finding it is listed for, and no control covers it. `homes_tracked` checks only that the named files are tracked (build/agent183/homes_tracked.txt:1 "every file named as a home or test checked against git ls-files"). `close_findings` "sees addresses, not findings" (build/agent183/close_findings_14z183.txt:8). The packet quotes each test's header line, e.g. build/agent183/packet_homes_14z183.txt:7 "EXPECTS: the 506 rows equal to tests/expected/move_parity_events.tsv". It never shows the scaler half of (b), the "nothing else" of (c) or the M19 comparison of (p) being reproduced. The claim's own disclaimer ("whether each home is ENOUGH for a reader is judged here") covers homes, not whether the tests reproduce their findings.
Q5: N-A — The claim credits no decision, approval or closure to anyone. The one ruling it touches as a home, (m), is quoted in the maintainer's words at build/agent183/packet_homes_14z183.txt:29 "PINNED TO THE M19 BUILD (maintainer-ruled 2026-09-26, 14z-183: "Pin it to M19".
VERDICT: VIOLATED
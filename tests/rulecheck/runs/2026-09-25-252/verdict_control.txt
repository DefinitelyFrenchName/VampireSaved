Q1: VIOLATED — The claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the artifacts show gaps it does not name. audit_move_parity.sh.txt:34-36 says the gate does not cover "per-hit damage, meter gain, hitboxes and projectile parameters", and P2's state "is never compared". audit_move_parity.sh.txt:25-29 and :130 start the comparison window at the rig's first event, not over the whole part. FIELDS at audit_move_parity.sh.txt:105 does not sample ff87c2. No artifact shows the ff87c2 poke, the "every consumer enumerated" search, or the "pristine vsavj: zero" result.
Q2: VIOLATED — The claim concludes that Phobos plays like native from a number alone ("6851/6851 frames identical"). The frozen divergence being fixed is a movement one a player would feel: move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f". No artifact shows a capture being produced or put before the maintainer.
Q3: VIOLATED — Both legs get the same rig pokes: _base is emitted in both the native and ours branches (audit_move_parity.sh.txt:88, :98, :101). Those pokes pin P1's X on every event, and every huitzil part-1 event is "far" (name_moves.py.txt:448): name_moves.py.txt:658 "pin_pokes += [f\"{t - 40}:ff8410:{x1:04x}\"". ff8410 is a compared field ("ff8410:w:x", audit_move_parity.sh.txt:105). The same holds for the shared level pin ff8116 (:89) against the compared "ff8116:b:lvl", and for the tenant-id poke ff8782 (name_moves.py.txt:672) against the compared "ff8782:b:id".
Q4: VIOLATED — The gate's only controls are "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:4-5, :185-208). Both run on the first part in SET, not necessarily huitzil_1 (:182). No control fires if the ff87c2 poke did not take effect or was overwritten: the byte is not in FIELDS (:105). No control fires if a runtime poke of 0x01 differs from a rebuilt flavor_default=0x01 build. No control fires if 0x01 changes state the gate excludes (P2, hits, the window before the first event). The claim names none of these failure modes. The 6851/6851 IDENTICAL figure also cannot come from this gate as written: it contradicts the frozen "huitzil_1	DIVERGES	Jump [8]+21f" (move_parity.tsv:20), so the gate would print "expected [...] measured [...]" and FAIL (audit_move_parity.sh.txt:178).
Q5: N-A — The claim attributes no decision, approval or ruling to the maintainer or any person.
VERDICT: VIOLATED
Q1: VIOLATED — The claim names no untested premise ("Not tested: nothing further — the comparison is frame-exact over the whole part"), but the artifacts show several: audit_move_parity.sh.txt:34-36 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters — separate instruments. P2 is Victor ... his state is never compared"; the comparison is 12 sampled fields (audit_move_parity.sh.txt:105 FIELDS=...), not frame-exact RAM, and its window begins at the rig's first event, not the part (audit_move_parity.sh.txt:25 "THE COMPARISON WINDOW STARTS AT THE RIG'S FIRST EVENT, not at the anchor", verdict_for --first-event at :130-135); no artifact contains RAM:$FF87C2, a poke of it, a "6851/6851" figure, or any consumer enumeration (name_moves.py.txt:670-681 writes only ff8850/ff8782/ff8b82/ff8410/ff8810/ff8509 pokes; pokes_for :86-103 adds only ff8116/ff80d4/ff8782); and move_parity.tsv:21-29 show huitzil_2..huitzil_10 all DIVERGES, so a part-1-only reading leaves every special, throw, meter and guard-cancel part untested by this evidence.
Q2: VIOLATED — The claim concludes Phobos's part-1 moves play identically to native, and part 1 is the movement/normals/float/air-dash rig (name_moves.py.txt:448-459 "Air Dash [j.66] ...", "Float [j.8 hold]"; the frozen divergence is a jump, move_parity.tsv:20 "huitzil_1	DIVERGES	Jump [8]+21f"); the only evidence offered is a frame count ("6851/6851 frames identical") and no artifact records an image or recording put before the maintainer.
Q3: VIOLATED — Both legs run the same generated rig and the same JSON pokes (audit_move_parity.sh.txt:88 `_base="$(python3 -c "...json.load(open('$_j'))['pokes']")"`, applied to native at :98 and to ours at :101), and those shared pokes WRITE compared fields: the position pin `pin_pokes += [f"{t - 40}:ff8410:{x1:04x}", ...]` (name_moves.py.txt:658) rewrites x on both legs before every far event while `ff8410:w:x` is a compared field (audit_move_parity.sh.txt:105); the stock poke `ff8509:09` (name_moves.py.txt:681) writes the compared `ff8509:b:stock`; the level pin `{f}:ff8116:06` (audit_move_parity.sh.txt:89) is written on both legs every frame from 2000 and compared as `ff8116:b:lvl`; the RNG `ff80d4:0000` (:90) is pinned on both from the anchor. A defect in any of these makes the legs agree by construction.
Q4: VIOLATED — The gate's two controls cover the placement translation (audit_move_parity.sh.txt:185-197, no-translation) and the native play-mode pin (:199-208, unpinned-level), and both run only on the first part of SET, `CTL="$(echo $SET | awk '{print $1}')"` (:182), i.e. donovan_1 under the default set (:79), never huitzil_1; the no-translation control is DEAD for any part frozen DIVERGES (:194-196), which huitzil_1 is (move_parity.tsv:20). No control fires on the way this claim could be wrong: that the 6851/6851 agreement came from a runtime poke of RAM:$FF87C2 rather than from the byte that `flavor_default 0x01` will put in the built ROM — nothing withholds that poke on the same run as a negative, nothing asserts the rebuilt ROM reproduces the poked result, and the claim does not name this gap.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to the maintainer or any person; the only maintainer references in the artifacts (audit_move_parity.sh.txt:8 "the maintainer's move lists", name_moves.py.txt:363 "the maintainer's ruling 14z-121") are not the decision under review.
VERDICT: VIOLATED

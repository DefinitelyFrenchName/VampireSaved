Q1: VIOLATED — tests/audit_throw_registration.sh:16 ("which nothing on vsavj reads in play") and docs/game/engine_internals.md:3089-3090 state the dead pair's deadness UNIVERSALLY from three parts, while docs/game/engine_internals.md:3067-3070 says the same family "is consumed by PORTED readers and must stay at vs2 offsets"; the claim's NOT-tested list names the CONTACT scope (rig, air throw, placed sites) but not that the READER set is measured only on the three parts' paths, and no artifact sweeps -0x4B74/-0x4B72 readers on the placed regions (build/meter_probe_14z166/census.txt:67-102 censuses only the vsavj displacements -0x4BC6/-0x4BC4/-0x4B8F).
Q2: VIOLATED — tests/audit_throw_registration.sh:2 draws a player-feelable conclusion ("the attacker gets the victim's flat 8 and the victim the throw record's meter", GitHub #136's meter family); every artifact is a script, log, disassembly or table, none is an image or recording, and none records a capture put before the maintainer before the conclusion.
Q3: OK — both legs share the rig, pokes and tests/lua/read_tap.lua (tests/audit_throw_registration.sh:57-73,128-137); the pokes write ff8850/ff8410/ff8810/ff8116/ff80d4/ff8509 (tests/replays/naming/*.json), none the compared meter words $FF850A/$FF890A or either registration pair; the claim names the shared rig ("the contact frames are the rig's"), and the compared result is a leg DIFFERENCE (p1/p2 reversed), which a shared defect cannot manufacture as agreement.
Q4: OK — legs-swapped (tests/audit_throw_registration.sh:120,191-194; build/gates_14z166/throw_reg_ctl_swapped.log:10-26,66) covers the frozen rows coinciding with the fixed or P1/P2-relabelled shape; dead-reader-planted (script:121,195-198; build/gates_14z166/throw_reg_ctl_planted.log:135-139,153) covers an in-play dead-pair reader going unreported; the VOID rule (script:94) covers a dead tap, and the tap's liveness on the dead range is the frozen boot reads (tests/expected/throw_registration.tsv:27).
Q5: N-A — the claim attributes no decision to anyone; tests/expected/throw_registration.tsv:3 records only "Frozen 14z-166 with FREEZE=1", and docs/game/engine_internals.md:3096-3097 leaves the fix "the maintainer's to schedule" without asserting a ruling.
VERDICT: VIOLATED

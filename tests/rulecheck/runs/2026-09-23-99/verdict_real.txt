Q1: VIOLATED — docs/project/agent_architecture_scope.md:110 marks the deny hook's "the denied command never ran" and "changed course" as MEASURED, but tools/agent/probe_hooks.sh:71 only checks for a PROBE-DENY tool result, and build/agent172/probe_run.txt:2 says only "the detached launch was denied and the reason reached the agent". So whether a denied command actually fails to run is untested, and the claim does not name it, although C0.1 rests on it. Also not shown in any artifact, and not named: scope.md:86 "WAITER LOOPS polling a detached job's PID" (build/agent172/tracked_14z174.txt:4-5 gives only the descriptions "Wait for the static tier to finish" and "Wait for the close tier"); the "3.8 s ... 4.2 s" durations at scope.md:113 (probe_run.txt:5 shows only 6050 ms); the model-reachability probe at scope.md:120, which no artifact records.
Q2: N-A — the claim concludes nothing a player could feel. It is about agent procedure: "No behaviour a player could feel is concluded".
Q3: OK — The census count and the itemised list use the same regex ("every DETACHED launch (transcript_gaps.DETACHED)", build/agent172/detached_14z174.txt:1). That shared regex can only hide a missed form, and the claim names that gap ("a detaching form it does not list would be missed"). The 12 matches were checked by their descriptions (detached_14z174.txt:4-17), and the table matches census.txt:13 (17/11/1/5, 1623, 12, 5).
Q4: VIOLATED — P1 has no control that would fire if the denial is reported but the command still runs. It could create a marker file, or check that "launched" is absent from the tool results, and it does neither (tools/agent/probe_hooks.sh:67-72). P4 (probe_hooks.sh:96-98) only shows the counters are not blind: it proves a different thing from execution being prevented. The claim does not name this failure mode.
Q5: OK — the maintainer's ask and proposed shape are quoted in build/agent172/issue_172.txt:5 and :11-14, and the scope document quotes them verbatim (scope.md:14-31). No ruling is attributed beyond that. The "standing ruling 'a change of model is a recalibration'" at scope.md:192 names no person; the spec behind it is docs/project/rule_checker.md:139-140.
VERDICT: VIOLATED

Q1: VIOLATED — the claim's stated basis "the stock pokes at FIRST_EVENT-60 ... address the P2 block or the globals and never the P1 block" is not what the artifacts show: name_moves.py.txt:710 pokes `ff8509` (P1 block +0x109; absent from these part-1 rows only because METER_PARTS at :646 excludes "1"), name_moves.py.txt:687 pokes `ff8410` (P1 block +0x010) 40 frames before every "far" event of huitzil_1/pyron_1 (:425, :477), and audit_forced_pick_fidelity.sh.txt:132 copies every rig poke but `ff8782` into both legs; the level pin `ff8116` and RNG pin `ff80d4` attributed "per name_moves.py" are the gate's own (audit_forced_pick_fidelity.sh.txt:133-134; neither address appears in name_moves.py.txt). Also "P2's block is asserted empty pre-match on every leg" is asserted only on the POKED-vs-REAL comparison (audit_forced_pick_fidelity.sh.txt:279); the REAL-vs-SELF comparison drops its P2 field (:277 cuts f1 and f6 only), and "exactly three LATCHED" is a statement over six pre-match samples (:99, :180, :193) with the TRANSIENT count unfrozen (:284) — neither stated.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."; the frozen play column (forced_pick_fidelity.tsv:15 `2980`) is not read as a behaviour.
Q3: OK — the legs share the prologue, MAME/replay.lua and the copied pokes (audit_forced_pick_fidelity.sh.txt:117-139), but no shared poke lands in the P1 block inside the pre-match window 1501-2362 (ff8410 pins from 2560 at name_moves.py.txt:687, ff8850 from 2550 at :699, ff8116 from 2000 and ff80d4 from 2363 are globals at :133-134), so a shared poke cannot make the LATCHED sets agree; identity is read from each leg's own dump (:170-179), and a blind diff would fail the frozen non-empty compare (:298-305).
Q4: OK — same-leg (audit_forced_pick_fidelity.sh.txt:310-316) fires if poked-vs-poked is empty where the frozen set is not, covering a diff that latches spuriously; wrong-cursor (:318-322) plus the per-leg ID/CELL assertions (:174, :179, :278) cover the real leg confirming another character; donovan-self (forced_pick_fidelity.tsv:17, :47-51) covers the poke mechanism itself latching; uncovered and named by the claim itself: a changed-value poke's own side effects being charged to the confirm, since SELF and donovan-self are both same-value writes (:26-28, :135).
Q5: N-A — the packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED

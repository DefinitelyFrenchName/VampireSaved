THE PACKET

Decision kind: expectation
Subject: M20 freeze: re-freeze the ten emulator expectations the battery's pass 1 found red, pin the #112 gate to M19, and add the LANDING-TURN-OPEN class
Claim (the working agent's sentence): Of the eleven pass-1 reds on merged-m20 (battery_reds.tsv), each re-freeze moves only what a named cause explains: the parity table's 34 meter-first DIFF rows become IDENT and 2 (huitzil_3 events 8/9) become DIFF on x, no other verdict moving (move_parity_events.tsv.diff), the attribution loses its METER-SWAP roots and names those two rows LANDING-TURN-OPEN, an open class for #179 ruled 'Freeze, ticket it' (decisions_lines_30_58.txt), whose signature matches both real rows and none of six plants (class_groundtruth.txt, ev89.txt); the throw-registration rows show every ours contact paying like native with the tenant stores now live-pair writers, under a contact rule widened to count live-pair writes (gate_edits.diff), plus pyron_3's two air-throw contacts which are stale since 14z-181 because they appear on native and on merged-m19 too (m19_vs_m20_reds.txt); column_flash and phobos_dmg_residual move only the build name and row; df_field_readers_live and reaction_class_live move only as the 14z-181 corpus growth (30 -> 32 naming parts) explains, their diff identical on merged-m19; defense_row_reads moves by that growth plus the attacker-combo read indexing the tenant's own row instead of the victim's (the #157 scaler fix) and suite read counts; test_mister_prg_window's pos pair moves two counters with every structural field unchanged; test_pod_black_foot_palette is not re-frozen but pinned to merged-m19 by ruling. NOT tested: the prg_window counter move is recorded as measured, not explained; #179's mechanism is unknown and the class is ground-truthed on huitzil_3 alone; reaction_class_live's value-set changes and defense_row_reads' suite-row counts are attributed by equality with the merged-m19 diff and by the earlier whole-RAM A/B (only replays 110/111 differ), not traced value by value; the third store pair was never observed writing.
Artifacts (read every one, in full):
  - build/rc183/refreeze2/battery_reds.tsv
  - build/rc183/refreeze2/m19_vs_m20_reds.txt
  - build/rc183/refreeze2/summary.txt
  - build/rc183/refreeze2/move_parity_events.tsv.diff
  - build/rc183/refreeze2/move_parity_attribution.tsv.diff
  - build/rc183/refreeze2/throw_registration.tsv.diff
  - build/rc183/refreeze2/column_flash.tsv.diff
  - build/rc183/refreeze2/phobos_dmg_residual.tsv.diff
  - build/rc183/refreeze2/df_field_readers_live.tsv.diff
  - build/rc183/refreeze2/reaction_class_live.tsv.diff
  - build/rc183/refreeze2/defense_row_reads.tsv.diff
  - build/rc183/refreeze2/mister_prg_window.txt.diff
  - build/rc183/refreeze2/gate_edits.diff
  - build/rc183/refreeze2/ev89.txt
  - build/rc183/refreeze2/class_groundtruth.txt
  - build/rc183/refreeze2/decisions_lines_30_58.txt

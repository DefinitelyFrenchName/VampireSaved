Q1: VIOLATED — The claim names five untested items, but it presents one premise as tested that was not. It says the three throw-registration controls "FIRED under that rule in pass 1". In pass 1, though, the gate compared against the stale M19 defect rows, and the unplanted run failed against those too (build/rc183/refreeze2/battery_reds.tsv:3 "audit_throw_registration ... FAIL"; build/rc183/refreeze2/throwreg_controls_p1.txt:8-16 show the old "< pyron_3 ours contact 2824 p1=+8 p2=+6" rows as the diff). So nobody tested whether those controls discriminate under the widened rule, and the claim does not say so. A second unnamed premise: the whole-RAM A/B ran on the probe, matched to merged-m20 on "PROGRAM ... 0 differing program words" only (build/rc183/refreeze2/whole_ram_ab_probe_vs_m19.txt:1). The M20 delta also includes "the mark's glyphs (M19 -> M20)", and the project's own candidate for the counter move is "gfx-content timing" (build/rc183/refreeze2/mister_prg_window.txt.diff:9-11). Also unexplained: on merged-m19, huitzil_3 and donovan_5 exited rc=1 and legacy_demitri rc=2 while each printed "ok ... as frozen" (build/rc183/refreeze2/m19_vs_m20_reds.txt:9-11 vs 42-47).
Q2: OK — The only point about how something feels to a player is #179, and the claim names it untested ("#179's mechanism and how it plays (a capture is its next step...)"). The ruling says the same: "the next step is a native-vs-ours capture for the maintainer before any mechanism work" (build/rc183/refreeze2/decisions_lines_30_58.txt:7). For #157, the maintainer played the probe recordings before ruling: "Everything felt normal. However I didn't measure anything" (build/rc183/refreeze2/maintainer_playtest.txt:2).
Q3: OK — Every comparison shares the same rig, tap and pins, but the artifacts show that shared setup telling states apart rather than forcing agreement. On the same rig, ours paid "p1=+8 p2=+6" on M19 and "p1=+6 p2=+8" on M20 (build/rc183/refreeze2/throw_registration.tsv.diff:30-49). The M19 re-run still shows the swap, "pyron_3 ours contact 5182 p1=+8 p2=+12" (build/rc183/refreeze2/m19_vs_m20_reds.txt:52). The BUILD setting did reach the compared state: build/rc183/refreeze2/defense_row_reads.m19_vs_m20.diff:15-21 differs between the two builds. No artifact shows a shared poke or rig writing the meter or x being compared.
Q4: VIOLATED — Failure mode: under the widened contact rule, the gate might accept a swapped or double-counted pair. Nothing controls this, and the claim does not name it as a gap. The cited controls only show "differs from the frozen rows" (build/rc183/refreeze2/throwreg_controls_p1.txt:2,4). In pass 1 that check was met regardless of the plant, because the frozen rows were the old defect rows and the real M20 run also differed. A legs-swapped M20 copy (+8/+6) in fact matches the stale rows at eight of its ten contacts, and it differs only through the new 5182/5602 contacts and the writer rows (build/rc183/refreeze2/throw_registration.tsv.diff:25-51). So these controls show the gate sees stale rows, not that it would catch a swap against the re-frozen rows. Pass 2 as described only has to "reproduce" the freeze; the claim does not say it re-runs the controls. The dead-reader plant ("adds a reader row", line 3) is the only control that tells a plant apart from the stale-row failure.
Q5: OK — Each decision the claim attributes to the maintainer is quoted in the maintainer's own words: "The maintainer: *"Freeze, ticket it (Recommended)"*" (build/rc183/refreeze2/decisions_lines_30_58.txt:5), "The maintainer: *"Pin it to M19 (Recommended)"*" (build/rc183/refreeze2/decisions_lines_30_58.txt:15), and the playtest "Everything felt normal. However I didn't measure anything" (build/rc183/refreeze2/maintainer_playtest.txt:2).
VERDICT: VIOLATED
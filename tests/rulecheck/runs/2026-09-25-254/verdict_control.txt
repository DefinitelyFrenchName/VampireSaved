Q1: VIOLATED — The claim says the SELF leg adds "a poke of a DIFFERENT id (a changed-value write)", so the inert-poke check is "tested directly on the same write the poked leg performs". The gate does something else. audit_forced_pick_fidelity.sh.txt:135 builds the poke from the row's own tenant id (`_pick="...f'{f}:ff8782:$_id'..."` with `_id="$(row_id "$_row")"`), and line 138 applies it to the self leg. Lines 26-27 of its header say "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". So SELF writes a value that is already there, while POKED changes 13 to 10 or 11. That a changed-value write does not itself perturb the P1 block is an untested premise, and the claim states it as tested.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here", and it draws none.
Q3: OK — Both legs share the same pokes (audit_forced_pick_fidelity.sh.txt:131-139: the rig's own pokes, the level pin at $FF8116 from 2000 and the RNG pin at $FF80D4 from 2363). The level and RNG pins write globals. The only shared write inside the P1 block is the X pin at $FF8410, which starts at frame 2560 (name_moves.py.txt:687 plus FIRST_EVENT = 2600 at :77), after the last pre-match sample at 2362 (audit_forced_pick_fidelity.sh.txt:180). No artifact shows a shared write reaching the P1 block in the pre-match window that decides LATCHED. The claim names the X pin's effect on the first-play frame.
Q4: VIOLATED — Neither control catches the case where the POKED-vs-REAL latched bytes come from the changed-id write itself rather than the confirm. SELF (audit_forced_pick_fidelity.sh.txt:135/138) and the donovan-self row (poke 13 over 13, row_id at :87) both write a value already present. The must-fire controls (same-leg, wrong-cursor, lines 4-5) cover only a vanished difference and a wrong identity. The claim does not name this gap; it says the opposite ("the poke mechanism is inert is tested directly on the same write").
Q5: N-A — No decision, approval or ruling is attributed to the maintainer or anyone else.
VERDICT: VIOLATED
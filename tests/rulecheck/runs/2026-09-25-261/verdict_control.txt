Q1: VIOLATED — The claim says "Not tested: nothing further", but the artifacts show several untested premises. audit_move_parity.sh.txt:34-36 says the gate does not cover "per-hit damage, meter gain, hitboxes and projectile parameters", and P2's state is never compared. Part 1 is only movement, normals, command normals, air dash and float (name_moves.py.txt:448-460), so Huitzil parts 2-10 are unexamined. The flavor byte ff87c2 is not among the sampled FIELDS (audit_move_parity.sh.txt:105), so the gate does not show the "0x01 native / 0x00 ours" reading. No named file shows a poke of ff87c2 (pokes_for, audit_move_parity.sh.txt:86-103). None of the three files shows the consumer enumeration ("pristine vsavj: zero"). The "6851/6851 frames" figure is also not something this gate outputs: it reports IDENTICAL/DIVERGES plus an event (lines 172-178), and move_parity.tsv:20 has huitzil_1 frozen as "DIVERGES	Jump [8]+21f".
Q2: VIOLATED — The claim concludes that Phobos's part-1 play (jump, air dash, float; the frozen divergence is "Jump [8]+21f", move_parity.tsv:20) is identical to native. The only instrument in the artifacts is a numeric per-frame field trace (audit_move_parity.sh.txt:118-121, tests/lua/field_trace.lua), and no artifact shows a capture produced or shown to the maintainer before the conclusion.
Q3: VIOLATED — Both legs get the same pokes, and those pokes write fields the gate compares. The native leg gets "$_base" "$_lvl" "$_rng" (audit_move_parity.sh.txt:98) and ours gets the same plus the pick (line 101). $_base includes the per-event position pins "{t - 40}:ff8410:{x1:04x}" (name_moves.py.txt:658; every huitzil part-1 event is "far", line 448). The compared FIELDS include "ff8410:w:x" and "ff8116:b:lvl" (audit_move_parity.sh.txt:105), and ff8116 is poked to 06 on both legs every frame (line 89). So a position divergence is overwritten on both legs 40 frames before each event, and the level field is forced equal. A defect there would make the legs agree.
Q4: VIOLATED — The gate's only controls are unpinned-level and no-translation (audit_move_parity.sh.txt:4-5). Neither fires if the ff87c2 poke did not land or did not drive the compared state (ff87c2 is neither poked nor sampled, lines 86-105). Neither fires if a runtime poke of the byte differs from building with flavor_default=0x01 (for example, the latch being read before the poke frame). The claim acts on the build but tested a poke, and names neither gap. Also, with huitzil_1 as the control part, its frozen verdict is DIVERGES (move_parity.tsv:20), so the no-translation control reports "CONTROL DEAD ... cannot discriminate" (audit_move_parity.sh.txt:195). An IDENTICAL result would also fail against that frozen row (line 178).
Q5: N-A — The claim attributes no decision, approval or ruling to the maintainer or any other person. "his part-1 rig" refers to Phobos, the character.
VERDICT: VIOLATED

Q1: VIOLATED — The claim says the class list was "checked by a script for no missing letter", but none of the named artifacts holds that script or its output. build/agent183/packet_verify.py:6-14 parses only the "(x) ...: file:line [tracked] quote" lines and checks the quotes against the tree; build/agent183/packet_homes_14z183.txt:1 is the classification itself, with no check result. So that premise is presented as tested but is actually unshown. On my own reading of packet_homes_14z183.txt:1 every letter (a)-(z) is present and only b, c and e are split, so the result holds; what is missing is the check. The other untested premises (class correctness, table completeness, pattern-list completeness, classing-line class, home sufficiency) are named in the claim. Also noted: a named test's own reach line reads "# EXPECTS: the 506 rows equal to tests/expected/move_parity_events.tsv" (packet_homes_14z183.txt:7, tests/audit_move_parity.sh:14), while STATE.md:84 reads "the frozen 526-row table". packet_verify.py checks only that the quoted text starts the line, so it cannot see this.
Q2: N-A — The claim concludes about documentation homes and test reach. It draws no conclusion about how a move plays or feels.
Q3: N-A — No two legs are compared. packet_verify.py and classing_cover.py are single-instrument checks, each with a plant on a copy (build/agent183/packet_quotes_verified.txt:57-60, build/agent183/classing_coverage.txt:24-26).
Q4: OK — packet_verify's plant fires on an altered quote and on a moved line number: "(m) tests/test_pod_black_foot_palette.sh:48 MISMATCH", "(u) tests/test_rehit_ring.sh:80 MISMATCH" (build/agent183/packet_quotes_verified.txt:57-60). classing_cover's plant fires on an unnamed hit file: "MISSING 'Lilith j.LP/j.LK': tests/rulecheck/ledger.tsv" (build/agent183/classing_coverage.txt:24-26). The retraction grep finds all four reach controls: "REACH: every reach control found (4)" (build/agent183/retraction_greps_14z183.txt:63). The remaining failure modes are all in the claim's "UNCHECKED BY ANY TOOL" list: wrong class, missing finding, gaps in the pattern list, wrong classing class, weak homes. My judgement on the named class risk: (r) looks wrongly placed under "fails on a regression". The table itself says "the misnaming was a document error no test can replay" (STATE.md:94), and tests/test_meter_gain.sh would not fail if a record said Lilith again. Because the claim names this risk, it is the agent's to accept in writing, not a violation here.
Q5: OK — Each ruling the table attributes is quoted in the maintainer's own words: STATE.md:93 *"Pin it to M19 (Recommended)"* and *"Freeze, ticket it (Recommended)"*; STATE.md:92 *"Freeze M20 now"*; build/agent183/packet_homes_14z183.txt:29 "maintainer-ruled 2026-09-26, 14z-183: "Pin it to M19".
VERDICT: VIOLATED

Q1: VIOLATED — The claim says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The gate does not do that. audit_forced_pick_fidelity.sh.txt:26-27 reads "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". At audit_forced_pick_fidelity.sh.txt:135, the SELF leg pokes `$_id` = row_id, which is the tenant's own id, set at line 87. So SELF is a no-op write. The poked leg's write changes the value (Donovan's 0x13 to 0x10/0x11). The donovan-self row is also same-id (line 87 "donovan-self) echo 13"). So whether a changed-value poke is inert is an untested premise, and the claim does not name it: it presents it as tested.
Q2: N-A — The claim says "No behavioural conclusion about how the character plays is drawn here". The frozen values are only RAM offsets and a frame number (forced_pick_fidelity.tsv:15-17).
Q3: OK — Both legs share the rig's pokes, the level pin and the RNG pin (audit_forced_pick_fidelity.sh.txt:137-138; name_moves.py.txt:687 and :699). None of them reaches the P1 block inside the 1501-2362 LATCHED window. The pins are "{t - 40}:ff8410" with t >= 2600, the HP pin is at 2550, $FF8116 and $FF80D4 are globals, and the claim itself names the P1 X pin as shared for the first-play frame.
Q4: VIOLATED — Failure mode: the changed-value id write at 1400/1450/1500 disturbs P1-block state by itself, so the three LATCHED bytes would come from the poke and not the confirm. The claim charges them to the confirm ("a latched byte is charged to the confirm only inside the P1 block"). The only poke-mechanism controls are REAL-vs-SELF and donovan-self (audit_forced_pick_fidelity.sh.txt:272, :281-282, :48-51), and both poke the id the leg already holds, so they test a different write than the one the claim rests on. The same-leg and wrong-cursor controls (lines 308-322) cover only a vanished difference and wrong identity. The claim does not name this gap: it claims it is covered.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person.
VERDICT: VIOLATED
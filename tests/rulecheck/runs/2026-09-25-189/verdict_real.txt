Q1: VIOLATED — The claim says "every finding of the sitting has a live home in the TRACKED tree". It names only the memory homes as outside the tree: "a home in a memory note ((a), (n)) is outside the tree by design". But the only home it gives (w) is "home **#174**", a GitHub ticket. The tracked mirror of that ticket does not carry the finding. docs/project/tickets.tsv.lines-74-90:2 (tickets.tsv line 75) reads "35 never-entered a2 chains of the tenants (7 with attack records): moves the naming corpus never produces, their ours-vs-native parity unmeasured". It does not mention Donovan's EX or a2:0x51/0x52, and its repro is the build-only "build/move_parity_census_14z164/chain_census.txt". The finding is in the tracked tree only in STATE row (8), which the table does not name, and that row cites "build/agent181/never_probe.log". The homes tool does not read "**#174**" at all: it is absent from build/agent181/homes_tracked_14z181.txt, including its OTHER list at line 29. The "NOT tested" list has a second slip. It says "(f) and (n) cite past rule-checker resolves as their tests", but (ff)'s test is also "run 165/166/169's resolves" (STATE.md.lines-72-100:24). It also calls (i) and (q) "RAM findings with no capture", yet docs/NEXT_SESSION.md:27-29 lists sent captures of "Pyron's heavy-hit reaction (#173)" and "Donovan's Ifrit Sword guard cancel".
Q2: VIOLATED — The claim restates conclusions about hits: "(ee) ... pyron_3's event 5, named "far (whiff)" by the rig, in fact connects" and "(dd) ... test `test_move_naming` pyron (both lines a2:0x23)" (both Galactic Throw strengths connect). The five captures put before the maintainer, listed at docs/NEXT_SESSION.md:27-32, include neither event 5 nor the Galactic Throw pair. The claim's "(dd) and (ee) are RAM findings with no capture" names the gap but does not produce a capture.
Q3: N-A — The packet compares no two legs of its own. Its conclusion is documentary: every home is tracked and every test exists. The ours-vs-native comparisons it restates belong to their gates (build/agent181/victim_parity_mode5_unpinned-level.log:36, build/agent181/trap_air_mode4_air-hit.log:6-10). The real-tool vs `--blind` pair in tests/test_close_tools.sh:58-59 is designed to disagree, and it reads DEAD if both pass.
Q4: VIOLATED — Two failure modes are neither controlled nor named. (1) A home cited as a ticket number (#173, #174, #175) is not read by tools/homes_tracked.py: line 50 scans only backticked tokens, and bold "**#174**" is not one. No self-test plant covers this (tools/homes_tracked.py:93). The claim names memory homes as its only outside-tree case, but #174's tracked row does not carry (w) and cites a build/ repro (docs/project/tickets.tsv.lines-74-90:2). (2) The retraction grep excludes "docs/GOTCHAS.md (generated from docs/project/gotchas.md)" (tools/retraction_grep.py:26,34), yet the claim describes its scope as "over docs, HANDOFF.md, ..." with no exclusion. A generated page left stale after the gotchas.md edits (build/agent181/gotchas_close_14z181.diff:38-41 removes "from a pin the screen edge clamped to 664" and a build/agent181 path) would keep a retracted wording unseen. That is the (hh) failure, "a generated page not regenerated", and no reach control targets that page (tests/rulecheck/retractions/14z-181.tsv:29-31).
Q5: N-A — The claim attributes no decision, approval or closure to anyone. It says only "the maintainer's read is theirs", and (t)'s recommendation sits under STATE "Decisions pending" (docs/NEXT_SESSION.md:22-27).
VERDICT: VIOLATED

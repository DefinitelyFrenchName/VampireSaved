Q1: VIOLATED — the claim lists as TESTED a premise the gate does not test. It says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write), so 'the poke mechanism is inert' is tested directly on the same write the poked leg performs". The gate says the opposite: audit_forced_pick_fidelity.sh.txt:26-28 "SELF — the REAL path PLUS the same-id poke: isolates the poke MECHANISM (a write at 1400-1500 to an id that already holds that value)". Lines 135/138 give SELF the row's own `$_id` (`f'{f}:ff8782:$_id'`, the `*)` case), and the REAL leg already holds that id (line 179 checks the cell). So whether a changed-value write is inert is an untested premise, and the claim does not name it. A smaller point: "position pins ... before each event" does not hold for donovan_1. Donovan's part-1 events have no 4th field, so no pins are emitted (name_moves.py.txt:681-687).
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — both legs share the rig's pokes (audit_forced_pick_fidelity.sh.txt:132-134, 137-138). The only shared write into a compared block is the X pin: `f"{t - 40}:ff8410:{x1:04x}"` (name_moves.py.txt:687). It first lands at 2560, which is after the pre-match window 1501-2362 (sh.txt:180). So it cannot make the LATCHED set agree, and the claim names its effect on the first-PLAY frame. The level pin (ff8116) and the RNG pin (ff80d4) are globals (sh.txt:133-134).
Q4: VIOLATED — no control covers this failure: the changed-value id write at 1400/1450/1500 could itself perturb P1-block state, and that would then be charged to the confirm as LATCHED. The claim does not name this gap. It says the gap is covered, but the SELF control (sh.txt:26-28, 281-282) and the donovan-self row (sh.txt:48-51, "poking 0x13 over Donovan's own confirm") both use only a same-value write, where nothing changes. That shows the instrument sees a different write than the POKED leg performs.
Q5: N-A — the packet attributes no decision, approval or ruling to the maintainer or any person.
VERDICT: VIOLATED

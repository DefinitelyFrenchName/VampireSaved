Q1: VIOLATED — The claim names five untested premises, but it rests on others it does not state. (1) It cites "close_findings.py checked 0 addresses", yet no artifact shows that run. The 14z-180 group in STATE.md ends at row (17), STATE.md:94, and has no CLOSE row like 14z-179's at STATE.md:106, so the figure is unsourced. (2) It assumes each named test actually reproduces its finding, and says only that the table's completeness rests on re-reading. For finding (m), STATE.md:94 says "(m) rig pokes counted one byte wide (72 for 74) … test `test_poke_readback` `unclassified-finding`". That control plants a LITERAL one-byte poke and a one-byte sample: tests/test_poke_readback.sh:60 "POKES=\"1400:ff8782:10;1450:ff8782:10\"\nFIELDS=\"ff8782:b:id,…\"". It never exercises a rig poke's width, so it would still fire if the width fix were reverted. (3) Several homes exist only in files outside the packet: the gen_gate_coverage.py docstring and safe() for (d)/(e), the test_freeze_basis_sandbox header for (f), the measures.sh comment for (i), rig_pokes for (m) and run_all_emulator.sh for (p). Their presence is asserted, not shown.
Q2: N-A — No conclusion about how anything plays is drawn. STATE.md:219 "Nothing a player feels changes by the ruling — what changes is what a gate CLAIMS".
Q3: N-A — Two legs are not compared. The claim is a re-reading of the table against the records, not a comparison of two methods over one compared state.
Q4: VIOLATED — Two ways this claim could be wrong have no control, and the claim names neither. (a) A named test that does not reproduce its finding: (m)'s `unclassified-finding` (tests/test_poke_readback.sh:56-62) is not a width test, and nothing checks that the table's tests match its findings. (b) The rewritten documents carry figures that disagree with one another. The claim says "385 descriptions" (so does STATE.md:72, "all 385 gates described"), but docs/NEXT_SESSION.md:33 says "380 of 380 gates described, census frozen 380/0", docs/NEXT_SESSION.md:41 says "384/384", and docs/project/gate_qualification_scope.md:161 says "all 380 described". No control reads a rewritten page's figures against their source.
Q5: OK — Each ruling the packet attributes is quoted in the maintainer's own words: STATE.md:81 (e.g. "Header line", "NOTE at session, FAIL at freeze/release", "Land it now"), docs/project/gate_header_contract.md:22-28, and gate_qualification_scope.md:23-25 ("Option 1 and ticket to qualify all gates in the future …") for the killshread READS-BACK row. The pending rulings are attributed to no one.
VERDICT: VIOLATED

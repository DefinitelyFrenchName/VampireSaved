Q1: VIOLATED — The claim says the SELF leg is "a poke of a DIFFERENT id (a changed-value write)" that tests "the same write the poked leg performs". The gate says the opposite. audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke: ... (a write at 1400-1500 to an id that already holds that value)". Line 135 pokes `ff8782:$_id`, where `_id` is row_id(row) (line 87), the id the real path's cell is asserted to hold (lines 174, 179). So the claim does not state one premise it rests on: that a changed-value write to $FF8782 is inert. Nothing tests that. Also, the rig pokes are copied from tests/replays/naming/<rig>.json (line 115, line 132), not from name_moves.py.txt, and that file is not among the artifacts. The route-residue result ("leave route residue") is not shown either: lines 90-94 and 286-288 only describe the MEASURE-only step that would report it.
Q2: N-A — "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — The pins both legs share land after the pre-match window where LATCHED is measured. The P1 and P2 X pins fire at t-40, so 2560 at the earliest (name_moves.py.txt:687). The P2 HP pin starts at 2550 (name_moves.py.txt:699). The level pin ($FF8116) and RNG pin ($FF80D4) are globals (audit_forced_pick_fidelity.sh.txt:133-134). No artifact line shows any of them writing the P1 block before 2362. The P1 X pin does reach the play-window field behind the first-play frame, but the claim names that ("a first difference outside the pinned field").
Q4: VIOLATED — Not controlled and not named: a changed-value write to $FF8782 perturbing P1 state, which would put poke effects in with the "latched by the confirm" set. Both inertness controls write a value the id already holds. SELF does (audit_forced_pick_fidelity.sh.txt:26-28, 135), and so does donovan-self: R,R is 0x13 and the poke is 0x13 (lines 48-51, 87-89). The other controls cover other failures. same-leg covers a vanished difference (lines 308-316), wrong-cursor covers identity (lines 317-322), and the P2-empty check covers the instrument's negative control (lines 279-280). The claim does not name this gap: it says the same-write case is "tested directly".
Q5: N-A — No decision, approval or closure is attributed to the maintainer or any other person.
VERDICT: VIOLATED

Q1: OK — build/agent183b/plan_153.md:77-91 lists every premise the recommendation rests on that no run exercised: the ROMDIR checksum step, immunity itself, a plain (non-shared) clone, the real runner in the tree at freeze cadence on 12571807, inputs read only by controls, the 5 release gates, a commit other than HEAD, the emulator tier, completeness beyond build/ and the siblings, a full-content hash's cost, and the m5_stock19/donovan6 registry row. The measured parts match the artifacts: probe153_static.log:283 "PASS 128   SKIP 30    FAIL 20"; runner_clone_supplied.log:209 "PASS 177   SKIP 0     FAIL 1"; runner_clone_supplied.log:125 "test_fbneo_tree_integrity          PASS"; snapshot_timing.txt:2 "real 8.91"; snapshot_timing_submodules.txt:2-7 (0.00-0.77 s). I found no further untested premise that the recommendation depends on.
Q2: N-A — the claim is about test-harness integrity, run counts and snapshot cost; it draws no conclusion about how a move plays, its timing or a hit.
Q3: OK — the clone leg reads its own copy of the run of record, not the tree's: staleness_clone_freeze.log:2 "run /private/tmp/.../scratchpad/clone153/build/emu_freeze_m20_p3 on d4cd4d519109" against staleness_tree_freeze.out:2 "run /Users/koneko/Developer/Vampire_Saved/VampireSaved/build/emu_freeze_m20_p3". The one shared tool that made two legs agree is run50.sh, which omits VS_CADENCE (run50.sh:6 exports only ROMDIR), so controls A and B both read test_emulator_staleness as PASS. The plan names this at plan_153.md:34-36, and the claim does not rest on A/B for that gate.
Q4: OK — test_fbneo_tree_integrity_control PASSes in the clone (runner_clone_supplied.log:126). The clone's gate log shows the clone reading its own copied record (staleness_clone_freeze.log:2). The runner's porcelain check reports "ok: no tracked file changed during the run" (runner_clone_supplied.log:191). The main remaining ways this could be wrong — the clone reading the working tree through a path outside the copies, or borrowed --shared objects — are named by the claim: "IMMUNITY ITSELF: no run changed the working tree while a clone run was in progress; that is S6's job once built" (plan_153.md:79), and plan_153.md:80 and 86-87.
Q5: OK — the plan quotes the maintainer's own words for the principle and the session-cadence scope: plan_153.md:4-6 "being immune from any change in the tree while tests, especially long tests, run" and plan_153.md:8-9 "the gain for session is marginal but the gain for freeze or release is massive". The claim attributes no approval; the design is offered "a recommendation for the maintainer" (plan_153.md:49).
VERDICT: OK

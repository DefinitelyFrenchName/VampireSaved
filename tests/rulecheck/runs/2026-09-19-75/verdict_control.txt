Q1: VIOLATED — the claim says "Not tested: nothing further", but the artifacts show several untested premises. (a) The gate compares only the 12 tenant fields in audit_move_parity.sh.txt:105. It does not compare $FF87C2 or whole RAM, and :34 says "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters". (b) :25 says the window "STARTS AT THE RIG'S FIRST EVENT, not at the anchor", so the comparison is not "frame-exact over the whole part". (c) Only part 1 was run: movement, normals, air dash and float (name_moves.py.txt:448-460). Parts 2-10 are frozen DIVERGES at move_parity.tsv:21-29 and are left unmentioned. (d) No $FF87C2 poke exists in pokes_for (audit_move_parity.sh.txt:86-103), so the premise that a runtime poke equals flavor_default=0x01 in the build is untested and the run is not this gate. (e) No artifact shows the consumer enumeration ("pristine vsavj: zero"). (f) move_parity.tsv:20 freezes "huitzil_1	DIVERGES	Jump [8]+21f", and the gate reports a verdict plus an event, not a frame count like 6851/6851.
Q2: VIOLATED — the claim concludes that Phobos's part-1 moves now play like native: Jump, Air Dash and Float (name_moves.py.txt:392, :451-459), with the old divergence frozen at "Jump [8]+21f" (move_parity.tsv:20). That conclusion rests on a number alone ("6851/6851 frames identical"). No artifact shows an image or recording, or that one was shown to the maintainer before the conclusion was drawn.
Q3: VIOLATED — the legs share pokes that write compared fields. ff8116 is pinned every frame on both legs (audit_move_parity.sh.txt:89, 98, 101) and compared as "ff8116:b:lvl" (:105). ff8410 is pinned 40 f before every "far" event on both legs (name_moves.py.txt:658, and every huitzil_1 event is "far" at :448) and compared as "ff8410:w:x". Both legs force Phobos with the same id poke, "pokes = [f\"{f}:ff8782:{tid}\" for f in (1400, 1450, 1500)] + pokes" (name_moves.py.txt:672; gate :92), and that field is compared as "ff8782:b:id". The poke lands after the prologue picks the default cursor with LP ("1300-1302 p1=1", name_moves.py.txt:54; :58-60 "the other two are FORCED by the early-window poke"). So the native 0x01 reading that the fix copies was taken on a leg where Phobos was never selected through the native select path.
Q4: VIOLATED — the gate's only controls are "unpinned-level" and "no-translation" (audit_move_parity.sh.txt:4-5), and both run on the first part of SET (:182). huitzil_1 is frozen DIVERGES (move_parity.tsv:20), so on it no-translation reports "CONTROL DEAD ... not frozen IDENTICAL" (:195), and unpinned-level can only be declared dead when the frozen verdict is IDENTICAL (:203). Neither control distinguishes anything on that part. No control toggles $FF87C2 on ours. None covers two failure modes the claim does not name: a runtime poke of $FF87C2 behaving differently from flavor_default set at build time, and effects of the flavor outside the 12 FIELDS (:105, :34) or outside part 1.
Q5: N-A — the packet attributes no decision, approval or ruling to the maintainer or anyone else. "flavor_default becomes 0x01 and the five tracks are rebuilt and frozen as M19" is the working agent's own action.
VERDICT: VIOLATED

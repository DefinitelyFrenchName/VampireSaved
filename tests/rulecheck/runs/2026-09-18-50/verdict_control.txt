Q1: VIOLATED — The claim says the SELF leg is "a poke of a DIFFERENT id (a changed-value write)" and that it tests inertness "on the same write the poked leg performs". The gate contradicts this. build/rulecheck/2026-09-18-50/a/audit_forced_pick_fidelity.sh.txt:26-28 reads "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". Line 135 builds the pick poke from the row's own tenant id (`_id="$(row_id "$_row")"`), and line 138 applies that same poke to the poked and self legs alike. So SELF writes a value $FF8782 already holds. The POKED leg changes Donovan's 0x13 (the R,R prologue at :117) to 0x10 or 0x11. Nothing tests whether a changed-value id poke writes or masks bytes in the P1 block, and the claim presents that premise as tested instead of naming it as untested.
Q2: N-A — the claim says "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — The shared pins do not reach the compared state (the P1 block $FF8400-$FF87FF at samples 1501-2362, :180 and :189). The level pin $FF8116 starts at 2000 (:133) and is a global. The RNG pin $FF80D4 starts at 2363 (:134). The rig's X pins start at 2560 (name_moves.py.txt:687) and its HP pin at 2550 (name_moves.py.txt:699). The only write inside the P1 block within the window is the $FF8782 poke (:135), and it runs on the POKED leg only (:137-138).
Q4: VIOLATED — One failure mode is neither controlled nor named: the changed-value $FF8782 poke at 1400-1500 could itself produce or mask a LATCHED byte (for example, game logic reacting to the id change), rather than the confirm's latch. The two poke controls cannot fire on it. REAL-vs-SELF (:272, :281) and donovan-self (forced_pick_fidelity.tsv:17, "a same-id poke") are both same-id writes. same-leg (:310-316) only proves that a vanished difference would be noticed. wrong-cursor (:318-322) only proves that a real leg on the wrong cell fails identity. The claim does not name this gap; it states the opposite ("tested directly on the same write the poked leg performs").
Q5: N-A — no decision, approval or ruling is attributed to any person; "report #151 step 1 as done" is the working agent's recommendation.
VERDICT: VIOLATED

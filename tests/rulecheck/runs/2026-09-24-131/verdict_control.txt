Q1: VIOLATED — The claim says nothing else is untested, but the gate lists what it leaves out: audit_move_parity.sh.txt:34-36 "WHAT IT DOES NOT COVER: per-hit damage, meter gain, hitboxes and projectile parameters ... P2 is Victor ... his state is never compared". It also compares only the FIELDS at audit_move_parity.sh.txt:105, which do not include ff87c2, and the window "STARTS AT THE RIG'S FIRST EVENT, not at the anchor" (audit_move_parity.sh.txt:25), so this is not "frame-exact over the whole part". The claim's "every consumer ... enumerated in every addressing form" appears in none of the artifacts. The frozen row move_parity.tsv:20 reads "huitzil_1	DIVERGES	Jump [8]+21f", not 6851/6851.
Q2: VIOLATED — The claim concludes that Phobos's moves play the same as native (node, seq, x, y, timing across part 1, including the Jump and Float events at name_moves.py.txt:448-459). The only support given is the number "6851/6851 frames identical"; the claim names no image or recording, and none is among the artifacts.
Q3: VIOLATED — Both legs get the same rig pokes: audit_move_parity.sh.txt:98 native "printf '%s;%s;%s' "$_base" "$_lvl" "$_rng"" and :101 ours "... "$_base" "$_lvl" "$_rng"". $_base includes the position pins at name_moves.py.txt:658 "pin_pokes += [f"{t - 40}:ff8410:{x1:04x}", ...]" (part 1 events are all "far", name_moves.py.txt:448), and ff8410 is a compared field at audit_move_parity.sh.txt:105 "ff8410:w:x". The same applies to ff8116: poked on both legs at :89 "f'{f}:ff8116:06'" and compared as "ff8116:b:lvl". It also applies to ff8782: native via name_moves.py.txt:672 and ours via audit_move_parity.sh.txt:92, compared as "ff8782:b:id". A defect in these pokes would make the two legs agree on x, level and id.
Q4: VIOLATED — The gate has only two must-fire controls, unpinned-level and no-translation (audit_move_parity.sh.txt:4-5, :185-208). Neither would fire if the result depends on a runtime poke of $FF87C2 on our leg rather than on the built flavor_default=0x01. pokes_for (audit_move_parity.sh.txt:86-103) has no ff87c2 poke and no way to add one, so the "poked to 0x01" run is not a run of this gate, and nothing checks that the rebuilt tracks set the latch. The claim does not name this gap. Also, run on huitzil_1, the no-translation control goes dead because that part's frozen verdict is not IDENTICAL (audit_move_parity.sh.txt:195 "the control part $CTL is not frozen IDENTICAL, so the control cannot discriminate").
Q5: N-A — The packet attributes no decision, approval or ruling to the maintainer or any other person; "Phobos's"/"his" refers to the character.
VERDICT: VIOLATED

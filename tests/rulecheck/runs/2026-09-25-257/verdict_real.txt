Q1: VIOLATED — The claim's NOT-tested list covers tests that "run the current value or rule, not the move or the old state", but it names only (u) and (x) under that heading. Finding (d) has the same gap and is not named. STATE.md:94 gives (d)'s tests as "tests `tests/test_meter_gain.sh`, `tests/test_vanilla_frame_join.sh`". STATE.md:87 shows both gates were re-frozen on the fixed rig ("a walk-in set's first event at `FIRST_EVENT + WALKIN_SHIFT` (2800)"). So they run the corrected schedule and never reproduce a first event eaten by the round intro. The only f2600 reproduction was a scratch probe (STATE.md:86, "probe scripts in the scratchpad"). Two more cases look the same and are not named either. For (h), the quoted test line tests/audit_forced_pick_fidelity.sh:313 is the fix itself, and the red-run proof was a one-off log (STATE.md:90, `build/agent183/t154_freeze_red.log`). For (b)'s "meter half", tests/audit_throw_registration.sh:252 reads "RE-FROZEN 14z-183 on the #157 fix", so the gate shows the M20 behaviour, not the M19 dead-pair behaviour.
Q2: N-A — The claim is about documentation homes, quotes and greps. It draws no conclusion about how a move plays, its timing or a hit (build/agent183/packet_homes_14z183.txt:1).
Q3: N-A — Nothing is compared across two legs (ours against native, before against after). packet_verify.py re-reads the tree against the packet's quotes, and classing_cover.py checks the hand-written classing lines against the grep's hit list in the same file. Neither is a comparison of two measured states (build/agent183/packet_verify.py:9-10; build/agent183/classing_cover.py:15-17).
Q4: OK — Each check has a control for the failure it could hide. The retraction grep's reach controls are all found: build/agent183/retraction_greps_14z183.txt:63 "REACH: every reach control found (4)". The classing check catches its plant: build/agent183/classing_coverage.txt:24-26 "PLANT (exit 1) … MISSING 'Lilith j.LP/j.LK': tests/rulecheck/ledger.tsv". close_findings and homes_tracked each failed on their first run (the missed GAP 0x02231E; one token FAILED), which shows they can fire (STATE.md:95). packet_verify.py has no plant of its own, and its `startswith` test would pass an empty quote. That case does not arise here: all 53 quotes in packet_homes_14z183.txt:2-54 are non-empty, and the claim already names "the quote check proves only that the text is at that file:line".
Q5: OK — Every ruling the table attributes to the maintainer has the maintainer's own words quoted in the artifacts. STATE.md:92 has *"Land it (Recommended)"* and *"Freeze M20 now"*. STATE.md:93 has *"Pin it to M19 (Recommended)"*. build/agent183/packet_homes_14z183.txt:29 quotes tests/test_pod_black_foot_palette.sh:48 "maintainer-ruled 2026-09-26, 14z-183: "Pin it to M19".
VERDICT: VIOLATED

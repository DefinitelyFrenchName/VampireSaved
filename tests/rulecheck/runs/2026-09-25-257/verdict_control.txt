Q1: VIOLATED — The claim treats one premise as tested, but the artifacts show it was not. The claim says "the SELF leg is the real cursor path plus a poke of a DIFFERENT id (a changed-value write)". The gate pokes the SAME id: audit_forced_pick_fidelity.sh.txt:135 is `_pick=... f'{f}:ff8782:$_id'` with `_id="$(row_id "$_row")"` (line 114), and the "self" leg falls to `*)` at line 138. Lines 26-27 say so directly: "SELF — the REAL path PLUS the same-id poke ... (a write at 1400-1500 to an id that already holds that value)". The donovan-self row is also a same-id poke (line 87: `donovan-self) echo 13`, path "R R"). No leg tests whether a changed-value write alone disturbs state. That premise goes unstated, while the claim reports it as "tested directly on the same write the poked leg performs".
Q2: N-A — The claim draws no conclusion a player could feel: "No behavioural conclusion about how the character plays is drawn here."
Q3: OK — Both legs share the rig's pokes and pins (audit_forced_pick_fidelity.sh.txt:137-138). In the pre-match window 1501-2362, none of them writes the P1 block. The level pin ($FF8116, line 133, from 2000) and the RNG pin ($FF80D4, line 134, from 2363) are globals. The HP pin at $FF8850 starts at FIRST_EVENT-50 = 2550 (name_moves.py.txt:699). The position pins at $FF8410/$FF8810 fire at t-40 from 2600, so 2560 at the earliest (name_moves.py.txt:687). $FF8410 is inside the P1 block, but it lands only in play. That shared write to P1 +0x010 affects only the frozen first-play frame, and the claim names it.
Q4: VIOLATED — The claim credits "REAL vs SELF latches nothing (the poke mechanism itself is inert)" (audit_forced_pick_fidelity.sh.txt:281) with ruling out the poke mechanism on the poked leg's changed-value write. That control is a same-value write (lines 26-28, 135). The donovan-self negative control is one too (lines 48-51: "poking 0x13 over Donovan's own confirm"). Neither control would fire if a changed-value $FF8782 write, not the confirm, produced the three latched bytes. So charging +3bd/+3c2/+3e0 to the confirm rests on a failure mode the gate does not control and the claim does not name as a gap; the claim says it is tested.
Q5: N-A — The packet does not attribute any decision, approval, ruling or closure to the maintainer or anyone else.
VERDICT: VIOLATED

Q1: OK — The claim names its untested premises and the gate confirms them: globals and P2/whole-RAM are diagnostics only (audit_forced_pick_fidelity.sh.txt:29-41, 201-211, "P2's block and the globals are reported, not frozen"); the TRANSIENT count is echoed but got.tsv carries only row/latched/first-play (lines 283-284); P2-empty is asserted only from the POKED-vs-REAL line and REAL-vs-SELF reads fields 1 and 6 alone (lines 272-282); REAL-vs-ALT is MEASURE-only (lines 90-94, 285-289); SELF = real path + same-id poke and donovan-self is R,R + 0x13 where "R,R IS Donovan" (lines 48-49, 117-119, 135-138); the six pre-match samples are 1501 1600 1800 2000 2200 2362 (line 99, 180); the level pin $FF8116 from 2000 and RNG $FF80D4 from 2363 are globals (lines 133-134) and name_moves.py.txt's part-1 pins land at t-40=2560 ($FF8410/$FF8810, lines 681-687) and the HP pin at $FF8850 from FIRST_EVENT-50=2550 (line 704), part 1 absent from METER_PARTS (line 646). One unnamed premise, not load-bearing for "exactly three": "Victor on every leg" is never asserted — no leg reads $FF8B82 (classify lines 169-179 check only $FF8782 and $FF8403).
Q2: N-A — The claim's own last sentence: "No behavioural conclusion about how the character plays is drawn here"; the frozen first-play frame (forced_pick_fidelity.tsv:15, 2980) is a sampled RAM-difference frame, not a statement about how a move plays.
Q3: OK — Both legs share mkleg (audit_forced_pick_fidelity.sh.txt:109-140, "ONE function writes a leg's replay and pokes"), the same MAME/run_mame.sh/replay.lua path (lines 142-149) and the same copied pins; a shared defect could only hide a LATCHED byte by writing into $FF8400-$FF87FF during 1501-2362, and no such shared write exists (the only P1-block pokes are the id poke, poked/self legs only, line 135-138, and the rig's pins at >=2550, name_moves.py.txt:681-704); a path or poke silently not applied is separated by the identity assertions at $FF8782@1600 on every leg and $FF8403@1290 on the real leg (lines 169-179).
Q4: OK — same-leg (lines 307-316): the first row's poked leg diffed against itself must read empty against a non-empty frozen set, covering a run where the real leg silently equals the poked leg or the reducer returns nothing; wrong-cursor (lines 317-322, control leg built at 252-254 one move short): covers a real leg that confirms another character, proving the identity assertion fires; identity at 1600 on the poked leg (line 171-174) covers an unapplied poke; P2-block-empty (line 279-280) covers instrument noise. Failure mode nothing covers in the PASS path: a route-residue byte inflating the P1 latched set — REAL vs ALT runs under MEASURE only (lines 249-251, 285-289) — which the claim discloses ("measured under MEASURE, never frozen"); the reducer's own sensitivity to a planted fourth byte is not controlled, but the diff is a uniform full-range loop (lines 183-189) and the frozen three prove it sees differences.
Q5: N-A — The packet attributes no decision, approval, ruling or closure to any person; the "maintainer" mentions in name_moves.py.txt:260, 392, 463 concern Donovan/Pyron schedule rows, not the decision under review.
VERDICT: OK

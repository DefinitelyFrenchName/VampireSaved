Q1: VIOLATED — the claim says "Not tested: nothing further — the comparison is frame-exact over the whole part", but the gate says what it leaves out. It does not cover "per-hit damage, meter gain, hitboxes and projectile parameters" or P2 state (audit_move_parity.sh.txt:34-36). It compares 12 tenant fields, not whole RAM (audit_move_parity.sh.txt:105). Its window "STARTS AT THE RIG'S FIRST EVENT, not at the anchor" (audit_move_parity.sh.txt:25-29). The claim rests on part 1 only, while Phobos has parts 2-10 (move_parity.tsv:21-29; name_moves.py.txt:461-551). It tests a runtime poke, not the built flavor_default. pokes_for has no ff87c2 poke (audit_move_parity.sh.txt:86-103), ff87c2 is not in FIELDS (audit_move_parity.sh.txt:105), and no artifact shows the "every consumer enumerated" search or a "6851/6851" output. The gate prints only IDENTICAL/DIVERGES per part, and huitzil_1 is frozen "DIVERGES	Jump [8]+21f" (move_parity.tsv:20).
Q2: VIOLATED — the conclusion is that Phobos's part-1 movement plays like native: jumps, floats, air dashes and normals (name_moves.py.txt:448-460), after a frozen divergence at "Jump [8]+21f" (move_parity.tsv:20). Its only support is a number ("6851/6851 frames identical"). The packet holds no image or recording, and nothing shows one was put before the maintainer.
Q3: VIOLATED — both legs run the same rig file and the same schedule pokes (audit_move_parity.sh.txt:109-112, 88). Those shared pokes write compared fields. The speed level ff8116 is poked to 06 every frame from 2000 on both legs (audit_move_parity.sh.txt:89, 98, 101) and is compared as "ff8116:b:lvl" (audit_move_parity.sh.txt:105). P1 X ff8410 is re-poked on both legs 40 frames before every "far" event, and every part-1 event is "far" (name_moves.py.txt:448, 657-658); it is compared as "ff8410:w:x" (audit_move_parity.sh.txt:105). So any flavor-driven difference in level, or in position carried across events, is overwritten identically on both legs.
Q4: VIOLATED — the gate's two controls cover the level pin ("unpinned-level") and the anim-pointer translation ("no-translation") (audit_move_parity.sh.txt:4-5), and nothing else. The claim does not name, and no control covers, three failure modes. (1) A runtime poke of ff87c2 does not reproduce flavor_default=0x01 in huitzil.toml, for example through a reader of the byte before the poke lands. (2) The latch changes damage, meter, hitboxes, projectiles or P2 state, which the gate never compares (audit_move_parity.sh.txt:34-36). (3) The latch changes moves in parts 2-10, which were not run. The controls also run only on the first part in SET (audit_move_parity.sh.txt:182), and huitzil_1 is frozen DIVERGES, so they report "CONTROL DEAD: no-translation — the control part $CTL is not frozen IDENTICAL, so the control cannot discriminate" (audit_move_parity.sh.txt:195).
Q5: N-A — the claim attributes no decision, approval or ruling to anyone: "so flavor_default becomes 0x01 and the five tracks are rebuilt and frozen as M19".
VERDICT: VIOLATED

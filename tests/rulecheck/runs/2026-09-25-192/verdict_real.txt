Q1: VIOLATED — The claim does not name one premise it rests on: that the retraction-grep output is a reading of the tree the close commits. The artifact shows it was not re-run after the last edits. build/agent181/retraction_greps_14z181.txt:11-12 lists a hit in docs/NEXT_SESSION.md: "that last one was captioned "at height 40 on the way down" and is WRONG". That text is not in docs/NEXT_SESSION.md (lines 1-87, the rewritten file that build/agent181/close_carriers_14z181.diff:32-202 records). The classing at retraction_greps_14z181.txt:74 still counts "docs/NEXT_SESSION.md's capture note", and STATE.md:96 says "NEXT_SESSION's five-captures item consumed". So the edits made after the maintainer's reads were never grepped: the NEXT_SESSION rewrite, the new DECISIONS_HISTORY entry, the engine_internals lines and STATE row (13). The claim presents that output as the retraction check without saying so. Separately, a figure check the claim does name as manual reading missed a stale count: (w)'s new tracked home still says "two of the 35 never-entered a2 chains of GitHub #174" (docs/game/engine_internals.md:1022), but the re-run census and #174's row say 36 (build/agent181/chain_census_rerun_14z181.txt:3,24,40, i.e. 15+11+10; docs/project/tickets.tsv:75).
Q2: VIOLATED — The claim restates findings a player could feel: "pyron_3's event 5, named "far (whiff)" by the rig, in fact connects", Pyron's Zodiac Fire guard cancel firing, the level-6 j.HP whiff, and the +1 damage. The claim itself says "still with NO capture: (q)'s Pyron half (Zodiac Fire), (g) (the level-6 whiff), and the damage figures of (r) and (t)". Every capture that does exist was sent after its conclusion was drawn. For (dd)/(ee): "AFTER the measurement (the naming freeze of a91a555a); the maintainer's read: NOT YET GIVEN" (build/agent181/captures_sent_14z181.txt:39). For each earlier sheet: "AFTER the RAM measurement" (captures_sent_14z181.txt:20-24). The trap sheet the claim says "covers (aa)/(bb)/(cc)" went out with the retracted caption: "the caption sent with the file said "at height 40 on the way down"" (captures_sent_14z181.txt:24). No artifact shows a corrected caption reaching the maintainer.
Q3: N-A — The packet's own conclusion is that each finding has a tracked home and a test; it compares no two legs. The ours-vs-native results it restates belong to their gates' own packets. The one pair it runs, the real homes tool against its `--blind` variant, is built to disagree, and the gate reads DEAD if both pass (tests/test_close_tools.sh:61-62; build/agent181/test_close_tools_14z181.log:7).
Q4: VIOLATED — Two ways this claim could be wrong are neither controlled nor named. (1) The `reach:N` threshold. The claim says "a `reach:2` control since run 190, so one carrier's miss fails it (probed: a reach:2 pattern with one carrier exits 1)", but no artifact shows that probe. The gate's only reach plant is a plain `reach` pattern with 0 hits (tests/test_close_tools.sh:42 and :58, "\treach\n"). So if the `need = int(kind.split(":")[1])` branch broke (tools/retraction_grep.py:75-76), control 2 could lose a carrier and still pass, and test_close_tools would stay green. (2) Whether the grep output is current. The reach controls prove only that the grep reached the tree on the day it ran. Nothing ties build/agent181/retraction_greps_14z181.txt to the committed tree, and the artifact is demonstrably older than docs/NEXT_SESSION.md (see Q1). Also, retraction_grep.py never exits non-zero on a hit for a retracted wording (tools/retraction_grep.py:76-86), so the gate cannot catch a stale output or a re-introduced carrier. The claim names only the hand classing, not this.
Q5: OK — The maintainer's own words are quoted for every attribution. #173 was closed on "yes, identical."; (j) is VALIDATED on "looks identical. The 1 frame difference and its origin must be documented but identical and validated otherwise."; the #169 request is "One thing where captures are missing is #169" (DECISIONS_HISTORY.md:36-37,41; build/agent181/captures_sent_14z181.txt:32-33,37). The closure is stated as the agent's act on the read, not as the maintainer's ruling. One wording nit: "every one identical" covers a read given as *"correct"* (DECISIONS_HISTORY.md:33).
VERDICT: VIOLATED

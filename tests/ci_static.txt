# ci_static.txt — gates that need ROMDIR and/or a build dir, but NO emulator.
# Run by tests/run_all_static.sh (14z-94, GitHub #30). One name per line.
#
# MEMBERSHIP RULE: no MAME, no FBNeo, no soak. If a gate boots an emulator it
# belongs in HANDOFF.md's manual index, not here — this file is the set cheap
# enough to run before a commit.
#
# Companion: tests/ci_portable.txt, the ROM-free tier that needs no inputs at
# all. A gate that needs neither ROMDIR nor a build dir belongs there instead.
#
# SKIP IS EXPECTED HERE and is NOT a pass (GitHub #29). Several of these read
# an untracked build/<name> dir, so on a fresh checkout they print
# `SKIP: no build at ...` and exit 0. run_all_static counts those separately;
# `--strict` makes them fatal.

# --- the set GitHub #30's handoff named, minus test_manifest_merge which is
#     already ROM-free and lives in ci_portable ---
test_m3a_reproducible
test_patch_overlap
test_reaction_hook_d2
test_region_overlap
test_region_overlap_control
test_romset_identity
test_shared_writes
test_tenant_loop
test_tenant_row_owner
test_thunk_addr_literal

# --- ADDED 14z-94: the rest of the emulator-free population. #30 counted 101
#     orphans of 130 scripts; the coverage check below found 38 that are
#     emulator-free and were in no registry. Registered here so they run,
#     rather than left to a future session remembering a filename. Several
#     read an untracked build/<name> dir and will SKIP on a fresh checkout —
#     that is reported, never counted as a pass (GitHub #29).
audit_gfx_merged_census
audit_select_bank_gates
test_accent_census
test_beam_list_type6
test_builder_rom_audit
test_census_regions
test_crypt_boundary
test_decode_stage_banners
test_effect_palette_table
test_extract_hp
test_fbneo_tree_integrity
test_fbneo_tree_integrity_control
test_frozen_rompath_guard
test_gfx_chain
test_gfx_layout3
test_gfx_menus_guard
test_fsm_census
test_gfx_tiles
test_hitclass_map_thunk
test_hui_winscreen
# The win-quote text system's STRUCTURE, frozen 14z-116: the root is a
# 4-entry REGION array (the four banks are LANGUAGE variants, not copies),
# the first-level variant rows alias the base half (the defect itself),
# every reachable line on all four banks walks clean against the renderer's
# own 66-word buffer, and vs2 carries un-aliased tenant blocks. Two must-fire
# controls. Needs ROMDIR only; ~40 s.
# docs/game/engine_internals.md "The WIN-QUOTE TEXT SYSTEM"
test_win_quote_decode
test_id_space
test_index_space
test_index_window_thunk
test_list_type_census
test_member_classify
test_null_build
test_obj_walker_relocation
test_patch_source_identity
test_pcrel_escapes
test_phasec_spaces
test_qs_id_table
test_qs_songs
test_suite_dispatch_selftest
test_type_stamp_census
test_variant_dispatch
test_voice_row_range

# NOT HERE, and why (14z-94): three gates were registered above on a first
# pass and REMOVED when the coverage check was made transitive — each
# reaches an emulator indirectly and so belongs to HANDOFF.md's manual
# index, not to a chain advertised as emulator-free:
#   test_crash_guard      -> tools/run_replay_guarded.sh
#   test_m2a_stage4_code  -> sources tests/lib/m2a_common.sh (run_replay_mame)
#   test_m2b_stage6       -> same route
# test_m2a_stage4_code ran for 208s inside the 'static' tier before this was
# caught, which is how the indirection was found.

# --- ADDED 14z-95 ---
# Ground truth for the ONE reconciliation matcher (GitHub #43(a)): the
# refactor is pinned INERT against the pre-refactor copy, and its parameters
# are proven load-bearing so #43(b) is a real change. Needs ROMDIR + git.
test_reconcile_matcher
# Ground truth for tools/ensure_merged_inputs.sh: the merged build's four
# ROM-derived inputs are PRODUCED, not demanded (GitHub #27, maintainer-ruled
# 2026-08-18 "it should be one command"). Needs ROMDIR + the pinned build
# dirs; SKIPs without them.
test_merged_inputs

# The KERNEL per-class voice tables (14z-96): vsavj's variant halves are
# byte-copies (the merged-m2 electrocute-grunt alias), vs2's carry the
# newcomers' real rows, and 0x2a1/0x2a2 are FREE Z80 rows in both games
# (Phobos' hurt events are voiced with deliberate silence). Needs ROMDIR.
test_kernel_voice_tables

# 14z-97 (GitHub #96): the M2 battery's legacy target is resolved from the
# build, and an unregistered image stops it. Needs ROMDIR (it builds a
# one-poke image to be 'not any frozen build'), no emulator.
test_m2a_target_resolution

# 14z-99 (GitHub #104): the option-(a) fix premises — the capture positioner's
# id-unmasked read, the 5-consumer set of 0xBE27A, the 14+2 alias shapes, the
# stride-equal cross-oracle twins, base-half byte-identity vsavj==vs2, the
# signed-16-bit bound, and the 15-block/0x11bd0 port inventory. Needs ROMDIR
# (decrypt cache), no emulator.
test_capture_pose_sources
test_pointer_flow
test_escape_triage

# 14z-102: the #109-B sweep inventory (filtered, verdicts frozen in header)
test_biased_list_inventory
test_release_roundtrip

# 14z-107 (4): the MiSTer SDRAM placement map fits the 64 MB tier by
# 0.125 MB as placed (0.708 MB retracted 14z-107 (9) by the census),
# and the three content extents + the PRG pin it depends on are frozen. Reads
# the built WIDE romset + manifests (build/m3b_merged15 by default,
# MAP_FIT_BUILD to override), no emulator. docs/project/mister_map.md
audit_mister_map_fit

# 14z-107 (5): MiSTer slice D0 — the MRA that TRIMS the WIDE image so it can be
# downloaded at all. Runs the real jtframe generator over the real romset and
# asserts the .rom is byte-for-byte the placement map's, that the stock vsavj
# MRA is still the reference core's twin, and that cores/cps2 cannot see the
# WIDE set. Two must-fire controls (untrimmed => overflow; +1 KiB => table
# breaks). Needs emu/jtcores, go and ROMDIR; ~15 s.
# docs/project/mister_map.md §3, docs/platform/mister.md
test_mister_mra_map

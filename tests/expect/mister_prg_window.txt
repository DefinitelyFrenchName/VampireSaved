# tests/test_mister_prg_window.sh — the frozen pair. One line per leg,
# the probe's own last per-frame report. Regenerate DELIBERATELY (--freeze):
# a moved count on a deterministic simulation is a finding.
# RE-FROZEN 14z-170 on merged-m19 (build/m3b_merged27) from the M19 freeze's own MiSTer romset lane
# (build/emu_freeze_m19_mister/test_mister_prg_window.log — every structural assertion PASSED there; only the
# pair moved, the control leg's line UNCHANGED). WHAT MOVED M18 -> M19: `first_addr` 4c13d0 -> 4c1400 and `max`
# 4d479e -> 4d47ce, both +0x30 — the relocated OBJ walker and the read ceiling carried by the allocator cascade of
# the three regrown thunks (27 placed regions +0x30, patch_notes 14z-170); `blocks` 8 -> 7 and `cyc` / `rd_lo`
# -1214 / -1078. RECORDED AS MEASURED, NOT EXPLAINED ([VSP-116]): the block count and the two counters are
# not attributed here — the candidate, untested, is the +0x30 shift moving a placed span across a block
# boundary and the fixes changing what the 68k executes in the match frames this replay reaches.
#
# Re-frozen 14z-134 on merged-m16 (build/m3b_merged23) from the M16 release run's own
# measurement: the first extension address the 68k executes is the relocated OBJ
# walker's PLACEMENT (4be7c0 on merged-m10 -> 4c13d0 there), which moves with the
# romset — cadence romset since.
# RE-FROZEN 14z-144 on merged-m17 (build/m3b_merged25) from the M17 sweep's own
# measurement; then AGAIN, SAME DAY, on merged-m18 (build/m3b_merged26) from the
# M18 RELEASE sweep's own measurement (build/emu_release_m18/, option (b) both
# times). TWICE IN ONE SESSION is the point: this pair follows the ROMSET, and
# nothing in the freeze ritual asks which tracked artifacts do — see STATE
# "Decisions pending", the [VSP-178] freeze-ritual check, on the open-items list
# at the maintainer's word 2026-09-09.
#
# WHAT MOVED M17 -> M18, and it is NOT what moved M16 -> M17. `max` did NOT move
# (4d479e both), because the M18 Donovan/Jedah fix changes a VALUE inside an
# already-placed blob rather than moving a placement — where M17's Pyron port
# appended 0xB80 and pushed the read ceiling up by exactly that. Only `cyc` and
# `rd_lo` moved, and they moved BACK to their merged-m16 values exactly
# (97642730 / 75764374). Two independent counters landing on prior values is not
# chance. RECORDED AS MEASURED, NOT EXPLAINED ([VSP-116]). The candidate
# mechanism, untested: object timing on this core is a function of GFX CONTENT
# (jtcps1_obj_draw.v:137 skips a tile whose fetched word is all-ones) and the
# only gfx delta across these three freezes is one character of the mark glyph,
# 6 -> 7 -> 8; a sparse 7 differing from a dense 6 and 8 would produce this.
# first_addr 4c13d0, first_frame 1119, min 413c70, blocks 8, cyc_hi_rd/wr and
# rd_hi are all UNCHANGED, and every structural assertion PASSED in that run.
# RE-FROZEN 14z-183 on merged-m20 (build/m3b_merged28) from the M20 freeze's own MiSTer romset
# lane (build/emu_freeze_m20_mister/test_mister_prg_window.log — every structural assertion PASSED
# there; only the pos pair moved, the neg line UNCHANGED). WHAT MOVED M19 -> M20: `cyc` 97641516 ->
# 97643082 and `rd_lo` 75763296 -> 75764759; `first_addr`, `min`, `max`, `blocks`, `cyc_hi_rd/wr` and
# `rd_hi` all UNCHANGED. RECORDED AS MEASURED, NOT EXPLAINED ([VSP-116]): the M20 delta is the #157
# store words (18 on merged; whether this replay's frames execute any of them is not measured) and
# the mark's glyphs (M19 -> M20); the candidate is the gfx-content timing the M17 -> M18 note names,
# untested.
#
pos frame 2900 wide 1 cyc 97643082 cyc_hi_rd 1211302 cyc_hi_wr 10816 rd_hi 1211302 rd_lo 75764759 blocks 7 first_frame 1119 first_addr 4c1400 min 413c70 max 4d47ce
neg frame 2900 wide 0 cyc 97337938 cyc_hi_rd 4 cyc_hi_wr 10113 rd_hi 0 rd_lo 75503450 blocks 0 first_frame -1 first_addr 000000 min 7ffffe max 000000

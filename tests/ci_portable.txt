# tests/ci_portable.txt — the gates a CLEAN CHECKOUT can run.
#
# "Clean checkout" means: no $ROMDIR, no decrypted views under build/out/,
# no build directories. That is what CI has, and it is the whole reason this
# list is shorter than "the ROM-free gates" — a gate that SKIPs for want of
# build/out/vsavj_opcodes.bin is not portable, it is silent.
#
# THE JOB MUST FAIL ON SKIP. A CI job named "tests" that runs 3% of them and
# reports green is its own false green; one that runs 12 and says so is
# honest. The runner asserts each line below exits 0 AND does not print SKIP.
#
# Kept beside the gates, not in the workflow file, so adding a gate is a
# one-line edit here rather than a change to executable CI config.
#
# EXCLUDED, with the reason — do not add these without solving it first:
#   test_beam_list_type6        needs build/out/*.bin + a huitzil build dir
#   test_hitclass_map_thunk     needs build/out/vsav2_opcodes.bin
#   test_obj_walker_relocation  needs build/out/vsavj_opcodes.bin + a build
#   test_type_stamp_census      needs build/out/*.bin + three build dirs
#   test_region_overlap         needs the three tenant build dirs
#   test_shared_writes          needs the three tenant build dirs
#   test_select_wheel           drives MAME
#   test_fbneo_tree_integrity*  needs the emu/fbneo submodule checked out
#   test_decode_stage_banners   needs build/out/{vsavj,vsav2}_data.bin
#   test_voice_row_range        needs the DATA view + tenant build dirs
#   test_member_classify        section 1 reads the real romsets or a packed
#                               build; it PASSES here only because build dirs
#                               exist locally, and would SKIP on a clean one
#   test_builder_rom_audit      its positive control needs a clean $ROMDIR
#   test_patch_source_identity  compares two real reference sets
#   test_guard_integrity        drives MAME
# Every one of those is derived from ROM content, which rule 7 keeps out of
# the repo. They stay local-only by design, not by oversight.
test_attribute_ramdiff
test_audit_merged_dispatch
test_baseset_mask_invariant
test_basis_publish_atomic
test_battery_accounting
test_build_gate_status
test_build_identity_distinct
test_build_ref_rot
test_classify_hitclass_probe
test_classify_pool_spawns
test_compare_composite
test_compare_flicker
test_compare_window
test_decrypt_cache
test_describe_masked_shape
test_fbneo_overlay_hygiene
test_fbneo_overlay_hygiene_control
test_freeze_basis_sandbox
test_freeze_retires_diverge
test_gfx_collision_gate
test_gfx_layout_fields_live
test_harness_frame_bound
test_hex_lengths
test_masked_compare
test_m2a_flicker_gate
test_m2a_target_policy
test_mame_mirror_guard
test_manifest_merge
test_meter_in_field_map
test_minitoml_subset
test_no_tracked_mutation
test_obj_record_walk
test_optimize_guard
test_phasea_a3_liveness
test_qs_ledger_binding
test_qs_wav_timebase
test_qs_window_law
test_record_walk_bounds
test_replay_stage_census
test_romset_path_guard
test_s4_thresholds
test_select_port_hygiene
test_shell_portability
test_static_runner
test_tenant_id
test_gfx_tile_codec
test_jtcores_twin
test_rpl2siminputs
test_sim_wram_contract
test_mister_wide_gate

# 14z-107 (10): the MiSTer SYNTHESIS document and its drawn page still
# describe the map tests/audit_mister_map_fit.sh defends. Re-derives every
# placement offset/length, the frozen extents, the .rom arithmetic and the
# ASCII figures embedded in docs/project/mister_core.md; recounts the group-C
# occupancy census and re-reads the palette when the build and the decrypted
# image are present, and SKIPs those two otherwise. Three must-fire controls.
# ROM-free, no emulator, ~5 s. tools/mk_mister_page.py --check
test_mister_page

# 14z-107 (11): the ROM-free half of the slice D4 discriminator. Locks the 68k
# program-ROM read probe's contract — it is inert unless JTCPS2W_PRGPROBE is
# defined (with the hoisted-line control); its window bit IS the decode's
# window bit, both re-read from the RTL; its ADDRESS half carries no chip
# select, which is what makes it speak on the profile-CLEAR leg. And the
# verdict logic is itself tested on synthetic logs: all three answers plus
# THREE refusals — a silent control, a control whose bytes do not verify, and
# a probe whose HI records sit below $400000 (the defect the first draft
# shipped with). ROM-free, no emulator, ~3 s.
test_mister_prg_probe

# test_mra_parts — ground truth for tools/check_mra_parts.py. jtframe resolves
# zip members by CRC32 ALONE and 0xFF-FILLS a part it cannot resolve rather
# than refusing, so an MRA can look complete and hand the machine a holed ROM.
# The checker's verdict logic is exercised on fixtures whose answer is known by
# construction: a clean resolve, plus THREE refusals — one bogus CRC (named in
# the report, not just counted), an absent zip, and the count excluding inline
# literal parts. ROM-free, ~1 s.
test_mra_parts

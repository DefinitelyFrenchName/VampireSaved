# pyron.toml — port configuration for the Pyron tenant (M3b Phase 5
# ladder, opened 14z-67). Consumed by tools/gen_donovan_patch.py via
# --port (driver: TENANT_MANIFEST/TENANT_CHAR on tools/build_donovan.sh).
#
# MINIMAL BY DESIGN (the huitzil.toml doctrine): this is the stage 1-3
# ladder manifest — null relocation -> passive data -> anim/sub-tables.
# Stage 4+ rows (init shim, engine hooks, sound stubs, thunks) are added
# ONLY as their mechanisms are MEASURED for THIS character; huitzil.toml
# and donovan.toml are references for the shapes, never templates to
# copy blind.
#
# Pyron facts already on the record (do not re-derive):
# - Variant-id tenant ONLY (0x11 — measured-free variant row,
#   docs/game/atlas/id_space.md); every build requires --profile cps2-wide-v1.
# - NO VS2/VH2 flavor fork (community-confirmed; D2 opens only if the
#   Phase-1-style measurement shows real vs2<->vh2 divergence).
# - Extraction is oracle-clean bare (14z-65 machinery, re-verified
#   14z-67): single code region 0x574C0+0x5200 (+0x30 shift, one 0xC
#   dead filler at +0x234), anim 0x264086+0x1B500, 769 engine refs,
#   ZERO char-id 0x11 immediates in code (unlike H's one site).
# - Both structural censuses CLEAN on his code region (14z-67,
#   tests/test_census_regions.sh section 2): 0 data_in_code, 0 pcrel
#   escapes. His support-zone roots are NOT extracted yet — the census
#   reruns as the R1 loop grows the region set (the gate's region-count
#   lock fails loudly then, by design).
# - gfx layout share RESERVED (build/manifest/gfx_layout3.toml row
#   "pyron": band 0x4ED5-0x8647 at DELTA 0, bank 4). His select
#   portrait palettes are a DEDICATED +0xBC row block in vs2 (measured
#   14z-67 at the vs2 0x6B1A6 compare chain) — the Donovan block shape,
#   NOT H's grid column.

[[tenant]]
name = "pyron"
src_set = "vsav2"
src_char = 0x11
id = 0x11
# 14z-67 (first mash-soak crash, vec3 f3383 in the OBJ emitter): a
# recycled effect slot ($FF9500) carried stale bytes under his
# satellite init — the alloc_wrap class exactly (his code calls the
# vs2 allocator pair like H's and Donovan's; vs2's allocator semantics
# need the 0x80-clear wrapper on vsavj). Same row as theirs.
alloc_wrap = "0x15702,0x1572e"
# His rows live in a per-tenant overlay — the shared map is frozen for
# Donovan's reproducibility (the reconciliation_huitzil.toml precedent).
recon_overlay = "build/manifest/reconciliation_pyron.toml"

# ── ADDRESS-SPACE MODEL — same shape as the other tenants'; duplicated
# for the single-tenant ladder. The M3b Phase 2 merge hoists this into a
# shared manifest; until then single-tenant builds each start from
# pristine vsavj and may not be naively merged (the op-overlap assertion
# would catch it).
[[space]]
name = "hole_a"
start = 0x0BF6A0
end = 0x100000
class = "crypt"
fallback = "hole_b"

[[space]]
name = "hole_b"
start = 0x3EC720
end = 0x400000
class = "raw"
fallback = "wide_ext"

[[space]]
name = "wide_ext"
start = 0x400010
end = 0x600000
class = "raw"
profile = "cps2-wide-v1"

# ══ 14z-67 moveset arc: SHARED-ZONE rows — P ports the same shared
# regions H does, so every region-scoped mechanism row H carries for
# them applies to P's copies verbatim (the rows are properties of the
# SHARED SOURCE BYTES; the Phase-2 merge dedups by span). First soak
# crash (vec3 f3383, OBJ emitter, +0x1C NULL chain -> the data-view
# [0x4] read) = the pod-zone data_in_code class biting its FOURTH time:
# x088512 is crypt-placed and its embedded offset/record table read
# garbage under re-encryption, so his satellite's record never
# installed.

# Companion queue-class remap (vs2 class 7 -> vsavj class 6).
[[port_patch]]
region = "x088512"
src_addr = 0x08B0F8
old_hex = "000e"
new_hex = "000c"
note = "companion queue class 7 (vs2-only) -> vsavj class 6 (the Anita/H precedent)"

# Secondary/companion type-dispatch union (both engine sites — the
# vanilla tables over-index for newcomer types otherwise; the H f4983
# class). Unresolved extras tripwire loudly.
[[obj_hook]]
site = 0x054470
vanilla_table = 0x054484
vanilla_entries = 59
src_table = 0x05C620
src_entries = 76

[[obj_hook]]
site = 0x05E542
vanilla_table = 0x05E556
vanilla_entries = 114
src_table = 0x06A51C
src_entries = 124

# The pod-zone embedded offset/record word table (x088512+0x3AE4 reader
# — the 14z-66 data_in_code census row, shared bytes).
[[data_in_code]]
region = "x088512"
table = 0x08C042
table_len = 0x100
reader = 0x08BFF6
reader_old_hex = "47fa004a30330000"
note = "pod-zone word offset/record table (a3 re-derived from it; self-relative; shared-zone copy)"

# The shared zones' pcrel escapes (oracle-invisible; targets resolved
# in the overlay — the same twin rows as H's).
[[pcrel_escape_fix]]
stage = 4
region = "x026142"
pad = 0x60

[[pcrel_escape_fix]]
stage = 4
region = "x05c800"
pad = 0x20

# ══ 14z-74: THE P GFX RUNG — stage-6 sections ═══════════════════════════
#
# Layout: build/manifest/gfx_layout3.toml row "pyron" — his art is vs2
# BANK 3 native codes, band 0x4ED5-0x8647, placed into WIDE group C bank 4
# at DELTA 0 (no record remap). No [gfx_remap] section ON PURPOSE: nothing
# moves, so there is no effect map and no tile-word rewrite. Same strongest-
# correctness class as Huitzil's rung.
#
# ONE-SOURCE-BANK PREMISE: MEASURED FOR HIM, not assumed
# (tests/test_list_type_census.sh, 14z-74). His FIGHTER anim span carries
# ZERO list-type-4 sprite lists — against a live 26-hit control on Huitzil's
# span, whose beam is a known type 4. Type 4 is the one format that composes
# its OWN bank word (bank 1) instead of taking the object's, so a tenant
# carrying one needs a ported handler + --strip-tiles to reach group C
# (Huitzil's beam, three sessions). Pyron needs neither: the bank plumbing
# below is his whole program-side gfx surface.
# CAVEAT: that census covers his FIGHTER span. His effect/companion data
# rides the SHARED x088512 region and is not extracted yet — re-run the
# census over it when his effect rung lands.

# ── OBJ bank setters: every hardcoded `move.w #$6000,$18(aN)` in P's
# ported regions. Measured 14z-74 by scanning his built region images
# (12 sites), against a control scan of Huitzil's build that re-derives
# his 12 manifest rows. 11 sites sit in SHARED regions at addresses
# IDENTICAL to H's rows (the rows are properties of the shared source
# bytes); the 12th, 0x05938C, is in H's x057456 but lands in P's own
# `code` region (his handler zone 0x0574C0 subsumes it).
# All tenants share WIDE bank 4 in the ratified layout, so the variant
# word is 0x1000 everywhere. new_hex == old_hex: there is no base-half
# P configuration (variant-id native), the row exists for the variant
# value only.
[[port_patch]]
stage = 6
region = "code"
src_addr = 0x05938C
old_hex = "397c60000018"
new_hex = "397c60000018"
new_hex_variant = "397c10000018"
note = "OBJ bank setter (P own code zone): vs2 bank 3 -> WIDE bank 4"

[[port_patch]]
stage = 6
region = "x05c800"
src_addr = 0x05CF38
old_hex = "3d7c60000018"
new_hex = "3d7c60000018"
new_hex_variant = "3d7c10000018"
note = "OBJ bank setter (shared zone): vs2 bank 3 -> WIDE bank 4"

[[port_patch]]
stage = 6
region = "x05c800"
src_addr = 0x0620D4
old_hex = "397c60000018"
new_hex = "397c60000018"
new_hex_variant = "397c10000018"
note = "OBJ bank setter (shared zone): vs2 bank 3 -> WIDE bank 4"

[[port_patch]]
stage = 6
region = "x05c800"
src_addr = 0x062194
old_hex = "397c60000018"
new_hex = "397c60000018"
new_hex_variant = "397c10000018"
note = "OBJ bank setter (shared zone): vs2 bank 3 -> WIDE bank 4"

[[port_patch]]
stage = 6
region = "x06800c"
src_addr = 0x068360
old_hex = "397c60000018"
new_hex = "397c60000018"
new_hex_variant = "397c10000018"
note = "OBJ bank setter (shared zone): vs2 bank 3 -> WIDE bank 4"

[[port_patch]]
stage = 6
region = "x06800c"
src_addr = 0x0683A2
old_hex = "397c60000018"
new_hex = "397c60000018"
new_hex_variant = "397c10000018"
note = "OBJ bank setter (shared zone): vs2 bank 3 -> WIDE bank 4"

[[port_patch]]
stage = 6
region = "x06800c"
src_addr = 0x0683EA
old_hex = "397c60000018"
new_hex = "397c60000018"
new_hex_variant = "397c10000018"
note = "OBJ bank setter (shared zone): vs2 bank 3 -> WIDE bank 4"

[[port_patch]]
stage = 6
region = "x06800c"
src_addr = 0x06842E
old_hex = "397c60000018"
new_hex = "397c60000018"
new_hex_variant = "397c10000018"
note = "OBJ bank setter (shared zone): vs2 bank 3 -> WIDE bank 4"

[[port_patch]]
stage = 6
region = "x0692f6"
src_addr = 0x069490
old_hex = "397c60000018"
new_hex = "397c60000018"
new_hex_variant = "397c10000018"
note = "OBJ bank setter (shared zone): vs2 bank 3 -> WIDE bank 4"

[[port_patch]]
stage = 6
region = "x088512"
src_addr = 0x08873E
old_hex = "3d7c60000018"
new_hex = "3d7c60000018"
new_hex_variant = "3d7c10000018"
note = "OBJ bank setter (pod/companion zone): vs2 bank 3 -> WIDE bank 4"

[[port_patch]]
stage = 6
region = "x088512"
src_addr = 0x089D26
old_hex = "3d7c60000018"
new_hex = "3d7c60000018"
new_hex_variant = "3d7c10000018"
note = "OBJ bank setter (pod/companion zone): vs2 bank 3 -> WIDE bank 4"

[[port_patch]]
stage = 6
region = "x088512"
src_addr = 0x08B100
old_hex = "3d7c60000018"
new_hex = "3d7c60000018"
new_hex_variant = "3d7c10000018"
note = "OBJ bank setter (pod/companion zone): vs2 bank 3 -> WIDE bank 4"

# ── The ported per-char OBJ bank table copy (x026142 tail), same shape as
# H's: rows = VANILLA vsavj 0x282D4 values EXCEPT the tenant's own row.
# Here row 0x11 = 0x1000 (P's sprites fetch from WIDE bank 4 on any read
# path that resolves through the ported copy) and row 0x10 is back to its
# vanilla 0x6000 alias — this is P's build, not H's.
[table_fix]
stage = 6
region = "x026142"
pad_len = 0x1440
table_off = 0x13EE
rows_hex = "600060000000200060000000400040000000600040006000600000000000400060001000000020006000000040004000"
note = "ported per-char OBJ bank table -> vanilla vsavj values + row 0x11 = WIDE bank 4"

# ── vsavj's own per-char OBJ bank table (0x282D4): the engine's char-init
# reads row = runtime char id. Variant row 0x11 is a vanilla alias of row
# 0x01 (the generator's alias anchor); superset-safe because no legacy
# gameplay path assigns ids 0x10-0x1F (tests/audit_id_writers.sh).
[[code_word]]
stage = 6
name = "obj_bank_word_slot"
slot_table = 0x0282D4
slot_stride = 2
old_hex = "6000"
new_hex = "6000"
new_hex_variant = "1000"

# shared_writes.toml — THE FROZEN SHARED-SURFACE WRITE INVENTORY (14z-79)
#
# ***********************************************************************
# ** RE-POINTED AND RE-REVIEWED 14z-128 (2026-09-03). The inventory now  **
# ** pins the CURRENT REGISTERED FREEZE — don_m18 / hui52 / pyron36      **
# ** (merged-m14, mark M12, 14z-119) — and every write in it has been    **
# ** attributed to a named change with its `why` recorded below, which   **
# ** is the step [VSP-97] refuses to let a re-point skip.                **
# **                                                                     **
# ** WHAT IT WAS: pinned to the 14z-91 builds (don_m7 / hui41 /          **
# ** pyron26), green against those dirs and not against what ships, for  **
# ** TEN FREEZES. The re-point added, per tenant, the #101 kernel        **
# ** voice-table words, the #104 capture-keyframe pointer rows, the      **
# ** Oboro select hook, the two random-select thunks, and (huitzil) the  **
# ** #109 beam effect-class row: donovan 90 -> 114, huitzil 87 -> 112,   **
# ** pyron 76 -> 100.                                                    **
# **                                                                     **
# ** AND NINE PER TENANT WERE INVISIBLE TO THE AUDIT ITSELF (14z-128):   **
# ** capture-keyframe rows 0x08-0x0F and 0x18 were EXEMPTED as "variant  **
# ** rows" because bank_map's `gap_be27a` is kind=auto carrying the      **
# ** scanner's default stride — eight LEGACY rows, in the gate whose     **
# ** purpose is to make a legacy-surface write a build-time review.      **
# ** Fixed in tools/audit_shared_writes.py; locked by section 4 of       **
# ** tests/test_shared_writes.sh. The ROOT cause is the bank_map row and **
# ** is NOT fixed: `kind` is load-bearing in extract_char.py and         **
# ** gen_donovan_patch.py, so correcting it can move BUILD OUTPUT — see  **
# ** STATE "Decisions pending".                                          **
# **                                                                     **
# ** ONE CORRECTION carried from the 14z-127 note: 0x0BE27A..0x0BE296 is **
# ** the #104 CAPTURE-KEYFRAME table, NOT the #99 AI-script unpark (that **
# ** is PRG:0xBF01A/09A/11A/19A, genuine variant rows, never shared      **
# ** surface).                                                           **
# **                                                                     **
# ** STILL TO DO AT THE NEXT FREEZE: the M13 tracks (don_m19 / hui53 /   **
# ** pyron37) add three `boot_title_saved_*` rows each — reviewed at     **
# ** 14z-127, so that re-point is a one-liner once M13 is registered.    **
# ***********************************************************************
#
# Every build op that lands OUTSIDE declared free space and OUTSIDE a known
# variant row (0x10-0x1F of a bank_map per-character table) is listed here,
# per tenant. `tests/test_shared_writes.sh` fails on any addition, removal or
# change.
#
# WHY THIS EXISTS. `tests/test_hui_ladder.sh` already requires every op to
# write free space or a variant row — but it runs stages 1-3, and the row that
# broke Bulleta was stage 4:
#
#     data_port df_palette_seq_rows -> vsavj 0x39ACC0, 0x80 bytes
#
# Those are palette-seq ids 0x1E-0x21, BULLETA'S Dark Force block. It shipped
# in 14z-69 and rendered a LEGACY character wrong for ten sessions, invisible
# to every RAM gate because the palette path never transits work RAM. Nothing
# failed, because the invariant that names that class stopped one stage short.
# `tools/audit_shared_writes.py` flags exactly that write on build/hui27 —
# the instrument's positive control.
#
# WHAT A GREEN RUN MEANS, precisely: the set of writes onto vanilla-readable
# bytes is UNCHANGED since it was last reviewed. It does NOT mean the writes
# are safe. An entry frozen without checking whose bytes it lands on stays
# wrong and green — which is what would have happened to the DF row. When this
# gate fails, the work is to establish WHOSE bytes the new write touches, not
# to re-freeze until it passes.
#
# THE CLASSES currently present (all three tenants):
#   * `site_thunk` engine sites — 6 bytes of `jmp`/`jsr` at a hooked engine
#     address. Justified per row in the manifest that declares them.
#   * select-wheel roster21: TABLE B cursor-navigation rows (3 new rows + 5
#     inbound edges from existing cells — required for the appended cells to
#     be reachable) and two march mid-row retargets. Gated by
#     `tests/test_select_wheel.sh`, which walks all 128 (cell,direction) pairs
#     in MAME.
#   * select record-array rows (0x2672xx/0x2675xx/0x268xxx) — the tenant's own
#     rows of 32-row arrays that bank_map does not declare, so the variant-row
#     check cannot see them. Gated by `tests/test_select_arrays.sh`.
#   * palette pointer-table variant halves (0x38c1d8 / 0x38c258) — row 0x10/
#     0x11 of the 32-row sprite/effect palette pointer tables. Gated by
#     `tests/test_effect_palette_table.sh`.
#   * `data_port` pointer-table rows (e.g. 0xbe2ba row 0x10) — a variant row of
#     a table bank_map does not declare.
#   * `code_word` / `obj_hook` engine words, each justified in its manifest row.
#
# Regenerate a tenant's list after a REVIEWED change:
#   python3 tools/audit_shared_writes.py build/<dir> | tail -n +2

[[tenant]]
name = "donovan"
build = "build/don_m18"
count = 114
writes = [
  # 14z-96 (GitHub #101) THE KERNEL VOICE-TABLE PORT, maintainer-ruled option
  # (a). REVIEWED 14z-128: these four words are THIS TENANT'S OWN variant-half
  # entry of the sound kernel's four per-class voice tables (vsavj bases
  # 0x3BCE / 0x3C3A / 0x3CA6 / 0x3D10, variant half at +0x20, one WORD per
  # class — so the address is base + 0x20 + 2*(id - 0x10): donovan 0x13 ->
  # +0x26, phobos 0x10 -> +0x20, pyron 0x11 -> +0x22, which is what the three
  # tenants' differing addresses are). Legacy classes are 0x00-0x0F and never
  # read the variant half. vsavj ships that half as byte-copies of the base
  # rows, which is exactly why a tenant class fired the LEGACY alias voice
  # (the merged-m2 electrocute grunt). `only_variant_slot` skips them on the
  # stock track, where the tenant sits at 0x0F and reads the legacy row
  # anyway. Locks: tests/test_kernel_voice_tables.sh (static),
  # tests/audit_hui_grunt.sh (the dynamic A/B over five electrocutions).
  "0x003bf4 2 code",
  "0x003c60 2 code",
  "0x003ccc 2 code",
  "0x003d36 2 code",
  # 14z-91 THE WALKER RELOCATION. REMOVED: "0x054470 6 code" and
  # "0x05e542 6 code" — the obj_hook dispatch sites are VANILLA again, so
  # the two engine-site writes that used to install `jmp thunk` are gone.
  # ADDED: the 23 rows below, each the 4-byte OPERAND of a `jsr <walker>`
  # at caller+2. REVIEWED: every one is a caller of vsavj's own object-pool
  # walker (0x54458 x2, 0x5E52A x21); the inventory is frozen in the
  # manifest's [[obj_hook]] rows, was enumerated BY REFERENCE FORM by
  # tools/audit_walker_callers.py (23 jsr abs.l and nothing else reaches
  # either walker), and the generator verifies each site's 6 vanilla bytes
  # against `caller_old_hex` before writing. The 4EB9 opcode word is never
  # touched. Dynamic completeness: tests/audit_walker_repoint.sh.
  "0x0053f8 4 code",
  "0x005412 4 code",
  "0x00577e 4 code",
  "0x0057aa 4 code",
  "0x00590c 4 code",
  "0x005ebe 4 code",
  "0x009438 4 code",
  "0x00943e 4 code",
  "0x009cac 4 code",
  "0x009f38 4 code",
  "0x00a18a 4 code",
  "0x00a806 4 code",
  "0x00abce 4 code",
  # 14z-87 (the voice-borrow fix, maintainer-decided b+c): the shared
  # engine borrow site (jsr + nop pad at 0x0AEF2/0x0AEF8 — the
  # keep-tenant thunk; only_variant_slot-gated, stock twin bit-identical)
  # and his two 0x40-byte candidate/voice-number table rows (variant rows
  # 0x13 of 0x00B268/0x00BB68, aliases today; read only when the OPPONENT
  # id is 0x10/0x11/0x13, which only tenants carry — reviewed superset-safe).
  "0x00aef2 6 code",
  "0x00aef8 2 code",
  "0x00b728 64 data",
  "0x00c028 64 data",
  "0x010dfc 4 code",
  "0x012a40 4 code",
  "0x012d18 4 code",
  "0x012e4e 4 code",
  "0x012e68 4 code",
  "0x018458 6 code",
  "0x0185ca 6 code",
  "0x020312 4 code",
  "0x020318 4 code",
  # 14z-105 (W1) THE OBORO SELECT HOOK. REVIEWED 14z-128: a [[site_thunk]] on
  # VANILLA code — the confirm-time `cmpi.b #$2,$382(a6)` at PRG:0x020B9C,
  # displaced and re-executed inside the thunk, so vanilla's own
  # Gallon-variant idiom one cell over (Gallon + START + punches -> Dark
  # Gallon 0x12) is preserved BY CONSTRUCTION. Profile-gated; no vanilla path
  # anywhere writes 0x18. Lock: tests/test_oboro_select.sh.
  "0x020b9c 6 code",
  # 14z-117 RANDOM SELECT INCLUDES THE TENANTS. REVIEWED 14z-128: two
  # profile-gated [[site_thunk]]s on vanilla select code — the random bound
  # and the roster draw table. Lock: tests/test_random_select_tenants.sh.
  "0x020c74 6 code",
  "0x020c80 6 code",
  # 0x01c586 / 0x01c59a REMOVED 14z-91: the fixture row-0x0F override thunks
  # are deleted (a legacy-regression root cause; those venue fixture-load
  # sites are shared by match intro AND attract, so legacy paid for them on
  # every venue load). Both sites are vanilla again.
  "0x021224 8 data",
  "0x02122c 8 data",
  "0x02123c 8 data",
  "0x021264 8 data",
  "0x02126c 8 data",
  "0x02127c 8 data",
  "0x02163a 4 code",
  "0x021adc 4 code",
  "0x021dec 4 code",
  "0x023ad8 6 code",
  "0x023ade 6 code",
  "0x0282fa 2 code",
  "0x028d4e 2 data",
  "0x028d50 4 data",
  "0x02a7c8 6 code",
  "0x02ad82 6 code",
  "0x02ad94 6 code",
  "0x02b342 6 code",
  "0x02b598 6 code",
  "0x02b7d8 6 code",
  "0x02b7e8 6 code",
  "0x05f146 6 code",
  "0x05f1b6 6 code",
  "0x05f24c 2 code",
  "0x05f24e 2 code",
  "0x05f328 6 code",
  "0x05f8b2 6 code",
  "0x05f9d0 6 code",
  "0x05fb22 4 code",
  "0x05fb26 4 code",
  "0x05fb2e 4 code",
  "0x05fce0 6 code",
  "0x06c0e0 6 code",
  "0x08459c 2 code",
  "0x0845ec 6 code",
  "0x0845f8 6 code",
  "0x084602 6 code",
  "0x084624 6 code",
  "0x0898aa 2 poke16",
  "0x08995c 4 poke32",
  "0x089960 4 poke32",
  # 14z-99 (GitHub #104) THE CAPTURE-POSE KEYFRAME BLOCKS, maintainer-ruled
  # option (a). REVIEWED 14z-128, and this is the group that most needed it:
  # rows 0x00-0x0F of the capture-keyframe pointer table 0x0BE27A[attacker]
  # are LEGACY attacker rows, plus row 0x18 (Oboro, which follows Bishamon).
  # The capture positioner (PRG:0x02802E) resolves the ATTACKER's block
  # through this table and then indexes its 32-word head by the VICTIM's id
  # UNMASKED; vsavj's blocks alias rows 0x10-0x1F onto 0x00-0x0F, so a TENANT
  # victim was held on the base character's sub-block (Donovan got Victor's,
  # Phobos got Bulleta's). Each row is repointed to that attacker's vs2 block,
  # ported VERBATIM into wide_ext.
  #
  # THE LEGACY-SAFETY PREMISE IS VERIFIED, not asserted:
  # tests/test_capture_pose_sources.sh section 5 — "every base sub-block
  # byte-identical vsavj vs vs2 (16/16)" — with its own must-fire control
  # (8b, a corrupted base sub-block IS detected). So a legacy attacker
  # capturing a legacy victim reads the same bytes it always did. Measured
  # legacy A/B at the 14z-99 rehearsal: EXACTLY ONE differing frame (f890,
  # the ratified §4 class-4 select-init pointer cache), bit-identical from
  # f891 through full matches including the grab-heavy legacy leg (replay 96
  # unpoked). `only_variant_slot` skips them on the stock track. Lock:
  # tests/audit_don_grab_pose.sh (EXPECT_MATCH=1).
  #
  # NINE OF THESE SEVENTEEN WERE INVISIBLE TO THIS AUDIT UNTIL 14z-128 (rows
  # 0x08-0x0F and 0x18): bank_map's `gap_be27a` is kind=auto carrying the
  # scanner's DEFAULT stride, so the variant-row exemption this tool computes
  # landed on those very rows. Fixed in tools/audit_shared_writes.py
  # variant_tables(); locked by tests/test_shared_writes.sh section 4.
  "0x0be27a 4 poke32",
  "0x0be27e 4 poke32",
  "0x0be282 4 poke32",
  "0x0be286 4 poke32",
  "0x0be28a 4 poke32",
  "0x0be28e 4 poke32",
  "0x0be292 4 poke32",
  "0x0be296 4 poke32",
  "0x0be29a 4 poke32",
  "0x0be29e 4 poke32",
  "0x0be2a2 4 poke32",
  "0x0be2a6 4 poke32",
  "0x0be2aa 4 poke32",
  "0x0be2ae 4 poke32",
  "0x0be2b2 4 poke32",
  "0x0be2b6 4 poke32",
  "0x0be2c6 4 poke32",
  "0x0be2da 4 poke32",
  "0x2672f6 4 poke32",
  "0x267376 4 poke32",
  "0x2673f6 4 poke32",
  "0x267476 4 poke32",
  "0x2674f6 4 poke32",
  "0x2675f6 4 poke32",
  "0x267676 4 poke32",
  "0x2689fe 4 poke32",
  "0x268a42 4 poke32",
  "0x268a46 4 poke32",
  "0x268a4e 4 poke32",
  "0x268ac2 4 poke32",
  "0x268ac6 4 poke32",
  "0x268ace 4 poke32",
  "0x268b42 4 poke32",
  "0x268b46 4 poke32",
  "0x268b4e 4 poke32",
  "0x38c1e4 4 poke32",
  "0x38c264 4 poke32",
  "0x3a3ac0 32 data",
  "0x3a3b20 32 data",
  "0x3a3b40 32 data",
]

[[tenant]]
name = "huitzil"
build = "build/hui52"
count = 112
writes = [
  # 14z-96 (GitHub #101) THE KERNEL VOICE-TABLE PORT, maintainer-ruled option
  # (a). REVIEWED 14z-128: these four words are THIS TENANT'S OWN variant-half
  # entry of the sound kernel's four per-class voice tables (vsavj bases
  # 0x3BCE / 0x3C3A / 0x3CA6 / 0x3D10, variant half at +0x20, one WORD per
  # class — so the address is base + 0x20 + 2*(id - 0x10): donovan 0x13 ->
  # +0x26, phobos 0x10 -> +0x20, pyron 0x11 -> +0x22, which is what the three
  # tenants' differing addresses are). Legacy classes are 0x00-0x0F and never
  # read the variant half. vsavj ships that half as byte-copies of the base
  # rows, which is exactly why a tenant class fired the LEGACY alias voice
  # (the merged-m2 electrocute grunt). `only_variant_slot` skips them on the
  # stock track, where the tenant sits at 0x0F and reads the legacy row
  # anyway. Locks: tests/test_kernel_voice_tables.sh (static),
  # tests/audit_hui_grunt.sh (the dynamic A/B over five electrocutions).
  "0x003bee 2 code",
  "0x003c5a 2 code",
  "0x003cc6 2 code",
  "0x003d30 2 code",
  # 14z-91 THE WALKER RELOCATION. REMOVED: "0x054470 6 code" and
  # "0x05e542 6 code" — the obj_hook dispatch sites are VANILLA again, so
  # the two engine-site writes that used to install `jmp thunk` are gone.
  # ADDED: the 23 rows below, each the 4-byte OPERAND of a `jsr <walker>`
  # at caller+2. REVIEWED: every one is a caller of vsavj's own object-pool
  # walker (0x54458 x2, 0x5E52A x21); the inventory is frozen in the
  # manifest's [[obj_hook]] rows, was enumerated BY REFERENCE FORM by
  # tools/audit_walker_callers.py (23 jsr abs.l and nothing else reaches
  # either walker), and the generator verifies each site's 6 vanilla bytes
  # against `caller_old_hex` before writing. The 4EB9 opcode word is never
  # touched. Dynamic completeness: tests/audit_walker_repoint.sh.
  "0x0053f8 4 code",
  "0x005412 4 code",
  "0x00577e 4 code",
  "0x0057aa 4 code",
  "0x00590c 4 code",
  "0x005ebe 4 code",
  "0x009438 4 code",
  "0x00943e 4 code",
  "0x009cac 4 code",
  "0x009f38 4 code",
  "0x00a18a 4 code",
  "0x00a806 4 code",
  "0x00abce 4 code",
  # 14z-87: the voice-borrow site + his candidate/voice-number rows
  # (variant rows 0x10) — see the donovan section's review note.
  "0x00aef2 6 code",
  "0x00aef8 2 code",
  "0x00b668 64 data",
  "0x00bf68 64 data",
  "0x010dfc 4 code",
  "0x012a40 4 code",
  "0x012d18 4 code",
  "0x012e4e 4 code",
  "0x012e68 4 code",
  "0x018460 6 code",
  # 14z-84 BACKFILL: the hitclass_map_extend site (the ADOPTED 14z-82c
  # fix, maintainer decision 1 — vsavj's 64-entry projectile hit-class
  # map extended to vs2's 80). Shipped in huitzil-m4 and -m5 but this
  # inventory was never regenerated past hui29; its review IS the
  # adoption record (tests/test_hitclass_map_thunk.sh reconstructs the
  # body from the ROMs).
  "0x01a888 6 code",
  "0x01b6aa 6 code",
  "0x020312 4 code",
  "0x020318 4 code",
  # 14z-105 (W1) THE OBORO SELECT HOOK. REVIEWED 14z-128: a [[site_thunk]] on
  # VANILLA code — the confirm-time `cmpi.b #$2,$382(a6)` at PRG:0x020B9C,
  # displaced and re-executed inside the thunk, so vanilla's own
  # Gallon-variant idiom one cell over (Gallon + START + punches -> Dark
  # Gallon 0x12) is preserved BY CONSTRUCTION. Profile-gated; no vanilla path
  # anywhere writes 0x18. Lock: tests/test_oboro_select.sh.
  "0x020b9c 6 code",
  # 14z-117 RANDOM SELECT INCLUDES THE TENANTS. REVIEWED 14z-128: two
  # profile-gated [[site_thunk]]s on vanilla select code — the random bound
  # and the roster draw table. Lock: tests/test_random_select_tenants.sh.
  "0x020c74 6 code",
  "0x020c80 6 code",
  "0x021224 8 data",
  "0x02122c 8 data",
  "0x02123c 8 data",
  "0x021264 8 data",
  "0x02126c 8 data",
  "0x02127c 8 data",
  "0x02163a 4 code",
  "0x021adc 4 code",
  "0x021dec 4 code",
  "0x022a0e 6 code",
  "0x0282f4 2 code",
  "0x028386 6 code",
  "0x028d4e 2 poke16",
  "0x028d50 2 poke16",
  "0x028d52 2 poke16",
  # 14z-84 REVIEWED: the DF gold rows (Phobos' own block, maintainer-
  # ratified design). 0x2a8c4 = table 0x02A8A4 row 0x10's word (the
  # VARIANT row — legacy never carries an id in 0x10-0x1F,
  # tests/audit_id_writers.sh) repointed at the trampoline; 0x2a8d6 =
  # rows 0x19-0x1B's alias words (indexable only by ids 0x19-0x1B,
  # which cannot occur: id guards + roster) carrying the 6-byte jmp to
  # the df_gold_variant_id thunk. Both READ-reachable only via ids that
  # do not exist on any track.
  "0x02a8c4 2 code",
  "0x02a8d6 6 code",
  "0x02b598 6 code",
  "0x02b7d8 6 code",
  "0x05e7d6 6 code",
  "0x05f146 6 code",
  "0x05f1b6 6 code",
  "0x05f240 2 code",
  "0x05f242 2 code",
  "0x05f328 6 code",
  "0x05f8b2 6 code",
  "0x05fb22 4 code",
  "0x05fb26 4 code",
  "0x05fb2e 4 code",
  "0x05fce0 6 code",
  "0x060ef0 6 code",
  "0x06c0e0 6 code",
  "0x080aec 4 code",
  # 14z-102 (GitHub #109) THE DARK FORCE CLONE-MODE BEAM EMITTER. REVIEWED
  # 14z-128: a [[code_ptr]] into effect-class ROW 31, which vsav ships as a
  # STUB (0x00080B44) while vs2 and vh2 both fill it. The deadness claim is
  # measured, not assumed: 0 reads of slot PRG:0x080B28 across replays
  # 02/07/09/30 AND both long mash marathons (21_don_mash, 26_don_arcade_mash
  # — the two that broke the type-6 deadness claim at 14z-89, included on
  # purpose), against a 2418-hit row-0 control on the same instrument, and
  # `old_hex` pins the slot to the stub so a moved table fails the build.
  # Locks: tests/audit_effect_class_rows.sh (the deadness),
  # tests/audit_clone_beam_lines.sh (the behaviour, burst-control must-fire).
  "0x080b28 4 code",
  "0x08245c 6 code",
  "0x0898a4 2 poke16",
  "0x089944 4 poke32",
  "0x089948 4 poke32",
  # 14z-99 (GitHub #104) THE CAPTURE-POSE KEYFRAME BLOCKS, maintainer-ruled
  # option (a). REVIEWED 14z-128, and this is the group that most needed it:
  # rows 0x00-0x0F of the capture-keyframe pointer table 0x0BE27A[attacker]
  # are LEGACY attacker rows, plus row 0x18 (Oboro, which follows Bishamon).
  # The capture positioner (PRG:0x02802E) resolves the ATTACKER's block
  # through this table and then indexes its 32-word head by the VICTIM's id
  # UNMASKED; vsavj's blocks alias rows 0x10-0x1F onto 0x00-0x0F, so a TENANT
  # victim was held on the base character's sub-block (Donovan got Victor's,
  # Phobos got Bulleta's). Each row is repointed to that attacker's vs2 block,
  # ported VERBATIM into wide_ext.
  #
  # THE LEGACY-SAFETY PREMISE IS VERIFIED, not asserted:
  # tests/test_capture_pose_sources.sh section 5 — "every base sub-block
  # byte-identical vsavj vs vs2 (16/16)" — with its own must-fire control
  # (8b, a corrupted base sub-block IS detected). So a legacy attacker
  # capturing a legacy victim reads the same bytes it always did. Measured
  # legacy A/B at the 14z-99 rehearsal: EXACTLY ONE differing frame (f890,
  # the ratified §4 class-4 select-init pointer cache), bit-identical from
  # f891 through full matches including the grab-heavy legacy leg (replay 96
  # unpoked). `only_variant_slot` skips them on the stock track. Lock:
  # tests/audit_don_grab_pose.sh (EXPECT_MATCH=1).
  #
  # NINE OF THESE SEVENTEEN WERE INVISIBLE TO THIS AUDIT UNTIL 14z-128 (rows
  # 0x08-0x0F and 0x18): bank_map's `gap_be27a` is kind=auto carrying the
  # scanner's DEFAULT stride, so the variant-row exemption this tool computes
  # landed on those very rows. Fixed in tools/audit_shared_writes.py
  # variant_tables(); locked by tests/test_shared_writes.sh section 4.
  "0x0be27a 4 poke32",
  "0x0be27e 4 poke32",
  "0x0be282 4 poke32",
  "0x0be286 4 poke32",
  "0x0be28a 4 poke32",
  "0x0be28e 4 poke32",
  "0x0be292 4 poke32",
  "0x0be296 4 poke32",
  "0x0be29a 4 poke32",
  "0x0be29e 4 poke32",
  "0x0be2a2 4 poke32",
  "0x0be2a6 4 poke32",
  "0x0be2aa 4 poke32",
  "0x0be2ae 4 poke32",
  "0x0be2b2 4 poke32",
  "0x0be2b6 4 poke32",
  "0x0be2ba 4 poke32",
  "0x0be2da 4 poke32",
  "0x2672ea 4 poke32",
  "0x26736a 4 poke32",
  "0x2673ea 4 poke32",
  "0x26746a 4 poke32",
  "0x2674ea 4 poke32",
  "0x2675ea 4 poke32",
  "0x26766a 4 poke32",
  "0x2689fe 4 poke32",
  "0x268a42 4 poke32",
  "0x268a46 4 poke32",
  "0x268a4e 4 poke32",
  "0x268ac2 4 poke32",
  "0x268ac6 4 poke32",
  "0x268ace 4 poke32",
  "0x268b42 4 poke32",
  "0x268b46 4 poke32",
  "0x268b4e 4 poke32",
  "0x38c1d8 4 poke32",
  "0x38c258 4 poke32",
  "0x3a3ac0 32 data",
  "0x3a3b20 32 data",
  "0x3a3b40 32 data",
]

[[tenant]]
name = "pyron"
build = "build/pyron36"
count = 100
writes = [
  # 14z-96 (GitHub #101) THE KERNEL VOICE-TABLE PORT, maintainer-ruled option
  # (a). REVIEWED 14z-128: these four words are THIS TENANT'S OWN variant-half
  # entry of the sound kernel's four per-class voice tables (vsavj bases
  # 0x3BCE / 0x3C3A / 0x3CA6 / 0x3D10, variant half at +0x20, one WORD per
  # class — so the address is base + 0x20 + 2*(id - 0x10): donovan 0x13 ->
  # +0x26, phobos 0x10 -> +0x20, pyron 0x11 -> +0x22, which is what the three
  # tenants' differing addresses are). Legacy classes are 0x00-0x0F and never
  # read the variant half. vsavj ships that half as byte-copies of the base
  # rows, which is exactly why a tenant class fired the LEGACY alias voice
  # (the merged-m2 electrocute grunt). `only_variant_slot` skips them on the
  # stock track, where the tenant sits at 0x0F and reads the legacy row
  # anyway. Locks: tests/test_kernel_voice_tables.sh (static),
  # tests/audit_hui_grunt.sh (the dynamic A/B over five electrocutions).
  "0x003bf0 2 code",
  "0x003c5c 2 code",
  "0x003cc8 2 code",
  "0x003d32 2 code",
  # 14z-91 THE WALKER RELOCATION. REMOVED: "0x054470 6 code" and
  # "0x05e542 6 code" — the obj_hook dispatch sites are VANILLA again, so
  # the two engine-site writes that used to install `jmp thunk` are gone.
  # ADDED: the 23 rows below, each the 4-byte OPERAND of a `jsr <walker>`
  # at caller+2. REVIEWED: every one is a caller of vsavj's own object-pool
  # walker (0x54458 x2, 0x5E52A x21); the inventory is frozen in the
  # manifest's [[obj_hook]] rows, was enumerated BY REFERENCE FORM by
  # tools/audit_walker_callers.py (23 jsr abs.l and nothing else reaches
  # either walker), and the generator verifies each site's 6 vanilla bytes
  # against `caller_old_hex` before writing. The 4EB9 opcode word is never
  # touched. Dynamic completeness: tests/audit_walker_repoint.sh.
  "0x0053f8 4 code",
  "0x005412 4 code",
  "0x00577e 4 code",
  "0x0057aa 4 code",
  "0x00590c 4 code",
  "0x005ebe 4 code",
  "0x009438 4 code",
  "0x00943e 4 code",
  "0x009cac 4 code",
  "0x009f38 4 code",
  "0x00a18a 4 code",
  "0x00a806 4 code",
  "0x00abce 4 code",
  # 14z-87: the voice-borrow site + his candidate/voice-number rows
  # (variant rows 0x11) — see the donovan section's review note.
  "0x00aef2 6 code",
  "0x00aef8 2 code",
  "0x00b6a8 64 data",
  "0x00bfa8 64 data",
  "0x010dfc 4 code",
  "0x012a40 4 code",
  "0x012d18 4 code",
  "0x012e4e 4 code",
  "0x012e68 4 code",
  # 14z-84 BACKFILL: the hitclass_map_extend site (ADOPTED 14z-82c, the
  # f7997 fix) — shipped in pyron-m3 but this inventory was never
  # regenerated past pyron20. Review = the adoption record.
  "0x01a888 6 code",
  "0x020312 4 code",
  "0x020318 4 code",
  # 14z-105 (W1) THE OBORO SELECT HOOK. REVIEWED 14z-128: a [[site_thunk]] on
  # VANILLA code — the confirm-time `cmpi.b #$2,$382(a6)` at PRG:0x020B9C,
  # displaced and re-executed inside the thunk, so vanilla's own
  # Gallon-variant idiom one cell over (Gallon + START + punches -> Dark
  # Gallon 0x12) is preserved BY CONSTRUCTION. Profile-gated; no vanilla path
  # anywhere writes 0x18. Lock: tests/test_oboro_select.sh.
  "0x020b9c 6 code",
  # 14z-117 RANDOM SELECT INCLUDES THE TENANTS. REVIEWED 14z-128: two
  # profile-gated [[site_thunk]]s on vanilla select code — the random bound
  # and the roster draw table. Lock: tests/test_random_select_tenants.sh.
  "0x020c74 6 code",
  "0x020c80 6 code",
  "0x021224 8 data",
  "0x02122c 8 data",
  "0x02123c 8 data",
  "0x021264 8 data",
  "0x02126c 8 data",
  "0x02127c 8 data",
  "0x02163a 4 code",
  "0x021adc 4 code",
  "0x021dec 4 code",
  "0x0282f6 2 code",
  "0x02a8c6 2 code",
  "0x02b598 6 code",
  "0x02b672 2 code",
  "0x02b7d8 6 code",
  "0x05f146 6 code",
  "0x05f1b6 6 code",
  "0x05f244 2 code",
  "0x05f246 2 code",
  "0x05f328 6 code",
  "0x05f8b2 6 code",
  "0x05fb22 4 code",
  "0x05fb26 4 code",
  "0x05fb2e 4 code",
  "0x05fce0 6 code",
  "0x06c0e0 6 code",
  "0x0737b2 2 code",
  "0x0898a6 2 poke16",
  "0x08994c 4 poke32",
  "0x089950 4 poke32",
  # 14z-99 (GitHub #104) THE CAPTURE-POSE KEYFRAME BLOCKS, maintainer-ruled
  # option (a). REVIEWED 14z-128, and this is the group that most needed it:
  # rows 0x00-0x0F of the capture-keyframe pointer table 0x0BE27A[attacker]
  # are LEGACY attacker rows, plus row 0x18 (Oboro, which follows Bishamon).
  # The capture positioner (PRG:0x02802E) resolves the ATTACKER's block
  # through this table and then indexes its 32-word head by the VICTIM's id
  # UNMASKED; vsavj's blocks alias rows 0x10-0x1F onto 0x00-0x0F, so a TENANT
  # victim was held on the base character's sub-block (Donovan got Victor's,
  # Phobos got Bulleta's). Each row is repointed to that attacker's vs2 block,
  # ported VERBATIM into wide_ext.
  #
  # THE LEGACY-SAFETY PREMISE IS VERIFIED, not asserted:
  # tests/test_capture_pose_sources.sh section 5 — "every base sub-block
  # byte-identical vsavj vs vs2 (16/16)" — with its own must-fire control
  # (8b, a corrupted base sub-block IS detected). So a legacy attacker
  # capturing a legacy victim reads the same bytes it always did. Measured
  # legacy A/B at the 14z-99 rehearsal: EXACTLY ONE differing frame (f890,
  # the ratified §4 class-4 select-init pointer cache), bit-identical from
  # f891 through full matches including the grab-heavy legacy leg (replay 96
  # unpoked). `only_variant_slot` skips them on the stock track. Lock:
  # tests/audit_don_grab_pose.sh (EXPECT_MATCH=1).
  #
  # NINE OF THESE SEVENTEEN WERE INVISIBLE TO THIS AUDIT UNTIL 14z-128 (rows
  # 0x08-0x0F and 0x18): bank_map's `gap_be27a` is kind=auto carrying the
  # scanner's DEFAULT stride, so the variant-row exemption this tool computes
  # landed on those very rows. Fixed in tools/audit_shared_writes.py
  # variant_tables(); locked by tests/test_shared_writes.sh section 4.
  "0x0be27a 4 poke32",
  "0x0be27e 4 poke32",
  "0x0be282 4 poke32",
  "0x0be286 4 poke32",
  "0x0be28a 4 poke32",
  "0x0be28e 4 poke32",
  "0x0be292 4 poke32",
  "0x0be296 4 poke32",
  "0x0be29a 4 poke32",
  "0x0be29e 4 poke32",
  "0x0be2a2 4 poke32",
  "0x0be2a6 4 poke32",
  "0x0be2aa 4 poke32",
  "0x0be2ae 4 poke32",
  "0x0be2b2 4 poke32",
  "0x0be2b6 4 poke32",
  "0x0be2da 4 poke32",
  "0x2672ee 4 poke32",
  "0x26736e 4 poke32",
  "0x2673ee 4 poke32",
  "0x26746e 4 poke32",
  "0x2674ee 4 poke32",
  "0x2675ee 4 poke32",
  "0x26766e 4 poke32",
  "0x2689fe 4 poke32",
  "0x268a42 4 poke32",
  "0x268a46 4 poke32",
  "0x268a4e 4 poke32",
  "0x268ac2 4 poke32",
  "0x268ac6 4 poke32",
  "0x268ace 4 poke32",
  "0x268b42 4 poke32",
  "0x268b46 4 poke32",
  "0x268b4e 4 poke32",
  "0x38c1dc 4 poke32",
  "0x38c25c 4 poke32",
  "0x3a3ac0 32 data",
  "0x3a3b20 32 data",
  "0x3a3b40 32 data",
]

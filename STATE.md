# STATE — living progress log

**SPLIT 2026-08-20 (14z-99 post-freeze close, maintainer-approved), AMENDED
2026-09-14 (CLAUDE.md [VSP-17] and [VSP-182]): this file holds the RECENT
session groups and the STANDING SECTIONS; the full detail of every older
session lives verbatim in `STATE_HISTORY.md`, under THE LEDGER at its head.**
How to work with it:
- **Lookup**: "STATE 14z-XX" references resolve here first, then in
  STATE_HISTORY.md (its LEDGER first) — section names are preserved verbatim
  in the archive. A reference to `STATE "Decisions pending"` for an entry no
  longer here resolves in `DECISIONS_HISTORY.md` (entries move there verbatim
  when ruled — since 2026-09-14; before that, once they stopped shaping work).
- **Claim-greps MUST include STATE_HISTORY.md** (the CLAUDE.md §5
  retraction-discipline command names it).
- **AN OPEN LIST HOLDS ONLY WHAT IS OPEN (maintainer-ruled 2026-09-14).**
  "Decisions pending" holds undecided items only; nothing closed stays in it,
  however it is marked (struck through, DONE, FIXED, CLOSED). Where each thing
  goes instead:
  - a RULED decision -> its whole entry moves VERBATIM to
    `DECISIONS_HISTORY.md` in the commit that records the ruling; while the
    ruling still constrains work, ONE line stating the rule sits under
    "Standing rulings" here, and the line is deleted when it stops;
  - a BUG, COSMETIC ITEM or EVOLUTION is a TICKET — listed in
    `docs/project/tickets.tsv`, its story on its GitHub issue, never listed
    here; a STATE entry for one that closes moves VERBATIM to
    `STATE_HISTORY.md` under the closing session's key (CLAUDE.md [VSP-182]:
    the four questions, the closing comment);
  - "STANDING PRINCIPLE" and "THE DEADNESS REGISTER" carry the [VSP-21..23]
    anchors and stay; the register keeps its withdrawn rows by [VSP-23].
  `tests/test_state_open_lists.sh` enforces it, with the size budget and the
  group count below.
- **ROLLOVER RULE (part of the session-close ritual)**: after writing the
  close entry, move session groups beyond the newest THREE to the TOP of
  STATE_HISTORY.md's body in newest-first order (below THE LEDGER, above the first
  archived `## Session` heading OLDER than the group)
  and add their one-line entries at the top of THE LEDGER there, composed from
  the group's own banner headers. If this file still exceeds ~150 KB, roll the
  oldest kept group early. The standing sections never roll.
- **THE CLOSE CHECKLIST (ruled 2026-09-18, 14z-167b; `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-167b)")**, after the close entry
  and before the tier: (1) a FINDINGS TABLE — every finding of the group, its live home and the test that
  reproduces it; (2) `python3 tools/close_findings.py <session>` — every GAP homed, every REVIEW answered
  by a home or an open item (never by reclassifying the finding), every PROMISE fulfilled or made an open
  item; its clean result is never cited as completeness; (3) a pointer on every earlier row the sitting
  resolves; (4) the promises (the tool's PROMISES section); (5) scratch scripts whose figures a document
  or STATE row quotes are promoted into `tools/` or `tests/` — ON TRIAL: record how many and how many
  were promoted; (6) the DOCUMENTATION PACKET through the rule-checker (`recommendation`, the table as
  the claim), its verdict resolved like any run.
- **THE LAST STEP OF THE CLOSE IS THE PUSH (maintainer-ruled 2026-09-10):**
  static tier strict green WITH every control executed (the default `all`), the doc checks green, nothing pending -> `git push
  origin main`; anything red or skipped leaves the commits local and the
  close entry says so.

## Session 14z-170 — **M19 FROZEN: THE FOUR RULED #136 FIXES (no gauge in the tenants' Dark Force; vs2's defense rows for Phobos and Donovan; the vs2 EX inputs do the no-stock move, Pyron's read naturally; the class-0x52 rule for the column and — on merged — the trap, scope S1) and a latent generator corruption the attribution of their delta exposed (52 x2b7ef4 records shipped since merged-m16; none read by any naming part) — donovan-m23 / huitzil-m30 / pyron-m24 / merged-m19, the combined overhead adding no frame. The freeze battery GREEN (159/159) after every red was attributed; Phobos's remaining +1 damage ruled "Freeze, ticket it"; the continue-switch rig re-authored as ruled. Two of the sitting's own readings were wrong and are corrected — the orange flash's writer and the x2b7ef4 reachability, both from a -debug watch's frame column ([CPE-5], paid twice). Thirty-one rule-checker runs (2026-09-19-54..84), every plant caught and every real finding true, resolved by work — the freeze check reading OK at run 76, the documentation packet's close runs 77-80, the close tier's re-freeze runs 81-84; six gates new, ten reworked.**

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, `docs/NEXT_SESSION.md`), `main` == `origin/main` at `a3619798`, bbh clean at `caee272`, `emu/fbneo` dirty by exactly the two tracked patches (841/4), nothing running, `gh` as `mechanyaa-ai`; ROM audit **76/76**; `build/rc169/fix1_gauge.patch` applies cleanly. The maintainer: *"proceed in the order you deem best"*. The standing line of the four-fixes ruling corrected ("each its own commit" cannot hold) |
| **(1) THE FOUR FIXES, EACH MEASURED AS A PROGRAM DELTA** | `build/rc170/progdelta.sh` (scratch: the merged generator + `patch_prg`, diffed against merged-m18's program image; control: HEAD's manifests reproduce merged-m18's image, 831 ops, 0 bytes). Defense rows: four `[[data_port]]` rows (Phobos in `huitzil.toml`, Donovan in `donovan.toml`; the curve row, and the threshold as a WORD — `patch_prg` writes words — with the neighbour byte asserted unchanged by a same-value `fixes` entry: Pyron's 0x11, and 0x12, the legacy-reachable Dark Gallon id, both 0x30 in vsavj, vs2 and vh2), `orc` against vh2's copies (`0x0D2350`/`0x0D66B0`): exactly 66 bytes. Gauge: `fix1_gauge.patch` applied: +3 adder tests (one in the crypt range, a 2-byte stored change). EX route: one `[[port_patch]]` per tenant, the `bne.w` after `tst.b $111` -> `bra.w` (Phobos `code` 0x05553C, Pyron `code` 0x057CCC, Donovan `code` 0x059D28): +3 sites. 0x52 rule (S1): `reaction_hook` `case_a4` writes the 0x38 marker; the two 14z-42 thunks test it (victim: inside the Donovan branch only, zero legacy cost; attacker: both branches, ~24 cycles on a grounded electric hit); `es_type51_dispatch` routes 0x52 to vsavj's KO handler `0x0186E0` (= vs2's KO[0x52] = KO[0x06], read); the column's three remaps retired (stage 99); Phobos's two trap remaps carry the new row key `unless_composed = "donovan"` (the generator: `composition_allows()` in `tenant_rows()`, the vocabulary derived by `port_tenant_names()`; new gate `tests/test_unless_composed.sh`, ci_portable, control `typo-accepted`, mode exits 1). The grown thunks shift 27 placed regions by exactly +0x30 (51 unmoved, sources and lengths identical): a pure allocator cascade, 247,070 bytes of relocated pointers. Ops 831 -> 835 (the four data_port rows); `tests/test_tenant_loop.sh` re-frozen (D 344, H 375, P 312, N=2 622, N=3 835) |
| **(2) THE 14z-169 EX-ROUTE DESIGN WAS MISLABELLED** | NEXT_SESSION said "force the placed Change entry's refusal (vs2 `0x2617A`)". Read: `0x2617A` is the "already in Power" test of vs2's POWER activation `0x026166` (sets `+0x1C3`, two stocks; reached in vs2 only by the two `bsr.w` at `0x020D8E/0x020DD2`, caller scan; no placed reference to its three copies on either build), not of the Change entry `0x02622A`, which is a plain setter reached by `jmp` from 14 per-character EX sites. The EX site's gate is `0x028534` (the stock tested before the input). Carriers to correct: `engine_internals.md` (the 23-readers paragraph), `tests/audit_df_modes.sh`'s header, NEXT_SESSION; STATE 14z-169 row (4) marked in place |
| **(3) VERIFIED IN-EMULATOR ON `build/fix170_all`** (`build/rc170/v1/`, `v2/`) | gauge: the tenants gain nothing in the mode (`audit_df_meter`: the six in-mode steps gone; its control `suppressed` now DEAD by construction). Column: victim freeze 24 at both hits, no attacker write, the whole timeline identical to native (hits 2838/2845, Donovan neutral 2859, Demitri 2885, gap 26 — was 14); the gate's mechanism check and control `native-shock` were written against the defect. Column KO (scratch `build/rc170/ko/`, HP poked to 1, both words): the KO at 2838 takes class 8 on native, fixed and merged-m18 alike, traces identical, no exception — the extended KO thunk path. Trap (scratch copy of `audit_trap_shock`, merged): class 0x38 -> the shock, the victim's freeze from 0x18, NO attacker freeze (merged-m18: Phobos 0x09 -> 0) — and the dome connects at f3500 on ours vs f3494 native on BOTH merged-m18 and the fix: pre-existing, unattributed. **[ATTRIBUTED 14z-170, row (7): the rig pinned neither the speed level nor the RNG (vs2 TURBO, vsavj NORMAL); pinned, native and the fix are frame-for-frame identical]** EX route: Phobos and Donovan at stock 3 take vs2's stock-0 path exactly; Pyron at stock 3 falls through to 623+PP, the ES move (one stock) — capture put before the maintainer (`build/rc170/cap_pyr/`), RULED *"Natural reading (Recommended)"* (`DECISIONS_HISTORY.md` "Ruled 2026-09-19 (14z-170)"). Defense: the fixed build answers vs2's bytes (row 0x10 -> 2, 0x13 -> 0; thresholds 40/48 HP). Lag: `audit_pass_overrun` PASS, every row as frozen — no new zero-pass frame over the #136 corpus |
| **(4) AN INSTRUMENT DEFECT: `audit_defense_row_residue` READ OURS FROM PRISTINE vsavj** | its "bytes answered" for ours were a lookup in `build/out/vsavj_data.bin`, so after the fix it still reported the old bytes ([VSP-147]). Fixed: ours from the build's own `verify_data.bin`, residue = the BUILD's row differs from vs2's; on merged-m18 residue 0x10/0x13/0x0A, on the fix 0x0A only (Sasquatch, vanilla vsavj's own); the control moved to 0x0A (a residue on every build); PASS on both builds, the mode exits 1 |
| **(5) A LATENT CORRUPTION IN EVERY SHIPPED DONOVAN TRACK SINCE AT LEAST merged-m16 — FOUND BY ATTRIBUTING THE FIXES' DELTA** | the four-fix image attributed op by op against merged-m18 (`build/rc170/delta_ops.py`, `delta_ops_step1.txt`/`step2.txt`): every difference is a designed edit or a uniform +0x30 relocation (24-bit tagged pointers and one-past-the-end bounds included) — except THREE records in Pyron's `x2b7ef4` copy. Root cause: the generator's `x2b7ef4` companion-effect pass resolved its `0xEE`-tagged coordinate-list placeholders by an IN-PLACE scan of every even offset, so a resolved pointer whose low word begins `0xEE` was re-read as a placeholder and rewritten — the pointer to an ODD address, the next word clobbered (the [VSP-68] class). The fix's +0x30 shift re-rolled three such sites into Pyron's copy — and the fixed resolver (`resolve_tagged_placeholders()`, recorded offsets only) showed HEAD's own manifests no longer reproduce merged-m18: **merged-m18 ships 52 corrupted sites** (Donovan's copy at `PRG:0x0F3F70`: 22 intra-region pointers and 18 coordinate-list pointers turned odd, each with its next word clobbered; Pyron's copy: 12 coordinate-list pointers, in records the generator's own comment says Pyron never draws). The 22-site class is identical in merged-m16 (released 14z-134), -m17, -m18, `don_m22` and the stock twin; Phobos's and Pyron's solo copies are clean. A record reached through one of them would read an odd address (an address error, a reboot on the board); no tracked recording or corpus run has ever raised one. Seen and not followed: the generated `docs/project/tables/chars/donovan.md` has reported "relocated ok 2032 / bad 33" for this region. New gate `tests/test_effect_placeholders.sh` (ci_portable, control `in-place-scan` — the old scan reproduces merged-m18's shipped bytes exactly, the mode exits 1). Which effect draws the corrupted records is NOT established. The maintainer ruled M19 supersedes M18 and the reachability measured first (`DECISIONS_HISTORY.md`); measured (`build/rc170/reach/`, a `-debug` read watch on merged-m18): no Donovan naming part (14, his movelist) and no Pyron part (6) reads a corrupted span in a match — all 53,924 in-match reads of Donovan's window fall in one other 1 KB block, attributed by an address register (the heuristic's positive control: 100%) — **the Donovan half of that measurement was VOID and is RE-MEASURED (row (13)):** his watch stopped thousands of times per match and desynced the replays ([CPE-5]); `tests/audit_x2b7ef4_reach_m18.sh` (the corrupted bytes only, armed at 2000, every run's node trajectory checked against a non-debug run) confirms none read |
| **(6) THE CORRECTED BUILD, AND THE FOUR GATES WHOSE PREMISE THE FIXES CHANGED** | `build/fix170_b` (fingerprint `681ac3ad`, the resolver fix included; its patch is op-for-op the attributed image): every verification re-run and identical to `fix170_all`'s (ex_refused, column, meter, pass-overrun rows, the residue gate, the trap, the KO trace). Reworked, each ground-truthed BOTH ways — PASS on the fix, FAIL on merged-m18 with the real defect named, its mode exiting 1: `audit_ex_refused` (the stocked legs: none of ours enters; Phobos's and Donovan's paths equal vs2's stock-0; control on vs2's stocked legs), `audit_column_shock` (ours' +0x5C writes equal native's in fighter, frame and value; the timeline equal; NEW section 1b, the column KO promoted from `build/rc170/ko/`; control `old-shock`), `audit_df_meter` (no in-mode gauge on any leg of ours, liveness by the out-of-mode swings; control `swing-planted`), `audit_trap_shock` (THREE legs: native, the merged build — class 0x38, attacker exempt — and the solo Phobos build, which keeps the remap and its freeze under S1; its first declared control `attacker-frozen`). Their frozen tables re-freeze at the M19 freeze. The M19 build names (`don_m23`, `hui57`, `pyron42`, `m5_stock18`, `m3b_merged27`) are TAKEN by the withdrawn 14z-159 freeze's builds and 14z-169's scratch — to be moved aside, not overwritten |
| **(7) THE RULE-CHECKER ON THE CHANGE — RUN 54: VIOLATED Q1 Q2 Q4, EVERY FINDING TRUE, RESOLVED BY WORK** | run `2026-09-19-54` (`build`, `--model claude-opus-5`; plant `forced-pick-14z159` CAUGHT): **Q3 Q5 OK; Q1 Q2 Q4 VIOLATED**, verbatim in `tests/rulecheck/runs/2026-09-19-54/`. Q1: op 806 unattributed, and the rally threshold measured by no leg. Resolved: `build/rc170/delta_ops.py` gained two named rules (a one-past-the-end bound; a biased base, the win-palette thunk's `a0 = block - 0xA00`) and `build/rc170/delta_edits_check.py` checks every remaining edit byte for byte against the designed bytes — and showed the claim's "uniform +0x30" wrong in wording: allocations at hole boundaries reshuffle between holes (the slot-clearing alloc wrapper `0x3FFFC0` -> `0x46A630`, content identical), which the op pairing attributes; `tests/audit_defense_row_residue.sh` gained THRESHOLD legs through `trace_writes.lua`'s new opt-in `REGS_EXTRA` (unset: output cmp-identical to HEAD's — `build/rc170/twcheck/`): the threshold read writes its byte into its own index register, so `D5` at the hit is the byte ANSWERED, live — the fix answers Phobos 40 HP and Donovan 48 HP as native, merged-m18 56 and 40. Q2: no capture of the fixed build had been put before the maintainer. Resolved: sheets of the column, the trap and Phobos's and Donovan's EX inputs (`build/rc170/cap_fix/`); the maintainer: column — *"The fix is mechanically perfect"*; *"Phobos_263PP_after_fix -> clean"*; *"Donovan_421KK_after_fix -> clean"*; the trap: *"I can't rule for now because the move does not just seem to end at a different frame, it seems it starts at a different frame so I would need more frames from the beginning of the move until the activation of the trap and frames until the end of recovery, on all builds"* — measured: rig 92 pinned neither the speed level nor the RNG (vs2 TURBO, vsavj NORMAL), so Phobos's air trap ran 2..6 frames slower on ours; PINNED, native and the fix are FRAME-FOR-FRAME identical 3395-3600 (`build/rc170/trapt_pinned/`), and merged-m18 differs only by Phobos's 11-frame hit-stop — the gate now pins and asserts that timeline; re-shot pinned, the maintainer: *"Yes, as expected"*. The orange flash on the deity (column, f2860): *"cosmetic but strange ... why it happens would answer whether it's a minor side effect or something that could have impacts elsewhere"* — measured: merged-m18 shows it (at 2866, its whole move 6 frames later), so the fix did not cause it; the OBJ lists at 2858/2860 are identical in palette usage, and palette ROW 11 differs — ours is loaded with a fire (orange-red) ramp as the move ends where native keeps its blue ramp through 2866, ~~by the engine's own staging copy (vsavj `0x01433E` / vs2 `0x0129B6`, same frames, 666 writes each) from different staged content; which effect stages it is not established~~ **RETRACTED later the same sitting (row (12)):** that reading took a `-debug` watch's per-hit counter for the frame. A non-debug write tap on the row shows the staging copy writing it only outside the move (240 writes each, whole run), and inside the window ONLY the palette-SEQUENCE uploader (`engine_internals.md` "The palette-SEQUENCE uploader") — ours uploads at 2807, 2813 and 2858, native at 2813 only; the extra 2858 upload is the fire ramp. The maintainer: *"Ticket it, go on (Recommended)"*. Q4: the pass-overrun log named no build and "every row as frozen" is what merged-m18 prints too; the claim's "over the #136 corpus" overstated a 3-part gate. Resolved: `audit_pass_overrun` prints the build's fingerprint and a completion line per leg; NEW gate **`tests/audit_lag_budget.sh`** (emulator tier, registered): all 42 naming parts on the build under test and the reference — the fix against merged-m18: no zero-pass frame the reference lacks (1 on each, the same), control `lag-planted` fired, its mode exits 1 (the first plant removed "one step" where the counter moves 2 a frame — the control caught it DEAD, fixed); `audit_df_meter` prints every leg's +0x111 span |
| **(8) THE M19 FREEZE — BUILT, REGISTERED, CARRIED AND FROZEN** (the maintainer: *"Freeze M19 now (Recommended)"*) | FIVE BUILDS by the tree's own recipes, the withdrawn 14z-159 dirs moved aside first (`build/withdrawn_m19_14z159/`): `build/don_m23`, `hui57`, `pyron42`, `m3b_merged27`, the stock twin `m5_stock18`, and the stage-4 image (`build/rc170/freeze/stage4`). `tests/test_m3a_reproducible.sh` re-frozen and green (program: donovan `48423867`, stock `c54f1fb8`, huitzil `7e2531b3`, pyron `ef4bbb25`, merged `681ac3ad` — program-identical to the verified `fix170_b`). SIX REGISTRY ROWS (whole-set keyed): donovan-m23 `22c9c9d8`, huitzil-m30 `af0bb263`, pyron-m24 `bc95f77a`, merged-m19 `61e9815a`; the battery's donovan-m23-stock `c54f1fb8` and donovan-m23-stage4 `2fa7c2f1` (moved by ONE byte, `case_a4`), the m19 pair annotated SUPERSEDED. EXPECTATION SETS CARRIED, THEN FROZEN (`build/rc170/freeze/suite/run.sh`, MAME_BIN pinned): the authored `.masked`/`.skip`/mask copied from each predecessor, `run_suite --freeze` then a plain verify per track; the freeze printed `authored .masked expectation` 53/52/52/53 times = the predecessors' counts, `01_attract_long` never self-frozen, SUITE GREEN on all four. Self-frozen tenant `.sha1`: same names as the predecessors; MOVED donovan 7 of 16 (first differing frames 2747-4861), huitzil 1 of 17 (`37_pick_huitzil_cell`, first differing frame 3160), pyron 0 of 18. The stock and stage-4 sets carried verbatim and verified by the battery's own masked leg (`m2a_legacy_gate_masked`, dispatched by fingerprint): 14/14 PASS each, every flicker/diverge constant as frozen. THE RE-POINT SWEEP (`build/rc170/freeze/sweep_scan.py`, `sweep_apply.py`): 304 live defaults in 153 files, dated records excluded by file:line and by the freeze-name pairing rule, four hand edits, `audit_lag_budget` REF -> `m3b_merged26` (M18, the build before the fixes). STATIC RE-FREEZES, each red attributed first: see row (9) |
| **(9) THE STATIC TIER ON M19, EVERY RED ATTRIBUTED BEFORE A RE-FREEZE — AND RUN 55** | the gates the freeze moves, one by one (`build/rc170/freeze/static/`): moved BY DESIGN and re-frozen — `test_defense_rows_census` (Phobos and Donovan now read native's rows), `test_df_field_readers` (the three `x028122` adder reads gone, addresses +0x30), `test_latch_readers` and `test_escape_triage` (+0x30), `test_reaction_classes` (case_a4 now the one constant 0x38 writer, the thunks' three 0x38 compares), `test_manifest_merge` ((23,63,22),97,10), `test_rule5_census` (+6 build-gate flags, ledger row), `test_pointer_flow` (NEW baselines merged-m19 / donovan-m23: the two STRONG findings +0x30, merged data:long +1 = one 2-aligned pointer relocated into a WIDE-hole-shaped window, Donovan -3 = three corrupted-site longs in his x2b7ef4 copy), `test_region_overlap` (x2b7ef4 1-differs 1042 -> 878: only Donovan's copy changed, 160 bytes), `test_charmap_current` (regenerated: Donovan's x2b7ef4 relocated_bad 33 -> 11, unattributed 692 -> 558; the 11 left are 0x4AFC TRIPWIRES at `PRG:0x0CBAE0`-`0x0CBF50` for vs2 targets past the region's end, identical in M18). TWO GATES REWORKED because a fix removed their anchor: `test_df_field_readers`'s known reader is now vs2's own `0x2617A` read from vs2's pristine opcode view ([VSP-166]); `test_reaction_classes`'s no-0x38 assertion is scoped to the reference images, which must exist, with exactly one writer on ours. **Rule-checker run `2026-09-19-55`** (`expectation`, plant `inferred-decision-14z162` CAUGHT): **Q2 Q3 Q5 OK/N-A; Q1 Q4 VIOLATED, every finding true, resolved by work** — three new checks had no failing-direction run (each now run failing by a scratch copy, `build/rc170/freeze/refreeze/`; the working agent's own first anchor scratch was a SYNTAX ERROR that exited 0, caught by reading its log), the rule-5 fact NOTE unattributed (+60 = the new rows' keys, measured under HEAD's OWN tool after today's tool had read the renamed pcrel sections as new), the old baselines and the uncapped M18 relocation listing missing from the packet (added: 11 of 11 identical), and the reference 0x38 check could pass on rows it no longer read (now required). THE MiSTer TAIL: the fork's `vsavjw` catalogue regenerated for M19 (eight members, exactly the merged delta) and committed LOCALLY as `7ef23018` (`mechanyaa-ai`), the pin bumped, series `0035`, `test_jtcores_twin` PASS; the first MiSTer lane run had failed on the stale catalogue (all three gates, `build/emu_freeze_m19_mister_p1_stalecat/`). THE RELEASE TREE `release/merged-m19/` packaged (MRAs from a private scratch clone: the BUILD block and the eight CRCs, the stock control identical; 82 trackable files as M18), `test_release_roundtrip` / `_asset_shape` / `_binaries` PASS. NEW GATE `tests/audit_column_flash.sh` (the orange flash, frozen as measured, row HASHES only — palette content is ROM-derived; control `flash-gone`; merged-m18 fails it). PROMOTED `tools/attribute_patch_delta.py` (from `build/rc170/delta_ops.py`: M19 vs itself 835 SAME; M18 -> M19 15 EDIT, the designed ones) |
| **(10) THE FREEZE RUNS THROUGH THE RULE-CHECKER, AND THE EMULATOR BATTERY'S REDS** | `freeze` runs **56, 57, 58, 59** (every plant CAUGHT; every real packet VIOLATED, every finding true, resolved by work): the merged row's attribution, the member lists and the stage-4 baseline were missing (the stage-4 row had said "against HEAD's rebuild reproducing 108f7523" — no such rebuild was run; the row now names `build/don_stage4_m21`); relocation was attributed by arithmetic alone (the promoted `tools/attribute_patch_delta.py` gained a CONTENT check on every relocated target, 7406 of 7406 on merged, and fires on a planted byte); the `x2b7ef4` repair was checked by nothing (the scratch `x2b7ef4_sites_check.py`, anchored in vs2's OWN source bytes: every changed byte of all five copies classed, 0 unexplained, a plant flagged — the checker's '0xEE placeholder' reading was the middle byte of the correctly relocated pointer 0x0FEE08); the designed-edit check covered merged only (the scratch `edits_check_tracks.py` now classes every EDIT and INSERTED op of every track by CONTENT, Donovan's id per track, 0 unexplained, a plant caught in an EDIT op and needed in an INSERTED one); every M18 baseline fingerprinted to its registry row; the committed merged set run as committed (PASS 53 / SKIP 19 / NO-EXPECTATION 16 / FAIL 0, the shipped shape, SUITE RED by design). THE FREEZE BATTERY, pass 1 (`build/emu_freeze_m19_p1/`, `--freeze --jobs 4`): **144 PASS / 15 FAIL**, no legacy regression — ten tables the fixes move by design re-frozen and verified (`build/rc170/freeze/refreeze2/`; the move-parity table on ALL=1: only DIFF->IDENT transitions — the column, five Plasma Trap events — and one meter field gone, NONE IDENT->DIFF); `bases.tsv`'s Pyron row +0x30; two live gates re-anchored (`audit_df_field_readers_live`: liveness from any +0x1C3 read, the adder reads now FORBIDDEN; `audit_reaction_class_live`: vanilla writes no 0x38, ours only the S1 marker, located in the build's own patch) and both FAIL on merged-m18; `test_tenant_winpal`'s lose leg REWORKED — Donovan now dies ~160 frames earlier in replay 62 so P2's trailing mash skipped Victor's quote screen (captures `build/rc170/freeze/winpal/replay62_post_ko.png`), the leg now traces the KO and drops P2's inputs after it: PASS on M18 and M19, FAIL on a wrong-pool plant; `audit_continue_switch` RE-AUTHORED as the maintainer ruled (*"Re-author as described (Recommended)"*, `DECISIONS_HISTORY.md`): Phobos no longer loses the mash by KO (a timeout), so P1 idles to CPU Bulleta's natural KO (f8100) — measured first that the continue landing on Jedah is the RIG (pristine vsavj's Victor lands on Jedah too) — and all five assertions hold (the #99 pairing at f9220). The MiSTer lane: census and gfxc_fetch PASS, the prg_window pair re-frozen. Rule-checker run **60** on the emulator re-freezes (VIOLATED Q1 Q4 Q5, every finding true, resolved by work — row (11)). **Pass 2 (`build/emu_freeze_m19_p2/`, `--freeze --jobs 4`): GREEN, PASS 159 / FAIL 0**, the MiSTer prg_window verify PASS; its dirty-file list flagged the working agent's own concurrent edits, and because that list cannot see re-edits or untracked files, every file changed since pass 2 began was inventoried against the gates naming it and those 36 gates re-run on the final tree through the runner: 36 PASS (`build/emu_rerun_p2/`; run 61 Q1) |
| **(11) PHOBOS'S REMAINING +1, RULED; RUNS 59 AND 60 RESOLVED; A NEW GATE FOR THE TICKET** | the pass-1 attribution had carried Demitri's 5HP on Phobos (#136 parts huitzil_5/6) as DEFENSE-ROW although it moved only 13 -> 12 against native 11 (run 60 Q1/Q4). Measured (`build/rc170/freeze/dmg/`): deterministic across RNG pins 0000/1234/5a5a on every leg (native 11, M19 12, M18 13), the same frame on both legs, with his curve row AND threshold word already vs2's on M19. Asked, the maintainer ruled *"Freeze, ticket it (Recommended)"* (`DECISIONS_HISTORY.md`): M19 frozen as built; the attribution tool gained the open class `PHOBOS-DMG-OPEN` (it reads the build's row 0x10 against vs2's: True on m3b_merged27, False on m3b_merged26), `move_parity_attribution.tsv` re-frozen and verified; NEW GATE **`tests/audit_phobos_dmg_residual.sh`** (the ticket's reproducer: premise both rows vs2's, ids by trace, one loss per part and pin, frozen AS MEASURED in `phobos_dmg_residual.tsv`; control `residual-gone` FIRED in-gate, its mode exits 1; merged-m18 FAILS the premise), registered (runner, gate index, provenance, must-fire census 131). Runs **59** (freeze) and **60** (expectation) RESOLVED BY WORK, every finding true: every rompath file and zip member of all six pairs diffed by CRC and SHA-1 (the stock twin's `vsav.zip` 21/21 unchanged; stage-4 one zip of 9); the moved gfx members shown BY CONTENT to be the one re-authored version glyph (tile `0x1fe42`, a plant outside it flagged); three plants in INSERTED ops each caught; the `reaction_class_live` attribution was WRONG ("read rows +0x30") — the new 0x38 values are the S1 marker and the three new reader PCs the designed thunks' compares, located by content (FAIL on M18); the win-pal matched window CAPTURED on the reworked leg — Victor's quote screen on both builds (`build/rc170/freeze/winpal/lose_leg_matched_window.png`), the plant one line (expected slice id 3 -> 2) |
| **(12) THE ORANGE FLASH'S WRITER WAS MISREAD — CORRECTED, AND CAPTURED AS A GATE LEG** | capturing the flash's writer measurement as a test ([VSP-18]) found it wrong: the row (7) reading — "the engine's staging copy rewrites row 11 on the same frames on both games" — came from a `-debug` write watch (`tests/lua/trace_writes.lua`) whose frame column counts debugger STOPS: [CPE-5], a recorded gotcha, paid again (`docs/platform/gotchas.md` "Debugger stops DESYNC replay frame counting"). A non-debug write tap (`read_tap.lua`) on the row: the staging copy's 240 writes are 16 words on each of 15 frames at 2313-2355 (the round-start fade); inside the move ONLY the palette-SEQUENCE uploader writes row 11 — the two `movem.l d0-d3,(a1)` at vsavj `PRG:0x02AD64`/`0x02AD78` (vs2 `0x02A0BA`/`0x02A0CE`; `engine_internals.md` had cited the post-instruction PCs `0x02AD68`/`0x02AD7C`, corrected) — ours at 2807, 2813 and 2858, native at 2813 only; the 2858 upload is ours alone and is the fire ramp (merged-m18: 2864, six frames later). Carriers corrected: row (7) (marked RETRACTED), the gate header, the issue draft, the findings table, the NEXT_SESSION draft. `tests/audit_column_flash.sh` gains a WRITER leg (the tap on both legs, each writer PC anchored to the uploader's instruction in its own leg's opcode image, liveness from the boot clear), ground-truthed on a scratch copy (`build/rc170/flash/acf_test*.log`: PASS on M19; control mode exits 1; merged-m18 FAILS on the 2864 writer rows; an opcode-anchor plant FAILS) — swapped in after the battery's pass 2, then given `run` rows (every writer of the row over the whole run with its count: the boot clear, the round-start fade STEPPER — vsavj `PRG:0x01433C` / vs2 `0x0129B4`, homed in `engine_internals.md` — and the uploader), re-frozen, verified, control mode and merged-m18 both exit 1. `tests/lua/bp_regs.lua`'s header claims the debugger keeps frame counting aligned, which this contradicts |
| **(13) THE x2b7ef4 REACHABILITY WAS MEASURED ON DESYNCED REPLAYS — RE-MEASURED SOUNDLY, THE CONCLUSION STANDS** | reviewing which scratch measurements lacked a test (checklist item 5) found the reach probe (`build/rc170/reach/reach.sh`, row (5), the maintainer's "Measure it now") used the same `-debug` watch: Donovan's watch covered his whole copy, including a block the match reads thousands of times (`CPU:$0FCC00`), so each part took 1,431-5,776 stops and its frame counter — and input playback — ran ahead by as much (donovan_4 reached its 8,520-frame stop near emulated frame 2,900): the Donovan half was VOID; Pyron's six runs took only the boot stop. `tests/lua/trace_writes.lua` gained three opt-in knobs (unset = exactly as before): several `;`-separated WATCH ranges, `WATCH_FROM` (arm at a frame, after the boot sweep) and `SAMPLE` (a change-only field log). NEW GATE **`tests/audit_x2b7ef4_reach_m18.sh`**: the corrupted bytes only (Donovan's three clusters, the M18/M19 difference inside his copy; Pyron's span), armed at 2000, each debug run's P1 anim-node trajectory compared with a non-debug `field_trace` run's. Measured: **no part reads a corrupted record** (Donovan 14, Pyron 6); 18 of 20 runs frame-exact with their reference to the end; donovan_10/12 skew by ONE frame at f2980/f3537 with no stop (deterministic, frozen as measured — a silent debug run is still its own timeline); control `hot-block` (the widened watch) hits 5,572 times from f2363 and then desyncs, its mode exits 1 (scratch ground truth `build/rc170/reach2/t_all.*.log`). Carriers corrected: `DECISIONS_HISTORY.md` (marked in place), row (5), patch_notes §5, HANDOFF, `docs/platform/gotchas.md` [CPE-5] |
| **(14) THE FREEZE CHECK — `freeze` RUNS 61 AND 63-76, THE LAST OK** | every plant CAUGHT; every real packet VIOLATED until run 76, every finding true and resolved by WORK (verdicts and resolutions verbatim in `tests/rulecheck/runs/2026-09-19-6*`/`-7*` and the ledger; the packet `build/rc170/freeze/rc_final/`). What the runs made the packet show, not assert: the runner's dirty list cannot see a re-edit or an untracked file (61) — replaced by ONE inventory over every gate that ran, 164 scripts, each against its own last start (`final_inventory.py`, 67); which build each MiSTer gate ran (63-65) and that the freeze changes no core (66); the registry's key columns unchanged since a capture made before any gate read them, now a script with a plant (64, 68, 75); every instrument and the reference sets unchanged outside git (69); the tree rebuilding every build bit-exact NOW (`test_m3a_reproducible` 16:43-16:47Z, 70); the untracked build dirs and `build/merged1`'s determinism (71). THE CAPTURES (72, 73): most sheets the maintainer read were shot on scratch builds (`fix170_b`; Pyron's on `fix170_all`, deleted) — every ours leg re-shot on `build/m3b_merged27`, **78/78 snapshots pixel-identical** (`recap/recap.sh`); the column-KO sheet's own MAME log opened `m3b_merged27`'s archive and its re-run reproduces **8/8 snapshots and all 8341 frames' whole-RAM checksums** (`recap/recap_ko.sh`); `fix170_b` carries M19's program exactly. THE REMOVAL SIDE (74): a directory's mtime shows only its LAST change, and the inventory skipped a missing gate-named file silently — checked directly: no tracked removal since HEAD (committed before the window); of 5835 repo paths the gates' code names, **48 missing, 32 in comments, 16 in code, each decided, none read** (`removals_check.py`, a plant reported; `removals_classified.txt`); 341 of them cut short by a variable or a glob were checked by their directory only — the residue (an untracked file removed that no gate names in full) NAMED. (75, 79) the merged-m19 registry note said the column and the trap equal native, each measured on a GROUNDED victim only (the trap gate asserts the ground case's marker 0x38): the note scoped (the airborne case open, #163, a comment extending it to the trap), and the packet's `DECISIONS_HISTORY.md` line citations re-derived (`recite_decisions.py`). (76) OK on all five; its note that the freeze ruling sat only in row (8) answered: recorded in `DECISIONS_HISTORY.md` "Ruled 2026-09-19 (14z-170) — freeze M19 this sitting", the question, options and times quoted from the session's own records (row (8) had kept only the answer). (77-79) the documentation packet for the table's rows 27-31 (run 62 covered rows 1-26): 77 — the 341 paths checked by directory only and the ruling entry's source named, and the attribution tool shown LISTING the 114 in-place relocations it counts (`attr_listing_check.sh`); 78 — the live #163 issue fetched and shown naming the airborne case (`build/rc170/gh/issue_163_live.txt`; the local draft had been cited), the listing check made a script and counted in ON TRIAL; 79 — SAME made LISTABLE (`attribute_patch_delta.py --same`, default output byte-identical on the six tracks, `tool_plants` and the edits checker reproducing): every listed SAME pair identical in kind, address and bytes on all six, a relocated long planted into SAME leaving it (`same_class_check.py`); the trap's grounded scope, and the removals scan's one-level closure, named; 80 OK on all five. Two gotchas paid: "A DIRECTORY'S MODIFICATION TIME HIDES A REMOVAL BEHIND ANY LATER ADDITION", "A CAPTURE IS EVIDENCE FOR THE BUILD IT WAS SHOT ON" (`docs/project/gotchas.md`) |

| **CLOSE (14z-170, 2026-09-19) — THE CHECKLIST** | (1) the findings table, 33 rows with their homes and tests (`build/rc170/findings_table_14z170.tsv`; rows 27-31 added after run 62: the attribution tool's absorbed class, row (14)'s three and the freeze ruling's record; rows 32-33 from the close tier, item (8)); (2) `tools/close_findings.py 14z-170 --base a3619798`: one GAP (`0x0129B6`, the retracted reading's address — homed: the fade stepper's write in `engine_internals.md`), three REVIEW (`0x01433E` — homed with it; `0x1fe42`, the mark's glyph, homed in `tools/mk_mister_page.py` since 14z-117, not a new finding; `CPU:$0FCC00`, merged-m18's hot block, homed in `tests/audit_x2b7ef4_reach_m18.sh`'s header and control), the one PROMISE (the maintainer's trap frames) fulfilled by the captures they ruled on; (3) pointers: row (5)'s reach and row (7)'s flash writer point to rows (13) and (12); (4) no other promise; (5) ON TRIAL — 19 scratch scripts had a figure quoted by a document or a STATE row, 8 promoted (`delta_ops.py` -> `tools/attribute_patch_delta.py`; the Phobos damage rig -> `tests/audit_phobos_dmg_residual.sh`; the flash write tap -> `audit_column_flash`'s writer leg; the reach probe -> `tests/audit_x2b7ef4_reach_m18.sh`; the column KO -> `audit_column_shock` 1b; the trap timeline -> `audit_trap_shock`; the lag check -> `tests/audit_lag_budget.sh`; the continue-switch phases -> the re-authored `audit_continue_switch`), 11 not (the freeze-time checkers `delta_edits_check.py` and its successor `edits_check_tracks.py`, `tool_plants.py`, `image_outside_ops.py`, `image_vs_patch.py`, `x2b7ef4_sites_check.py`, `members_all.py`, `gfx_mark_check.py`, `reader_pcs_check.py` and `progdelta.sh` are one-freeze attributions of a delta that no longer moves; the win-pal capture script produced a picture, the gate asserts the pool); and the freeze check's own packet scripts whose figures row (14) quotes, 11 more, none promoted (`final_inventory.py`, `image_delta_classes.py`, `authored_carry.py`, `removals_check.py`, `mister_names_expected.py`, `registry_keys_check.py`, `recite_decisions.py`, `attr_listing_check.sh`, `same_class_check.py`: one-freeze attributions of this packet; `recap/recap.sh`, `recap/recap_ko.sh`: re-shoots against untracked scratch captures, the behaviours they carry gated on M19 by `audit_column_shock`, `audit_trap_shock` and `audit_ex_refused`) — the trial's figure for this sitting: 30 quoted, 8 promoted, 22 not; (6) the documentation packet, rule-checker run **2026-09-19-62** (`recommendation`, plant CAUGHT): **Q3 OK; Q1 Q2 Q4 Q5 VIOLATED, every finding true, resolved by work** — close_findings re-run after homing the fade stepper (GAPS none); the column KO had been concluded from RAM traces alone: a capture made and sent, the maintainer read it *"looks clean to me"* (`DECISIONS_HISTORY.md`); the column/trap ticket kept OPEN for its airborne case rather than closed with an option in it; x2b7ef4's open question stated as not pursued, by its ruling; finding 10's identity named as ungated; the defense-rows ticket now names the residual; every ruling the drafts attribute now quoted (and the flash and trap rulings, found recorded only in STATE, added to `DECISIONS_HISTORY.md`). THEN POSTED as `mechanyaa-ai`: #161 (Phobos's +1, open), #162 (the orange flash, cosmetic, open), #163 (the column/trap rule, open for the airborne case), #164 (the defense rows), #165 (the EX route), #166 (the x2b7ef4 corruption) — the last three closed with their resolutions; comments on #136, #157 and #163; the index rows with their four answers (`tools/tickets.py check` PASS, 166 tickets). (7) THE FINDINGS ADDED AFTER RUN 62 (table rows 27-31) through their own documentation packet, `recommendation` runs **77, 78, 79, 80** (every plant CAUGHT; 77-79 VIOLATED, every finding true, resolved by work — row (14); **80 OK**). Run 80's claim carried a stale phrase, "no new GitHub post follows from these rows", beside its own recommendation to post the #163 comment; its checker read it OK; recorded here, not re-run. THEN POSTED as `mechanyaa-ai`, as run 80 recommended: a #163 comment extending its open airborne item to the Plasma Trap (`build/rc170/gh/comment_163_trap.md`). (8) THE CLOSE TIER (`--strict --cadence freeze`, every control executed). The first run was made WITHOUT `ROMDIR` — 81 gates SKIPPED, read red by `--strict` (`build/rc170/static_close.log`) — and before the tags (`test_freeze_tag_coverage` red by design). Then the close COMMITTED (`e3f7537c`) and the four freeze tags created (annotated, `mechanyaa-ai`). The second run (`build/rc170/static_close2.log`, 2515 s): **PASS 162, FAIL 2** — `test_phasec_spaces` and `test_bbh_fidelity`, both still on M18 and both missed by the freeze (table rows 32-33; the gotcha "THE RE-POINT SWEEP SEES BUILD NAMES IN THIS TREE — A PINNED FINGERPRINT AND THE HARNESS'S DEFAULTS ARE OUTSIDE IT"): phasec's stock-twin fingerprint re-frozen `e86e1d04` -> `c54f1fb8` (donovan-m23-stock, the delta the freeze's own attribution of the stock twin; PASS on the new pin, FAIL on the old) through rule-checker runs **81-84** (`expectation`; 81-83 VIOLATED, every finding true and resolved by work — the rule-checker's birth key classified as a permanent anchor, both phasec legs logged with their invocation and exit, the stock twin's delta shown from the image side with stock-track plants, the harness census and both harness verdicts re-read on the committed `6e20d58`; **84 OK**, noting two overstatements that leave no failure mode uncovered: one plant called "the DEFENSE data op" is a merged RECORD op, DEFENSE covered by two INSERTED-op plants, and the harness census's before-the-re-point grep searched build names only); every other M18 fingerprint in `tests/` and `tools/` a dated comment or already re-frozen (`build/rc170/m18_fp_grep.txt`); the harness's lineage defaults re-pointed to M19 (blackbox-harness `6e20d58`, four files as the M18 re-point `10a82d2`; on the committed harness `test_bbh_fidelity` PASS, F4 identical, and `bbh selftest` GREEN, PASS 32 SKIP 2, each log naming the harness state). THE THIRD RUN, on the tree as it then stood (`build/rc170/static_close3.log`, 21:36:33Z to 22:18:08Z, 2495 s): **PASS 164, SKIP 0, FAIL 0, MISSING 0 — GREEN**, `fired 190 / declared 190`, `executed 190 honoured 190 lies 0 refused 0 died 0`, 5 gates triggered above cadence; no tracked file's CONTENT changed during it (one mtime moved, `tests/expected/checkdocs_rom_covered.tsv`, which `test_checkdocs_rom` restores from its own backup — git reads it unchanged). This row and the tier rows above were written after the tier; the doc gates were re-run over them, the tier was not |

## Session 14z-169 — **THE ANALYSIS THE RULED #136 FIXES STILL NEEDED, DONE IN THE MAINTAINER'S ORDER (items 2, 1, 3). (2) The 0x52 rule's candidate discriminator — a column/trap record remapped to class 0x38 — is FALSE three ways: the STAGER rewrites a record's class before the reaction (vsavj writes 6 for both 0x06 and 0x38), one legacy Victor record already carries 0x38, and the GUARD decision sends a 0x38 record to the hit path against a standing victim where 0x06 is in neither list (`+0x121` measured to be the crouch flag; read as a low, no hit run); but `+0x54` = 0x38 ITSELF is produced by nothing in vanilla (static + 149 tapped runs) and read by every consumer exactly as 6 — the role 0x52 plays on vs2, whose shock handler is vsavj's plus one test. (1) Every defense-curve and rally-threshold read over the corpus indexed the VICTIM'S OWN id, tenants included (identity by hitbox base), so the ruled defense-row fix can be data-only. (3) vs2's EX never sets the Power flag `+0x1C3`: of its 26 placed readers only the three meter adders play differently — the other 23 read 0 natively too. The maintainer confirmed the vs2 EX inputs, and on the captures of the refused input ruled the disabled route does the no-stock (sub-pattern) move; the 0x52 fix scoped S1; ALL FOUR FIXES RULED GO — the gauge fix built and verified (3 program bytes) but NOT committed: a manifest fix lands only with a freeze, so the four land with M19. Five gates new and three re-frozen, each frozen, verified and control-run; three rule-checker runs, every finding true and resolved; `read_tap.lua` gained an opt-in pc filter, its ten users re-run green. No shipped ROM byte moved; the tree stays at merged-m18.**

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, `docs/NEXT_SESSION.md`), `main` == `origin/main` at `024495b7`, bbh clean at `caee272`, `emu/fbneo` dirty by exactly the two tracked patches (841/4), nothing running, `gh` as `mechanyaa-ai`; ROM audit **76/76**. The maintainer, on the EX inputs: *"as far as I know these are the correct inputs in VS2, and given you were able to trigger the moves for the comparison we made last session I assume they are indeed correct. As far as I know these EX input do not work in Vampire Saved BUT if they do, indeed they should be removed. And indeed meter gain during Dark Force should be removed."* — told they DO work on ours (14z-168 row (5); `audit_df_modes` ex/ours rows: 496/478/478 frames of `+0x111`) — and *"Do as you proposed, starting. with item 2 then 1 then 3"*. The confirmation replaced "the maintainer to confirm" on its four live carriers (engine_internals, the project gotcha, both DF gate headers) |
| **(1) ITEM 2 — A HIT'S CLASS GOES RECORD → GUARD → STAGER → `+0x54` → REACTION, AND THE STAGER REWRITES IT** | static (`tools/audit_reaction_classes.py`, gate **`tests/test_reaction_classes.sh`**, ci_static, 193 rows): the record's `+0x17` is dispatched by one of three stager tables (ground `0x018468` / air `0x018510` / KO `0x0185DA`), whose handler writes `+0x54` — the class (copy handler `0x01868C`) or a constant: vsavj's ground stager writes **6 for 0x06 AND 0x38**, air 7 for 0x06/0x07/0x38, KO 8 for 0x06/0x07/0x38/0x39/0x48; no stager handler and no `move.b #imm,$54(An)` in either game writes 0x38. vs2 differs for its column only on a GROUNDED victim (its ground stager writes 0x52; its shock handler `0x22656` = vsavj's `0x23AC8` + `cmpi.b #$52,$54(a6); beq` around the attacker's freeze; air/KO fold 0x52 into 7/8). Record census (1,085 reachable vsavj legacy records): one Victor record carries 0x38 on both games, one Bishamon record 0x52 on vs2. Consumers of `+0x54`: 80+ constant compares per game, never against 0x06/0x07/0x08/0x38/0x4E (vs2: 0x52 once, the shock test); the three per-class tables → `+0x26` and the property map hold the same byte for 0x06 and 0x38; the `+0x375` compares can match neither. **So 0x38 IN `+0x54` reacts exactly as 6, and nothing in vanilla produces it.** 0x38 IN THE RECORD does not: the GUARD decision (`0x0182E8`-`0x0183E4`) sends record classes 0x02/0x03/0x38/0x39 to the hit path when the victim's crouch flag is 0 and 0x2C/0x37/0x42/0x48/0x4A/0x4D when it is 1 (read as lows and overheads; no hit against a guard run, no capture). Our build already carries vs2's whole shock handler, 0x52 test included, in Huitzil's `x022400@huitzil` (`PRG:0x41BD66`), never executed by the corpus. Live (gate **`tests/audit_reaction_class_live.sh`**, 149 tap runs: every suite replay on pristine vsavj, the 30 naming parts on ours and native): no write of 0x38 on vsavj or ours; native writes 0x52 at `0x016FEC` on four parts (column, trap) and 0x4E at `0x016FE4` on one (Lightning Sword) where ours writes 6 at `0x0186D0`; **the legacy suite never lands a grounded class-6 hit** (one airborne 7) — the 14z-42 thunks' legacy path is not exercised by it. The 14z-168 idea RETRACTED on every carrier (engine_internals, STATE 14z-168 row (11) marked); a game gotcha |
| **(2) THE CROUCH FLAG** | the guard reading rested on what `+0x121` is (undocumented): measured 1 exactly while Down is held (donovan_1, 2840-2870) on native vsav2 and ours — gate **`tests/audit_crouch_flag.sh`** (control `down-removed`), atlas row added |
| **(3) ITEM 1 — EVERY DEFENSE READ INDEXES THE VICTIM'S OWN ID** | gate **`tests/audit_defense_row_reads.sh`** (219 tap runs; `read_tap.lua` `RPCS` on `+0x382` and `+0x60`): the curve read `0x018C10` and the threshold read `0x018C78`, identity by the victim's hitbox base `+0x60` (`bases.tsv`), never `+0x382`: on ours the 12 victim parts, the 30 attacker parts and every suite replay (1P CPU matches, where the voice-class borrow runs, included) — Donovan 394 hits, Phobos 38, Pyron 28, all their own id; on pristine vsavj every legacy victim reads its own row. Only the host's two reads name either table by address; no placed code names vs2's. The data-only route for the ruled defense rows stands on this; the attacker-side combo read `0x018C40` (through the hit-registration pair, #157's family) is logged and frozen, not judged |
| **(4) ITEM 3 — THE POWER FLAG'S 23 OTHER READERS READ 0 ON BOTH ENGINES** | `audit_df_modes.sh` gained a `pow` column (`+0x1C3`, positive control: every vsav2 P+K leg holds it — 388 frames, 297 Morrigan/Lilith), re-frozen: never set by Change on either game nor by the tenants' vs2 EX. Of the 26 placed reads: 3 meter adders (the ruled defect — natively the EX pays gauge because the adder tests Power), 3 the Change entry's "not during Power" test (vs2 `0x2617A`, the EX route) **[MISLABELLED — 14z-170: `0x2617A` is the "already in Power" test of vs2's POWER activation `0x026166`, not of the Change entry, and the EX route never passes it — STATE 14z-170 row (2)]**, 2 Phobos's powered specials (vs2 `0x551D0`/`0x5540C`, `+0x106` 0x1A), 18 a per-move Power latch `+0x19C` in vs2's shared special-move routines, tested at five `tst.b $19c` sites, all placed per tenant — vsavj loads/tests `+0x19C` nowhere. Natively in the EX all read 0 as on ours: only the adders need the fix |
| **(5) THE INSTRUMENT** | `tests/lua/read_tap.lua` gained `RPCS` (log a READ only from listed pcs; unset = the old output). Edited while `audit_reaction_class_live`'s control modes ran (MAME loads it per run; the output is identical unset); its ten users re-run after the edit, every PASS line read (`build/rc169/rtap_users.status`; `audit_don_ko_writer`'s verdict reads `AUDIT PASS`, 12 s against its header's ~8 min) |
| **(6) THE FREEZES THROUGH THE RULE-CHECKER — RUN 51: VIOLATED Q1 Q2 Q4 Q5, EVERY FINDING TRUE, RESOLVED** | run `2026-09-18-51` (expectation, the five tables; `--model claude-opus-5`, `--id` explicit — the tool's counter collided with run 50, #160): plant (`forced-pick-14z159`) CAUGHT; **Q3 OK; Q1 Q2 Q4 Q5 VIOLATED**, verbatim in `tests/rulecheck/runs/2026-09-18-51/`. Q1: no artifact linked `m3b_merged26` to merged-m18, and the constant-write census never ran on our image, whose placed code writes `+0x54` — resolved by WORK: the static gate resolves the build's fingerprint through the registry and freezes it as an `ours build` row (merged-m18), and censuses our whole 6 MB image — which found TWO PLACED STAGER WINDOWS writing 0x52 into `+0x54` for a class-0x52 record (the `reaction_hook` `case_a4` at `0x0FFF50`, `index_window_018468` at `0x4716DC`) while vsavj's reaction table has no entry 0x52: why the column and trap records carry 0x06; no record on merged-m18 carries 0x50-0x53 (`tools/audit_fsm_census.py`: 0) and no corpus run executes either. Q2: "must be blocked low" was stated as fact with no capture — WITHDRAWN on every carrier (the code fact kept, the play reading labelled unmeasured; re-grep empty). Q4: the live defense gate is blind to readers at other pcs and the static reader scan was scratch — resolved by WORK: `tests/test_defense_rows_census.sh` gained READER rows (every absolute long and pc-relative `lea`/`pea` naming either table, vsavj, vs2 and our whole image: exactly the two host reads) and a `reader-planted` control. Q5: two ruling attributions unquoted — the words quoted in the gate headers and the regenerated `df_modes.tsv` header. Re-frozen and verified: `reaction_classes.tsv` (204 rows), `defense_rows_census.tsv` (41), `df_modes.tsv` (rows unchanged); every control fired, every mode exits 1. Follow-up run `2026-09-18-52` on the three re-frozen tables (plant `inferred-decision-14z162` CAUGHT): **Q2 N-A, Q3 Q5 OK, Q1 Q4 VIOLATED**, every point true — the frozen key was merged-m18's PROGRAM key (shared with `build/merged1`), not its whole-set key; the reader census matched only a table's exact base and planted only its absolute arm; the fsm census's zero carried no count of what it walked; the two stager windows' attribution was no artifact. Resolved: the WHOLE-SET key frozen (`00f9cf13`, `reaction_classes.tsv` re-frozen, verified, both modes exit 1); the absolute arm matches any address inside a table (rows unchanged) and `reader-planted` plants both arms, one row each; the census's count stated (179 VS2 regions, 162 node runs, 1,104 records) with `test_fsm_census.sh` and its negative controls green on this build; the attribution cited to the build's atlas fragment and `donovan.toml`'s `[reaction_hook]` bytes. A one-row re-freeze, resolved in writing and not re-run a third time |
| **(7) THE MID-SESSION COMMIT** | the tree at the commit: no shipped ROM byte moved, merged-m18 current. The eight doc gates, `tools/check_state_lists.py` and `tools/tickets.py check` green first; every new or changed gate run with its `CONTROL=` modes, each exit 1 (`build/rc169/`). The mid-session tier (`run_all_static.sh --strict --exec-controls none`, `build/rc169/static_mid.log`): **PASS 147 / SKIP 0 / FAIL 1 / MISSING 0**, 14 deferred by cadence, 3 triggered — the red was `test_poked_legs`: the census of forced-pick legs found the `planted-flavor` control's in-match poke of P2's `+0x382` (0x01 at frame 2680) as a new unresolved leg (`?`, its replay not named in the script). Reviewed — a deliberate perturbation after select, not a pick — and the census re-frozen with that one row (282), verified, both control modes exit 1; the tier is not re-run for a one-row re-freeze of a gate that was its only red (the 14z-168 row (9) precedent). Mid-session form: controls not executed, no push |
| **(8) THE TICKET DRAFTS AND THE DESIGNS** | the maintainer, on the EX route: *"I've never noticed, I always entered Dark Force with P+K of the same strength."* Asked to proceed: *"yes, with a caveat: we're at almost 80% context so maybe we'll hit the need for compacting if we proceed. I don't mind but I need to raise the point."* Drafts rewritten for the rulings and this sitting's findings (`build/rc169/gh/`: the column+trap, defense-rows and EX-route issues, comments on #136 and #157), not posted. Designs read from the build: the gauge — `x028122` offset `0xC4A` (vs2 `0x28D6C`) `tst.b $1c3`->`tst.b $111`; the defense rows — vs2's rows and thresholds into 0x10/0x13; the 0x52 rule — the stager/reaction tables are 16-bit pc-relative (a repointed entry must land within 32 KB, inside the encrypted base program), so the "dead class + stub" route is impractical and the design restores native 0x52 in the data and marks it through the port's own `reaction_hook` (`case_a4` writes 0x38), with the KO-stager thunk and the 14z-42 thunks extended; OPEN: on the solo Phobos track that machinery (donovan.toml's) is absent. NEXT_SESSION carries the plan |
| **(9) THE SCOPE QUESTION AND THE EX ROUTE, MEASURED** | the maintainer: *"carry on, we'll gain the 25minutes for once"*. THE SCOPE: read on the four images, only Donovan's tracks carry the 0x52 machinery (`don_m22` and merged hook `0x018458`, `0x0185CA`, `0x023AD8`); the solo Phobos build `hui56` hooks only `0x018460` (`index_window_018468`) and solo Pyron nothing — so native-0x52 trap records would crash the solo Phobos track; no composition-scoped row exists in the generator (searched), so the plan offers S1 (a row key applying Phobos's two trap remaps only when Donovan's manifest is absent) or S2 (the machinery in a shared manifest). THE EX ROUTE: new gate **`tests/audit_ex_refused.sh`** (12 legs, control `stock-kept`, frozen, verified, the mode exits 1): with the stock at 0 the tenants' vs2 EX inputs never enter the mode and take the SAME (seq/sub) path on vs2 and ours for all three (Phobos seq 0x14, Pyron 0x0E, Donovan 0x0A, neutral within ~35 frames); stock 3 enters on both. Captures `build/rc169/cap_ex/` (three native|ours sheets) delivered before any conclusion |
| **(10) THE RULINGS, AND THE SECOND MID-SESSION COMMIT** | asked three questions with the captures before them, the maintainer ruled (verbatim in `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-169)", three entries; standing lines below): the disabled EX route does the no-stock move at every stock level (*"Yes, the no-stock move"* — and the refused moves are sub-patterns of the EX input: *"Pyron is 623+P, Donovan is 1+K, and Phobos I'm unsure"*); the 0x52 fix scoped S1; all four fixes GO. The defense-row mechanism read: `[[data_port]]` copies in place from vs2's image (`dst_old_head`/`dst_end` guards), `only_variant_slot` so the stock twin (Donovan at 0x0F) is untouched. The mid-session tier (`build/rc169/static_mid2.log`): **PASS 148 / SKIP 0 / FAIL 0 / MISSING 0**, 14 deferred by cadence; this row written after it, the doc gates re-run. Mid-session form: no push |
| **(11) FIX 1 BUILT AND VERIFIED — AND WHY THE FIXES LAND WITH A FREEZE** | the gauge fix (one `[[port_patch]]` per tenant manifest: region `x028122`, `src_addr 0x028D6C`, `4a2e01c3` -> `4a2e0111`) built with `tools/build_merged.sh` into `build/m3b_merged27` (program key `04db6a44`): its program image differs from merged-m18's in exactly 3 bytes, the three adder tests. `build/m3b_merged27` EXISTED before the build and was overwritten without a look first — no tracked file names it (grep of tests, tools, docs, STATE, STATE_HISTORY, DECISIONS_HISTORY, manifests: empty), and the builder regenerates from the extracts. The constraint found: a manifest change triggers `test_m3a_reproducible` against the registered fingerprints and every gate targets merged-m18, so no fix commit is green alone — the four fixes land with the M19 freeze (my "each gets its own commit" in the question put to the maintainer did not hold; told). The manifests restored to the commit; the fix kept as `build/rc169/fix1_gauge.patch` (re-applies cleanly); NEXT_SESSION carries the plan |
| **CLOSE (14z-169, 2026-09-19) — THE CHECKLIST** | the maintainer: *"Close"*. (1) the findings table, 25 rows with their homes and tests (`build/rc169/findings_table_14z169.tsv`); four rows are static readings with no gate of their own, named as such (the `+0x19C` latch census, the per-build hook sites, the 16-bit table encoding, the freeze constraint). (2) `tools/close_findings.py 14z-169`: first run GAPS `0x0FFF50`, `0x4716DC` and REVIEW `0x018458`/`0x018460`/`0x0185CA`/`0x023AD8`/`0x28D6C` — each answered by a HOME in `engine_internals.md` (the placed 0x52 writers, which tracks carry the machinery, the adder's source); re-run: GAPS none, REVIEW none, PROMISES none. (3) pointers: the 14z-168 row (11) marker; NEXT_SESSION's 14z-168 items replaced by the open ones (the 14z-168 opener archived verbatim in `NEXT_SESSION_HISTORY.md`). (4) promises: none open beyond NEXT_SESSION's list. (5) ON TRIAL: 12 scratch measurements had figures quoted; 8 promoted (the six class-route scripts into `tools/audit_reaction_classes.py`, the crouch probe into `tests/audit_crouch_flag.sh`, the defense trial into `tests/audit_defense_row_reads.sh`), 4 not (the fsm walked count, the `+0x19C` census, the per-build hook-site read, the gauge fix's image diff — each quoted with its method; the last re-measured at the M19 freeze); `build/rc169/cap_ex/cap.sh` makes captures, no figures. (6) the documentation packet, rule-checker run `2026-09-18-53` (plant `fidelity-14z160-false-statement` CAUGHT): **VIOLATED Q1 Q4, both true** — a third static-only row unnamed and the freeze constraint's "test" never run red; vs2's one legacy 0x52 record (Bishamon) homeless, and the checker row's test checking records, not findings. Resolved: the table corrected, the Bishamon fact homed in `engine_internals.md` with its row. A project gotcha homed (a manifest fix lands with a freeze). THE CLOSE TIER (`build/rc169/static_close.log`, 21:58:48 to 22:20:14): SESSION cadence, `--strict`, EVERY control executed — **PASS 148 / SKIP 0 / FAIL 0 / MISSING 0**, `fired 166 / declared 166`, `executed 166 honoured 166 lies 0 refused 0 died 0`, wall 1,286 s (gates 538 s, controls 740 s), 14 deferred by cadence, 3 triggered (`test_emulator_runner`, `test_bbh_fidelity` <- `tests/ci_emulator.tsv`; `test_suite_dispatch_selftest` <- PROVENANCE), no tracked file changed during the run; this row written after the tier (the doc gates re-run over it; the tier is not). Rule-checker runs this sitting: `-51`, `-52`, `-53`, every plant caught, every VIOLATED true and resolved. GitHub: nothing posted (the drafts wait for the M19 session). Commits: `dadbc5ae`, `dc8f1409` and this close. **The close ends with a push** ([VSP-17]): green, nothing red, skipped or pending |

## Session 14z-168 — **#136 FULLY ANALYSED: all 108 frozen DIFF rows attributed by ablation and a measured signature, none unexplained; Dark Force corrected at its root (vs2's P+K is Dark Force POWER, the tenants' reference is their vs2 EX move, which reaches their Change handlers natively); every capture put to the maintainer RULED — the block re-entry, Pyron's form and Donovan's sword IDENTICAL, the column shock a defect; FIVE FIXES RULED, none built: no gauge in Dark Force (#157's tail), the EX route disabled, the tenants' DEFENSE ROWS become vs2's (the rows today are their shells'), the column shock and the Plasma Trap take vs2's class-0x52 rule — all under the maintainer's condition that the combined overhead never adds a frame of lag. Eleven gates new or extended, every one frozen, verified and control-run; four rule-checker runs on the freezes, every finding true and resolved by work, every plant caught; a latent MAME-status flake fixed in nine gates. No shipped ROM byte moved; the tree stays at merged-m18.**

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, NEXT_SESSION — the file is `docs/NEXT_SESSION.md`), `main` == `origin/main` at `13793617`, bbh clean at `caee272`, `emu/fbneo` dirty by exactly the two tracked patches (841/4), nothing running, `gh` as `mechanyaa-ai`; ROM audit **76/76**. The maintainer: *"let's start with #136. While I usually prefer to fix first, here we have many divergences which may or may not share sources so let's finish all the analysis first. Then we'll fix and then relentlessly test for regressions, so all the rigs we create to test the build against vsavj or VS2 will be valuable at least at freeze/release time."* (ruled: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — #136: ALL the analysis first") |
| **(1) THE ATTRIBUTION — ABLATION, THEN A SIGNATURE PER ROOT** | the parity legs re-run with every trace kept and an extended field set (the 506 verdicts reproduced exactly — the extra fields perturb nothing). A root's INPUTS removed (the rig regenerated through `tools/name_moves.gen` with that recipe emptied, checked byte-identical to the committed rig first), both legs re-run, the rows that vanish are its: 10 steps, **65 roots for 110 rows** (the 108 frozen + 2 throws the committed rigs never land, surfaced once an earlier root was removed: donovan_8 ev3, pyron_3 ev0). Classes, each a measured signature: METER-SWAP 39 (#157), SLOWDOWN 29, DF-STOCK 11, ENTRANCE 10, GUARD-REENTRY 7, P2-DISPLACEMENT 5 (#159), TRAP-REMAP 5 (ruled 14z-85g(2)), COLUMN-SHOCK 2, DEFENSE-ROW 2 (ruled 2026-08-14; Phobos takes 13 where native takes 11 from Demitri's 5HP). Promoted: `tools/move_parity_attribution.py`, gate **`tests/audit_move_parity_attribution.sh`** (OTHER fails; `--no-ablate` its control) |
| **(2) THE cnt FAMILY IS A CPU OVERRUN, AND TWO OF THREE ARE NATIVE'S** | the 14z-167 first look ("an engine tick lost on ours") measured with the pass counter `$FF8081`: three zero-pass frames in the whole corpus, all on Blizzard Sword, right after a double-pass frame — native donovan_2 (3098) and donovan_11 (4580), ours donovan_10 (2977); the rest of each part stays one engine pass out of step with its inputs (neither frame nor pass alignment resyncs it). Not a port cost: the scheduler's idle-spin dispatches (slot 15, the task table `$FF025C`) average the same per event window (ratio 1.00-1.01). MAME 0.288's Lua has no screen `vpos` (the first timing attempt logged nothing; a probe script hung MAME — platform gotcha). Gate **`tests/audit_pass_overrun.sh`** |
| **(3) REFLECT WALL: vsavj RE-ENTERS THE BLOCK ANIMATION** | huitzil_5 ev1-7's node DIFF at +16/+17: at the freeze end (seq 0 -> 2) ours writes the block chain's first node with no counter tick where native continues the loop (non-debug write tap). A `-debug` logging breakpoint drifted the input playback (INPUT-VIOLATION) and hid the divergence (platform gotcha). The LEGACY CONTROL decided it: a Demitri mirror on the same rig on pristine vsavj, vsav2 and ours — vsavj reloads the counter on every frame back is held after the freeze (2 frames at hit+6, 6 at hit+10), vsav2 ticks, and ours equals vsavj on EVERY sampled frame. An engine difference; the host engine governs. Gate **`tests/audit_guard_reentry.sh`** |
| **(4) PLASMA TRAP AND THE COLUMN — ONE REMAP CLASS, ONE NEW DEFECT** | all five Plasma Trap rows carry the ruled 14z-85g(2) signature exactly (P2 class 0x52 native / 0x06 ours, the attacker freeze on ours only, nothing else differing first). Donovan's Killshread Lightning column carries the same class pair, but a non-debug tap shows ours writing the victim's freeze 0x0C and Donovan's 4 from `PRG:0x3FFBC4/0x3FFBF4` — the placed 14z-42 Lightning Sword thunks, reached because 14z-33 remapped the column's 0x52 to 0x06 — where vs2's `0x022656` writes 0x18 and exempts him: **the column shocks for 12 frames where native shocks for 24**, damage equal. No prior record (archaeology: `git log`, STATE_HISTORY). Capture delivered first (`build/p136_14z168/cap_column/`). Gate **`tests/audit_column_shock.sh`** |
| **(5) DARK FORCE — THE WRONG BASELINE, CAUGHT BY THE MAINTAINER, AND WHAT IT HID** | an in-DF rig variant (DF re-activated per group; vsav's 36-frame DF-end state seq 0x1A needed a longer wait) first compared our P+K with vs2's P+K. The captures (Pyron's flame burst, Donovan's flying sword and Anita — `cap_pyron_df/`, `cap_donovan_df/`, delivered) drew the maintainer's correction: *"you are testing DF in our build versus the global/common Dark Force Power in VS2"* — vs2's P+K is Power (two stocks, no startup window, a global buff), vsav's is Change; the tenants' Change counterpart in vs2 is their personal-DF EX move. Measured then (`tests/audit_df_modes.sh`, 40 legs, real picks): vs2's P+K arms NO window on all 15 characters its wheel reaches (the 14z-126 "`0x025F2A` writes 1" sentence RETRACTED — that write does not run); the tenants' EX inputs (421+KK, 263+PP, 2623+PP, measured candidates; captures `cap_*_ex*`, delivered) reach vs2's own Change entry `0x02622A` and arm the SAME windows (64/79/41) from their own handlers, period 5, 478/509/478 frames of `+0x111` — **the 14z-126 "the sequels ship these handlers but never reach them" is wrong for the tenants** (corrected in place in `engine_internals.md` ×5, `preserved_data.md`, `docs/README.md`, two gate headers, `DECISIONS_HISTORY.md`, the skill line [VSE-69]); vsav's duration is 112 timer units x a per-character period (3 or 4 ticks, a 32-row table) plus each character's form: 269..540 frames of `$FF802E`, NOT global. On ours the EX input still enters the mode (the placed copy of `0x02622A`, period 5, the flag and background) |
| **(6) THE GAUGE IN DARK FORCE — A DEFECT, #157's TAIL** | the shells on vsavj gain nothing from a whiffed attack inside Change; the tenants on ours gain +6 (their `x028122` adder tests `+0x1C3`, vs2's Power field, never set by Change: measured 1 throughout vs2's Power, 0 throughout ours' Change). The maintainer: *"I see, it makes sense that you can't build meter during DF"*. The Power-field census (`tools/audit_df_field_readers.py`): 44 placed instructions, 26 reads (all `+0x1C3`) and 18 writes — first frozen calling all 44 "readers"; rule-checker run 2026-09-18-46 (Q4) sent it back for an access column, a fixture self-test and a live cross-check (7 reads executed by the #136 corpus, nothing it runs outside the census). One read measured. Gates **`tests/audit_df_meter.sh`**, **`tests/test_df_field_readers.sh`**, **`tests/audit_df_field_readers_live.sh`** |
| **(7) THE ENTRANCE, AND THE COMMUNITY TABLE** | our build draws Phobos's three entrances ({0,2,6} at `+0x0A`) over six RNG seeds, as native does — the 14z-167 artifact is the rig's pin, not a lost entrance (gate **`tests/audit_entrance_draw.sh`**). Mizuumi's Dark Force table (the maintainer's pointer, `../community/`): Invulnerability equals our `+0x147` arm for 11 of 15 (the other four arm late or re-arm); Duration follows no constant ratio (definition and speed unstated) — our measurement stands, the column's definition is open |
| **(8b) THE ALTERED ATTACKS, ON THE RIGHT BASELINE — THEY MATCH IN HITS, DAMAGE AND HIT FRAMES** | the in-DF rig rebuilt with per-leg activation (ours P+K, native the vs2 EX input), positions pinned far, stock re-poked, groups spaced past native's longer mode: all 20 activations entered on both legs. At 70 frames of settle Phobos's first native move was lost (his EX form comes ~75 frames in) — re-run at 110. Result over the 29 in-DF events: 10 SAME, 18 differing only in P1's gauge (the two rules: vs2's EX pays start-up and hit gauge; ours pays no hit gauge — ruled — but the tenants' start-up gauge — #157's tail), 1 other: Lightning Sword's class byte 0x4E / 0x06 (the known remap, equal damage and frame). The flying sword, the clone beams and the flame burst land the same hits for the same damage on the same frames. The RAM rows do not compare appearance: captures on this baseline (`build/p136_14z168/cap_dfx_*`, sent 13:17Z) show Donovan's sword in a different pose before its hits and Pyron's form in a different palette at activation — the maintainer's call. Gate **`tests/audit_df_moves.sh`** |
| **(9) THE EIGHT FREEZES READ VIOLATED ON ALL FIVE QUESTIONS — EVERY FINDING TRUE, RESOLVED BY WORK** | rule-checker run 2026-09-18-46 (the eight #136 expectations; plant caught) returned VIOLATED on Q1-Q5, verdict verbatim in `tests/rulecheck/runs/2026-09-18-46/`. Q1: no verify/CONTROL logs kept for five gates, and a wrong stated basis for three (df_meter has no RNG pin and its own rig; df_modes runs `df/97`; the entrance pokes the RNG once) — logs now kept for every gate, basis restated. Q2: the guard re-entry had no capture — a vsav2 | vsavj | ours sheet sent 13:11Z (`cap_guard/`), ours pixel-identical to vsavj on 13/13 frames, no fighter difference visible between the games; the gate freezes those pixel rows now. Q3: the entrance gate's sets counted the intro's initialisation 0 as a draw — it now reads each draw's variant, cross-checked against x, and the control fails the SET compare. Q4: the Power-field census called 18 writes "readers" and had no false-row or missed-reader control — access and base columns, a fixture self-test with decoys, and a new gate **`tests/audit_df_field_readers_live.sh`** (both fighter blocks tapped over the 30 naming parts: 7 reads executed, none missed; `tests/lua/read_tap.lua` extended to several ranges, all ten users re-run green). Q5: #114 now quoted verbatim; the two 2026-08-14 decisions (defense rows, trap remap) survive only as recorded decisions — no words kept, no transcript before 2026-08-19 — cited as such, and put to the maintainer. Then df_moves captured on its own baseline before its freeze was checked (`cap_dfx_*`, sent 13:17Z: the hits match, Donovan's sword pose and Pyron's activation palette differ — the maintainer's call). A latent flake found on the way: nine backgrounded MAME legs wrote their status under `set -e`, so a teardown segfault read `exited none` (measured 2/30 on read_tap) — `set +e` and the instrument's summary line decide, in all nine and in `tools/move_parity_attribution.py` (`docs/project/gotchas.md`). Follow-up run 2026-09-18-47 on the five new or re-frozen tables: VIOLATED Q1 Q4 Q5, every finding true — my "ten read_tap users" came from a grep for mentions (the eight real runners, `audit_latch_reads` included, now listed with their verdicts in `build/p136_14z168/regress/read_tap_users.txt`); the live gate trusted one write anywhere as liveness for two ranges (now both ranges per run, a `range-silent` control, the fighter block in every row, re-frozen); df_moves ASSUMED its events ran inside the mode from the rig's arithmetic (now `+0x111` measured on both legs at every event and hit, 29/29, a `mode-lost` control); and a 2026-09-17 agreement was cited without words — recovered verbatim from that sitting's transcript into `DECISIONS_HISTORY.md`. Run 2026-09-18-48 on the two changed gates: VIOLATED Q1 Q3 Q4 Q5, true again — df_moves never showed a move CAME OUT (five SAME rows with no hit and no gauge on either leg could be two legs agreeing on nothing: now P1 must act on both legs, an `idle-leg` control, the no-hit events' state paths printed — the Killshread [LK] inputs come out as a KICK on both engines in the mode, and an L button's swing cost is 0); liveness proved nothing after 2362, where the corpus stops touching the fields (now an END PROBE poke of `+0x1C9` on each run's last frame, measured inert); the shared level/RNG pins' meaning on vsav2 now stated with evidence; two rulings quoted. Resolved; rows unchanged, every control mode exits 1. Three runs on these freezes (-46, -47, -48), 12 violated questions, every one true; every plant caught. The mid-session tier (`build/p136_14z168/static_mid.log`): PASS 144 / FAIL 2. Both reds came from one row of mine: the guard_reentry PROVENANCE row named an evidence class the page does not define (fixed to `in-emulator (reference + ours)`, naming its two references). `test_bbh_fidelity`'s F9 went red with it for a harness reason: its provenance pair pipes this tree's gate through `sed`, so our side's exit reads 0 while bbh's reads 1, on a tree where both tools printed the same finding. A false fidelity difference, unfixed here (bbh `selftest/test_fidelity_vampire.sh:356`); the three dependent gates re-run green |
| **(10) THE MAINTAINER'S QUESTIONS ON THE CAPTURES — MEASURED, THEN RULED** | every-frame captures on request (`build/p136_14z168/col_full/`, `don_sword/`, both DELIVERED). The column: Donovan returns to neutral 6 frames later and Demitri 6 earlier, a recovery gap of 14 on ours against 26 natively — *"I agree with your analysis"*. The block re-entry, by the maintainer's own test (the victim attacks at its first possible frame): hit+35 on all three games — *"it's identical"*. A mash INSIDE the block window measured the advancing guard instead (vsavj fires on light presses, vs2 does not; documented). Pyron's "palette" was not measured before; measured now, identical (0x0a/0x0b) — his form's idle phase differs until the move realigns it — *"confirmed identical"*. Donovan's sword: not RNG, the idle loop 10 frames later natively, the flight path identical — *"to me it's identical"*. The defense rows compared for the ruling: the 15 legacy characters identical between the games but Sasquatch; the tenants' rows are their SHELLS' (vsavj copies base rows into variant ids); ±1–2 per hit, not ±1 — then RULED: take the vs2 rows. The trap question answered (the vs2 0x52 rule fixes the column AND removes the trap's attacker freeze) — RULED: fix. Persisted: `tests/test_defense_rows_census.sh` (new, static), `audit_column_shock.sh` (+timeline), `audit_guard_reentry.sh` (+the act test, control `act-late`), `audit_df_moves.sh` (+the form rows, control `form-moved`). The live carriers of the two superseded 2026-08-14 decisions updated; `build/manifest/huitzil.toml`'s DEVIATION comment is left for the fix commit (a manifest edit moves build fingerprints). Rule-checker run 2026-09-18-49 on the four tables (plant caught): VIOLATED Q1 Q4, true — the claim cited a sword y and differing HUD palettes no row recorded, the palette comparison had no liveness, and nothing showed the second RNG seed reached an RNG consumer. Resolved by work: rows for the sword's y, the palettes seen differing (the HUD-row ones as the comparison's liveness) and the RNG reads in the window (native 636, ours 621); controls `palette-blind` and `rng-dead`; all six df_moves controls exit 1 |
| **(11) THE TWO RULED FIXES — DESIGN FACTS READ, NOTHING BUILT** | the maintainer on the conditions: *"a total cost that doesn't end up introducing a new frame of lag (i.e. the total overhead cost of our combined changes is less than 1/60s at all times)"* (verbatim with both fix rulings; memory's performance rule sharpened). DEFENSE ROWS: both reads index by the victim's id byte (`0x18C10`-`0x18C26` masked `#$1f`; the threshold `0x18C78`-`0x18C82` unmasked), so a data-only edit of rows/bytes 0x10 and 0x13 is possible; to measure first: that every hit on a tenant victim reads the tenant's rows over the corpus (`+0x382` is the voice-flavor class in a match; own-class for tenants since 14z-87). THE 0x52 RULE: the 14z-42 thunks are `jsr`s inside vsavj's `0x06` handler `0x23AC8` (every electric hit, legacy included); vsavj routes classes 0x06 AND 0x38 to that handler with the same property byte 0x0F — remapping the column/trap records to 0x38 would let the existing thunk tell them from Lightning Sword; to measure first: a census of class 0x38 in every legacy attack/projectile record, and the combined cycle cost (no new zero-pass frame). **[MEASURED 14z-169 — the idea is FALSE three ways: the stager rewrites a 0x38 record to 6 before the reaction (nothing in vsavj produces `+0x54` = 0x38), one legacy Victor record already carries 0x38, and the guard decision sends a 0x38 record to the hit path against a standing victim where the column's 0x06/0x52 is in neither of its lists — STATE 14z-169, `tests/test_reaction_classes.sh`]** Homes: `docs/game/engine_internals.md` (the column paragraph; the defense port note) |
| **(8) THE RULINGS (verbatim in `DECISIONS_HISTORY.md`)** | the order (analysis first); the tenants' Dark Force — the maintainer's design statement, the duration stays vsavj's (*"agreed"*); the EX route on our build disabled (*"agreed"*); no gauge in Dark Force; then (row (10)) the tenants' defense rows become vs2's; the column shock and the Plasma Trap take vs2's class-0x52 rule; the conclusions on the captures (identical: the block re-entry, Pyron's form, Donovan's sword); the condition on every fix — no new frame of lag; and the 2026-09-17 in-DF rig agreement recovered from its transcript. Standing lines below |
| **CLOSE (14z-168, 2026-09-18) — THE CHECKLIST** | the maintainer asked for the close at 73% context. (1) the findings table, 26 rows with their homes and tests (`build/p136_14z168/findings_table_14z168.tsv`); four rows name OPEN ITEMS, carried to NEXT_SESSION (the threshold read and the reaction-table alias are static reads without a gate; the unsafe-leg census gate; bbh's F9 pair). (2) `tools/close_findings.py 14z-168`: 13 addresses, GAPS none, REVIEW none; one PROMISE flagged — "until the move realigns it" (row (10)) is a measured fact, frozen as `pieces=same` at f2730 in `tests/expected/df_moves.tsv`, not a promise. (3) pointers: the 14z-167 rows (3), (5) and the archived 14z-85f / 14z-85g(2) decisions carry forward markers to this sitting. (4) promises: none open beyond NEXT_SESSION's list. (5) ON TRIAL: nine measurements quoted in STATE or a document came from scratch scripts or inline runs (the attribution, the overrun, the in-DF rig, the column timeline, the act test, Pyron's palette, the sword, the defense comparison, the Power-field census) — all nine promoted into gates or tools; not promoted: `build/p136_14z168/cap.sh` (captures, no figures) and the regress batch drivers. (6) the documentation packet: rule-checker run 2026-09-18-50 (plant caught): VIOLATED Q1 Q4 Q5, true — two rows named non-tests (the set-e fix's "gates themselves", which a green run cannot show; a build/ record), `defense_rows.md` still called option (a) "rejected" under the new ruling, and it said "the maintainer accepted" 2026-08-14 without words while the 2026-09-04 tolerance ruling was unquoted. Resolved by work (rows corrected, the doc and the engine_internals port note fixed, the 2026-09-04 words quoted). NEXT_SESSION rewritten (the 14z-167 opener archived verbatim); four traps homed as gotchas. THE CLOSE TIER, first run (`build/p136_14z168/static_close.log`): PASS 145 / FAIL 1 / MISSING 1. `test_defense_rows_census` was registered but not executable (written with the file tool), and `test_static_runner` failed on the same row; `tests/audit_df_field_readers_live.sh`, already committed, lacked the bit too. The static tier never checks emulator gates for it — a gotcha and a NEXT_SESSION item. Both fixed. THE CLOSE TIER, re-run (`build/p136_14z168/static_close2.log`, 19:12:12 to 19:33:43): SESSION cadence, `--strict`, EVERY control executed — **PASS 147 / SKIP 0 / FAIL 0 / MISSING 0**, `fired 163 / declared 163`, `executed 163 honoured 163 lies 0 refused 0 died 0`, 14 deferred by cadence, 3 triggered, no tracked file changed during the run. This row's tier sentence was written after the run (the doc gates re-run over it; the tier is not). Then the commit and the push |


---

# STANDING SECTIONS (current state — never archived)
## Standing rulings

One line per ruling that still constrains work; the full entry lives where the line says. A line is deleted when its ruling stops constraining work (CLAUDE.md [VSP-17]).

- **The x2b7ef4 placeholder corruption lands with M19; merged-m18 stays as published (2026-09-19, 14z-170 — "M19 supersedes it (Recommended)"):** the corrected resolver ships in the M19 freeze with a ticket; no M18 hotfix. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-19 (14z-170) — the x2b7ef4 placeholder corruption".
- **The disabled EX route does the no-stock move (2026-09-18, 14z-169 — "Yes, the no-stock move"), refined 2026-09-19 (14z-170 — "Natural reading (Recommended)"):** the tenants' vs2 EX input is read as its sub-pattern at every stock level — for Phobos and Donovan vs2's no-stock move; for Pyron 623+PP, the ES move when a stock is available and the normal move without one. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-169) — the disabled EX route does the NO-STOCK move" and "Ruled 2026-09-19 (14z-170) — Pyron's 2623+PP with stock reads NATURALLY".
- **The 0x52 fix is scoped S1 (2026-09-18, 14z-169):** Phobos's two trap remaps stay on the solo Phobos track only (a row key: unless Donovan's manifest is in the composition); the merged build carries the full fix. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-169) — the class-0x52 fix is scoped".
- **#136: all the analysis first, then the fixes, then relentless regression testing (2026-09-18, 14z-168 — the maintainer: "let's finish all the analysis first. Then we'll fix and then relentlessly test for regressions"):** no #136 fix (nor #157, #159 or the 14z-168 tickets) lands before every family is analysed; every rig built for it becomes a gate that runs at freeze and release. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — #136: ALL the analysis first".
- **The tenants' Dark Force (2026-09-18, 14z-168):** vsav's Dark Force CHANGE with the shells' common rules, each tenant's own vs2 startup window, and its personal Dark Force (vs2's EX move) active for the mode; compared against the vs2 EX move, never vs2's P+K (Dark Force POWER); the DURATION stays vsavj's (360/377/360, *"agreed"*); the tenants' vs2 EX input into the mode on our build is to be DISABLED (*"agreed"*). Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168)" (the Dark Force and the EX-route entries).
- **The tenants' DEFENSE rows become vs2's (2026-09-18, 14z-168 — "given the measurements we should take the vs2 rows"):** Phobos and Donovan take their vs2 defense rows and rally thresholds (Pyron needs nothing), with no frame cost and no change to any legacy character; supersedes 2026-08-14 option (b). Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — the tenants' DEFENSE rows become vs2's".
- **The column shock and the Plasma Trap take vs2's class-0x52 rule (2026-09-18, 14z-168 — "I'm all for fixing"):** victim shock 0x18 and the attacker exempt, for the tenants' 0x52-origin hits; no noticeable lag, no new breakage; the 14z-85g(2) accepted deviation is withdrawn. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — the column shock and the Plasma Trap".
- **No gauge is built during Dark Force (2026-09-18, 14z-168 — "it makes sense that you can't build meter during DF"):** the shells' rule; the tenants' start-up gauge in the mode is a defect (#157's Dark Force tail). Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — no gauge is built during Dark Force".
- **The close checklist (2026-09-18, 14z-167b — the maintainer: "let's implement them and keep item 2"):** at every close, (1) a findings table — each finding of the group, its live home and its test; (2) `tools/close_findings.py <session>`, every GAP homed and every REVIEW answered, its clean result never cited as completeness; (3) a pointer on every earlier row the sitting resolves; (4) a grep for promises; (5) cited scratch scripts promoted, on trial and measured per close; (6) the documentation packet through the rule-checker. The steps live in this file's header (ruled 2026-09-18). Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-167b)" (two).
- **#136 goes over again by the 14z-164 census (2026-09-17, maintainer: "I agree with the 3 propositions"):** the move-parity gate moves to PER-EVENT verdicts with the stock, the meter fraction and P2's HP compared and every "in DF" event asserted in DF; a P2 whose data is the same on both games (a second tenant, or a legacy character byte-identical between vsavj and vs2, found statically) — done 14z-165, Demitri; the 13 rows re-labelled by their fields on #136 before any family is root-caused. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-164)".
- **The rule-checker's Q3 and Q4 are BOUNDED, and a VIOLATED is resolved question by question in writing (2026-09-17, 14z-163, #152 — the maintainer: "both"):** Q3 counts only a shared thing the artifacts SHOW can write the compared state; Q4 reads OK when the claim itself names the uncovered mode; what a bounded question still finds stops the action until `rulecheck resolve` answers each violated question. Every fixture recalibrated on the new wording. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-163) — the rule-checker's questions".
- **The rule-checker runs on the default model (2026-09-17, 14z-163):** a change of model is a recalibration. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-163)"; the "#152 closes after a first real run" half of that line was met and the ticket closed by ruling 2026-09-17 (14z-164b).
- **#136 is gone over again IN FULL, not only through its faithfulness control (2026-09-16):** the maintainer insists it stays on the list until every part of it — the naming rigs, the pinning protocol, the 27 frozen verdicts, and the Pyron and Phobos legs above all — has been re-examined; no #136 verdict is built on until then. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-16 (14z-160)".
- **The M19 freeze is WITHDRAWN (2026-09-16):** #147's fix was a regression on a rig artifact; the tree is back at merged-m18 and #151 re-examines #136's forced-pick native legs. No freeze is current beyond merged-m18. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-16 (14z-159)".
- **The static tier runs by CADENCE (2026-09-17, #148 lever A):** `tests/ci_cadence.tsv` lists twelve build/content/tooling gates as `freeze` (the seven costliest plus the five 35-39 s gates, second cut) and five release/MiSTer gates as `release` (the three ≤2 s ones back to session); a session run (the default, mid-session AND at the close) defers them BY NAME unless a path they depend on changed; a freeze runs `--cadence freeze`, a release `--cadence release`, each on the commit it builds from. A new listing is a reviewed event. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-162) — #148 lever A".
- **Static-tier controls at the CLOSE (2026-09-14; REAFFIRMED 2026-09-17 after 14z-161 pushed on the mid-session form):** a mid-session commit runs `tests/run_all_static.sh --strict --exec-controls none` plus `CONTROL=<name>` for every gate it adds or changes; the session close runs every control (the default) and ONLY then pushes — not to be compromised until #148 is worked. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-14 (14z-154)" and "Ruled 2026-09-17 (14z-161b)".
- **The order of work (2026-09-17):** a stable, reliable state first, then #148 (the static tier's cost, done), then #152 (the adversarial rule-checker, done 14z-164b), then everything else — the #136 families included. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-161b)".
- **Release scope (2026-09-02):** at release every test whose SUBJECT is the released artifact runs, its legacy content included (a reference leg on vsav2 or pristine vsavj is not the subject); anything red or skipped is a hard fail unless approved at release time, so a release-scope gate fails loudly on a missing prerequisite. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **A red gate is a question (2026-09-02):** before fixing the gate, fixing what it caught or deleting it, establish which side's expectation rests on a measurement; a frozen number whose provenance cannot be named is a claim. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Cross-game comparisons at a matched speed level and a pinned RNG (2026-09-15, #135, #142; amends the 2026-09-02 #114 line):** vsav2 defaults P1 to TURBO and vsavj to NORMAL, and the two games' RNG states differ, so `tests/test_don_immortal_native.sh` pins `RAM:$FF8116` (levels 6 and 8) and `RAM:$FF80D4-D5` on both legs and asserts ours == native in hit count, damage and hit frames; LP's accepted "one hit short" is retracted as a mode artifact and #142 is closed invalid; the 2026-09-02 ruling that the vanilla vsav engine takes precedence stands. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-158)" and "Moved 2026-09-14 (14z-154)".
- **No static substituted-wheel gate (2026-09-02):** declined — the pattern matches 26 legitimate gates; the defence is [VSP-163] (assert `+0x60` against `bases.tsv`, or force the pick). Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Oboro's intro stays (2026-08-28):** never delay round start or cut the intro for a cosmetic reason. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Community data (2026-08-31):** measurement is king — a community source that matches ours exactly or by a constant offset validates our measurement, and an inconsistent pattern means re-check OUR measurement first. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Cosmetic is optional (2026-08-27):** cosmetic and single-player-only surfaces are nice-to-have; competitive 2P versus is the focus. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **No legacy character meets a tenant in 1P arcade (2026-08-28):** not a problem, 2P versus is the focus; not built, not scoped. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **The living-docs generalisation, when scheduled (2026-09-08, #119):** independent of bbh but compatible with it and on the same principles; the site generator in scope; fidelity required in principle, its form argued in the scope document; the open items first. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Linux release binaries (2026-09-13, #121):** self-containment is checked against an external host-provided list plus the ruled exceptions; the WSL2 Linux binaries are never published. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Generator refactor policy (2026-08-21, #50):** no dedicated refactor of `tools/gen_donovan_patch.py`'s `main()` is ever scheduled absent a new measured cost; a handler moves to module level only when a test needs to drive it, with its unit test in the same change, and byte-identity under `tests/test_m3a_reproducible.sh` and `tests/test_phasec_spaces.sh` is the refactor gate. Full entry: `STATE_HISTORY.md` "Session 14z-102 (post-freeze rulings)".
- **Don't assume; measure first; ask if necessary (2026-09-15, clarified 2026-09-16):** no factual claim — about code, a count, a behaviour or a name — is stated without a measurement or a tree source behind it; a question a measurement can settle is measured first and never published as "unreconciled"; asking the maintainer is right when a measurement cannot settle it or is not enough. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)" and "Ruled 2026-09-16 (14z-158b)".
- **Pipeline-image registry rows (2026-09-15, #96):** the M2 battery dispatches through `tests/expected/registry.tsv` on the build's fingerprint, never a pinned set name; `donovan-mN-stock` / `-stage4` register its two pipeline images (untagged, never shipped); each freeze rebuilds both and records whether they moved, and an unregistered fingerprint is a rule-6 stop. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)".
- **The merged set carries no tenant expectations (2026-09-15, #111; the 14z-133b B2 design):** the merged expectation set holds the legacy classes, the mask and the `.skip` markers; tenant-content `.sha1` expectations live in the solo sets, and the merged build's tenant content is covered by `audit_merged_legacy` leg (b) and the tenant gates. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)".

## STANDING PRINCIPLE (maintainer, 2026-08-05): vanilla wins ties

**[VSP-21]** "vsav vanilla is always better when we can." **When a console port and
arcade vsav differ and both would work, take vanilla.** A console port's
choice is not evidence that vanilla is wrong; it is evidence of what that
port's designers preferred.

This is a general rule, not a one-off: the PS1 capture is a reference for
what is POSSIBLE and for data we cannot otherwise obtain (cell placement,
the adjacency of NEW cells), not a style guide for content vsav already
defines. Paired with the maintainer's other statement — "as long as we can
select characters it's good" — the test is: does keeping vanilla still let
the feature work? If yes, keep vanilla.

Applied immediately, twice:
- **`Bishamon DL` and `Aulbath DR` stay vanilla** (Anakaris / Sasquatch).
  PS1 sets both to "no move"; neither is needed for reachability, so
  vanilla stands.
- **Horizontal wrap stays vanilla.** Vsav wraps left/right (cell `0x01`
  Left goes to `0x05`, measured and confirmed in-emulator); the PS1 report
  of "no wrapping" reflects untested extremes. We touch none of those
  cells, so nothing to decide.

Judgment applied under the same rule, open to veto: the three inbound edges
from `0x0B` (`D`/`DL`/`DR` into the new row) DO diverge from vanilla, and
strictly they are not required — Phobos and Donovan are already reachable
via `Bishamon D` and `Aulbath D`, and Pyron through them. They are kept
because without them, pressing Down on the cell directly above the new row
does nothing while three medallions are visible below it, which is the UX
failure "as long as we can select characters" is meant to exclude. Dropping
them would reduce the legacy footprint from 5 bytes to 2.

## Decisions pending (human)

Only undecided items live here; a ruling moves its entry to `DECISIONS_HISTORY.md` in the
commit that records it (CLAUDE.md [VSP-17]).

*None open.*

## THE DEADNESS REGISTER (opened 14z-71, maintainer's standing instruction)

**[VSP-23]** Every claim of the form **"legacy never reaches this, so we may reuse
it"**. Each is measured by ABSENCE, which is the weakest kind of evidence
we accept, so each is listed here with its guard. **These are the FIRST
PLACES TO CHECK for any unexplained regression in vanilla assets, engine
behaviour or rendering** — before anything else is suspected.

| Reused resource | Claim | Guard | Fallback if wrong |
|---|---|---|---|
| ~~palette-seq ids 0x1E-0x21 (`0x39ACC0`)~~ **CLAIM FALSE, ROW WITHDRAWN 14z-79 (they are Bulleta's DF block)** | vanilla only ever requests 0x26/0x27 | `tests/audit_palette_seq_ids.sh` (10,504 sampled calls) | none — the palette path never transits work RAM, so the audit is the ONLY guard |
| effect-class row 16 (`0x080AEC`) | vanilla never dispatches class 16 | `tests/audit_effect_class_rows.sh` §1, 0 reads vs a 1760-hit control | none needed: the row was a stub (`rts`), so a wrong claim costs at most the old no-op |
| ~~drawer list-type 6 (`0x01B6AA`)~~ **CLAIM FALSE (measured 14z-89) — LEGACY LISTS DO REACH TYPE 6** | vanilla has no type-6 sprite lists | `audit_effect_class_rows.sh` §1/§4 + `tests/test_beam_list_type6.sh` | **THE FALLBACK HELD — this is what a safe-and-loud design buys.** 14z-89 measured the tripwire ARMED on legacy content on huitzil-m13: `21_don_mash` 387 times and `26_don_arcade_mash` 948 times, PC-attributed to inside the thunk body (0x0FD060). Rendering stayed correct throughout (the fallback runs vsav's own type-6 code, reproduced instruction-for-instruction), so nothing rendered wrong and no playtest ever saw it — exactly the outcome the register's "prefer designs where being wrong is safe and loud" rule was written for. WHY IT WAS MISSED: the deadness measurement was sound but its COVERAGE was four replays (`02/07/09/30`), and the gate has always run on that default set; the two replays that arm it are long mash/arcade rigs nobody pointed it at. COST TODAY: `$FF010C/$FF010D` is a live work-RAM counter vanilla does not keep, so both replays diverge permanently from the vanilla masked basis — they are `.pending` on huitzil-m13 pending the maintainer's ruling. ~~OPEN: does the fallback need to stop counting (make the tripwire diagnostic-only / move it out of work RAM), or is the counter acceptable? See "Decisions pending — 14z-89"~~ **ANSWERED 14z-91: the counter was REMOVED** — the `beam_list_type6` `RAM:$FF010C` counter deleted, which cleared all six `.pending` legacy replays (`HANDOFF.md` Build registry, the 14z-91 row (C)) |

**[VSP-22]** Rules for adding a row: the claim must be measured with a POSITIVE CONTROL
on the same instrument and leg (a blind instrument and a real zero look
identical — paid for three times in 14z-71); it must name its guard; and
it must say what happens if the claim is wrong. Prefer designs where being
wrong is *safe and loud* over designs that are merely well-measured.

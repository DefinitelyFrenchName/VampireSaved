# STATE — living progress log

**SPLIT 2026-08-20 (14z-99 post-freeze close, maintainer-approved), AMENDED
2026-09-14 (CLAUDE.md [VSP-17] and [VSP-182]): this file holds the RECENT
session groups and the STANDING SECTIONS; the full detail of every older
session lives verbatim in `STATE_HISTORY.md`, under THE LEDGER at its head.**
How to work with it:
- **Lookup**: "STATE 14z-XX" references resolve here first, then in
  STATE_HISTORY.md (its LEDGER first) — section names are preserved verbatim
  in the archive. A reference to `STATE "Decisions pending"` for an entry no
  longer here resolves in `DECISIONS_HISTORY.md` (entries move there verbatim
  when ruled — since 2026-09-14; before that, once they stopped shaping work).
- **Claim-greps MUST include STATE_HISTORY.md** (the CLAUDE.md §5
  retraction-discipline command names it).
- **AN OPEN LIST HOLDS ONLY WHAT IS OPEN (maintainer-ruled 2026-09-14).**
  "Decisions pending" holds undecided items only; nothing closed stays in it,
  however it is marked (struck through, DONE, FIXED, CLOSED). Where each thing
  goes instead:
  - a RULED decision -> its whole entry moves VERBATIM to
    `DECISIONS_HISTORY.md` in the commit that records the ruling; while the
    ruling still constrains work, ONE line stating the rule sits under
    "Standing rulings" here, and the line is deleted when it stops;
  - a BUG, COSMETIC ITEM or EVOLUTION is a TICKET — listed in
    `docs/project/tickets.tsv`, its story on its GitHub issue, never listed
    here; a STATE entry for one that closes moves VERBATIM to
    `STATE_HISTORY.md` under the closing session's key (CLAUDE.md [VSP-182]:
    the four questions, the closing comment);
  - "STANDING PRINCIPLE" and "THE DEADNESS REGISTER" carry the [VSP-21..23]
    anchors and stay; the register keeps its withdrawn rows by [VSP-23].
  `tests/test_state_open_lists.sh` enforces it, with the size budget and the
  group count below.
- **ROLLOVER RULE (part of the session-close ritual)**: after writing the
  close entry, move session groups beyond the newest THREE to the TOP of
  STATE_HISTORY.md's body in newest-first order (below THE LEDGER, above the first
  archived `## Session` heading OLDER than the group)
  and add their one-line entries at the top of THE LEDGER there, composed from
  the group's own banner headers. If this file still exceeds ~150 KB, roll the
  oldest kept group early. The standing sections never roll.
- **THE LAST STEP OF THE CLOSE IS THE PUSH (maintainer-ruled 2026-09-10):**
  static tier strict green WITH every control executed (the default `all`), the doc checks green, nothing pending -> `git push
  origin main`; anything red or skipped leaves the commits local and the
  close entry says so.

## Session 14z-157 — **THE STALE LIVE CLAIMS CORRECTED; RAM:$FF8130's WRITERS MEASURED (five direct writers, the 14z-64 taps' two on the neighbour byte); EVERY ITEM THE #75-#114 THREADS LEFT RULED AND CARRIED
## (#141 filed, #96's rows ratified, kind labels on #75-#114, nine lessons housed), AND CLAUDE.md [VSP-24] RESTATED. No shipped ROM byte moved.**

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, NEXT_SESSION — at `docs/NEXT_SESSION.md`), `main` == `origin/main` at `5bf0f07d`, bbh at `37db3ac`, nothing running. The maintainer asked how best to edit the README and committed it themselves: `e09bef2e` (pushed) and `30020ac7` (local), `README.md` only. Then: *"check if good on your end, then start the following item — 2. Stale live claims"* |
| **(1) THE STALE LIVE CLAIMS** (`e4fbb4a1`) | each checked at HEAD against its ticket's own thread: `venue_assets.md` §2 addendum's "DORMANT in every measured flow" (#100's next-stage screen shows the fold's signature), `test_pod_black_foot_palette.sh`'s "still unknown" (answered 2026-09-02), `audit_effect_rects.py`'s #112 attribution (retracted in #112's thread the same session), `test_don_reactions.sh`'s "MASHING is NOT covered" (section 5 of `test_don_immortal_native.sh` since 14z-127 (5)), `test_voice_row_range.sh`'s voice-class reading (#92, 14z-94; the filename kept — *"we don't want to break things needlessly"*), HANDOFF's "no `.rbf` on a DE10-Nano" and `mister_core.md`'s "Still never: on HARDWARE", and HANDOFF's current-build and runtime figures. GitHub: #77's `contested` removed (*"I agree with removing it"*); #112's 14z-154 closing note corrected (*"Yes, correct please"*) |
| **(2) RAM:$FF8130's WRITERS, MEASURED** (`e4fbb4a1`) | I had put "which writer count is right?" to the maintainer and written "unreconciled" into `venue_assets.md`; the maintainer: *"the question can be settled by measurement rather than memory -> then it must! We measure, we don't believe or assume and we don't compromise on the discipline"*. Measured: an every-offset decode of the vsavj opcode view finds five direct writers (the fold's store `PRG:0x00A446` and four constant writes #100's scan could not see); a word tap bucketed by write mask puts the 14z-64 taps' two writers on `$FF8131`; none of the five fires in boot, attract, select, a match or a 1P arcade run. `tests/audit_ff8130_writers.sh` (emulator tier, ~40 s; `one-form-scan` and `lane-blind` FIRED in-gate, both modes exit 1, an undeclared name REFUSED); `ram.md` rows for `$FF8130`/`$FF8131`; the tap-width gotcha's mirror case; the checkdocs_rom denominator 346 -> 364 (the ruled definition, the measured count); bbh `833f7a7` and `13ff687` (its copy of the gate-index page prose, which F9 compares) |
| **(3) THE #75-#114 RULINGS** (`339c1513`) | four read-only agents prepared a decision sheet and every load-bearing claim was re-checked at HEAD and on GitHub before it reached the maintainer. RULED — A, B, D *"I agree with all recommendations"*; C *"Issue 77, 92, 93, 95, 100, 101, 102, 112 I agree with the proposals"*, #91 *"Link [VSP-20]"*: #78 -> #141 (the full FBNeo legacy track, parked); #80, #93 (both residuals), #103, #111 declined; #100 folded into #125, #109 into #136; #96's pipeline-image rows ratified; #90's ruling copied; the #78/#108/#112 calls confirmed; #101 and #105 cosmetic; the kind labels on #75-#114 applied and read back; nine lesson carriers (two new gotchas: a MAME breakpoint condition that does not parse sets no breakpoint, and two symptoms are one defect only if the mechanism predicts both); CLAUDE.md [VSP-24] restated (*"agreed"*); the 14z-85f flaky reset recorded as the maintainer's testimony (*"I'm 99.9% sure the problem was Donovan, not Sasquatch"*). `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)"; three "Standing rulings" lines |
| **TRAPS PAID** | the first retraction grep matched the claims' wordings, not their paraphrases: `id_space.md`'s "written ONLY here" and `ram.md`'s `$FF8114` voice-class row survived it, and a read-only agent found them; I told the maintainer the portable tier took ~1 min (HANDOFF's stale figure) and their run took 18.5 min with its controls; the first strict tier went NOT GREEN, 152 / 0 / 2, on my own edits (the checkdocs_rom denominator, and bbh F9 comparing a copy of page prose I had changed on one side); the new audit's first run died on `dict(**{int: ...})`; a scratch-copy index check cannot resolve a link to a row that exists only in the scratch copy; `ps \| grep '[x]'` inside a `zsh -c` line matches the shell's own command line |
| **CLOSE (2026-09-15)** | the close tier, every control executed, after every edit of this close (`build/gates_14z157/static_close.log`, started 19:37:23, wall 2,588 s): **PASS 154 / SKIP 0 / FAIL 0 / MISSING 0**, `fired 172 / declared 172`, `executed 172 honoured 172 lies 0 refused 0 died 0`, no tracked file changed during the run; this row was written after the tier. STATE rolled: the 14z-154 group to the archive with its ledger line; NEXT_SESSION archived and rewritten; `14z-157` added to the sessions of the 21 tickets this sitting worked on; the GitHub snapshot refreshed (unchanged). This sitting's commits: `e4fbb4a1`, `339c1513` and this close; bbh `833f7a7` and `13ff687`; the maintainer's `e09bef2e` (pushed at the opening) and `30020ac7` (`README.md`). **The close ends with a push** ([VSP-17]): bbh first, then `main` |

## Session 14z-156 — **THE bbh F1 FLAKE FIXED AND 14z-155 PUSHED ON A GREEN TIER; #140 FILED (the static runners' must-fire readout overstates on a red run); THE BACKFILL FINISHED (#75-#114, debt 40 -> 0);
## #135 STARTED — vsav2 doubles its logic pass every third frame, vsavj every fourth or fifth, globally and inside one game-task activation; the decider not yet located. No shipped ROM byte moved.**

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, NEXT_SESSION — at `docs/NEXT_SESSION.md`), `main` 7 ahead of `origin/main` at `59f67c85` (14z-155 not pushed), bbh clean at `e7d6767`, nothing running; ROM audit **76/76**. The maintainer: *"yes, item 1, then file a ticket for item 3, then item 4, then item 5. I'll do item 2 later"* |
| **(1) THE F1 FLAKE — THE SELECTION WAS NOT MASKED** (bbh `6e11c48`, `37db3ac`, pushed) | the kept failure log (`build/gate_failures_static/test_bbh_fidelity.log`): the runners printed the costliest-controls list as `g_xhon · g_cfired · g_xdied` against `g_cfired · g_xdied · g_xhon`. Both run the same code (`run_all_static.sh:456-460` = `bbh-run-static:325-329`); the list is the top three by summed WHOLE seconds, ties by name, so its PICKS and ORDER follow the clock as its figures do — a different gate picked is the same class as the reorder 14z-155 named. F1's `norm()` now maps each `<gate> Ns over <n>` on that line to `G Ns over N` (count and format still compared); prototyped on the two real lines (the old normaliser reproduces the red, the new equates them; 3-vs-2 entries, a malformed entry and another separator still differ; BSD and GNU sed agree); F1 checks the normaliser itself. `bbh selftest` PASS 32 / SKIP 2 / FAIL 0; the fidelity test run directly PASS (`build/bbh_fidelity_14z156.log`); the dated `docs/rebaselines.md` line |
| **14z-155 PUSHED** | strict static tier with every control over the 14z-155 commits and #140's (`build/static_14z156_push.log`, started 00:12:25, wall 2,348 s): **PASS 154 / SKIP 0 / FAIL 0 / MISSING 0**, `fired 172 / declared 172`, `executed 172 honoured 172 lies 0 refused 0 died 0`, no tracked file changed; `795760ca..5ee1643a` |
| **(3) #140 FILED** (`5ee1643a`) | *"file a ticket for item 3"*. At HEAD: `vs_classify` (`tests/lib/classify.sh:68`) runs the controls reader only on PASS, so after FAIL/SKIP/TIMEOUT `VS_CTL_*` hold the previous gate's values and `run_all_static.sh:176-180` re-adds them (only UNDECLARED is gated on PASS); line 192 picks a failing exit-0 gate's label from the stale verdict; the harness carries the same lines; `run_all_emulator.sh:604-609` reads afresh. REPRODUCED on stub gates through both runners (`g_decl g_fail` 2/2, `g_decl g_fail g_fail` 3/3, `g_none g_fail` two declaring none, the shell-crash label). Issue #140 (bug, open, not started); snapshot refreshed (140); the row by script, checked on a copy while the real index without it FAILED; a project gotcha is the local record |
| **(4) THE BACKFILL, SECOND HALF** (`d8857bf5`) | 40 threads saved (522 KB) and read to their closing comments by four read-only agents, each proposal checked at HEAD (link paths exist and their text is on one line; `learned`/`wrong` live only; every key resolves); the TSV written by a merge script (only all-`?` debt rows; titles byte-kept; 169 lines byte-identical), checked on scratch copies, re-merged from the real files cmp-identical before the copy. bug 36 / cosmetic 3 / evolution 1; done 34, declined 2, not-ours 2, invalid 2. `test_tickets` PASS with its six controls fired, all six modes exit 1. Mid-session tier `--exec-controls none` (`build/static_14z156_backfill.log`, started 00:55:00): **PASS 154 / SKIP 0 / FAIL 0 / MISSING 0**, no tracked file changed. The residuals and gaps the threads left are in the commit message and NEXT_SESSION, for the maintainer |
| **(5) #135 — THE TICK CADENCE, STEPS 1-2** (`4042351b`; issue comment) | STATIC: the address-free windows around vsavj's tick routine and node entry occur once in vsav2 at `-0xDAC`; the frame interrupt, the main loop and the task scheduler (`PRG:0x0011A4`, 16 slots at `$FF025C`) are identical code in both games, so a second pass must come from inside a task activation. MEASURED (write taps, no debugger, the Victor mirror of `test_don_immortal_native` §4, frames 2300-2900, reference MAME): vs2's live tick writer `PRG:0x0271C4`; ticks vsav2 501/651 vs vsavj 465/615 (+36 on both fighters, all as double-tick frames); the double tick GLOBAL (both fighters, same frames); gaps vsav2 3 ×113 (`211`, ~1.33 passes/frame) vs vsavj 4 ×53 / 5 ×26 (~1.23); the game-task slots dispatched at most once per frame on double-tick frames too (both passes in one activation); P2's hit-freeze drains 11 in NINE frames on both games — §4's "9 vs 10" is a different measure. NOT LOCATED: the decider — ~~not keyed on `$FF8080`, no bit or value of `$FF8000-$FF83FF` predicts it, and the all-RAM accumulator search is NOT a result (its planted control never ran: no numpy on this Mac)~~ **all three quoted before any had found a planted case; re-run with plants at the close check, (6): ruled out**. Captured as `tests/audit_tick_cadence.sh` (emulator tier, out/romset, ~~~7 min~~ **49 s** — corrected at the close check, (6)): PASS, `twin-pc` FIRED in-gate and FAILs as a mode, a bogus name REFUSED; registered, census frozen (96 declaring); the measured addendum in `engine_internals.md`, flagging the paragraph above it as under re-examination — the carrier corrections stay with #135's closing step, as its plan says |
| **(6) THE CLOSE CHECK — A [VSP-18] GAP, TWO OVERSTATED CLAIMS AND AN UNMEASURED RUNTIME, FIXED** (maintainer: *"check that the close ritual has run, if it hasn't, run it"*) | the mechanical steps verified: both repos in sync with nothing local, three session groups, 14z-153 archived with its ledger line, NEXT_SESSION archived and rewritten, `test_state_open_lists` and `test_tickets` PASS, a fresh `tools/tickets.py refresh` leaving the committed snapshot unchanged, nothing running. NOT RUN: [VSP-18] — the per-frame work-RAM dump probes and their three searches were never captured under `tests/`, and the engine_internals addendum quoted the frame-counter modulus and the bit/value predictor as results although neither had found a planted case (and "no modulus" without its bound). FIXED: section B of `tests/audit_tick_cadence.sh` — per-frame dumps of all work RAM on both games, `RAM:$FF8080` stepping +1 as the alignment control, and each search required to find a case planted in the real dumps (modulus 3; bit 3 of a constant byte; a byte and a word accumulator with pauses, 43 carries each) before its negative counts: every plant found, all three negatives hold on both games; the bullet corrected in place, the modulus bounded to 2..64. AND A RUNTIME NEVER MEASURED: the committed `~7 min` (and the rewrite's `~12 min`) was read from MAME's emulated-time line — the audit takes **49 s** wall, measured solo; header, registry note, gate index and row (5) corrected. `twin-pc` FIRED in-gate and FAILs as a mode (exit 1); a bogus name REFUSED |
| **(7) AFTER THE CLOSE — A README DRAFT, AND THE STALE SENTENCE IT SURFACED** | at the maintainer's request (*"greenfield a new README … Don't commit it, I'll look at it and greenfield my own from it afterwards"*; then *"keep it scratch, not added, do not modify the existing README.md"*): `README.greenfield.md` beside `README.md`, UNTRACKED and never added, `README.md` untouched — built from the SMS, VS, BBH and BBX READMEs, every fact checked against the release's package READMEs, the binary records, the release's asset list, a fresh count of the project's scale, the ticket index and the select-screen atlas; its deviations section lists every player-visible deviation by kind, each pointing to its ticket or document (the maintainer: *"players deserve to know upfront what is a feature, not a bug, but let's not copy-paste details that might go stale"*). It SURFACED A STALE SENTENCE: `docs/game/atlas/select_screen.md` still said the random cycle cannot reach the tenants and that neither select path is exercised by any replay — the 14z-116 state, false since 14z-117 (`tests/test_random_select_tenants.sh`) and for the copy path (`113_shadow_vs_tenant` under `tests/test_shadow_tenant.sh`). Corrected in place (*"we'll need to update the document then"*); the retraction grep leaves only the 14z-117 correction's own quote of the old claim and one archive line |
| **TRAPS PAID** | the first accumulator search demanded a strictly constant step on EVERY frame, which rejects an accumulator that pauses on the zero-tick hit-freeze frames — widened, and still not validated; a validation script assumed numpy, absent here, so the synthetic control never ran and the negative stays a non-result; a static add-then-bcc scan found 0 sites with no known-present case to prove it could see one; `grep -P` is not in macOS grep; MAME's `Average speed … (48 seconds)` is EMULATED time, and a gate runtime read from it came out ~10× too long; a README line blended a vanilla mechanism (the arcade ladder can send a later match to an earlier stage) with the look the shells give it (the maintainer: the tenants make it look like pinballing between stages) — removed, 1-player mode never in scope |
| **CLOSE (2026-09-15)** | the close tier, every control executed, after every edit of this close (`build/static_14z156_close.log`, started 01:29:12, wall 2,327 s): **PASS 154 / SKIP 0 / FAIL 0 / MISSING 0**, `fired 172 / declared 172`, `executed 172 honoured 172 lies 0 refused 0 died 0`, no tracked file changed during the run; this row was written after the tier. STATE rolled: the 14z-153 group to the archive with its ledger line; NEXT_SESSION archived and rewritten; #140's `sessions` gained 14z-156. This sitting's commits: bbh `6e11c48` and `37db3ac` (pushed); `5ee1643a` (pushed with the 14z-155 commits on the first green tier, as NEXT_SESSION prescribed); `d8857bf5`, `4042351b` and this close. THEN THE CLOSE CHECK, (6): its edits (the audit's section B, the corrected bullet, row (5), NEXT_SESSION, the runtime carriers) verified by a second close tier, every control executed (`build/static_14z156_close2.log`, started 08:24:46, wall 2,364 s): **PASS 154 / SKIP 0 / FAIL 0 / MISSING 0**, `fired 172 / declared 172`, `executed 172 honoured 172 lies 0 refused 0 died 0`, no tracked file changed during the run; committed as the close-check commit and pushed, the #135 correction posted. THEN (7): the `select_screen.md` correction and row (7) verified by a third close tier, every control executed (`build/static_14z156_close3.log`, started 09:36:37, wall 3,362 s): **PASS 154 / SKIP 0 / FAIL 0 / MISSING 0**, `fired 172 / declared 172`, `executed 172 honoured 172 lies 0 refused 0 died 0`, no tracked file changed; committed and pushed at the maintainer's word (*"push when green"*); `README.greenfield.md` stays untracked. **The close ends with a push** ([VSP-17]) |

## Session 14z-155 — **THE 74 AUDIT ROWS ANSWERED FROM THEIR OWN THREADS (the ticket debt 114 -> 40); FOUR TICKETS FILED AND TWO OF THEM CLOSED THE SAME SITTING (#136 parked, #137 and #139 hardenings in, #138 opened);
## #50'S POLICY GIVEN ITS STANDING LINE, THE 74 AUDIT ISSUES KIND-LABELLED, THE MAINTAINER'S README MERGED AND ITS MISSING SECTIONS ADDED. No shipped ROM byte moved.**

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, NEXT_SESSION — it lives at `docs/NEXT_SESSION.md`), `main` == `origin/main` at `c3fc435c`. NEXT_SESSION's first item, the backfill; the maintainer: *"Start the backfill with the 74 audit rows, be extra cautious with TSV, they break easily"* |
| **THE AUDIT BACKFILL** (`fcf8ff1a`) | all 74 threads saved and read. The dispositions table is a 14z-95 SNAPSHOT — it lists 41 findings "open" while GitHub had closed all 73 — so every row was answered from the issue's CLOSING COMMENT checked against HEAD; sessions from git (every commit a thread cites, resolved to its subject's key); `learned`/`wrong` link LIVE carriers only (gate headers, tool and patch comments, gotchas, CLAUDE.md, `.gitignore`), the dispositions doc being HIST. 71 done, #45 invalid, #50 declined, #56 duplicate; 68 bug, 6 evolution. The TSV was written by a script (9 fields asserted, every other line byte-identical, checked on a scratch copy before the real file). `tools/tickets.py`'s session resolver learned the `##`/`###` sub-entry heading: 14z-90, the adjudicating session, resolved nowhere; 34 keys resolve only by that form, an invented key still nowhere |
| **#136 FILED, NOT STARTED** | the maintainer asked whether the VS2 tenants' other moves were ever checked against VS2 on the WIDE build. A read-only census answered NO: throws and captures are the only move class swept (640 cells); the two move batteries against native run on the STOCK twin (Donovan) and the SOLO freeze (Phobos); Pyron has none. *"file it but don't start it"*: `parked`, a GAP row and gap-list entry 4 in `docs/project/coverage_matrix.md`, #135 first |
| **#137 — THE REFERENCE GUARD PROVES ITS PREDICATE** (`cb1ca29d`) | the #63 residual of 2026-08-16, re-examined at the maintainer's request: not done, still applicable. `tests/test_wide_profile.sh` trusted a NEGATIVE from `strings -a … \| grep` that nothing in the run proved could see the profile. *"yes"*: one `carries_profile`; must-fire `contaminated-ref` (known-bad) and `blind-predicate` (shadow-tool); the full gate PASS (12 replays), both modes exit 1, `CONTROL=bogus` refused. `tools/setup_fbneo.sh` unchanged — its `WIDE=0` path has an independent source-level check |
| **THE KIND LABELS** | *"do it"*: `bug` on 68 and `evolution` on 6 of the 74 audit issues, from the index's `kind`, added only, every one read back |
| **THREE RESIDUAL RULINGS** (`2b56f6bb`) | *"#1 : trap variant / #55 : open the ticket / #50: add the line"*. **#139**: `tools/rompath_reject.sh`, sourced by both builders — any exit before the final OK moves `rompath` to `rompath.REJECTED`, and a status 0 that never reached the disarm becomes 1 ([VSP-176]); `tests/test_rompath_reject.sh` (sh, bash and dash; the wiring of both real builders; must-fire `rename-disabled` and `disarm-early`); HANDOFF's troubleshooting item. **#138** opened: a stock-track build directory without its `vsavj.zip` dispatches `run_suite.sh` to the vanilla expectations (measured, no emulator; recorded under [VSP-106]). **#50**: its line under "Standing rulings" |
| **THE HARNESS TICKET** | *"write that small ticket but don't do it"*: BBH-frame-based #1 — F9's demand-after-trap pair discards the lineage side's findings, so it can only agree on a clean tree |
| **THE MAINTAINER'S README** (merges `77b2cbe8`, `47c1a1e7`) | two remote updates merged as MERGES, so the hashes cited on #137 and #139 stay valid; reviewed against the tree (the untestable claims, the stretch-goal wording and several typos went in the maintainer's second update). *"I'm ok with the commit"*: "Get it and play it", "Report a problem" and "Find your way around the project" added, every fact from the release, the shipped READMEs and `BINARY.txt`, the MAME recording command smoke-tested on the release `cps2` (record and playback exit 0). The readability proposals are next session's, in `docs/NEXT_SESSION.md` |
| **TRAPS PAID** | a strict tier started INSIDE a background task died with it under the harness's low-memory kill (0 s FAILs on the runner's deleted work directory) — relaunched from a foreground call; a watcher that left its loop unprinted lost a red run's second FAIL; the new gate's own `ok` message printed `\${VAR:?}`, and `test_demand_after_trap` (a text scan) and `test_bbh_fidelity` F9 went red — one tier spent; the red run read `fired 175 / declared 175` where the headers give 172: `vs_classify` reads a gate's controls only on PASS and the runner re-adds the previous gate's counts — verdicts unaffected, not filed, awaiting the maintainer; I told the maintainer the orange P2 sword was "recorded nowhere" — `docs/project/patch_index.md` records it as the medallion fix's accepted trade, and my search had skipped that file |
| **CLOSE (2026-09-14) — NOT GREEN, NOT PUSHED** | the close tier (strict, every control executed; `build/static_14z155_close.log`, 22:54:42 -> 23:39:04): **PASS 153 / SKIP 0 / FAIL 1**, `executed 172 honoured 172 lies 0 refused 0 died 0`, no tracked file changed during the run. The one red is `test_bbh_fidelity` F1: the tree's runner and bbh's printed the `(costliest controls: …)` list in a different ORDER (`g_xhon · g_cfired · g_xdied` against `g_cfired · g_xdied · g_xhon`). Both sort by whole seconds summed per gate; F1's `norm()` masks each figure as `Ns` but not the order the figures produced, so a synthetic control crossing a second boundary in one run and not the other reorders the list — likelier under a controls-executing tier's load. Not caused by this sitting's changes: the gate re-run alone on the idle machine PASSED (`build/bbh_fidelity_14z155_rerun.log`). The same readout's `fired 175 / declared 175` (the headers give 172) is the ledger defect of the TRAPS row again, the failing gate inheriting `test_mister_page`'s 3. **The maintainer, 2026-09-14: *"Commit, don't push"*** — the README commit and the close commit are LOCAL; the bbh fix, a green close tier and the push open the next session. STATE rolled: the 14z-152 group to the archive with its ledger line; NEXT_SESSION archived and rewritten; this row and NEXT_SESSION's first item were written after the tier |

---

# STANDING SECTIONS (current state — never archived)
## Standing rulings

One line per ruling that still constrains work; the full entry lives where the line says. A line is deleted when its ruling stops constraining work (CLAUDE.md [VSP-17]).

- **Static-tier controls at the CLOSE (2026-09-14):** a mid-session commit runs `tests/run_all_static.sh --strict --exec-controls none` plus `CONTROL=<name>` for every gate it adds or changes; the session close runs every control (the default). Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-14 (14z-154)".
- **Release scope (2026-09-02):** at release every test whose SUBJECT is the released artifact runs, its legacy content included (a reference leg on vsav2 or pristine vsavj is not the subject); anything red or skipped is a hard fail unless approved at release time, so a release-scope gate fails loudly on a missing prerequisite. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **A red gate is a question (2026-09-02):** before fixing the gate, fixing what it caught or deleting it, establish which side's expectation rests on a measurement; a frozen number whose provenance cannot be named is a claim. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Cross-game comparisons at a matched speed level and a pinned RNG (2026-09-15, #135, #142; amends the 2026-09-02 #114 line):** vsav2 defaults P1 to TURBO and vsavj to NORMAL, and the two games' RNG states differ, so `tests/test_don_immortal_native.sh` pins `RAM:$FF8116` (levels 6 and 8) and `RAM:$FF80D4-D5` on both legs and asserts ours == native in hit count, damage and hit frames; LP's accepted "one hit short" is retracted as a mode artifact and #142 is closed invalid; the 2026-09-02 ruling that the vanilla vsav engine takes precedence stands. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-158)" and "Moved 2026-09-14 (14z-154)".
- **No static substituted-wheel gate (2026-09-02):** declined — the pattern matches 26 legitimate gates; the defence is [VSP-163] (assert `+0x60` against `bases.tsv`, or force the pick). Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Oboro's intro stays (2026-08-28):** never delay round start or cut the intro for a cosmetic reason. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Community data (2026-08-31):** measurement is king — a community source that matches ours exactly or by a constant offset validates our measurement, and an inconsistent pattern means re-check OUR measurement first. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Cosmetic is optional (2026-08-27):** cosmetic and single-player-only surfaces are nice-to-have; competitive 2P versus is the focus. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **No legacy character meets a tenant in 1P arcade (2026-08-28):** not a problem, 2P versus is the focus; not built, not scoped. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **The living-docs generalisation, when scheduled (2026-09-08, #119):** independent of bbh but compatible with it and on the same principles; the site generator in scope; fidelity required in principle, its form argued in the scope document; the open items first. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Linux release binaries (2026-09-13, #121):** self-containment is checked against an external host-provided list plus the ruled exceptions; the WSL2 Linux binaries are never published. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Generator refactor policy (2026-08-21, #50):** no dedicated refactor of `tools/gen_donovan_patch.py`'s `main()` is ever scheduled absent a new measured cost; a handler moves to module level only when a test needs to drive it, with its unit test in the same change, and byte-identity under `tests/test_m3a_reproducible.sh` and `tests/test_phasec_spaces.sh` is the refactor gate. Full entry: `STATE_HISTORY.md` "Session 14z-102 (post-freeze rulings)".
- **A measurable question is measured (2026-09-15):** a factual question a measurement can settle is measured before it is reported — never put to the maintainer as a question, never published as "unreconciled". Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)".
- **Pipeline-image registry rows (2026-09-15, #96):** the M2 battery dispatches through `tests/expected/registry.tsv` on the build's fingerprint, never a pinned set name; `donovan-mN-stock` / `-stage4` register its two pipeline images (untagged, never shipped); each freeze rebuilds both and records whether they moved, and an unregistered fingerprint is a rule-6 stop. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)".
- **The merged set carries no tenant expectations (2026-09-15, #111; the 14z-133b B2 design):** the merged expectation set holds the legacy classes, the mask and the `.skip` markers; tenant-content `.sha1` expectations live in the solo sets, and the merged build's tenant content is covered by `audit_merged_legacy` leg (b) and the tenant gates. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)".

## STANDING PRINCIPLE (maintainer, 2026-08-05): vanilla wins ties

**[VSP-21]** "vsav vanilla is always better when we can." **When a console port and
arcade vsav differ and both would work, take vanilla.** A console port's
choice is not evidence that vanilla is wrong; it is evidence of what that
port's designers preferred.

This is a general rule, not a one-off: the PS1 capture is a reference for
what is POSSIBLE and for data we cannot otherwise obtain (cell placement,
the adjacency of NEW cells), not a style guide for content vsav already
defines. Paired with the maintainer's other statement — "as long as we can
select characters it's good" — the test is: does keeping vanilla still let
the feature work? If yes, keep vanilla.

Applied immediately, twice:
- **`Bishamon DL` and `Aulbath DR` stay vanilla** (Anakaris / Sasquatch).
  PS1 sets both to "no move"; neither is needed for reachability, so
  vanilla stands.
- **Horizontal wrap stays vanilla.** Vsav wraps left/right (cell `0x01`
  Left goes to `0x05`, measured and confirmed in-emulator); the PS1 report
  of "no wrapping" reflects untested extremes. We touch none of those
  cells, so nothing to decide.

Judgment applied under the same rule, open to veto: the three inbound edges
from `0x0B` (`D`/`DL`/`DR` into the new row) DO diverge from vanilla, and
strictly they are not required — Phobos and Donovan are already reachable
via `Bishamon D` and `Aulbath D`, and Pyron through them. They are kept
because without them, pressing Down on the cell directly above the new row
does nothing while three medallions are visible below it, which is the UX
failure "as long as we can select characters" is meant to exclude. Dropping
them would reduce the legacy footprint from 5 bytes to 2.

## Decisions pending (human)

Only undecided items live here; a ruling moves its entry to `DECISIONS_HISTORY.md` in the
commit that records it (CLAUDE.md [VSP-17]). *None open.*

## THE DEADNESS REGISTER (opened 14z-71, maintainer's standing instruction)

**[VSP-23]** Every claim of the form **"legacy never reaches this, so we may reuse
it"**. Each is measured by ABSENCE, which is the weakest kind of evidence
we accept, so each is listed here with its guard. **These are the FIRST
PLACES TO CHECK for any unexplained regression in vanilla assets, engine
behaviour or rendering** — before anything else is suspected.

| Reused resource | Claim | Guard | Fallback if wrong |
|---|---|---|---|
| ~~palette-seq ids 0x1E-0x21 (`0x39ACC0`)~~ **CLAIM FALSE, ROW WITHDRAWN 14z-79 (they are Bulleta's DF block)** | vanilla only ever requests 0x26/0x27 | `tests/audit_palette_seq_ids.sh` (10,504 sampled calls) | none — the palette path never transits work RAM, so the audit is the ONLY guard |
| effect-class row 16 (`0x080AEC`) | vanilla never dispatches class 16 | `tests/audit_effect_class_rows.sh` §1, 0 reads vs a 1760-hit control | none needed: the row was a stub (`rts`), so a wrong claim costs at most the old no-op |
| ~~drawer list-type 6 (`0x01B6AA`)~~ **CLAIM FALSE (measured 14z-89) — LEGACY LISTS DO REACH TYPE 6** | vanilla has no type-6 sprite lists | `audit_effect_class_rows.sh` §1/§4 + `tests/test_beam_list_type6.sh` | **THE FALLBACK HELD — this is what a safe-and-loud design buys.** 14z-89 measured the tripwire ARMED on legacy content on huitzil-m13: `21_don_mash` 387 times and `26_don_arcade_mash` 948 times, PC-attributed to inside the thunk body (0x0FD060). Rendering stayed correct throughout (the fallback runs vsav's own type-6 code, reproduced instruction-for-instruction), so nothing rendered wrong and no playtest ever saw it — exactly the outcome the register's "prefer designs where being wrong is safe and loud" rule was written for. WHY IT WAS MISSED: the deadness measurement was sound but its COVERAGE was four replays (`02/07/09/30`), and the gate has always run on that default set; the two replays that arm it are long mash/arcade rigs nobody pointed it at. COST TODAY: `$FF010C/$FF010D` is a live work-RAM counter vanilla does not keep, so both replays diverge permanently from the vanilla masked basis — they are `.pending` on huitzil-m13 pending the maintainer's ruling. ~~OPEN: does the fallback need to stop counting (make the tripwire diagnostic-only / move it out of work RAM), or is the counter acceptable? See "Decisions pending — 14z-89"~~ **ANSWERED 14z-91: the counter was REMOVED** — the `beam_list_type6` `RAM:$FF010C` counter deleted, which cleared all six `.pending` legacy replays (`HANDOFF.md` Build registry, the 14z-91 row (C)) |

**[VSP-22]** Rules for adding a row: the claim must be measured with a POSITIVE CONTROL
on the same instrument and leg (a blind instrument and a real zero look
identical — paid for three times in 14z-71); it must name its guard; and
it must say what happens if the claim is wrong. Prefer designs where being
wrong is *safe and loud* over designs that are merely well-measured.

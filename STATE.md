# STATE — living progress log

**SPLIT 2026-08-20 (14z-99 post-freeze close, maintainer-approved), AMENDED
2026-09-14 (CLAUDE.md [VSP-17] and [VSP-182]): this file holds the RECENT
session groups and the STANDING SECTIONS; the full detail of every older
session lives verbatim in `STATE_HISTORY.md`, under THE LEDGER at its head.**
How to work with it:
- **Lookup**: "STATE 14z-XX" references resolve here first, then in
  STATE_HISTORY.md (its LEDGER first) — section names are preserved verbatim
  in the archive. A reference to `STATE "Decisions pending"` for an entry no
  longer here resolves in `DECISIONS_HISTORY.md` (entries move there verbatim
  when ruled — since 2026-09-14; before that, once they stopped shaping work).
- **Claim-greps MUST include STATE_HISTORY.md** (the CLAUDE.md §5
  retraction-discipline command names it).
- **AN OPEN LIST HOLDS ONLY WHAT IS OPEN (maintainer-ruled 2026-09-14).**
  "Decisions pending" holds undecided items only; nothing closed stays in it,
  however it is marked (struck through, DONE, FIXED, CLOSED). Where each thing
  goes instead:
  - a RULED decision -> its whole entry moves VERBATIM to
    `DECISIONS_HISTORY.md` in the commit that records the ruling; while the
    ruling still constrains work, ONE line stating the rule sits under
    "Standing rulings" here, and the line is deleted when it stops;
  - a BUG, COSMETIC ITEM or EVOLUTION is a TICKET — listed in
    `docs/project/tickets.tsv`, its story on its GitHub issue, never listed
    here; a STATE entry for one that closes moves VERBATIM to
    `STATE_HISTORY.md` under the closing session's key (CLAUDE.md [VSP-182]:
    the four questions, the closing comment);
  - "STANDING PRINCIPLE" and "THE DEADNESS REGISTER" carry the [VSP-21..23]
    anchors and stay; the register keeps its withdrawn rows by [VSP-23].
  `tests/test_state_open_lists.sh` enforces it, with the size budget and the
  group count below.
- **ROLLOVER RULE (part of the session-close ritual)**: after writing the
  close entry, move session groups beyond the newest THREE to the TOP of
  STATE_HISTORY.md's body in newest-first order (below THE LEDGER, above the first
  archived `## Session` heading OLDER than the group)
  and add their one-line entries at the top of THE LEDGER there, composed from
  the group's own banner headers. If this file still exceeds ~150 KB, roll the
  oldest kept group early. The standing sections never roll.
  **THEN RE-POINT WHAT CITED THE ROLLED GROUP (added 14z-173, after it bit):**
  a rollover silently breaks every `STATE.md § <section>` citation into the group
  it moved, and `tests/test_tickets.sh` is what notices — at 14z-173 it went red on
  #167's `decided` link anchoring "THE CLOSE TIER", which had just left STATE.md for
  the archive. After moving a group, re-point those citations to
  `STATE_HISTORY.md § <section>` (the convention 20 rows already use) and regenerate
  the page with `python3 tools/tickets.py page`. The sweep is one command:
  `python3 -c "..."` over `docs/project/tickets.tsv` comparing every `STATE.md §`
  anchor against STATE.md's current text — do it for ALL rows, not just the one the
  gate stops at.
- **THE CLOSE CHECKLIST (ruled 2026-09-18, 14z-167b; `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-167b)")**, after the close entry
  and before the tier: (1) a FINDINGS TABLE — every finding of the group, its live home and the test that
  reproduces it; (2) `python3 tools/close_findings.py <session>` — every GAP homed, every REVIEW answered
  by a home or an open item (never by reclassifying the finding), every PROMISE fulfilled or made an open
  item; its clean result is never cited as completeness; (3) a pointer on every earlier row the sitting
  resolves; (4) the promises (the tool's PROMISES section); (5) scratch scripts whose figures a document
  or STATE row quotes are promoted into `tools/` or `tests/` — ON TRIAL: record how many and how many
  were promoted; (6) the DOCUMENTATION PACKET through the rule-checker (`recommendation`, the table as
  the claim), its verdict resolved like any run.
- **THE LAST STEP OF THE CLOSE IS THE PUSH (maintainer-ruled 2026-09-10):**
  static tier strict green WITH every control executed (the default `all`), the doc checks green, **the process
  sweep CLEAN** (C0.3, #172 slice S2, since 2026-09-23: `python3 tools/agent/sweep.py`
  run AFTER the tier, which spawns processes of its own, exits 0 — every survivor
  killed leaves first, as its hint lists them, or declared in writing with
  `--declare PID "REASON"` and named in the close entry), **the procedure check (C1, #172
  slice S3, since 2026-09-23) passed or resolved** — `tools/agent/extract.py` over the session's
  transcript, a `procedure` run of `tools/rulecheck.py` on it, every VIOLATED question answered by
  `resolve`, and — ruled 2026-09-24 (14z-179, *"Standing step"*) — `python3 tools/agent/transcript_gaps.py --subagents <session> --cap` exit 0
  (every worker at most Opus-class, none fallen back); BOUND to the push by `tools/agent/hooks/pre_push.py` since 14z-176b, which refuses a
  push of this repository until such a run covers a commit in the pushed range — nothing pending -> `git push
  origin main`; anything red or skipped leaves the commits local and the
  close entry says so.

## Session 14z-180 — **#171 GATE QUALIFICATION: SCOPED, RULED AND EVERY ONE OF ITS SEVEN SLICES BUILT IN ONE SITTING, on the #172 setup the maintainer asked to see in use.** Four measurer workers took the census and one figure of theirs was wrong; the pinned rule-checker read FOUR packets of mine (runs 141-144), caught every plant, and found true defects in every real one — all resolved by work. The maintainer ruled the six questions and ADDED a seventh slice at the head, Q0: a human-readable WHAT / HOW / EXPECTS in every gate's header, rendered from the headers into a generated coverage page they review family by family — **all 385 gates described**. Then Q1 (module references), Q3 (`# FOLLOWS:` on all 199 emulator gates, three reconciliation classes, the carry tool derived from them), Q4+Q5 (the runner's commit of record, the staleness gate, `--stale`, half the cap), Q2 (the `# MEASURES:` contract read inside the classifier, a freeze guard that refuses an empty table, six floors) and Q6 (the poke read-back census: 74 findings, one row each). **What remains of #171 is the maintainer's: 73 rulings, one per finding** ("Decisions pending"). The maintainer: *"in order, and in any case first we finish #172 because we need to use that setup for the upcoming work"*, *"can you confirm you are using the swarm of agents described in #172 and that any further session will?"*, *"I review as I go"*.

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, `docs/NEXT_SESSION.md` — asked for as `docs/project/next_session.md` again), `main` == `origin/main` at `97d84268`, `emu/fbneo` dirty by exactly its two tracked patches (841/4), `python3 tools/agent/sweep.py` CLEAN (exit 0), one untracked stray `.claude/settings.json.` (trailing dot, byte-identical to the tracked settings, dated 2026-09-23 22:31 — reported, not touched). #172 verified CLOSED on GitHub (2026-09-24 12:18Z) and `done` in the index before any #171 work |
| **(0b) 14z-179's TAIL CUT FOR C1** | `extract.py --session 8037cb8c --from 721` -> `build/agent172/extract_8037cb8c_tail.txt` (181 lines, records 721 to the end; no open task, 0 detached launches) — the second span of this close's procedure run |
| **(1) THE SWARM QUESTION, ANSWERED HONESTLY** | this is a plain session on Fable 5.1 (every assistant record `claude-fable-5-1` on a mid-sitting read of 60; re-read at the close); the tracked settings unmodified, the three definitions loaded at start; but every measurement of the opener had been run INLINE and no worker spawned — said so, and from that point every quoted figure went through `measurer` specs (five workers this sitting) and the recommendation through the pinned rule-checker. Mechanical for every future session: the three hooks, the definition gates, the push refusal; procedural only: the Fable model (the maintainer's launch) and delegation itself — a C1 question would bind it, put to the maintainer, not written in. Memory note `use-the-agent-swarm` |
| **(2) #171 MEASURED BEFORE PROPOSED — four measurers, one figure wrong** | shape 1: 270 cross-module Python references over 108 (file, module) pairs, every one resolving (the census instrument `tools/audit_module_refs.py`, built this sitting); 7 gates compare a hashed table. Shape 2: 199 registry rows, 189/63/197/31 gates naming replays/expected/tools/manifest, the runner records NO commit and knows no trigger, `PROVENANCE.md` maps 63 files to their owner gate. Shape 3: on the 2026-09-22 release run no gate or control at or above half its timeout (max 0.38), 14 rows with an explicit timeout, the emulator tier not run since — 6 sittings. Shape 4: 109 gates poke, 79 sample, 46 both; 4 naming-rig gates read the stock byte the generator pokes. **Worker C returned 13 explicit-timeout rows where its own list had 14 and the command prints 14** — re-derived, the wrong figure kept beside the correction (`build/gatequal180/measurer_returns_14z180.txt`, every FIG line and every spec's commands verbatim) |
| **(3) THE SCOPE DOCUMENT THROUGH THE RULE-CHECKER — TWO TRUE DEFECTS** | `docs/project/gate_qualification_scope.md` (six slices Q1-Q6, six rulings). Run `2026-09-24-141` (`recommendation`, plant `inferred-decision-14z162` CAUGHT, both readers `claude-opus-5-5`/`high`, prompt IDENTICAL) returned **VIOLATED on Q1 and Q4, both TRUE, resolved by work**: Q1 — the historical table's figures (nine sessions, 234 s, 210 s) traced to no artifact (the 14z-174 row, the runner's default and the registry header now cut into `sources_14z180.txt`, the table's lead names its sources), the returns artifact carried FIG lines without their commands and three abridged lines (restored verbatim, every spec's COMMANDS appended), and "136 files" was a LINE count including prose (63 file rows, a fifth measurer); Q4 — the tool's `--plant` appended the reference AFTER parsing and proved only the resolve step (now it parses the historical caller `git show e01ae8de^:tools/vanilla_join_rig.py` through the same extractor and finds `name_moves.PROLOGUE` at its line 110), and the census had no floor (now `--floor`, default 1, and a `--plant-empty` control: 0 references over 0 files fails). Ledger row resolved in writing |
| **(4) THE RULINGS — seven, the seventh the maintainer's** | (1) *"As proposed WITH THIS ADDITION: I want for each test a human-readable description of the test (at least what it tests, how it tests and what is the expected result). The rationale for this is human supervision …"*, then *"That form, first, by family"*; (2) the declaration form — first asking *"I don't see the pros and cons anywhere"* (they sat above the dialog, which hid them; put inside the question the second time) — *"Header line"*; (3) *"NOTE at session, FAIL at freeze/release"*; (4) *"Declared/fired grammar"*; (5) *"Half the cap"*; (6) *"Land it now"*; and on the review form: *"I want the doc I read to be generated from the headers themselves, to avoid it rotting in the future. For legibility I favor a nice webpage but I don't mind an md file …"*; on the order: *"Continue with character-data; I review as I go"*. Verbatim in `DECISIONS_HISTORY.md` "Ruled 2026-09-24 (14z-180) — #171 gate qualification"; the standing line in this file |
| **(5) Q1 LANDED** | `tools/audit_module_refs.py` + `tests/test_module_refs.sh` (ci_portable, controls `planted-prologue` and `empty-census` as modes, both reaching FAIL; a bogus mode REFUSED exit 3), the floor frozen at 270 in `tests/expected/module_refs_floor.txt` with its PROVENANCE row, registered in `ci_portable.txt`, `gate_index.tsv` (runner) and the must-fire census (145 declaring) |
| **(6) Q0 BUILT: THE DESCRIPTION GRAMMAR, ITS READER, ITS CENSUS, THE COVERAGE PAGE** | `docs/project/gate_header_contract.md` (REFERENCE: the WHAT/HOW/EXPECTS grammar; FOLLOWS and MEASURES ruled-not-built); `tools/gate_descriptions.py` the ONE reader (a field continues on `#  ` lines, ends at the next field or a bare `#`); `tests/test_gate_descriptions.sh` (ci_portable, `declares` grows only / `undeclared` shrinks only, frozen `tests/expected/gate_descriptions.tsv` with its PROVENANCE row; controls `dropped-field`, `field-outside-block` — a first form perturbed nothing because it passed `tests` instead of `.` as the root and its "REFUSED" fell inside a `$(...)`, caught by running the modes: both exited 0); `tools/gen_gate_index.py` renders the fields per family as a second table in `gate_index.md` AND as a second GENERATED page for READING, `docs/project/gate_coverage.md` (one entry per gate, the undescribed listed per family), both checked by `--check` and `test_gate_index_current` (its mode copy carries both pages); the site renders it (`docs/site/project/gate_coverage.html`) — `test_docs_site` went red on `tests/*.sh` in a cell pairing two `*` into an `<em>`; the renderer now wraps an asterisk-bearing token in a code span |
| **(7) BATCHES: RUNNER 22/22, TENANT 82/82** | descriptions written from each script's leading comment block (collected to `build/gatequal180/*_headers.txt` and read in full), inserted after the header's first paragraph by `build/gatequal180/desc_*.py`; every header-reading gate green after each batch (header_defaults, shell_portability, must_fire_census, bg_leg_shape, demand_after_trap, mame_bin_pinned); the census frozen at 105 declares / 274 undeclared; the maintainer chose *"tenant, then character-data"* and *"Continue with character-data; I review as I go"* |
| **(8) THE MID-SESSION TIER WENT RED ON MY OWN CHANGE — bbh's F9** | `ROMDIR=... tests/run_all_static.sh --strict --exec-controls none` (the mid-session form): **PASS 168, FAIL 1** — `test_bbh_fidelity`: the generic harness renders `gate_index.md` with a lifted COPY of `gen_gate_index.py` and F9 requires byte-identity, and I had put the description table INTO the index. Rule 6: the index generator and `test_gate_index_current` restored from git, the coverage rendering moved to its own generator `tools/gen_gate_coverage.py` (rows from `gen_gate_index.collect()`, so the two pages cannot disagree) with its own gate `tests/test_gate_coverage_current.sh` (ci_portable, controls `hand-edited-coverage`, `description-dropped`, both FAIL as modes), the docs re-pointed; `test_bbh_fidelity` PASS again. CONTROL= modes of every changed gate run by hand (module_refs 2, gate_descriptions 2, gate_index_current 3, gate_coverage_current 2, all FAIL as required). The tier re-run: the second run **GREEN — PASS 170, SKIP 0, FAIL 0** (`build/gatequal180/static_mid2_14z180.log`); committed `bd31b1a3` (186 files, 0 from build/) |
| **(9) CHARACTER-DATA 55/55** | the third batch, from the 55 headers (2,329 lines, read in full) — the census frozen at 161 declares / 219 undeclared, every header-reading gate and `test_docs_site` green |
| **(10) THE OTHER SEVEN FAMILIES, 219 GATES — ALL 380 DESCRIBED** | platform 38, pipeline 58, review-triage 31, oracle 29, gfx 25, mister 20, docs 18, each family's headers collected into `build/gatequal180/<family>_headers.txt`, read in full, the three fields written from what each script does (`desc_platform.py` .. `desc_docs.py`, every insert `ok`). The gates caught two defects in the batch: `test_freeze_basis_sandbox` already carried a `# HOW:` line inside its WHY prose, which the reader saw as a duplicate field and the census as `undeclared` (the prose line renamed `METHOD (14z-91)`); and a `<header>` token in `test_docs_site`'s HOW rendered as raw HTML on the site (`gen_gate_coverage.safe()` now code-spans tag-shaped tokens as it does asterisk-bearing ones). Census FROZEN **380 declares / 0 undeclared**; `gate_coverage.md` "380 of 380 gates described"; the seventeen gates a header edit can reach all green (gate_descriptions, gate_coverage_current, gate_index_current, must_fire_census 146, header_defaults, shell_portability, bg_leg_shape, demand_after_trap, mame_bin_pinned, docs_site, docshape, checkdocs, static_runner, expectation_provenance, module_refs, bbh_fidelity, freeze_basis_sandbox). Committed `6e6be0c9` (224 files, 0 from build/). The maintainer reviews `docs/site/project/gate_coverage.html` family by family; corrections are header edits. |
| **(11) Q3 LANDED — `# FOLLOWS:` ON ALL 199 EMULATOR-TIER GATES, ONE READER, THE CENSUS AND THE RECONCILIATION, THE CARRY TOOL REWRITTEN** | `tools/gate_follows.py` (imports `gate_descriptions.leading_block`, so one definition of "the header"): the declaration grammar (repo path PREFIXES, `ci_cadence.tsv`'s sense; own script and `tests/ci_emulator.tsv` implied), and the REFERENCE extractor — every `tests/ tools/ docs/ emu/ build/manifest` token in a script's non-comment lines and, transitively, in the RUN-TIME HARNESS it reaches (`tests/lib/*.sh` sourced, `tools/run_*.sh`/`tools/tap_*.sh` called — the build chain excluded on purpose: through `build_donovan.sh` a gate would "reference" 92 paths; through the harness the widest declares 26), plus the registry row's args; a variable-composed path is cut at the variable so only a wider prefix covers it; untracked build outputs are not references. Three extractor blind spots found by READING the proposals against each gate's HOW field, each fixed before the declarations were written: `"$REPO/…"` and `"$PWD/…"`-spelled paths (33 gates showed NO references at first), `${VAR:-tests/replays/x.rpl}` defaults (three gates' replays invisible), and a trailing `}` / `.` on a cut token. The 199 declarations = the collapsed references WIDENED by what reading shows a gate follows beyond its text: `emu/mame-patches/ tools/setup_mame.sh` for every gate that runs the WIDE MAME (two gates that run the stock `mame` from PATH — decrypt_oracle, patch_prg — deliberately do not), `emu/fbneo/ emu/fbneo-patches/ tools/setup_fbneo.sh` for FBNeo, `emu/jtcores/ emu/jtcores-patches/ tests/rtl/ tools/setup_jtcores.sh` for the MiSTer lane, `build/manifest/` for a gate that takes or builds a romset, `tools/name_moves.py` / `tools/vanilla_join_rig.py` for the generated rigs (`build/gatequal180/follows_apply.py`, its per-gate review listing `follows_review.txt`). `tests/test_gate_follows.sh` (ci_portable, family runner): census FROZEN **199 declares / 0 undeclared** (`tests/expected/gate_follows.tsv`, PROVENANCE row), `--reconcile` **PASS: 0 declaring gate(s) with a reference outside its declaration**, controls `dropped-declaration` and `narrow-declaration` as modes (both FAIL, bogus REFUSED exit 3). `tools/audit_lane_carry.py` REWRITTEN: `LANE_SUBJECTS` and `KNOWN_OMISSIONS` deleted, subjects = the union of the lane's declarations + its gate scripts + the registry (`gate_follows.lane_subjects`), an undeclared gate = MUST RE-RUN by construction (the control the ticket asked for); `--repo` for its ground truth `tests/test_lane_carry.sh` (a scratch git repository: unchanged MAY CARRY, an unrelated change still MAY CARRY, a declared replay edited MUST RE-RUN naming it, an undeclared lane refused; shadow-tool controls `carry-blind`, `undeclared-carried`). On the real tree the header edits themselves read as moved gate scripts (mister: 9 paths since `bd31b1a3`) — a changed gate IS a changed measurement. The FOLLOWS section of `gate_header_contract.md` is live; the scope doc's §2 carry bullet marked SUPERSEDED, Q3 LANDED (the reader is Python, not the `tests/lib/follows.sh` first proposed — the two consumers import it instead of re-parsing). |
| **(12) Q4+Q5 LANDED — THE COMMIT OF RECORD, THE STALENESS GATE, `--stale`, HALF THE CAP** | `tests/run_all_emulator.sh` writes `commit.txt` (HEAD, then the dirty tracked paths) into every run directory before its first row, and `--stale` runs exactly the names `tools/audit_emulator_staleness.py --names` prints (an empty list exits 0 before writing a run directory, saying so). The tool reads the newest `build/emu_*` run WITH `commit.txt` (runs before 14z-180 are skipped by name), diffs the recorded commit against the tree (committed + working tree + untracked), matches moved paths to each PASSED gate's declaration (own script and registry implied), names STALE gates and UNDECLARED passed gates, and checks every results row's `seconds` against its cap (7th column, else 5,400; a `gate@ctl` row shares its gate's cap) at the ruled 0.5. Verdict by cadence AS RULED: NOTE + exit 0 at session, FAIL at freeze/release; headroom FAILs at every cadence. `tests/test_emulator_staleness.sh` (ci_portable, session cadence): section 1 the tree's run of record at `VS_CADENCE` (NEW: `tests/run_all_static.sh` exports `VS_CADENCE="$CADENCE"`, `test_static_runner` green) — today "NO RUN OF RECORD", a NOTE at session and a FAIL at freeze/release (a freeze runs the tier); section 2 the instrument on a scratch git repository (a moved declared replay names its gate, an unmoved gate is not named, the undeclared one is named unjudgeable, session exit 0 with the `--stale` hint, freeze exit 1, a row at exactly 300/600 FAILs, its control row at 20/600 does not, `--names` prints exactly the stale gate); controls `moved-input`, `headroom-eaten` (known-bad plants; modes FAIL, bogus REFUSED). `tests/test_emulator_runner.sh` case 15: `no-git` outside a checkout, HEAD inside one, `--stale` running exactly the stubbed audit's name and nothing else, the empty list exiting before a run directory — PASS. The runner's help range re-aimed after the usage grew (`sed -n '2,82p'`). |
| **(13) THE FREEZES THROUGH THE RULE-CHECKER — RUN `2026-09-24-142` VIOLATED Q1/Q3/Q4, ALL TRUE, RESOLVED BY WORK** | packet: the FOLLOWS census freeze (199/0), the three re-frozen censuses and the carry rewrite; plant `forced-pick-14z159` CAUGHT (Q1 Q4). The real reader found: (Q1/Q4) the WIDENING RULES lived only in the one-shot `follows_apply.py`, so a rule that failed to fire was checked by nothing — the two bare-`mame` gates it named (`test_decrypt_oracle`, `test_patch_prg`) are deliberately unwidened (they run whatever `mame` is on PATH, no binary this tree builds) but that stood on a pattern's silence, not a statement; (Q3) the reconciliation checked the extractor AGAINST ITSELF — "0 uncovered" was true by construction for the initial declarations; (Q1/Q4) "the frozen files change only by the new gates" had no diff behind it and PROVENANCE still said 270 while the floor read 284. Work: the rules moved into the reader (`gate_follows.required`) and `--reconcile` gained a RULE class (637 required prefixes, 0 uncovered) and a PROSE class — an INDEPENDENT second extractor reading the replays each gate's own WHAT/HOW names (57 resolve, 0 uncovered; the description written by reading the gate is the second witness); two new controls `rule-unwidened` and `prose-uncovered` (the latter DEAD on its first run because the perturbed copy had no replay corpus to resolve against — the gate said so; the copy now links `tests/replays/`); the freeze deltas recorded in `build/gatequal180/q3q4_gate_outputs_14z180.txt`: both censuses differ from HEAD by exactly the three new gates, and the floor's rise attributed per file by the census tool's own `references()` — 270 (at `97d84268`) + 4 (`gen_gate_coverage.py`, the sitting's earlier commits) + 11 (`gate_follows.py` 3, `audit_lane_carry.py` 3, `audit_emulator_staleness.py` 5) = 285, the floor re-frozen there; the PROVENANCE floor row carries no number (the file does); the contract names the two bare-`mame` gates and the circularity. Gotcha appended (`docs/project/gotchas.md`): a path extractor's blind spots are the spellings it never met, and its own census cannot show them. |
| **(14) Q2 LANDED — THE MEASUREMENT CONTRACT: `# MEASURES: <name> — <floor>` / `MEASURED: <name> = <n>`** | `tests/lib/measures.sh` (the one reader: `vs_meas_declared`, `vs_meas_read`), sourced by `tests/lib/classify.sh` and read inside `vs_classify` AFTER the controls read, on a PASS only — a declared name not printed, a value below its floor, or a printed name no header declares is plain FAIL (no fifth verdict; the three runner ground truths green: static, emulator, battery). Ground truth `tests/test_measures_contract.sh` (ci_portable, family runner: the grammar in the leading block, declared-vs-measured through the SHIPPED classifier, the controls read still first, SKIP/exit≠0 untouched; controls `missing-measurement`, `below-floor` as known-bad modes, both FAIL, bogus REFUSED; it declares `synthetic-cases — 14` and prints it — dogfood). First reader draft parsed `name<TAB>floor` with a parameter expansion on a literal tab and misread the second declaration ("files 6: integer expression expected"); rewritten on `cut`. **The surface:** of the seven gates the scope's text heuristic named (Worker A/C5), five compare a produced table and now declare six measurements — `test_charmap_current` charmap-anim-rows 12922 (the three out-of-tree anim pages' rows, each hash-equal to `charmap_pages.sha256`), `test_meter_gain` meter-gain-rows **49** (the header first said 51 from the out-of-tree file's line count, which includes its two header lines; the gate RUN on MAME measured 49 and the floor was corrected to the measurement — the contract would have failed it), `test_vanilla_frame_join` slot-map-rows 180 + hit-damage-rows 90 (both RUN on MAME: `MEASURED: slot-map-rows = 180`, `MEASURED: hit-damage-rows = 90`, PASS), `test_release_binaries` binary-record-rows 2 (one row per BINARY.txt record at the least; NOT run this sitting — an emulator-lane gate with boot legs; its count is summed in `check_dir` over sections 1-2), `test_release_os_metadata` zip-members 20 (run: 20). `test_build_environment_entry` (a synthetic record's stub hash) and `test_release_roundtrip` (release records whose files may be absent by ruling) compare no produced table and declare nothing — said by name in the contract. `test_bbh_fidelity` read FAIL once, run BESIDE the frame-join MAME run, and PASS alone (the known flake: memory `bbh-fidelity-flake`, "never beside another gate run here"). Censuses re-frozen: must-fire 150, descriptions 384. Contract section live; scope Q2 LANDED; HANDOFF and README rows. |
| **(15) Q2 THROUGH THE RULE-CHECKER — RUN `2026-09-24-143` VIOLATED Q1/Q3/Q4, ALL TRUE, RESOLVED BY WORK** | plant `forced-pick-14z159` CAUGHT (Q1-Q4). The real reader found: (Q1) the evidence showed the reader over TWO of the five runs and named the wrong BINARY.txt files (the release copies, not `release/emulators/…` that the gate reads), the runner ground truths' green was asserted not shown, and the dogfood printed a CONSTANT (`vs_measured synthetic-cases 14` — the file had 17 checks); (Q3) every floor was checked against the same expression on the same file that produced it — "read back as OK" agreed by construction; (Q4) no control reproduced the M19 shape on a REAL gate — the two controls prove the reader on hand-written logs — and the binary floor of 2 can never fire (`check_dir` already fails an empty record). Work: `vs_meas_guard` in `tests/lib/measures.sh` — under `FREEZE=1` a below-floor value prints `REFUSED FREEZE:` and returns 1, and the three freezing gates (`charmap_current` with its freeze DEFERRED past the measurement, `meter_gain`, `vanilla_frame_join` ×2) call it before writing; the M19 shape as a real-gate control, `test_charmap_current` `empty-page-frozen` (donovan's page emptied after generation, `FREEZE=1` against a scratch copy: `REFUSED FREEZE: charmap-anim-rows = 7664 < floor 12922`, exit 1; in-gate the guard on the real numbers minus donovan's 5258 fires); the contract gate's control `freeze-refused` and a COUNTED dogfood (`n_checks` incremented by ok/bad: 17, the floor set to the measurement); `build/gatequal180/q2_independent_counts.txt` — each floor from a DIFFERENT file by a different expression (the frozen slot set 180 non-comment rows, the hit-damage record's own "90 rows", the out-of-tree meter file 51 lines − 2 headers = 49, the three out-of-tree anim pages 5258 + 4210 + 3454 = 12922 each hash-equal to the frozen row; zip-members has none and says so); the evidence rebuilt with every log read through the SHIPPED classifier, the four runner/contract ground truths' PASS lines captured (`q2_runner_truths.txt`), the records the gate reads counted (macOS 4 + 2, Windows 31 + 1) and the binary floor named STRUCTURAL in its header and the contract. `meter_gain` and `vanilla_frame_join` RE-RUN on MAME after sourcing the library (49; 180 + 90, PASS). |
| **(16) Q6 — THE POKE READ-BACK CENSUS LANDED; ITS 73 RULINGS ARE THE MAINTAINER'S** | `tools/audit_poke_readback.py`: per registry gate, every poked byte range (literal `frame:addr:hex` tokens anywhere in the script — a variable-valued poke still shows its address — plus the `pokes` of every `name_moves.py gen` / `vanilla_join_rig.py gen` schedule the gate names, or every part of the tenants it names when it loops) intersected with every sampled range (`FIELDS` b/w/l, `DUMPS` frame:lo-hi, `TAP`/`WATCH`/`RTAP` addr,len lists, `FBNEO_HTAP` lo-hi): **74 findings over 31 of the 199 gates** (72 before the rig pokes were given their real byte width — a 4-byte HP pin also covers the white-HP word), the known positive case among them (`test_killshread_es ff8509 FIELDS:stock`), ~2 s. Not read, said in the tool's header: what a reducer does with a column, addresses with no literal, the -debug watchpoint scripts. `tests/expected/poke_readback.tsv`: one row per finding, class READS-BACK for the killshread `stock` column (the maintainer's 2026-09-22 ruling, quoted from the gate's header) and UNCLASSIFIED for the other 73 — the classification is the maintainer's, never the tool's (OBSERVES grows only, READS-BACK shrinks only, RETIRED keeps a finding that stopped deriving). `tests/test_poke_readback.sh` (ci_portable, family runner): every derived finding has a row, every live row derives, the known case derives and reads READS-BACK; controls `known-case-blind` (the stock field removed from a copy of killshread: the finding vanishes) and `unclassified-finding` (a synthetic gate poking and sampling ff8782: a finding with no row), both modes FAIL, bogus REFUSED. The rulings go to STATE "Decisions pending" as ONE item (the table is the list). **Run `2026-09-24-144` (expectation; plant caught) VIOLATED Q1/Q4, both true:** the census joined a CONTROL leg's plant (`PLANT_POKE="2680:ff8b82:01"` in defense_row_reads, `"3000:ff8454:38"` in reaction_class_live — known-bad plants that read their poke back BY DESIGN) to the measuring leg's sample and called the total "a floor"; and the "72 before the width fix" had no artifact (the first census file was overwritten by the second — the figure survives only in this narrative). Resolved: the tool attributes a LEG per poke by the line it sits on (`PLANT`/`plant`/`ctl`/`CONTROL`/`VS_CTL`/`vs_ctl_is`/`MODE` → control; rig pokes main), the table carries it as a sixth column (2 control, 72 main), the gate holds each row's leg to the census and gains the control `control-leg-flagged` (a synthetic gate's `PLANT_POKE` derives as control); the scope now says the 74 is neither a floor nor a ceiling and that the census compares addresses, not dataflow. |
| **(17) THE FINDINGS TABLE** (checklist step 1: every finding, its live home, the test that reproduces it) | (a) a worker's figure contradicted its own list (13 vs 14) — home NEXT_SESSION trap 1 and `measurer_returns_14z180.txt`; test: none, a discipline. (b) the scope's first draft: unsourced historical figures, a plant that proved only the resolve step, no census floor — home scope §1 lead and `audit_module_refs.py`; tests `test_module_refs` `planted-prologue`, `empty-census`. (c) the description census's controls perturbed nothing (`--root tests`, REFUSED inside `$(...)`) — home the gate's `perturb`, NEXT_SESSION trap 2; test: its two modes exit 1. (d) bbh's F9 renders `gate_index.md` byte-identically, so the description table could not live there — home `gen_gate_coverage.py` docstring; tests `test_bbh_fidelity` F9, `test_gate_coverage_current`. (e) two `*` in a cell pair into `<em>`, a `<header>` token renders raw — home `gen_gate_coverage.safe()`; test `test_docs_site`. (f) a pre-existing `# HOW:` line in WHY prose read as a duplicate field — home `test_freeze_basis_sandbox` header (`METHOD`); test `test_gate_descriptions`. (g) three extractor blind spots (`$REPO/`/`$PWD/` paths, `${VAR:-default}`, a trailing `}`/`.`) — home `gate_follows.py` `_body_lines`/`_norm` comments and the gotcha; test `test_gate_follows` PROSE class (the independent witness). (h) the widening rules unchecked, the reconciliation circular, the freeze deltas unshown (run 142) — home `gate_follows.required`, the contract's FOLLOWS section, `q3q4_gate_outputs_14z180.txt`; tests `rule-unwidened`, `prose-uncovered`. (i) the reader parsed `name<TAB>floor` with a literal-tab expansion — home `measures.sh` comment; test `test_measures_contract` §2. (j) a floor counted the out-of-tree file's header lines (51 for 49) — home `test_meter_gain` header; test: its MEASURED line read through the classifier. (k) a constant dogfood, self-witnessed floors, no real-gate control, a structural binary floor (run 143) — home the contract's MEASURES section, `q2_independent_counts.txt`; tests `freeze-refused`, `empty-page-frozen`. (l) `test_bbh_fidelity` red beside a MAME run, green alone — home memory `bbh-fidelity-flake`, row (14); test: none new. (m) rig pokes counted one byte wide (72 for 74) — home `audit_poke_readback.py` `rig_pokes`; test `test_poke_readback` `rig-poke-width` (a rig's 4-byte HP pin must reach the white-HP word; added after run 145 found the first-named control was not a width test). (n) control-leg plants joined to the measuring leg, a figure without an artifact (run 144) — home the tool's THE LEG paragraph, scope Q6, `q6_evidence_14z180.txt`; test `control-leg-flagged`. (o) two gates run a bare `mame` from PATH and follow no binary this tree builds — home the contract; test: the RULE class. (p) the runner's `--help` range cut after its usage grew — home `run_all_emulator.sh`; test: none, cosmetic. (q) a perturbed copy without the replay corpus made a control DEAD — home `test_gate_follows` `perturb`; test `prose-uncovered`. (r) **the first close tier died on its first gate in 3 s**: the measurement reader's `grep -v '^$'` over an EMPTY declaration list returned 1 under the runner's `set -e`, and neither runner ground truth could reach it because their synthetic repositories never linked the new library — home `measures.sh` (`awk 'NF'`, with the comment) and both ground truths' fakerepos, which now link it; test `test_static_runner`, `test_emulator_runner` (a gate declaring nothing must pass through the runner). (s) **the second close tier read one red, `test_poked_legs`**: its census classes a leg `native` from any `vsav2` and any MAME-runner mention in a script's WHOLE text, comments included — `audit_latch_reads.sh` reaches MAME only through `tools/tap_latch_reads.sh`, so the census had never seen a runner there until the `# FOLLOWS:` line named `tools/run_mame.sh`, and two native PARAM legs (`$1`/`$2` parameter ids, no cross-flavour) surfaced that the frozen census had MISSED; re-frozen after review, the diff exactly those two rows — home `tests/expected/poked_legs.tsv` and this row; test `test_poked_legs` (the census reads comments: a text heuristic, stated here, not fixed — the rows it added are true). (t) **I told the maintainer the third tier was "alive and passing its first gates" on a liveness task's completion notice alone, without reading its output** (procedure run `2026-09-24-146`, QP2) — home NEXT_SESSION trap 10; test: none (a discipline; the tier's outcome was read from its log afterwards). (u) **two measurers ran a command outside their COMMANDS, one did not STOP on an error exit, both appended a note or a conclusion their spec forbade** (run 146, QP5) — I read their returns for the figures, not their tool calls against the spec; home NEXT_SESSION trap 11 and `docs/project/worker_spec.md`'s reading rule; test: C1's QP5, which is what found it. (v) the resolve text of run 146 was passed as a double-quoted shell string and lost two backticked tokens to command substitution — the ledger row stands with the holes (the tool refuses a second resolve); the tokens were `measurer_returns_14z180.txt` (STATE row 2) and `git rev-parse HEAD` (the command the measurers ran outside their spec); home memory `gh-comment-bodies-via-file` (updated) and this row; test: none. |
| **CLOSE (14z-180, 2026-09-24) — THE CHECKLIST** | **(1) THE FINDINGS TABLE** — row (17), nineteen findings (a-s), two of them the close's own: (r) the first tier died in 3 s on the measurement reader's empty-list `grep`, (s) the second read one red where a header declaration surfaced two legs a comment-reading census had missed. **(2)** `python3 tools/close_findings.py 14z-180 --base 97d84268` -> `build/gatequal180/close_findings_14z180.txt`: 0 addresses checked, GAPS none, REVIEW none, PROMISES none — cited as what it is, never as completeness. **(3) POINTERS**: 14z-179's carried item 0b consumed by this close's procedure run (its tail the second artifact); NEXT_SESSION's #171 item rewritten (the slices built; the 73 rulings open). **(4) PROMISES**: none open; `test_release_binaries`' declared measurement not yet printed in a run is an open item in NEXT_SESSION, not a promise. **(5) SCRATCH SCRIPTS** (on trial): 14 under `build/gatequal180/` (`desc_lib.py`, 9 `desc_*.py`, `follows_apply.py`, three proposal/composition analyses); promoted 1 — the widening rules of `follows_apply.py` into `gate_follows.required` after run 142; the figures row (11) quotes from the unpromoted analyses (92 paths through the build chain, 26 through the harness) are design measurements and say so there. **(6) THE DOCUMENTATION PACKET** — run `2026-09-24-145` (`recommendation`, plant `fidelity-14z160-false-statement` CAUGHT): VIOLATED Q1/Q4, both true — the close-findings run was not an artifact, finding (m)'s named test was not a width test, three documents disagreed on the description count (380 / 384 / 385, each written at a different point of the sitting) — resolved: the run saved, the control `rig-poke-width` added and named, every narrative figure at 385 with the frozen census named as the count (a narrative figure is not gate-checked: a named gap). **THE TIER**: `ROMDIR=... tests/run_all_static.sh --strict` (every control executed) three times — the first died in 3 s (finding r), the second **PASS 181 FAIL 1** (`test_poked_legs`, finding s), the third **GREEN — PASS 182, SKIP 0, FAIL 0; controls executed 250, honoured 250, lies 0, refused 0, died 0** (`build/gatequal180/static_close3_14z180.log`, 19:41-20:31, wall 2,987 s). **THE SWEEP**: `python3 tools/agent/sweep.py` after the tier: CLEAN, exit 0. **THE PROCEDURE CHECK (C1)**: `extract.py --session 63442647` (2,163 lines, 0 detached launches, no open task) plus 14z-179's tail (181 lines) through run `2026-09-24-146` (plant `proc-planted-figure-14z176` CAUGHT on QP3): **VIOLATED QP2 / QP3 / QP5, all true, resolved in writing** — findings (t), (u) and the worker figure of row (2); the resolution's two lost tokens are finding (v). **THE WORKER CAP**: `transcript_gaps.py --subagents 63442647 --cap`: 13 workers (5 measurer on `claude-sonnet-5`, 8 rule-checker on `claude-opus-5-5`, every reader context-free), 0 fallbacks, 0 breaches, exit 0. **GitHub**: the sitting's story posted on #171 (comment 5818249836) as `mechanyaa-ai`. **THE PUSH**: the last step, after this row's commit — `git push origin main` through `pre_push.py` (run 146 covers `54b92736` in the range); a refusal would leave the commits local and a follow-up commit would say so here. |

## Session 14z-179 — **#172's S5 GATE SITTING — a plain session on Fable 5.1, working 14z-178's carried items in order; the ticket closes on this sitting's C0/C1 record and worker cap — THEY READ GREEN, AND #172 IS CLOSED.** The maintainer: *"We must finish #172 first, so take the next session items in order"*. **On the way, the open-lists gate was found matching only one of the ledger's two line forms — after a wrong "missing line" finding of mine from the same pattern — and fixed with a control; the rule-checker's third real run on the pinned reader found three true defects in my recommendation, all resolved by work.** **The gate read green on every item of the ruling's list — refusals 0, six workers all Opus-class with no fallback, the orchestrator on Fable 5.1 at `high` throughout, tier green, sweep clean, procedure check resolved — and #172 was closed on *"Close it on green"*.**

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, `docs/NEXT_SESSION.md` — asked for as `docs/project/next_session.md` again), `main` AHEAD of `origin/main` by 14z-178's two post-close commits (`6147357e`, `576eeee5`), `emu/fbneo` dirty by exactly its two tracked patches (841/4), `python3 tools/agent/sweep.py` CLEAN (exit 0). This session runs on Fable 5.1 (the harness names `claude-fable-5-1`, effort `high` per the user settings' per-model key), so it is S5's gate sitting; the user settings' `model` key reads `opus`, so the model came from somewhere other than that key (how, not measured). The maintainer: *"We must finish #172 first, so take the next session items in order"* |
| **(1) ITEM 0b(c) — 14z-178's close row item (5) corrected in place** | 8 quoted / 5 promoted -> 9 / 5, the fourth unpromoted script named (the census-reads cutter behind `build/agent172/pkt178/census_reads_14z178.txt`); the original text kept, the correction beside it |
| **(2) ITEM 0b(b) — 14z-178's unchecked tail cut for C1** | `extract.py --session 55857809 --from 2704` -> `build/agent172/extract_55857809_tail.txt` (309 lines, records 2704 to the session's end: the `extract.py` fix, the re-run tier, the two silent STATE edits and their re-application, the push, `cleanupPeriodDays`, the three carried items); no tracked task open at its end, 0 detached launches. It is the second artifact of this close's procedure run |
| **(3) THE LEDGER'S SECOND LINE FORM, WHICH THE GATE NEVER READ — a wrong finding of mine first** | preparing this close's rollover, a census with the gate's own key pattern (`^- Sessions? KEY`) read 14z-175 as archived at the 14z-177 close WITHOUT a ledger line — and I told the maintainer so, then found the line exists as `- **14z-175** (2026-09-23) — …`, a SECOND FORM that four lines use (14z-175, 14z-171, 14z-169, 14z-156), which `check_state_lists.py`'s `LEDGER_KEY_RE` does not match: the gate's "every ledger key resolves" had never read those four (all four DO resolve, measured). Corrected to the maintainer in the next message. Measured with both forms: 205 form-A lines, 4 form-B, 384 `## Session` headings below the ledger over 233 distinct keys AFTER this close's rollover (the packet file's second cut; before it, one heading and one key fewer — 14z-176's — by arithmetic), 36 keys with no ledger line in either form (all 14z-147 or older; whether a group headline's `[+N more entries]` covers them was NOT checked). FIXED: the checker reads both forms and the gate gains control `unresolved-ledger-key-bold` (a planted form-B line with no record must fail; both ledger modes reach the gate's FAIL, exit 1); 14z-176's line below is written in form A. The reverse check (every record has a line) is NOT built and no ruling asks for it — the 14z-154 check was written as key resolution; whether to want it is the maintainer's. Rule-checker run `2026-09-24-138` (Q1) also caught that the packet's gate log had been cut to its last three lines, showing the wrong control under the old mode's label: re-captured in full. `build/agent172/pkt179/ledger_coverage_14z179.txt` keeps both cuts, the wrong one first |
| **(4) THE RECOMMENDATION THROUGH THE RULE-CHECKER — the pinned reader's third real run** | run `2026-09-24-138` (`recommendation`, plant `inferred-decision-14z162` CAUGHT on Q5; both readers spawn-checked at `claude-opus-5-5`/`high`, prompt IDENTICAL, no fallback) returned **VIOLATED on Q1, Q4 and Q5, all TRUE, resolved by work**: the model/effort and refusal reads were opener SNAPSHOTS (83 records; 13:28) while my claim said "every record" — the close now re-reads both over the whole transcript; the close's conditions named four checks where the ruling names five — the hook refusals are now re-read at the close and the orchestrator's own model and effort are read too; "the gate's ruled rule is key resolution" rested on no ruling (withdrawn; the 14z-154 check was WRITTEN that way); STATE quoted pre-rollover ledger figures (383/232) beside a post-rollover artifact (384/233) — both now given; the packet's gate log was `tail -3` of each mode, showing the bold control under the old mode's label — re-captured in full; and "the maintainer started it with an explicit model" was an inference from the settings key — now the measured fact only |
| **(5) THE RULINGS** | *"Close it on green (Recommended)"* — #172 is closed at this close if the ruling's list reads green (hook refusals re-read, the sweep, the procedure check over BOTH extracts, `--cap` over every worker, the orchestrator's model and effort over the whole transcript); *"No ticket (Recommended)"* — the ledger's reverse check is not built, the 36 keys stay recorded in row (3); *"Standing step (Recommended)"* — after run 139 caught the cap read written into the close as standing without a ruling, it was withdrawn and put to the maintainer: every close runs `--cap` after the procedure check. Verbatim in `DECISIONS_HISTORY.md` "Ruled 2026-09-24 (14z-179)" |
| **CLOSE (14z-179, 2026-09-24) — THE CHECKLIST** | **(1) THE FINDINGS TABLE** — 10 findings (the tenth from the procedure check: a reader echoing a claim's figures — `rule_checker.md` "What it will not catch" / the extract's `W#` line, run 140 QP3), each with its live home and the test that reproduces it: 14z-178's item (5) count (its close row, corrected in place / none — a record correction); 14z-178's unchecked tail (`build/agent172/extract_55857809_tail.txt` / this close's procedure run `2026-09-24-140`, OK on both spans' QP1-QP2/QP4-QP5); the ledger's SECOND line form, unmatched by the checker (`tools/check_state_lists.py`'s comment, NEXT_SESSION trap 2 / `test_state_open_lists` control `unresolved-ledger-key-bold`, exit 1 as a mode); 36 archived keys with no ledger line of their own (row (3), `build/agent172/pkt179/ledger_coverage_14z179.txt` / none — ruled *"No ticket"*); a mid-sitting read is a snapshot, not "every record" (NEXT_SESSION trap 1 / the close's re-reads, a step not a gate); `tail -N` of a control mode shows whichever control printed last (NEXT_SESSION trap 3 / none — the packet artifact is kept in full); two inferences rewritten as measured facts — the "explicit model" and the "ruled rule" (row (4) / rule-checker run `2026-09-24-138`); the S5 gate's result (scope doc STATUS, row (4) of this group, the scope doc's S5 row / the three close reads, `--cap` exit 0); the `--cap` read as a standing close step — written as standing BEFORE anyone asked (rule-checker run `2026-09-24-139` Q5 caught it), withdrawn, then RULED *"Standing step"* (row (5), STATE's header / none — a procedure). **(2)** `close_findings.py 14z-179 --base 576eeee5`: **0 addresses, 0 GAPS, 0 REVIEW, 3 PROMISE lines — all three QUOTES of the maintainer's "take the next session items in order" (the third being item (4) of this row)**; not cited as completeness. **(3) POINTERS:** 14z-178's row item (5) corrected in place (row (1)); 14z-178's START HERE items 0b(a)-(c) are done and gone; the scope doc's STATUS banner and S5 row, and HANDOFF's hooks row, name this sitting as S5's gate. **(4) PROMISES:** none of mine open — the tool's three PROMISE lines are QUOTES of the maintainer's *"take the next session items in order"*, not commitments; the push is this close's last step. **(5) SCRATCH SCRIPTS, ON TRIAL: 3 inline measurements whose figures a record quotes; 0 PROMOTED, named:** the ledger census in both cuts (its output is the packet file `ledger_coverage_14z179.txt`), this session's model/effort read from its transcript (`session_model_reads.txt`, re-cut at the close), and the rollover slice script (its output IS the rollover; the recipe is the memory note and STATE's header). **(6) THE DOCUMENTATION PACKET** — run `2026-09-24-139` (`recommendation`, plant `fidelity-14z160-false-statement` CAUGHT on Q1, recorded through the bound `record --session`) returned **VIOLATED on Q1, Q4 and Q5, all TRUE, resolved by work**: three headlines (STATE's group heading, NEXT_SESSION's, the scope doc's STATUS) stated the unrun gate's result as fact — each now carries the PENDING token inside the headline until the result is in; four figures had no source in the packet (841/4, the opener sweep, run 138's plant and spawn facts, the pre-rollover 232) — cut into `build/agent172/pkt179/` or restated as arithmetic, and the packet file's wrong 'same tree' label corrected; the row said three PROMISE lines in one place and two in another (three, the third its own item (4)); and **the `--cap` read had been written into the close as STANDING when the ruling covered this sitting only** — withdrawn, put to the maintainer, ruled *"Standing step"* (row (5)), written back with the ruling. **THE TIER:** `ROMDIR=... tests/run_all_static.sh --strict` (session cadence, every control executed): **PASS 157, SKIP 0, FAIL 0, MISSING 0, `fired 203 / declared 203`, executed 203 honoured 203, lies 0, refused 0, died 0 — GREEN**, 1410 s wall (gates 455 s, controls 943 s); 17 gates deferred by cadence and NAMED (12 freeze, 5 release). Log `build/static_tier_14z179.log`. **THE SWEEP:** `tools/agent/sweep.py` (after the tier) — **CLEAN: no survivor** (exit 0; `build/agent172/pkt179/sweep_after_tier.txt`). **THE S5 GATE READS AT THE CLOSE:** `transcript_gaps.py --refusals`: **C0.1 0, edit-lock 0, harness 0, permission 0**; `--subagents 8037cb8c --cap`: **6 workers (the six pinned readers of runs 138-140), every one `claude-opus-5-5` at `high`, context none, 0 fallbacks — 0 breaches, exit 0**; the orchestrator's own transcript: **171 assistant records, all `claude-fable-5-1` at `high`, 0 fallback blocks** (`build/agent172/pkt179/s5_gate_close_reads.txt`, read 14:17 after every worker had finished); with the tier, the sweep and the procedure check below, every item of the ruling's list is GREEN — #172 CLOSED (its row `done` with the four answers, the closing comment on the issue, `tickets_github.tsv` refreshed). **THE PROCEDURE CHECK:** `tools/agent/extract.py --session 8037cb8c` (515 lines, 6 workers, 0 detached launches, no open task) plus 14z-178's tail (row (2)), run `2026-09-24-140` (`procedure`, plant `proc-promise-14z174` CAUGHT on QP1+QP4, recorded through the bound path, both readers spawn-checked) — **OK on QP1, QP2, QP4 and QP5 over both spans, VIOLATED on QP3, TRUE of a worker:** the plant-side reader of run 139 reported "the pins land at 2550/2560" as read from two lines of its staged file that hold only the expressions (`{t - 40}`, `FIRST_EVENT - 50`; neither figure occurs in the file, grepped) — it echoed the claim's own figures, and the extract's `W#` line is what saw it. Resolved in writing; nothing of mine carried the figures; recorded as a reader limit in `rule_checker.md` "What it will not catch". The span after record 721 (these results, the ticket close, the commit, the push) is carried to the next close (NEXT_SESSION 0b). **THIS ROW WAS WRITTEN BEFORE ALL OF THEM** except the sentences stating their results. The push follows, through the hook. |

## Session 14z-178 — **#172 SLICE S4 LANDED IN FULL: the pinned rule-checker, after measuring that no reader had ever been context-free — every one was handed CLAUDE.md and the memory index — and with every reader now bound, at `record`, to what its own transcript shows.** The maintainer: *"yes, we need #172 before anything else"*. **Measurement moved the design four times:** subagents see CLAUDE.md, and interactively the memory index the docs say they do not; `omitClaudeMd: true` removes both; a definition written mid-session WAS spawnable (14z-177 had found otherwise on 2.1.280); and a worker can silently finish on another model after a safety-classifier stop. **The pinned reader's first real run (`2026-09-24-134`) found three true defects in my own report**, all resolved by work.

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, `docs/NEXT_SESSION.md` — asked for as `docs/project/next_session.md` again), `main` == `origin/main` at `d00232da`, `emu/fbneo` dirty by exactly its two tracked patches (841/4), `python3 tools/agent/sweep.py` CLEAN (exit 0). The maintainer: *"yes, we need #172 before anything else"* |
| **(1) THE FIRST ACT OF STEP 4: DOES A READER SEE CLAUDE.md? — YES, AND THE MEMORY INDEX TOO** | the archive first (`transcript_gaps.py --subagents`, now printing each worker's CONTEXT from its `instructions` attachment's `files` list — a text match over the record missed every one): every `general-purpose` worker since 2.1.270 (217 on the morning's read) and the one `measurer` carried `Project:CLAUDE.md` and `AutoMem:MEMORY.md`; Explore, Plan and forks none. Then controlled: probe leg **A13** (a random CLAUDE.md codeword, asked for with no tool) — the main session, a rule-checker-shaped definition and `general-purpose` named it, each worker's answer agreeing with its attachment; the same definition with **`omitClaudeMd: true`** (documented, found by a docs lookup) named nothing and carried nothing; a headless session loads NO memory, so A13's memory half is VOID. Interactively, a no-tool `general-purpose` worker quoted `MEMORY.md`'s first three entries (the docs say a non-fork subagent never gets it). Homes: `docs/platform/gotchas.md` (new entry), scope doc §2, `rule_checker.md` "The context" |
| **(2) A10 WENT RED ON 2.1.281 — A THIRD REPORT FORM** | a headless run now launches every worker in the BACKGROUND and its report arrives as the `<result>` of the completion `<task-notification>`; the probe's matcher learned it (and stopped counting a notification as the prompt), and `extract.py` now sources it with an `H` line (selftest 21 checks; the new check fails on a copy with the block disabled). `42997845` |
| **(3) THE PINNED READER** | `.claude/agents/rule-checker.md` — `opus`, effort `high`, Read/Grep/Glob, `omitClaudeMd: true` (the 14z-175 ruling: "never CLAUDE.md"); `agent_defs.py` requires it and the key (control `context-leak`), two call-gate cases (27). Its first interactive spawn ran on Opus 5.5 at `high` with NO attachment and named neither file, beside the `general-purpose` worker that had quoted `MEMORY.md`: `omitClaudeMd` removes BOTH. **The harness then announced it mid-session ("New agent types are now available")** — 14z-177's "a session loads its agent list at START" was true on 2.1.280 for that moment and is corrected in place |
| **(4) QP5 + QP3 OVER WORKERS, AND THE BINDING** | the procedure checklist gained QP5 SPEC-CONFORMANCE and QP3 over a worker's figures, with its legend for the extract's worker lines; `proc-planted-spec-14z178` (one worker command off-spec in the frozen real `measurer` run — the extracts differ by that one line) and its clean twin `proc-clean-worker-14z178` (ten fixtures, not the nine ruled: `proc-clean-14z176` shows no worker, so QP5 could not be shown QUIET there, [VSP-19]). `rulecheck.py`: each run records the questions it was asked (older ones parse under their legacy set) and its reader's sha; a calibration counts only if the CURRENT definition read it (control `moved-reader`). First recalibration, runs `114..123`. `adba8f08` |
| **(5) THE ALIAS HOLE, A14, AND THE FALLBACK** | reading #158 against what I had built: `model: opus` is an alias that can move to a new model with the sha unchanged — pinned to `claude-opus-5-5` (`agent_defs.py` refuses an alias, control `alias-model`). Leg **A14** (a version id under a haiku parent) FAILED reading `claude-opus-4-8`: a **safety-classifier stop** on the worker's first answer had made Claude Code FALL BACK, recorded only as a `fallback` content block — 2 of 2 probe runs, 0 of 1 ad hoc. A14 now reads the model REQUESTED; 14/14 PASS. `spawned` checks each reader's own transcript (model, effort, fallback, instructions attachment, prompt verbatim), `collect` writes each verdict from the reader's own hand-back, never retyped. Second recalibration on the version-id sha, runs `124..133`, every reader verified. `9bef83c0` |
| **(6) THE ARCHIVE SHRANK UNDER THE TOOLS** | `--subagents all` read 288 workers in the morning and 280 later, 15 added between: Claude Code deleted the 23 workers of the 2.1.240 sessions at its 30-day retention default (`cleanupPeriodDays`), most likely when the probe's `claude -p` runs started. A census over `~/.claude/projects/` is a census of the last 30 days; figures are now dated. Gotcha; ruled (below) |
| **(7) THE PINNED READER'S FIRST REAL RUN — ON MY REPORT** | run `2026-09-24-134` (`recommendation`, plant `forced-pick-14z159` CAUGHT) returned **VIOLATED on Q1, Q4 and Q5, all TRUE, resolved by work**: Q1 my claim inferred "every reader before 114 was general-purpose" from a census by TYPE — the new `rulecheck.py readers` links runs to readers: 107 of 113 runs linked, all 192 readers `general-purpose` with CLAUDE.md and MEMORY.md, six runs link to none (why not measured); Q4 `record` did not require the spawn check — **now it does** (it refuses a pinned-reader run without its transcript, or with any failing reader; section RECORD BINDING, control `unbound-record`); Q5 **"a change of model is a recalibration" was never the maintainer's words** — theirs were *"I am fine with default model. We can always recalibrate later if we need, right?"*; the rule is the spec's ([VSP-184]); STATE's superseded default-model line deleted, the scope doc and `agent_defs.py` corrected, the archive marked |
| **(9) S5 MEASURED BEFORE PROPOSED — `--agent` REPLACES CLAUDE CODE'S DEFAULT SYSTEM PROMPT** | the maintainer: *"Scope S5 now (Recommended)"*. The prompt_snapshot of an `--agent` main session is the definition's body (64 characters against about 28,200 — 28,207 to 28,209 over the runs that read it), and a discriminator confirmed it on the model's side: asked for the first "IMPORTANT:" sentence of its system prompt, the plain session quotes the default prompt's *"Assist with authorized security testing, …"* and the `--agent` one the preamble heading CLAUDE.md — CLAUDE.md is kept, even with `omitClaudeMd: true` (probe A15/A16); `--append-system-prompt` keeps the default (A16c). **Two discriminators failed first**, both recorded in the leg: "the first sentence of your system prompt" (an SDK preamble outside the snapshot) and "the primary working directory" (the environment block reaches both); and one run of mine died on `$extra` unsplit in zsh (the known trap). Gotcha: `docs/platform/gotchas.md`. So no orchestrator definition: a plain session on Fable 5.1 |
| **(10) THE RULE-CHECKER ON THE S5 PROPOSAL** | run `2026-09-24-135` (plant `inferred-decision-14z162` CAUGHT; the first run recorded through the BOUND `record --session`) returned **VIOLATED on Q1, Q4 and Q5, all TRUE, resolved by work**: the effort a plain Fable session runs at was asserted from the settings file, never measured — now probe leg **A17** (a plain session takes the per-model `modelSettings` effort; discriminated by claude-opus-5 at `xhigh` against a top-level `high`; Fable 5.1 at `high`; 16/16 legs PASS); and "the ruled gate" was the scope doc's PROPOSAL — put to the maintainer instead |
| **(8) THE RULINGS** | *"Close it (Recommended)"* — #158 closed with its row, four answers and a closing comment that also corrects the issue's misattribution; *"You set it, user level (Recommended)"* — the maintainer adds `cleanupPeriodDays` to `~/.claude/settings.json`; *"Plain session (Recommended)"* and *"That gate (Recommended)"* — S5 is a plain Fable 5.1 session the maintainer starts, gated by that sitting's close with `transcript_gaps.py --subagents <session> --cap` (new: exits 1 on any worker above Opus-class, with no model, or that fell back; its selftest fails on a copy whose cap admits Fable). Verbatim in `DECISIONS_HISTORY.md` "Ruled 2026-09-24 (14z-178)" (two entries) |
| **CLOSE (14z-178, 2026-09-24) — THE CHECKLIST** | **(1) THE FINDINGS TABLE** — 17 findings (the 17th found by the procedure check: a worker's text written after its hand-back was read as its report — `extract.py` / its selftest case), each with its live home and the test that reproduces it: a subagent is handed CLAUDE.md (`docs/platform/gotchas.md` / probe A13); interactively also the memory index, against the docs (gotcha / NO scripted leg — a headless session loads no memory; the evidence is cut to `build/agent172/pkt178/memory_probe_evidence.txt`, outside the tree); `omitClaudeMd: true` removes CLAUDE.md (gotcha / A13 `w-rco`) and the memory index (gotcha, `rule_checker.md` / every pinned-reader run: `record --session` refuses a reader with an instructions attachment); a definition written mid-session was spawnable on 2.1.281 (gotcha, corrected in place / none — observed once); the third report form (gotcha, `extract.py` / A10 and the extract selftest); every linkable reader before run 114 had CLAUDE.md and the memory index (`rule_checker.md` "The context" / `rulecheck.py readers`, a tool, not a gate — six runs link to no reader and why is NOT measured); an alias model can move under a pinned sha (`agent_defs.py` / `test_agent_defs` control `alias-model`); a safety-classifier stop triggers a silent model FALLBACK (gotcha / A14, the `spawned` selftest's fallback case, `transcript_gaps.py --cap`'s selftest); Claude Code deletes transcripts after 30 days (gotcha / none — measured by the census count, 288 -> 280); `--agent` replaces the default system prompt and keeps CLAUDE.md (gotcha / A15, A16), `--append-system-prompt` keeps it (A16c); a plain session's effort is the per-model setting (scope §4 O / A17); "a change of model is a recalibration" was the agent's gloss, not the maintainer's words (DECISIONS_HISTORY marked, scope doc, `agent_defs.py` / none — a record correction); `record` was not bound to the spawn check (`rule_checker.md` / `test_rule_checker` RECORD BINDING, control `unbound-record`); the calibration binds the definition's sha (`rule_checker.md` / control `moved-reader`); QP5 (`rule_checker.md` / `proc-planted-spec-14z178` and `proc-clean-worker-14z178`). **(2)** `close_findings.py 14z-178 --base d00232da`: **0 addresses, 0 GAPS, 0 REVIEW, 0 PROMISES**; not cited as completeness. **(3) POINTERS:** 14z-177's row (1) "a session loads its agent list at START" marked CORRECTED in place; the scope doc's stale "Next: S4" banner rewritten; NEXT_SESSION's START HERE item 1 (S4 step 4) is done and gone. **(4) PROMISES:** the one open item was the maintainer's — `cleanupPeriodDays` — *DONE after the push: the maintainer's "done", verified (`~/.claude/settings.json` valid JSON, top-level `cleanupPeriodDays: 365`, the other seven keys intact)*; S5's gate is a sitting the maintainer starts (item 2). **(5) SCRATCH SCRIPTS, ON TRIAL: 8 whose figures a record quotes; 5 PROMOTED** *— CORRECTED 14z-179 (carried from 14z-178 on the maintainer's "Let's carry them."): 9 quoted, 5 promoted, 4 NOT promoted — the fourth is the census-reads cutter behind `build/agent172/pkt178/census_reads_14z178.txt`, named in item (6) below and left out of this count* — the archive context census (`transcript_gaps.py --subagents` context + census), the fallback scan (its FALLBACK count), the prompt-fidelity check (`rulecheck.py spawned`), the S5 discriminators (A15/A16/A16c), the per-model effort run (A17); **3 NOT promoted, named:** the memory-probe evidence cutter (its output is the packet file), the four-way alias/id model run (A14 covers the id; the alias under a haiku parent is not a leg), and the `du` of the transcript archive (604 MB). **(6) THE DOCUMENTATION PACKET** — run `2026-09-24-136` (`recommendation`, plant `fidelity-14z160-false-statement` CAUGHT, recorded through the bound `record --session`) returned **VIOLATED on Q1 and Q4, all TRUE, resolved in the records**: `rule_checker.md` said EVERY reader of the ledger had CLAUDE.md and the memory index (now: every reader linked to a run before 114, 192 over 107 of 113 runs, six unlinked); the snapshot size was quoted as a fixed 28,209 (it is 28,207-28,209 over the runs); the gotcha's 280 and the packet's census file (294) are two reads at different times, now both named; my own DECISIONS entry said the maintainer HAD set `cleanupPeriodDays` when they ruled they WOULD; and five figures came from tool results the packet did not carry — now cut, verbatim with their record numbers, into `build/agent172/pkt178/census_reads_14z178.txt`. **THE TIER:** `ROMDIR=... tests/run_all_static.sh --strict` (session cadence, every control executed): **PASS 161, SKIP 0, FAIL 0, MISSING 0, `fired 205 / declared 205`, executed 205 honoured 205, lies 0, refused 0, died 0 — GREEN**, 1936 s wall (gates 901 s, controls 1023 s); 13 gates deferred by cadence and NAMED. **THE SWEEP:** `tools/agent/sweep.py` — **CLEAN: no survivor**. And, as a record rather than a gate (this sitting ran on Opus 5.5), `transcript_gaps.py --subagents 55857809 --cap`: 0 breaches. **THE PROCEDURE CHECK:** `tools/agent/extract.py --session 55857809` (2,832 lines, 36 workers, 0 detached launches, no open task), run `2026-09-24-137` (`procedure`, plant `proc-planted-claim-14z176` CAUGHT on QP2; QP5's first real reading) — **OK on QP1-QP4, VIOLATED on QP5, and the finding was the INSTRUMENT's**: the memory-probe worker handed back exactly the three names its spec asked for, then wrote a prose text AFTER the hand-back that was never delivered, and `extract.py` took a worker's LAST text as its report. Fixed (the delivered hand-back is the report; a selftest case, and a copy ignoring the hand-back fails it); resolved in writing; `test_agent_extract` and `test_agent_worker` green with every control mode failing its gate. The tier was then RE-RUN on the fixed tree: **PASS 161, SKIP 0, FAIL 0, MISSING 0, `fired 205 / declared 205`, executed 205 honoured 205, lies 0, refused 0, died 0 — GREEN**, 1897 s wall; the sweep again **CLEAN**. **Two STATE edits of mine failed silently here** (an anchor that matched 14z-177's row too, refused by the edit's own assertion, the failure hidden inside a background command) and were re-applied after the commit that should have carried them. The push follows, through the hook (run 137, resolved, checked `63cb2d1d`, inside the pushed range). **THIS ROW WAS WRITTEN BEFORE ALL OF THEM** except the sentences stating their results. |

# STANDING SECTIONS (current state — never archived)
## Standing rulings

One line per ruling that still constrains work; the full entry lives where the line says. A line is deleted when its ruling stops constraining work (CLAUDE.md [VSP-17]).

- **#172, the agent architecture (2026-09-23, 14z-175 — "Hard block + written resolve is fine but that doesn't mean that the session should grind to a halt"):** a block binds the TASK AT FAULT, never the session — redone or corrected, re-checked, unblocked when green; deterministic hook checks hard-block, the model checker blocks until resolved in writing; the checker reads a generic checklist plus a transcript extract, never CLAUDE.md; the model checker runs at every push and every close; slice S1 (the deterministic hooks) goes first, alone — and S1 is INSTALLED as C0.1 (`.claude/settings.json`, only the maintainer edits it) with C0.2 moved to the procedural checker (*"Install + protect"*, *"Move it to C1"*). S3, ruled 2026-09-23 (14z-176, *"Build it as described"*), is the procedural checker as QP1-QP4 on the rule-checker's machinery, QP2/QP3 fixtures planted into a real clean span; the `agentlib` tracked-launch fix was APPLIED by the maintainer (14z-176b), and C1's push hook INSTALLED (*"since we both think the push hook is a good idea, I'll install it"*) — a push of this repository now needs a passed or resolved `procedure` run covering a commit in its range. S4, ruled 2026-09-23 (14z-177): workers' caps are enforced on the CALL (a hook) and by a static gate on every definition, FORKS pass as the orchestrator (*"Allow forks"*); the rule-checker becomes a pinned definition (opus, effort high) after a recalibration (*"Pin it now"*); C1 widens QP3 to workers' figures and gains QP5 SPEC-CONFORMANCE (*"QP3 wider + QP5"*); built in the proposed order (*"As proposed"*); the call gate `pre_agent.py` INSTALLED by the maintainer (*"I'll install it"*, *"done"*); step 4's pinned readers are SUBAGENTS (*"Subagents"*), its first act measuring whether a reader sees CLAUDE.md. Step 4 LANDED 14z-178: the pinned `rule-checker` (a version id, `omitClaudeMd: true`) with every run's readers bound to the spawn check at `record`; #158 closed on it (*"Close it"*), and the maintainer raises `cleanupPeriodDays` at user level (*"You set it, user level"*). S5 RULED 2026-09-24 (14z-178): no orchestrator definition — a PLAIN session the maintainer starts on Fable 5.1 (*"Plain session"*), gated by one real sitting under it whose close reads the C0/C1 record and `transcript_gaps.py --subagents <session> --cap` (*"That gate"*). That sitting was 14z-179, on Fable 5.1, and #172 closes on its green (*"Close it on green"*); the worker cap read (`--cap`) is a step of every close beside the procedure check (*"Standing step"*, 14z-179). Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-23 (14z-175) — #172", "— #172 slice S1" "Ruled 2026-09-23 (14z-176) — #172 slice S3", "Ruled 2026-09-23 (14z-176b)" and "Ruled 2026-09-23 (14z-177)" and "Ruled 2026-09-24 (14z-178)" (two) and "Ruled 2026-09-24 (14z-179)".
- **#171 Q6, the 73 poke read-back findings are RULED as proposed (2026-09-25, 14z-181 — "I agree with the proposal"):** 40 OBSERVES, 29 READS-BACK (21 never-compared FIELDS columns DROPPED from eight gates and RETIRED, the rest labelled in their gates' headers; one row corrected from the proposal's OBSERVES after rule-checker run 2026-09-25-147 — `test_pyron_cosmo`'s no-reset guard reads the poked id back), 3 CONTROL-PLANT and 1 CROSS-LEG, the two new classes in the table; every drop re-measured on its gate before commit. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-25 (14z-181) — #171 Q6".
- **#171, gate qualification (2026-09-24, 14z-180 — "As proposed WITH THIS ADDITION: I want for each test a human-readable description of the test (at least what it tests, how it tests and what is the expected result)"):** the shape is `docs/project/gate_qualification_scope.md`'s seven slices in the order Q0 (the WHAT/HOW/EXPECTS description in every gate's header, rendered per family by the GENERATED gate index, landing by family for the maintainer's review), Q1 (the module-reference gate — landed this sitting, *"Land it now"*), Q3+Q4 with Q5 inside (`# FOLLOWS:` header declarations with a reconciliation control — *"Header line"*; the runner records its commit and a session gate names STALE emulator gates — NOTE at session, FAIL at freeze/release; headroom fails at half the cap), Q2 (the MEASURES/MEASURED grammar), Q6 last (the poke read-back census, per-finding rulings still to come). Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-24 (14z-180) — #171 gate qualification". **14z-180 close: every slice is BUILT (Q0-Q6, four commits `bd31b1a3`..`54b92736`); what the ruling still constrains is the REVIEW — a description or a declaration is corrected in the gate's header, never in the generated page — and the 73 poke read-back rulings under "Decisions pending".**
- **FBNeo patch 0003 (drop SDL2_image) is RATIFIED for RELEASE builds (2026-09-21, 14z-173 — "in that case then yes, i approve of patch 0003"):** frontend-only, applied by `build_release_emulators.sh` and NOT by `setup_fbneo.sh`; the macOS bundle goes 24 files -> 4, so "Open Anyway" costs 4 approvals instead of 24, and it is proven inert over 12,120 frames in work RAM AND framebuffer. Full entry: `DECISIONS_HISTORY.md` "Ratified 2026-09-21 (14z-173) — FBNeo patch 0003".
- **#144 is macOS blocking the binaries and right-click > Open does NOT get past it (2026-09-20, 14z-173 — the maintainer's captures and "Does not open"):** the shipped advice is withdrawn from every live carrier; clearing the quarantine flag is what works, `PLAY.command` offers it, and System Settings > "Open Anyway" also works but PER FILE — **MAME 2 approvals, FBNeo 24** — so the READMEs lead with the one-step route; the ticket now turns on whether to notarize (a paid Apple account). **The player READMEs are written for a non-developer who has never seen this project** ("it must have felt clear to the expert who wrote them"). **The applier page ships as a local file in the asset, one page per release.** Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-20 (14z-173) — #144 is macOS BLOCKING the binaries".
- **The applied set is DEFLATED and the QSound BIOS member is OPTIONAL (2026-09-20, 14z-173 — "If it is identical content, I see no reason not to go for the smaller size" and "Ideally I'd love the option to choose to add it or not"):** `apply_release.py` writes deflated (73.7 -> 28.0 MB, +0.26 s once at load, measured) and `--no-qsound-bios` omits `dl-1425.bin`, which MAME needs, FBNeo does not, and MiSTer resolves from the card's `qsound.zip`; the MiSTer page tells players to use the flag and the emulator pages the default. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-20 (14z-173) — the applied set is DEFLATED".
- **The deliverables' order of work, and macOS FIRST (2026-09-20, 14z-173 — "Agreed with one caveat: let's focus on MacOS first, even if we were to release M19 only for MiSTer and MacOS, the proof of concept would be fully validated"):** standalone set -> launcher per platform -> reproduce #144/#145 on clean hosts -> README pass -> the applier app; macOS and MiSTer are the proof-of-concept release and Windows/Linux run in parallel without blocking it (so #145 leaves the near-term path, #144 and #146 stay). Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-20 (14z-173) — THE DELIVERABLES".
- **The standalone set lands in the RELEASE, not the build (2026-09-20, 14z-173 — "C"):** the applier completes `vsavjw.zip` with 7 pristine members so a player places ONE zip; no build fingerprint, expectation set, registry row or MiSTer CRC moves, and the shipped set being a declared superset of the gated one is held by `test_release_roundtrip` §1 and `test_release_binaries` §2b, never by argument. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-20 (14z-173) — the standalone set lands in the RELEASE".
- **The disabled EX route does the no-stock move (2026-09-18, 14z-169 — "Yes, the no-stock move"), refined 2026-09-19 (14z-170 — "Natural reading (Recommended)"):** the tenants' vs2 EX input is read as its sub-pattern at every stock level — for Phobos and Donovan vs2's no-stock move; for Pyron 623+PP, the ES move when a stock is available and the normal move without one. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-169) — the disabled EX route does the NO-STOCK move" and "Ruled 2026-09-19 (14z-170) — Pyron's 2623+PP with stock reads NATURALLY".
- **The 0x52 fix is scoped S1 (2026-09-18, 14z-169):** Phobos's two trap remaps stay on the solo Phobos track only (a row key: unless Donovan's manifest is in the composition); the merged build carries the full fix. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-169) — the class-0x52 fix is scoped".
- **#136: all the analysis first, then the fixes, then relentless regression testing (2026-09-18, 14z-168 — the maintainer: "let's finish all the analysis first. Then we'll fix and then relentlessly test for regressions"):** no #136 fix (nor #157, #159 or the 14z-168 tickets) lands before every family is analysed; every rig built for it becomes a gate that runs at freeze and release. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — #136: ALL the analysis first".
- **The tenants' Dark Force (2026-09-18, 14z-168):** vsav's Dark Force CHANGE with the shells' common rules, each tenant's own vs2 startup window, and its personal Dark Force (vs2's EX move) active for the mode; compared against the vs2 EX move, never vs2's P+K (Dark Force POWER); the DURATION stays vsavj's (360/377/360, *"agreed"*); the tenants' vs2 EX input into the mode on our build is to be DISABLED (*"agreed"*). Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168)" (the Dark Force and the EX-route entries).
- **The tenants' DEFENSE rows become vs2's (2026-09-18, 14z-168 — "given the measurements we should take the vs2 rows"):** Phobos and Donovan take their vs2 defense rows and rally thresholds (Pyron needs nothing), with no frame cost and no change to any legacy character; supersedes 2026-08-14 option (b). Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — the tenants' DEFENSE rows become vs2's".
- **The column shock and the Plasma Trap take vs2's class-0x52 rule (2026-09-18, 14z-168 — "I'm all for fixing"):** victim shock 0x18 and the attacker exempt, for the tenants' 0x52-origin hits; no noticeable lag, no new breakage; the 14z-85g(2) accepted deviation is withdrawn. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — the column shock and the Plasma Trap".
- **No gauge is built during Dark Force (2026-09-18, 14z-168 — "it makes sense that you can't build meter during DF"):** the shells' rule; the tenants' start-up gauge in the mode is a defect (#157's Dark Force tail). Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — no gauge is built during Dark Force".
- **The close checklist (2026-09-18, 14z-167b — the maintainer: "let's implement them and keep item 2"):** at every close, (1) a findings table — each finding of the group, its live home and its test; (2) `tools/close_findings.py <session>`, every GAP homed and every REVIEW answered, its clean result never cited as completeness; (3) a pointer on every earlier row the sitting resolves; (4) a grep for promises; (5) cited scratch scripts promoted, on trial and measured per close; (6) the documentation packet through the rule-checker. The steps live in this file's header (ruled 2026-09-18). Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-167b)" (two).
- **#136 goes over again by the 14z-164 census (2026-09-17, maintainer: "I agree with the 3 propositions"):** the move-parity gate moves to PER-EVENT verdicts with the stock, the meter fraction and P2's HP compared and every "in DF" event asserted in DF; a P2 whose data is the same on both games (a second tenant, or a legacy character byte-identical between vsavj and vs2, found statically) — done 14z-165, Demitri; the 13 rows re-labelled by their fields on #136 before any family is root-caused. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-164)".
- **The rule-checker's Q3 and Q4 are BOUNDED, and a VIOLATED is resolved question by question in writing (2026-09-17, 14z-163, #152 — the maintainer: "both"):** Q3 counts only a shared thing the artifacts SHOW can write the compared state; Q4 reads OK when the claim itself names the uncovered mode; what a bounded question still finds stops the action until `rulecheck resolve` answers each violated question. Every fixture recalibrated on the new wording. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-163) — the rule-checker's questions".
- **#136 is gone over again IN FULL, not only through its faithfulness control (2026-09-16):** the maintainer insists it stays on the list until every part of it — the naming rigs, the pinning protocol, the 27 frozen verdicts, and the Pyron and Phobos legs above all — has been re-examined; no #136 verdict is built on until then. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-16 (14z-160)".
- **The M19 freeze is WITHDRAWN (2026-09-16):** #147's fix was a regression on a rig artifact; the tree is back at merged-m18 and #151 re-examines #136's forced-pick native legs. No freeze is current beyond merged-m18. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-16 (14z-159)".
- **The static tier runs by CADENCE (2026-09-17, #148 lever A):** `tests/ci_cadence.tsv` lists twelve build/content/tooling gates as `freeze` (the seven costliest plus the five 35-39 s gates, second cut) and five release/MiSTer gates as `release` (the three ≤2 s ones back to session); a session run (the default, mid-session AND at the close) defers them BY NAME unless a path they depend on changed; a freeze runs `--cadence freeze`, a release `--cadence release`, each on the commit it builds from. A new listing is a reviewed event. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-162) — #148 lever A".
- **Static-tier controls at the CLOSE (2026-09-14; REAFFIRMED 2026-09-17 after 14z-161 pushed on the mid-session form):** a mid-session commit runs `tests/run_all_static.sh --strict --exec-controls none` plus `CONTROL=<name>` for every gate it adds or changes; the session close runs every control (the default) and ONLY then pushes — not to be compromised until #148 is worked. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-14 (14z-154)" and "Ruled 2026-09-17 (14z-161b)".
- **The order of work (2026-09-17):** a stable, reliable state first, then #148 (the static tier's cost, done), then #152 (the adversarial rule-checker, done 14z-164b), then everything else — the #136 families included. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-161b)".
- **Release scope (2026-09-02):** at release every test whose SUBJECT is the released artifact runs, its legacy content included (a reference leg on vsav2 or pristine vsavj is not the subject); anything red or skipped is a hard fail unless approved at release time, so a release-scope gate fails loudly on a missing prerequisite. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **A red gate is a question (2026-09-02):** before fixing the gate, fixing what it caught or deleting it, establish which side's expectation rests on a measurement; a frozen number whose provenance cannot be named is a claim. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Cross-game comparisons at a matched speed level and a pinned RNG (2026-09-15, #135, #142; amends the 2026-09-02 #114 line):** vsav2 defaults P1 to TURBO and vsavj to NORMAL, and the two games' RNG states differ, so `tests/test_don_immortal_native.sh` pins `RAM:$FF8116` (levels 6 and 8) and `RAM:$FF80D4-D5` on both legs and asserts ours == native in hit count, damage and hit frames; LP's accepted "one hit short" is retracted as a mode artifact and #142 is closed invalid; the 2026-09-02 ruling that the vanilla vsav engine takes precedence stands. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-158)" and "Moved 2026-09-14 (14z-154)".
- **No static substituted-wheel gate (2026-09-02):** declined — the pattern matches 26 legitimate gates; the defence is [VSP-163] (assert `+0x60` against `bases.tsv`, or force the pick). Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Oboro's intro stays (2026-08-28):** never delay round start or cut the intro for a cosmetic reason. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Community data (2026-08-31):** measurement is king — a community source that matches ours exactly or by a constant offset validates our measurement, and an inconsistent pattern means re-check OUR measurement first. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Cosmetic is optional (2026-08-27):** cosmetic and single-player-only surfaces are nice-to-have; competitive 2P versus is the focus. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **No legacy character meets a tenant in 1P arcade (2026-08-28):** not a problem, 2P versus is the focus; not built, not scoped. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **The living-docs generalisation, when scheduled (2026-09-08, #119):** independent of bbh but compatible with it and on the same principles; the site generator in scope; fidelity required in principle, its form argued in the scope document; the open items first. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Linux release binaries (2026-09-13, #121):** self-containment is checked against an external host-provided list plus the ruled exceptions; the WSL2 Linux binaries are never published. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Generator refactor policy (2026-08-21, #50):** no dedicated refactor of `tools/gen_donovan_patch.py`'s `main()` is ever scheduled absent a new measured cost; a handler moves to module level only when a test needs to drive it, with its unit test in the same change, and byte-identity under `tests/test_m3a_reproducible.sh` and `tests/test_phasec_spaces.sh` is the refactor gate. Full entry: `STATE_HISTORY.md` "Session 14z-102 (post-freeze rulings)".
- **Don't assume; measure first; ask if necessary (2026-09-15, clarified 2026-09-16):** no factual claim — about code, a count, a behaviour or a name — is stated without a measurement or a tree source behind it; a question a measurement can settle is measured first and never published as "unreconciled"; asking the maintainer is right when a measurement cannot settle it or is not enough. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)" and "Ruled 2026-09-16 (14z-158b)".
- **Pipeline-image registry rows (2026-09-15, #96):** the M2 battery dispatches through `tests/expected/registry.tsv` on the build's fingerprint, never a pinned set name; `donovan-mN-stock` / `-stage4` register its two pipeline images (untagged, never shipped); each freeze rebuilds both and records whether they moved, and an unregistered fingerprint is a rule-6 stop. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)".
- **The merged set carries no tenant expectations (2026-09-15, #111; the 14z-133b B2 design):** the merged expectation set holds the legacy classes, the mask and the `.skip` markers; tenant-content `.sha1` expectations live in the solo sets, and the merged build's tenant content is covered by `audit_merged_legacy` leg (b) and the tenant gates. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)".

## STANDING PRINCIPLE (maintainer, 2026-08-05): vanilla wins ties

**[VSP-21]** "vsav vanilla is always better when we can." **When a console port and
arcade vsav differ and both would work, take vanilla.** A console port's
choice is not evidence that vanilla is wrong; it is evidence of what that
port's designers preferred.

This is a general rule, not a one-off: the PS1 capture is a reference for
what is POSSIBLE and for data we cannot otherwise obtain (cell placement,
the adjacency of NEW cells), not a style guide for content vsav already
defines. Paired with the maintainer's other statement — "as long as we can
select characters it's good" — the test is: does keeping vanilla still let
the feature work? If yes, keep vanilla.

Applied immediately, twice:
- **`Bishamon DL` and `Aulbath DR` stay vanilla** (Anakaris / Sasquatch).
  PS1 sets both to "no move"; neither is needed for reachability, so
  vanilla stands.
- **Horizontal wrap stays vanilla.** Vsav wraps left/right (cell `0x01`
  Left goes to `0x05`, measured and confirmed in-emulator); the PS1 report
  of "no wrapping" reflects untested extremes. We touch none of those
  cells, so nothing to decide.

Judgment applied under the same rule, open to veto: the three inbound edges
from `0x0B` (`D`/`DL`/`DR` into the new row) DO diverge from vanilla, and
strictly they are not required — Phobos and Donovan are already reachable
via `Bishamon D` and `Aulbath D`, and Pyron through them. They are kept
because without them, pressing Down on the cell directly above the new row
does nothing while three medallions are visible below it, which is the UX
failure "as long as we can select characters" is meant to exclude. Dropping
them would reduce the legacy footprint from 5 bytes to 2.

## Decisions pending (human)

Only undecided items live here; a ruling moves its entry to `DECISIONS_HISTORY.md` in the
commit that records it (CLAUDE.md [VSP-17]).

- **The parity gates' per-frame RNG and level pins as their equalised INPUT — a ruling that settles the rule-checker's Q3 for every cross-game gate (opened 14z-181, 2026-09-25).** Every ours-vs-native parity gate (`audit_move_parity`, `audit_df_moves`, `test_don_immortal_native`, their kin) writes the speed level (`RAM:$FF8116` := 6) and the RNG word (`RAM:$FF80D4` := 0) on EVERY frame of both legs — the method the maintainer chose for `test_don_immortal_native` on 2026-09-15 (*"Pin both, assert frames"*, `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-158)") and the gates adopted since 14z-160. The pinned rule-checker returns Q3 VIOLATED on every packet that compares two legs under those pins (runs `2026-09-25-150`, `-152`, `-153`, `-154`): a shared write that selects the compared hits (the RNG's bit 0 picks the pass order), so a port defect in the RNG's ADVANCE would be reset away on both legs and read as agreement. The session accepted the finding in writing as the gates' design (run 153's resolution). Options: (a) RULE the per-frame pins the parity gates' equalised input, with the RNG-advance question its own ticket (a gate that compares the RNG's consumption per frame between the games, which `audit_df_moves`' sword rows already record without a verdict: native 201 / ours 198 reads in one window) — Q3 then reads as a named, ruled premise; (b) require every parity gate to add an unpinned-leg control (`audit_df_moves` has `pins-unpinned` since 14z-181; `audit_move_parity` has `unpinned-level`) and keep accepting Q3 per packet; (c) redesign the comparison to pin the RNG once at the match anchor only (the 14z-160 form) and accept that the two engines' draws diverge. Recommendation: (a) with the ticket — the per-frame pin is what makes a two-engine comparison a comparison, and the advance question is real but separate. Nothing a player feels changes by this ruling.

## THE DEADNESS REGISTER (opened 14z-71, maintainer's standing instruction)

**[VSP-23]** Every claim of the form **"legacy never reaches this, so we may reuse
it"**. Each is measured by ABSENCE, which is the weakest kind of evidence
we accept, so each is listed here with its guard. **These are the FIRST
PLACES TO CHECK for any unexplained regression in vanilla assets, engine
behaviour or rendering** — before anything else is suspected.

| Reused resource | Claim | Guard | Fallback if wrong |
|---|---|---|---|
| ~~palette-seq ids 0x1E-0x21 (`0x39ACC0`)~~ **CLAIM FALSE, ROW WITHDRAWN 14z-79 (they are Bulleta's DF block)** | vanilla only ever requests 0x26/0x27 | `tests/audit_palette_seq_ids.sh` (10,504 sampled calls) | none — the palette path never transits work RAM, so the audit is the ONLY guard |
| effect-class row 16 (`0x080AEC`) | vanilla never dispatches class 16 | `tests/audit_effect_class_rows.sh` §1, 0 reads vs a 1760-hit control | none needed: the row was a stub (`rts`), so a wrong claim costs at most the old no-op |
| ~~drawer list-type 6 (`0x01B6AA`)~~ **CLAIM FALSE (measured 14z-89) — LEGACY LISTS DO REACH TYPE 6** | vanilla has no type-6 sprite lists | `audit_effect_class_rows.sh` §1/§4 + `tests/test_beam_list_type6.sh` | **THE FALLBACK HELD — this is what a safe-and-loud design buys.** 14z-89 measured the tripwire ARMED on legacy content on huitzil-m13: `21_don_mash` 387 times and `26_don_arcade_mash` 948 times, PC-attributed to inside the thunk body (0x0FD060). Rendering stayed correct throughout (the fallback runs vsav's own type-6 code, reproduced instruction-for-instruction), so nothing rendered wrong and no playtest ever saw it — exactly the outcome the register's "prefer designs where being wrong is safe and loud" rule was written for. WHY IT WAS MISSED: the deadness measurement was sound but its COVERAGE was four replays (`02/07/09/30`), and the gate has always run on that default set; the two replays that arm it are long mash/arcade rigs nobody pointed it at. COST TODAY: `$FF010C/$FF010D` is a live work-RAM counter vanilla does not keep, so both replays diverge permanently from the vanilla masked basis — they are `.pending` on huitzil-m13 pending the maintainer's ruling. ~~OPEN: does the fallback need to stop counting (make the tripwire diagnostic-only / move it out of work RAM), or is the counter acceptable? See "Decisions pending — 14z-89"~~ **ANSWERED 14z-91: the counter was REMOVED** — the `beam_list_type6` `RAM:$FF010C` counter deleted, which cleared all six `.pending` legacy replays (`HANDOFF.md` Build registry, the 14z-91 row (C)) |

**[VSP-22]** Rules for adding a row: the claim must be measured with a POSITIVE CONTROL
on the same instrument and leg (a blind instrument and a real zero look
identical — paid for three times in 14z-71); it must name its guard; and
it must say what happens if the claim is wrong. Prefer designs where being
wrong is *safe and loud* over designs that are merely well-measured.

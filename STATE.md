# STATE — living progress log

**SPLIT 2026-08-20 (14z-99 post-freeze close, maintainer-approved), AMENDED
2026-09-14 (CLAUDE.md [VSP-17] and [VSP-182]): this file holds the RECENT
session groups and the STANDING SECTIONS; the full detail of every older
session lives verbatim in `STATE_HISTORY.md`, under THE LEDGER at its head.**
How to work with it:
- **Lookup**: "STATE 14z-XX" references resolve here first, then in
  STATE_HISTORY.md (its LEDGER first) — section names are preserved verbatim
  in the archive. A reference to `STATE "Decisions pending"` for an entry no
  longer here resolves in `DECISIONS_HISTORY.md` (entries move there verbatim
  when ruled — since 2026-09-14; before that, once they stopped shaping work).
- **Claim-greps MUST include STATE_HISTORY.md** (the CLAUDE.md §5
  retraction-discipline command names it).
- **AN OPEN LIST HOLDS ONLY WHAT IS OPEN (maintainer-ruled 2026-09-14).**
  "Decisions pending" holds undecided items only; nothing closed stays in it,
  however it is marked (struck through, DONE, FIXED, CLOSED). Where each thing
  goes instead:
  - a RULED decision -> its whole entry moves VERBATIM to
    `DECISIONS_HISTORY.md` in the commit that records the ruling; while the
    ruling still constrains work, ONE line stating the rule sits under
    "Standing rulings" here, and the line is deleted when it stops;
  - a BUG, COSMETIC ITEM or EVOLUTION is a TICKET — listed in
    `docs/project/tickets.tsv`, its story on its GitHub issue, never listed
    here; a STATE entry for one that closes moves VERBATIM to
    `STATE_HISTORY.md` under the closing session's key (CLAUDE.md [VSP-182]:
    the four questions, the closing comment);
  - "STANDING PRINCIPLE" and "THE DEADNESS REGISTER" carry the [VSP-21..23]
    anchors and stay; the register keeps its withdrawn rows by [VSP-23].
  `tests/test_state_open_lists.sh` enforces it, with the size budget and the
  group count below.
- **ROLLOVER RULE (part of the session-close ritual)**: after writing the
  close entry, move session groups beyond the newest THREE to the TOP of
  STATE_HISTORY.md's body in newest-first order (below THE LEDGER, above the first
  archived `## Session` heading OLDER than the group)
  and add their one-line entries at the top of THE LEDGER there, composed from
  the group's own banner headers. If this file still exceeds ~150 KB, roll the
  oldest kept group early. The standing sections never roll.
  **THEN RE-POINT WHAT CITED THE ROLLED GROUP (added 14z-173, after it bit):**
  a rollover silently breaks every `STATE.md § <section>` citation into the group
  it moved, and `tests/test_tickets.sh` is what notices — at 14z-173 it went red on
  #167's `decided` link anchoring "THE CLOSE TIER", which had just left STATE.md for
  the archive. After moving a group, re-point those citations to
  `STATE_HISTORY.md § <section>` (the convention 20 rows already use) and regenerate
  the page with `python3 tools/tickets.py page`. The sweep is one command:
  `python3 -c "..."` over `docs/project/tickets.tsv` comparing every `STATE.md §`
  anchor against STATE.md's current text — do it for ALL rows, not just the one the
  gate stops at.
- **THE CLOSE CHECKLIST (ruled 2026-09-18, 14z-167b; `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-167b)")**, after the close entry
  and before the tier: (1) a FINDINGS TABLE — every finding of the group, its live home and the test that
  reproduces it; (2) `python3 tools/close_findings.py <session>` — every GAP homed, every REVIEW answered
  by a home or an open item (never by reclassifying the finding), every PROMISE fulfilled or made an open
  item; its clean result is never cited as completeness; (3) a pointer on every earlier row the sitting
  resolves; (4) the promises (the tool's PROMISES section); (5) scratch scripts whose figures a document
  or STATE row quotes are promoted into `tools/` or `tests/` — ON TRIAL: record how many and how many
  were promoted; (6) the DOCUMENTATION PACKET through the rule-checker (`recommendation`, the table as
  the claim), its verdict resolved like any run.
- **THE LAST STEP OF THE CLOSE IS THE PUSH (maintainer-ruled 2026-09-10):**
  static tier strict green WITH every control executed (the default `all`), the doc checks green, nothing pending -> `git push
  origin main`; anything red or skipped leaves the commits local and the
  close entry says so.

## Session 14z-174 — **THE APPLIER PAGE SHIPPED: a player with no Python and no terminal now double-clicks one file and gets the romset.** The maintainer set the priority (*"this is the most value item for M19 release unless I am missing something"*) and ruled two tickets closed. Slices A2-A6 landed: `apply_release.html`, ONE self-contained generated file per platform dir, which **equals `apply_release.py` MEMBER FOR MEMBER on both variants** and carries no network primitive of any kind — both held by gates, the second by construction in the generator. **THE PLAN'S CENTRAL ASSUMPTION WAS WRONG AND MEASUREMENT CAUGHT IT BEFORE ANY CODE WAS WRITTEN:** a `file://` page cannot read the manifest or the patches sitting beside it — `fetch`, XHR and cross-file `import` are ALL refused by Chrome and Firefox alike — so everything is inlined. **Two gates, five controls, and FOUR DEFECTS THE GATES FOUND IN WORK THAT LOOKED FINISHED**, two of them in the SHIPPED `apply_release.py`. #146 and #144 CLOSED on the maintainer's rulings; #170 opened for the option #144 kept open.

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, `docs/NEXT_SESSION.md`), `main` == `origin/main` at `fdb41802`, `emu/fbneo` dirty by exactly its two tracked patches (841/4), nothing running. The maintainer ruled on all four START HERE items in one message |
| **(1) THE MEASUREMENT THAT DECIDED THE DESIGN, TAKEN BEFORE THE DESIGN** | `applier_app_scope.md` §2 assumed the page would sit beside `manifest.json` and `patches/` and read them. A capability probe run from `file://` in BOTH engines says it cannot: `import("./mod.mjs")`, `fetch("./x.json")` and `XMLHttpRequest` are **all blocked** (Chrome 141 and Firefox 156 alike), while inline `<script type="module">`, `isSecureContext` (**true**), `crypto.subtle` SHA-1 and `DecompressionStream("deflate-raw")` all work. So the page is ONE file with this release's manifest, its 20 patches (base64) and its three modules inlined — 3.4 MB. **That is not a compromise of the 2026-09-20 ruling ("a local file in the asset", "one page per release"), it is the only way to obey it**, and it makes the version-matching the ruling was about physically impossible to get wrong. Homed in `docs/platform/gotchas.md` with the table |
| **(2) THE PAGE, AND WHY IT IS THREE MODULES AND A SHELL** | `tools/applier/{vcdiff,zip,applier}.mjs` + `page_shell.html`, inlined by `tools/gen_applier_page.py`. The split is what makes the fidelity claim possible: `apply_node.mjs` drives the SAME modules behind `apply_release.py`'s own CLI, so the gate compares two appliers rather than inspecting one. `applier.mjs` follows the Python tool's ORDER step for step, because failing in a different order would mean failing with a different message. The UI is four numbered steps (browser check, choose dumps, the optional member, build), the refusals are panels naming the file, and the download is built through the DOM — **a literal `href=` would be an external-resource reference and the generator refuses a page that has one** |
| **(3) THE GENERATOR REFUSES A PAGE THAT COULD PHONE HOME** | the hard constraint of §2 is rule-7-shaped: the page is handed commercial dumps, so "it does not upload them" has to be checkable by reading the file. `gen_applier_page.py` scans its own output for `fetch`/XHR/`WebSocket`/`EventSource`/`sendBeacon`/`importScripts`/dynamic `import` and for any `src=`/`href=` that is not a `#fragment`, and refuses. **It refused MY first two pages** — a comment that used the literal token, and the download link — and both were fixed rather than the check weakened, because an exemption is exactly where a real one would hide. `test_applier_page.sh` §1 re-asserts the property on the OUTPUT, independently of the generator that wrote it, and §2 asserts the page carries the modules the gate measures, so the node measurements are evidence about the PAGE |
| **(4) FIDELITY (A5) — AND WHAT IT DELIBERATELY DOES NOT CLAIM** | both appliers over `$ROMDIR`, both variants: **32 and 31 members byte-identical, member order identical, every zip header field identical, `testzip` clean, and the set keys `923c1e7a` / `19ca6f3f` the manifest declares**. `build_fingerprint.py` reads the page-written set as `681ac3ad`, the same as the Python one. **The CONTAINER bytes differ and the contract does not claim them:** `CompressionStream("deflate-raw")` is not byte-comparable to `zlib.compressobj` even at the same level, because it flushes on stream chunk boundaries — node and Python agree on one 200 KB buffer and **disagree on all 32 real members**, and Firefox differs from both again (2042 bytes against zlib's 1424 on the same fixture). A one-buffer test had said "identical" and would have supported a false claim; the real members are what refuted it. Gotcha written |
| **(5) FOUR DEFECTS THE GATES FOUND IN WORK THAT LOOKED FINISHED — TWO IN THE SHIPPED PYTHON APPLIER** | (a) **a truncated or half-copied dump raised a raw `BadZipFile` traceback at the player** in `apply_release.py`, naming CPython's `zipfile.py` and not the file that is wrong; the page's applier did the same. A half-finished download is an ordinary thing to have. BOTH now say `vsav2.zip: not a readable zip file (damaged, incomplete, or not a zip)`, through one opener and one member-reader each. (b) A damaged MEMBER inside a valid zip had the same shape; both now name the member. (c) **The page let you click Build while it was still reading your files**, so a second pick raced the button and the page built from the PREVIOUS file and reported success — found by the browser gate's refusal leg, which dropped a damaged dump and got a clean build. Build is now blocked while reading. (d) My own `no-member-check` control was DEAD on its first run: removing the rebuilt-member check does not let a corrupted patch through, because VCDIFF's own adler32 catches it. The control was re-aimed at a TAMPERED MANIFEST — the one perturbation only that check can see — and a matching case was added to the gate it protects |
| **(6) THE REFUSALS, READ OUT OF THE TOOL OF RECORD'S SYNTAX TREE** | A3 said every message `apply_release.py` produces must have a page equivalent. That is now mechanical: the gate walks the Python file's AST for every `sys.exit(...)` and `bad.append(...)`, reduces each f-string to its literal fragments, and requires every fragment of 12 characters or more to appear in `applier.mjs` — **15 fragments, all present**, and a new refusal in the Python tool fails the gate until the page has one. A first version used a regex and reported a false positive (it handed back the source's `\n` escape, which no message contains); the AST reads what survives interpolation. Four refusals are also exercised END TO END on both tools — modified dump, truncated dump, corrupted patch, tampered manifest — each requiring both to refuse, both to name the same thing, and NEITHER to write a file |
| **(7) THE BROWSER GATE: THE SHIPPED BYTES, DRIVEN AS A PLAYER DRIVES THEM** | `test_applier_page_browser.sh` runs `cat apply_release.html tests/lib/applier_driver.html` and asserts the shipped file is a byte-PREFIX of what the browser loads, so what runs is the shipped page plus a driver and nothing edited. The driver touches no private binding: it reads the list of dumps THE PAGE asks for, drops them on the drop zone, clicks the radio, clicks Build, reads the panel. Both variants reach the declared set keys and a damaged dump reaches the refusal panel naming the file. **Two mechanics are not optional and cost measurements to find:** waits must be `MutationObserver`s, never `setTimeout`, because under `--virtual-time-budget` a timer advances virtual time instantly and Chrome dumps the DOM mid-run; and the download must NOT be fetched back from its `blob:` URL, which leaves a fetch pending and truncates the dump |
| **(8) THE PIPELINE, NOT A HAND PLACEMENT** | the page is GENERATED by `package_release.py` and re-generated per platform by `package_release_platforms.py` (the only per-platform difference is the "when you have the file" line). The release was re-packaged from `build/m3b_merged27/rompath`: **no manifest moved and no patch moved** — only the READMEs, the applier and the new page. It joined the RULED release inventory (`test_release_roundtrip.sh` §4, both ways: allowed AND required) and every published asset's self-sufficiency definition (`test_release_asset_shape.sh` + a new `page-missing` control, `upload_release_assets.sh`'s refusal). The player READMEs now lead step 1 with the browser and keep the command line beside it; Python is no longer the first thing a non-developer is asked to install |
| **(10) THE RULE-CHECKER FOUND THREE THINGS, AND ONE OF THEM REPLACED A MECHANISM** | run `2026-09-21-90` (`recommendation`, plant `inferred-decision-14z162` CAUGHT) returned **VIOLATED on Q1, Q4 and Q5 — all three TRUE, all three resolved BY WORK**. **Q1: NO GATE HAD EVER OPENED THE SHIPPED FILE.** Both gates measured a FRESH generation and "the shipped page is that generation" was assumed by everything, so a hand-edited or stale page would have passed. Now `test_applier_page.sh` requires every platform dir's `apply_release.html` to equal a fresh generation (3/3), and the browser gate loads the shipped file itself. Its second limb is named rather than closed: the node leg runs through `apply_node.mjs`, so the page's OWN wiring is covered only from the browser side, by the set key. **Q4 IS THE SHARPEST: "no network primitive of any kind" rested on a denylist of seven names with ONE control, and the page itself proved the blind spot** — it carries `link.href = URL.createObjectURL(...)`, which the scan cannot see. A denylist cannot prove absence, so **the MECHANISM was replaced, not the list lengthened**: the page now carries `default-src 'none'; connect-src 'none'; form-action 'none'; base-uri 'none'` and the generator refuses to emit it without. **The CSP proved itself immediately and awkwardly** — the browser gate's driver lived inside the document and died on "Failed to fetch" the moment the policy landed, so the driver was rewritten as a separate page that IFRAMES the shipped file, keeping the real policy intact rather than diluting it to be testable. The denylist stays as a second, independent check, widened to the five primitives the checker named; a second control `page-without-csp` fires on a different mechanism; and the refusal cases exercised went **four -> six**. **ALSO MEASURED AND REJECTED:** Chrome's `--log-net-log` as a direct "zero requests" measurement — unusable, because the netlog records the BROWSER's traffic (15 non-`file://` URLs on a default profile, still 9 with every background-networking flag off), none of them the page's; an assertion built on filtering those would be an instrument that lies. **Q5 was a PACKET error, not a false claim:** the packet attributed two closures to the maintainer and carried no artifact holding their words. They are recorded verbatim in `DECISIONS_HISTORY.md` and in both closing comments; the lesson — a packet that attributes a decision must carry the decision RECORD as an artifact, and a fidelity claim and a recommendation do not belong in one packet — is a gotcha |
| **(9) TWO TICKETS CLOSED ON THE MAINTAINER'S RULINGS, ONE OPENED** | **#146** — *"README is good enough for now. It may be improved, but it will be on the basis of more user feedback"* — closed `done`; the transferable lesson (first use against first need; the fix re-introduces the bug) went to `docs/project/gotchas.md` rather than staying on the issue. **#144** — *"Indeed, closeable with an explicit comment as mitigated not solved"* — closed `done` with a closing comment that states plainly what is still true: macOS blocks unsigned binaries by design, this project does not sign them, and the player either lets `PLAY.command` clear the flag or approves each file. **#170 opened** for the option that close kept open — notarization, PARKED on community demand — because closed means nothing is left to do |
| **(11) THE M19 RELEASE RUN — AND THE TIER EARNED ITS 6 h 24 min** | the maintainer: *"we absolutely should aim to bring M19 forward"*, then *"you can go ahead with the M19 release"*. Release-cadence STATIC tier GREEN first (PASS 169, controls 201/201). Then the emulator tier at release scope, `--scope all --lane all --strict --controls --jobs 4`: **PASS 275, SKIP 1, FAIL 8, TIMEOUT 1 — NOT GREEN**, 6 h 24 min. **The MiSTer lane came in entirely green, 13 gates with their controls**, including `test_mister_gfxc_fetch` at 98 min, and the runner confirmed `no tracked file changed during the run`. **NOT ONE OF THE EIGHT WAS A DEFECT IN THE ROMSET**, and the release halted on them under rule 6 |
| **(12) THE EIGHT REDS, IN THREE SHAPES, AND WHAT EACH SAYS ABOUT THE SUITE** | **(a) FIVE GATES HAD ASSERTED NOTHING SINCE 14z-165.** `tools/vanilla_join_rig.py` called `name_moves.PROLOGUE`, deleted at `e01ae8de` when the rigs' P2 became per-tenant; three died at 0 s on `AttributeError` and two then measured **0 rows** and compared an empty table whose sha256 is the hash of the empty string. Four days, nine sessions. Fixed by splitting `prologue()` into a path-taking builder plus `REPLAY17_PROLOGUE`, **asserted byte-identical to the deleted constant BEFORE use**, so the repair restores the exact rig those expectations were measured on — and any surviving red would be a real finding. All five pass. **(b) THREE EXPECTATIONS WERE STALE BEHIND RIGS THAT MOVED** at 14z-171 and 14z-172. **(c) THE TIMEOUT WAS THE CAP, NOT THE CONTROL:** `test_release_binaries` takes 234 s under `--jobs 4` and its `flipped-library-byte` control 210 s ALONE — the 300 s row was a quiet-machine number that the release run's own contention broke, so that control had never run. 300 -> 900, set from the loaded case with the measurement in the row. **The common factor is the finding: the emulator tier only runs at a freeze or a release, so breakage accumulates and surfaces at the worst possible moment** |
| **(13) THE ATTRIBUTION, AND THE RULE-CHECKER THAT WOULD NOT LET IT PASS** | the three stale expectations were attributed with ONE variable: restore `tests/replays/naming` — and `tools/name_moves.py` too for killshread, which REGENERATES its rig — to the M19 freeze, change nothing else, and all three PASS against their existing rows; the shipped build is byte-unmoved since that freeze (0 manifest paths, 0 generator paths, fingerprint `681ac3ad`). **Run `2026-09-22-91` returned VIOLATED on Q1/Q2/Q3 and was right three times, twice about the work and once about ME.** Q1: the narrative named DF=195 as a cause for killshread and **it cannot reach that rig** — `donovan_12`'s first event is `"far"` and far parts do not move; the measurement had already said so and I had not listened. **Q2: the finding rested on my own claim that DAMAGE had moved, which was a MISREADING** — I compared a removed row against a DIFFERENT added row; paired properly, `hp`, `white` and `cls` are unchanged and the six rows differ in `stock` alone. **Q3, verified in the code: `name_moves.py:802` pokes `ff8509` and `test_killshread_es.sh:50` samples it, so the `stock` column READS THE RIG'S OWN POKE BACK**, and [VSP-170]'s ES stock-drop discriminator is neutralised in that rig. **Maintainer-ruled** (*"Option 1 and ticket to qualify all gates in the future because either the gates are moving silently or they never were validated"*): re-freeze as-is, record what the column is — done in the gate header and `PROVENANCE.md` — and open **#171**. Every diff was reviewed PAIRED ROW BY ROW first: in all three tables **every identity column is unchanged** (leg, direction, pc, value, kind, victim identity), only counts moved |
| **(14) M19 IS RELEASED, AND THE MISTER LANE WAS CARRIED ON EVIDENCE** | the re-run dropped `--lane all` because **the MiSTer lane is 13.8 h of the tier's 19.8 h serial, 70%** — and every one of its subjects was byte-unchanged. That is not a judgement: `tools/audit_lane_carry.py` (NEW) diffs each lane's subjects against the tree, exit 0 MAY CARRY / exit 1 MUST RE-RUN, demonstrated both ways (mister 0, mame 25 paths moved). **WHAT IT DOES AND DOES NOT DO, corrected at the close after the rule-checker caught this row overstating it (run `2026-09-22-92` Q1/Q4): the subject lists are HARDCODED in the tool**, and it reads `ci_emulator.tsv` only for gate NAMES — it never parses the `cadence` column, and an earlier version of this sentence said it did. The list also OMITS `tests/replays` (a load-bearing operand of a mister gate, `ci_emulator.tsv` "THE ARGS ARE LOAD-BEARING") and `ci_emulator.tsv` itself. **The M19 carry-forward conclusion still holds, verified BY HAND over exactly those omitted paths**: `tests/replays` 0 changed since `cf4dfd86`, and the one `ci_emulator.tsv` row that moved is `test_release_binaries`, a MAME-lane row. So the answer was right, and the METHOD did not earn it. Widening the list and building the control that reconciles it against the registry is #171. Re-run: **PASS 281, FAIL 0, TIMEOUT 0**, controls `fired 92 / declared 92`, one SKIP — `audit_mask_window_ff42a2`, an attribution INSTRUMENT with no operands. A previous release had approved it; **that was not treated as standing** and the maintainer approved it again (*"yes"*). Static tier re-run GREEN (PASS 169, controls 201/201). **PUBLISHED: seven assets on `freeze/merged-m19`, each downloaded back from GitHub and compared file-by-file, M18's seven PRUNED** — which retires the README that still told macOS players to right-click > Open, the live problem this sitting opened with. **AND THE MAINTAINER CLOSED THE LAST GAP NO GATE COULD:** *"I tested on MiSTer, M19 is clean and the rom made with the local webapp is good"* — the page had never been used by a human and the set it produces had never been played |

| **CLOSE (14z-174, 2026-09-22) — THE CHECKLIST** | **(1) THE FINDINGS TABLE** — 21 findings, each with its live home and the test that reproduces it: the `file://` blockade of fetch/XHR/cross-file import (`docs/platform/gotchas.md`, the table / `test_applier_page` §1's self-containment assertion); `CompressionStream` not being byte-comparable to `zlib.compressobj` (same, new / §3, which compares members and says the container is out of scope); the page's member-for-member equality on both variants (`applier_app_scope.md` §4 A5 / `test_applier_page` §3); the refusal vocabulary read from the Python tool's AST (same A3 / §4, 15 fragments, six exercised); the damaged-dump refusal that neither applier had (`tools/apply_release.py`'s `open_dump` + `applier.mjs`'s `readRef` / §4's truncated and missing cases); the Build-while-reading race (`page_shell.html`'s `reading` flag comment / `test_applier_page_browser`'s refusal leg, which found it); the CSP as the enforced guarantee (`release_format.md` + `gen_applier_page.py` / the `page-without-csp` control); the expert-document tell (`docs/project/gotchas.md`, new / #146's close); the packet-attribution lesson (same, new / run `2026-09-21-90` Q5); **PROLOGUE's deletion and the five dead gates** (`name_moves.py`'s `REPLAY17_PROLOGUE` comment / the five gates themselves); **the stock column reading back a poke** (`test_killshread_es.sh` header + `PROVENANCE.md` / #171); the three attributions (`tools/attribute_expectation.sh` / the tool re-runs each); the lane carry-forward (`tools/audit_lane_carry.py` / itself, both verdicts); the loaded-timeout rule (`ci_emulator.tsv`'s row note / the control's 210 s standalone run). **(2)** `close_findings.py 14z-174 --base fdb41802`: **0 addresses, 0 GAPS, 0 REVIEW**; its one PROMISE is the same `until a` FALSE POSITIVE as 14z-173, matching "fails the gate until the page has one" — gate behaviour, not a commitment. Its clean result is not cited as completeness. **(3) POINTERS:** #144's and #146's entries carry their closures; the applier scope's §2a records the correction to its own central assumption. **(4) PROMISES:** one made and kept — the Q3 resolution promised the packet-attribution lesson as a gotcha rather than a ledger row, and it is in `docs/project/gotchas.md`. **(5) SCRATCH SCRIPTS, ON TRIAL: 11 written, 3 PROMOTED** — `mister_carry.sh` -> `tools/audit_lane_carry.py`, `attribute_rigs.sh` + `attr_killshread2.sh` -> `tools/attribute_expectation.sh`, and the JS fidelity runner had already become `tests/test_applier_page.sh`. **The promotion paid for itself immediately**: re-run as a tool, the attribution reported NOT ATTRIBUTED — because the expectation had since been re-frozen, so it was comparing an old rig against new rows. That is a different question, and the tool now REFUSES it (exit 2) naming the moved expectation, rather than answering it. The remaining 8 are inline perturbations and patch scripts whose figures nothing quotes. **(6) THE DOCUMENTATION PACKET** through the rule-checker: run `2026-09-22-92`. **THE TIER:** `ROMDIR=... tests/run_all_static.sh --strict` (session cadence, every control executed). **THE FIRST RUN CAME BACK NOT GREEN — one red, and fixing it found a worse bug than the red.** `test_applier_page_browser` FAILED at 7 s and PASSED run alone: its driver stalled during ~48 MB of `file://` reads, because **`--virtual-time-budget` ADVANCES while the renderer sits idle on I/O**, so a cold page cache expires the budget and Chrome dumps the DOM early. **But the flake was not the defect: the gate then printed EIGHT CONTENT-SHAPED FAILURES** — "standalone panel: got '', expected 'ok'" and the rest — for a run that had measured no content at all, so a reader would have concluded the shipped applier page was broken. A gate lying about what it saw, in a gate written the day before, and #171's own subject. Fixed three ways: an incomplete run is now ITS OWN VERDICT (it names where the driver stopped and states that NO CONTENT VERDICT IS CLAIMED, then exits without evaluating the checks), the reads are warmed before Chrome starts, and there is ONE retry FOR INCOMPLETENESS ONLY — never for a content failure, because re-rolling a real verdict until it passes is how a suite lies. **RE-RUN IN FULL**, a partial re-run not being the close condition: **PASS 163, SKIP 0, FAIL 0, MISSING 0, `fired 188 / declared 188` — GREEN**, with the repaired gate PASS at 17 s in the same tier conditions that broke it; 6 gates deferred by cadence and NAMED. **THIS ROW WAS WRITTEN BEFORE THE TIER** except the sentences stating its result. **THE COST OF MY OWN DISCIPLINE FAILURE, recorded because it is the sitting's largest single loss:** three finished background jobs sat unpolled for 2.5 h, 1.5 h and 1.5 h. The strategy was correct and stated (`poll-long-jobs-directly`); it simply was not executed, which the maintainer identified as the worrying part — *"Polling is likely the good option. The problem is actually doing it"* — and which is now **#172**, the agent-level architecture. |


## Session 14z-173 — **THE DELIVERABLES SITTING: the release package rebuilt around the person who downloads it.** The maintainer set the order (standalone set -> launcher -> the per-OS binary tickets -> the README pass -> the applier app) and ruled macOS first, a MiSTer+macOS release being proof enough. Landed: a STANDALONE `vsavjw.zip` so a player places ONE file (7 pristine members completed in from their own dumps, DEFLATED 73.7 -> 28.0 MB, the QSound BIOS member OPTIONAL); `PLAY.command`, a launcher that encodes the three things a player cannot know; the player READMEs rewritten against the maintainer's own brief for #146. **#144 REPRODUCED FROM THE MAINTAINER'S CAPTURES and the advice this project shipped for it WITHDRAWN as false** — right-click > Open does not get past macOS's block — with the mitigation measured end to end: "Open Anyway" works but is PER FILE, 2 for MAME against 24 for FBNeo, so **FBNeo patch 0003 was written, proven inert over 12,120 frames and RATIFIED, taking that bundle 24 files -> 4**. The applier app scoped with its three questions ruled, its one hard slice landed and proven. **Eleven gates new or reworked, census 134 -> 136, and four of my own measurements were wrong in one shape: an ad-hoc rig's defect attributed to the subject.**

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, `docs/NEXT_SESSION.md`), `main` == `origin/main` at `18187544`, `emu/fbneo` dirty by exactly its two tracked patches (841/4), nothing running. The maintainer set out their view of the deliverables and asked for mine |
| **(1) THE STANDALONE SET — ONE ZIP IN `roms/`** | A build packs only what it authors and the emulators resolve the rest from the parent plus, on MAME, the QSound BIOS set — so a player placed THREE archives and the READMEs named two: **the MAME README never mentioned `qsound_hle.zip` at all, and MAME refuses the set without it** (`dl-1425.bin - NOT FOUND`, measured). Two routes were costed and put to the maintainer — build-side (every whole-set key moves: registry rows, expectation sets, the re-point sweep, a MiSTer catalogue regen and fork commit, the ~5 h battery, and M19 superseded before shipping) against release-side — and they ruled **"C"**, the release. The applier now completes `vsavjw.zip` with 7 pristine members DERIVED from `gen_vsavjw_xml.PARENT_MEMBERS` rather than listed twice; `qsound_hle.zip` is a pristine SOURCE only, deliberately out of the source blob, so **no patch file moved**. Measured: the applied set ALONE, `05_timeout_idle`, 12,120 frames — work RAM and framebuffer BIT-IDENTICAL on both emulators, `-verifyroms` zero NOT FOUND, the de-substitution invariant intact, and all 31 CRC-matched WIDE MRA parts resolving from it |
| **(2) DEFLATE, AND THE OPTIONAL BIOS MEMBER** | *"If it is identical content, I see no reason not to go for the smaller size (I assume it's loaded decompressed in ram ... so there's no decompression overhead, right?)"* — **73.7 -> 28.0 MB**, and THE ASSUMPTION WAS MEASURED rather than agreed with: right about the part that matters (no per-frame cost) and not quite right as put — MAME boot + 1 s, stored 1.67/1.69 s against deflate 1.90/1.98 s, **~+0.26 s once** for 45.7 MB. MiSTer UNMEASURED, no hardware here. A first attempt changed only the `ZipFile(...)` argument and left the set at 71 MB: `writestr` with an explicit `ZipInfo` uses THAT object's `compress_type`, which defaults to STORED. Caught by measuring the output, not by trusting the flag. **The QSound BIOS member is now OPTIONAL** (*"Ideally I'd love the option to choose to add it or not"*): `--no-qsound-bios`, a second declared key, and the three properties the pages rely on ASSERTED not stated — MAME refuses the smaller set, FBNeo runs it identically, the WIDE MRA resolves all 31 parts with the BIOS from the card's own `qsound.zip` |
| **(3) `PLAY.command`** | One launcher per emulator platform, encoding what a player has no way to know: the set is `vsavjw` and only the patched build knows it; **FBNeo's SDL frontend has NO rom-path option** and reads `roms/` relative to the current directory, so browsing to the file in its GUI cannot work; and a macOS download is quarantined. It OFFERS to clear the flag rather than doing it silently — the only branch that touches the player's security state. Verified for real, not by dry run: MAME booted and exited cleanly, FBNeo reached 31 members `(OK)` with the profile active. New gate `test_release_launcher` drives the success path and every refusal, asserting each exits NON-ZERO **and names its cause**, because the messages are the deliverable |
| **(4) #144 — REPRODUCED, AND THE SHIPPED ADVICE WITHDRAWN** | The maintainer's captures: *"fbneo" Not Opened — Apple could not verify …*, only Done and Move to Bin, both binaries; asked directly about the documented workaround, *"Does not open"*. **So right-click > Open is FALSE on current macOS and is withdrawn from every live carrier** (the record's text, the launcher's refusal, `release_format.md`, a comment in `bundle_dylibs.py`); re-grepped clean. Then the mitigation, measured: System Settings > "Open Anyway" DOES work — *"yes BUT needs the open anyway for the executable AND for libSDL3.0.dylib"* — but is **PER BLOCKED FILE**: **MAME 2, FBNeo 24**. An instrument nearly misled me: `spctl -a` printed `rejected` with AND without the quarantine flag, from which I almost concluded the `xattr` workaround was useless — it reports the SIGNING-POLICY verdict while Gatekeeper BLOCKS on the flag. Two questions, two instruments; written up as a gotcha |
| **(5) #146 — THE READMEs, AGAINST THE MAINTAINER'S OWN BRIEF** | *"it must have felt clear to the expert who wrote them … things arbitrarily considered obvious not explained or quoted … the user is usually not a developer, and basically never on the project."* Read cold, the first page assumed `vsavj`, "15+1", "CPS-2 WIDE profile", "driver", "dumps", "manifest", "applier", "VCDIFF", "members" and *"a gate asserts it"* before saying what the reader would do. Rewritten: plain description, then three numbered steps, then detail; every term glossed at first use; **the "Play on …" section MOVED from the end of the file to directly after the build step** (`append_play` fills a marker instead of appending), because that is the order the reader acts in; the troubleshooting list led by the macOS block. I wrote a NEW instance of the same bug while fixing it — a line telling Windows users to double-click a launcher that does not exist for Windows — and corrected it |
| **(6) THE APPLIER APP — SCOPED, A1 LANDED** | `docs/project/applier_app_scope.md`, the plan before the work. **Recommendation: one static client-side HTML page**, because #144 and #145 exist precisely because an unsigned binary is hard to get an OS to run, and shipping a second one that asks for the player's ROMs doubles that. Feasibility MEASURED, dependency count zero: `deflate-raw` round-tripped byte-identical, `crypto.subtle` SHA-1 agreeing with Python's, and **the JS VCDIFF port decoding all 20 shipped patches to the manifest's exact sha1 in 0.2 s**. Slice A1 landed as `tools/applier/vcdiff.mjs` + `tests/test_applier_vcdiff.sh`, which asserts the BLOB's own sha1 first so a wrong blob cannot be mistaken for a wrong decoder. The three open questions RULED: a local file in the asset, one page per release, a browser page acceptable. The doc says which of its figures are gated and which are not |
| **(7) FBNEO PATCH 0003 — WRITTEN, PROVEN, RATIFIED** | Notarization was ruled OUT (*"I wouldn't mind spending the money if the community supported, which is not (yet) the case"*), so the 24-file bundle was attacked instead. FBNeo links SDL2_image for ONE call — an optional preview PNG in its own game-browser menu, a path that already did nothing here — and that call drags in 21 transitive image codecs. Patch 0003 removes it: **24 files -> 4**; "Open Anyway" should then cost 4 instead of 24, which is INFERRED from MAME's measured 2-file/2-approval case and exercised at neither count. RELEASE-ONLY — `build_release_emulators.sh` applies it, `setup_fbneo.sh` does not, so every gate still measures the same instrument. Proven at frame level: a harness build carrying 0001+0002+0003 against the current 0001+0002 binary is **BIT-IDENTICAL in work RAM AND framebuffer over 12,120 frames**, 7,891 distinct rendered frames so the comparison is not vacuous. RATIFIED — *"in that case then yes, i approve of patch 0003"* — but only after the maintainer pointed out the first attempt to ask: *"You do realise that mentions like 0003 are internal numbering and I have no idea what they represent"*, the same failure #146 is about, one message after claiming to have fixed it. It also drops the link on Windows, whose bundle is 31 files, for free at the next build |
| **(8) FOUR OF MY OWN MEASUREMENTS WERE WRONG, IN ONE SHAPE** | Each time, an ad-hoc rig's defect was attributed to the subject. (a) A reference leg run with a RELATIVE `ROMDIR` resolved against the sandbox and loaded SIX members short; the framebuffer divergence looked like CRC hash-shadowing until a separating control killed it — the romset was innocent, the instrument was misconfigured. **The durable fact: FBNeo ran 12,120 frames with six missing 4 MB members and work RAM BIT-IDENTICAL, only the framebuffer showing it** (the 14z-60z class; gotcha written). (b) `bundle_dylibs.py` called BY HAND without the builder's `--extra` produced a hanging bundle, reported as the bundler silently under-collecting — wrong: `extra_sdl3_args` already knows sdl2-compat dlopens SDL3, finds it by string scan and refuses loudly when absent. (c) A rewritten shell string produced `\"\\"`, closing the string and leaving `<name>` as a redirect; **`sh -n` ACCEPTED it**, being valid shell but not the shell meant. (d) Two replay logs compared while the second was still being written read DIFFER on both streams and nearly condemned patch 0003; the poll waited for the file to EXIST, not for the run to finish. A fifth was caught by arithmetic rather than luck: a gate edit had silently never applied because its script aborted on an earlier assertion, and the tell was **six files staged, five reported changed** |

| **CLOSE (14z-173, 2026-09-21) — THE CHECKLIST** | **(1) THE FINDINGS TABLE** — 17 findings, each with its live home and the test that reproduces it: the standalone completion and its derivation (`release_format.md` "THE STANDALONE SET" / `test_release_roundtrip` §1); the applied-set behavioural equivalence (same / `test_release_binaries` §3a-3c); the optional BIOS member's three properties (`release_format.md` + `MISTER.md` / §3d); deflate's one-off load cost (`DECISIONS_HISTORY.md` 2026-09-20 / ungated, stated); the `ZipInfo.compress_type` trap (`tools/apply_release.py` comment / the 28.0 MB output itself); FBNeo's missing rom-path and the launcher's three encoded facts (`release_format.md` PLAY.command row / `test_release_launcher`); the four launcher refusals (same / same); the quarantine consent branch (same / `test_release_launcher`'s macOS leg); `unzip` propagating quarantine and `spctl` answering the wrong question (`docs/platform/gotchas.md`, new / no test — a platform fact); **right-click > Open being FALSE** (every generator + `release_format.md` / re-grep, clean); "Open Anyway" being PER FILE, 2 vs 24 (`release_format.md`, the READMEs / the 0003 lock); the missing-member blindness of work RAM (`docs/platform/gotchas.md`, new / `test_release_binaries` §3's video half + its gfx control); the READMEs' assumed vocabulary (#146 / the generated READMEs themselves); the applier page's feasibility (`applier_app_scope.md` §3, which states which rows are gated) ; the JS VCDIFF port (`tools/applier/` / `test_applier_vcdiff`, 20/20); patch 0003's inertness (`DECISIONS_HISTORY.md` ratification / the 12,120-frame A/B, one-off, plus the standing 0003 lock); and — found BY THE CLOSE — MAME's macOS record still shipping the withdrawn advice (rebuilt; `test_release_binaries`). **(2)** `close_findings.py 14z-173 --base 18187544`: **0 addresses named** (a packaging and tooling sitting, no ROM addresses), **0 GAPS, 0 REVIEW**; its one PROMISE line is a FALSE POSITIVE of the `until a` pattern matching narrative past tense ("until a separating control killed it"), not a commitment. Its clean result is not cited as completeness. **(3) POINTERS:** #144's entry carries the reproduction, the withdrawal and the per-file cost; the applier scope's §6 records the three rulings in place of the questions. **(4) PROMISES:** the one real outstanding commitment — that the stale `BINARY.txt` records would correct themselves at the next macOS build — WAS DISCHARGED IN THE CLOSE rather than deferred: FBNeo's came with the 0003 rebuild, and **MAME's was found still stale by an explicit check and rebuilt**. `merged-m18`'s two records are left as published history. **(5) SCRATCH SCRIPTS, ON TRIAL: 2 quoted, 2 promoted** — the JS decoder and its fidelity runner became `tools/applier/vcdiff.mjs` and `tests/test_applier_vcdiff.sh`. The rest of the sitting's measurements were inline commands, not scripts; those whose figures a document quotes are either GATED (the bundle count, by the new 0003 lock; the quarantine branch; the standalone equivalence) or explicitly marked UNGATED in the document that quotes them (`applier_app_scope.md` §3's `deflate-raw` and SHA-1 rows, and the 12,120-frame 0003 A/B, which would need a harness build of a release-only patch). **(6) THE DOCUMENTATION PACKET** through the rule-checker: run `2026-09-21-89` (`recommendation`, plant `forced-pick-14z159` CAUGHT) returned **VIOLATED on Q1 and Q4, both TRUE and both RESOLVED BY WORK, and they are one finding**: the close claimed "MAME 2, FBNeo 24, reduced to 4" as MEASURED approval counts when **only MAME's 2 was ever exercised by a human** — 24 and 4 are bundle FILE counts, and the step from files to approvals assumes ONE approval per blocked file, a premise that fits MAME's single data point and has been tested at neither 24 nor 4. Every carrier now separates the two (the patch header, `build_release_emulators.sh` twice, the 0003 lock — which now says outright that it asserts the FILE COUNT, being what it can see — `patch_index.md`, HANDOFF, STATE, `DECISIONS_HISTORY.md`, `NEXT_SESSION`), re-grepped with no unqualified assertion left. Q4's second limb was a REAL GAP: **nothing exercised the PATCHED SITE**, since the 12,120-frame A/B and the headless boot both run the emulation core while 0003 edits `LoadTitleImage` in FBNeo's own game-browser GUI. Measured for the first time here: the patched binary started with `-menu` under `SDL_VIDEODRIVER=dummy` and was still running at 15 s with no error, segfault or abort. **Stated rather than overclaimed:** that does not establish `LoadTitleImage` was REACHED in an idle dummy-video run, so the patched line itself is still unexercised and the honest residual is "the GUI starts and does not crash". Q3 and Q5 read OK. **AND THE RESOLUTION MOVED A SHIPPED RECORD:** editing the patch header changed its sha1, which `BINARY.txt` names and no gate verifies, so FBNeo was REBUILT rather than leave a record a reader could hash and find wrong. **A LIVE ISSUE THE CLOSE SURFACED AND DID NOT FIX:** `merged-m18` is the CURRENTLY PUBLISHED release, and its README and `BINARY.txt` tell macOS players to right-click > Open — advice now measured false. Nobody downloading today is served correctly. Not rewritten here, because a published release is history and its GitHub assets would still differ; raised for the maintainer as either a note on the release page or a reason to bring the M19 release forward. **THE TIER:** `ROMDIR=... tests/run_all_static.sh --strict` (session cadence, every control EXECUTED — the default `all`). **The first run came back NOT GREEN, and the cause was the close itself:** `test_tickets` failed on **#167**, whose `decided` link anchored "THE CLOSE TIER" in `STATE.md` — a section that had left for the archive an hour earlier when this very close rolled the 14z-170 group. **A rollover silently breaks every citation into the group it moves, and the documented procedure said nothing about it.** Fixed in three parts: #167 re-pointed to `STATE_HISTORY.md §` (the convention 20 rows already use) and the page regenerated; ALL rows swept rather than only the one the gate stopped at (every `STATE.md §` anchor checked against STATE's current text — exactly one was broken, so fixing what the gate named would have been luck, not method); and **the rollover procedure in this file's own header now carries the re-point step**, why it exists and which gate notices. Then RE-RUN IN FULL, a partial re-run not being the close condition: **PASS 159, SKIP 0, FAIL 0, MISSING 0, `fired 178 / declared 178`, executed 178 honoured 178, lies 0, refused 0, died 0 — GREEN**, 1983 s wall (gates 1061 s, controls 913 s); 8 gates deferred by cadence and NAMED. **THIS ROW WAS WRITTEN BEFORE THE TIER** except this sentence, which states its result. **THAT IS THREE THINGS THE CLOSE FOUND THAT THE SITTING ITSELF LEFT WRONG** — MAME's stale record, the patch sha1 in `BINARY.txt`, and this — each invisible to the work that created it and visible only to a check that ran afterwards. |

## Session 14z-172 — **#168 FIXED, AND THE MECHANISM BEHIND IT NAMED: THE ENGINE'S DOUBLE-PASS CADENCE IS STRICTLY PERIODIC IN THE FRAME INDEX (one residue class mod 3 at speed level 8, three mod 13 at level 6, measured per-frame from `RAM:$FF8081` on the rig's own legs), so a schedule shift has a PHASE. 14z-171's 190 and 200 were ≡ 1 and ≡ 2 mod 3, which is why they re-rolled the corpus's input-window-edge events; `tools/name_moves.py` now derives its shift as `ceil(shortfall / 39) * 39` — 39 = lcm(3, 13) — giving 195, and over the whole naming corpus that moves exactly TWO lines, both corrections of events that were recording PYRON'S MEDIUM PUNCH under a throw's name, identified from HIS OWN part-1 row and settled by a CAPTURE. `audit_move_parity` DIFF **102 -> 94**, the ENTRANCE root's 15 x-only rows gone. Two new gates (`audit_tick_phase`, the provenance of both constants and of the 2545 round start with its separating control, which discharges 14z-171's named promotion debt; `audit_rig_opening` rewritten to claim CONVERGENCE rather than agreement, with `pin-before-round` as its ground truth). A [VSP-13] correction to `engine_internals.md`: 14z-156's "no modulus of the frame counter separates the double-tick frames" is TRUE OF THE BYTE — `$FF8080` wraps at 256 and 256 mod 3 = 1, and its window contains a wrap at frame 2652 — and its positive control could not have caught that, being defined in the instrument's own coordinates. One new ticket, #169.**

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, `docs/NEXT_SESSION.md`), `main` == `origin/main` at `e7973dd0`, `emu/fbneo` dirty by exactly its two tracked patches (841/4), nothing running, ROM audit **76/76**. The maintainer: *"#168 first"*, then *"carry on, the findings are valuable, make things right"* |
| **(1) THE MECHANISM — MEASURED, NOT SEARCHED FOR** | `#168`'s open question was WHY the corpus is frame-fragile. The pass counter `RAM:$FF8081` read per frame on a naming-rig leg (`huitzil_7`, native vsav2, frames 2560-6000) answers it: the double-pass frames are **exactly one residue class mod 3 at level 8** (1147 of 3440, every gap 3 — the level `test_move_naming` runs, vsav2's default TURBO) and **exactly three classes mod 13 at level 6** (793 of 3440, gaps 4×528 / 5×264 — the pin `audit_move_parity` puts on both legs), each disjoint from the single-pass frames. So an input scheduled ΔF later meets a different pass phase unless ΔF is a multiple of the period, and a recipe at an input-window edge then fires a different move. 14z-171's ΔF = 190 is ≡ 1 mod 3 and 200 ≡ 2 — which is also why 200 was the worse of the two. Homed in `docs/game/engine_internals.md` |
| **(2) THE FIX — A DERIVED SHIFT, NOT A TUNED FLOOR** | `tools/name_moves.py` carries `ROUND_START = 2545` and `TICK_QUANTUM = 39` and computes `first = FIRST_EVENT + ceil(shortfall / 39) * 39`, with the guard asserting no position pin lands before the round start. ΔF = **195**, first event 2600 -> 2795, first pin 2370 -> 2565. It moves **7 rigs and leaves 35 byte-identical** — the `far`-first parts pin at 2560, already 15 frames past the round start, and the `PIN_FLOOR` form 14z-171 wrote would have shifted all 35 of them by 5. The round-fit assertion now measures from `ROUND_START`, a fixed origin, instead of from the moving `first` |
| **(3) WHAT IT BUYS, MEASURED** | `test_move_naming` over the whole corpus moves **exactly two lines**, both CORRECTIONS: `pyron_3 Corona Whip [6MP]` `a2:0x03` -> `a2:0x1e a2:0x20`, which is what `[6HP]` and `[4MP]` already read, and `pyron_5 Planet Burning [MP] step back (control)` `a2:0x02` -> `a2:0x1e a2:0x21 a2:0x22`, the chain `moves_pyron.toml` names. **WHAT THOSE TWO IDS ARE — and 14z-171's issue text called both "a walk", which is wrong:** walks are table `a` (`a:0x02` forward, `a:0x04` back), and `a2` is the ATTACK table; `move_naming_pyron.txt` part 1 names **`a2:0x02` as Pyron's own `5MP`**. So the rigs were recording PYRON'S MEDIUM PUNCH — the normal a command throw leaves when it does not connect — under the throw's name. **A FIRST VERSION OF THIS ROW OVER-REACHED AND THE RULE-CHECKER CAUGHT IT (run `2026-09-20-87` Q1):** it read `a2:0x03` as "the FAR standing MP slot" from `tests/expected/vanilla_normal_slots.tsv` and inferred that the id alone proved the fighters met the event at range. That table covers the **15 VANILLA characters only, no tenant row**, and its mapping is character-dependent — `near MP` is `a2:0x02` for all 15, but `far MP` is `a2:0x03` for only **10**, the other five (AN, BI, JE, QB, ZA) taking `a2:0x02` at both distances. **Pyron is not in it**, so what `a2:0x03` is for HIM is not established and the range inference is WITHDRAWN. What establishes the range is the capture. CAPTURED before and after on native vsav2 and put before the maintainer (`build/rig172/cap/*_sheet.png`, six frames per leg at matched offsets): BEFORE, Pyron punches and Demitri stands untouched; AFTER, Demitri is thrown on `pyron_3` and is caught inside the burning planet on `pyron_5`. The control's identity was settled by the [VSP-170] discriminator, not by the chain (normal and ES share it): it spends **0** stock where all five ES events of the part spend **1**. `audit_move_parity` **DIFF 102 -> 94**, x-only DIFF **24 -> 9**. **The 15 rows that leave the x-only class are not 15 rows removed** (the rule-checker's Q1, run `2026-09-20-86`, and the claim as first written overstated it): **10 become IDENT, 5 stay DIFF under another field** (`meter` x4, `p1hp` x1 — the entrance artifact had been masking the real divergence), and **2 rows go IDENT -> DIFF**, the two corrected events, which compare a real move against native for the first time instead of comparing two whiffed punches. 102 - 10 + 2 = 94. `donovan_13` is shifted and its verdicts are UNCHANGED — the mod-13 half holding at level 6. Every moved column attributed: verdict 12, delta/fields 18, the excluded-frames marker 43 (the pokes follow the schedule) |
| **(4) THE CONTROLS — AND THE TIDIER CLAIM THEY KILLED** | seven shifts run over the corpus (`build/rig172/shift_sweep.txt`). **The Circuit Scrapper family moves at every shift with mod 3 ≠ 0 (190, 194, 208) and at none with mod 3 = 0 (192, 195, 234, 273) — 3 negatives, 4 positives, no exception.** 195 and 234 are clean outright; 194, one frame from 195, brings back **six** regressions. A "the period is exactly 39" claim looked rigorous after 195 and 234 (it divides gcd(195,234), and 192/208 exclude 3 and 13) — and **273 = 7×39 falsified it**, which is why it was run. What survives: 195 is a MEASURED-CLEAN shift, not a proven-safe one, and the residual is `pyron_3`'s marginal air throw, ticketed as **#169** — at HEAD its `[j.6HP]` row is already frozen as `a2:0x14`, a plain j.HP, so the rig has been recording a whiff under the move's name since 14z-120; #168's shift leaves both rows exactly as HEAD had them |
| **(5) TWO NEW GATES** | **`audit_tick_phase`** — the provenance of the generator's two constants, which nothing asserted: the cadence's period and gap set at both levels (the assertion is the PERIOD — how many residue classes, disjoint from the singles — never the residue VALUES, which depend on where frame numbering starts), `TICK_QUANTUM == lcm(3, 13)`, and the round start equal on both legs AND with the rig's own speed-level pin WITHHELD. That last one **discharges the 14z-171 close's named promotion debt** (`roundstart.sh` / `rs_unpinned.sh`, whose 2545 `engine_internals.md` and #168 both quote and no gate asserted). Controls `flat-pass`, `quantum-off`. **`audit_rig_opening` rewritten** (its code was lost to a `git checkout` at 14z-171): it no longer claims the legs agree — they do not, and that is native behaviour — but that the rig is CLEAR of the difference: same round start on both legs, every naming part's first pin at or after it (read from the committed schedules, 22 parts), the legs DIFFER at a pre-pin sample so the convergence assertion is not vacuous, and from the pin on they agree on x and seq at every sample including the first event (2795: `x=899 seq=4` on both, where the pre-fix rows stood 117 px apart). Its `pin-before-round` control regenerates the rig from an unfloored copy of the generator and fails **both** 2.2 and 2.4 — the ground truth the issue specified |
| **(6) A [VSP-13] CORRECTION, AND A TRANSFERABLE ONE** | the new measurement appeared to contradict `engine_internals.md`'s live "no modulus 2..64 of the frame counter `RAM:$FF8080` separates the double-tick frames" (14z-156). It does not: **`$FF8080` is a BYTE.** It wraps at 256, and 256 mod 3 = 1, 256 mod 13 = 9, so every wrap ROTATES the residue — and the 14z-156 window (2560-2760) contains one, at frame **2652** (255 -> 0), which alone splits the level-8 double set across residues {1, 2} and overlaps the single-pass ones. The same set indexed by the frame INDEX is disjoint in that very window (67 doubles, residue {2}). So the negative was sound and the sentence drawn from it ("not the frame counter") was too wide; both carriers corrected, `audit_tick_cadence` re-run GREEN (its assertion was always about the byte). **AND ITS POSITIVE CONTROL COULD NOT HAVE CAUGHT IT**: the plant was *"a target set defined as counter % 3 == 0 must be found at modulus 3"* — defined in the INSTRUMENT'S OWN COORDINATES, so it rides through the wrap with the instrument. Two gotchas written: the schedule-shift phase, and this ([VSP-166]'s shape) |
| **(7) ONE OF MY OWN READINGS WAS WRONG AND IS CORRECTED** | the parity figures were first reported from a run taken after a control run had left `tools/name_moves.py` at quantum **273**, not 195 — `build/rig172/shift_control.sh` mutates the generator and nothing unwound it. Re-run at 195: the two tables are **byte-identical over all 506 rows**, so the figures stood, but they had not been measured on the schedule that lands. The script now restores the tool and the committed rigs on EXIT ([VSP-40]'s shape: build in one step, measure in another) |

| **(8) THE RITUAL CHECK FOUND A DEAD GATE BRANCH — AND MY OWN VIOLATION IN IT** | asked to verify the close was complete, the structural items all passed and then the SUBSTANCE did not: NEXT_SESSION's START HERE item 2 read **"#168 IS DONE (14z-172)"** — a closed item in an open list, exactly what [VSP-17] bars, "however it is marked". `test_state_open_lists` was GREEN over it. **WHY: the start-here branch of `tools/check_state_lists.py` parsed ZERO entries.** `entries()` recognised only `- ` bullets, and NEXT_SESSION's START HERE became a **numbered** list on 2026-09-18 (`024495b7`) — measured across the last 15 revisions of the file: 4-5 entries parsed up to `13793617`, then **0 for every revision since**, ten of them. The branch asserted nothing for four days. **WHAT LET IT ROT:** the gate declares five must-fire controls and **not one of them exercised start-here** — the [VSP-181] shape, a branch with no control going dead unnoticed. FIXED: `entries()` takes `- ` or `1. ` (blast radius measured first — START HERE 0 -> 5 entries, "Decisions pending" 0 -> 0, "Standing rulings" 32 -> 32, so the only new catch was mine); a sixth control **`closed-start-here`** perturbs the REAL `docs/NEXT_SESSION.md` copy with a DONE entry and reaches the gate's own FAIL; the revived check then caught the violation on its first run and the item is gone from START HERE, its mechanism pointer folded into #169's entry where it belongs (facts live in the docs, not in an open list). All six controls fire in-gate and as modes; the census re-frozen at 134 declaring |

| **CLOSE (14z-172, 2026-09-20) — THE CHECKLIST** | **(1) THE FINDINGS TABLE** — 15 findings, each with its live home and the test that reproduces it: the two cadence periods (`engine_internals.md` the cadence bullet / `audit_tick_phase` §1); the byte-wrap account of 14z-156's negative (same bullet + `audit_tick_cadence`'s header / `audit_tick_phase` §1's BYTE line); the own-coordinates control trap (`docs/project/gotchas.md`, new / a method gotcha, no test); the schedule-shift phase rule (`docs/project/gotchas.md`, new / `audit_tick_phase` §2); the 2545 round start with its separating control (`engine_internals.md` / `audit_tick_phase` §3 — this DISCHARGES 14z-171's named debt); the seven affected parts and the derived shift (`tools/name_moves.py` / `audit_rig_opening` §2.2, 22 parts); the convergence at the first event (`tests/expected/rig_opening.tsv` / `audit_rig_opening` §2.3-2.4); the pin frame is not convergence evidence (`audit_rig_opening` header + §2.5); `a2:0x02` is Pyron's own `5MP` and both ids are `a2` NORMALS, not walks — and the slot table that tempted a wider reading covers vanilla characters only (`docs/project/gotchas.md`, new / `move_naming_pyron.txt` part 1); the ES-vs-normal stock discriminator on `pyron_5` (`tools/naming_event_stock.sh` header / `audit_move_parity` §2b); the Galactic Throw bistability (#169 / `test_move_naming`); the 102 -> 94 composition (row (3) / `audit_move_parity`); the quantum-273 slip (`docs/NEXT_SESSION.md` trap 1 / the restoring script); x2b7ef4's aligned count following the longer idle (the #168 closing comment / `audit_x2b7ef4_reach_m18`); and — found by the ritual check AFTER the first tier — the dead start-here branch and the closed item it was not catching (row (8) / `test_state_open_lists`'s new `closed-start-here` control). **(2)** `close_findings.py 14z-172 --base e7973dd0`: **0 GAPS, 0 REVIEWs, 0 PROMISES**; its clean result is not cited as completeness. **(3) POINTERS:** two added to the 14z-171 group — its close row's promotion debt marked DISCHARGED, and its row (6)'s "the floor value decides WHICH" marked with the mechanism this sitting found; its "a walk" reading corrected in place. **(4) PROMISES:** none outstanding. **(5) SCRATCH SCRIPTS, ON TRIAL:** **8** under `build/rig172/`; **4 promoted** — `tick_leg.sh` and `tickphase.py` into the new gate `tests/audit_tick_phase.sh` (which ASSERTS their figures rather than quoting them), `stock_at_events.sh` -> `tools/naming_event_stock.sh`, `correction_sheet.sh` -> `tools/naming_shift_sheet.sh`; **3 quote nothing** (`p2cycle.sh`, `readonly_sweep.sh`, `refreeze.sh`); **1 NAMED RESIDUAL** — `shift_control.sh`, whose seven-row sweep the new gotcha and the #168 closing comment both quote. It is NOT promoted and the reason is stated rather than waved: the CONSTANT it produced (39) is asserted by `audit_tick_phase`, and the sweep's rows are one-off historical measurements, each with its log under `build/rig172/naming_*.log` — a gate re-running seven whole-corpus shifts would cost ~20 min to re-derive a number already gated. **(6) THE DOCUMENTATION PACKET** through the rule-checker: run `2026-09-20-87` (`recommendation`, plant `inferred-decision-14z162` CAUGHT) returned **VIOLATED on Q1 and Q3**, both findings TRUE and both RESOLVED BY WORK — **Q1** caught this row's own first version reading `a2:0x03` as "the FAR standing MP slot" from a table that covers the 15 VANILLA characters only and maps `far MP` to `a2:0x03` for just 10 of them, with no Pyron row (the range inference withdrawn, every carrier corrected, a second comment posted to #168), and **Q3** caught `audit_tick_phase` §3 claiming to separate the RIG when its two legs share one replay and one base poke list and it separates only the SPEED PIN (the header and both verdict lines now say exactly that). The EARLIER run `2026-09-20-86` (`expectation`, plant CAUGHT) returned VIOLATED on Q1 Q2 Q3, also all true and all resolved by work — its **Q3 found a real defect in the new `audit_rig_opening`** (the pin frame, whose `x` the rig itself writes, sat inside the convergence assertion) and its **Q2 produced the capture that falsified the "a walk" wording**. **THE TIER:** `ROMDIR=... tests/run_all_static.sh --strict` (session cadence, every control executed — the default `all`): **PASS 151, SKIP 0, FAIL 0, MISSING 0, `fired 170 / declared 170`, executed 170 honoured 170, lies 0, refused 0, died 0 — GREEN** on the first run, 1339 s wall (gates 563 s, controls 766 s). 14 gates deferred by cadence and NAMED; three above-cadence gates triggered by a changed path they depend on (`test_emulator_runner` and `test_bbh_fidelity` from `ci_emulator.tsv`, `test_suite_dispatch_selftest` from `PROVENANCE.md`) and all three passed. **THIS ROW WAS WRITTEN BEFORE THE TIER** — the omission the 14z-171 close recorded against itself; only this one sentence could not be, since it states the tier's own result, and the doc gates were re-run over it afterwards. **THEN THE RITUAL CHECK REOPENED IT** (row (8)): the tier above predates `check_state_lists.py` and `test_state_open_lists.sh`, so it was RE-RUN IN FULL on the fixed tree rather than patched with the individual gates — a partial re-run is not the close condition. **SECOND TIER:** **PASS 148, SKIP 0, FAIL 0, MISSING 0, `fired 168 / declared 168`, executed 168 honoured 168, lies 0, refused 0, died 0 — GREEN**, 1091 s wall. It runs 148 where the first ran 151 because the three above-cadence gates the first triggered (`test_emulator_runner`, `test_bbh_fidelity` from `ci_emulator.tsv`, `test_suite_dispatch_selftest` from `PROVENANCE.md`) no longer trigger — those paths were committed at `1d9b6a13` and `git diff 5ca995d2 --` over both is EMPTY, so the first tier's green on them describes this content unchanged; the cadence is working, not hiding anything. |


# STANDING SECTIONS (current state — never archived)
## Standing rulings

One line per ruling that still constrains work; the full entry lives where the line says. A line is deleted when its ruling stops constraining work (CLAUDE.md [VSP-17]).

- **FBNeo patch 0003 (drop SDL2_image) is RATIFIED for RELEASE builds (2026-09-21, 14z-173 — "in that case then yes, i approve of patch 0003"):** frontend-only, applied by `build_release_emulators.sh` and NOT by `setup_fbneo.sh`; the macOS bundle goes 24 files -> 4, so "Open Anyway" costs 4 approvals instead of 24, and it is proven inert over 12,120 frames in work RAM AND framebuffer. Full entry: `DECISIONS_HISTORY.md` "Ratified 2026-09-21 (14z-173) — FBNeo patch 0003".
- **#144 is macOS blocking the binaries and right-click > Open does NOT get past it (2026-09-20, 14z-173 — the maintainer's captures and "Does not open"):** the shipped advice is withdrawn from every live carrier; clearing the quarantine flag is what works, `PLAY.command` offers it, and System Settings > "Open Anyway" also works but PER FILE — **MAME 2 approvals, FBNeo 24** — so the READMEs lead with the one-step route; the ticket now turns on whether to notarize (a paid Apple account). **The player READMEs are written for a non-developer who has never seen this project** ("it must have felt clear to the expert who wrote them"). **The applier page ships as a local file in the asset, one page per release.** Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-20 (14z-173) — #144 is macOS BLOCKING the binaries".
- **The applied set is DEFLATED and the QSound BIOS member is OPTIONAL (2026-09-20, 14z-173 — "If it is identical content, I see no reason not to go for the smaller size" and "Ideally I'd love the option to choose to add it or not"):** `apply_release.py` writes deflated (73.7 -> 28.0 MB, +0.26 s once at load, measured) and `--no-qsound-bios` omits `dl-1425.bin`, which MAME needs, FBNeo does not, and MiSTer resolves from the card's `qsound.zip`; the MiSTer page tells players to use the flag and the emulator pages the default. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-20 (14z-173) — the applied set is DEFLATED".
- **The deliverables' order of work, and macOS FIRST (2026-09-20, 14z-173 — "Agreed with one caveat: let's focus on MacOS first, even if we were to release M19 only for MiSTer and MacOS, the proof of concept would be fully validated"):** standalone set -> launcher per platform -> reproduce #144/#145 on clean hosts -> README pass -> the applier app; macOS and MiSTer are the proof-of-concept release and Windows/Linux run in parallel without blocking it (so #145 leaves the near-term path, #144 and #146 stay). Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-20 (14z-173) — THE DELIVERABLES".
- **The standalone set lands in the RELEASE, not the build (2026-09-20, 14z-173 — "C"):** the applier completes `vsavjw.zip` with 7 pristine members so a player places ONE zip; no build fingerprint, expectation set, registry row or MiSTer CRC moves, and the shipped set being a declared superset of the gated one is held by `test_release_roundtrip` §1 and `test_release_binaries` §2b, never by argument. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-20 (14z-173) — the standalone set lands in the RELEASE".
- **M19 is frozen, NOT released (2026-09-20, 14z-170 close — "let's not release now, especially since we have tickets relative to the deliverables on various OS"):** the release run waits on #144 (macOS blocks the prebuilt binaries), #145 (Windows: they fail to load) and #146 (the player READMEs); `release/merged-m19/` stays packaged and gated in-tree. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-20 (14z-170 close)".
- **The x2b7ef4 placeholder corruption lands with M19; merged-m18 stays as published (2026-09-19, 14z-170 — "M19 supersedes it (Recommended)"):** the corrected resolver ships in the M19 freeze with a ticket; no M18 hotfix. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-19 (14z-170) — the x2b7ef4 placeholder corruption".
- **The disabled EX route does the no-stock move (2026-09-18, 14z-169 — "Yes, the no-stock move"), refined 2026-09-19 (14z-170 — "Natural reading (Recommended)"):** the tenants' vs2 EX input is read as its sub-pattern at every stock level — for Phobos and Donovan vs2's no-stock move; for Pyron 623+PP, the ES move when a stock is available and the normal move without one. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-169) — the disabled EX route does the NO-STOCK move" and "Ruled 2026-09-19 (14z-170) — Pyron's 2623+PP with stock reads NATURALLY".
- **The 0x52 fix is scoped S1 (2026-09-18, 14z-169):** Phobos's two trap remaps stay on the solo Phobos track only (a row key: unless Donovan's manifest is in the composition); the merged build carries the full fix. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-169) — the class-0x52 fix is scoped".
- **#136: all the analysis first, then the fixes, then relentless regression testing (2026-09-18, 14z-168 — the maintainer: "let's finish all the analysis first. Then we'll fix and then relentlessly test for regressions"):** no #136 fix (nor #157, #159 or the 14z-168 tickets) lands before every family is analysed; every rig built for it becomes a gate that runs at freeze and release. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — #136: ALL the analysis first".
- **The tenants' Dark Force (2026-09-18, 14z-168):** vsav's Dark Force CHANGE with the shells' common rules, each tenant's own vs2 startup window, and its personal Dark Force (vs2's EX move) active for the mode; compared against the vs2 EX move, never vs2's P+K (Dark Force POWER); the DURATION stays vsavj's (360/377/360, *"agreed"*); the tenants' vs2 EX input into the mode on our build is to be DISABLED (*"agreed"*). Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168)" (the Dark Force and the EX-route entries).
- **The tenants' DEFENSE rows become vs2's (2026-09-18, 14z-168 — "given the measurements we should take the vs2 rows"):** Phobos and Donovan take their vs2 defense rows and rally thresholds (Pyron needs nothing), with no frame cost and no change to any legacy character; supersedes 2026-08-14 option (b). Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — the tenants' DEFENSE rows become vs2's".
- **The column shock and the Plasma Trap take vs2's class-0x52 rule (2026-09-18, 14z-168 — "I'm all for fixing"):** victim shock 0x18 and the attacker exempt, for the tenants' 0x52-origin hits; no noticeable lag, no new breakage; the 14z-85g(2) accepted deviation is withdrawn. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — the column shock and the Plasma Trap".
- **No gauge is built during Dark Force (2026-09-18, 14z-168 — "it makes sense that you can't build meter during DF"):** the shells' rule; the tenants' start-up gauge in the mode is a defect (#157's Dark Force tail). Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — no gauge is built during Dark Force".
- **The close checklist (2026-09-18, 14z-167b — the maintainer: "let's implement them and keep item 2"):** at every close, (1) a findings table — each finding of the group, its live home and its test; (2) `tools/close_findings.py <session>`, every GAP homed and every REVIEW answered, its clean result never cited as completeness; (3) a pointer on every earlier row the sitting resolves; (4) a grep for promises; (5) cited scratch scripts promoted, on trial and measured per close; (6) the documentation packet through the rule-checker. The steps live in this file's header (ruled 2026-09-18). Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-167b)" (two).
- **#136 goes over again by the 14z-164 census (2026-09-17, maintainer: "I agree with the 3 propositions"):** the move-parity gate moves to PER-EVENT verdicts with the stock, the meter fraction and P2's HP compared and every "in DF" event asserted in DF; a P2 whose data is the same on both games (a second tenant, or a legacy character byte-identical between vsavj and vs2, found statically) — done 14z-165, Demitri; the 13 rows re-labelled by their fields on #136 before any family is root-caused. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-164)".
- **The rule-checker's Q3 and Q4 are BOUNDED, and a VIOLATED is resolved question by question in writing (2026-09-17, 14z-163, #152 — the maintainer: "both"):** Q3 counts only a shared thing the artifacts SHOW can write the compared state; Q4 reads OK when the claim itself names the uncovered mode; what a bounded question still finds stops the action until `rulecheck resolve` answers each violated question. Every fixture recalibrated on the new wording. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-163) — the rule-checker's questions".
- **The rule-checker runs on the default model (2026-09-17, 14z-163):** a change of model is a recalibration. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-163)"; the "#152 closes after a first real run" half of that line was met and the ticket closed by ruling 2026-09-17 (14z-164b).
- **#136 is gone over again IN FULL, not only through its faithfulness control (2026-09-16):** the maintainer insists it stays on the list until every part of it — the naming rigs, the pinning protocol, the 27 frozen verdicts, and the Pyron and Phobos legs above all — has been re-examined; no #136 verdict is built on until then. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-16 (14z-160)".
- **The M19 freeze is WITHDRAWN (2026-09-16):** #147's fix was a regression on a rig artifact; the tree is back at merged-m18 and #151 re-examines #136's forced-pick native legs. No freeze is current beyond merged-m18. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-16 (14z-159)".
- **The static tier runs by CADENCE (2026-09-17, #148 lever A):** `tests/ci_cadence.tsv` lists twelve build/content/tooling gates as `freeze` (the seven costliest plus the five 35-39 s gates, second cut) and five release/MiSTer gates as `release` (the three ≤2 s ones back to session); a session run (the default, mid-session AND at the close) defers them BY NAME unless a path they depend on changed; a freeze runs `--cadence freeze`, a release `--cadence release`, each on the commit it builds from. A new listing is a reviewed event. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-162) — #148 lever A".
- **Static-tier controls at the CLOSE (2026-09-14; REAFFIRMED 2026-09-17 after 14z-161 pushed on the mid-session form):** a mid-session commit runs `tests/run_all_static.sh --strict --exec-controls none` plus `CONTROL=<name>` for every gate it adds or changes; the session close runs every control (the default) and ONLY then pushes — not to be compromised until #148 is worked. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-14 (14z-154)" and "Ruled 2026-09-17 (14z-161b)".
- **The order of work (2026-09-17):** a stable, reliable state first, then #148 (the static tier's cost, done), then #152 (the adversarial rule-checker, done 14z-164b), then everything else — the #136 families included. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-161b)".
- **Release scope (2026-09-02):** at release every test whose SUBJECT is the released artifact runs, its legacy content included (a reference leg on vsav2 or pristine vsavj is not the subject); anything red or skipped is a hard fail unless approved at release time, so a release-scope gate fails loudly on a missing prerequisite. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **A red gate is a question (2026-09-02):** before fixing the gate, fixing what it caught or deleting it, establish which side's expectation rests on a measurement; a frozen number whose provenance cannot be named is a claim. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Cross-game comparisons at a matched speed level and a pinned RNG (2026-09-15, #135, #142; amends the 2026-09-02 #114 line):** vsav2 defaults P1 to TURBO and vsavj to NORMAL, and the two games' RNG states differ, so `tests/test_don_immortal_native.sh` pins `RAM:$FF8116` (levels 6 and 8) and `RAM:$FF80D4-D5` on both legs and asserts ours == native in hit count, damage and hit frames; LP's accepted "one hit short" is retracted as a mode artifact and #142 is closed invalid; the 2026-09-02 ruling that the vanilla vsav engine takes precedence stands. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-158)" and "Moved 2026-09-14 (14z-154)".
- **No static substituted-wheel gate (2026-09-02):** declined — the pattern matches 26 legitimate gates; the defence is [VSP-163] (assert `+0x60` against `bases.tsv`, or force the pick). Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Oboro's intro stays (2026-08-28):** never delay round start or cut the intro for a cosmetic reason. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Community data (2026-08-31):** measurement is king — a community source that matches ours exactly or by a constant offset validates our measurement, and an inconsistent pattern means re-check OUR measurement first. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Cosmetic is optional (2026-08-27):** cosmetic and single-player-only surfaces are nice-to-have; competitive 2P versus is the focus. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **No legacy character meets a tenant in 1P arcade (2026-08-28):** not a problem, 2P versus is the focus; not built, not scoped. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **The living-docs generalisation, when scheduled (2026-09-08, #119):** independent of bbh but compatible with it and on the same principles; the site generator in scope; fidelity required in principle, its form argued in the scope document; the open items first. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Linux release binaries (2026-09-13, #121):** self-containment is checked against an external host-provided list plus the ruled exceptions; the WSL2 Linux binaries are never published. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Generator refactor policy (2026-08-21, #50):** no dedicated refactor of `tools/gen_donovan_patch.py`'s `main()` is ever scheduled absent a new measured cost; a handler moves to module level only when a test needs to drive it, with its unit test in the same change, and byte-identity under `tests/test_m3a_reproducible.sh` and `tests/test_phasec_spaces.sh` is the refactor gate. Full entry: `STATE_HISTORY.md` "Session 14z-102 (post-freeze rulings)".
- **Don't assume; measure first; ask if necessary (2026-09-15, clarified 2026-09-16):** no factual claim — about code, a count, a behaviour or a name — is stated without a measurement or a tree source behind it; a question a measurement can settle is measured first and never published as "unreconciled"; asking the maintainer is right when a measurement cannot settle it or is not enough. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)" and "Ruled 2026-09-16 (14z-158b)".
- **Pipeline-image registry rows (2026-09-15, #96):** the M2 battery dispatches through `tests/expected/registry.tsv` on the build's fingerprint, never a pinned set name; `donovan-mN-stock` / `-stage4` register its two pipeline images (untagged, never shipped); each freeze rebuilds both and records whether they moved, and an unregistered fingerprint is a rule-6 stop. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)".
- **The merged set carries no tenant expectations (2026-09-15, #111; the 14z-133b B2 design):** the merged expectation set holds the legacy classes, the mask and the `.skip` markers; tenant-content `.sha1` expectations live in the solo sets, and the merged build's tenant content is covered by `audit_merged_legacy` leg (b) and the tenant gates. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)".

## STANDING PRINCIPLE (maintainer, 2026-08-05): vanilla wins ties

**[VSP-21]** "vsav vanilla is always better when we can." **When a console port and
arcade vsav differ and both would work, take vanilla.** A console port's
choice is not evidence that vanilla is wrong; it is evidence of what that
port's designers preferred.

This is a general rule, not a one-off: the PS1 capture is a reference for
what is POSSIBLE and for data we cannot otherwise obtain (cell placement,
the adjacency of NEW cells), not a style guide for content vsav already
defines. Paired with the maintainer's other statement — "as long as we can
select characters it's good" — the test is: does keeping vanilla still let
the feature work? If yes, keep vanilla.

Applied immediately, twice:
- **`Bishamon DL` and `Aulbath DR` stay vanilla** (Anakaris / Sasquatch).
  PS1 sets both to "no move"; neither is needed for reachability, so
  vanilla stands.
- **Horizontal wrap stays vanilla.** Vsav wraps left/right (cell `0x01`
  Left goes to `0x05`, measured and confirmed in-emulator); the PS1 report
  of "no wrapping" reflects untested extremes. We touch none of those
  cells, so nothing to decide.

Judgment applied under the same rule, open to veto: the three inbound edges
from `0x0B` (`D`/`DL`/`DR` into the new row) DO diverge from vanilla, and
strictly they are not required — Phobos and Donovan are already reachable
via `Bishamon D` and `Aulbath D`, and Pyron through them. They are kept
because without them, pressing Down on the cell directly above the new row
does nothing while three medallions are visible below it, which is the UX
failure "as long as we can select characters" is meant to exclude. Dropping
them would reduce the legacy footprint from 5 bytes to 2.

## Decisions pending (human)

Only undecided items live here; a ruling moves its entry to `DECISIONS_HISTORY.md` in the
commit that records it (CLAUDE.md [VSP-17]).

*None open.*

## THE DEADNESS REGISTER (opened 14z-71, maintainer's standing instruction)

**[VSP-23]** Every claim of the form **"legacy never reaches this, so we may reuse
it"**. Each is measured by ABSENCE, which is the weakest kind of evidence
we accept, so each is listed here with its guard. **These are the FIRST
PLACES TO CHECK for any unexplained regression in vanilla assets, engine
behaviour or rendering** — before anything else is suspected.

| Reused resource | Claim | Guard | Fallback if wrong |
|---|---|---|---|
| ~~palette-seq ids 0x1E-0x21 (`0x39ACC0`)~~ **CLAIM FALSE, ROW WITHDRAWN 14z-79 (they are Bulleta's DF block)** | vanilla only ever requests 0x26/0x27 | `tests/audit_palette_seq_ids.sh` (10,504 sampled calls) | none — the palette path never transits work RAM, so the audit is the ONLY guard |
| effect-class row 16 (`0x080AEC`) | vanilla never dispatches class 16 | `tests/audit_effect_class_rows.sh` §1, 0 reads vs a 1760-hit control | none needed: the row was a stub (`rts`), so a wrong claim costs at most the old no-op |
| ~~drawer list-type 6 (`0x01B6AA`)~~ **CLAIM FALSE (measured 14z-89) — LEGACY LISTS DO REACH TYPE 6** | vanilla has no type-6 sprite lists | `audit_effect_class_rows.sh` §1/§4 + `tests/test_beam_list_type6.sh` | **THE FALLBACK HELD — this is what a safe-and-loud design buys.** 14z-89 measured the tripwire ARMED on legacy content on huitzil-m13: `21_don_mash` 387 times and `26_don_arcade_mash` 948 times, PC-attributed to inside the thunk body (0x0FD060). Rendering stayed correct throughout (the fallback runs vsav's own type-6 code, reproduced instruction-for-instruction), so nothing rendered wrong and no playtest ever saw it — exactly the outcome the register's "prefer designs where being wrong is safe and loud" rule was written for. WHY IT WAS MISSED: the deadness measurement was sound but its COVERAGE was four replays (`02/07/09/30`), and the gate has always run on that default set; the two replays that arm it are long mash/arcade rigs nobody pointed it at. COST TODAY: `$FF010C/$FF010D` is a live work-RAM counter vanilla does not keep, so both replays diverge permanently from the vanilla masked basis — they are `.pending` on huitzil-m13 pending the maintainer's ruling. ~~OPEN: does the fallback need to stop counting (make the tripwire diagnostic-only / move it out of work RAM), or is the counter acceptable? See "Decisions pending — 14z-89"~~ **ANSWERED 14z-91: the counter was REMOVED** — the `beam_list_type6` `RAM:$FF010C` counter deleted, which cleared all six `.pending` legacy replays (`HANDOFF.md` Build registry, the 14z-91 row (C)) |

**[VSP-22]** Rules for adding a row: the claim must be measured with a POSITIVE CONTROL
on the same instrument and leg (a blind instrument and a real zero look
identical — paid for three times in 14z-71); it must name its guard; and
it must say what happens if the claim is wrong. Prefer designs where being
wrong is *safe and loud* over designs that are merely well-measured.

# STATE — living progress log

**SPLIT 2026-08-20 (14z-99 post-freeze close, maintainer-approved), AMENDED
2026-09-14 (CLAUDE.md [VSP-17] and [VSP-182]): this file holds the RECENT
session groups and the STANDING SECTIONS; the full detail of every older
session lives verbatim in `STATE_HISTORY.md`, under THE LEDGER at its head.**
How to work with it:
- **Lookup**: "STATE 14z-XX" references resolve here first, then in
  STATE_HISTORY.md (its LEDGER first) — section names are preserved verbatim
  in the archive. A reference to `STATE "Decisions pending"` for an entry no
  longer here resolves in `DECISIONS_HISTORY.md` (entries move there verbatim
  when ruled — since 2026-09-14; before that, once they stopped shaping work).
- **Claim-greps MUST include STATE_HISTORY.md** (the CLAUDE.md §5
  retraction-discipline command names it).
- **AN OPEN LIST HOLDS ONLY WHAT IS OPEN (maintainer-ruled 2026-09-14).**
  "Decisions pending" holds undecided items only; nothing closed stays in it,
  however it is marked (struck through, DONE, FIXED, CLOSED). Where each thing
  goes instead:
  - a RULED decision -> its whole entry moves VERBATIM to
    `DECISIONS_HISTORY.md` in the commit that records the ruling; while the
    ruling still constrains work, ONE line stating the rule sits under
    "Standing rulings" here, and the line is deleted when it stops;
  - a BUG, COSMETIC ITEM or EVOLUTION is a TICKET — listed in
    `docs/project/tickets.tsv`, its story on its GitHub issue, never listed
    here; a STATE entry for one that closes moves VERBATIM to
    `STATE_HISTORY.md` under the closing session's key (CLAUDE.md [VSP-182]:
    the four questions, the closing comment);
  - "STANDING PRINCIPLE" and "THE DEADNESS REGISTER" carry the [VSP-21..23]
    anchors and stay; the register keeps its withdrawn rows by [VSP-23].
  `tests/test_state_open_lists.sh` enforces it, with the size budget and the
  group count below.
- **ROLLOVER RULE (part of the session-close ritual)**: after writing the
  close entry, move session groups beyond the newest THREE to the TOP of
  STATE_HISTORY.md's body in newest-first order (below THE LEDGER, above the first
  archived `## Session` heading OLDER than the group)
  and add their one-line entries at the top of THE LEDGER there, composed from
  the group's own banner headers. If this file still exceeds ~150 KB, roll the
  oldest kept group early. The standing sections never roll.
- **THE LAST STEP OF THE CLOSE IS THE PUSH (maintainer-ruled 2026-09-10):**
  static tier strict green WITH every control executed (the default `all`), the doc checks green, nothing pending -> `git push
  origin main`; anything red or skipped leaves the commits local and the
  close entry says so.

## Session 14z-167 — **THE RULE-CHECKER RECALIBRATED ON OPUS 5 AND 14z-166's UNCHECKED RE-FREEZE READ. Fable 5.1 was unreachable on the account's spend limit, and the tool never enforced a model change (#158 filed), so every fixture was recalibrated by hand under `--model claude-opus-5`; the re-freeze's packet read VIOLATED on Q1 Q2 Q4, every finding true, resolved: the ids filter dropped word stores (widened, every frozen row reproduced), a third must-fire control `double-write`, the 70 tenant rows shown identical to the first freeze. Then #136's x family, CAPTURE FIRST: the three Phobos first-event rows are the round-start ENTRANCE drawn differently per leg, identified by the maintainer on the capture, not the moves; donovan_3's row is downstream of Killshread Summon (ES) — its records carry facing rule 5, which vs2's hit code resolves and vsavj's XORs in raw, so the returning wave drags Demitri toward Donovan on native and pushes him away on ours: filed as **#159**, maintainer-confirmed on the capture, rule-checked, frozen AS MEASURED by the new gate `tests/audit_facing_rule.sh`. No shipped ROM byte moved; the tree stays at merged-m18.**

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, NEXT_SESSION), `main` == `origin/main` at `1a4e69c8`, bbh clean at `caee272`, `emu/fbneo` dirty by exactly the two tracked patches (841/4), nothing running, `gh` as `mechanyaa-ai`; ROM audit **76/76**. The session runs on Opus 5 (the maintainer's `/model opus`). The maintainer: *"start, let me know when you need me. Don't hesitate to have me checked results especially if the comparison is cheap for me and expensive for you (e.g. image comparison... but you know that already)"* |
| **(1) THE CHECKER'S MODEL — RECALIBRATED ON OPUS 5, AND #158** | a probe agent pinned to Fable 5.1 died on the account's monthly spend limit (HTTP 429, `req_011CfATjWFFhSxNutrVB9Yia`), as run `2026-09-18-36`'s readers had, so the readers run on Opus 5 — and under the 14z-163 ruling a change of model is a recalibration. `tools/rulecheck.py`'s `calibrated()` checks the checklist hash only, never the model, and every ledger row read `default`: the tool would have prepared an Opus run on the old calibrations without a word. Filed as **#158** (bug, the fix candidate stated, not applied). Recalibrated BY HAND, `--model claude-opus-5`: runs `-37` forced-pick (VIOLATED Q1-Q4, CAUGHT), `-38` inferred-decision (Q1 Q4 Q5, CAUGHT), `-39` fidelity-false-statement (Q1 Q3 Q4, CAUGHT), `-40` the negative fidelity **OK** beside a caught plant. Run 36's directory keeps its `NOT-RUN.txt`, so the new runs carry explicit ids (the tool's counter would have re-used 36) |
| **(2) THE UNCHECKED RE-FREEZE READ — RUN 41: VIOLATED Q1 Q2 Q4, ALL TRUE, RESOLVED** | run 36's packet re-prepared as `2026-09-18-41`, the claim VERBATIM, every artifact's SHA-1 unchanged but the STATE range (44-62 -> 44-57, the banner edit moved it). Plant caught; **Q3 Q5 OK; Q1 Q2 Q4 VIOLATED**. Q1: the ids reducer kept only `mask == ff00` and so dropped any word or long store to the id byte, and "70 tenant rows unchanged" had no artifact — resolved by WORK: the filter keeps every write touching the byte (`mask & ff00`), the verify run reproduces every frozen row (`build/gates_14z167/throw_reg_verify5.log`), and the 70 rows are identical to the first freeze as staged by run 32 (`throw_reg_tenant_rows_vs_first_freeze.txt`). Q2: the legacy capture followed the numbers and cannot show 9-vs-8 on a gauge drawn in 16-unit bands — ACCEPTED in writing, the legacy meter rule rests on the taps. Q4: nothing showed the `/Nw` marker could print — resolved by WORK: a third control `double-write` (a second write planted on the first contact frame of our P1 meter tap) fires in-gate (`+8/2w`) and its mode exits 1; the other two modes re-run, exit 1. `tests/expected/throw_registration.tsv` did not move (`b6d163a5`), nothing re-frozen; `ci_emulator.tsv` names three controls. The re-freeze is CHECKED now |
| **(3) #136 — THE PHOBOS FIRST-EVENT x ROWS ARE THE ROUND-START ENTRANCE** | the 24 x-first DIFF rows: four are a part's first divergence (huitzil_5/6/7 event 0 at +0, donovan_3 event 6 at +1). A scratch copy of the parity gate keeping both fighters' fields (`build/x_family_14z167/audit_move_parity_keep.sh`, every part reproducing its frozen verdicts) shows the three Phobos parts open identically: the rig's first X pin (2370) lands inside the round-start entrance (the round starts at 2544, `$FF812D`); native's entrance carries Phobos from x=360 through the pin to 702, ours leaves him at 552, the 15 walk frames after 2544 leave them 117 px apart at the event. The capture went to the maintainer FIRST (`build/x_family_14z167/cap/huitzil_5_opening_sheet.png`, `DELIVERED.txt` 07:01:41Z), who identified the entrances: *"It is not the same sequence: Phobos has I believe 3 possible: one where he arrives as a car with Cecil (his kid sidekick) as the driver and transforms back into his humanoid shape(the one on native in the screen captures) , one where he picks up Cecil who's trying to stop the fight and sets him to safety behind him and one where he arrives with Cecil in his hand (the one on ours in the screen captures)."* A RIG artifact, not the moves; the fix is a rig change (the first pin after the round starts), not built. The 14z-165 reading ("Demitri's 5HP re-times P1's block") is CORRECTED for these first events on every carrier (the gate header, the 14z-165 gotcha, the 14z-165 banner and row (5), a new gotcha); the later events of those parts (re-pinned in-round, first differing at +16/+17 on node/cnt) stay unattributed |
| **(4) #136 — donovan_3's x ROW IS DOWNSTREAM OF KILLSHREAD SUMMON (ES)** | Demitri's x is not a compared field, so event 5 (Killshread Summon ES, frame 3840) reads IDENT, but at 3929 — one frame after the Summon's last hit, the class 0x16 contact of `killshread_es.txt` at +88 — native moves Demitri from 835 to 755 and ours leaves him at 835; Press of Death at 4080 then moves Donovan on native only. Capture FIRST (`cap_d3/donovan_3_killshread_summon_sheet.png`, 07:03:38Z); the maintainer read 3928-3970 as *"looks identical both sprites and GUI"*; a closer crop at 3900/4040 (`killshread_demitri_position_crop.png`) is with them. NOT concluded [CONCLUDED later this sitting: row (7)] |
| **(5) #136 — THE cnt FAMILY, A FIRST LOOK** | on donovan_2 event 2 and donovan_10 event 1 BOTH fighters' node counters fall one tick behind on ours at the same frame with every other field equal — an engine tick lost on ours, not a move; the candidates (a slowdown frame from our extra code, or the speed-level pattern's phase) are separable by the pass counter `$FF8081`; unmeasured |
| **(6) THE MID-SESSION COMMIT** | the tree at the commit: no shipped ROM byte moved, merged-m18 current. The eight doc gates, `tools/check_state_lists.py` and `tools/tickets.py check` green first (`test_tickets` red once on the stale generated page, regenerated); the three `audit_throw_registration` control modes run by hand, each exit 1 (`build/gates_14z167/throw_reg_ctl_*.log`); then the mid-session tier (`run_all_static.sh --strict --exec-controls none`, `build/gates_14z167/static_mid.log`, 09:18:24 to 09:26:49): **PASS 144 / SKIP 0 / FAIL 0 / MISSING 0**, `fired 161 / declared 161`, 15 deferred by cadence, 2 triggered (`test_emulator_runner` and `test_bbh_fidelity` <- `tests/ci_emulator.tsv`), no tracked file changed during the run; this row was written after the tier. Mid-session form: controls not executed, no push — the close runs them |
| **(7) #159 — KILLSHREAD SUMMON (ES): THE FACING RULE vsavj LACKS** | the maintainer, on the crop: *"This may or may not be relevant: anita moves on her own and I don't know the rules that move her, so there may be RNG at play. We'd be better off measuring Demitri's position relative to the background"*, then *"But looking at the background it does seems that VS2 has demitri going forward while our Demitri stays in place. Demitri going forward also explains why Anita changes orientation while seemingly being at the same coordinates"*. Measured (`build/x_family_14z167/`): Demitri's facing `+0x5D` splits at the Summon's FIRST contact (3882) — a non-debug write tap with writer PCs: both engines seed it from the attacker's `+0xB` (vs2 `0x17178`, vsavj `0x1884E`), then native's resolver takes the rule-5 branch `0x171E0` (1 on the outgoing wave, 0 on the returning one) and ours falls through vsavj's resolver `0x18854` — which tests rules 2, 3, 4 only — to `eor.b d0,$5d(a1)` at `0x1886C`, writing 1 XOR 5 = 4 at all six contacts. The pushback step routine (vs2 `0x27038`) runs its index-2 list (80 px / 20 f) on BOTH legs and signs each step by `+0x5D` (0 negated, nonzero added): native slides 835 -> 755 toward Donovan, ours holds at 835. Legacy control (static, `legacy_facing_census.txt`): rule 5 on 0 of 1,085 reachable vsavj legacy records, 1 of 1,143 on vs2 (a Lilith projectile); values above 5 are XORed raw by both resolvers. Rule-checker run `2026-09-18-42` (recommendation, file the ticket): plant caught; **Q1 Q2 Q4 Q5 OK, Q3 VIOLATED** — my claim excluded the step table on a counter sampled as a BYTE (`ff8964:b`) when `+0x164` is a WORD; true, resolved by WORK (re-traced as a word: 0 -> 20 on both legs; the step table IS the mover; `step_routine_and_trace.txt`). Filed **#159** (bug, severity: medium, needs-maintainer-ruling; no fix proposed). New gate **`tests/audit_facing_rule.sh`** (emulator tier): the two resolvers' compare chains read from the decrypted images, the facing writes 3850-3960 and Demitri's x at 3924/3946 on both legs, THE DEFECT FROZEN AS MEASURED in `tests/expected/facing_rule.tsv` (30 rows), frozen then VERIFIED, two must-fire controls (`rule5-resolved`, `branch-planted`) fired in-gate and both modes exit 1 (`build/gates_14z167/facing_*.log`); a write tap of P2's x (`$FF8810`) segfaulted MAME on both legs twice, so x is SAMPLED (`field_trace.lua`) and every run's exit status is asserted. Registered: `ci_emulator.tsv`, `gate_index.tsv` and the generated index, PROVENANCE, the must-fire census re-frozen (+1 declaring). Docs: `engine_internals.md` (the `+0xE` row, the step sign and the word counter), two gotchas (the byte-sampled word, the crashing hot-field tap). #159 commented with the gate; #136 commented with the x family's attributions |
| **CLOSE (2026-09-18, 10:31:34)** | the close tier under the #148 ruling — SESSION cadence, `--strict`, EVERY control executed — after every edit of this close but this row (`build/gates_14z167/static_close.log`, 10:10:58 to 10:31:34, wall 1,236 s: gates 522 s, controls 705 s): **PASS 145 / SKIP 0 / FAIL 0 / MISSING 0**, `fired 161 / declared 161`, `executed 161 honoured 161 lies 0 refused 0 died 0`, 14 deferred by cadence, 3 triggered (`test_emulator_runner` and `test_bbh_fidelity` <- `tests/ci_emulator.tsv`, `test_suite_dispatch_selftest` <- PROVENANCE), no tracked file changed during the run; this row was written after the tier. **The first close run was RED** (`static_close_red1.log`, 144 / 0 / 1): `test_decrypt_cache` §5 caught the new `audit_facing_rule.sh` decrypting directly instead of through `tests/lib/decrypt_cache.sh` (#69's rule: a short image refused, never read) — converted, the gate re-verified (`facing_verify2.log`, every row as frozen) and both its modes re-run (exit 1), the tier re-run in full. Every doc gate, `check_state_lists.py` and `tickets.py check` green. GitHub, all by `mechanyaa-ai`: **#158** filed (the checker's model binding), **#159** filed (the facing rule 5) and commented with its gate, **#136** commented with the x family's attributions. Rule-checker runs this sitting: `-37`..`-40` calibrations on Opus 5, `-41` (the unchecked re-freeze, resolved), `-42` (the #159 filing, resolved). Commits: `5d8627f1` (mid-session) and this close. **The close ends with a push** ([VSP-17]): the tier is green and nothing is red, skipped or pending |

## Session 14z-166 — **GitHub #136's METER FAMILY ROOT-CAUSED AND FILED AS #157: every tenant THROW pays the attacker the victim's flat 8 and the victim the throw record's meter, because the ported throw code's placed hit-registration stores still write vs2's displacements (the 14x rollback) and the vsavj hit stager reads the collision walk's leftover (P2, P1) — 28 of #136's DIFF rows, one mechanism, ON OUR SIDE. The 14x reason ("ported readers consume those vars") RETRACTED by measurement; the MAINTAINER'S LEGACY CONTROL (Demitri throwing Victor on pristine vsavj and on vsav2, +9 / +8 on both, maintainer-confirmed on the capture) excludes the engine-generation reading. New gate `tests/audit_throw_registration.sh`, the defect frozen AS MEASURED. Six rule-checker runs, every plant caught, every VIOLATED true and worked off; the seventh, on the final re-freeze, COULD NOT RUN (the account's spend limit) so that freeze is unchecked and nothing is built on it. The sitting OPENED by repairing the 14z-165 record, whose row (8) and CLOSE row had been committed and pushed as the literal placeholders `MIDCOMMIT_ROW` / `CLOSE_ROW`, and rolling the 14z-163 group. No shipped ROM byte moved; the tree stays at merged-m18.**

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, NEXT_SESSION), `main` == `origin/main` at `e01ae8de`, bbh clean at `caee272`, `emu/fbneo` dirty by exactly the two tracked patches (841/4), nothing running, `gh` as `mechanyaa-ai`; ROM audit **76/76**. The opener found the two placeholder rows at HEAD, pushed, with the tier behind them green in `build/gates_14z165/static_close.log`. The maintainer: *"update state.md, it's no good to carry stale/rotting information — then start on #136"* |
| **(1) THE 14z-165 RECORD REPAIRED** | both rows filled IN PLACE from the log's figures and marked as filled at 14z-166 (the entry was committed, so the correction is marked in the rows themselves per [VSP-17]); the 14z-163 group rolled to STATE_HISTORY with its ledger line. The mid-session tier: the row below |
| **(2) THE MID-SESSION COMMIT** | the tree at the commit: no shipped ROM byte moved, merged-m18 current; only STATE.md and STATE_HISTORY.md changed. `tools/check_state_lists.py`, `tools/tickets.py check`, `test_state_open_lists` and `test_docshape` green first; then the mid-session tier (`run_all_static.sh --strict --exec-controls none`, `build/gates_14z166/static_mid.log`, 23:28:04 to 23:36:24): **PASS 142 / SKIP 0 / FAIL 0 / MISSING 0**, `fired 159 / declared 159`, 17 deferred by cadence, none triggered, no tracked file changed during the run; this row was written after the tier. Mid-session form: controls not executed, no push — the close runs them |
| **(3) #136 — THE METER FAMILY ROOT-CAUSED: ONE MECHANISM, ON OUR SIDE** | the 108 DIFF rows censused by first field (47 cnt / 28 meter / 24 x / 7 node / 1 p1hp / 1 p2hp; only 19 are a part's first divergence, the rest `coupled=after`). The meter family (28 rows, 7 parts) is every THROW contact and its downstream. The 14z-164 traces already carried the signature — every differing meter step is a throw and ours pays the attacker exactly 8 (native 6 Corona Whip / 9 Donovan's and Phobos's throws / 12 / 18 Planet Burning). Static: the meter adder (vs2 `0x28D48`, vsavj `0x29A16`, reconciled) is called from ~140 per-character `moveq #N; jsr` sites per game — the SWING costs, identical on the placed twins — and each tenant's x028122 copy carries vs2's adder verbatim (its DF gate tests `+0x1C3`, vs2's field, where vsavj's tests `+0x111` — DF-only, unmeasured). Dynamic (`build/meter_probe_14z166/`, `meter_probe*.sh`, `read_part.py`, `census.py`, `disasm.txt`): a logging breakpoint at each engine's adder shows the generic hit stager (vs2 `0x172F2`, vsavj `0x18980`) calling it twice per throw — the record's `+0x14` for the fighter registered at `-0x4BC6(a5)` (vs2 `-0x4B74`), a flat 8 for the one at `-0x4BC4` — landing on (P1, P2) natively and (P2, P1) on ours. Non-debug taps on both pairs: native's throw code writes its pair at `0x289C6/CA` and `0x28A94/98` on the contact frame; ours' placed copies write vs2's displacements, `$FF348C-F` — on vsavj two words of a hit-value ring `PRG:0x0194AE` shifts (trigger unmeasured; only the boot RAM test read them in the three rigs; no ported instruction reads them, `census.txt` parts 4-6: all 24 placed sites are writes, the 4 engine sites are the ring shift, no absolute or lea form) — so the vsavj stager reads its own pair, written in play only by the per-frame collision walk (P1,P2) then (P2,P1). **That is the 14v fix's family, rolled back in 14x ("ported readers consume those vars") — measured now: it has NO ported reader; its price is every tenant throw's meter paid to the wrong fighters.** The consumer census 14x demanded: 34 vsavj engine sites (`census.txt` part 3), the meter stager and four damage-scaler attacker reads among them (their DAMAGE effect unmeasured — equal HP drops on every throw). Measured on pyron_3, huitzil_3 (Phobos's Sky Capture AIR throw included) and donovan_5; Pyron's air throw is not produced by the Demitri rig (an air normal on both legs) |
| **(4) THE FINDING THROUGH THE RULE-CHECKER, TWICE, AND A GATE FROZEN** | run `2026-09-17-30` (recommendation): plant caught; **VIOLATED Q1 Q4** — Galactic Throw quoted as measured flat-8 on this rig when the figure was the Victor-P2 trace's and the Demitri rig never throws it; Phobos's signature shown by no artifact; no control for a throw bypassing the placed store sites. True; resolved by WORK (huitzil_3 and donovan_5 measured under the same instruments) and re-run as `-31`: plant caught; **Q2-Q5 OK, Q1 VIOLATED** on the claim's wording alone — the first huitzil_3 event sentence had been read off the -debug probes, which disagree with the non-debug taps on that event on both legs ([VSP-130]); resolved in writing, not re-run. Then the persistent capture ([VSP-18]): `tests/audit_throw_registration.sh` — seven non-debug taps per part (both meters, both pairs, the dead pair), one reducer, `tests/expected/throw_registration.tsv` frozen (70 rows) and VERIFIED, THE DEFECT FROZEN AS MEASURED; two must-fire controls (`legs-swapped`, `dead-reader-planted`) fired in-gate and both modes exit 1; registered in `ci_emulator.tsv`, `gate_index.tsv`, PROVENANCE, the must-fire census re-frozen at 107. Its freeze went through the checker as run `2026-09-18-32` (expectation): plant caught, **VIOLATED Q1 Q2** — my new engine section contradicted the stale paragraph above it ("consumed by PORTED readers", a [VSP-13] miss), the deadness of vs2's displacements on vsavj had been measured dynamically only, and the meter conclusion is player-feelable with no capture before it. Resolved by WORK: the retraction grep marked every live carrier (engine_internals, the 2243 gotcha, patch_notes, huitzil.toml); `census.txt` part 4 swept every instruction naming vs2's displacements on the 6 MB build image — 24 placed sites, ALL writes, plus 4 vsavj-engine sites: **`PRG:0x0194AE`, a four-word hit-value ring shift `-0x4B76..-0x4B70(a5)`, the one vsavj reader of `$FF348C-F`**, which fired in none of the rigs (trigger unmeasured; parts 5-6: no direct caller, no absolute or lea form); captures taken (`captures/meter_sheet_pyron3.png`, native beside ours after Corona Whip and Planet Burning) and delivered to the maintainer BEFORE any GitHub posting, the order stated. Re-run as `-33`: plant caught, **Q3-Q5 OK, Q1 Q2 VIOLATED** — Q1 on four true points ("28 placed" was 24 + 4 engine; the caller search not an artifact; the d16(a5)-only sweep unnamed; the rigs' input fidelity unnamed), Q2 the order of record. Resolved in writing: the figure corrected everywhere, the caller search and the other-form sweep added as census parts 5-6 with the pointer-derived read NAMED as the one form no static sweep sees, and the input premise MEASURED — all six legs re-run under the non-debug guard read 0 INPUT-VIOLATION (`inputcheck/`), the violations were the -debug probes' alone. Not re-run a fourth time. Docs: `engine_internals.md` "THE HIT-REGISTRATION PAIR, MEASURED", `atlas/ram.md` `$FF343A`, one game gotcha, one project gotcha |
| **(5) #157 FILED, #136 TOLD** | `gh` as `mechanyaa-ai`: **#157** filed (bug, severity high, needs-maintainer-ruling) — the tenant throws' meter paid to the wrong fighters, the mechanism, the reproduction (`audit_throw_registration.sh`), what is not measured, and the FIX CANDIDATE for the maintainer to schedule: reconcile ONLY the three pair-store pairs (all three manifests' x028122 copies), leave the throw-state-byte clears, measure under the gate, `audit_tenant_throws`, the capture gates and a hand-played recording; its index row and the regenerated page. The #136 comment posted after the capture was delivered, naming the remaining families (the x rows, the cnt rows, huitzil_3's first event). No fix proposed, no ruling attributed ([VSP-10]) |
| **(6) THE MAINTAINER'S CONTROL — A LEGACY THROW ON BOTH GAMES** | the maintainer, on the second capture sheet (the frames where the two legs' meter values sit in different 16-unit bands, the stock crossing included — the first sheet's frames were pixel-identical, as they measured, because every pair fell in one band): *"the values are quite widly different, we really need that control you're doing with a legacy character"* — and before it: *"if legacy characters exhibit the same behaviour this is a nothing burger but if they don't that at least tells us what it is not"*. Demitri (0x01) throwing Victor (0x03) — the tenant-throw audit's all-legacy leg, `judge/02_throw.rpl`, level and RNG pinned — under the same taps on PRISTINE vsavj and on vsav2: both register the pair at their own throw site (vsavj `0x029694/98` at 3062, vs2 `0x0289C6/CA` at 3061) and pay the attacker **+9** (the record) and the victim **+8**. The host engine's rule IS vs2's and the legacy record carries the same value in both games: the tenant rows are a PORT DEFECT, the engine-generation reading is excluded, #157 stands. Folded into the gate as part `legacy_demitri` (legs `vsavj`/`native`), re-frozen and verified — the re-freeze's checker run and the tier: the rows below. The tier that was running (`static_mid3.log`) was STOPPED four minutes in to run this control at the maintainer's word, noted in its log, and re-run in full after the last edit |
| **(7) THE LEGACY CONTROL THROUGH THE CHECKER — TWO RUNS, BOTH TRUE, BOTH WORKED OFF** | run `2026-09-18-34` (the re-freeze with the control): plant caught, **Q2-Q4 OK, Q1 Q5 VIOLATED** — the forced-pick premise for Demitri had been stated as measured by a gate that never measured him, and the maintainer's request was paraphrased, not quoted. Resolved by WORK: the legacy part runs REAL cursor picks with no id poke (both decoded wheels put P1's default on Demitri 0x01 and P2's R,R on Victor 0x03 — `wheel_resolve.txt`), both id bytes TAPPED and frozen as an `ids` row, the maintainer's two sentences quoted verbatim in the gate header, the probe script, the engine section and row (6). A one-byte tap window logs nothing — the gate's VOID rule refused it (four runs), the windows are two bytes and the byte is read off the tap's mask. Run `-35`: plant caught, **Q3-Q5 OK, Q1 Q2 VIOLATED** — the reducer took the LAST write's delta on a contact frame (the one-write premise unmeasured), the ids row kept neither PC nor mask, and the legacy meter conclusion had no capture of its own. Resolved by WORK: the reducer sums every write on the frame and marks `/Nw` (re-frozen, verified: no contact frame carries more than one write), the ids rows carry the writer PC and read the id byte's mask only (`01@009008`/`03@020a80` on vsavj, `01@0077d6`/`03@01f64a` on vs2), and Demitri's throw was CAPTURED on both games (`captures_legacy/legacy_throw_sheet.png`, the hold and the pay, same pose, same frame, same bars) and delivered — after the numbers, as before. The final re-freeze: run `-36`, the row below |
| **(8) THE FINAL RE-FREEZE IS UNCHECKED — THE CHECKER COULD NOT RUN** | the reducer fix was re-frozen and verified (`throw_reg_freeze4.log` / `verify4.log`, both controls fired, both modes exit 1) and its packet prepared as run `2026-09-18-36`; **both reader agents terminated before reading anything, on the account's monthly SPEND LIMIT** (`rate_limit` HTTP 429, `req_011Cf9soGHWfQ2BfsRhaMnW3` / `req_011Cf9sogYHBQzFedgYLgor6`). There is no verdict, no plant result and NO ledger row — the run does not exist as evidence and is not counted as one (`tests/rulecheck/runs/2026-09-18-36/NOT-RUN.txt`). So the CURRENT freeze of `throw_registration.tsv` is unchecked under [VSP-183] while its four predecessors are checked (runs 32-35, every plant caught, every VIOLATED resolved by work); the gate is green, verified and controlled, and nothing is built on the freeze — re-prepare and read that packet before anything is. NEXT_SESSION carries it as the first item. **The maintainer CONFIRMED the legacy capture** (*"confirmed on my end"*). Also for the record: this session's own model changed mid-sitting (Fable 5.1 -> Opus 5); the readers that ran were on the default model throughout, but under the 2026-09-17 ruling a change of the CHECKER's model is a recalibration — check the reader model before trusting a fixture's calibration next sitting |
| **CLOSE (2026-09-18, 02:00:23)** | the close tier under the #148 ruling — SESSION cadence, `--strict`, EVERY control executed — after every edit of this close but this row (`build/gates_14z166/static_close.log`, 01:29:17 to 02:00:23, wall 1,866 s: gates 1,088 s, controls 764 s): **PASS 152 / SKIP 0 / FAIL 0 / MISSING 0**, `fired 162 / declared 162`, `executed 162 honoured 162 lies 0 refused 0 died 0`, 7 deferred by cadence, **10 triggered** (the six freeze-cadence build gates by `build/manifest/huitzil.toml`'s corrected comment, `test_emulator_runner` and `test_bbh_fidelity` by `ci_emulator.tsv`, `test_suite_dispatch_selftest` by PROVENANCE), no tracked file changed during the run; this row was written after the tier. Every doc gate, `check_state_lists.py` and `tickets.py check` green. GitHub, all by `mechanyaa-ai`: **#157** filed (the tenant throws' meter, with the mechanism, the reproduction and the fix candidate) and commented with the legacy control; **#136** commented with the family's root cause and what remains. Commits this sitting: `7593edf0` (the 14z-165 record repaired) and this close. **The close ends with a push** ([VSP-17]): the tier is green and nothing is red or skipped; **a document check AFTER the push** (the maintainer: *"can you check that the ritual is done documents-wise"*) found this group's BANNER still describing the sitting as it opened rather than what it found — the banner is what the next rollover composes THE LEDGER's line from, so it was rewritten to lead with the meter finding and committed separately, with the eight doc gates, `check_state_lists.py` and `tickets.py check` re-run green over the edit (the close tier itself is NOT re-run for a banner edit, and this row says so). The rest of the document ritual verified item by item: three session groups, the 14z-163 group and its ledger line in the archive, the 14z-165 opener archived verbatim, #157's index row and page, the new gate in the generated gate index, PROVENANCE and `ci_emulator.tsv` rows, HANDOFF carrying no stale carrier of the retracted claim and needing no row (a gate's home is the generated index), "Decisions pending" still empty ([VSP-182]: the #157 fix is a TICKET, not a pending decision); the one open item — the final re-freeze's unread checker packet (row 8) — is disclosed here and is NEXT_SESSION's first line, and nothing is built on that freeze |

## Session 14z-165 — **THE PARITY RIGS' P2 SWITCHED FROM VICTOR TO DEMITRI (the ruled 14z-164b P2), by his REAL cursor route `R` from P2's default cell 0x05 — the same route on the vsav2 AND the merged wheel, so the parity gate's two legs share the P2 lines verbatim. `tools/name_moves.py` carries `p2_id`/`p2_path` for the three naming tenants (the victim rigs untouched), `tools/move_parity.py` gained a `p2check` asserting P2 = 0x01 and NEVER in b:0x71/b:0x74 (the two chains `same_data_p2.tsv` marks as differing between the games) from each leg's own game image, and five expectations re-frozen on him: three `move_naming`, `projectile_census`, `killshread_es` and the 506 `move_parity_events`. The table moved 103 -> 108 DIFF: 13 former Victor-first rows turned IDENT, the huitzil_5/6 guard-cancel events turned DIFF (Demitri's 5HP re-times P1's block) [CORRECTED 14z-167: not their FIRST events, whose x DIFF is the round-start entrance drawn differently per leg — the rig's first X pin lands inside it; the later events' node/cnt DIFFs stand, unattributed]. No shipped ROM byte moved; the tree stays at merged-m18.**

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, NEXT_SESSION), `main` == `origin/main` at `a0f5a485`, bbh clean at `caee272`, `emu/fbneo` dirty by exactly the two tracked patches (841/4), nothing running, `gh` as `mechanyaa-ai`; ROM audit **76/76**. The maintainer: *"start as planned on the P2 switch"* (NEXT_SESSION's first item) |
| **(1) THE ROUTE MEASURED FIRST** | `tools/select_paths.py` on the three decoded wheels (vsav2, the merged build's, pristine vsavj): P2's default cell is `0x05` and Demitri is `0x01`, reached by a single `R` on **every** wheel, so the naming rigs' P2 prologue is one line (`1104-1106 p2=R`) identical on both legs — no per-leg P2 route, unlike P1. `select_paths --check` still passes (the 14z-160 P1/P2 paths resolve) |
| **(2) THE MECHANISM** | `tools/name_moves.py`: `TENANTS` gained `p2_id="01"`/`p2_path=("R",)` for donovan, huitzil, pyron; `prologue()` rewritten to emit each cursor's real route (P1 and P2), `P2_NEVER=("b:0x71","b:0x74")`, the schedule json carries a `p2` block, and `expect()` marks `WRONG-P2` from the trace's `p2id`. The **victim rigs are NOT parity rigs** and are unchanged — their P1 is Victor the ATTACKER (his moves are the events), their P2 the tenant. All 60 naming rigs regenerated; every gate's step-1 regeneration check passes |
| **(3) THE NEW ASSERTION — `move_parity.py p2check`** | maps P2's sampled node pointer (`RAM:$FF881C`) onto P2's own chain graph decoded from THAT leg's game image (`audit_same_data_p2.node_index` under the game's bank layout: the vs2 data view for native, the build's `verify_data.bin` for ours — Demitri is legacy and unmodified, so his data on the build equals pristine vsavj), asserts `p2id` (`RAM:$FF8B82`) = 0x01 at the first sampled frame, and FAILS on any frame in `b:0x71`/`b:0x74`. **The rule-checker (run 7) caught that a THIRD Demitri chain differs — `b:0x10`, the held-pose push box — so the assertion was split:** `b:0x71`/`b:0x74` are ATTACK records (they feed P2's compared HP) and stay a hard NEVER (measured: never entered); `b:0x10` is a POSE Demitri DOES enter (donovan_3 22 f, donovan_12 43 f), so `p2check --report` counts it and the parity gate asserts the count EQUAL on both legs — it is, so its pushbox datum is a bounded positional confound named for the family pass. Every leg: P2 = 0x01, 0 frames in the never-set, `b:0x10` held equally |
| **(4) FIVE EXPECTATIONS RE-FROZEN, EACH VERIFIED BY A SECOND RUN** | `move_naming_{donovan,huitzil,pyron}.txt` (freeze then verify, 202/153/98 lines — moved: donovan 1 line, huitzil 7, pyron 1, all where P2's timing feeds P1); `projectile_census.txt` (byte-identical to the Victor freeze but the per-event spawn ORDINAL, which is pool-occupancy-indexed); `killshread_es.txt` (17 lines, Demitri's defense — the waves unchanged, the +frames shifted); `move_parity_events.tsv` (506 events, ALL=1 freeze then verify). Logs in `build/gates_14z165/` |
| **(5) WHAT THE PARITY TABLE MOVED** | 377 IDENT / 103 DIFF (Victor) -> **372 IDENT / 108 DIFF** (Demitri), same 506 events, 25 NOT-IN-DF and 1 VOID unchanged. 43 events changed verdict/first/fields: **13 DIFF -> IDENT** (five of them the census's Victor-first rows — pyron_4 Cosmo Disruption held `+156`/`+167` p2hp, donovan_2 Blizzard Sword cnt, huitzil_8 Reflect Wall p1hp, pyron_5 Cosmo `+126` p2hp), **18 IDENT -> DIFF** (dominated by huitzil_5/6 Reflect Wall [CORRECTED 14z-167: the FIRST event of each is the round-start entrance, not the 5HP timing — STATE 14z-167 (3)]: the guard-cancel rigs time P1's block to P2's 5HP and Demitri's 5HP differs from Victor's in startup/range, so P1's own x and node diverge ours-vs-native), 12 DIFF -> DIFF (a different first frame/field). p2hp-first DIFF rows fell 7 -> 1; meter-first 33 -> 30. All frozen AS MEASURED — no family root-caused (the ruled order) |
| **(6) THE MUST-FIRE CONTROL REBUILT — a DATA-DEPENDENT CONTROL WENT DEAD** | the 14z-164 `pins-ignored` control hunted for a real part whose verdict rows MOVED with the pin exclusion off; that worked only because Victor's pinned HP frames disagreed between the games. Demitri's defense is the same on both games, so both legs agree on every pin frame and no real row moves — the control read DEAD. Rebuilt as a **perturbed-copy**: OUR trace with x altered +7 on exactly the rig's `$FF8410` X-pin frames must keep every verdict under the exclusion and move one without it (one function shared by the control section and the `CONTROL=pins-ignored` mode; the mode judges verdict columns only). All three modes exit 1 on their own finding; the gate's default set is green with all three FIRED. Two gotchas in `docs/project/gotchas.md` |
| **(7) THE RE-FREEZE THROUGH THE RULE-CHECKER — AND IT CAUGHT A REAL GAP** | the re-freeze is an `expectation` decision, so it went through `tools/rulecheck.py` before the commit (run `2026-09-17-29`, plant `forced-pick-14z159` blind in slot a): **plant CAUGHT**; the real verdict **VIOLATED Q4** — same_data_p2.tsv lists THREE differing Demitri chains and `p2check` asserted only two, leaving `b:0x10` (a pushbox difference that could shift P1's compared x) neither controlled nor named. **True finding.** Resolved by WORK (not by widening the never-set, which failed because Demitri enters the pose): the never/report split of row (3), the equality assertion, and `b:0x10` NAMED in the claim, the expectation header and a gotcha. Q1 OK, Q2 N-A, Q3 OK (every shared pin excluded, identity per leg), Q5 OK (the maintainer's own words quoted). `rulecheck resolve` records the Q4 fix; both verdicts verbatim in `tests/rulecheck/runs/2026-09-17-29/` |
| **(8) THE MID-SESSION COMMIT** | there was none — the sitting's one commit, `e01ae8de`, was the close, and its tier is the row below. **This row and the CLOSE row were committed and pushed as the literal placeholders `MIDCOMMIT_ROW` / `CLOSE_ROW` and were filled in at 14z-166 (2026-09-17) from the tier's own log at the maintainer's word (*"it's no good to carry stale/rotting information"*); every figure below is the log's, none is recollection** |
| **CLOSE (2026-09-17, 22:24:58)** | the close tier under the #148 ruling — SESSION cadence, `--strict`, EVERY control executed (`build/gates_14z165/static_close.log`, wall 1,238 s: gates 527 s, controls 704 s, the log written 22:24:58): **PASS 145 / SKIP 0 / FAIL 0 / MISSING 0**, `fired 161 / declared 161`, `executed 161 honoured 161 lies 0 refused 0 died 0`, 14 deferred by cadence, 3 triggered (`test_emulator_runner` and `test_bbh_fidelity` <- `tests/ci_emulator.tsv`, `test_suite_dispatch_selftest` <- `tests/expected/PROVENANCE.md`), no tracked file changed during the run. GitHub: nothing touched this sitting (the newest issue activity by `mechanyaa-ai` is 14z-164b's, #152 at 18:46 UTC). STATE: the 14z-162 group rolled to STATE_HISTORY; NEXT_SESSION archived and rewritten; the rule-checker ledger gained run 29. Commits this sitting: `e01ae8de` only. **The close ended with a push** ([VSP-17]): `main`, one commit — pushed with this row and row (8) still placeholders, the defect 14z-166 corrects |
| **TRAPS PAID** | a P2 whose data matches on both games turns a data-dependent must-fire control DEAD (perturb a copy of the real input on the frames the assertion protects); a matched-data P2 stops being a cross-generation confound but re-times every rig whose P1 input is cued off P2's attack, so a guard-cancel part re-freezes by construction; NARROWING a differing-data set needs each excluded member named harmless or the rule-checker finds it (run 7, Q4: `b:0x10` reaches a compared field, x, through pushbox contact where the two attack records do not, so it is REPORTED and asserted equal, not a hard never — Demitri enters the pose); `p2check` reads OURS against the build's own `verify_data.bin` because Demitri is legacy-unmodified there; a skip-then-continue loop over a single-line ruling deletes the NEXT line too (caught and restored) |

---

# STANDING SECTIONS (current state — never archived)
## Standing rulings

One line per ruling that still constrains work; the full entry lives where the line says. A line is deleted when its ruling stops constraining work (CLAUDE.md [VSP-17]).

- **#136 goes over again by the 14z-164 census (2026-09-17, maintainer: "I agree with the 3 propositions"):** the move-parity gate moves to PER-EVENT verdicts with the stock, the meter fraction and P2's HP compared and every "in DF" event asserted in DF; a P2 whose data is the same on both games (a second tenant, or a legacy character byte-identical between vsavj and vs2, found statically) — done 14z-165, Demitri; the 13 rows re-labelled by their fields on #136 before any family is root-caused. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-164)".
- **The rule-checker's Q3 and Q4 are BOUNDED, and a VIOLATED is resolved question by question in writing (2026-09-17, 14z-163, #152 — the maintainer: "both"):** Q3 counts only a shared thing the artifacts SHOW can write the compared state; Q4 reads OK when the claim itself names the uncovered mode; what a bounded question still finds stops the action until `rulecheck resolve` answers each violated question. Every fixture recalibrated on the new wording. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-163) — the rule-checker's questions".
- **The rule-checker runs on the default model (2026-09-17, 14z-163):** a change of model is a recalibration. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-163)"; the "#152 closes after a first real run" half of that line was met and the ticket closed by ruling 2026-09-17 (14z-164b).
- **#136 is gone over again IN FULL, not only through its faithfulness control (2026-09-16):** the maintainer insists it stays on the list until every part of it — the naming rigs, the pinning protocol, the 27 frozen verdicts, and the Pyron and Phobos legs above all — has been re-examined; no #136 verdict is built on until then. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-16 (14z-160)".
- **The M19 freeze is WITHDRAWN (2026-09-16):** #147's fix was a regression on a rig artifact; the tree is back at merged-m18 and #151 re-examines #136's forced-pick native legs. No freeze is current beyond merged-m18. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-16 (14z-159)".
- **The static tier runs by CADENCE (2026-09-17, #148 lever A):** `tests/ci_cadence.tsv` lists twelve build/content/tooling gates as `freeze` (the seven costliest plus the five 35-39 s gates, second cut) and five release/MiSTer gates as `release` (the three ≤2 s ones back to session); a session run (the default, mid-session AND at the close) defers them BY NAME unless a path they depend on changed; a freeze runs `--cadence freeze`, a release `--cadence release`, each on the commit it builds from. A new listing is a reviewed event. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-162) — #148 lever A".
- **Static-tier controls at the CLOSE (2026-09-14; REAFFIRMED 2026-09-17 after 14z-161 pushed on the mid-session form):** a mid-session commit runs `tests/run_all_static.sh --strict --exec-controls none` plus `CONTROL=<name>` for every gate it adds or changes; the session close runs every control (the default) and ONLY then pushes — not to be compromised until #148 is worked. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-14 (14z-154)" and "Ruled 2026-09-17 (14z-161b)".
- **The order of work (2026-09-17):** a stable, reliable state first, then #148 (the static tier's cost, done), then #152 (the adversarial rule-checker, done 14z-164b), then everything else — the #136 families included. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-161b)".
- **Release scope (2026-09-02):** at release every test whose SUBJECT is the released artifact runs, its legacy content included (a reference leg on vsav2 or pristine vsavj is not the subject); anything red or skipped is a hard fail unless approved at release time, so a release-scope gate fails loudly on a missing prerequisite. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **A red gate is a question (2026-09-02):** before fixing the gate, fixing what it caught or deleting it, establish which side's expectation rests on a measurement; a frozen number whose provenance cannot be named is a claim. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Cross-game comparisons at a matched speed level and a pinned RNG (2026-09-15, #135, #142; amends the 2026-09-02 #114 line):** vsav2 defaults P1 to TURBO and vsavj to NORMAL, and the two games' RNG states differ, so `tests/test_don_immortal_native.sh` pins `RAM:$FF8116` (levels 6 and 8) and `RAM:$FF80D4-D5` on both legs and asserts ours == native in hit count, damage and hit frames; LP's accepted "one hit short" is retracted as a mode artifact and #142 is closed invalid; the 2026-09-02 ruling that the vanilla vsav engine takes precedence stands. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-158)" and "Moved 2026-09-14 (14z-154)".
- **No static substituted-wheel gate (2026-09-02):** declined — the pattern matches 26 legitimate gates; the defence is [VSP-163] (assert `+0x60` against `bases.tsv`, or force the pick). Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Oboro's intro stays (2026-08-28):** never delay round start or cut the intro for a cosmetic reason. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Community data (2026-08-31):** measurement is king — a community source that matches ours exactly or by a constant offset validates our measurement, and an inconsistent pattern means re-check OUR measurement first. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Cosmetic is optional (2026-08-27):** cosmetic and single-player-only surfaces are nice-to-have; competitive 2P versus is the focus. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **No legacy character meets a tenant in 1P arcade (2026-08-28):** not a problem, 2P versus is the focus; not built, not scoped. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **The living-docs generalisation, when scheduled (2026-09-08, #119):** independent of bbh but compatible with it and on the same principles; the site generator in scope; fidelity required in principle, its form argued in the scope document; the open items first. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Linux release binaries (2026-09-13, #121):** self-containment is checked against an external host-provided list plus the ruled exceptions; the WSL2 Linux binaries are never published. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Generator refactor policy (2026-08-21, #50):** no dedicated refactor of `tools/gen_donovan_patch.py`'s `main()` is ever scheduled absent a new measured cost; a handler moves to module level only when a test needs to drive it, with its unit test in the same change, and byte-identity under `tests/test_m3a_reproducible.sh` and `tests/test_phasec_spaces.sh` is the refactor gate. Full entry: `STATE_HISTORY.md` "Session 14z-102 (post-freeze rulings)".
- **Don't assume; measure first; ask if necessary (2026-09-15, clarified 2026-09-16):** no factual claim — about code, a count, a behaviour or a name — is stated without a measurement or a tree source behind it; a question a measurement can settle is measured first and never published as "unreconciled"; asking the maintainer is right when a measurement cannot settle it or is not enough. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)" and "Ruled 2026-09-16 (14z-158b)".
- **Pipeline-image registry rows (2026-09-15, #96):** the M2 battery dispatches through `tests/expected/registry.tsv` on the build's fingerprint, never a pinned set name; `donovan-mN-stock` / `-stage4` register its two pipeline images (untagged, never shipped); each freeze rebuilds both and records whether they moved, and an unregistered fingerprint is a rule-6 stop. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)".
- **The merged set carries no tenant expectations (2026-09-15, #111; the 14z-133b B2 design):** the merged expectation set holds the legacy classes, the mask and the `.skip` markers; tenant-content `.sha1` expectations live in the solo sets, and the merged build's tenant content is covered by `audit_merged_legacy` leg (b) and the tenant gates. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)".

## STANDING PRINCIPLE (maintainer, 2026-08-05): vanilla wins ties

**[VSP-21]** "vsav vanilla is always better when we can." **When a console port and
arcade vsav differ and both would work, take vanilla.** A console port's
choice is not evidence that vanilla is wrong; it is evidence of what that
port's designers preferred.

This is a general rule, not a one-off: the PS1 capture is a reference for
what is POSSIBLE and for data we cannot otherwise obtain (cell placement,
the adjacency of NEW cells), not a style guide for content vsav already
defines. Paired with the maintainer's other statement — "as long as we can
select characters it's good" — the test is: does keeping vanilla still let
the feature work? If yes, keep vanilla.

Applied immediately, twice:
- **`Bishamon DL` and `Aulbath DR` stay vanilla** (Anakaris / Sasquatch).
  PS1 sets both to "no move"; neither is needed for reachability, so
  vanilla stands.
- **Horizontal wrap stays vanilla.** Vsav wraps left/right (cell `0x01`
  Left goes to `0x05`, measured and confirmed in-emulator); the PS1 report
  of "no wrapping" reflects untested extremes. We touch none of those
  cells, so nothing to decide.

Judgment applied under the same rule, open to veto: the three inbound edges
from `0x0B` (`D`/`DL`/`DR` into the new row) DO diverge from vanilla, and
strictly they are not required — Phobos and Donovan are already reachable
via `Bishamon D` and `Aulbath D`, and Pyron through them. They are kept
because without them, pressing Down on the cell directly above the new row
does nothing while three medallions are visible below it, which is the UX
failure "as long as we can select characters" is meant to exclude. Dropping
them would reduce the legacy footprint from 5 bytes to 2.

## Decisions pending (human)

Only undecided items live here; a ruling moves its entry to `DECISIONS_HISTORY.md` in the
commit that records it (CLAUDE.md [VSP-17]).

*None open.*

## THE DEADNESS REGISTER (opened 14z-71, maintainer's standing instruction)

**[VSP-23]** Every claim of the form **"legacy never reaches this, so we may reuse
it"**. Each is measured by ABSENCE, which is the weakest kind of evidence
we accept, so each is listed here with its guard. **These are the FIRST
PLACES TO CHECK for any unexplained regression in vanilla assets, engine
behaviour or rendering** — before anything else is suspected.

| Reused resource | Claim | Guard | Fallback if wrong |
|---|---|---|---|
| ~~palette-seq ids 0x1E-0x21 (`0x39ACC0`)~~ **CLAIM FALSE, ROW WITHDRAWN 14z-79 (they are Bulleta's DF block)** | vanilla only ever requests 0x26/0x27 | `tests/audit_palette_seq_ids.sh` (10,504 sampled calls) | none — the palette path never transits work RAM, so the audit is the ONLY guard |
| effect-class row 16 (`0x080AEC`) | vanilla never dispatches class 16 | `tests/audit_effect_class_rows.sh` §1, 0 reads vs a 1760-hit control | none needed: the row was a stub (`rts`), so a wrong claim costs at most the old no-op |
| ~~drawer list-type 6 (`0x01B6AA`)~~ **CLAIM FALSE (measured 14z-89) — LEGACY LISTS DO REACH TYPE 6** | vanilla has no type-6 sprite lists | `audit_effect_class_rows.sh` §1/§4 + `tests/test_beam_list_type6.sh` | **THE FALLBACK HELD — this is what a safe-and-loud design buys.** 14z-89 measured the tripwire ARMED on legacy content on huitzil-m13: `21_don_mash` 387 times and `26_don_arcade_mash` 948 times, PC-attributed to inside the thunk body (0x0FD060). Rendering stayed correct throughout (the fallback runs vsav's own type-6 code, reproduced instruction-for-instruction), so nothing rendered wrong and no playtest ever saw it — exactly the outcome the register's "prefer designs where being wrong is safe and loud" rule was written for. WHY IT WAS MISSED: the deadness measurement was sound but its COVERAGE was four replays (`02/07/09/30`), and the gate has always run on that default set; the two replays that arm it are long mash/arcade rigs nobody pointed it at. COST TODAY: `$FF010C/$FF010D` is a live work-RAM counter vanilla does not keep, so both replays diverge permanently from the vanilla masked basis — they are `.pending` on huitzil-m13 pending the maintainer's ruling. ~~OPEN: does the fallback need to stop counting (make the tripwire diagnostic-only / move it out of work RAM), or is the counter acceptable? See "Decisions pending — 14z-89"~~ **ANSWERED 14z-91: the counter was REMOVED** — the `beam_list_type6` `RAM:$FF010C` counter deleted, which cleared all six `.pending` legacy replays (`HANDOFF.md` Build registry, the 14z-91 row (C)) |

**[VSP-22]** Rules for adding a row: the claim must be measured with a POSITIVE CONTROL
on the same instrument and leg (a blind instrument and a real zero look
identical — paid for three times in 14z-71); it must name its guard; and
it must say what happens if the claim is wrong. Prefer designs where being
wrong is *safe and loud* over designs that are merely well-measured.

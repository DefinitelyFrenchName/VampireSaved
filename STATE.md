# STATE — living progress log

**SPLIT 2026-08-20 (14z-99 post-freeze close, maintainer-approved), AMENDED
2026-09-14 (CLAUDE.md [VSP-17] and [VSP-182]): this file holds the RECENT
session groups and the STANDING SECTIONS; the full detail of every older
session lives verbatim in `STATE_HISTORY.md`, under THE LEDGER at its head.**
How to work with it:
- **Lookup**: "STATE 14z-XX" references resolve here first, then in
  STATE_HISTORY.md (its LEDGER first) — section names are preserved verbatim
  in the archive. A reference to `STATE "Decisions pending"` for an entry no
  longer here resolves in `DECISIONS_HISTORY.md` (entries move there verbatim
  when ruled — since 2026-09-14; before that, once they stopped shaping work).
- **Claim-greps MUST include STATE_HISTORY.md** (the CLAUDE.md §5
  retraction-discipline command names it).
- **AN OPEN LIST HOLDS ONLY WHAT IS OPEN (maintainer-ruled 2026-09-14).**
  "Decisions pending" holds undecided items only; nothing closed stays in it,
  however it is marked (struck through, DONE, FIXED, CLOSED). Where each thing
  goes instead:
  - a RULED decision -> its whole entry moves VERBATIM to
    `DECISIONS_HISTORY.md` in the commit that records the ruling; while the
    ruling still constrains work, ONE line stating the rule sits under
    "Standing rulings" here, and the line is deleted when it stops;
  - a BUG, COSMETIC ITEM or EVOLUTION is a TICKET — listed in
    `docs/project/tickets.tsv`, its story on its GitHub issue, never listed
    here; a STATE entry for one that closes moves VERBATIM to
    `STATE_HISTORY.md` under the closing session's key (CLAUDE.md [VSP-182]:
    the four questions, the closing comment);
  - "STANDING PRINCIPLE" and "THE DEADNESS REGISTER" carry the [VSP-21..23]
    anchors and stay; the register keeps its withdrawn rows by [VSP-23].
  `tests/test_state_open_lists.sh` enforces it, with the size budget and the
  group count below.
- **ROLLOVER RULE (part of the session-close ritual)**: after writing the
  close entry, move session groups beyond the newest THREE to the TOP of
  STATE_HISTORY.md's body in newest-first order (below THE LEDGER, above the first
  archived `## Session` heading OLDER than the group)
  and add their one-line entries at the top of THE LEDGER there, composed from
  the group's own banner headers. If this file still exceeds ~150 KB, roll the
  oldest kept group early. The standing sections never roll.
  **THEN RE-POINT WHAT CITED THE ROLLED GROUP (added 14z-173, after it bit):**
  a rollover silently breaks every `STATE.md § <section>` citation into the group
  it moved, and `tests/test_tickets.sh` is what notices — at 14z-173 it went red on
  #167's `decided` link anchoring "THE CLOSE TIER", which had just left STATE.md for
  the archive. After moving a group, re-point those citations to
  `STATE_HISTORY.md § <section>` (the convention 20 rows already use) and regenerate
  the page with `python3 tools/tickets.py page`. The sweep is one command:
  `python3 -c "..."` over `docs/project/tickets.tsv` comparing every `STATE.md §`
  anchor against STATE.md's current text — do it for ALL rows, not just the one the
  gate stops at.
- **THE CLOSE CHECKLIST (ruled 2026-09-18, 14z-167b; `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-167b)")**, after the close entry
  and before the tier: (1) a FINDINGS TABLE — every finding of the group, its live home and the test that
  reproduces it; (2) `python3 tools/close_findings.py <session>` — every GAP homed, every REVIEW answered
  by a home or an open item — and, since 14z-181, `python3 tools/homes_tracked.py "(N) THE FINDINGS TABLE"`
  (every file the table names as a home or test is tracked; exit 0) and `python3 tools/retraction_grep.py
  tests/rulecheck/retractions/<key>.tsv` (the sitting's retracted wordings, its reach controls found) (never by reclassifying the finding), every PROMISE fulfilled or made an open
  item; its clean result is never cited as completeness; (3) a pointer on every earlier row the sitting
  resolves; (4) the promises (the tool's PROMISES section); (5) scratch scripts whose figures a document
  or STATE row quotes are promoted into `tools/` or `tests/` — ON TRIAL: record how many and how many
  were promoted; (6) the DOCUMENTATION PACKET through the rule-checker (`recommendation`, the table as
  the claim), its verdict resolved like any run.
- **THE LAST STEP OF THE CLOSE IS THE PUSH (maintainer-ruled 2026-09-10):**
  static tier strict green WITH every control executed (the default `all`), the doc checks green, **the process
  sweep CLEAN** (C0.3, #172 slice S2, since 2026-09-23: `python3 tools/agent/sweep.py`
  run AFTER the tier, which spawns processes of its own, exits 0 — every survivor
  killed leaves first, as its hint lists them, or declared in writing with
  `--declare PID "REASON"` and named in the close entry), **the procedure check (C1, #172
  slice S3, since 2026-09-23) passed or resolved** — `tools/agent/extract.py` over the session's
  transcript, a `procedure` run of `tools/rulecheck.py` on it, every VIOLATED question answered by
  `resolve`, and — ruled 2026-09-24 (14z-179, *"Standing step"*) — `python3 tools/agent/transcript_gaps.py --subagents <session> --cap` exit 0
  (every worker at most Opus-class, none fallen back); BOUND to the push by `tools/agent/hooks/pre_push.py` since 14z-176b, which refuses a
  push of this repository until such a run covers a commit in the pushed range — nothing pending -> `git push
  origin main`; anything red or skipped leaves the commits local and the
  close entry says so.

## Session 14z-183b — **#153 SLICE 1 BUILT AND CLOSED: the freeze/release static tier runs on a SNAPSHOT at a named commit, immune to the working tree (`tests/run_on_snapshot.sh`, proved by `tests/test_run_on_snapshot.sh` and four controls), its record naming the commit and every input; slice 2 (the emulator tier) is #181. A release question answered first.** A continuation after the 14z-183 close, same transcript (d33b9bfe), Opus 5.5. The maintainer: *"If I wanted to release MiSTer + MacOS, what would that take?"*, then *"I am not in a hurry to release and we have bugs to solve and before that, first I'd like to do #153 anyway. Could you start on #153?"*

| | |
|---|---|
| **(1) THE RELEASE QUESTION** | answered from the tree, nothing run for the release: `release/merged-m20/` is packaged for all three platforms (the freeze made it); a release still needs the release-cadence static tier, the full emulator tier at release scope (`tools/audit_lane_carry.py mister --since 1ae2d05d` read MUST RE-RUN, 22 subject paths moved, so the MiSTer lane cannot carry M19's green) and `tools/upload_release_assets.sh freeze/merged-m20 --prune`; Windows rides along by the every-platform rule (`release_format.md`); the `.rbf` is byte-identical to M19's, so the stock-control MRA needs no board run. No release was asked for |
| **(2) #153 — THE CENSUS** (my own runs; `build/agent183b/`) | the prior art read by a `reader` (only BBX clones the repo; bbh runs in place with a porcelain check); a `--shared` clone of `12571807` with nothing supplied read the freeze-cadence static tier **PASS 128 / SKIP 30 / FAIL 20**; the 50 non-passes' reasons all name inputs git does not carry — `build/` products (current tracks and older reference builds, several with no registry row), the submodules, the bbh and community siblings, the emulator run records. Two fingerprint readings in the census, NOT checked: the m5_stock19 and donovan6 build dirs both resolve to the registry row donovan-m24-stock, and the wide0 build dir resolves to vanilla vsavj |
| **(3) #153 — THE CONTROLS, and the rule-checker (runs 265-269, 269 OK)** | control A: the 50 gates one by one in the working tree, 50 PASS; control B: the same 50 in the clone with only out-of-git inputs supplied as copy-on-write copies, 48 PASS + 2 once three inputs the census missed (`build/don_m19`, `build/hui53` named in `build/manifest/shared_writes.toml`, and `../community`) were added; control C, asked for by run 266: the REAL runner in that clone, **PASS 177 / FAIL 1**, the FAIL (`test_emulator_staleness`, three gates stale since `d4cd4d51` — the three headers the 14z-183 close edited) failing the same way in the working tree with `VS_CADENCE=freeze`. The per-gate script had omitted the runner's `VS_CADENCE` and read that gate PASS — gotcha written. A copy-on-write snapshot of all of `build/`: 8.91 s real, 62,298 files, 9.1 GB. A first control-B run was VOID (my script's relative output dir) and is kept marked so |
| **(4) RULINGS** | the principle, before the design (verbatim in `DECISIONS_HISTORY.md`): *"being immune from any change in the tree while tests, especially long tests, run. Also, know against which commit we ran the tests"*; then *"Build slice 1 (Recommended)"*, *"Listing + fingerprints (Recommended)"*, and *"Close; new ticket for slice 2 (Recommended)"* — `DECISIONS_HISTORY.md` "Ruled 2026-09-26 (14z-183b)" (two entries). #181 opened as `mechanyaa-ai` |
| **(5) BUILT** | `tests/run_on_snapshot.sh` (the runner: re-exec from a private copy of itself, a plain clone at the commit verified whole, copy-on-write snapshot of `build/` with the commit's tracked `build/` files restored, each submodule at its gitlink with its module dir, the community and bbh siblings, ROMDIR verified at start and end, the record under `build/snapshot_runs/`, the snapshot removed); `tests/test_run_on_snapshot.sh` (ci_portable, ~5 s): a probe paused under the runner while a tracked file is edited and committed, `build/` output and a tracked `build/` file rewritten, a submodule file, both siblings and the runner's own file changed — nothing reaches it, the record is complete, and the same perturbation reaches an in-place run; four controls (`link-build`, `in-place`, `shared-bbh`, `no-self-copy`) each fire for the right reason in-gate and turn the gate red as modes. On the way, three dead controls were first reported as fired (their copies crashed; a leg-dir name collision and a clobbered shell variable), then made to count only a perturbation that REACHES the run. `docs/project/snapshot_runs.md` (new REFERENCE), HANDOFF's command list, the README route, the gate/description/must-fire inventories re-frozen for the two new scripts after review |
| **(6) THE REAL RUNS** | `tests/run_on_snapshot.sh -- tests/run_all_static.sh --cadence freeze --exec-controls none` on `12571807`: the first read PASS 177 / FAIL 1 and then the RUNNER died at a syntax error — I had edited its header under it (`sh` reads a script as it runs), the one input still read from the tree; fixed by the self-copy re-exec, the gate given the runner-file perturbation and the `no-self-copy` control. The second: **PASS 177 / FAIL 1** again, exit propagated, ROMDIR verified at end, record complete (`build/snapshot_runs/20260926T205306-12571807fae2/`), while STATE, DECISIONS_HISTORY and docs were being edited in the tree. The staleness red stands until the next freeze's `--stale` re-run (NEXT_SESSION) |
| **(7) ROLLOVER** | STATE rolled 14z-181 to the archive (the group count) |
| **(9) THE ROOT README, AT THE MAINTAINER'S ASK** | *"do you think you could update the root readme of the github repo, especially the \"how it was made section and its \"The scale\" subsection?"* — the scale re-derived by a `measurer` on a spec whose commands reproduce the 2026-09-15 figures at their commit `e09bef2e` (six exactly — 331, 154, 173, 184, 7, 140; commits 1,598 against the published 1,596; the session counter 156 against "more than 150") and then read today's tree (`build/agent183b/readme_scale_measurer.txt`): 1,731 commits, 2026-07-25 to 2026-09-26, session counter 183, 392 test scripts, 184 emulator-free checks (100 portable + 84 static), 254 controls executed at the 14z-183 close, 203 emulator/FPGA checks, 194 replays, 10 recordings, 181 tickets. "How that is checked" gains two bullets (the independent checkers and the procedure check; snapshot runs), and the stale "#136 is tracked separately" now says the comparison was done and its differences are tickets |
| **(8) THE FINDINGS TABLE** (checklist step 1: every finding, its live home, the test that reproduces it) | (a) a clone with nothing supplied reads the freeze-cadence static tier PASS 128 / SKIP 30 / FAIL 20, every non-pass an input git does not carry — home `docs/project/snapshot_runs.md` ("The census it rests on"); test: none, a census (the probe was a scratch run). (b) three of those inputs are named in data or beside the tree, not in the gates' text (the don_m19 build dir, the hui53 build dir via `build/manifest/shared_writes.toml`; ../community), so a curated input list would miss them — home `docs/project/snapshot_runs.md` (the same section: "which is why the whole of build/ is snapshotted"); test `tests/test_run_on_snapshot.sh` snapshots all of build/ and a sibling, it does not replay the miss. (c) a per-gate re-run is not the runner: mine omitted `VS_CADENCE` and read a freeze-cadence red as PASS — home `docs/project/gotchas.md` "A SCRIPT THAT RE-IMPLEMENTS THE RUNNER IS NOT THE RUNNER"; test: none, a discipline. (d) `sh` reads a script as it runs, so the runner itself was the one input still read from the tree — the first real run died after its tier — home `docs/project/snapshot_runs.md` (step 0) and `tests/run_on_snapshot.sh` (the self-copy re-exec); test `tests/test_run_on_snapshot.sh` (the runner file truncated mid-run; control `no-self-copy`) replays it. (e) a run on a snapshot is immune to a tracked edit and commit, build/, a tracked build/ file, a submodule, both siblings and its own file — home `docs/project/snapshot_runs.md` ("What is proved"); test `tests/test_run_on_snapshot.sh` replays it, with a positive in-place leg and four controls. (f) a copy-on-write snapshot of all of build/ costs 8.91 s real for 62,298 files, and a run about 50 s of setup — home `docs/project/snapshot_runs.md`; test: none, a measurement. (g) three in-gate controls first counted as fired because their copies crashed — home `docs/project/gotchas.md` "A CONTROL THAT \"FIRES\" BECAUSE ITS COPY CRASHED PROVES NOTHING"; test `tests/test_run_on_snapshot.sh` (a control fires only on a REACHED line; any other failure is DEAD). (h) the freeze-cadence static tier is red on `test_emulator_staleness` until the next freeze's `--stale` re-run (the three gate headers the 14z-183 close edited) — home `docs/NEXT_SESSION.md` (START HERE item 5); test `tests/test_emulator_staleness.sh` at freeze cadence replays it. (i) the MiSTer lane cannot carry M19's green into a release (`tools/audit_lane_carry.py mister --since 1ae2d05d`: MUST RE-RUN, 22 paths) — a session event, home this group's row (1); test `tools/audit_lane_carry.py` re-derives it. (j) the m5_stock19 build dir and the donovan6 build dir fingerprint to the same registry row and the wide0 build dir to vanilla vsavj — seen in the census, NOT checked — a session event, home this group's row (2), which states it; test: none. (k) a first control-B run was VOID — my re-run script took a relative output dir and then changed directory, so every gate "failed" on the script's own error; kept, marked VOID, and re-run — a session event, home this group's row (3), which records it; test: none. (l) the MiSTer bitstream of merged-m20 is byte-identical to merged-m19's (one sha1 over both), so by the once-per-new-bitstream rule the stock-control MRA needs no board run for an M20 release — home this group's row (1), which states it, and the rule's home `.claude/skills/mister-vampire-saved/SKILL.md` ([MSV-22]); test: none, a hash comparison. (m) the root README still said "#136 is tracked separately" after #136 closed (14z-181), and its scale figures dated from 2026-09-15; re-derived by commands that reproduce six of them exactly at their own commit (its commit count sits two below that commit's own, 1,596 against 1,598) — home `README.md` ("How it was made", corrected) and this group's row (9); test: none, a document. |
| **CLOSE (14z-183b, 2026-09-26) — THE CHECKLIST** | **(1) THE FINDINGS TABLE** — row (8), thirteen findings (a)-(m) ((k) and (l) added after the documentation packet's run 2026-09-25-272 found them missing, (m) after run -273), each test's reach classed in the packet header. **(2)** `python3 tools/close_findings.py 14z-183b` -> 0 addresses, GAPS none, REVIEW unavailable (no base: this group's heading is not in git yet), two PROMISE lines — both the staleness re-run, an open item in `docs/NEXT_SESSION.md` (START HERE item 5); `tools/homes_tracked.py "(8) THE FINDINGS TABLE"` exit 0 (its first runs FAILED on the new files, unstaged then, and on prose build paths — fixed); `tools/retraction_grep.py tests/rulecheck/retractions/14z-183b.tsv`: no tracked wording retracted (the plan's superseded forms lived only in its untracked draft), three reach controls, all found. **(3)** no earlier row is resolved by this sitting. **(4)** the promise is the staleness re-run, open in NEXT_SESSION. **(5) ON TRIAL** — one scratch script produced figures a STATE row quotes: the per-gate re-runner (controls A and B); NOT promoted — finding (c) is that it is not the runner; the census, the fingerprints and the timings are commands logged in the untracked census artifacts. **(6) THE DOCUMENTATION PACKET — rule-checker runs 270-274, 274 OK.** Runs 270-273 each VIOLATED on the real packet (every plant caught), every finding true and resolved by work: (j)'s home did not state it, and (b) quoted the runner for the gate its finding names (270); an excerpt no check compared and a packet no check covered (271); two findings missing (272: (k), (l)) and one more (273: (m), the README), and a heading count taken outside the checks (273). Four close-time checkers, each with a plant that fires, are kept with this close's artifacts: quote re-opening, one-class-per-finding, quoted-test-named-by-its-finding, and row coverage plus excerpt fidelity. **THE TIER**: `run_all_static.sh --strict --exec-controls all` at session cadence first read **NOT GREEN — PASS 167, FAIL 2**: `test_demand_after_trap` named `${VAR:?}` demands after the EXIT trap in both new scripts (on macOS bash 3.2 such an abort exits 0, so a dead runner reads PASS), and `test_bbh_fidelity`'s F9 differed on the same lines; rewritten as explicit tests, the gate and its four controls re-run, then the whole tier again: **GREEN — PASS 169, SKIP 0, FAIL 0; fired 234 / declared 234, executed 234 honoured 234**, no tracked file changed (`build/rc183b_tier_close2.log`; 15 gates deferred by the session cadence). **THE SWEEP**: one survivor, a Claude Code daemon-hosted session (`9e46f0f6`, `--agent claude`, pid 7346/7419) that the harness launched at 22:23:44 when this session was resumed as fork `038cb80d` — not started by any tool call here — DECLARED, not killed (killing a Claude session could end live work: the maintainer's to decide). **THE PROCEDURE CHECK (C1)**: `extract.py` over `d33b9bfe` from record 8988 (after 14z-183's three spans) and over the fork `038cb80d` from record 2395 (the harness resumed this session as a fork at 22:23; the fork's earlier records repeat `d33b9bfe` from the 14:13 compaction); procedure run `2026-09-25-275` (plant caught; recorded against the fork, where its readers were spawned) **VIOLATED on QP5, true, resolved in writing**: the prior-art `reader` added four interpretive notes to its verbatim quotes against its spec's "nothing else"; the design used only its quotes, and its one conclusion taken (bbh does not clone the repo) rests on its grep results. QP1-QP4 OK. **THE WORKER CAP**: `transcript_gaps.py --subagents <session> --cap` exit 0 on both — 0 breaches over 107 workers (`d33b9bfe`) and over 2 (`038cb80d`). Then the commit and the push of `main`. |

## Session 14z-183 — **M20 FROZEN (#157: the tenants' hit-registration pair stores reconciled — six words per tenant copy, a thrower now paid the record's meter and the damage scaler reading the attacker; *"Freeze M20 now"*), after a probe the maintainer played; ITEM 0b CLOSED (every #175 figure re-derived, `tools/trap_air_static.py`); #134, #138, #140 and #154 FIXED AND CLOSED; the freeze's battery surfaced four gates stale since 14z-181 and one pre-existing Phobos difference (#179, ruled *"Freeze, ticket it"*) and pinned the #112 gate to M19 by ruling. Rule-checker runs 225-250.** A plain session on Opus 5.5 (the ruled orchestrator), on the #172 setup. The maintainer at the opener: *"0b first then #157 #134 #138 #140 #154 in that order"*.

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, `docs/NEXT_SESSION.md` — asked for as `docs/project/next_session.md`, which does not exist), `main` 1 ahead of `origin/main` (`8835c4b3`, the 14z-182 carry note, local by design), `emu/fbneo` dirty by its tracked patches, `python3 tools/agent/sweep.py` **CLEAN**; an untracked `.claude/settings.json.` (trailing dot, same size as `settings.json`, dated 2026-09-23) noted, not touched |
| **(1) ITEM 0b — THE #175 FIGURES RE-DERIVED** (NEXT_SESSION 0b, procedure run `2026-09-25-224` QP1) | a `measurer` on a spec (return condensed in `build/agent183/ob_measurer_return.txt`): ROM audit 76/76; `build/out/vsav2_data.bin`, `build/out/vsavj_data.bin` and `build/m3b_merged27/verify_data.bin` each byte-identical to a fresh decrypt of the zip it is named for; **the dome's projectile records 5 and 6 = `(0, 0, 40, 6)`, class 0x52, on vs2 AND ours; Victor's vuln0-only family `03000003` = vuln0 `(0, 69, 32, 36)`, bottom 33, on all three images; Felicia's `b:0x0f` vuln0 = `(-3, 76, 17, 14)` on vs2 and `(-8, 70, 12, 8)` on vsavj and ours; her `a2:0x14#4` vuln1 bottom -6 on all three** — every figure `docs/game/engine_internals.md` quotes, reproduced. The static reader promoted as `tools/trap_air_static.py` (output byte-identical to the scratch script on all eight invocations), pointed to from `engine_internals.md` and HANDOFF's #175 instruments row |
| **(2) #157 — RULED: PROBE BUILD NOW** | asked before any byte moved (the 2026-09-18 "analysis first" ruling names #157); the maintainer chose *"Probe build now (Recommended)"* — `DECISIONS_HISTORY.md` "Ruled 2026-09-25 (14z-183) — #157's fix goes to a PROBE BUILD now" |
| **(3) #157 — THE SCOPE, MEASURED** | a `measurer` census over merged-m19's opcode view and pristine vsavj's (`vanilla_op.bin` == `build/out/vsavj_opcodes.bin`): each tenant's placed x028122 copy (Donovan `0xcf370`, Phobos `0x41ecb0`, Pyron `0x477f50`, `len 0xe00`) carries EXACTLY 3 vs2-form attacker stores (`3b4eb48c`), 3 victim stores (`3b49b48e`) and 2 state-byte clears (`422db4c3`) — 24 in all, the ticket's count; the vsavj-form stores and clears in merged-m19 are pristine vsavj's own, no tenant site among them |
| **(4) #157 — THE PROBE** | manifest COPIES `build/probe157/manifest/` (Donovan's six stage-99 pair-store rows flipped to stage 6, the two clr.b rows left at 99; the same six rows appended to Phobos's and Pyron's; the mark `M19` -> `P57` so the probe cannot be mistaken for the release), built by a scratch copy of `tools/build_merged.sh` differing only in `REPO` and `--port` into `build/probe157/merged` (log `build/agent183/build_probe157.log`, fingerprint `2dca438d`, unregistered by design). **A `measurer` diffed it against merged-m19: 18 program words differ — exactly the six displacement words per tenant copy (`b48c` -> `b43a`, `b48e` -> `b43c`) at the census addresses + 2 — and four members (`vm3j.04d`, `vsw.41` program; `vsw.33m`, `vsw.37m` the mark's glyphs)** |
| **(5) #157 — THE PROBE UNDER THE GATES (in progress)** | `audit_tenant_throws` PASS, `audit_pyron_capture_block` (EXPECT_MATCH=1) PASS, `audit_capture_matrix` PASS, `audit_don_grab_pose` exit 0 on the probe. `audit_throw_registration`: the tenant throw sites are now LIVE-pair writers (`0cfc14…`, `41f554…`, `4787f4…`) where merged-m19 has them as dead-pair writers — the fix does what it says at the store — and ours reads **0 contacts**, an INSTRUMENT ARTIFACT: the gate defines an ours contact as a throw-site write to the DEAD vs2 pair (its header, "WHAT IT FREEZES"), which the fix removes; merged-m19 alone reads its 8 frozen contacts on `donovan_5`. The gate's contact rule must change with the re-freeze (a later, deliberate step). The first run, five gates in parallel, printed four MAME segmentation faults in that gate's tap runs; re-run alone on both builds, none — load, recorded, not yet explained |
| **(6) #157 — THE PARITY GATE ON THE PROBE** (`audit_move_parity ALL=1`, the gate given an opt-in `GOT_OUT=<path>` that copies its computed table, verdicts unaffected) | merged-m19 re-run the same way **PASS** (the baseline: the gate reproduces the frozen 526-row table today). The probe (`build/agent183/p157_parity_got.tsv` vs `tests/expected/move_parity_events.tsv`): **36 rows' verdict columns moved — exactly the 36 frozen DIFF rows whose first field is `meter`; 34 became IDENT, 2 became DIFF on `x`** (`huitzil_3` 8 and 9, Phobos's Sitting Attack off throw); no other verdict moved. **Throw DAMAGE (the 14z-182 named check):** the only `p2hp` DIFF anywhere is `pyron_4` 13 ("5LP in DF", not a throw), in the frozen table and the probe's alike. **The two `x` rows are PRE-EXISTING, surfaced:** a scratch copy of the gate keeping its work dir, `huitzil_3` on both builds (`build/agent183/keep_{m19,p157}_h3/`), native legs byte-identical across the two runs — in events 8 and 9 our x and y differ from native on the SAME frames on merged-m19 and on the probe (x 179 / 35 frames from +116, y 39 from +118 in the raw traces), hidden until now behind the meter difference at +89 *[CORRECTED in row (13), this session: these figures were read from the kept native trace, which the gate's unpinned-level control had overwritten; against the pinned native leg the meter and P2 HP matched on every frame and x first differed at +156 — `docs/project/gotchas.md` "A KEPT PARITY WORK DIR HOLDS AN UNPINNED NATIVE TRACE"]* |
| **(6b) #157 — THE LEGACY A/B AND THE DAMAGE FINDING** | **Legacy:** all 88 suite replays run whole-RAM (no mask) on merged-m19 and on the probe, same pinned MAME (`build/agent183/p157_ab/summary.txt`): **86 byte-identical; the two that differ are Donovan tenant replays** (110 / 111, the extended-wheel arcade mash), first at f3138. **What differs there is DAMAGE:** a tap (`build/agent183/p157_110/tap_*.txt`) shows Donovan's copy of vs2's site 2 (`0cfce2/0cfce6`, vs2 `0x28A94/98`, inside the ported OBJECT-HIT applier `0x28A6A` — also the throws' damage path) write the pair, then vsavj's damage scaler read the attacker (`0x018B9C`, `0x018D28`): merged-m19 reads `0x8800` (P2, the victim — the dead pair left the collision walk's stale pair) and deals **4**, the probe reads `0x8400` (Donovan) and deals **6**. The scaler indexes `attack_table[class*32 + attacker +0x3B3]`; at that hit P1 Donovan's `+0x3B3` is 0x10 and P2 Q-Bee's (the CPU) 8 — both fighters get 0x10 at round start (`PC 0x02231E`), then P2's alone is rewritten every frame by `PC 0x00D46A` (7, then 8). **The separating experiment:** the parity gate (a scratch copy: `EXTRA_POKES` holding P2's `+0x3B3` at 8 on BOTH legs, traces kept, `build/agent183/stat8_{m19,p157}/`): **merged-m19 diverges from native vs2 first on P2's HP in 49 events — every tenant throw, Final Guardian Beta, Planet Burning (ES); the probe matches native on all 49** (46 IDENT, the two pre-existing `x` rows, `pyron_4` 13 as before) and its whole table equals its unpoked table (526 of 526 verdicts). So on M19 a tenant's throw and applier damage is scaled by the VICTIM's `+0x3B3`: invisible in 2P versus (both 0x10, which is why the parity corpus read equal damage), wrong against the CPU; the fix makes it native's. Native registers the attacker at that site (the frozen native writer rows of `tests/expected/throw_registration.tsv`: `0289c6,0289ca,028a94,028a98`) |
| **(7) #134 — TWO MECHANISMS, MEASURED** (probe scripts in the scratchpad; traces `build/agent183/p134/`, `p134n/`) | **(a) the five LP legs: the first event's walk-in is eaten by the round intro** — every walk-in set puts LP FIRST at f2600 with the walk from f2410, but P1's first walk step is f2546, so LP is pressed at 115-134 px where every later event is at 43-60 px (Sasquatch, Zabel, Lei-Lei traced); with the first event at f2800 (the naming rigs' own remedy, `audit_entrance_draw` / `audit_guard_reentry` / the attribution's step E) all five LP legs CONNECT. **(b) Lei-Lei's j.LK is a genuine whiff at this geometry** — pressed at 49 px, where her j.MP/j.MK/j.HK connect, it lands nothing, at f2600 and f2800 alike. *[CORRECTED in row (8), this session: a whiff at the default press (+14) only — the sweep found it connects at every offset +17..+32]* **Retracted: the jump legs are LEI-LEI's (`LE`, 0x0D), not Lilith's** — 14z-182's corrected record said Lilith; the ticket row fixed, the archived ruling marked in place. **The same artifact reaches `tests/expected/vanilla_normal_slots.tsv`:** its 15 `near LP` rows were pressed at mid range; with LP at near range (43-59 px) EIGHT characters (AU BU DE FE LE LI MO SA) enter `a2:0x00`, not the frozen `0x01` — the rig header's "their LP is 0x01 at BOTH distances" is the artifact; control: Sasquatch `near` at f2600 reproduces its frozen rows exactly. Sasquatch's HP is a knife edge (59 px `0x04`, 60 px `0x05`). Not built: the fix moves five gates' measurements (frame_join, meter_gain, rehit_ring, df_startup_invuln, aerial_join) — put to the maintainer first |
| **(8) #134 — FIXED IN THE RIG, RE-FROZEN, CLOSED (rule-checker 225-231, the last OK)** (the maintainer: *"Fix, and chase j.LK too"*) | `tools/vanilla_join_rig.py`: a walk-in set's first event at `FIRST_EVENT + WALKIN_SHIFT` (2800) with its HP pins from `t0 - 50`, and `PRESS_AT` (Lei-Lei `hit_jump` j.LK at +24 — first set at +18 on a four-point sweep, moved after runs 226-227, below) — the tracked rig regenerates the measured legs byte for byte; `test_rehit_ring.sh`'s JE control window read from the schedule (it was the literal 3560-3700; its IndexError was the first sign) — gate PASS, both modes FAIL. The five gates re-run (`build/agent183/p134g/`): `audit_df_startup_invuln` and `test_vanilla_aerial_join` PASS unchanged; `test_vanilla_frame_join`, `test_meter_gain` and `test_community_crosscheck` FROZEN then VERIFIED — `vanilla_normal_slots.tsv` 9 rows (eight near LP 0x01 -> 0x00, SA near HP 0x04 -> 0x05, the 59/60 px knife edge), the out-of-tree meter table (the six VOID legs now 1 hit, BOTH), the hit-damage table (ten 5LP rows 0 hits -> 1: the same artifact, unrecorded until now), the cross-check (no verdict moved; BU LE LI MO SA +1 compared row each); the pre-change tables regenerated at HEAD in a clean worktree, both gates PASS there (`build/agent183/p134_old/`). The rig header and the cross-check generator's paragraph corrected (the old "LP is 0x01 at both distances"; BU's HK and GA's MK had also been missing). **Rule-checker run `2026-09-25-225` (plant caught) VIOLATED on Q1 and Q2 — both true:** the claim called the rig the ONLY change and gave no rehit-ring log although the diff rewrites that gate's control, and said 124-134 px where Lei-Lei's LP was 115; and the hit/whiff conclusions rested on numbers with no capture. Resolved by work over SIX more runs, every plant caught, every real finding true: **226** — +18 sat one frame inside the band's edge (+15..+17 unmeasured) and the docstring overstated a four-point sweep; **227** — the band's edges +11..+13 and +32..+33 unmeasured; so every offset +10..+34 was measured (`build/agent183/p134_evidence/lk_sweep.txt`): j.LK connects +10, whiffs +11..+16, **connects every offset +17..+32**, whiffs +33..+34 — the press moved to **+24** (7 below, 8 above), the meter table re-frozen and verified from it; **228** — my description to the maintainer claimed the FIRST ATTACK banner in the +18 row (it marks the round's first hit, f2817, in both legs); the reader's row-3 banner was its own misreading (the frame's file sha1 is identical across two runs); the maintainer read a dense unlabelled 16-frame sheet statement by statement: *"yes"*, *"yes, but to be precise she is too high to hit and the jump does not result in a cross-up (i.e. she doesn't change sides). Adding the precisison because your description was ambiguous"*, *"yes"*; **229** — the out-of-tree diff had been written at +18: regenerated from the +24 tables, whose bodies (header lines excluded, what the gates hash) MATCH the frozen `.sha256`; **230** — the strip printed the trace's HP under each frame, so only the dense Lei-Lei sheet is independent of the trace: the claim narrowed to that. **231 read OK on all five questions.** The first sheet the maintainer saw had too few frames (*"I got too few frames to know because I have no frame with a reaction to being hit."*). `tools/rulecheck.py prepare` computed id 224, already taken (ledger rows one fewer than runs) — `--id` passed each time |
| **(9) #138 — FIXED AND CLOSED** | `tools/build_fingerprint.py --fronted`: a `;` rompath whose FIRST directory holds no `<set>.zip` is REFUSED (exit 3) instead of falling through to `$ROMDIR`'s vanilla row; `tests/run_suite.sh` and the battery's `m2a_masked_target` pass it (the battery fell through the same way — the ticket's open question, answered by reading `tests/lib/m2a_common.sh`). `tests/test_suite_dispatch.sh` 1c (the hazard reproduced without the flag, the refusal with it, a set-holding front unaffected, both dispatchers checked) with control `fronted-fallthrough` (fires; its mode FAILs); `tests/test_m2a_target_resolution.sh` 2b; [VSP-106]'s "NOTHING ENFORCES" note marked resolved |
| **(10) #140 — FIXED ON BOTH SIDES AND CLOSED** | `tests/lib/classify.sh` and the harness's `lib/sh/classify.sh` reset the controls reader's state at entry; ground truths `tests/test_static_runner.sh` 11b and the harness's `selftest/test_run_static.sh` 10b (the ticket's orders: fired 1 / declared 2, one none-declaring gate, the shell-crash label) — each FAILS on the unfixed classifier with the ticket's numbers (fired 3 / declared 5, none 2, the stale label); F1's fake repo gains `g_ffail` after a declaring gate, the re-baseline line written; `selftest/test_fidelity_vampire.sh` PASS (`build/agent183/bbh_fid_140.log`) |
| **(11) #154 — FIXED AND CLOSED** | `tests/audit_forced_pick_fidelity.sh`: the FREEZE branch refuses when section 2 is red (and under any control mode); proven with `FREEZE=1 CONTROL=wrong-cursor ROWS=huitzil` — `FAIL: section 2 is red — REFUSED to freeze`, the expectation's sha1 unchanged (`build/agent183/t154_freeze_red.log`). The class across the other FREEZE gates is #150's (open), not swept here |
| **(12) THE FOUR TICKETS CLOSED** | #134, #138, #140 and #154: each GitHub issue given a closing comment (the resolution, the local record) and closed as `mechanyaa-ai`; the ticket rows moved to `done` with their four answers; two new gotchas (`docs/project/gotchas.md` "A RIG'S FIRST EVENT CAN BE EATEN BY THE ROUND INTRO", "A FREEZE BRANCH THAT COPIES AND EXITS BEFORE ITS ASSERTIONS CAN FREEZE A RED RUN") and #140's marked FIXED; `python3 tools/tickets.py refresh` then `check` PASS (178 tickets). STATE rolled 14z-180 to the archive (the group budget) |
| **(13) #157 — THE MAINTAINER'S RECORDINGS, THE RULING, AND THE M20 FREEZE STARTED** | The maintainer played the probe (*"p157-probe-04 for Donovan / p157-probe-07 for Pyron / p157-probe-08 for Phobos"*, the other five deleted on their word) and *"Everything felt normal. However I didn't measure anything"*. Under `tools/run_inp_guarded.sh` all three play back on the probe to `END` with no exception; tapped on both builds (`build/agent183/p157_inp/`): every tenant throw pays the thrower the move's step and the victim 8 on the probe, the reverse on merged-m19, damage equal on every matched event; two throws pay the thrower 0 on both builds where comparable, a stock spend earlier and through the routine holding the site-3 stores, +0x111 never set — unexplained. **Ruled:** first *"Land it (Recommended)"* on an option I had worded wrongly (a manifest change cannot be committed green between freezes — the 14z-169 gotcha), corrected before any byte moved; then *"Freeze M20 now"* — `DECISIONS_HISTORY.md` "Ruled 2026-09-26 (14z-183) — #157 lands WITH an M20 freeze". Commit `5caff0df` landed today's other work first (harness `ca014d9`). The six rows entered the three tracked manifests (Donovan's stage 99 -> 6, Phobos's and Pyron's added; the clr.b rows stay parked) with the mark M19 -> M20 — equal to the probe's rows but for the `note` strings. The `build` rule-checker: runs **232** (VIOLATED Q1/Q4: the 49-row wording, the A/B method, replay 111 untraced, the members, the zero-meter throws, the x difference unshown), **233** (VIOLATED Q1/Q4: two empty quotes, the site-3 stores never shown writing, and a meter difference in the kept traces the gate had not reported) — **that last one exposed my own error: the kept native traces had been OVERWRITTEN by the gate's unpinned-level control**, so the "+82 native / +89 ours" timing I had reported to the maintainer was an artifact, RETRACTED; against the pinned native leg the probe's meter and P2 HP equal native's on every frame of Phobos's events 8/9 (gotcha "A KEPT PARITY WORK DIR HOLDS AN UNPINNED NATIVE TRACE"); run **234** reading. Next: build the five tracks and the stage-4 image from the tracked manifests, prove the program equals the probe's, then the freeze ritual (vampire-saved-port D.4) |
| **(14) THE M20 FREEZE — BUILT, CARRIED, BATTERY-RUN, RE-FROZEN** | Builds `build/don_m24`, `build/hui58`, `build/pyron43`, `build/m3b_merged28`, stock twin `build/m5_stock19`, stage-4 `build/rc183/stage4` (a `measurer`: six program words per solo track, 18 on merged, 0 against the probe; `build/rc183/deltas_measurer.txt`); registry rows donovan-m24 / huitzil-m31 / pyron-m25 / merged-m20 / donovan-m24-stock (stage-4 unchanged, donovan-m23-stage4 carries); `test_m3a_reproducible` and `test_phasec_spaces` re-pinned. **Suite:** the four WIDE sets carry M19's authored `.masked`/`.skip`/mask byte-identical, frozen then verified SUITE GREEN (PASS 69/69/70/69, SKIP 19/19/18/19); merged-m20's 16 self-frozen `.sha1` and their logs deleted (#111); the stock and stage-4 legs 14/14 through the battery's masked leg. **MiSTer tail:** fork commit `cf075a09` (pushed), series `0036`, pin bumped, MRAs regenerated (WIDE 31/31, STOCK CONTROL 22/22 parts), `release/merged-m20/` packaged; the release gates re-pointed and PASS. **The harness** re-pointed (bbh `1683592`, local), `test_bbh_fidelity` PASS. **Static re-freezes** (`reaction_classes.tsv`, `charmap_pages.sha256` and the three charmap pages): rule-checker 235-237, the last OK. **Battery pass 1:** 156 PASS / 9 FAIL, MiSTer romset lane 2/1; every red re-run on merged-m19 (`build/rc183/m19ab/`), dry-run frozen with the tracked file restored, and put through rule-checker runs **238-244** (every plant caught; six real VIOLATED, every finding true and resolved by work — the gates' printed diffs are `head -40` windows, so the corpus gates were frozen on BOTH builds and compared whole; the causes split into #157's, the 14z-181 corpus's and the build row's; every FOLLOWS input scanned; the throw gate verified with its controls against the new rows) — **244 OK**. Two rulings in the maintainer's words: the #112 gate *"Pin it to M19 (Recommended)"*, and Phobos's landing difference *"Freeze, ticket it (Recommended)"* (GitHub #179, class `LANDING-TURN-OPEN`) — `DECISIONS_HISTORY.md`. **Pass 2:** the nine MAME reds PASS against the re-frozen files, the full parity table (ALL=1) verifies. The #157 carriers in `engine_internals.md` and `gotchas.md` updated; three gotchas added (a rig change leaves the gates that copy it stale; a gate's printed diff is a window; the kept-parity-work-dir trace). The maintainer's three recordings tracked under `tests/inp/p157-probe-04/07/08`. **The freeze-cadence static tier** (`build/rc183/tier_freeze.log`): 178 PASS / 5 FAIL — `test_m3a_reproducible`'s member manifests and `test_manifest_merge`'s port_patch counts re-pinned to the measured M20 values (both PASS after); `test_freeze_tag_coverage`, `test_rule_checker` §5 and `test_emulator_staleness` clear only by the freeze's order (the tags, the `freeze` row, a `--stale` re-run on the committed tree). **The `freeze` rule-checker: runs 245-250** (every plant caught; five real VIOLATED, every finding true and resolved by work — the stock twin added to the word-level delta attribution; the M19 baselines fingerprinted to their pins and every compared image equal to a fresh decrypt of its zip; a member check asserting each pair's moved members against the design and M19's glyph checker generalized to the two re-authored glyphs, each with a plant; the stage-4 image rebuilt fresh; the solo sets' self-frozen `.sha1` compared to M19's, 49 of 51 identical, the two moved being Donovan's 110/111) — **250 OK** (`build/rc183/freeze/`). Next: the freeze commit, the tags, the `--stale` emulator re-run and the tier re-run on the committed tree |
| **(15) THE FINDINGS TABLE** (checklist step 1: every finding, its live home, the test that reproduces it) | (a) every #175 figure re-derived from the three data views — home `docs/game/engine_internals.md` (the #175 figures, pointing to the tool); test `tools/trap_air_static.py` (the reader, byte-identical to the scratch on all eight invocations). (b) #157's mechanism: the tenants' placed pair stores wrote vs2's dead pair, so the vsavj stager paid the pair reversed and the damage scaler read the victim's `+0x3B3` — home `docs/game/engine_internals.md` "THE HIT-REGISTRATION PAIR, MEASURED" (updated: LANDED, MEASURED) ; tests `tests/audit_throw_registration.sh` (the meter half AS FIXED — re-frozen on M20, it fails on a regression but does not replay M19's dead-pair writes) and `tests/audit_move_parity.sh` — the DAMAGE half (the scaler reading the victim's byte) is reproduced by no tracked test: the parity corpus runs 2P, where both bytes are 0x10, and the separating run (P2's byte held at 8) was a scratch copy of the gate, not promoted. (c) the M20 delta, six store words per tenant copy and nothing else — home `docs/project/patch_notes.md` 14z-183; test `tests/test_m3a_reproducible.sh` reproduces the images, not the "nothing else" (the word-by-word attribution was a one-freeze checker, not promoted). (d) #134: a walk-in rig's first event eaten by the round intro — home `docs/project/gotchas.md` "A RIG'S FIRST EVENT CAN BE EATEN BY THE ROUND INTRO"; tests `tests/test_meter_gain.sh`, `tests/test_vanilla_frame_join.sh` — both re-frozen on the FIXED rig: they fail on a regression, but the eaten first event itself was reproduced only by scratch probes. (e) Lei-Lei's j.LK connects at every press offset +17..+32 — home `tools/vanilla_join_rig.py` (PRESS_AT); test `tests/test_meter_gain.sh` reproduces the chosen +24 only — the band sweep was a scratch script, not promoted (item 5). (f) #138: a fronted rompath without its set fell through to the vanilla row — home `docs/project/gotchas.md` (ENFORCED AT BOTH DISPATCHERS SINCE 14z-183); test `tests/test_suite_dispatch.sh` (its 1c replays the hazard without the flag, then the refusal with it). (g) #140: the static runners re-added a previous gate's control counts — home `docs/project/gotchas.md` (FIXED 14z-183); test `tests/test_static_runner.sh` 11b — it runs the FIXED classifier on the ticket's scenario; its failing on the unfixed one was shown once, not kept. (h) #154: a FREEZE branch could freeze a red run — home `docs/project/gotchas.md` "A FREEZE BRANCH THAT COPIES AND EXITS BEFORE ITS ASSERTIONS"; test: none that runs the refusal — `tests/audit_forced_pick_fidelity.sh` carries the fix, and its red-run proof was a one-off log. (i) a kept parity work dir holds an unpinned native trace for its control part — home `docs/project/gotchas.md`; test: none, a discipline. (j) a rig or corpus change re-frozen in the gates it was made for left `audit_throw_registration` and three live audits stale since 14z-181 — home `docs/project/gotchas.md` "A RIG CHANGE RE-FROZEN IN THE GATES IT WAS MADE FOR"; test: none reproduces the past staleness (the gates were re-frozen); `tests/test_emulator_staleness.sh` catches the class going forward, now that a run of record exists. (k) a gate's printed diff is a `head -40` window — home `docs/project/gotchas.md` "A GATE'S PRINTED DIFF IS A WINDOW"; test: none, a discipline. (l) Phobos's Sitting Attack landing after a throw, displaced 31 px and not turning (GitHub #179, pre-existing) — home `tools/move_parity_attribution.py` (class LANDING-TURN-OPEN) and the ticket; test `tests/audit_move_parity_attribution.sh`. (m) the #112 recording plays out differently on M20 after Donovan's first pair store — home `tests/test_pod_black_foot_palette.sh` (pinned, header) and `DECISIONS_HISTORY.md`; test: none reproduces the M20 divergence (the pinned gate reproduces the M19 black foot). (n) test_mister_prg_window's counters moved M19 -> M20, unexplained — home `tests/expect/mister_prg_window.txt` (the note); test `tests/test_mister_prg_window.sh`. (o) two stock-spend throws pay the thrower 0 on both M19 and M20, and the third store pair was never observed writing — home `docs/project/patch_notes.md` 14z-183 ("Not explained") and ticket #180; test: none yet, the recordings `tests/inp/p157-probe-04/p157-probe-04.inp`, `tests/inp/p157-probe-07/p157-probe-07.inp` and `tests/inp/p157-probe-08/p157-probe-08.inp` reproduce the first. (p) the solo sets' self-frozen `.sha1` moved only for Donovan's 110/111 — home `docs/project/patch_notes.md` 14z-183; test: none, a one-off comparison at the freeze (`tests/run_suite.sh` checks the current sets only). (q) a standing-ruling line ("No freeze is current beyond merged-m18") had outlived M19 — home `DECISIONS_HISTORY.md` "Ruled 2026-09-16 (14z-159)" (the ruling the line summarised); the finding itself — the line outlived the M19 freeze and was deleted in the M20 commit `d4cd4d51` — is a session event and lives in this row, which rolls verbatim to STATE_HISTORY; test: none, a document. (r) #134's two jump legs are Lei-Lei's (`LE`, 0x0D), not Lilith's as 14z-182's record said — home `docs/project/tickets.tsv` (#134's row) and the in-place correction in `DECISIONS_HISTORY.md`; test `tests/test_meter_gain.sh` runs the current, correctly named Lei-Lei legs — the misnaming was a document error no test can replay. (s) `tools/rulecheck.py prepare` computes an id already taken, so every prepare passes `--id` — a known defect, #160; home `docs/project/rule_checker.md` ("every later `prepare` takes `--id`"); test: none. (t) four MAME segmentation faults in `audit_throw_registration`'s tap runs with five gates in parallel, none when re-run alone on both builds — NOT EXPLAINED (whether they are the known TEARDOWN class was not checked); home: this session's row (5) records the instance, `docs/project/gotchas.md` (the teardown `Segmentation fault: 11` paragraph) the class it may belong to; test: the gate's VOID rule (`tests/audit_throw_registration.sh`: a tap log without END is refused, never read), so a mid-run fault cannot pass as evidence — it does not reproduce the fault. (u) `test_rehit_ring.sh`'s JE control window was a literal (3560-3700) that the #134 rig move invalidated (its IndexError the first sign) — home `tests/test_rehit_ring.sh` (the comment on the window, now read from the rig's own schedule); test `tests/test_rehit_ring.sh` runs the window as now read from the schedule, not the broken literal. (v) the rig header's slot list had also omitted BU's HK and GA's MK, which the table already carried — home `tools/vanilla_join_rig.py` (the CORRECTED 14z-183 paragraph); test: none, a comment (the table `tests/test_vanilla_frame_join.sh` checks already carried both slots). (w) three of #134's capture sheets were weaker than they looked — too few frames to show a reaction, a strip labelled with the trace's own HP, a description claiming a banner that marks the round's first hit — home `docs/project/gotchas.md` "A CAPTURE LABELLED WITH THE TRACE'S VALUES IS NOT INDEPENDENT OF THE TRACE"; test: none, a discipline. (x) `audit_throw_registration` defined an ours contact as a write to the DEAD vs2 pair, so on the fix it read 0 contacts — an instrument artifact, not a lost throw; the rule now counts the leg's LIVE-pair writes (plus the dead pair on ours) — home `tests/audit_throw_registration.sh` (the header's contact rows); test `tests/audit_throw_registration.sh` reproduces the NEW rule's contacts on M20, not the old rule's 0-contact reading (no control plants it). (y) my first #157 question offered *"Land it (Recommended)"*, a manifest change between freezes that the 14z-169 gotcha forbids; corrected before any byte moved — home `DECISIONS_HISTORY.md` "Ruled 2026-09-26 (14z-183) — #157 lands WITH an M20 freeze, now" ("That option was WRONG as offered") and the gotcha "A MANIFEST FIX CANNOT BE COMMITTED GREEN BETWEEN FREEZES"; test `tests/test_m3a_reproducible.sh` (the red it would have turned). (z) an untracked file named settings.json. (trailing dot) under .claude/ found at the opener — the maintainer's configuration, not touched; home this session's "opened with" row; test: none. (aa) `audit_move_parity`'s header still said 506 events over 30 parts; the frozen table has 526 rows over 32 parts since 14z-181 added `donovan_15` and `pyron_7` (found by the documentation packet's run 2026-09-25-259) — home `tests/audit_move_parity.sh` (the header, corrected, with `docs/project/gate_coverage.md` regenerated from it); the same stale 30 corrected in `tests/audit_df_field_readers_live.sh` (its frozen `tests/expected/df_field_readers_live.tsv` reads 32 runs since the M20 re-freeze), `tests/audit_reaction_class_live.sh` and two `tests/expected/PROVENANCE.md` rows; `docs/game/engine_internals.md`'s "#136 corpus (30 parts" is a measurement dated 14z-168 and stays; test: none, a comment (the gates compare their tables whole, never the counts). |
| **CLOSE (14z-183, 2026-09-26) — THE CHECKLIST** | **(1) THE FINDINGS TABLE** — row (15), twenty-seven findings (a)-(aa) ((r)-(t) added after the documentation packet's run 2026-09-25-254 found them missing, (u)-(w) after run -255, (x)-(z) from my own full re-read of rows (1)-(14) after -255, and (aa) from run -259). **(2)** `python3 tools/close_findings.py 14z-183` -> `build/agent183/close_findings_14z183.txt`: 9 addresses checked; its first run named three REVIEW addresses (`0x018B9C`, `0x018D28`, `0x28A6A`, homed only by older text) and one GAP (`0x02231E`, NO HOME) — the GAP was MISSED by my own filter and reported clean to the documentation packet's first run (2026-09-25-251 Q1, true); all four now homed in `engine_internals.md`'s MEASURED line, re-run: GAPS none, REVIEW none, PROMISES none — never cited as completeness; `tools/homes_tracked.py "(15) THE FINDINGS TABLE"` exit 0 (its first run FAILED one token, the three recordings written as one path — split); `tools/retraction_grep.py tests/rulecheck/retractions/14z-183.tsv` -> `build/agent183/retraction_greps_14z183.txt`: its patterns grew over the documentation packet's runs (8 retracted at first; Lilith after -254, two #134 wordings after -255, the two (aa) counts after -259, row (6)'s unpinned figures after -260 — the file, not this row, holds the current list and the artifact the current counts) (two at 0 hits; every other hit a quoted retraction, a struck line, a dated archive record or the untracked generated site — classed by hand in the artifact) and four reach controls, all found. **(3)** row (13)'s "next" is row (14); row (5)'s "the gate's contact rule must change with the re-freeze" is done in row (14). **(4)** no promise open. **(5) ON TRIAL** — fourteen scratch scripts produced figures a document or STATE row quotes; ONE promoted (`ob_boxes.py` -> `tools/trap_air_static.py`, row (1)); not promoted: the freeze-time checkers `delta_attr.py`, `members_check.py`, `gfx_mark_check.py` (one-freeze attributions, kept under `build/rc183/freeze/` with their plants), the probe-era census/delta/A-B/stat scripts (#157's figures, now reproduced by the tracked gates re-frozen on M20), the recording tap/reduce pair (the recordings are tracked; no gate reads their meter yet — finding (o)), the #134 sweep scripts (the chosen offset lives in `tools/vanilla_join_rig.py`), the kept-trace copy of the parity gate and the #112 divergence taps. **(6) THE DOCUMENTATION PACKET — rule-checker runs 251-261, 261 OK.** Runs 251-260 each VIOLATED on the real packet (every plant caught), every real finding true and resolved by work: the GAP my filter missed (251); findings (r)-(t) (254), (u)-(w) (255) and (aa) (259, a stale gate-header count, corrected in three gate headers and two PROVENANCE rows) added, (x)-(z) from my own full re-read; row (7)'s and row (6)'s superseded statements marked in place (255, 260); each test's reach stated per finding (256-259); the counts this row once restated replaced by pointers (260). Three close-time checkers were written for it, each with a plant that fires — `packet_verify.py` (every quote re-opened), `class_letters.py` (every finding in exactly one test class), `classing_cover.py` (every retracted hit classed) — kept under the close's artifact dir with their outputs, NOT promoted (ON TRIAL, item 5: they check this close's packet shape; a candidate for `tools/` beside `homes_tracked.py`, carried in `docs/NEXT_SESSION.md`). **THE TIER**: `ROMDIR=... tests/run_all_static.sh --strict --exec-controls all` at session cadence **GREEN — PASS 182, SKIP 0, FAIL 0; fired 254 / declared 254, executed 254 honoured 254, lies 0**, no tracked file changed during the run (one freeze-cadence gate deferred by cadence, `test_reconcile_matcher`; `build/rc183/tier_close.log`). **THE SWEEP**: `python3 tools/agent/sweep.py` **CLEAN: no survivor**. **THE CLOSE COMMIT** `1a20119e`. **THE PROCEDURE CHECK (C1)**: this session's extract ran 1.74M characters, three times the largest checked before, so it was cut into three spans of about 580k (records 5-3570 with 14z-182's carried tail from 39efeec2 record 2821, 3571-6753, 6754 to the cut), each its own `procedure` run — `2026-09-25-262`, `-263`, `-264`, every plant caught, all three VIOLATED and every finding resolved in writing: **true** — the opener statement promised every quoted figure from a worker spec and the agent then quoted figures from its own tool runs to the maintainer without restating the scope (262 QP1); a measurer ran one unlisted `shasum` and wrote a summary line, and the 0b measurer added two notes, none feeding a quoted figure (262 QP5); the statement at [7314] that close_findings read no GAP, when its output held `0x02231E` behind the agent's filter (264 QP2/QP3, the miss step (2) already records); the readers of runs 255-261 read `docs/project/gotchas.md` in slices, so run 261's OK rests on a partial read of that artifact, whose one load-bearing quote `packet_verify.py` re-opened (264 QP5); **true in form, immaterial** — plant-leg workers restated 2550/2560 (FIRST_EVENT 2600 - 50 / - 40, `tools/name_moves.py`) handed by the fixture (262/263 QP3); **false** — the `Exit code 1` at [7350] was a trailing `grep -c` counting zero misses (264 QP2). **THE WORKER CAP**: `transcript_gaps.py --subagents d33b9bfe --cap` exit 0 — 0 breaches over 85 workers (80 rule-checker, 5 measurer), no fallback. Then the push of `main` and bbh `1683592`. |

## Session 14z-182 — **THREE RULINGS AT THE OPENER: the parity gates' per-frame pins are their equalised input (#176 opened for the RNG's advance), #161 closed as `not-ours` (vsavj's own damage pipeline, the attribution class renamed `DMG-VSAVJ`), and #132 closed as `declined` (merged-m15 stays unpackaged).** A plain session on Opus 5.5 (the ruled orchestrator). The maintainer: *"agreed"* to both pending recommendations, then *"Can we rule on #132 before any other ticket?"*

| | |
|---|---|
| opened with | the opener read (HANDOFF, STATE, `docs/NEXT_SESSION.md` — asked for as `docs/project/next_session.md`), `main` 4 ahead of `origin/main` at `fb5c2bc7` (the carried post-close commits), `emu/fbneo` dirty by its tracked patches; **`python3 tools/agent/sweep.py` NOT CLEAN (exit 1): ONE survivor, PID 85150 — Claude Code's own `claude.exe daemon run` with its `bg-pty-host` / `bg-spare` children, started 11:19 from the home directory, not a project job; reported to the maintainer, not killed** |
| **(1) THE PARITY PINS, RULED** | the maintainer: *"agreed"* to option (a) — the pending entry moved verbatim to `DECISIONS_HISTORY.md` "Ruled 2026-09-25 (14z-182) — the parity gates' per-frame level and RNG pins", a standing line here; **GitHub #176** opened for the RNG-advance comparison (row in `tickets.tsv`) |
| **(2) #161 CLOSED `not-ours`** | the maintainer: *"agreed"* to option (a) — entry moved verbatim; the attribution classes `PHOBOS-DMG-OPEN` / `DMG-OPEN` renamed to the ruled `DMG-VSAVJ` in `tools/move_parity_attribution.py` and the frozen `move_parity_attribution.tsv` (12 lines, the class and its evidence text only); the live carriers corrected by claim-grep (`defense_rows.md`, the gotcha, HANDOFF's M19 line, `audit_phobos_dmg_residual.sh`'s header and freeze writer + the frozen file's comment line, `ci_emulator.tsv`, PROVENANCE, the two generated gate pages); `patch_notes.md` (a LOG) keeps its M19 wording as shipped. Attribution gate re-run by a `measurer` on MAME (`build/agent182/attribution_14z182.log`): **PASS** — 64 roots, 100 rows, 6 `DMG-VSAVJ`, every row as frozen, `no-ablation` FIRED; the ROM audit 76/76 before it (`build/agent182/audit_roms_14z182.log`). #161 closed on GitHub with its comment; #164's `learned` anchor re-pointed to the renamed heading (`tools/tickets.py check` caught it) |
| **(3) #132 CLOSED `declined`** | measured first (a `measurer`: `release/` holds merged-m6..m14 and m16..m19, all nineteen `freeze/merged-*` tags exist, GitHub releases exist for m18 and m19 only — `build/agent182/t132_measurer_return.txt`); the 14z-134 opener had recorded it *"superseded before release, recorded, not owed"* but it was never ruled. Rule-checker run `2026-09-25-215` (plant caught) VIOLATED Q1/Q4 — true: I had tied `build/m3b_merged22` to merged-m15 on a program fingerprint merged-m16 shares — resolved by resting the way back on the tag alone. The maintainer chose *"Close as declined (Recommended)"*; `DECISIONS_HISTORY.md` "Ruled 2026-09-25 (14z-182) — #132"; closed on GitHub with its comment |
| **(4) #136'S WHOLE SCOPE RE-DERIVED — the maintainer's question: *"I never had the confirmation that now all the scope had been covered, can you tell me more?"*, then *"yes!"*** | 14z-181 had closed #136 against its LAST list. #136's body and all 11 comments dumped (`build/agent182/issue136_thread.md`), every scope item quoted by a `reader`, mapped to a gate or ticket with two more readers and three measurers (`build/agent182/t136_*.txt`, the map `t136_scope_map.md`); rule-checker runs `2026-09-25-216` to `-218`, every plant caught, every real finding true and fixed: 216 — the scaler 'no ticket' from a keyword search alone, the air gate misdescribed, a movement list called complete, two rulings unquoted; 217 — the scope return abbreviated, two ticket statuses unsourced, the surfaced-throw rows (donovan_8:3, pyron_3:0) unmapped; 218 — eight files cleared on a paraphrase, the attribution run unshown, and **Hop Kick in DF connecting on neither leg of either rig** (a SAME on a whiff). Everything maps except FOUR findings, ruled *"AGREEED!"*: **#177** (movement parity — no ours-vs-native movement gate for any tenant), **#178** (Hop Kick in Donovan's DF never compared), the throw-damage scaler question a named check on **#157**'s fix (commented; `engine_internals.md` points to it), and `coverage_matrix.md`'s row corrected (it read COVERED over three already-covered items). #136 stays closed, a comment there records it; `DECISIONS_HISTORY.md` "Ruled 2026-09-25 (14z-182) — #136's full-scope re-derivation"; 14z-181's (11b) marked in place |
| **(5) #134, #138, #140, #154 — STATUS ESTABLISHED** (the maintainer: *"they may be still relevant or not but we need to establish that"*) | two readers quoted the current code, a measurer searched the git history since each filing, a measurer counted the out-of-tree meter table: **#138** (`run_suite.sh` still resolves the fingerprint through the search path with no fronted-zip check), **#140** (`classify.sh` still returns before the controls reader on a non-PASS gate, `run_all_static.sh` still adds the stale counts), **#154** (the freeze branch still exits 0 before any failure test) — all still present as filed, kept open; **#134**: 6 VOID legs of 49 (5 LP, 1 LK), reported and never counted, no port effect — kept open with its record corrected (*"yes"*): title, row, a GitHub comment, `DECISIONS_HISTORY.md` "Ruled 2026-09-25 (14z-182) — #134 kept open" |
| **(6) THE SWEEP SURVIVOR, MEASURED READ-ONLY** (the maintainer: kill only on their command, after establishing no adverse effect should Claude crash) | PID 85150 (`claude.exe daemon run --origin transient`, started 11:19:35) was spawned by PID 72141, a claude process of THIS repository that no longer exists, and holds the rendezvous socket `rv/152495c2.sock` — 14z-181's second transcript: a LEFTOVER of 14z-181. This session's claude (PID 17452, 17:50:01) descends from Terminal → login → zsh, not from the daemon, and holds 0 sockets naming it (`lsof -p 17452`); the daemon's `pty/` (active background sessions) is EMPTY, its four children are two idle pre-warmed `bg-spare` slots and their `bg-pty-host`s; the Claude desktop app (PID 71476, since 2026-09-24 23:38) is a separate tree. Not killed — awaiting the maintainer's command |
| **(7) #175 CLOSED — THE PLASMA TRAP HITS AN AIRBORNE VICTIM, IDENTICALLY ON BOTH GAMES** (the maintainer: *"#175"*, *"agreed"* to the census, *"Yes, do the Felicia rig, so that we can compare on VS2 and Vampire Saved and be certain"*, and on the sheets *"Yes, identical … #175 can be clsoed"*) | native vs2 first: the dome's attack records `(0, 0, 40, 6)` (12 px tall, top 6 px above the ground); a probe (`tools/trap_air_probe.sh` + `trap_air_boxes.py`) put a jumping Victor 30 px above the box at his lowest airborne frame — **an intermediate reading of mine, "overlapping in the air but not hit, a grounded-only rule", was WRONG**: the resolver built phantom boxes for zero vuln ids; a read tap and execution breakpoints showed the per-box loop (vs2 `0x1699C`/`0x169F0`/`0x16A46`, A6 = the dome) testing vuln0 alone on airborne passes, every pass, no grounded rule (gotcha "A VULN ID OF 0 IS NO BOX"). `tools/air_hurtbox_census.py` found nodes reaching the feet on six characters' jump attacks; **Felicia's j.HP node 4 (bottom −6)**, rigged with REAL picks on both games (P1 Phobos, P2 Felicia; her j.HP first hit Phobos and cancelled the trap until he was parked at x 540), is hit IN THE AIR at f3521 (y 42, class 0x07, knocked upward) on native AND ours, **every traced field equal over 3500-3530**; the control press (3511) is hit on the landing frame, 0x52 / 0x38. Gate `tests/audit_trap_air_hit.sh` — frozen through rule-checker runs `2026-09-25-219` to `-222` (every plant caught; 219-221 VIOLATED on true findings, each resolved by work: the gate compared only part of what it traced, the 3511 sheet and two rulings' own words were missing from the packet, the per-chain charge was too coarse, our Felicia was not shown to be pristine vsavj's, the data files were not shown to be the romsets the legs run; **222 read OK on all five questions**) — so the gate now compares facing, RESOLVED hurtboxes and the dome's type/record/box as well, charges per NODE only the hurtboxes whose static data differs between the games, requires our Felicia equal to pristine vsavj's on every node entered, and decrypts the three running zips against the files it reads; **seven controls**, each reaching FAIL on its own check. **Widening the comparison found a vanilla data difference nothing had recorded: Felicia's hit reaction `b:0x0f` head hurtbox is `(-8, 70, 12, 8)` on vsavj and `(-3, 76, 17, 14)` on vs2** (engine_internals.md; our build = pristine vsavj's, so not the port); `tests/audit_trap_airborne.sh`'s "over the active dome" wording tightened (the box switches on at f3500 in that rig); `engine_internals.md` and the gotcha carry the facts; captures `tools/trap_air_sheet.sh` read identical. |
| **(8) THE FINDINGS TABLE** (checklist step 1: every finding, its live home, the test that reproduces it) | (a) a recommendation tied the merged-m15 build directory to that freeze on a program fingerprint merged-m16 shares — home `DECISIONS_HISTORY.md` "Ruled 2026-09-25 (14z-182) — #132 closed"; test: none, a discipline (run 215). (b) `coverage_matrix.md`'s tenant-moves row read COVERED over three already-covered items — home `docs/project/coverage_matrix.md` (the row, corrected); test: none, a document. (c) #136 had closed against its LAST list; re-derived from the whole body and thread, four findings — home `DECISIONS_HISTORY.md` "Ruled 2026-09-25 (14z-182) — #136's full-scope re-derivation"; test: none (the tickets carry the gaps). (d) Hop Kick in Donovan's Dark Force connects on neither leg of any rig (a SAME on a whiff) — home #178 + the coverage row; test: none yet (#178's rig). (e) no ours-vs-native movement gate for any tenant — home #177 + the coverage row; test: none yet (#177). (f) #161's +1 is vsavj's own pipeline, the class renamed `DMG-VSAVJ` — home `docs/project/tables/defense_rows.md` "THE ONE RESIDUAL IS VSAVJ'S OWN"; test `tests/audit_move_parity_attribution.sh`, `tests/audit_phobos_dmg_residual.sh`. (g) #134's hit rigs leave 6 VOID legs (5 LP, 1 LK), reported and never counted — home the #134 ticket row + `tests/test_meter_gain.sh` section D; test `tests/test_meter_gain.sh`. (h) #138, #140 and #154 are still present as filed — home their ticket rows + `DECISIONS_HISTORY.md` "#134 kept open" (the status entry); test: none (tickets). (i) A VULN ID OF 0 IS NO BOX — a resolver that reads it builds a phantom box (read by me once as a grounded-only rule) — home `docs/project/gotchas.md` "A VULN ID OF 0 IS NO BOX" + `docs/game/engine_internals.md`; test `tests/audit_trap_air_hit.sh` (its resolver skips zero ids; the gate's hit-frame check). (j) a projectile hits a fighter through the per-box loop on every pass, no grounded rule — home `docs/game/engine_internals.md` "A projectile hits a fighter through the SAME per-box loop"; test: none as a gate (`tools/trap_air_probe.sh` ORDER=1 reproduces the breakpoints). (k) the dome's attack records are (0, 0, 40, 6); a jumping Victor's airborne hurtbox bottom is 33 px above his feet — home the same `engine_internals.md` bullet; test `tests/audit_trap_airborne.sh`. (l) the dome hits an airborne Felicia (j.HP node 4) with class 0x07, identically on vs2 and ours — home the same bullet + `DECISIONS_HISTORY.md` "#175 closed"; test `tests/audit_trap_air_hit.sh`. (m) Felicia's b:0x0f head hurtbox differs between vsavj and vs2 (vanilla data) — home the same bullet; test `tests/audit_trap_air_hit.sh` (the per-node charge and the pristine check). (n) `audit_trap_airborne.sh`'s "jump over the active dome" overstated (the box switches on at f3500) — home that gate's header, corrected; test: none (wording). (o) the agent hook reads inline-script `&` bitmasks as backgrounding — home `docs/NEXT_SESSION.md` trap 3; test: none (write such edits to a scratch file). (p) the opener's sweep survivor is 14z-181's Claude daemon leftover, this session not under it — home STATE row (6); test: none. (q) the shared `tools/hitbox_records.py` `node_boxes()` also resolves id 0 — home the gotcha (i); test: none (no verdict rests on it; noted, not changed mid-task). |
| **CLOSE (14z-182, 2026-09-25) — THE CHECKLIST** (the maintainer: *"we're at 82% context, might as well close"*) | **(1) THE FINDINGS TABLE** — row (8), seventeen findings (a)-(q). **(2)** `python3 tools/close_findings.py 14z-182` -> `build/agent182/close_findings_14z182.txt`: 3 addresses checked, GAPS none, REVIEW none, PROMISES none — never cited as completeness; `tools/homes_tracked.py "(8) THE FINDINGS TABLE"` exit 0 (its first run FAILED four: the new gate and probe not yet tracked, a `build/` path cited as a home — the files staged, the wording changed; one REVIEW, item (o), a discipline with no artifact); `tools/retraction_grep.py tests/rulecheck/retractions/14z-182.tsv` — five retracted wordings, every hit classed under the output (archives, rename notes, 14z-181's row marked in place, the local rendering), both reach controls found. **(3) POINTERS**: 14z-181's (11b) marked in place; NEXT_SESSION rewritten (the 14z-181 opener moved verbatim to NEXT_SESSION_HISTORY). **(4) PROMISES**: none open. **(5) SCRATCH SCRIPTS** (on trial): the probe, sheet and census were written as tools from the start (`tools/trap_air_probe.sh`, `trap_air_boxes.py`, `trap_air_prologue.py`, `trap_air_sheet.sh`, `air_hurtbox_census.py`); scratch: the in-scratchpad edit scripts (repository edits, not figure sources) and the reader/measurer specs; figures quoted in documents come from the gate or the tools. **(6) THE DOCUMENTATION PACKET** — rule-checker run `2026-09-25-223` (plant caught) VIOLATED on two true findings, resolved in writing: the packet counted five no-test findings where row (8) names ELEVEN — (a) (b) (c) (d) (e) (h) (j) (n) (o) (p) (q), each 'test: none' with its reason — and each home's sufficiency is this author's reading, not a tool's; and the retracted reading "a grounded-only rule" was missing from `tests/rulecheck/retractions/14z-182.tsv` — added, its seven hits classed (retraction notes only). Earlier this sitting the pinned rule-checker ran 215-222: every plant caught; 215-221 VIOLATED on true findings, each resolved by work; 222 OK. **THE TIER**: `run_all_static.sh --strict` (all controls) first read **NOT GREEN — PASS 168, FAIL 1** (`test_decrypt_cache` section 5: the new `audit_trap_air_hit.sh` shells out to `cps2_decrypt.py` — by design, it checks the cache against the running zips — so it joined the gate's exemption list with its reason, beside `audit_victim_parity`; the exempt count's wording three -> four); the second strict tier **GREEN — PASS 169, FAIL 0; fired 233 / declared 233, executed 233 honoured 233** (`build/agent182/tier_close2_14z182.log`). **THE SWEEP**: the survivor tree killed on the maintainer's command (*"kill after the tier finishes"*: `kill 85184 85170 85166 85158 85150`, children first); `python3 tools/agent/sweep.py` then **CLEAN: no survivor**; its one OPEN TASK line is the mid-session tier this close stopped with TaskStop (no process holds its output). **THE PROCEDURE CHECK (C1)**: `extract.py` over this transcript (39efeec2, 3561 lines) and 14z-181's carried tail (152495c2 from record 3209, 454 lines); procedure run `2026-09-25-224` (plant caught) **VIOLATED on QP1, QP2, QP3, QP5 — all true, resolved in writing**: the measurer re-derivation of the #175 figures promised twice and dropped (the gate reproduces them; carried as NEXT_SESSION item 0b), "every change committed at the kill" said before the close commit that followed the kill, "two readers" where one reader and the agent's own read, "unchanged since filed" where "still present as filed" was what was checked, 1,700 for 1,702 unmarked, and three workers straying from their specs (a substitution after a `grep -P` error, a run past two failing commands, a misquoted line — no finding rests on them; NEXT_SESSION trap 7). QP4 OK. **THE WORKER CAP**: `transcript_gaps.py --subagents <id> --cap` — 39efeec2 **0 breaches over 31 workers**, 152495c2 **0 over 29**, no fallback (exit 0 both). **THE PUSH**: `git push origin main` after this row. |

# STANDING SECTIONS (current state — never archived)
## Standing rulings


One line per ruling that still constrains work; the full entry lives where the line says. A line is deleted when its ruling stops constraining work (CLAUDE.md [VSP-17]).

- **The parity gates' per-frame level and RNG pins are their EQUALISED INPUT (maintainer, 2026-09-25, 14z-182 — option (a), *"agreed"*):** every ours-vs-native parity gate may write `RAM:$FF8116` and `RAM:$FF80D4` on every frame of both legs; the rule-checker's Q3 on such a packet reads that as a named, ruled premise, not a finding; whether the RNG ADVANCES alike on both games is its own ticket, #176 — DECISIONS_HISTORY.md 'Ruled 2026-09-25 (14z-182) — the parity gates' per-frame level and RNG pins'.
- **The opener's sweep survivor is killed before the push ONLY on the maintainer's command (maintainer, 2026-09-25, 14z-182):** *"we shall kill it before the push BUT ONLY ON MY COMMAND after we have made sure that we won't have any adverse effects should Claude crash"* — so the close does not kill or declare PID 85150's tree itself; it puts the no-adverse-effect evidence (STATE 14z-182 row (6)) to the maintainer and waits for the command — DECISIONS_HISTORY.md 'Ruled 2026-09-25 (14z-182) — the opener's sweep survivor'.
- **The orchestrator is Opus 5.5 at effort High (maintainer, 2026-09-25, after the 14z-181 close):** *"The orchestrator must be a Opus 5.5 High instead of Fable 5.1 (access to Fable not guaranteed so I need to account for it)"* — it supersedes the Fable 5.1 named in #172's S5 ruling (the rest of that ruling stands); a sitting on any other model says so at the opener — DECISIONS_HISTORY.md 'Ruled 2026-09-25 (after the 14z-181 close) — the orchestrator'.
- **A /btw fork is exempt from the worker cap (maintainer, 2026-09-25, 14z-181 close):** it runs on the session's model and is not a worker the agent spawned; a `--cap` read whose only breach is such a fork is green for the push, and the close names the fork — DECISIONS_HISTORY.md 'Ruled 2026-09-25 (14z-181) — the worker cap'.
- **The nine 14z-181 capture reads (eighteen sheets) all IDENTICAL, by the maintainer (2026-09-25):** Victor's one-frame throw difference is documented and validated, #173 closed on the read, #175's next step the vs2 check. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-25 (14z-181) — the maintainer's reads".
- **#172, the agent architecture (2026-09-23, 14z-175 — "Hard block + written resolve is fine but that doesn't mean that the session should grind to a halt"):** a block binds the TASK AT FAULT, never the session — redone or corrected, re-checked, unblocked when green; deterministic hook checks hard-block, the model checker blocks until resolved in writing; the checker reads a generic checklist plus a transcript extract, never CLAUDE.md; the model checker runs at every push and every close; slice S1 (the deterministic hooks) goes first, alone — and S1 is INSTALLED as C0.1 (`.claude/settings.json`, only the maintainer edits it) with C0.2 moved to the procedural checker (*"Install + protect"*, *"Move it to C1"*). S3, ruled 2026-09-23 (14z-176, *"Build it as described"*), is the procedural checker as QP1-QP4 on the rule-checker's machinery, QP2/QP3 fixtures planted into a real clean span; the `agentlib` tracked-launch fix was APPLIED by the maintainer (14z-176b), and C1's push hook INSTALLED (*"since we both think the push hook is a good idea, I'll install it"*) — a push of this repository now needs a passed or resolved `procedure` run covering a commit in its range. S4, ruled 2026-09-23 (14z-177): workers' caps are enforced on the CALL (a hook) and by a static gate on every definition, FORKS pass as the orchestrator (*"Allow forks"*); the rule-checker becomes a pinned definition (opus, effort high) after a recalibration (*"Pin it now"*); C1 widens QP3 to workers' figures and gains QP5 SPEC-CONFORMANCE (*"QP3 wider + QP5"*); built in the proposed order (*"As proposed"*); the call gate `pre_agent.py` INSTALLED by the maintainer (*"I'll install it"*, *"done"*); step 4's pinned readers are SUBAGENTS (*"Subagents"*), its first act measuring whether a reader sees CLAUDE.md. Step 4 LANDED 14z-178: the pinned `rule-checker` (a version id, `omitClaudeMd: true`) with every run's readers bound to the spawn check at `record`; #158 closed on it (*"Close it"*), and the maintainer raises `cleanupPeriodDays` at user level (*"You set it, user level"*). S5 RULED 2026-09-24 (14z-178): no orchestrator definition — a PLAIN session the maintainer starts on Fable 5.1 (*"Plain session"*; the MODEL superseded 2026-09-25: Opus 5.5 at High, the line above), gated by one real sitting under it whose close reads the C0/C1 record and `transcript_gaps.py --subagents <session> --cap` (*"That gate"*). That sitting was 14z-179, on Fable 5.1, and #172 closes on its green (*"Close it on green"*); the worker cap read (`--cap`) is a step of every close beside the procedure check (*"Standing step"*, 14z-179). Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-23 (14z-175) — #172", "— #172 slice S1" "Ruled 2026-09-23 (14z-176) — #172 slice S3", "Ruled 2026-09-23 (14z-176b)" and "Ruled 2026-09-23 (14z-177)" and "Ruled 2026-09-24 (14z-178)" (two) and "Ruled 2026-09-24 (14z-179)".
- **#171 Q6, the 73 poke read-back findings are RULED as proposed (2026-09-25, 14z-181 — "I agree with the proposal"):** 40 OBSERVES, 29 READS-BACK (21 never-compared FIELDS columns DROPPED from eight gates and RETIRED, the rest labelled in their gates' headers; one row corrected from the proposal's OBSERVES after rule-checker run 2026-09-25-147 — `test_pyron_cosmo`'s no-reset guard reads the poked id back), 3 CONTROL-PLANT and 1 CROSS-LEG, the two new classes in the table; every drop re-measured on its gate before commit. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-25 (14z-181) — #171 Q6".
- **#171, gate qualification (2026-09-24, 14z-180 — "As proposed WITH THIS ADDITION: I want for each test a human-readable description of the test (at least what it tests, how it tests and what is the expected result)"):** the shape is `docs/project/gate_qualification_scope.md`'s seven slices in the order Q0 (the WHAT/HOW/EXPECTS description in every gate's header, rendered per family by the GENERATED gate index, landing by family for the maintainer's review), Q1 (the module-reference gate — landed this sitting, *"Land it now"*), Q3+Q4 with Q5 inside (`# FOLLOWS:` header declarations with a reconciliation control — *"Header line"*; the runner records its commit and a session gate names STALE emulator gates — NOTE at session, FAIL at freeze/release; headroom fails at half the cap), Q2 (the MEASURES/MEASURED grammar), Q6 last (the poke read-back census, per-finding rulings still to come). Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-24 (14z-180) — #171 gate qualification". **14z-180 close: every slice is BUILT (Q0-Q6, four commits `bd31b1a3`..`54b92736`); what the ruling still constrains is the REVIEW — a description or a declaration is corrected in the gate's header, never in the generated page — and the 73 poke read-back rulings under "Decisions pending".**
- **FBNeo patch 0003 (drop SDL2_image) is RATIFIED for RELEASE builds (2026-09-21, 14z-173 — "in that case then yes, i approve of patch 0003"):** frontend-only, applied by `build_release_emulators.sh` and NOT by `setup_fbneo.sh`; the macOS bundle goes 24 files -> 4, so "Open Anyway" costs 4 approvals instead of 24, and it is proven inert over 12,120 frames in work RAM AND framebuffer. Full entry: `DECISIONS_HISTORY.md` "Ratified 2026-09-21 (14z-173) — FBNeo patch 0003".
- **#144 is macOS blocking the binaries and right-click > Open does NOT get past it (2026-09-20, 14z-173 — the maintainer's captures and "Does not open"):** the shipped advice is withdrawn from every live carrier; clearing the quarantine flag is what works, `PLAY.command` offers it, and System Settings > "Open Anyway" also works but PER FILE — **MAME 2 approvals, FBNeo 24** — so the READMEs lead with the one-step route; the ticket now turns on whether to notarize (a paid Apple account). **The player READMEs are written for a non-developer who has never seen this project** ("it must have felt clear to the expert who wrote them"). **The applier page ships as a local file in the asset, one page per release.** Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-20 (14z-173) — #144 is macOS BLOCKING the binaries".
- **The applied set is DEFLATED and the QSound BIOS member is OPTIONAL (2026-09-20, 14z-173 — "If it is identical content, I see no reason not to go for the smaller size" and "Ideally I'd love the option to choose to add it or not"):** `apply_release.py` writes deflated (73.7 -> 28.0 MB, +0.26 s once at load, measured) and `--no-qsound-bios` omits `dl-1425.bin`, which MAME needs, FBNeo does not, and MiSTer resolves from the card's `qsound.zip`; the MiSTer page tells players to use the flag and the emulator pages the default. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-20 (14z-173) — the applied set is DEFLATED".
- **The deliverables' order of work, and macOS FIRST (2026-09-20, 14z-173 — "Agreed with one caveat: let's focus on MacOS first, even if we were to release M19 only for MiSTer and MacOS, the proof of concept would be fully validated"):** standalone set -> launcher per platform -> reproduce #144/#145 on clean hosts -> README pass -> the applier app; macOS and MiSTer are the proof-of-concept release and Windows/Linux run in parallel without blocking it (so #145 leaves the near-term path, #144 and #146 stay). Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-20 (14z-173) — THE DELIVERABLES".
- **The standalone set lands in the RELEASE, not the build (2026-09-20, 14z-173 — "C"):** the applier completes `vsavjw.zip` with 7 pristine members so a player places ONE zip; no build fingerprint, expectation set, registry row or MiSTer CRC moves, and the shipped set being a declared superset of the gated one is held by `test_release_roundtrip` §1 and `test_release_binaries` §2b, never by argument. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-20 (14z-173) — the standalone set lands in the RELEASE".
- **The disabled EX route does the no-stock move (2026-09-18, 14z-169 — "Yes, the no-stock move"), refined 2026-09-19 (14z-170 — "Natural reading (Recommended)"):** the tenants' vs2 EX input is read as its sub-pattern at every stock level — for Phobos and Donovan vs2's no-stock move; for Pyron 623+PP, the ES move when a stock is available and the normal move without one. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-169) — the disabled EX route does the NO-STOCK move" and "Ruled 2026-09-19 (14z-170) — Pyron's 2623+PP with stock reads NATURALLY".
- **The 0x52 fix is scoped S1 (2026-09-18, 14z-169):** Phobos's two trap remaps stay on the solo Phobos track only (a row key: unless Donovan's manifest is in the composition); the merged build carries the full fix. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-169) — the class-0x52 fix is scoped".
- **#136: all the analysis first, then the fixes, then relentless regression testing (2026-09-18, 14z-168 — the maintainer: "let's finish all the analysis first. Then we'll fix and then relentlessly test for regressions"):** no #136 fix (nor #157, #159 or the 14z-168 tickets) lands before every family is analysed; every rig built for it becomes a gate that runs at freeze and release. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — #136: ALL the analysis first".
- **The tenants' Dark Force (2026-09-18, 14z-168):** vsav's Dark Force CHANGE with the shells' common rules, each tenant's own vs2 startup window, and its personal Dark Force (vs2's EX move) active for the mode; compared against the vs2 EX move, never vs2's P+K (Dark Force POWER); the DURATION stays vsavj's (360/377/360, *"agreed"*); the tenants' vs2 EX input into the mode on our build is to be DISABLED (*"agreed"*). Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168)" (the Dark Force and the EX-route entries).
- **The tenants' DEFENSE rows become vs2's (2026-09-18, 14z-168 — "given the measurements we should take the vs2 rows"):** Phobos and Donovan take their vs2 defense rows and rally thresholds (Pyron needs nothing), with no frame cost and no change to any legacy character; supersedes 2026-08-14 option (b). Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — the tenants' DEFENSE rows become vs2's".
- **The column shock and the Plasma Trap take vs2's class-0x52 rule (2026-09-18, 14z-168 — "I'm all for fixing"):** victim shock 0x18 and the attacker exempt, for the tenants' 0x52-origin hits; no noticeable lag, no new breakage; the 14z-85g(2) accepted deviation is withdrawn. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — the column shock and the Plasma Trap".
- **No gauge is built during Dark Force (2026-09-18, 14z-168 — "it makes sense that you can't build meter during DF"):** the shells' rule; the tenants' start-up gauge in the mode is a defect (#157's Dark Force tail). Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-168) — no gauge is built during Dark Force".
- **The close checklist (2026-09-18, 14z-167b — the maintainer: "let's implement them and keep item 2"):** at every close, (1) a findings table — each finding of the group, its live home and its test; (2) `tools/close_findings.py <session>`, every GAP homed and every REVIEW answered, its clean result never cited as completeness; (3) a pointer on every earlier row the sitting resolves; (4) a grep for promises; (5) cited scratch scripts promoted, on trial and measured per close; (6) the documentation packet through the rule-checker. The steps live in this file's header (ruled 2026-09-18). Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-18 (14z-167b)" (two).
- **#136 goes over again by the 14z-164 census (2026-09-17, maintainer: "I agree with the 3 propositions"):** the move-parity gate moves to PER-EVENT verdicts with the stock, the meter fraction and P2's HP compared and every "in DF" event asserted in DF; a P2 whose data is the same on both games (a second tenant, or a legacy character byte-identical between vsavj and vs2, found statically) — done 14z-165, Demitri; the 13 rows re-labelled by their fields on #136 before any family is root-caused. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-164)".
- **The rule-checker's Q3 and Q4 are BOUNDED, and a VIOLATED is resolved question by question in writing (2026-09-17, 14z-163, #152 — the maintainer: "both"):** Q3 counts only a shared thing the artifacts SHOW can write the compared state; Q4 reads OK when the claim itself names the uncovered mode; what a bounded question still finds stops the action until `rulecheck resolve` answers each violated question. Every fixture recalibrated on the new wording. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-163) — the rule-checker's questions".
- **#136 is gone over again IN FULL, not only through its faithfulness control (2026-09-16):** the maintainer insists it stays on the list until every part of it — the naming rigs, the pinning protocol, the 27 frozen verdicts, and the Pyron and Phobos legs above all — has been re-examined; no #136 verdict is built on until then. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-16 (14z-160)".
- **The static tier runs by CADENCE (2026-09-17, #148 lever A):** `tests/ci_cadence.tsv` lists twelve build/content/tooling gates as `freeze` (the seven costliest plus the five 35-39 s gates, second cut) and five release/MiSTer gates as `release` (the three ≤2 s ones back to session); a session run (the default, mid-session AND at the close) defers them BY NAME unless a path they depend on changed; a freeze runs `--cadence freeze`, a release `--cadence release`, each on the commit it builds from. A new listing is a reviewed event. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-162) — #148 lever A".
- **Static-tier controls at the CLOSE (2026-09-14; REAFFIRMED 2026-09-17 after 14z-161 pushed on the mid-session form):** a mid-session commit runs `tests/run_all_static.sh --strict --exec-controls none` plus `CONTROL=<name>` for every gate it adds or changes; the session close runs every control (the default) and ONLY then pushes — not to be compromised until #148 is worked. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-14 (14z-154)" and "Ruled 2026-09-17 (14z-161b)".
- **The order of work (2026-09-17):** a stable, reliable state first, then #148 (the static tier's cost, done), then #152 (the adversarial rule-checker, done 14z-164b), then everything else — the #136 families included. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-17 (14z-161b)".
- **Release scope (2026-09-02):** at release every test whose SUBJECT is the released artifact runs, its legacy content included (a reference leg on vsav2 or pristine vsavj is not the subject); anything red or skipped is a hard fail unless approved at release time, so a release-scope gate fails loudly on a missing prerequisite. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **A red gate is a question (2026-09-02):** before fixing the gate, fixing what it caught or deleting it, establish which side's expectation rests on a measurement; a frozen number whose provenance cannot be named is a claim. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Cross-game comparisons at a matched speed level and a pinned RNG (2026-09-15, #135, #142; amends the 2026-09-02 #114 line):** vsav2 defaults P1 to TURBO and vsavj to NORMAL, and the two games' RNG states differ, so `tests/test_don_immortal_native.sh` pins `RAM:$FF8116` (levels 6 and 8) and `RAM:$FF80D4-D5` on both legs and asserts ours == native in hit count, damage and hit frames; LP's accepted "one hit short" is retracted as a mode artifact and #142 is closed invalid; the 2026-09-02 ruling that the vanilla vsav engine takes precedence stands. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-158)" and "Moved 2026-09-14 (14z-154)".
- **No static substituted-wheel gate (2026-09-02):** declined — the pattern matches 26 legitimate gates; the defence is [VSP-163] (assert `+0x60` against `bases.tsv`, or force the pick). Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Oboro's intro stays (2026-08-28):** never delay round start or cut the intro for a cosmetic reason. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Community data (2026-08-31):** measurement is king — a community source that matches ours exactly or by a constant offset validates our measurement, and an inconsistent pattern means re-check OUR measurement first. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Cosmetic is optional (2026-08-27):** cosmetic and single-player-only surfaces are nice-to-have; competitive 2P versus is the focus. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **No legacy character meets a tenant in 1P arcade (2026-08-28):** not a problem, 2P versus is the focus; not built, not scoped. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **The living-docs generalisation, when scheduled (2026-09-08, #119):** independent of bbh but compatible with it and on the same principles; the site generator in scope; fidelity required in principle, its form argued in the scope document; the open items first. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Linux release binaries (2026-09-13, #121):** self-containment is checked against an external host-provided list plus the ruled exceptions; the WSL2 Linux binaries are never published. Full entry: `DECISIONS_HISTORY.md` "Moved 2026-09-14 (14z-154)".
- **Generator refactor policy (2026-08-21, #50):** no dedicated refactor of `tools/gen_donovan_patch.py`'s `main()` is ever scheduled absent a new measured cost; a handler moves to module level only when a test needs to drive it, with its unit test in the same change, and byte-identity under `tests/test_m3a_reproducible.sh` and `tests/test_phasec_spaces.sh` is the refactor gate. Full entry: `STATE_HISTORY.md` "Session 14z-102 (post-freeze rulings)".
- **Don't assume; measure first; ask if necessary (2026-09-15, clarified 2026-09-16):** no factual claim — about code, a count, a behaviour or a name — is stated without a measurement or a tree source behind it; a question a measurement can settle is measured first and never published as "unreconciled"; asking the maintainer is right when a measurement cannot settle it or is not enough. Full entries: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)" and "Ruled 2026-09-16 (14z-158b)".
- **Pipeline-image registry rows (2026-09-15, #96):** the M2 battery dispatches through `tests/expected/registry.tsv` on the build's fingerprint, never a pinned set name; `donovan-mN-stock` / `-stage4` register its two pipeline images (untagged, never shipped); each freeze rebuilds both and records whether they moved, and an unregistered fingerprint is a rule-6 stop. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)".
- **The merged set carries no tenant expectations (2026-09-15, #111; the 14z-133b B2 design):** the merged expectation set holds the legacy classes, the mask and the `.skip` markers; tenant-content `.sha1` expectations live in the solo sets, and the merged build's tenant content is covered by `audit_merged_legacy` leg (b) and the tenant gates. Full entry: `DECISIONS_HISTORY.md` "Ruled 2026-09-15 (14z-157)".

## STANDING PRINCIPLE (maintainer, 2026-08-05): vanilla wins ties

**[VSP-21]** "vsav vanilla is always better when we can." **When a console port and
arcade vsav differ and both would work, take vanilla.** A console port's
choice is not evidence that vanilla is wrong; it is evidence of what that
port's designers preferred.

This is a general rule, not a one-off: the PS1 capture is a reference for
what is POSSIBLE and for data we cannot otherwise obtain (cell placement,
the adjacency of NEW cells), not a style guide for content vsav already
defines. Paired with the maintainer's other statement — "as long as we can
select characters it's good" — the test is: does keeping vanilla still let
the feature work? If yes, keep vanilla.

Applied immediately, twice:
- **`Bishamon DL` and `Aulbath DR` stay vanilla** (Anakaris / Sasquatch).
  PS1 sets both to "no move"; neither is needed for reachability, so
  vanilla stands.
- **Horizontal wrap stays vanilla.** Vsav wraps left/right (cell `0x01`
  Left goes to `0x05`, measured and confirmed in-emulator); the PS1 report
  of "no wrapping" reflects untested extremes. We touch none of those
  cells, so nothing to decide.

Judgment applied under the same rule, open to veto: the three inbound edges
from `0x0B` (`D`/`DL`/`DR` into the new row) DO diverge from vanilla, and
strictly they are not required — Phobos and Donovan are already reachable
via `Bishamon D` and `Aulbath D`, and Pyron through them. They are kept
because without them, pressing Down on the cell directly above the new row
does nothing while three medallions are visible below it, which is the UX
failure "as long as we can select characters" is meant to exclude. Dropping
them would reduce the legacy footprint from 5 bytes to 2.

## Decisions pending (human)

Only undecided items live here; a ruling moves its entry to `DECISIONS_HISTORY.md` in the
commit that records it (CLAUDE.md [VSP-17]).

## THE DEADNESS REGISTER (opened 14z-71, maintainer's standing instruction)

**[VSP-23]** Every claim of the form **"legacy never reaches this, so we may reuse
it"**. Each is measured by ABSENCE, which is the weakest kind of evidence
we accept, so each is listed here with its guard. **These are the FIRST
PLACES TO CHECK for any unexplained regression in vanilla assets, engine
behaviour or rendering** — before anything else is suspected.

| Reused resource | Claim | Guard | Fallback if wrong |
|---|---|---|---|
| ~~palette-seq ids 0x1E-0x21 (`0x39ACC0`)~~ **CLAIM FALSE, ROW WITHDRAWN 14z-79 (they are Bulleta's DF block)** | vanilla only ever requests 0x26/0x27 | `tests/audit_palette_seq_ids.sh` (10,504 sampled calls) | none — the palette path never transits work RAM, so the audit is the ONLY guard |
| effect-class row 16 (`0x080AEC`) | vanilla never dispatches class 16 | `tests/audit_effect_class_rows.sh` §1, 0 reads vs a 1760-hit control | none needed: the row was a stub (`rts`), so a wrong claim costs at most the old no-op |
| ~~drawer list-type 6 (`0x01B6AA`)~~ **CLAIM FALSE (measured 14z-89) — LEGACY LISTS DO REACH TYPE 6** | vanilla has no type-6 sprite lists | `audit_effect_class_rows.sh` §1/§4 + `tests/test_beam_list_type6.sh` | **THE FALLBACK HELD — this is what a safe-and-loud design buys.** 14z-89 measured the tripwire ARMED on legacy content on huitzil-m13: `21_don_mash` 387 times and `26_don_arcade_mash` 948 times, PC-attributed to inside the thunk body (0x0FD060). Rendering stayed correct throughout (the fallback runs vsav's own type-6 code, reproduced instruction-for-instruction), so nothing rendered wrong and no playtest ever saw it — exactly the outcome the register's "prefer designs where being wrong is safe and loud" rule was written for. WHY IT WAS MISSED: the deadness measurement was sound but its COVERAGE was four replays (`02/07/09/30`), and the gate has always run on that default set; the two replays that arm it are long mash/arcade rigs nobody pointed it at. COST TODAY: `$FF010C/$FF010D` is a live work-RAM counter vanilla does not keep, so both replays diverge permanently from the vanilla masked basis — they are `.pending` on huitzil-m13 pending the maintainer's ruling. ~~OPEN: does the fallback need to stop counting (make the tripwire diagnostic-only / move it out of work RAM), or is the counter acceptable? See "Decisions pending — 14z-89"~~ **ANSWERED 14z-91: the counter was REMOVED** — the `beam_list_type6` `RAM:$FF010C` counter deleted, which cleared all six `.pending` legacy replays (`HANDOFF.md` Build registry, the 14z-91 row (C)) |

**[VSP-22]** Rules for adding a row: the claim must be measured with a POSITIVE CONTROL
on the same instrument and leg (a blind instrument and a real zero look
identical — paid for three times in 14z-71); it must name its guard; and
it must say what happens if the claim is wrong. Prefer designs where being
wrong is *safe and loud* over designs that are merely well-measured.

# patch_index — one-page registry of every patch

**[VSP-99]** Updated in the same commit as any patch change. This page is the
REGISTRY — status, dependencies, exclusivity, deprecation — not the
detail. Byte detail: docs/project/patch_notes.md (newest first) + each build's
generated `patch/patch_notes_fragment.md`. Mechanism rationale: the
manifests (every row carries its session tag inline). Rebuilt 14z-67
from those sources (the page had never grown past M1; the staleness was
found in the 14z-66 close audit).

## Romset patch bundles (program + gfx content)

Every bundle is generated by `tools/gen_donovan_patch.py` from an
oracle-validated extraction, driven by `tools/build_donovan.sh <stage>
<outbase>` with `TENANT_MANIFEST`/`TENANT_CHAR`/`GEN_FLAGS`. Nothing is
hand-edited; bundles chain builders and diff once against clean.

| Patch | Status | Depends on | Exclusive with | Notes |
|---|---|---|---|---|
| **boot name screen** `SAVIOR` -> `SAVED` (14z-127) | **active, UNREGISTERED** (built as don_m19 `8065bc92` / hui53 `08944a7e` / pyron37 `a43da974` / m5_stock14 `e86e1d04` / m3b_merged22 `f42f7569`; expectation sets NOT yet frozen) | donovan.toml + huitzil.toml + pyron.toml (three identical `aux_poke poke16` rows, deduped to one shared engine row on a merged build) | — | Three words at `PRG:0x01C822/24/26` in the per-region display script at `PRG:0x01C806`, JAPAN entry only. TEXT, not tiles. **The start-COLUMN byte must stay EVEN — an odd one is a vector-3 ADDRESS ERROR that soft-boots** (`game/gotchas.md`). Measured free: work RAM identical to pristine over 1,621 frames; `audit_merged_legacy` 96/0 with attract bit-identical. Detail: patch_notes 14z-127 |
| null vsavj | active | — | — | `build/manifest/vsavj.toml`: straight member copies, bit-identical. The baseline every patch chains onto. |
| **donovan STOCK track** (slot 0x0F, `build/m5_stock`; current twin `build/m5_stock13`, 14z-119 — **MOVED**, `38e9cb2c`) | **CURRENT `38e9cb2c`** (donovan-m18-stock, 14z-119 — `port_param32` is a per-row data_port, not profile-gated: the substituted track writes Donovan's VS2 physics onto stock slot 0x0F too, six data ops in `vm3j.04d`, no legacy row; prior `d29fd062` donovan-m13-stock, 14z-110b — the 0x51->0x44 data remap is not profile-gated; UNCHANGED through 14z-111/113/115/117/117b. CORRECTED 14z-114: this cell had stopped at 14z-102. Prior generations: `cf455760` 14z-110 (the D2 window), `883e7d17` 14z-102 (#107's reconciliation flip rides the shared map, not profile-gated), `16da59b6` 14z-99 (#103), `a054de5c` 14z-91..96, `6c93cfa8` 2026-08-06 = the old `ae701ffb` + the 2-byte 14z-2 mirror-victim fix) | donovan.toml + reconciliation.toml + effect_tail.json; stages 1-6 | replaces Jedah slot 0x0F (his art/select/records substituted) | The M2a-M2c arc condensed: ~335K ported, two engine hooks, R1 map ~120 rows, gfx in Jedah's band (delta +0x2750, group B), select/HUD/win/palette surgery in place. Sfx stubbed until M5 (25 rows). Full battery + masked legacy basis (3 windows). History: patch_notes M2a..14z-49 sections. |
| **donovan-m3a WIDE track** (native id 0x13, `build/m5_wide`; current generation **donovan-m18** = `build/don_m18`) | **CURRENT `7109f835`** (donovan-m18, FROZEN 14z-119, 339 ops — THE PHYSICS PORT: `port_param32 = true`, his walk/jump rows carry VS2's values, maintainer-ruled 2026-08-29; prior donovan-m17 `90a225ce` 14z-117b, 336 ops — random select includes the tenants; prior donovan-m16 `7950c844` 14z-117 (the Pyron-medallion fix, M10); prior donovan-m15 `38a4becb` 14z-115, 332 ops — the select-wheel separation; prior donovan-m14 `772d8052` 14z-111, the #99 root-cause fix: bank_map `ai_script_0..3` + his vs2 AI-script data root; prior donovan-m13 `ec86330f` 14z-110b, donovan-m12 `60b55a12` 14z-110, donovan-m11 `1de9a027` 14z-105, donovan-m10 `c6a02cb0` 14z-102 = donovan-m9 (`428fc0c9`, the 14z-99 window: #103/#104/#105) + #107's verified reconciliation row. The m3a freeze `4b7d0dc7` (2026-08-06, maintainer-ratified) remains THE WIDE REFERENCE for the de-substitution architecture) | the stock-track manifest + `--profile cps2-wide-v1` (id_by_profile=0x13); the WIDE overlay romset; both emulator 0002 patches | **de-substitution**: Jedah fully restored; `select_port.py` must NOT run (variant-id path; build gates on tenant.json) | The M3a de-substitution: tenant at 0x13, tiles in group C bank 4 (+0x2750 kept, group B pristine), select records generator-composed into wide_ext + six array rows, wheel roster21 from bank 5, variant-id HUD/win-pal, 20 site_thunks. Masked basis V2. Gates: run_suite by fingerprint + the tenant gate family. Reproducibility: `tests/test_m3a_reproducible.sh` on every machinery commit. |
| **huitzil ladder** (native id 0x10, stages 1-4, behavior; current generation **huitzil-m25** = `build/hui52`, `ae953657`, 370 ops, FROZEN 14z-119 — program UNCHANGED from huitzil-m24 (same fingerprint; only the M12 glyph tiles moved); prior huitzil-m24 = `build/hui51`, FROZEN 14z-117b — random select includes the tenants; prior huitzil-m23 `7ade3180` 14z-117 (M10); prior huitzil-m22 `7bb36d0c` 14z-115 — the select-wheel separation; prior huitzil-m21 `cd362ca4` 14z-111 + his AI-script root and four reconciliation rows (#99); prior huitzil-m20 `24a27940` 14z-105, huitzil-m19 `1a7249d6` 14z-102 (the #109 row-31 clone-beam fix), huitzil-m18 `c4bbb375` 14z-99) | active — maintainer-confirmed through 7 playtest rounds (14z-66) | huitzil.toml + reconciliation_huitzil.toml (overlay rows) + the WIDE profile (variant id requires it) | single-tenant: cells 0x11/0x13 exist on its wheel but are unbacked | The 14z-65/66 arc: extraction de-Donovanized (piecewise shifts, dead filler, the insertion sliver), his R1 roots + 12 farm zones, jump-handler clone x02592a + tenant_jump_seq, [[data_in_code]] x5, [[pcrel_escape_fix]] x3 (x02592a, x026142, x05c800), port_param32 (velocities + jump_params), shadow_seq_guard, VS2/VH2 flavor (D1: VS2 default, polarity measured). Sfx stubbed until M5. Gates: boot/soak/ladder/ex/walk/air/grab/pairs/oracle. |
| **huitzil gfx rung + ping fixes** (stage 6, `build/hui9`) | **HISTORY (14z-67) — the rung was absorbed into the huitzil ladder row above from huitzil-m12 (14z-86) on; `hui9`/`hui6` were deleted in the 14z-102/112 build-dir sweeps.** As written: BUILT `9e3105e0` = PING #8; freeze after the effect-flow closure + maintainer confirmation | the ladder + gfx_layout3.toml row "huitzil" (delta-0 placement) + per-tenant effect_tail key | rides the same wheel/select machinery as m3a (shared sections duplicated per manifest until the Phase-2 merge) | 14z-67: his real art end to end — band at native codes in group C bank 4 (no record remap), 12 bank setters, table_fix row 0x10, sprite/effect/win palettes, 7 select_records, drawer bank thunks, grid-column select palettes (vs2 maps him to column 0x0B), HUD rows (pool 0xBE92/0xBE9A), wheel roster21. Every gate green incl. behavior battery on this build. **build/hui6 is pinned as the playtest artifact — do not rebuild into it.** |
| pyron (native id 0x11) | **SHIPPED — current generation `pyron-m19` (`1222df18`, `build/pyron36`, 307 ops, FROZEN 14z-119 — program UNCHANGED from pyron-m18, only the M12 glyph tiles moved); prior pyron-m18 (`build/pyron35`, FROZEN 14z-117b — random select includes the tenants); prior pyron-m17 `01b39c39` 14z-117 (M10); prior pyron-m16 `7177229a` 14z-115 — the select-wheel separation; prior pyron-m15 `c403a283` 14z-111 (his AI-script root, #99); prior pyron-m14 `6bf265ab` 14z-105, pyron-m13 `dbce705b` 14z-102 (#107 via the shared map), pyron-m12 `4c3c072b` 14z-99; prior: pyron-m10 `e29cac23` frozen 14z-94, registry row + freeze tag; STATUS CORRECTED 14z-94, this cell still said "planned" long after his freeze** (layout share taken: gfx_layout3 row, band 0x4ED5-0x8647 delta 0; code region censuses CLEAN 14z-67) | the H template; his stage-6 refusal stays until his manifest exists | — | D4 order: after the H freeze. He has no flavor fork (D2 pending only if vs2/vh2 diverge); select palettes = a dedicated +0xBC block (easier than H's grid column). |
| WIDE overlay romset | active | `tools/build_wide_romset.py` (group C zero-filled; sentinel CRCs 0xdec0de31..37) | the canary romset (`--gfx-copy-group-b`) is B4-only, NEVER merged | The profile's appended members; content builds merge `build/wide0/rompath/vsavjw.zip`. `tools/audit_romset_identity.py` gates every build (the 14z-60z hash-shadow class). |

## Emulator patches (Rule 1 v2 — profile-gated, ratified per profile version)

| Patch | Status | Emulator | Scope | Notes |
|---|---|---|---|---|
| `emu/fbneo-patches/0001-vampire-saved-harness.patch` | active | FBNeo | frontend ONLY | Replay harness + instruments (`-hinput/-hout/-hframes/-hdump`, `FBNEO_HVIDEO/HGFX/HTAP/HPOKE`). Touches no emulation; kept separate from 0002 so the trust surfaces stay separable. Gate: `tests/test_fbneo_instruments.sh`. |
| `emu/fbneo-patches/0002-cps2-wide-v1.patch` | active | FBNeo | 5 CPS-2 driver files | The `vsavjw` descriptor + TWO gated blocks in `cps_obj.cpp` (the bit-12 promote and the `CPS2_WIDE_CANARY` positive control — CLAUDE.md rule 1 v2; this row said "ONE widened condition" until 14z-114) gated on `Cps2Wide` (file-scope global — MUST clear in `DrvExit`). Gate: `tests/test_wide_profile.sh` (needs `FBNEO_REF`). |
| `emu/mame-patches/0002-cps2-wide-v1.patch` | active | MAME 0.288 (submodule, tag mame0288) | cps2.cpp + one mame.lst row | The MAME twin: 164 lines added, exactly ONE removed (sprite tile-code composition, gated on `m_cps2_wide`, a driver member — no reset needed). Gates: `tests/test_mame_parity.sh` (prerequisite, unpatched build) then `tests/test_mame_wide.sh`. |
| `emu/jtcores-patches/0001-cps2w-scaffold.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `cores/cps2w/cfg` only | The SEPARATE core scaffold (14z-106): `game.yaml` VERBATIM from cps2, `CORENAME=JTCPS2W`, the MRA set restricted to the vsav family. **No RTL.** Gate: `tests/test_jtcores_twin.sh`. |
| `emu/jtcores-patches/0002-jtframe-sim-wramdump.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `modules/jtframe/hdl/ver/test.cpp` — the Verilator TESTBENCH, not RTL and not a core file | `JTFRAME_SIM_WRAMDUMP` (14z-107): 64 added lines, all inside `#ifdef _JTFRAME_SIM_WRAMDUMP`, writing `wram/dump_<frame>_<addr>.bin` in CPU byte order at the VS rising edge. Inert unless the macro is defined; the block to dump is fully macro-parameterised so jtframe stays core-agnostic. Gates: `tests/test_sim_wram_contract.sh` (ROM-free, incl. a static proof of the guard) and `tests/test_mister_sim_anchor.sh` (the live oracle). |
| `emu/jtcores-patches/0003-jtframe-sim-sdram-top-address-bit.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `modules/jtframe/hdl/ver/test.cpp` — TESTBENCH | 14z-107 (3): the Verilator SDRAM model decoded only 22 of 23 address bits at `JTFRAME_SDRAM_LARGE`, so the upper 8 MB of every bank aliased. The dropped bit is `addr[22]` riding on `sdram_a[9]`, NOT `addr[9]`. `#ifdef _JTFRAME_SDRAM_LARGE`, so 32 MB-module cores are byte-for-byte unaffected. |
| `emu/jtcores-patches/0004-jtframe-sim-advance-model-time.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `modules/jtframe/hdl/ver/test.cpp` — TESTBENCH | 14z-107 (3): the model's own clock never advanced, so no `#` delay in the design ever fired and `jtsim -verilator -stats` reported nothing. |
| `emu/jtcores-patches/0005-jtframe-sim-sdram-stats-raw.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `modules/jtframe/hdl/sdram/jtframe_sdram_stats_sim.v` — a SIMULATION-ONLY reporter | 14z-107 (3): raw machine-readable per-bank counters; the two existing lines are cumulative and rounded and cannot be differenced per phase. Consumer: `tests/audit_sdram_bank_load.sh`. |
| `emu/jtcores-patches/0006-cps2w-wide-mra-trim.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `cores/cps2w/cfg/mame2mra.toml` + `doc/mame.xml` | **Slice D0** (14z-107 (5)): the `vsavjw` machine entry (tagged `sourcefile="capcom/cps2w.cpp"`, which is the profile gate — the reference core cannot see the set) and the MANDATORY QSound trim, without which the WIDE `.rom` is 70.26 MB and overflows both the 26-bit `ioctl_addr` and the 16-bit header start word. **No RTL.** Gate: `tests/test_mister_mra_map.sh`. |
| `emu/jtcores-patches/0007-cps2w-qsound-width-runtime-gate.patch` | active (pushed — the "LOCAL-ONLY" mark was stale from 14z-107 (6) to 14z-113) | jtcores (MiSTer, fork `vampire-saved`) | `cores/cps2w/hdl` (4 files) + `cfg/game.yaml` + `cfg/mame2mra.toml` + `ver/game` | **Slice D1** (14z-107 (6)) — **THE FIRST RTL COMMIT.** The QSound sample-bank latch widened to 8 bits, gated by `wide_en` decoded at RUNTIME from MRA header byte 41 (ACTIVE LOW; `0xFF` fill = profile off). Two NEW files (`jtcps2w_profile.v`, `jtcps2w_qsnd_bank.v`) and two OVERRIDES of files shared with the reference cores (`jtcps15_sound.v`, `jtcps2_game.v`), which stay BYTE-UNTOUCHED. `PCM_AW` is NOT widened — it cannot be (`jtframe_romrq_bcache.v:74`). Gates: `tests/test_mister_wide_gate.sh` (frozen delta, the profile byte in three copies, two exhaustive Verilator benches, four must-fire controls), `tests/test_jtcores_twin.sh` (the enumerated override set + `git diff` on the reference cores), `tests/test_mister_mra_map.sh` (the bit in both MRAs and both `.rom`s), `tests/test_mister_sim_anchor.sh` (the FPGA superset leg, run on `cps2w`). |

| `emu/jtcores-patches/0008-jtframe-sim-optional-frame-writer.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `modules/jtframe/hdl/ver/test.cpp` — TESTBENCH | 14z-107 (7): `JTFRAME_SIM_NOVIDEO` compiles the per-changed-frame ImageMagick fork out; children reaped (`waitpid(WNOHANG)`) — upstream leaves one zombie per frame and hits `RLIMIT_NPROC`. The lane's default (`--frame-output off`) |
| `emu/jtcores-patches/0009-jtframe-sim-child-must-exit-hard.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `test.cpp` — TESTBENCH, one word | 14z-107 (7): the forked child `_exit(0)`s instead of `exit(0)`; `exit()` fclose()d the inherited `sim_inputs.hex` stream and REWOUND the parent's input script once per fork. The root fix of the "video-sensitive anchor" |
| `emu/jtcores-patches/0010-jtframe-sim-joystick-top-bits.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `test.cpp` — TESTBENCH | 14z-107 (8): `& ~0xf` and `0x3ff` seeds — `SimInputs` treated the `[9:0]` active-low port as 8-bit and held P1's AND P2's buttons 5/6 DOWN on every 6-button core. Plain upstream bug; unconditional |
| `emu/jtcores-patches/0011-cps2w-sdram-placement.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `cores/cps2w/hdl` (`jtcps1_sdram.v`, `jtcps1_prom_we.v` overrides) + `cfg/game.yaml` + `modules/jtframe/hdl/sdram/jtframe_ram1_7slots.v` (NEW, pulled by cps2w's yaml only) | **Slice D2** (14z-107 (9)): the bank-0 re-pack, the group-C GFX redirect, the QSound split on `pcm_addr[23]`, the PCM-high slot. Evidence: the whole-image SDRAM census (`test_mister_sdram_census`) |
| `emu/jtcores-patches/0012-jtframe-sim-sdram-read-probe.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `test.cpp` — TESTBENCH, compiled out unless `JTFRAME_SIM_RDPROBE` | 14z-107 (10): four per-bank byte-window read counters (reads / distinct 128 B blocks = tile codes / first frame / range). Two slots for the windows under test, two for must-fire controls |
| `emu/jtcores-patches/0013-cps2w-obj-promote.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `cores/cps2w/hdl` — `jtcps2w_obj_bank.v` (NEW) + `jtcps2_obj_scan.v`, `jtcps2_obj.v`, `jtcps1_obj_draw.v`, `jtcps1_video.v` overrides (three of them width only) | **Slice D3** (14z-107 (10)): the CPS-2 Turbo promote `{ wide_en & table_y[12], table_y[14:13] }` read AFTER the terminator test; 3-bit bank at every port; `rom0_bank[2]` untied. Exhaustive bench in `tests/rtl/`, gate `test_mister_wide_gate` section 8 |
| `emu/jtcores-patches/0014-jtframe-sim-frame-window.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `test.cpp` — TESTBENCH | 14z-107 (10): `JTFRAME_SIM_VIDEO_FIRST/_LAST/_STRIDE` bound the frame writer (a filmstrip instead of ~3,000 jpgs). Defaults = upstream behaviour |
| `emu/jtcores-patches/0015-cps2w-prg-window.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `cores/cps2w/hdl/jtcps2_main.v` override + `jtcps2_game.v` + `jtcps1_sdram.v` (`SLOT3_AW` 22) | **Slice D4** (14z-107 (10)): `rom_cs \|= wide_en & RnW & (A[23:21]==3'b010)`, `rom_addr` 22 bits, `one_wait` boundary `wide_en ? 4'h6 : 4'h5`. Proven by the D4 probe (0016) |
| `emu/jtcores-patches/0016-cps2w-prg-read-probe.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `jtcps2_main.v` override, compiled out unless `JTCPS2W_PRGPROBE` (sim only) | 14z-107 (11): the 68k program-ROM read probe — an ADDRESS half with no chip select and a DATA half logging RAW vs LATCHED words. Found D5. Gates `test_mister_prg_probe` (contract, ROM-free) + `test_mister_prg_window` (the frozen pair) |
| `emu/jtcores-patches/0017-cps2w-decrypt-range.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `cores/cps2w/hdl/jtcps2_decrypt.v` override (`jtcps2_dec_ctrl` NOT overridden) | **Slice D5** (14z-107 (11)): `rng_eff = wide_en ? { addr_rng[15:10], ~addr_rng[9:0] } : addr_rng` — the CPS-2 key's encrypted-opcode range word is stored COMPLEMENTED and the reference core reads it straight (decrypts to `$F03FFF` where MAME/FBNeo stop at `$0FFFFF`). Profile-gated; a reference-core defect worth reporting upstream. Gate `test_mister_wide_gate` section 9 |
| `emu/jtcores-patches/0018-cps2w-retract-d4-decrypt-claim.patch` | active (comment only) | jtcores (MiSTer, fork `vampire-saved`) | `jtcps2_main.v` override HEADER | 14z-107 (11): retracts D4's "the extension is not decrypted, and that is correct" — no code change |
| `emu/jtcores-patches/0019-cps2w-readme-d0-d5.patch` | active (docs only) | jtcores (MiSTer, fork `vampire-saved`) | `cores/cps2w/README.md` | 14z-109: the core's README brought from "slice D1" to D0-D5 (maintainer-authorised push) |
| `emu/jtcores-patches/0020-jtframe-sim-p2-scriptable.patch` | active | jtcores (MiSTer, fork `vampire-saved`) | `test.cpp` — TESTBENCH | 14z-109: PLAYER 2 scriptable from `sim_inputs.hex` (file bits 12+; older replays byte-identical, frozen sha1 unmoved). Buttons 4/5/6 still refused by `rpl2siminputs.py` |
| `emu/jtcores-patches/0021-cps2w-vsavjw-crcs-14z110.patch` | active (catalogue) | jtcores (MiSTer, fork `vampire-saved`) | `doc/mame.xml` — the `vsavjw` entry's CRCs | 14z-110: the MiSTer TAIL of the donovan-m12 / merged-m7 freeze — jtframe resolves members by CRC32 alone, so every romset re-freeze needs a catalogue commit (`tools/gen_vsavjw_xml.py --check`) |
| `emu/jtcores-patches/0022-cps2w-vsavjw-crcs-m7-mark.patch` | active (catalogue) | jtcores (MiSTer, fork `vampire-saved`) | `doc/mame.xml` | 14z-110: the M7 version-mark glyph members (`vsw.33m/37m`) |
| `emu/jtcores-patches/0023-cps2w-vsavjw-crcs-remap-110b.patch` | active (catalogue) | jtcores (MiSTer, fork `vampire-saved`) | `doc/mame.xml` | 14z-110b: `vm3j.10b` for the 0x51->0x44 remap (merged-m8) |
| `emu/jtcores-patches/0024-cps2w-vsavjw-crcs-99-rootcause-14z111.patch` | active (catalogue) | jtcores (MiSTer, fork `vampire-saved`) | `doc/mame.xml` | 14z-111: the #99 root-cause freeze (merged-m9, M8 mark) — `vm3j.03d/04d/07b/10b`, `vsw.41/42`, `vsw.33m/37m`. Pin `63496069` until 14z-115. The 14z-112 one-zip repackaging needed NO catalogue change (members keyed by name+CRC, not container) |
| `emu/jtcores-patches/0025-cps2w-vsavjw-crcs-wheel-14z115.patch` | active (catalogue) | jtcores (MiSTer, fork `vampire-saved`) | `doc/mame.xml` | 14z-115: the select-wheel separation freeze (merged-m11, M9 mark) — `vm3j.03d/04d/07b/10b`, `vsw.41/42` (record + coords + ring bases) and `vsw.31m/33m/35m/37m` (36 outline tiles + the M9 glyphs). Pin `202fc3e6` until 14z-117 |
| `emu/jtcores-patches/0026-cps2w-vsavjw-crcs-medallion-14z117.patch` | active (catalogue) | jtcores (MiSTer, fork `vampire-saved`) | `doc/mame.xml` | 14z-117: the Pyron-medallion freeze (merged-m12, M10 mark) — `vm3j.10b`, `vsw.41` (the thunk's ten in-place bytes + one glyph coord pair) and `vsw.31m/33m/35m/37m` (the third M10 glyph tile). Pin `80e08111` until 14z-117b |
| `emu/jtcores-patches/0027-cps2w-vsavjw-crcs-random-select-14z117b.patch` | active (catalogue) | jtcores (MiSTer, fork `vampire-saved`) | `doc/mame.xml` | 14z-117b: the random-select freeze (merged-m13, M11 mark) — `vm3j.03d` (the two sites), `vm3j.10b` (the hole-b bodies), `vm3j.04d/07b` + `vsw.41/42` (ext shifts behind the bodies), `vsw.33m/37m` (the M11 glyphs). Pin `f997cfe1` until 14z-119. |
| `emu/jtcores-patches/0028-cps2w-vsavjw-crcs-physics-port-14z119.patch` | active (catalogue) | jtcores (MiSTer, fork `vampire-saved`) | `doc/mame.xml` | 14z-119: the physics-port freeze (merged-m14, M12 mark) — `vm3j.04d` (Donovan's three physics value rows), `vsw.33m/37m` (the M12 glyphs); three CRCs. **The current pin `2bf41090`.** |

The two `0002-cps2-wide-v1` patches (FBNeo + MAME) are ONE profile
expressed twice; their descriptors stay member-for-member identical (one
romset zip feeds both). The jtcores series is a different axis and has
**four strands at 24 patches (registry brought current 14z-113 — it had
stopped at 0007 with D1 marked "LOCAL-ONLY"; the fork has been PUBLIC and
current since 14z-107 (9), pushes standing-authorised 2026-08-24)**: a core
SCAFFOLD (0001); NINE Verilator-testbench changes (0002-0005, 0008-0010,
0012, 0014, 0020 — none of them RTL and none a core file); the MiSTer
profile itself — the MRA layer (0006, slice D0) and the RTL slices D1-D5
(0007, 0011, 0013, 0015, 0017) plus the sim-only D4 probe (0016) and two
comment/README commits (0018, 0019); and the CATALOGUE tail (0021-0024),
one per romset freeze. The MiSTer edition of Rule 1 v2 is specified in
`docs/project/cps2_wide.md` "THE MiSTer EDITION OF THE SAME RULE"; the
slice plan and its per-slice ratification are `docs/project/mister_map.md`
§10.
Deprecation candidates: none. `CPS2_WIDE_CANARY` stays as the B4
positive control.

## Mechanism inventory (generator vocabulary)

Authoritative implementations + section comments:
`tools/gen_donovan_patch.py`. Manifest rows carry per-use rationale.

| Mechanism | What it does | Introduced |
|---|---|---|
| regions / layout_group | oracle-validated source regions placed with preserved spacing where PC-referenced | M2a |
| port_patch | stage-gated byte patch in a ported region (old/new/new_hex_variant) | M2a |
| region_fix (`[[region_fix]]`) | guarded single-byte patch INSIDE an extractor region blob, old-verified against the extracted source bytes, applied after extraction and before relocation — for value-level porting the extraction copies faithfully but the host engine numbers differently (hit-class / node-state renumbers). Flat `off/old_hex/new_hex/note`; `stage=99` = retired-but-documented. 14z-33 (column-KO 0x52->0x06 x3, 0x50->0x0F x6, `hitbox_proj`); the 14z-35 0x51->0x4E cluster is RETIRED at stage 99 (superseded by the `es_type51_dispatch` site_thunk — records carry native 0x51). **Note (14z-110, #99): the 14z-43 consumer audit that retired 14z-35 named dispatchers 1+3 but not dispatcher 2 (`0x018508`), which is why #99's 0x51 node still crashes there; census `tools/audit_fsm_census.py`.** Gate: audit_hex_lengths (static). | 14z-27 |
| site_thunk (+jmp, jmp_ok, id_literal_ok, region_subst, data_subst[+gather], row_subst) | displace a vanilla site into a ghost-clean thunk; substitutions embed placed blocks | M2a; gather 14z-67 |
| obj_hook / state_hook / reaction_hook | union-shaped engine dispatch extensions, tripwired when unported. **obj_hook AMENDED 14z-91: the dispatch SITE is no longer patched.** The 0x2C-byte pool WALKER (`0x54458` / `0x5E52A`) is relocated verbatim into free space with the union table appended at copy+0x2C — `site == walker+0x18` and the dispatch is `movea.l (0x12,PC,D0.w),A0`, so the copy's instruction resolves to the copy's own table by construction — and only the 4-byte OPERAND of each `jsr <walker>` is rewritten (2 + 21 callers, inventory frozen in the manifest and enumerated by form by `tools/audit_walker_callers.py`). The old `jmp thunk` at the site cost cycles on every dispatch (270,991 across the legacy corpus at `0x05E542`) and was the measured cause of the `24_don_winmash` superset-invariant regression. Table is now a `code` op (read pc-relatively). Gates: test_obj_walker_relocation, audit_walker_repoint, audit_walker_ghost | M2a stage 4, amended 14z-91 |
| obj_hook_extra | AUTHORED union rows `{site,index,src}` for types in neither table — lets a tenant reach a SHARED type's rewritten machine without touching the shared row; no-gap assertion (engine indexes by type*4) | 14z-68 |
| type_renumber (generator-automatic, no manifest rows) | multi-tenant only: every NON-FIRST resolver tenant that STAMPS a multi-owner obj_hook type (site 0x5E542, types 114-119) gets its OWN type number — the frozen stamp inventory's immediates are rewritten per tenant copy (TT byte only, full-span old-verified) and the union grows per-tenant entries resolving to that tenant's OWN copy. First resolver keeps originals (maintainer-decided scope). Consumes `build/manifest/type_stamps.toml`; empty at N=1 by construction. Gates: test_type_stamp_census, audit_type_writes, audit_type_dispatch_range, audit_merged_vec3 | 14z-82 |
| init_shim | per-char init interception (flavor latch etc.). **F2 FIXED 14z-82**: on a multi-tenant build ONE merged shim is assembled at engine_here and planted on every DECLARING tenant's dispatch row; chain blocks exit into their OWNER's handler; unmatched id → tripwire; non-declaring tenants (Pyron) stay direct by ratified decision | M2a stage 5; merged 14z-82 |
| sound_table (per-tenant, 14z-85b) | pyr_sfx_records + hui_sfx_records join don's: curated per-node sfx record arrays (keep/zero per docs/project/tables/sfx_records.md), ptr rows 0x11/0x10 repointed, helper unstub idempotent across rows. Fixes the merged music retrigger at its source AND makes solo H/P node sfx audible. Freezes: huitzil-m7 (284e3b1c), pyron-m4 (ac22418f) | M5 14z-52 (don); H/P 14z-85b |
| owner_tag (generator-automatic, no manifest rows) | multi-tenant only, site 0x54470's family (59-75, maintainer option (a)): every frozen stamp site is detoured (6-byte `jsr`) through a per-(tenant, instruction) thunk — `move.b #tenant_id,(0x7F,A4)` then the ORIGINAL stamp CCR-last, `rts` — and obj_hook entries 64-75 become tag stubs (`cmpi.b #id,(0x7F,A6)` per resolver; zero/unclaimed tag → tripwire). Tag byte +0x7F of the $FF9400 slot, measured free 14z-85 (NOT $FFB800 — the 14z-84 census's pool error, retracted). No renumbering, so vs2's embedded 0x5C602 walker and the hit-class map are untouched. Side file `tag_map.json`. Empty at N=1 by construction (solos bit-exact, verified). Gates: audit_pool_free_byte (post-tag mode), audit_type_dispatch_range §4-6, test_tenant_loop §4b, audit_pyron_ring | 14z-85 |
| hitclass_map_extend (site_thunk row, `huitzil.toml` + `pyron.toml`, deduped on the merge) | vsavj's projectile hit sweep maps both colliding objects' type bytes through ONE 64-entry byte map (`0x1A888`, seven `bsr` callers); vs2's has 80. A ported type >= 64 in the `$FF94xx` pool that LANDS A HIT over-indexes it — the f7997 vec3. Body GENERATED (`tools/gen_hitclass_map_thunk.py`): vanilla's 64 bytes verbatim (incl. the 59-63 zeros — Donovan's 61/62 stay vanilla by maintainer decision) + vs2's 16 extension entries + a loud `>= 80` ILLEGAL. **Donovan does NOT declare it** (his 59-63 fit). Gates: `test_hitclass_map_thunk` (reconstruction), `audit_hitclass_map_cost` (cost + both fire censuses). **KEEP — DECIDED 2026-08-16 (maintainer), "at least for now: we have more to lose by dropping it than keeping it".** NOT a deprecation candidate. Measured basis (14z-93): the tenant enters the map 0 times over all 37 hui+pyron rigs, but the same corpus puts **121 objects of type >= 64 into the `$FF9400` pool** (9 distinct types, 64-72, in 22 of 37 rigs) — the gap is CONTACT, not absence, since the sweep is POOL-vs-POOL. Cost of keeping is measured and small (legacy 43/46 bit-identical, 3 transient re-convergent, 230 legacy entries all at indices < 64 so legacy reads vanilla's own bytes). Cost of dropping is a wild jump into map[64] = the `rts` opcode the first time any of those 121 objects collides with another pool object. The crash rig being dead is a RIG failure, not evidence: `pyron/70_pyron_mash` reaches the map 0 times while still spawning 13 type-64/67 objects (section 4). **Revisit only if a pool-vs-pool contact rig is authored and section 3 then measures 0 extension entries** — that, not the dead control, would be evidence. | 14z-82b; ADOPTED 14z-82c; KEEP ratified 14z-93 |
| alloc_wrap | allocation-path wrappers for companion spawns | M2a |
| port_patch x028122 damage work-vars (per-tenant, 6 rows each) | the object-hit damage applier's A5 staging displacements reconciled to the vsavj layout (same-value class #4; b498→b446, b494→b442, b496→b444 at vs2 src 0x28AC2-0x28AF2). Donovan's session-14n throw fix, propagated to H/P 14z-85f after Phobos' FG beam measured 12 combo-counted ticks / ZERO HP (staged into vs2's $FF3494 family, unread on vsavj). The 14x rollback family (-0x4B74/-0x4B72/-0x4B3D) stays at vs2 offsets (ported readers). Freezes: huitzil-m8 (c48cd722), pyron-m5 (65e9a40e). Gates: audit_fg_parity (native A/B staircase), audit_fg_damage (reframed) | don 14n; H/P 14z-85f |
| sound_table / stubbed_sound | per-char sfx record arrays; stub rows keep newcomer ids silent until M5. 14z-85g: sound_table gained `remap_ids` (content-equivalent id rewrite, allowlist-checked) | M2a / 14z-52 |
| sound_stub (recon-row kind, 14z-85g) | a vs2 sound-farm stub whose 0x7xx id has no same-id vsavj meaning but whose SAMPLE CONTENT exists on vsavj under another id: the generator synthesizes the vsavj twin stub (save/id/helper/restore) and resolves the ref to it. First instance: 0x4F2E → sfx 0x199 (the trap-detonation chirp, huitzil-m9). Check content BEFORE stubbed_sound (GOTCHAS) | 14z-85g |
| data_in_code | relocate a data table embedded in crypt-placed code + reroute its reader. **GAP PARTLY CLOSED (14z-69): the census now detects DEFERRED readers of any shape — `(An)+`, `(d16,An)`, `(d8,An,Xn)` — at any distance (`scan_deferred_reader`), and a `shape = "pointer"` row rewrites the 4-byte lea to `bsr.w` a `lea.l #table,An; rts` helper. The generator's ORIGINAL 8-byte `jsr+nop` reroute still requires the reader to follow the lea. For a region's OWN tables the preferred fix is neither: force the region to contain them (`:f<off>` root) and emit that tail RAW** | 14z-66 |
| pcrel_escape_fix | trampoline pad for word-form pcrel branches escaping a region | 14z-66 |
| table_fix | pad + rewrite the ported per-char OBJ bank table | M2b |
| gfx_remap | rewrite main-band tile words by the placement delta (delta-0 tenants skip it) | M2b; delta-0 14z-67 |
| dispatch_keeper | keeper-routed per-char dispatch interception | 14z-6x |
| values / VALUE_SKIP (port_param32) | 32-row value-table rows for variant ids (velocities, jump params) | 14z-66 |
| select_records / select_wheel / win_pal_variant / code_word / aux_poke / data_port / palette | the select/HUD/palette family (per-piece rows) | M2b-M3a |
| **win-screen family** (code_word x2 + win_pal_variant + quote records) | a tenant's victory screen: position table 0x5F200 (CODE rows), palette pool 0x3C2BBC via the OPCODE-view remap table 0x6B2F2, quote records at the `-4`-BIASED bases 0x267426/0x2674A6. **Read `docs/game/engine_internals.md` "The WIN SCREEN subsystem" BEFORE authoring any of these — Donovan's solution (14z-45) was re-derived from scratch for Huitzil (14z-68) and got 2 of 3 pieces wrong.** | 14z-45 / corrected 14z-68m |
| `extra_tiles/<char>.json` | codes merged into the group-C copy inventory that the OBJ-record walk cannot reach (it follows pointers; offset-computed records are invisible). Without them the bank is remapped while the tile is absent and the sprite renders as a SOLID RECTANGLE — the child sidekick's shadow. Guarded by `tests/audit_empty_tiles.sh` | 14z-69o |
| root spec `:f<off>` | force a region past the sibling-oracle boundary and emit from `<off>` as RAW data. For a code region whose OWN pc-relative tables sit beyond the boundary: inside the region the pointers resolve correctly, and raw emission makes the DATA reads return the stored bytes (CPS-2 decrypts opcode fetches only). Forced tail is registered as a dead zone | 14z-69i/j |
| ~~`data_port df_palette_seq_rows`~~ **WITHDRAWN 14z-79 — it overwrote BULLETA's DF block and broke a legacy character; see huitzil.toml "WITHDRAWN 14z-79"** | palette-seq rows 0x1E-0x21 (vsavj 0x39ACC0) <- the sequence native's Dark Force shows (vs2 0x3ABEDC / vh2 0x38BEB0). Legacy-inert ONLY because vanilla never requests those ids — and no RAM gate can check that, so `tests/audit_palette_seq_ids.sh` is the sole guard | 14z-69p |
| `site_thunk index_window_018468` (instance) | engine site 0x018460 -> a 470-byte thunk in hole_a covering the out-of-range window of table 0x018468 (entries 80-83 run vs2's handlers inline; every other index takes the vanilla path; anything else is a defined vec3). Fixes Plasma Trap (82, LOUD) and Reflect Wall (83, SILENT). Owned by Phobos; tenant-id-independent, so the merged build inherits it. Legacy-safe by IMPOSSIBILITY — vanilla reaching 80-83 crashes today. Gate: tests/test_index_window_thunk.sh. Generator: tools/gen_index_window_thunk.py | 14z-79 |
| `extra_tiles/0x10.json` **2 -> 569 tiles** | the 214+P grenade's GROUND detonation drew a solid FUCHSIA rectangle: codes correctly remapped bank 3->4, tiles never copied into group C. Span rule — every tile in 0x0A00-0x0C40 that vs2 bank 3 has art for and group C lacks. **A per-drawn-tile inventory (115) DOES NOT WORK**: `obj_records_dump` reports a multi-tile sprite's BASE code only, and the block is one 6x6 sprite whose other 35 tiles are invisible to it | 14z-70f |
| `:f` on a SOURCE-ONLY (`:s`) root | `extract_char.py`'s `:s` branch returned early and never honoured `f<off>`; the generator already read `raw_from` per region. Now a source-only code region can carry its own pc-relative data tables. Used by `0x88512:0x3b98:s:f0x3b78` | 14z-70d |
| `x088512` grown 0x3B40 -> 0x3B98 | three `lea (d16,pc)` tables at 0x08C08A/9A/A2 sat past the region end and resolved into the ANIM region placed right after. A REAL latent repair — but **behaviourally inert**: the code that reads them never executes in any measured scenario, and the fix changed no rendered pixel. Kept on the gate sweep, not on a behavioural claim | 14z-70d |
| `data_port grab_hold_keyframes` | the grab-victim HOLD placement. The capture positioner writes `victim_pos = attacker_pos ± facing-flipped keyframe (Xoff,Yoff)`, selecting a per-ATTACKER keyframe block via pointer table `0xBE27A[attacker_id]` (H reaches it through his ported clone `0xc9eb0`). vsavj row 0x10 aliased character 0's block (`0x092C4A`), so H held the victim with the wrong offsets (dx −27 vs native +74). Ports H's own vs2 block `0x0C56AA` (len `0x1D80`; orc-identical to vh2 `0x0C4F3C` through `0x1E1A`) into `wide_ext` and repoints row `0xBE2BA`. The exact twin of Donovan's `throw_victim_keyframes`; superset-safe (only the variant row moves). Guarded by `tests/test_hui_grab_victim.sh` (phase-tolerant relative-offset A/B, `matches` peak Δ=0) | 14z-73 |

**Ordering note (14z-70):** the two shipped changes are independent —
`extra_tiles` is gfx-only (the program fingerprint is unchanged by it,
which is itself the evidence) and the `x088512` root change is
program-only and inert.
**Exclusivity / ordering note:** the `:f` root and the raw-emit tail are
one mechanism — landing `:f` alone moves the tables without fixing their
byte image. `extra_tiles` and `data_port df_palette_seq_rows` are
independent of both and of each other.
**Ordering / exclusivity note (14z-73):** `grab_hold_keyframes` is
independent of every other patch — one variant-row repoint plus a placed
block; the program fingerprint changes but no legacy path is touched
(masked-v2 EXACT). It is the twin of `throw_victim_keyframes` on a
different tenant and shares the same `slot_ptr_table` mechanism.

## Named patches and windows — the per-session sections, folded (14z-122)

One registry table, NEWEST FIRST; each row keeps its session tag in the
status cell (this replaces eleven per-session `## 14z-N additions`
sections that had accreted out of order — nothing was dropped, rows moved
whole). Byte detail stays in `patch_notes.md`. The 14z-99 window's four
builds were each BIT-FOR-BIT their rehearsal probe (`build/probe_window`;
the window's build fingerprints live in the bundle rows above and in
HANDOFF's registry row, maintainer "go" 2026-08-20).

| item | status | depends on | exclusivity | notes |
|---|---|---|---|---|
| **DONOVAN'S PHYSICS ROWS PORTED (14z-118 charmap, frozen 14z-119)** — `build/manifest/donovan.toml` `[[tenant]] port_param32 = true`: his `param32_a` / `param32_b` / `jump_params` bank rows carry VS2's walk/jump values (three value ops at `PRG:0x0BD912/0x0BDF0A/0x0BE392`, 17 bytes) instead of the vsavj alias row's (Victor's) | **FROZEN 14z-119** as donovan-m18 / huitzil-m25 / pyron-m19 / merged-m14 (M12); maintainer-ruled 2026-08-29 ("use VS2 parameters and not the shell character's"); **stock twin MOVED** (`38e9cb2c` — the same rows on stock slot 0x0F, six ops, not profile-gated); **FIELD VERDICT GREEN 2026-08-30 (maintainer, MiSTer, 14z-121): "all green"** on bundle `../mister_fieldtest_14z119/` | the existing generator default (`VALUE_SKIP` unless `port_param32`), which Huitzil (14z-66) and Pyron already used — no new vocabulary | byte detail: patch_notes 14z-118 (charmap, 2) + 14z-119; the map `docs/project/tables/chars/donovan.md` reads the rows `byte`; legacy: every masked class PASS on three suites, his tenant rigs move at the FIRST MOVEMENT frame (`$FF8441/42` = the X-velocity word, 2.5 -> 3.0) |
| **Donovan's physics rows PORTED (14z-118)** — `[[tenant]] port_param32 = true` in `donovan.toml`: `param32_a/b` + `jump_params` row 0x13 now carry VS2's values (17 bytes) instead of the vsavj alias row's (Victor's) | **BUILT on probe `build/don_phys_probe` (`7109f835`), validated (legacy ×3 bit-identical, 4 WIDE gates), NOT FROZEN — the next battery (M12)** | gen `VALUE_SKIP` (14z-66 per-tenant opt-in) | none | found by the character-data map; maintainer-ruled 2026-08-29 "VS2 parameters, not the shell character's"; patch_notes 14z-118 (charmap, 2) |
| **RANDOM SELECT includes the tenants (14z-117)** — `random_select_bound` (site `0x020C74`, the wrap bound = 15 + tenants) + `random_select_roster` (site `0x020C80`, the table read + the routine's `rts` + an 18-entry table: 15 vanilla ids + this build's tenants via `roster_subst`) | **FROZEN 14z-117** as donovan-m17 / huitzil-m24 / pyron-m18 / merged-m13 (M11); stock twin unchanged; field-validated on the board 14z-118 (2026-08-29) | `[[site_thunk]]` x2, `profile = "cps2-wide-v1"`, hole b (body B is read as data); declared by all three tenants, ENGINE-SITE emitted once | supersedes the 14z-116 "structurally impossible" record for this item; the confirm path is untouched (vanilla: what is showing is what you get) | patch_notes 14z-117 (2); gate `tests/test_random_select_tenants.sh`; `select_screen.md` "THE WALKER HAS TWO PATHS" |
| **Pyron medallion / P2 sword row collision (14z-116)** — `select_sword_pal_variant_id`'s P2 branch no longer writes palette row `0x1A`; ten bytes inside the existing 126-byte thunk body (`beq`+`adda.w #$60,a1` -> `bne` to the pop/rts + two NOPs), length unchanged so nothing re-allocated | **FROZEN 14z-117** (donovan-m16 / huitzil-m23 / pyron-m17 / merged-m12, M10 mark; `build/m3b_merged19` rebuilt with the mark, `cde712e1` — the 14z-116 candidate `af21bc88` was the same bytes under M9); field-validated on the board 14z-116 | `[[site_thunk]] select_sword_pal_variant_id`, `site = 0x05F9D0`, `only_variant_slot` (variant builds only; the stock twin has no such thunk) | supersedes the 14z-64 FINAL ALLOCATION's documented trade. **Accepted trade:** the P2 select-figure sword draws with Pyron's medallion palette (orange) while P2 hovers a tenant — select screen only, no in-match surface. A partial fix is IMPOSSIBLE (sword and medallion share the row's entries, measured). Do NOT drop the whole thunk: P1's accent (row `0x17`) still needs it | patch_notes 14z-116; gate `tests/test_pyron_medallion_2p.sh` |
| **select-wheel separation (E2)** — the three appended cells repositioned (`wheel_layout_proposed.json` pos + highlight_base), one authored 4x3 outline sprite per cell interleaved before its medallion in the wheel record (`cell_outline` / `outline_base = 0x1F800` / `outline_pal = 0x19`), M9 mark | FROZEN 14z-115 (donovan-m15 / huitzil-m22 / pyron-m16 / merged-m11); stock twin unchanged | `[[select_wheel]]` bank5 (group C) — outlines are rendered from the vs2 medallion tiles by build_gfx | none; the ring look is a placeholder for the maintainer's own mockup, which replaces the outline tiles through the same knobs | patch_notes 14z-115 |
| **CPU AI action-script tables unparked** — bank_map `ai_script_0..3` (`0xBF01A/09A/11A/19A`, data_ptr, `region = "auto"`) + one DATA extra root per tenant (`0x100000:0xE3C` H / `0x100E3C:0xC8E` P / `0x101ACA:0x10CE` D) | **FROZEN 14z-111: donovan-m14 / huitzil-m21 / pyron-m15 / merged-m9 (M8 mark; stock twin unchanged — WIDE-only, `optional = true` rows)** — acceptance `test_inp_crash_merged_m8_01 MODE=clean` PASS; don_vs_cpu / merged_legacy 47/47 / guard corpus 332/332 / stage-4 / m3a all PASS on the shipping images | the bank_map data_ptr pipeline; extractor `region = "auto"` (new); the recon rows below | supersedes NOTHING — the 14z-110 d2 window and 14z-110b remap stay (equivalence-correct; they were never this crash's fix) | THE #99 ROOT CAUSE: the tables are 16 classes + the same 16 repeated, so tenant classes aliased Demitri/class-1/class-3 AI; CPU tenants now run their own vs2 AI. **Gameplay-bearing (1P CPU behaviour) — maintainer-ruled.** Byte detail patch_notes 14z-111; mechanism STATE 14z-111; GitHub #99. |
| **reconciliation_huitzil rows** `0x2CBDE->0x2D3F2`, `0x2CE0A->0x2D5B2`, `0x2CE3E->0x2D5E6`, `0x364A->0x364A` (engine_sub, verified) | FROZEN 14z-111 with the row above (huitzil-m21) | Phobos's jump handler `x02592a` | — | The R1 loop's first fire on the probe: four engine calls tripwired because only his own AI reaches them. Twins measured (byte-identical / identical-modulo-attributed-operands / same-address kernel entry). |
| **the 0x51 -> 0x44 state remap** (six `region_fix` rows on `hitbox`, donovan.toml — the deity nodes' next-state bytes at +0x10E9/+0x1109/+0x1129/+0x1149/+0x1169/+0x1189; byte detail patch_notes "14z-110b") | **SHIPPED 14z-110b: donovan-m13 `ec86330f` / merged-m8 (merged15 `73690f21`) / stock8 `d29fd062`; MAME-validated (merged_legacy 47/47, suite 12/13 identical — only 110_don_arcade_mash moved). FBNeo partial oracle refit RULED, in progress (STATE 14z-110b CLOSE). Tags NOT yet cut.** Maintainer-conditioned GO 2026-08-26 | the `region_fix` mechanism; the 14z-110 d2 window stays as a dead-but-equivalent guard | supersedes the retired 14z-35 0x51->0x4E cluster (stage 99) and the withdrawn 0x51->0x19 ruling (b) — see the `region_fix` row above. A seventh companion row on the ported x022400 copy was landed and RETRACTED the same session (Huitzil's region, parked thunks, never fires). | The residual #99: the STORED state 0x51 over-ran a SECOND 80-vs-84 dispatcher `PRG:0x2384E` the 14z-43 audit missed. 0x44 measured byte-equivalent at all five consumers (three node dispatchers, the stored-state dispatcher, the property lookup) with no collision in either engine's store/constant inventory. **RETRACTED 14z-111 (was "field surfaces closed"): the board crash after Donovan 1P -> Bishamon PERSISTS with this remap loaded (CRC-verified) and reproduces on MAME by hand — the remap is equivalence-correct but is NOT the crash's fix; #99 OPEN.** Victor KO-neutral: unconfirmed either way. vs2's 0x117-flag residual recorded in the manifest comment. |
| **reaction_hook d2-window extension** (the #99 fix — `[reaction_hook]` thunk body, donovan.toml) | **BUILT + SHIPPED 14z-110 (2)-(4): frozen as donovan-m12 / merged-m7 (stock twin moved — not profile-gated); carried into donovan-m13 / merged-m8 (14z-110b).** (Status corrected 14z-111 — the row had said "NOT YET BUILT" through two freezes.) Ruled 2026-08-26 (maintainer, 14z-110), order FIX -> AUDIT -> RE-FREEZE | the existing `reaction_hook` thunk (site `0x018458`, singleton, merged inherits); vs2 dispatcher-2 twin table `0x016DE4` as the verbatim handler source | supersedes ruling (b) of the original #99 remap (`0x51 -> 0x19` — measured UNSAFE: diverges on dispatcher 3 at `0x0185DA[0x19]=0x18694` and breaks the `es_type51_dispatch` `cmpi #0x51`; any copy-aliased value changes `property[class]`, the 14z-44 ES-freeze family) | The thunk's bne-arm — the ONLY entry into dispatcher 2 (`0x018508`), measured: sole `bne.w` at `0x01845C`, inside the hook's site prefix — gains the same `0x50-0x53` window it already runs for dispatcher 1, via a SECOND ext table to vs2 `0x016DE4` handlers verbatim; all other indices `jmp 0x018508` as today. Vanilla dispatcher byte-untouched (#99 ruling (a)); data stays native `0x51` (dispatcher 3 + property path untouched). **DATA-TRIGGERED, not tenant-id-gated** — `0x50-0x53` node bytes exist only in vs2-numbered ported data (vanilla crashes on them today: legacy-safe by impossibility, the `index_window_018468` precedent), and the walking object can be a LEGACY character's (field: Bishamon), so an id-gate would be wrong. Global cost = ~2 compares on the hit-stun path (`+0x38` set), all characters — **the flicker-inventory measurement gates shipping; if it moves, stop-and-escalate**. Gate plan: reconstruct-from-ROMs like `test_index_window_thunk`; `test_fsm_census` stays 6/6 native-`0x51`. Census basis + measurements: STATE 14z-110, GitHub #99. |
| **W1 `oboro_select_hook`** (`[[site_thunk]]` at `PRG:0x020B9C`, all three manifests, deduped, `profile = "cps2-wide-v1"`) | **SHIPPED 14z-105 (donovan-m11 `1de9a027` / huitzil-m20 `24a27940` / pyron-m14 `6bf265ab` / merged-m6 `64426955`; stock twin unchanged `883e7d17`)** | the roster wheel (Bishamon's cell is a vanilla cell; nothing else) | profile-gated; WIDE-only | Bishamon's cell + START held at confirm commits vanilla vsavj's Oboro (0x18) — the Gallon-variant idiom one cell over. Start bit MEASURED (`+0x394` = $8000). +2 ops. Gate `test_oboro_select.sh` (5 legs incl. P2 + stock). Naked-eye tell: the pale colorway. |
| **W2 version string** (`version_text/font/x/y/pal/base` on `[[select_wheel]] roster21`; `build/manifest/version_font.json`; `gfx_tiles.encode`; `wheel_bank5.json "authored"`) | **SHIPPED 14z-105 (same builds)** | select_wheel bank5 (group C must exist — skipped with a note otherwise) | profile-gated by the wheel row | "M6" at screen (340,202), pal row 0x19, codes 0x1FE40+ (since 14z-117 THREE glyphs, "M10", at screen (324,202) — the third glyph would clip at x=340). 0 ops (record/coord data grow). Pixel-exact gate `test_version_string.sh`; codec law `test_gfx_tile_codec.sh`. THE TEXT NAMES THE FREEZE GENERATION — bump it at every freeze. |
| **#109 clone-beam fix** (`beam_effect_class31` code_ptr at `PRG:0x080B28` + root `0x926e4:0x11e:t0x922f0` in the huitzil census) | **SHIPPED 14z-102 (huitzil-m19 `1a7249d6` == the probe bit-for-bit; merged-m5 `393f92a5` == the rehearsal probe bit-for-bit); FIELD-CONFIRMED 2026-08-21 (beams strobe in the maintainer's hands); audit default flipped to EXPECT_LINES=1** | the 14z-71 `beam_effect_class16` pattern (row-16 precedent); the 14z-83 strip band (line tiles pre-relocated); the type-6 takeover (composite/strip compose) | huitzil-only (row 31 dead in vanilla — 0 reads incl. both long marathons, 2418-hit control) | vsavj ships effect-class row 31 as a stub; it is the DF clone-mode per-frame beam emitter (the maintainer's "sound but no beams", GitHub #109). Gate: `audit_clone_beam_lines.sh`, both modes controlled. Gold tint kept (maintainer default ruling 2026-08-21). |
| **#107 reconciliation row flip** (`reconciliation.toml` `0x0448a6`: `0x02563e` plausible-0.94 → `0x04367a` verified) | **SHIPPED 14z-102 (the window freeze: donovan-m10 `c6a02cb0` / stock `883e7d17` / all tenants via the shared map)** | the tie-refusal matcher policy (14z-101); the farm/callsite twin anchoring (STATE 14z-101 + re-derived at the flip, patch_notes 14z-102) | affects every build consuming the shared map (all tenants + stock) | The wrong sibling was a 4-way pattern-tie's first candidate (M2a era). Target COLD on the corpus (H3.2), so no behavioral gate exists yet; the verified content diff is the evidence (6/0x2E, all inside verified operand pairs). |
| **#103 donovan lose-flow fix** (recon_overlay INSIDE `[[tenant]]` + `pcrel_escape_fix` rows `x026142` pad 0x60, `x05c800` pad 0x20) | **SHIPPED 14z-99; FIELD-CONFIRMED 2026-08-20 (no stall any VS2 win/lose incl. Donovan-vs-Lilith), issue CLOSED** | the pcrel_escape_fix machinery (14z-66); the reconciliation overlay scoping (14z-80) | **NOT profile-gated by design** — the stock track carries it too, which is why the stock twin moved (`16da59b6`) for the first time since 14z-91 | A ported pc-rel escape pinned Donovan's WHITE HP to 1 mid-match, so the round judge (sign test) never fired on his death and the arcade lose flow stalled ~8,000 frames (#103). The `x026142` pad is the closer; `x05c800` is the H-proven sibling. Relocations, not additions: net 0 ops. **The staged "top-level recon_overlay" wording was a measured trap** — doc-root placement hijacks every tenant's overlay on a merged build; correct placement is inside `[[tenant]]`. Gates: `audit_don_lilith_ko` EXPECT_STALL=0 + `audit_don_ko_writer` EXPECT_DEFECT=0, both with WEAKEN_P1=1 (fix-verification mode; the natural mash is lottery-bound and mash-Donovan WINS on the fixed build). |
| **#104 capture_kf slot_rows** (15 `data_port` rows per manifest, all three tenants) | **SHIPPED 14z-99; FIELD-CONFIRMED 2026-08-20 (correct sprites under Victor AND Bulleta 6+HP throws, all three tenants — Bulleta unprompted, the option-(a) breadth paying off), issue CLOSED** | the `slot_ptr_table`/data_port family (the grab_hold_keyframes twin, 14z-73) | variant-gated (only variant rows move) | Victor's 6+HP headbutt grab garbled tenant victim poses: the per-victim capture table at the head of every attacker's keyframe block aliases its variant half (the variant-row alias class — NOT an index-space drift; the 14z-98 index-space reading was retracted 14z-99 (2)). Ports every attacker's keyframe block whole from vs2 so tenants hold NATIVE capture records ("all sixteen, not fourteen" — maintainer-ruled option (a)). +32 merged ops. Legacy cost: exactly the f890 class-4 pointer-cache frame, inside the ratified select windows. Gate: `audit_don_grab_pose` EXPECT_MATCH=1 (all three tenants hold native's records 11/26/11). |
| **#105 win_pal colors 8→10** (one value, all three manifests) | **SHIPPED 14z-99; FIELD-CONFIRMED 2026-08-20 (AUTO portraits colored), issue CLOSED** | win_pal_variant (the M2b family) | variant-gated | AUTO mode's winner used palette sets 8/9, which the port did not carry at `colors = 8` — tenant victory portraits rendered as pure white shapes (correct shapes, no fill; ours, not the engine's). `colors = 10` ports the AUTO sets. +6 ops. Gate: `audit_win_pal_auto` EXPECT_WHITE=0 (all four legs COLORED). |
| **#43(b) `ALLOW_FALLBACK=True`** (`tools/find_equiv.py` matcher state — tool behavior, not ROM bytes) | **LANDED 14z-99 W1; #43 CLOSED** | the #43(a) refactor (14z-95: one matcher, parameters pinned) | — | The ruled 3-row movement had decayed to ONE row: `0x028122` → `0x028e42` plausible-0.90, with **ZERO build effect** (no live consumer; the merged patch regenerates bit-identical). Wholesale map regen measured DESTRUCTIVE (drops rows other extracts own) — the row was hand-carried; trap recorded at the row note. Gate: `test_reconcile_matcher.sh` now polices the FLIPPED state. |
| **kernel voice-table port** (`{don,pyr,hui}_kernel_voice_e0-e3` code_word rows ×12 + 16 `[[song]]` pair rows + batch scope `0x730,0x733`) | **FROZEN 14z-96** (GitHub #101, maintainer-ruled option (a); registered `d038553d`/`bfd819a0`/`738bcfc2`, merged-m3 by tag + HANDOFF row) — carried unchanged into the 14z-99 window batch (donovan-m9 / huitzil-m18 / pyron-m12 / merged-m4). ~~BUILT, UNREGISTERED — awaits the freeze~~ (stale until 2026-08-20; the freeze it awaited happened at 14z-96's close) | the qs_songs `[[song]]` machinery (ejection-pilot shape); `only_variant_slot`; the batch's APPEND-ONLY id order | none — 12 variant-half words (rows the base half never reads for legacy) + 16 new Z80 songs at free (base,alias) id pairs | The tenants fired LEGACY kernel voices (Phobos: Bulleta's `0x1d2` = the video-confirmed electrocute grunt; Donovan: Victor's `0x322`) because vsavj's variant halves are byte-copies. Now each fires its own vs2 voice — or vs2's deliberate-silence ids `0x2a1/0x2a2` (free Z80 rows both games). The path calls the REAL `0x4CE2`, so voiced words need live (base,+0x300) pairs — mirrored from native's own twins. Stock twin bit-identical (`a054de5c`). Byte detail: patch_notes 14z-96. |
| **arcade-ladder stage retarget** (`voice_borrow_voicenums_b` `fixes` rows, huitzil + pyron) | **SHIPPED** in `huitzil-m16` / `pyron-m10` / `merged-m2`; FIELD-CONFIRMED 2026-08-18 | each tenant's table-B row at `PRG:0x00BB68 + class*0x40` | none — 8 authored bytes, no vanilla byte moves | `0x18` (REVENGER'S ROOST, vs2's 13th stage) -> `0x0a` (ABARAYA). vsavj ships 12 stages, so `0x18` walked off the banner family into its own `0x00400000` terminator and died at `vec3` (#92). All eight sit opposite candidate class `0x13` — the entries that schedule the Donovan match. Byte detail: patch_notes 14z-94. |
| `gfx_layout3.toml` scatter bounds now ENFORCED | active | `build_gfx_donovan.check_profile_fields` / `check_scatter_bounds` | — | The fields were inert; on first enforcement huitzil's `scatter_hi` failed and was RE-MEASURED `0x06D8` -> `0x0AF5` (#87). Output-inert, proven twice. Bank words and `collision_rule` are now checked against the builder, not obeyed — they are baked into frozen references, so making them settable would be a route to a non-reproducible build. |
| **the pre-commit gate chain** `tests/run_all_static.sh` | active — PASS 96 / SKIP 0 / FAIL 0 at the 14z-100 close (the count grows; read the runner, not this cell) | `tests/ci_portable.txt` + `tests/ci_static.txt` | NOT `run_battery_m2.sh` (that builds a ROM; it is the stage-6 dev chain) | #30. Counts SKIP separately from PASS (#29) and names any emulator-free gate registered in neither list, so the orphan population cannot regrow. Before it, 101 of 130 test scripts had no caller. |

## The beam takeover trio (Huitzil — three mechanisms, 14z-71)

| row | kind | what it does | safety |
|---|---|---|---|
| `beam_effect_class16` | `[[code_ptr]]` | effect-class table row 16 (`PRG:0x080AEC`) -> the ported vs2 row-16 handler family (region `x093460`). vsav ships the row as a stub; the tenant's beam object already selected class 16. | row measured DEAD in vanilla (`tests/audit_effect_class_rows.sh` §1, 0 reads vs a 1760-hit control). Legacy masked-v2 EXACT. |
| `beam_list_type6` | `[[site_thunk]]` (`rts_ok`) | takes over vsav's sprite-list TYPE 6 (`PRG:0x01B6AA`) — believed unused, **measured 14z-89 to be REACHED BY LEGACY TOO** (see evidence) for the type-12 composite the beam needs; body also carries a ported type-4 handler (bank 4 + vs2's code bias) and a child dispatcher. The tenant's 39 composite lists are retyped `000C -> 0006` by `[[port_patch]]` rows. | **THE DEADNESS CLAIM IS FALSE — measured 14z-89, and the fallback held.** types 6/8 measured dead on FOUR replays (0 reads vs 4329/2702/2260/321 controls), but the coverage was the gate's default set: on `21_don_mash` and `26_don_arcade_mash` legacy lists DO reach type 6 (tripwire armed 387 / 948 times, PC-attributed inside the thunk). **The assumption was never load-bearing and that is why nothing broke** — non-tenant lists fall through to vsav's own type-6 code, reproduced instruction-for-instruction, so rendering stayed correct. **RESIDUAL COST REMOVED 14z-91** (maintainer ruling 14z-89 (2)): the `$FF010C` counter write is gone from the fallback (body 0x102 -> 0xFE; the one branch crossing the cut, the `bsr.w` at +0x32, re-derived 0x2C -> 0x28). `audit_effect_class_rows.sh` §4 now watches the fallback's EXECUTION against a frozen per-replay inventory instead of a counter's value — zero legacy RAM perturbation, no mask window — and its default replay set now INCLUDES the two rigs that arm it, which is why the false claim survived ten sessions. Previously: the counter was live work RAM vanilla does not keep, so those two legacy replays could not re-converge with the vanilla basis (both `.pending` on huitzil-m13). Zero legacy cycles otherwise; the legacy flicker inventory is IDENTICAL to baseline run-for-run. |
| `strip_tiles/0x10.json` | gfx (`--strip-tiles`) | copies vs2 **bank-1** strip art into group C bank 4 at `code+0x1000` (span `0x4EA0-0x4FBF`). The type-4 handler composes its own bank, so this art cannot reach group C by the record path. | readback-verified in the builder; `audit_empty_tiles.sh` on the beam replays. Chosen over vsav's own bank 1, measured 160/240 occupied. |

Full rationale and the porting checklist: `porting_sprite_lists.md`.
Facts: `../game/atlas/sprite_lists.md`.

## Tooling

**Release packaging (14z-105 (2)):** `tools/package_release.py` turns a
frozen rompath into `release/<name>/` — xdelta3 deltas against the four
reference dumps' concatenated members (no ROM bytes; rule-7 scan),
`manifest.json`, the verifying `apply_release.py`, README. Deterministic.
`release/merged-m6/` is the shipped 14z-105 package; gate
`tests/test_release_roundtrip.sh` (ci_static). Re-run at every freeze.

`tools/patch_prg.py` applies the JSON op-list in 68k word-value space
(`code` re-encrypts; op-overlap is a NAMED build error since 14z-65).
`tools/pack_build.sh` packs a runnable rompath (WIDE builds merge the
overlay). Verification: crash guard, dual-emulator comparator,
auto-detecting `tests/run_suite.sh` (fingerprint -> expectation set via
`tests/expected/registry.tsv`), and the per-gate ground-truth tests
(CLAUDE.md §4: verdict logic is itself tested).

## DEFERRED BY MAINTAINER DECISION (14z-76) — the win-quote bank relocation

**STATUS 14z-116: FORGONE FOR NOW BY MAINTAINER DECISION (2026-08-28) —
PARKED WITH A CONSTRAINT, AND THE PLAN BELOW IS SUPERSEDED IN THREE PLACES.**
The ruling: forgo for now, document everything, **and if it is ever done, do
it the CLEAN way — not touching vanilla.** That rules the whole-bank
relocation below OUT as an implementation, permanently, rather than merely
ranking it second: it repoints a shared root and moves `RAM:$FFF230` on
legacy win screens. **The sanctioned shape for any future attempt** is the
tenant-only one: the three vs2 blocks + a generated first-level table in
`wide_ext`, ~330 glyph tiles authored into group C **bank 5's** font window
(in-group `0x13800-0x147FF`, measured 4096/4096 BLANK on `m3b_merged18`),
the shipping `winquote_bank_variant_id` gate (14z-62j) already routing the
tenant win-quote drawer to bank 5, and ONE `site_thunk` on the selector for
winner `>= 0x10` — with the vanilla bank, the four-entry region root, tables
A/B and `$FFF230`'s vanilla value all byte-identical. **The one measurement
still owed before that can be scoped:** whether the TEXT object (set up at
`PRG:0x00C840-0x00C862`, fed by `$30(a4)`) takes its bank from the same
field the `0x5F328` gate writes — a different chain — or needs the thunk to
write it. Everything else is measured and in the tree.
 The section
below is kept as written because its eliminations stand; read this header
first. `tools/scan_quote_window.py` RE-DERIVED its central claim and it
HOLDS (zero runs of `0x20`+ free bytes at either hop, so appending in reach
is impossible). What changed:
1. **"change ONE long" is wrong — the root is a FOUR-ENTRY REGION ARRAY**
   and the other three banks are the ENGLISH text. Relocating "the bank"
   means choosing whether the other three languages come too.
2. **The relocation is NOT legacy-invisible.** `move.l a1,$30(a4)` installs
   an absolute bank pointer into `RAM:$FFF230`, measured live at the vanilla
   win screen on replays 23 (`0x00331136`) and 28 (`0x0033101E`). Moving the
   bank perturbs LEGACY work RAM on every win-reaching replay — a superset
   hit by construction, needing a new ratified class per replay for a
   cosmetic. That is the argument against the relocation, and it did not
   exist when this section was written.
3. **The blocker is the GLYPHS, not the text.** 326 of 327 non-pad codes the
   vs2 tenant blocks use render a DIFFERENT character at vsavj's shared font
   base; the glyphs exist in vsavj only in gfx bank 1 (`0x22000-0x2FFFF`),
   unreachable by a 12-bit code, and the bank-0 font window is 4096/4096
   occupied, so no remap target exists. Any version of this fix must move
   ~330 authored glyph TILES, which this section never budgeted.
Full measurements: `../game/engine_internals.md` "The WIN-QUOTE TEXT SYSTEM"
§8; gate `tests/test_win_quote_decode.sh`; tools `decode_win_quotes.py`,
`audit_quote_font.py`, `scan_quote_window.py`.

**Status: NOT STARTED, deliberately. Do this LAST**, on the merged M3b build,
after the mechanical port is complete and certified. Maintainer's ordering,
and it is the correct one — see "why later is cheaper" below.

**What it fixes.** All three tenants render a host character's win quote:
Phobos a Bulleta line, Pyron Demitri's, Donovan Victor's. Cosmetic only; no
gameplay surface. Mechanism fully decoded in
`../game/engine_internals.md` "The WIN-QUOTE TEXT SYSTEM".

**Root cause.** The per-character split is the first-level 16-bit offset table
at the quote bank base. vsavj's table has 32 entries and its variant half is
**exactly aliased** — `0x10->0x00`, `0x11->0x01`, `0x13->0x03`.

**Why the cheap fix is impossible.** The offsets are 16-bit and SIGNED
relative to the bank base, so a ported block must live inside
`0x32D28A +/- 0x8000`. That window was scanned: **zero free bytes** — not one
run of 0x40 bytes of `00`/`FF`. `hole_b` (`0x3EC720`) and `wide_ext`
(`0x400010`) are both far out of reach. Appending in place cannot be done.

**The only path — relocate the whole bank.** Every offset inside the bank is
relative to its base, so moving it wholesale preserves the entire structure:

| step | detail |
|---|---|
| 1 | copy the bank verbatim, **~`0x40DC` bytes** from `PRG:0x32D28A`, into `wide_ext` |
| 2 | append the three tenant blocks (~`0xC20`); total ~`0x4CFC`, inside a `+/-0x8000` window |
| 3 | port from vs2 (`root 0x00F954 -> bank 0x09CA24`): Phobos `0x09FE24` len `0x360`, Pyron `0x0A0184` len `0x460`, Donovan `0x0A05E4` |
| 4 | write the three first-level offsets at `newbase + id*2` for `0x10`/`0x11`/`0x13` |
| 5 | change **ONE long**: the root at `PRG:0x0112BC`. Read as DATA (inside the crypt range but on the data path) — write it in the DATA view |

**THE RISK, and why it is not a variant-row change.** The root long is a
SHARED surface: it reroutes *every* character's quote lookup through our copy.
The safety argument is "the copy is byte-identical for rows `0x00-0x0F`" —
sound, but an argument about a 16KB blob rather than a two-byte repoint. It
therefore needs, at minimum:
- a gate asserting the copied span byte-for-byte against vanilla;
- the legacy suite green (replay 28 reaches a win screen);
- **a gate that LOOKS at a vanilla win screen** — text rendering never
  transits work RAM, so the RAM suite is blind to it for exactly the reason it
  was blind to the effect palette (14z-76). An OBJ-record or pixel A/B of the
  pal-0 objects is the only real check.

**Why later is cheaper, not just safer.**
1. Done now it would be done THREE times, in three single-tenant builds,
   producing three copies of the same bank to reconcile at the merge. After
   M3b it is one copy, one root change, one set of offsets.
2. It is the only shared-surface change in the port. Landing it LAST makes a
   regression a one-commit bisect; landing it early taxes every subsequent
   tenant investigation with "rule out the 16KB blob" first.
3. Nothing decays: every address above is a property of the two ROMs.
   `wide_ext` has `0x1FB0F0` free against a need of ~`0x4CFC` — no reservation
   required.

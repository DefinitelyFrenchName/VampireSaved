# NEXT SESSION — orientation (rewritten at the 14z-145 CLOSE (2), 2026-09-10)

> Rewritten at every session close ([VSP-17]). ROLLOVER: the previous opener
> moves VERBATIM to the top of `NEXT_SESSION_HISTORY.md` — this file holds ONLY
> the live orientation. Session state, not knowledge: facts belong in the docs,
> status in STATE.md.

## NOTHING SHIPPED CHANGED. TWO OPEN-ITEMS LISTS CLOSED, EVERYTHING PUSHED.

M18 (`merged-m18`, `build/m3b_merged26`) is the current freeze and release.
The close now ENDS WITH A PUSH when all is green (CLAUDE.md §5, ruled
2026-09-10); this one did. `git status -sb` says the push state.

Closed this sitting (STATE 14z-145 rows 1-9): the freeze-artifact check reads
`patch_index`'s cells; `PRG:0x028D50`'s three names; the aerial cross-check
(two aerial slot sets, the join corrected); the tenants' forward-jump set;
the Phobos ±1 residue (the defense-curve row); Zabel's `0x18-0x1D` (his
D+button set) and Anakaris's hover (a private jump handler); Bishamon's 5MK
startup (the workbook's). Also: the must-fire census (`test_must_fire_census`,
step one of the machine reader); seven BBX findings verified; R10 read.

## START HERE — what is open

- **The re-hit rule (KNOWLEDGE).** Same-id attack windows land once on SA 5HK
  (`2(2)3`), three times on JE 5HP (`2(5)2(5)2`), four on MO 5HK (one window,
  two id-1 records) — the hit rig's numbers. "A hit id lands once" was tried
  and refuted in one re-freeze. Measure the gate `+0x147` and the recent-hit
  ring's rotation. Until then the per-hit sequences over-count SA 5HK/2HP the
  way the workbook does (visible only in the gauge column).
- **The gauge column has no arbiter** (six cells): a meter-gain rig, the `hit`
  set's shape reading P1's meter across a connecting hit.
- **Anakaris's `J.2K`** need a forward-jump-then-down rig (his neutral jump is
  a hover; D+button there is the hover drop). Two unarbitrated active cells:
  AU 5MP (+9), LE J.HP (−1); AU 5HP startup +2.
- **Step two of the must-fire reader** — ruled in shape, grammar fixed by
  BBX's R10 (STATE "Decisions pending"); waits on BBX's answers to the two
  points the maintainer is raising there (executable controls; the header
  definition "leading comment block"). Ten header-only gates are the debt.
- **Zabel j.LK proximity guard** — its own session (recording first).
- Smaller: `audit_mask_window_ff42a2` deprecated-vs-case-specific;
  `release/merged-m15` never packaged; #112 option (B); the living-docs
  generalisation (ruled, not scheduled); the wider-host re-measure of the
  pull queue's gain.

## TRAPS PAID FOR THIS SITTING — read before the next rig, freeze or count

1. **A Lua `DUMPS` read through a read-watched range IS a watched read**;
   take the dump inside the discarded window (platform gotcha).
2. **"The first new chain after the event" is the JUMP when the recipe jumps
   first**; observe from the button window, and a press token may carry a
   direction (`D4`).
3. **A table-a chain with no terminator walks into a2** (AU `a:0x34`); the
   node map walks a2 first.
4. **Which variant a community row documents is a fact about the ROW** — from
   the corpus's own lists, never from which chain fits ([VSP-166]).
5. **A comment edit to a manifest moves the charmap pages** (manifest hash).
6. **The agent shell's `grep` is ugrep honouring `.gitignore`** — never a
   reproducible count; `command grep` or a tool.
7. **Prior art first, again** ([VSP-155]): Pyron's `0x18` was in his move
   list since 14z-120 and I called it unmeasured for a day.
8. **A derivation rule is refuted by the rig that motivated it** if it
   generalises past the one case — check the rig's other rows before
   re-freezing (the same-id dedup, reverted).

**IF A DOC IS TOUCHED:** the eight `--check`s, exit statuses captured directly,
`${=cmd}` in zsh. **A running script is never edited** ([MSC-54]). **The static
tier is never run beside another gate run in this tree.**

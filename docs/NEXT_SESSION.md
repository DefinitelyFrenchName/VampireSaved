# NEXT SESSION — orientation (rewritten at the 14z-146 CLOSE, 2026-09-10)

> Rewritten at every session close ([VSP-17]). ROLLOVER: the previous opener
> moves VERBATIM to the top of `NEXT_SESSION_HISTORY.md` — this file holds ONLY
> the live orientation. Session state, not knowledge: facts belong in the docs,
> status in STATE.md.

## THE THREE KNOWLEDGE ITEMS CLOSED, IN THE MAINTAINER'S ORDER. NO BUILD BYTE MOVED.

M18 (`merged-m18`, `build/m3b_merged26`) is still the current freeze and
release. `git status -sb` says the push state (the close ends with a push
when all is green).

Closed this sitting (STATE 14z-146): **(1) the re-hit rule** — the victim's
recent-hit slot `+0x6C[attacker block]`, written at contact, CLEARED every
tick the attacker's node has no attack record (`tests/test_rehit_ring.sh`);
the derivation already encoded it, SA's residue is the juggle gate, and the
14z-145 "MO 5HK two id-1 records" was a far/close join slip. **(2) the gauge
column** — the meter steps on a connect: swing 0/3/6 by strength, then the
record's `+0x14` per LANDED hit (`tests/test_meter_gain.sh`); the six cells
are hits-landed residues, two are workbook defects (FE 5MP typo, ZA J.2HK
wrong-strength row). **(3) Anakaris's J.2K** (one chain `a2:0x1e`, the aerial
gate's fourth direction) and the three timing cells (AU 5MP a notation slip,
AU 5HP one high, LE J.HP a landing cut — `test_tick_durations` per node).

## START HERE — what is open

- **Step two of the must-fire reader** — ruled in shape, grammar fixed by
  BBX's R10 (STATE "Decisions pending"); waits on BBX's answers to the two
  points the maintainer is raising there. Ten header-only gates are the debt.
- **Zabel j.LK proximity guard** — its own session (recording first).
- **The community cross-check's remaining residue is all named**: the
  workbook's double-counts (§2), the ±1/+2 conventions with the sheet one
  off on BI 5MK, SA J.MP, LI 2MK, AU 5HP; the gauge and timing cells above.
  Nothing on the page is unarbitrated. The specials/supers/throws (the bulk
  of the workbook) still have no naming rigs on vsavj.
- Smaller: `audit_mask_window_ff42a2` deprecated-vs-case-specific;
  `release/merged-m15` never packaged; #112 option (B); the living-docs
  generalisation (ruled, not scheduled); the wider-host re-measure of the
  pull queue's gain; the `hit` rigs' LP events whiff at contact range (VOID,
  reported) — a closer walk-in if LP cells ever matter.

## TRAPS PAID FOR THIS SITTING — read before the next rig, freeze or count

1. **The hit rig stands at NEAR distance, so it measures the `CL.` variant**
   — join a rig's numbers to the chain the trace ENTERED, never to the move
   name (MO 5HK's "paradox"; LE 5HP's meter law) (project gotcha).
2. **The engine clamps the fighters to 336 px** and travelling moves drag the
   camera: no whiff geometry exists for a dive kick; a whiff leg whose
   victim's HP moved is VOID. Ask first whether the quantity's STEPS separate
   inside the connect leg (game gotcha).
3. **A frame-sampled trace and a write tap agree except on a node's LAST
   sample** (a two-tick frame leaves the node before the clearer runs) —
   state that as the tolerance, never widen it.
4. **A runner invoked by a relative path inside a `cd` subshell runs
   nothing**, and the analyser then reports a missing trace.
5. **A generator's "insert before the next section" lands at the file's tail
   when there is no next section** — check the parse before regenerating.
6. **The workbook writes `N(M)` for a conditional second hit**; summing it
   invented a +6. The reader now takes N, with the measurement as licence.

**IF A DOC IS TOUCHED:** the eight `--check`s, exit statuses captured directly,
`${=cmd}` in zsh. **A running script is never edited** ([MSC-54]). **The static
tier is never run beside another gate run in this tree.**

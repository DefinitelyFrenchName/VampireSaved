# patch_index — one-page registry of every patch

Updated in the same commit as any patch change. Columns: status
(planned/active/deprecated), depends-on, exclusive-with, notes.

| Patch | Status | Depends on | Exclusive with | Notes |
|---|---|---|---|---|
| (null) | active | — | — | `build/manifest/vsavj.toml`: straight member copies, bit-identical vsavj. The baseline every other patch chains onto. |
| donovan-m2 | **M2a FROZEN at `a02aeeff…`; M2b-CORE FROZEN at `71601263…`; M2b+ASSETS FROZEN at `b91647c7…` -> donovan-m2c (2026-08-02, 3-window masked basis)**; dev head == the m2c freeze (stage 6: gfx tile placement + record remap + palette + effect map + select/VS/win-quote records + throw work-vars + Anita bank-2 strip [effect_tail.json bank2_recs, empirical 54-rec closure] + ls_freeze_vs2_{victim,attacker} site_thunks [14z-42: vs2 hit-freeze constants + $147 re-hit gate for Donovan-attributed electric hits — the Lightning Sword hit-count/speed fix; legacy paths byte-identical, char-0x0F + player-block guarded] + es_type51_dispatch thunk + ES record remaps 0x4E->0x06 x7 + 0x51 restored x6 + props_ext_lo [14z-43/44: the full ES chain — 9 hits native-exact, ES-finish death fixed; meter system decoded (stock ff8509, bar 0x90 units)] + win-screen fix [14z-45: pos table entries 0x0F/1F + Jedah's 8 win-palette slices replaced in place with vs2 sets — palette + (-128,+24) shift both native-locked] + state_hook seq-id map fix [14z-46: per-stub ids build-verified vs vs2's table; swordless deity blue + 2 latent live states fixed; 5 dead states = safe no-ops] + accent owner-link venue fallback [14z-47: select post-confirm blink fixed, rows native-exact] + HC motion farm_ports x3 [14z-48: 41236/63214 tables ported — Blizzard Sword, Sword Grapple, Press of Death all functional] + HUD mugshot/name [14z-49: effect_tail places 0x4D62->0x3DC8 (2x2) + 0x4D55->0xBE8C (3x1) + hud_name_entry_0f_hi/lo aux_pokes — stager bases vsavj +0x3800 vs vs2 +0x4200, live-OBJ measured] + select medallion [14z-49: effect_tail 0xB10B->0xB526 (Jedah's ring-verified cell; Gallon's 3x3 untouched) + med_pal_row14_a data_port (select row 14 block A <- vs2 0x3BAFDC)]; open: row-0x0F fixture override port [statue accents — vs2 0x3CB7DC block, STATE 14z-19], table-B 0x38C1D8 slot-0F repoint [alt-color], win-quote palette, small cosmetics; 14z-19 REVERTED a Victor-accent overwrite shipped in fa89812 — palette data_ports are RAM-gate-blind, each needs a static guard [test_don_accent.sh]) (registry row live; suite auto-detects; stages 1-5 closed; playtest 3x clean; sfx silent until M5 — 25 stubbed rows; gfx garbled until M2b) | patch_prg tooling, extract_char oracle, reconciliation.toml | replaces Jedah slot 0x0F | M2 proof-of-life: Donovan into vsavj. Generated by `tools/gen_donovan_patch.py --stage N` from the oracle-validated extraction (`tools/build_donovan.sh N`). ~335K of 336.6K free ROM placed; two engine hooks (extended type-dispatch tables, ghost-clean jmp-thunk topology); R1 map ~120 verified rows. Gates: `tests/test_m2a_stage{1,2,3}*.sh` PASS + the stage-4 triple — `test_m2a_stage4_code.sh` (veto lock, moveset END-clean, masked legacy v2), `test_m2a_stage4_oracle.sh` (vsav2-as-oracle: anchors/neutral-exact/HP-trajectory/comparative bound), `test_m2a_stage4_xemu.sh` (MAME+FBNeo field agreement) — ALL PASS on 67753ee3. Per-stage detail: docs/patch_notes.md. |

## Emulator patches (Rule 1 v2 — profile-gated, ratified per profile version)

These are patches to the EMULATORS, not to the romset, and they live under
a different rule: bounded, declarative, profile-gated, and subject to the
emulator superset invariant. Both emulators must carry the same profile —
dual-emulator agreement means nothing if they implement different hardware.

| Patch | Status | Emulator | Scope | Notes |
|---|---|---|---|---|
| `emu/fbneo-patches/0001-vampire-saved-harness.patch` | active | FBNeo | frontend ONLY (makefile, main.cpp, harness.cpp) | The replay harness: `-hinput/-hout/-hframes/-hdump`, plus `FBNEO_HVIDEO` per-frame framebuffer checksums, `FBNEO_HGFX` gfx-buffer dumps, and the B5b instrument set — `FBNEO_HTAP` (write tap with PC attribution, 68k handler slot 7), `FBNEO_HPOKE` (frame-scheduled pokes) and address-resolved dumps reaching OBJ/palette RAM. Gate: `tests/test_fbneo_instruments.sh`. Probe breakpoints with register capture remain BLOCKED by Rule 1 — `m68000_intf.h` exposes no instruction-level hook (STATE 14z-59e). Deliberately kept separate from 0002 so the trust surfaces stay separable — this one touches no emulation. |
| `emu/fbneo-patches/0002-cps2-wide-v1.patch` | active | FBNeo | 5 CPS-2 driver files | CPS-2 WIDE v1: the `vsavjw` descriptor + ONE widened condition in `cps_obj.cpp` (bit-12 promote) gated on `Cps2Wide`, plus the flag's definition/extern/init/reset. `Cps2Wide` is a file-scope global and MUST be cleared in `DrvExit` or the profile leaks into the next game loaded. Gate: `tests/test_wide_profile.sh` (needs `FBNEO_REF`). |
| `emu/mame-patches/0002-cps2-wide-v1.patch` | active (B5, 2026-08-03) | MAME 0.288 (`emu/mame`, tag `mame0288`) | `src/mame/capcom/cps2.cpp` + one row in `src/mame/mame.lst` | The MAME twin. **164 lines added, exactly ONE removed** — the sprite tile-code composition, gated on `m_cps2_wide`. Additive otherwise: `cps2_wide_map`/`decrypted_opcodes_wide_map` (PRG 6MB, output regs still win by map order), the `cps2wide` machine config, the `vsavjw` ROM descriptor (PRG 6MB / GFX 48MB / QSound 16MB), one `GAME()` row. The flag is a DRIVER MEMBER, not a global — MAME builds a fresh driver object per run, so no `DrvExit`-style reset is needed. Gates: `tests/test_mame_parity.sh` (prerequisite, on the UNPATCHED build) then `tests/test_mame_wide.sh`. |

**Exclusivity:** none of these are exclusive with each other; the two 0002
patches are the SAME profile expressed twice and must be kept
member-for-member identical in their descriptors — one romset zip
(`tools/build_wide_romset.py`) is loaded by both.

**Deprecation candidates:** none. The `CPS2_WIDE_CANARY` env probe in both
0002 patches is test-only and off by default; it is the B4 positive
control and stays until content authoring makes it redundant.

## Tooling: `tools/patch_prg.py`

Applies a JSON op-list to a CPS-2 program set in 68k word-value space
(addresses = analysis/logical byte addresses). `data`/`poke` write raw;
`code` re-encrypts with the set's key so opcode fetches decrypt correctly
(verified against MAME, `tests/test_patch_prg.sh`). Null patch is
bit-identical (superset-invariant guard). `tools/pack_build.sh` packs the
output into a runnable rompath dir (use `-rompath "<dir>;$ROMDIR"`).

No behavioral patches exist yet. First real entries expected at M2
(Donovan-replaces-slot proof of life).

## Tooling: verification harness (M2a C0, 2026-07-25)

Crash guard (`tests/lua/replay_guard.lua`), dual-emulator field comparator
(`tools/compare_fields.py` + `tests/fields_m2a.tsv`), auto-detecting suite
runner (`tests/run_suite.sh` + `tools/build_fingerprint.py` +
`tests/expected/registry.tsv` + `tools/check_diverge.py`). All verdict logic
ground-truth tested (`tests/test_crash_guard.sh`,
`tests/test_compare_fields_selfcheck.sh`, `tests/test_suite_dispatch.sh`)
per the CLAUDE.md §4 doctrine. Patched-build suite runs:
`MAME_ROMPATH="<packed>;$ROMDIR" tests/run_suite.sh` — needs a registry row
(frozen expectation set) for the build's program-image fingerprint.

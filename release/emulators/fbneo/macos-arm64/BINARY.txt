fbneo — FBNeo (SDL2 frontend) carrying the CPS-2 WIDE v1 driver patch, prebuilt for macos-arm64
===============================================================================================

# Every file in this directory, and its sha256 — verify before running:
#   awk '/^sha256 /{print $2"  "$3}' BINARY.txt | shasum -a 256 -c     (macOS, or any host with perl)
#   awk '/^sha256 /{print $2"  "$3}' BINARY.txt | sha256sum -c        (Linux, MSYS2)
sha256 6b0410be54a2351393fd7863f16bf5a646aa4b2eb9148424dd951c8e6d5f54af fbneo
sha256 7a757810321233cd96e4a40f7e3a97ec9eff6b4369c417b75f41f9f75f84ef36 libSDL2-2.0.0.dylib
sha256 b15a4124b6e0abd76f3ff2888d65f91a4265af310ca0f6574e4bfe1b02009dc5 libSDL3.0.dylib
sha256 843cd71ee24c30ed25559b6eb6a80fa4fa224468beb32b77b6ab2b2a343fc1a5 libSDL3.dylib

upstream   https://github.com/finalburnneo/FBNeo
pin        79188379cc8442c54712acbe3b7e73dce157985f
patch      0002-cps2-wide-v1.patch  sha1 17cd7516a25e6198376bc69b88b3392d85122d0e  and 0003-drop-sdl2-image.patch  sha1 a18ea29e0f31fa0b837abdf912c3f8b92489f127  (the ONLY patches applied; the project's replay-harness patch is a test instrument and is not in this binary. 0003 drops the SDL2_image link, which removes 21 transitive image codecs from this directory — 24 files to 4 — and with them 20 of the macOS Gatekeeper approvals a downloaded copy needs)
recipe     git clone https://github.com/finalburnneo/FBNeo fbneo && cd fbneo && git checkout 79188379cc8442c54712acbe3b7e73dce157985f && git apply 0002-cps2-wide-v1.patch && git apply 0003-drop-sdl2-image.patch && make sdl2 SKIPDEPEND=1 -j12 -k; make sdl2 SKIPDEPEND=1 -j12   (twice on a fresh clone: the first parallel pass dies on burn.o until the driver list is generated)
built      2026-09-20 on macOS 26.6.2 (arm64), Apple clang version 21.0.0 (clang-2100.3.34.2), by tools/build_release_emulators.sh fbneo
tree       4ccf4d32fbf6 (with uncommitted changes to the build scripts or patches)
jobs       12
env        macOS 26.6.2 arm64; Command Line Tools 27.0.0.0.1788430756
env        brew sdl2-compat 2.32.70
env        brew sdl3 3.4.12
env        brew sdl2_image 2.8.12_1
env        brew pkgconf 3.0.5
requires   macOS 26.0 or later, arm64. Nothing to install: the libraries the recipe links from Homebrew (SDL2 (sdl2-compat over SDL3 where the host uses it), SDL2_image and their image codecs) are bundled here with @loader_path install names, all ad-hoc signed.
run        run it from a directory that has a `roms/` subdirectory holding your built vsavjw.zip — that ONE file, since the release set is standalone (FBNeo has no rom-path option: it reads `roms/` relative to the current directory, or the paths set in its config): `./fbneo vsavjw`. Controls: FBNeo's own menu (Tab).
gatekeeper the binary is ad-hoc signed, NOT notarized, so macOS BLOCKS IT ON FIRST LAUNCH: a dialog saying the program Not Opened, Apple could not verify it is free of malware, offering only Done and Move to Bin. RIGHT-CLICK > OPEN DOES NOT GET PAST THIS on current macOS (confirmed 2026-09-20 on both binaries; the older advice to do that was wrong and is withdrawn). WHAT WORKS, measured: clear the quarantine flag and launch again, via the shipped PLAY.command or by running: xattr -dr com.apple.quarantine . inside this directory. System Settings > Privacy and Security > Open Anyway also works but is PER BLOCKED FILE, so it costs one approval per file in this directory. Notarization would remove the question entirely and needs a paid Apple Developer account - see GitHub #144.
not works  "Unknown system: vsavjw" means the binary is not this one (no driver patch). A stock emulator fed the set renamed to vsavj.zip STALLS on the legal screen forever (measured 2026-09-11) — renaming is never the fix.
provenance a prebuilt is the recipe above run on one host and nothing else; the reviewable trust surface stays the patch (its sha1 above). Rebuild from the recipe to check: the binary is rebuildable, not byte-reproducible (link order, timestamps).
